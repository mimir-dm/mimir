# Code Index

> Generated: 2026-03-13T02:42:24Z | 558 files | JavaScript, Python, Rust, TypeScript

## Project Structure

```
├── crates/
│   ├── mimir/
│   │   ├── build.rs
│   │   ├── frontend/
│   │   │   ├── __tests__/
│   │   │   │   ├── components/
│   │   │   │   │   ├── campaigns/
│   │   │   │   │   │   ├── CampaignSourcesModal.test.ts
│   │   │   │   │   │   ├── CharacterCard.test.ts
│   │   │   │   │   │   ├── NPCsTab.test.ts
│   │   │   │   │   │   └── PCsTab.test.ts
│   │   │   │   │   ├── modules/
│   │   │   │   │   │   ├── CreateModuleModal.test.ts
│   │   │   │   │   │   ├── ModuleDocumentsPanel.test.ts
│   │   │   │   │   │   ├── ModuleMaps.test.ts
│   │   │   │   │   │   ├── ModuleMonsters.test.ts
│   │   │   │   │   │   ├── ModuleNPCs.test.ts
│   │   │   │   │   │   └── ModuleUserDocuments.test.ts
│   │   │   │   │   └── sheet/
│   │   │   │   │       ├── CharacterDetailsTab.test.ts
│   │   │   │   │       ├── CharacterStatsTab.test.ts
│   │   │   │   │       ├── EquipmentSection.test.ts
│   │   │   │   │       └── SpellsSection.test.ts
│   │   │   │   ├── composables/
│   │   │   │   │   ├── useCatalogSearch.test.ts
│   │   │   │   │   ├── useDebouncedSearch.test.ts
│   │   │   │   │   ├── useLightSources.test.ts
│   │   │   │   │   ├── usePlayerDisplay.test.ts
│   │   │   │   │   ├── usePlayerDisplayEvents.test.ts
│   │   │   │   │   ├── usePlayerViewport.test.ts
│   │   │   │   │   ├── useSpells.test.ts
│   │   │   │   │   ├── useTokenVision.test.ts
│   │   │   │   │   └── useVisionCalculation.test.ts
│   │   │   │   ├── fixtures/
│   │   │   │   │   ├── backgrounds.ts
│   │   │   │   │   ├── characters.ts
│   │   │   │   │   ├── classFeatures.ts
│   │   │   │   │   ├── classes.ts
│   │   │   │   │   ├── homebrew.ts
│   │   │   │   │   ├── index.ts
│   │   │   │   │   ├── items.ts
│   │   │   │   │   ├── monsters.ts
│   │   │   │   │   ├── races.ts
│   │   │   │   │   ├── spells.ts
│   │   │   │   │   ├── subclassFeatures.ts
│   │   │   │   │   └── subclasses.ts
│   │   │   │   ├── formatters/
│   │   │   │   │   ├── itemFormatting.test.ts
│   │   │   │   │   ├── modalFormatters.test.ts
│   │   │   │   │   ├── monsterFormatterEnhanced.test.ts
│   │   │   │   │   ├── monsterFormatting.test.ts
│   │   │   │   │   └── spellFormatting.test.ts
│   │   │   │   ├── helpers/
│   │   │   │   │   ├── __tests__/
│   │   │   │   │   │   ├── fixtures.test.ts
│   │   │   │   │   │   └── mockInvoke.test.ts
│   │   │   │   │   ├── fixtures.ts
│   │   │   │   │   ├── index.ts
│   │   │   │   │   ├── mockInvoke.ts
│   │   │   │   │   └── mountHelpers.ts
│   │   │   │   ├── homebrew/
│   │   │   │   │   ├── homebrewCrud.test.ts
│   │   │   │   │   └── homebrewRendering.test.ts
│   │   │   │   ├── integration/
│   │   │   │   │   └── CharacterSheetIntegration.test.ts
│   │   │   │   ├── services/
│   │   │   │   │   ├── DocumentService.test.ts
│   │   │   │   │   └── ModuleService.test.ts
│   │   │   │   └── utils/
│   │   │   │       ├── contentRenderer.test.ts
│   │   │   │       ├── textFormatting.test.ts
│   │   │   │       └── tokenSizes.test.ts
│   │   │   ├── env.d.ts
│   │   │   ├── postcss.config.js
│   │   │   ├── src/
│   │   │   │   ├── app/
│   │   │   │   │   ├── main.ts
│   │   │   │   │   └── router/
│   │   │   │   │       └── index.ts
│   │   │   │   ├── components/
│   │   │   │   │   ├── characters/
│   │   │   │   │   │   └── index.ts
│   │   │   │   │   ├── print/
│   │   │   │   │   │   └── index.ts
│   │   │   │   │   └── shared/
│   │   │   │   │       └── __tests__/
│   │   │   │   │           └── AppModal.test.ts
│   │   │   │   ├── composables/
│   │   │   │   │   ├── map/
│   │   │   │   │   │   ├── __tests__/
│   │   │   │   │   │   │   ├── useFog.test.ts
│   │   │   │   │   │   │   ├── useLightSources.test.ts
│   │   │   │   │   │   │   ├── useMapMarkers.test.ts
│   │   │   │   │   │   │   ├── usePlayerDisplayEvents.test.ts
│   │   │   │   │   │   │   ├── usePlayerViewport.test.ts
│   │   │   │   │   │   │   ├── useVisibilityPolygon.test.ts
│   │   │   │   │   │   │   └── useVisionCalculation.test.ts
│   │   │   │   │   │   ├── useEntityDragDrop.ts
│   │   │   │   │   │   ├── useFog.ts
│   │   │   │   │   │   ├── useLightSources.ts
│   │   │   │   │   │   ├── useMapMarkers.ts
│   │   │   │   │   │   ├── usePlayerDisplayEvents.ts
│   │   │   │   │   │   ├── usePlayerViewport.ts
│   │   │   │   │   │   ├── useTokenDrag.ts
│   │   │   │   │   │   ├── useTokenVision.ts
│   │   │   │   │   │   ├── useTokens.ts
│   │   │   │   │   │   ├── useUvttMap.ts
│   │   │   │   │   │   ├── useVisibilityPolygon.ts
│   │   │   │   │   │   └── useVisionCalculation.ts
│   │   │   │   │   ├── useApiCall.ts
│   │   │   │   │   ├── useClipboard.ts
│   │   │   │   │   ├── useDashboardLink.ts
│   │   │   │   │   ├── useDataEvents.ts
│   │   │   │   │   ├── useDebouncedSearch.ts
│   │   │   │   │   ├── useDevTools.ts
│   │   │   │   │   ├── usePagination.ts
│   │   │   │   │   ├── useWeaponRegistry.ts
│   │   │   │   │   └── windows/
│   │   │   │   │       ├── __tests__/
│   │   │   │   │       │   └── usePlayerDisplay.test.ts
│   │   │   │   │       ├── useDmMapWindow.ts
│   │   │   │   │       └── usePlayerDisplay.ts
│   │   │   │   ├── constants/
│   │   │   │   │   ├── defaultSystemPrompt.ts
│   │   │   │   │   └── tokenSizes.ts
│   │   │   │   ├── dm-map.ts
│   │   │   │   ├── features/
│   │   │   │   │   ├── campaigns/
│   │   │   │   │   │   ├── components/
│   │   │   │   │   │   │   └── dashboard/
│   │   │   │   │   │   │       └── modules/
│   │   │   │   │   │   │           └── index.ts
│   │   │   │   │   │   └── composables/
│   │   │   │   │   │       ├── useDashboardState.ts
│   │   │   │   │   │       └── useModalsState.ts
│   │   │   │   │   ├── characters/
│   │   │   │   │   │   └── composables/
│   │   │   │   │   │       ├── __tests__/
│   │   │   │   │   │       │   ├── useFeatureSelection.test.ts
│   │   │   │   │   │       │   ├── useLevelUp.test.ts
│   │   │   │   │   │       │   └── useSpellManagement.test.ts
│   │   │   │   │   │       ├── useFeatureSelection.ts
│   │   │   │   │   │       ├── useLevelUp.ts
│   │   │   │   │   │       └── useSpellManagement.ts
│   │   │   │   │   ├── modules/
│   │   │   │   │   │   └── composables/
│   │   │   │   │   │       ├── useModuleMaps.ts
│   │   │   │   │   │       ├── useModuleMonsters.ts
│   │   │   │   │   │       └── usePlayNotes.ts
│   │   │   │   │   └── sources/
│   │   │   │   │       ├── composables/
│   │   │   │   │       │   ├── catalog/
│   │   │   │   │       │   │   ├── index.ts
│   │   │   │   │       │   │   ├── useActions.ts
│   │   │   │   │       │   │   ├── useBackgrounds.ts
│   │   │   │   │       │   │   ├── useCatalogSearch.ts
│   │   │   │   │       │   │   ├── useClasses.ts
│   │   │   │   │       │   │   ├── useConditions.ts
│   │   │   │   │       │   │   ├── useCults.ts
│   │   │   │   │       │   │   ├── useDeities.ts
│   │   │   │   │       │   │   ├── useFeats.ts
│   │   │   │   │       │   │   ├── useItems.ts
│   │   │   │   │       │   │   ├── useLanguages.ts
│   │   │   │   │       │   │   ├── useMonsters.ts
│   │   │   │   │       │   │   ├── useObjects.ts
│   │   │   │   │       │   │   ├── useOptionalFeatures.ts
│   │   │   │   │       │   │   ├── usePsionics.ts
│   │   │   │   │       │   │   ├── useRaces.ts
│   │   │   │   │       │   │   ├── useRewards.ts
│   │   │   │   │       │   │   ├── useSpells.ts
│   │   │   │   │       │   │   ├── useTables.ts
│   │   │   │   │       │   │   ├── useTraps.ts
│   │   │   │   │       │   │   ├── useVariantRules.ts
│   │   │   │   │       │   │   └── useVehicles.ts
│   │   │   │   │       │   ├── useBookContent.ts
│   │   │   │   │       │   ├── useBookLibrary.ts
│   │   │   │   │       │   ├── useBookNavigation.ts
│   │   │   │   │       │   ├── useCrossReferences.ts
│   │   │   │   │       │   ├── useImageLoader.ts
│   │   │   │   │       │   ├── useSearch.ts
│   │   │   │   │       │   └── useSourceSearch.ts
│   │   │   │   │       ├── formatters/
│   │   │   │   │       │   ├── actionFormatter.ts
│   │   │   │   │       │   ├── backgroundFormatter.ts
│   │   │   │   │       │   ├── classFormatterEnhanced.ts
│   │   │   │   │       │   ├── conditionFormatter.ts
│   │   │   │   │       │   ├── cultFormatter.ts
│   │   │   │   │       │   ├── deityFormatter.ts
│   │   │   │   │       │   ├── featFormatter.ts
│   │   │   │   │       │   ├── itemFormatterEnhanced.ts
│   │   │   │   │       │   ├── languageFormatter.ts
│   │   │   │   │       │   ├── modalFormatters.ts
│   │   │   │   │       │   ├── monsterFormatterEnhanced.ts
│   │   │   │   │       │   ├── objectFormatter.ts
│   │   │   │   │       │   ├── optionalFeatureFormatter.ts
│   │   │   │   │       │   ├── psionicFormatter.ts
│   │   │   │   │       │   ├── raceFormatter.ts
│   │   │   │   │       │   ├── rewardFormatter.ts
│   │   │   │   │       │   ├── spellFormatterEnhanced.ts
│   │   │   │   │       │   ├── tableFormatter.ts
│   │   │   │   │       │   ├── trapFormatter.ts
│   │   │   │   │       │   ├── variantRuleFormatter.ts
│   │   │   │   │       │   └── vehicleFormatter.ts
│   │   │   │   │       ├── services/
│   │   │   │   │       │   └── SearchService.ts
│   │   │   │   │       └── utils/
│   │   │   │   │           ├── renderers/
│   │   │   │   │           │   └── contentRenderer.ts
│   │   │   │   │           └── textFormatting.ts
│   │   │   │   ├── player-display.ts
│   │   │   │   ├── services/
│   │   │   │   │   ├── DocumentService.ts
│   │   │   │   │   ├── HomebrewMonsterService.ts
│   │   │   │   │   ├── HomebrewService.ts
│   │   │   │   │   ├── HomebrewSpellService.ts
│   │   │   │   │   ├── ModuleService.ts
│   │   │   │   │   ├── PrintService.ts
│   │   │   │   │   ├── __tests__/
│   │   │   │   │   │   ├── PrintService.test.ts
│   │   │   │   │   │   └── createHomebrewService.test.ts
│   │   │   │   │   └── createHomebrewService.ts
│   │   │   │   ├── shared/
│   │   │   │   │   └── components/
│   │   │   │   │       ├── catalog/
│   │   │   │   │       │   ├── __tests__/
│   │   │   │   │       │   │   └── CatalogTable.test.ts
│   │   │   │   │       │   └── config/
│   │   │   │   │       │       ├── actionConfig.ts
│   │   │   │   │       │       ├── backgroundConfig.ts
│   │   │   │   │       │       ├── classConfig.ts
│   │   │   │   │       │       ├── conditionConfig.ts
│   │   │   │   │       │       ├── cultConfig.ts
│   │   │   │   │       │       ├── deityConfig.ts
│   │   │   │   │       │       ├── featConfig.ts
│   │   │   │   │       │       ├── itemConfig.ts
│   │   │   │   │       │       ├── languageConfig.ts
│   │   │   │   │       │       ├── monsterConfig.ts
│   │   │   │   │       │       ├── objectConfig.ts
│   │   │   │   │       │       ├── optionalFeatureConfig.ts
│   │   │   │   │       │       ├── psionicConfig.ts
│   │   │   │   │       │       ├── raceConfig.ts
│   │   │   │   │       │       ├── rewardConfig.ts
│   │   │   │   │       │       ├── spellConfig.ts
│   │   │   │   │       │       ├── trapConfig.ts
│   │   │   │   │       │       ├── types.ts
│   │   │   │   │       │       ├── variantRuleConfig.ts
│   │   │   │   │       │       └── vehicleConfig.ts
│   │   │   │   │       └── ui/
│   │   │   │   │           └── __tests__/
│   │   │   │   │               ├── EmptyState.test.ts
│   │   │   │   │               ├── LoadingSpinner.test.ts
│   │   │   │   │               ├── MultiSelectFilter.test.ts
│   │   │   │   │               └── ThemeSelector.test.ts
│   │   │   │   ├── sources-main.ts
│   │   │   │   ├── stores/
│   │   │   │   │   ├── __tests__/
│   │   │   │   │   │   └── theme.test.ts
│   │   │   │   │   ├── campaigns.ts
│   │   │   │   │   ├── characters.ts
│   │   │   │   │   └── theme.ts
│   │   │   │   ├── test/
│   │   │   │   │   ├── setup.ts
│   │   │   │   │   ├── test-env.d.ts
│   │   │   │   │   └── utils/
│   │   │   │   │       └── mockTauri.ts
│   │   │   │   ├── types/
│   │   │   │   │   ├── api.ts
│   │   │   │   │   ├── book.ts
│   │   │   │   │   ├── character.ts
│   │   │   │   │   ├── content.ts
│   │   │   │   │   ├── domain.ts
│   │   │   │   │   ├── generated/
│   │   │   │   │   │   ├── Campaign.ts
│   │   │   │   │   │   ├── CampaignHomebrewItem.ts
│   │   │   │   │   │   ├── CampaignHomebrewMonster.ts
│   │   │   │   │   │   ├── CampaignHomebrewSpell.ts
│   │   │   │   │   │   ├── Character.ts
│   │   │   │   │   │   ├── CharacterClass.ts
│   │   │   │   │   │   ├── CharacterInventory.ts
│   │   │   │   │   │   ├── CharacterProficiency.ts
│   │   │   │   │   │   ├── CharacterResponse.ts
│   │   │   │   │   │   ├── Document.ts
│   │   │   │   │   │   ├── Map.ts
│   │   │   │   │   │   ├── Module.ts
│   │   │   │   │   │   └── index.ts
│   │   │   │   │   ├── index.ts
│   │   │   │   │   └── reference.ts
│   │   │   │   └── utils/
│   │   │   │       ├── __tests__/
│   │   │   │       │   └── characterUtils.test.ts
│   │   │   │       ├── api.ts
│   │   │   │       ├── characterUtils.ts
│   │   │   │       ├── dataEvents.ts
│   │   │   │       ├── debounce.ts
│   │   │   │       ├── debug.ts
│   │   │   │       ├── formatters.ts
│   │   │   │       └── windows.ts
│   │   │   ├── tailwind.config.js
│   │   │   ├── vite.config.ts
│   │   │   └── vitest.config.ts
│   │   └── src/
│   │       ├── commands/
│   │       │   ├── archive.rs
│   │       │   ├── asset.rs
│   │       │   ├── campaign.rs
│   │       │   ├── catalog/
│   │       │   │   ├── characters.rs
│   │       │   │   ├── helpers.rs
│   │       │   │   ├── items.rs
│   │       │   │   ├── level_up.rs
│   │       │   │   ├── mod.rs
│   │       │   │   ├── monsters.rs
│   │       │   │   ├── other.rs
│   │       │   │   ├── spells.rs
│   │       │   │   └── world.rs
│   │       │   ├── character.rs
│   │       │   ├── dev.rs
│   │       │   ├── dm_map.rs
│   │       │   ├── document.rs
│   │       │   ├── homebrew.rs
│   │       │   ├── homebrew_monster.rs
│   │       │   ├── homebrew_spell.rs
│   │       │   ├── map/
│   │       │   │   ├── crud.rs
│   │       │   │   ├── fog.rs
│   │       │   │   ├── light.rs
│   │       │   │   ├── mod.rs
│   │       │   │   ├── pois.rs
│   │       │   │   ├── traps.rs
│   │       │   │   └── uvtt.rs
│   │       │   ├── mod.rs
│   │       │   ├── module.rs
│   │       │   ├── player_display.rs
│   │       │   ├── print/
│   │       │   │   ├── character.rs
│   │       │   │   ├── document.rs
│   │       │   │   ├── helpers.rs
│   │       │   │   ├── map.rs
│   │       │   │   ├── mod.rs
│   │       │   │   ├── monster.rs
│   │       │   │   └── trap.rs
│   │       │   └── source.rs
│   │       ├── error.rs
│   │       ├── lib.rs
│   │       ├── main.rs
│   │       └── state.rs
│   ├── mimir-core/
│   │   ├── bindings/
│   │   │   └── bindings/
│   │   │       ├── Campaign.ts
│   │   │       ├── CampaignHomebrewItem.ts
│   │   │       ├── CampaignHomebrewMonster.ts
│   │   │       ├── CampaignHomebrewSpell.ts
│   │   │       ├── Character.ts
│   │   │       ├── CharacterClass.ts
│   │   │       ├── CharacterInventory.ts
│   │   │       ├── CharacterProficiency.ts
│   │   │       ├── CharacterResponse.ts
│   │   │       ├── Document.ts
│   │   │       ├── Map.ts
│   │   │       └── Module.ts
│   │   ├── schema/
│   │   │   └── bundle-schemas.mjs
│   │   ├── src/
│   │   │   ├── catalog/
│   │   │   │   ├── action.rs
│   │   │   │   ├── background.rs
│   │   │   │   ├── class.rs
│   │   │   │   ├── condition.rs
│   │   │   │   ├── cult.rs
│   │   │   │   ├── deity.rs
│   │   │   │   ├── feat.rs
│   │   │   │   ├── item.rs
│   │   │   │   ├── language.rs
│   │   │   │   ├── mod.rs
│   │   │   │   ├── monster.rs
│   │   │   │   ├── object.rs
│   │   │   │   ├── optionalfeature.rs
│   │   │   │   ├── psionic.rs
│   │   │   │   ├── race.rs
│   │   │   │   ├── reward.rs
│   │   │   │   ├── spell.rs
│   │   │   │   ├── table.rs
│   │   │   │   ├── trap.rs
│   │   │   │   ├── types.rs
│   │   │   │   ├── variantrule.rs
│   │   │   │   └── vehicle.rs
│   │   │   ├── dal/
│   │   │   │   ├── campaign/
│   │   │   │   │   ├── campaign.rs
│   │   │   │   │   ├── campaign_asset.rs
│   │   │   │   │   ├── campaign_homebrew_item.rs
│   │   │   │   │   ├── campaign_homebrew_monster.rs
│   │   │   │   │   ├── campaign_homebrew_spell.rs
│   │   │   │   │   ├── campaign_source.rs
│   │   │   │   │   ├── character.rs
│   │   │   │   │   ├── character_class.rs
│   │   │   │   │   ├── character_feat.rs
│   │   │   │   │   ├── character_feature.rs
│   │   │   │   │   ├── character_inventory.rs
│   │   │   │   │   ├── character_proficiency.rs
│   │   │   │   │   ├── character_source.rs
│   │   │   │   │   ├── character_spell.rs
│   │   │   │   │   ├── document.rs
│   │   │   │   │   ├── fog.rs
│   │   │   │   │   ├── homebrew_crud.rs
│   │   │   │   │   ├── light_source.rs
│   │   │   │   │   ├── map.rs
│   │   │   │   │   ├── map_poi.rs
│   │   │   │   │   ├── map_trap.rs
│   │   │   │   │   ├── mod.rs
│   │   │   │   │   ├── module.rs
│   │   │   │   │   ├── module_monster.rs
│   │   │   │   │   ├── module_npc.rs
│   │   │   │   │   └── token_placement.rs
│   │   │   │   ├── catalog/
│   │   │   │   │   ├── action.rs
│   │   │   │   │   ├── background.rs
│   │   │   │   │   ├── book.rs
│   │   │   │   │   ├── class.rs
│   │   │   │   │   ├── class_feature.rs
│   │   │   │   │   ├── condition.rs
│   │   │   │   │   ├── cult.rs
│   │   │   │   │   ├── deity.rs
│   │   │   │   │   ├── disease.rs
│   │   │   │   │   ├── feat.rs
│   │   │   │   │   ├── hazard.rs
│   │   │   │   │   ├── item.rs
│   │   │   │   │   ├── item_attunement.rs
│   │   │   │   │   ├── language.rs
│   │   │   │   │   ├── mod.rs
│   │   │   │   │   ├── monster.rs
│   │   │   │   │   ├── object.rs
│   │   │   │   │   ├── optional_feature.rs
│   │   │   │   │   ├── psionic.rs
│   │   │   │   │   ├── race.rs
│   │   │   │   │   ├── reward.rs
│   │   │   │   │   ├── sense.rs
│   │   │   │   │   ├── skill.rs
│   │   │   │   │   ├── source.rs
│   │   │   │   │   ├── spell.rs
│   │   │   │   │   ├── spell_list.rs
│   │   │   │   │   ├── subclass.rs
│   │   │   │   │   ├── subclass_feature.rs
│   │   │   │   │   ├── table.rs
│   │   │   │   │   ├── trap.rs
│   │   │   │   │   ├── variant_rule.rs
│   │   │   │   │   └── vehicle.rs
│   │   │   │   └── mod.rs
│   │   │   ├── db.rs
│   │   │   ├── fts/
│   │   │   │   ├── entry_flattener.rs
│   │   │   │   ├── mod.rs
│   │   │   │   └── search.rs
│   │   │   ├── import/
│   │   │   │   ├── collector.rs
│   │   │   │   ├── discovery.rs
│   │   │   │   ├── filter.rs
│   │   │   │   ├── images.rs
│   │   │   │   ├── mod.rs
│   │   │   │   ├── service.rs
│   │   │   │   └── srd.rs
│   │   │   ├── lib.rs
│   │   │   ├── models/
│   │   │   │   ├── campaign/
│   │   │   │   │   ├── campaign.rs
│   │   │   │   │   ├── campaign_asset.rs
│   │   │   │   │   ├── campaign_homebrew_item.rs
│   │   │   │   │   ├── campaign_homebrew_monster.rs
│   │   │   │   │   ├── campaign_homebrew_spell.rs
│   │   │   │   │   ├── campaign_source.rs
│   │   │   │   │   ├── character.rs
│   │   │   │   │   ├── character_class.rs
│   │   │   │   │   ├── character_feat.rs
│   │   │   │   │   ├── character_feature.rs
│   │   │   │   │   ├── character_inventory.rs
│   │   │   │   │   ├── character_proficiency.rs
│   │   │   │   │   ├── character_source.rs
│   │   │   │   │   ├── character_spell.rs
│   │   │   │   │   ├── document.rs
│   │   │   │   │   ├── fog.rs
│   │   │   │   │   ├── light_source.rs
│   │   │   │   │   ├── map.rs
│   │   │   │   │   ├── map_poi.rs
│   │   │   │   │   ├── map_trap.rs
│   │   │   │   │   ├── mod.rs
│   │   │   │   │   ├── module.rs
│   │   │   │   │   ├── module_monster.rs
│   │   │   │   │   ├── module_npc.rs
│   │   │   │   │   └── token_placement.rs
│   │   │   │   ├── catalog/
│   │   │   │   │   ├── action.rs
│   │   │   │   │   ├── background.rs
│   │   │   │   │   ├── book.rs
│   │   │   │   │   ├── class.rs
│   │   │   │   │   ├── class_feature.rs
│   │   │   │   │   ├── condition.rs
│   │   │   │   │   ├── cult.rs
│   │   │   │   │   ├── deity.rs
│   │   │   │   │   ├── disease.rs
│   │   │   │   │   ├── feat.rs
│   │   │   │   │   ├── hazard.rs
│   │   │   │   │   ├── item.rs
│   │   │   │   │   ├── item_attunement.rs
│   │   │   │   │   ├── language.rs
│   │   │   │   │   ├── mod.rs
│   │   │   │   │   ├── monster.rs
│   │   │   │   │   ├── object.rs
│   │   │   │   │   ├── optional_feature.rs
│   │   │   │   │   ├── psionic.rs
│   │   │   │   │   ├── race.rs
│   │   │   │   │   ├── reward.rs
│   │   │   │   │   ├── sense.rs
│   │   │   │   │   ├── skill.rs
│   │   │   │   │   ├── source.rs
│   │   │   │   │   ├── spell.rs
│   │   │   │   │   ├── spell_list.rs
│   │   │   │   │   ├── subclass.rs
│   │   │   │   │   ├── subclass_feature.rs
│   │   │   │   │   ├── table.rs
│   │   │   │   │   ├── trap.rs
│   │   │   │   │   ├── variant_rule.rs
│   │   │   │   │   └── vehicle.rs
│   │   │   │   └── mod.rs
│   │   │   ├── schema.rs
│   │   │   ├── seed/
│   │   │   │   ├── dev.rs
│   │   │   │   └── mod.rs
│   │   │   ├── services/
│   │   │   │   ├── archive.rs
│   │   │   │   ├── asset.rs
│   │   │   │   ├── campaign.rs
│   │   │   │   ├── catalog/
│   │   │   │   │   ├── action.rs
│   │   │   │   │   ├── background.rs
│   │   │   │   │   ├── class.rs
│   │   │   │   │   ├── class_feature.rs
│   │   │   │   │   ├── condition.rs
│   │   │   │   │   ├── cult.rs
│   │   │   │   │   ├── deity.rs
│   │   │   │   │   ├── feat.rs
│   │   │   │   │   ├── hazard.rs
│   │   │   │   │   ├── item.rs
│   │   │   │   │   ├── language.rs
│   │   │   │   │   ├── mod.rs
│   │   │   │   │   ├── monster.rs
│   │   │   │   │   ├── object.rs
│   │   │   │   │   ├── optional_feature.rs
│   │   │   │   │   ├── psionic.rs
│   │   │   │   │   ├── race.rs
│   │   │   │   │   ├── reward.rs
│   │   │   │   │   ├── spell.rs
│   │   │   │   │   ├── subclass.rs
│   │   │   │   │   ├── subclass_feature.rs
│   │   │   │   │   ├── table.rs
│   │   │   │   │   ├── trap.rs
│   │   │   │   │   ├── variant_rule.rs
│   │   │   │   │   └── vehicle.rs
│   │   │   │   ├── character.rs
│   │   │   │   ├── document.rs
│   │   │   │   ├── homebrew.rs
│   │   │   │   ├── map.rs
│   │   │   │   ├── mod.rs
│   │   │   │   ├── module.rs
│   │   │   │   └── token.rs
│   │   │   ├── templates/
│   │   │   │   └── mod.rs
│   │   │   ├── test_utils.rs
│   │   │   ├── tokens.rs
│   │   │   └── utils.rs
│   │   └── tests/
│   │       ├── catalog_import.rs
│   │       ├── common/
│   │       │   └── mod.rs
│   │       └── srd_smoke_test.rs
│   ├── mimir-mapgen/
│   │   ├── src/
│   │   │   ├── assets.rs
│   │   │   ├── biomes.rs
│   │   │   ├── contour.rs
│   │   │   ├── curves.rs
│   │   │   ├── distribution.rs
│   │   │   ├── elevation.rs
│   │   │   ├── format/
│   │   │   │   ├── entities.rs
│   │   │   │   ├── godot_types.rs
│   │   │   │   ├── header.rs
│   │   │   │   ├── mod.rs
│   │   │   │   └── world.rs
│   │   │   ├── lib.rs
│   │   │   ├── main.rs
│   │   │   ├── noise_gen.rs
│   │   │   ├── objects.rs
│   │   │   ├── paths.rs
│   │   │   ├── pipeline.rs
│   │   │   ├── polygons.rs
│   │   │   ├── rooms.rs
│   │   │   ├── terrain.rs
│   │   │   └── water.rs
│   │   └── tests/
│   │       ├── cli_integration.rs
│   │       ├── format_roundtrip.rs
│   │       └── polygon_snapshots.rs
│   ├── mimir-mcp/
│   │   └── src/
│   │       ├── context.rs
│   │       ├── error.rs
│   │       ├── handler.rs
│   │       ├── lib.rs
│   │       ├── main.rs
│   │       ├── response.rs
│   │       └── tools/
│   │           ├── campaign.rs
│   │           ├── catalog.rs
│   │           ├── character.rs
│   │           ├── document.rs
│   │           ├── homebrew.rs
│   │           ├── homebrew_monster.rs
│   │           ├── homebrew_spell.rs
│   │           ├── map.rs
│   │           ├── mapgen.rs
│   │           ├── mod.rs
│   │           └── module.rs
│   └── mimir-print/
│       └── src/
│           ├── builder.rs
│           ├── embedded_templates.rs
│           ├── error.rs
│           ├── lib.rs
│           ├── map_renderer.rs
│           ├── markdown.rs
│           ├── sections/
│           │   ├── card_utils.rs
│           │   ├── character.rs
│           │   ├── character_battle_card.rs
│           │   ├── equipment_cards.rs
│           │   ├── map.rs
│           │   ├── markdown.rs
│           │   ├── mod.rs
│           │   ├── monster_cards.rs
│           │   ├── spell_cards.rs
│           │   ├── token_cutouts.rs
│           │   └── trap_cards.rs
│           ├── service.rs
│           └── world.rs
├── docs/
│   ├── book/
│   │   ├── book.js
│   │   ├── clipboard.min.js
│   │   ├── elasticlunr.min.js
│   │   ├── highlight.js
│   │   ├── mark.min.js
│   │   ├── mermaid-init.js
│   │   ├── mermaid.min.js
│   │   ├── searcher.js
│   │   ├── searchindex.js
│   │   └── toc.js
│   ├── mermaid-init.js
│   └── mermaid.min.js
└── scripts/
    ├── build-sidecar.mjs
    └── extract-srd-fixtures.py
```

## Modules

### crates/mimir

> *Semantic summary to be generated by AI agent.*

#### crates/mimir/build.rs

-  `main` function L1-3 — `()`

### crates/mimir/frontend/__tests__/components/campaigns

> *Semantic summary to be generated by AI agent.*

#### crates/mimir/frontend/__tests__/components/campaigns/CampaignSourcesModal.test.ts

-  `setupSourceMocks` function L35-38 — `function setupSourceMocks(selectedSources: string[] = ['PHB', 'DMG', 'MM'])`
-  `mountModal` function L56-67 — `function mountModal(selectedSources?: string[])`

#### crates/mimir/frontend/__tests__/components/campaigns/CharacterCard.test.ts

-  `makeCharacter` function L13-27 — `function makeCharacter(overrides: Record<string, unknown> = {})`

#### crates/mimir/frontend/__tests__/components/campaigns/NPCsTab.test.ts

-  `makeCampaign` function L19-29 — `function makeCampaign(overrides: Record<string, unknown> = {})`
-  `makeNPC` function L31-45 — `function makeNPC(overrides: Record<string, unknown> = {})`
-  `mountTab` function L58-72 — `function mountTab(npcs = [makeNPC()])`

#### crates/mimir/frontend/__tests__/components/campaigns/PCsTab.test.ts

-  `makeCampaign` function L19-29 — `function makeCampaign(overrides: Record<string, unknown> = {})`
-  `makePC` function L31-45 — `function makePC(overrides: Record<string, unknown> = {})`
-  `mountTab` function L58-72 — `function mountTab(pcs = [makePC()])`

### crates/mimir/frontend/__tests__/components/modules

> *Semantic summary to be generated by AI agent.*

#### crates/mimir/frontend/__tests__/components/modules/CreateModuleModal.test.ts

-  `mountModal` function L20-25 — `function mountModal(show = true)`

#### crates/mimir/frontend/__tests__/components/modules/ModuleDocumentsPanel.test.ts

-  `makeDocument` function L18-31 — `function makeDocument(overrides: Record<string, unknown> = {})`
-  `mountPanel` function L36-40 — `function mountPanel(documents = [makeDocument()])`

#### crates/mimir/frontend/__tests__/components/modules/ModuleMaps.test.ts

-  `makeMap` function L24-41 — `function makeMap(overrides: Record<string, unknown> = {})`
-  `mountComponent` function L54-70 — `function mountComponent(maps = [makeMap()])`

#### crates/mimir/frontend/__tests__/components/modules/ModuleMonsters.test.ts

-  `makeModuleMonster` function L25-40 — `function makeModuleMonster(overrides: Record<string, unknown> = {})`
-  `mountComponent` function L53-68 — `function mountComponent(monsters = [makeModuleMonster()])`

#### crates/mimir/frontend/__tests__/components/modules/ModuleNPCs.test.ts

-  `makeModuleNpc` function L23-34 — `function makeModuleNpc(overrides: Record<string, unknown> = {})`
-  `mountComponent` function L47-60 — `function mountComponent(npcs = [makeModuleNpc()])`

#### crates/mimir/frontend/__tests__/components/modules/ModuleUserDocuments.test.ts

-  `makeDocument` function L23-36 — `function makeDocument(overrides: Record<string, unknown> = {})`
-  `mountComponent` function L49-63 — `function mountComponent(documents = [makeDocument()])`

### crates/mimir/frontend/__tests__/components/sheet

> *Semantic summary to be generated by AI agent.*

#### crates/mimir/frontend/__tests__/components/sheet/CharacterDetailsTab.test.ts

-  `makeCharacter` function L8-54 — `function makeCharacter(overrides: Partial<Character> = {}): Character`
-  `ClassFeature` interface L56-66 — `{ name: : string, source: : string, class_name: : string, class_source: : string...`
-  `makeClassFeature` function L68-78 — `function makeClassFeature(overrides: Partial<ClassFeature> = {}): ClassFeature`
-  `BackgroundDetail` interface L80-85 — `{ name: : string, source: : string, data: : Record<string, unknown>, fluff: : st...`
-  `SubclassDetail` interface L87-93 — `{ name: : string, source: : string, class_name: : string, class_source: : string...`
-  `makeBackgroundDetail` function L95-122 — `function makeBackgroundDetail(overrides: Partial<BackgroundDetail> = {}): Backgr...`
-  `makeSubclassDetail` function L124-140 — `function makeSubclassDetail(overrides: Partial<SubclassDetail> = {}): SubclassDe...`
-  `makeClassData` function L142-157 — `function makeClassData(): Record<string, Record<string, unknown>>`
-  `defaultProps` function L159-167 — `const defaultProps = ()`

#### crates/mimir/frontend/__tests__/components/sheet/CharacterStatsTab.test.ts

-  `makeCharacter` function L17-73 — `function makeCharacter(overrides: Partial<Character> = {}): Character`
-  `makeInventoryItem` function L75-87 — `function makeInventoryItem(overrides: Partial<CharacterInventory> = {}): Charact...`
-  `ClassFeature` interface L89-99 — `{ name: : string, source: : string, class_name: : string, class_source: : string...`
-  `makeClassFeature` function L101-111 — `function makeClassFeature(overrides: Partial<ClassFeature> = {}): ClassFeature`
-  `defaultProps` function L113-122 — `const defaultProps = ()`

#### crates/mimir/frontend/__tests__/components/sheet/EquipmentSection.test.ts

-  `makeCharacter` function L15-52 — `function makeCharacter(overrides: Partial<Character> = {}): Character`
-  `makeInventoryItem` function L54-66 — `function makeInventoryItem(overrides: Partial<CharacterInventory> = {}): Charact...`
-  `defaultProps` function L68-72 — `const defaultProps = ()`

#### crates/mimir/frontend/__tests__/components/sheet/SpellsSection.test.ts

-  `makeWizard` function L15-52 — `function makeWizard(overrides: Partial<Character> = {}): Character`
-  `makeSpellResponse` function L54-69 — `function makeSpellResponse(overrides: Record<string, unknown> = {}): Record<stri...`
-  `formatMod` function L71 — `const formatMod = (mod: number)`
-  `setupSpellMocks` function L73-82 — `function setupSpellMocks(spells: Record<string, unknown>[] = [])`
-  `mountWithSpell` function L393-410 — `function mountWithSpell(spellOverrides: Record<string, unknown> = {})`

### crates/mimir/frontend/__tests__/composables

> *Semantic summary to be generated by AI agent.*

#### crates/mimir/frontend/__tests__/composables/useCatalogSearch.test.ts

-  `makeSpellSummary` function L21-31 — `function makeSpellSummary(overrides: Record<string, unknown> = {})`

#### crates/mimir/frontend/__tests__/composables/useDebouncedSearch.test.ts

-  `createSearch` function L29-36 — `function createSearch(overrides: Record<string, unknown> = {})`

#### crates/mimir/frontend/__tests__/composables/useLightSources.test.ts

-  `makeLightSource` function L20-36 — `function makeLightSource(overrides: Record<string, unknown> = {})`

#### crates/mimir/frontend/__tests__/composables/usePlayerDisplay.test.ts

-  `getUsePlayerDisplay` function L20-23 — `function getUsePlayerDisplay()`

#### crates/mimir/frontend/__tests__/composables/usePlayerDisplayEvents.test.ts

-  `createHandlerSpies` function L39-48 — `function createHandlerSpies(): PlayerDisplayEventHandlers`
-  `mountWithHandlers` function L50-57 — `function mountWithHandlers(handlers: PlayerDisplayEventHandlers)`

#### crates/mimir/frontend/__tests__/composables/useSpells.test.ts

-  `makeSpell` function L19-34 — `function makeSpell(overrides: Record<string, unknown> = {})`

#### crates/mimir/frontend/__tests__/composables/useTokenVision.test.ts

-  `makeToken` function L19-36 — `function makeToken(overrides: Record<string, unknown> = {})`

#### crates/mimir/frontend/__tests__/composables/useVisionCalculation.test.ts

-  `makeToken` function L18-43 — `function makeToken(overrides: Partial<Token> = {}): Token`
-  `makeLightSource` function L45-60 — `function makeLightSource(overrides: Partial<LightSourceSummary> = {}): LightSour...`
-  `createVision` function L62-75 — `function createVision( tokens: Token[] = [], lightSources: LightSourceSummary[] ...`

### crates/mimir/frontend/__tests__/formatters

> *Semantic summary to be generated by AI agent.*

#### crates/mimir/frontend/__tests__/formatters/itemFormatting.test.ts

-  `findItem` function L19-21 — `function findItem(name: string)`

#### crates/mimir/frontend/__tests__/formatters/monsterFormatterEnhanced.test.ts

-  `findMonster` function L19-21 — `function findMonster(name: string)`

#### crates/mimir/frontend/__tests__/formatters/monsterFormatting.test.ts

-  `findMonster` function L33-35 — `function findMonster(name: string)`

#### crates/mimir/frontend/__tests__/formatters/spellFormatting.test.ts

-  `findSpell` function L15-17 — `function findSpell(name: string)`

### crates/mimir/frontend/__tests__/helpers

> *Semantic summary to be generated by AI agent.*

#### crates/mimir/frontend/__tests__/helpers/fixtures.ts

- pub `findClass` function L74-76 — `function findClass(name: string): Fixture`
- pub `findSubclass` function L78-80 — `function findSubclass(name: string): Fixture`
- pub `findSubclassByClass` function L82-84 — `function findSubclassByClass(className: string): Fixture[]`
- pub `findClassFeatures` function L86-88 — `function findClassFeatures(className: string): Fixture[]`
- pub `findClassFeaturesAtLevel` function L90-94 — `function findClassFeaturesAtLevel(className: string, level: number): Fixture[]`
- pub `findSubclassFeatures` function L96-98 — `function findSubclassFeatures(subclassName: string): Fixture[]`
- pub `findSubclassFeaturesAtLevel` function L100-104 — `function findSubclassFeaturesAtLevel(subclassName: string, level: number): Fixtu...`
- pub `findBackground` function L108-110 — `function findBackground(name: string): Fixture`
- pub `findRace` function L112-114 — `function findRace(name: string): Fixture`
- pub `findItem` function L118-120 — `function findItem(name: string): Fixture`
- pub `findItemsByType` function L122-124 — `function findItemsByType(itemType: string): Fixture[]`
- pub `findItemsByRarity` function L126-128 — `function findItemsByRarity(rarity: string): Fixture[]`
- pub `findSpell` function L132-134 — `function findSpell(name: string): Fixture`
- pub `findSpellsByLevel` function L136-138 — `function findSpellsByLevel(level: number): Fixture[]`
- pub `findSpellsBySchool` function L140-142 — `function findSpellsBySchool(school: string): Fixture[]`
- pub `findMonster` function L146-148 — `function findMonster(name: string): Fixture`
- pub `findMonstersByCR` function L150-152 — `function findMonstersByCR(cr: string): Fixture[]`
- pub `findMonstersByType` function L154-156 — `function findMonstersByType(creatureType: string): Fixture[]`
- pub `getTestCharacter` function L160-162 — `function getTestCharacter(id: string): Fixture`
- pub `getTestCharacterByName` function L164-166 — `function getTestCharacterByName(name: string): Fixture`
- pub `getAllTestCharacters` function L168-170 — `function getAllTestCharacters(): Fixture[]`
- pub `getHomebrewItem` function L174-176 — `function getHomebrewItem(name: string): Fixture`
- pub `getAllHomebrewItems` function L178-180 — `function getAllHomebrewItems(): Fixture[]`
- pub `buildClassDataMap` function L188-199 — `function buildClassDataMap(classNames?: string[]): Record<string, Fixture>`
- pub `buildSubclassDataMap` function L204-215 — `function buildSubclassDataMap(subclassNames?: string[]): Record<string, Fixture>`
-  `Fixture` type L47 — `= Record<string, unknown>`
-  `findByName` function L49-58 — `function findByName(items: Fixture[], name: string): Fixture`
-  `findById` function L60-66 — `function findById(items: Fixture[], id: string | number): Fixture`
-  `filterBy` function L68-70 — `function filterBy(items: Fixture[], key: string, value: unknown): Fixture[]`

#### crates/mimir/frontend/__tests__/helpers/mockInvoke.ts

- pub `InvokeHandler` type L26 — `= (params?: Record<string, unknown>) => unknown | Promise<unknown>`
- pub `setupInvokeMock` function L52-87 — `function setupInvokeMock(): ReturnType<typeof vi.mocked<typeof invoke>>`
- pub `resetInvokeMock` function L92-99 — `function resetInvokeMock(): void`
- pub `getInvokeMock` function L104-109 — `function getInvokeMock(): ReturnType<typeof vi.mocked<typeof invoke>>`
- pub `mockCommand` function L121-132 — `function mockCommand( command: string, data: T, options: { persistent?: boolean ...`
- pub `mockCommandError` function L141-152 — `function mockCommandError( command: string, error: string, options: { persistent...`
- pub `mockCommandRaw` function L158-169 — `function mockCommandRaw( command: string, response: unknown, options: { persiste...`
- pub `mockCommandHandler` function L175-186 — `function mockCommandHandler( command: string, handler: InvokeHandler, options: {...`
- pub `mockCommandReject` function L191-203 — `function mockCommandReject( command: string, error: string | Error, options: { p...`
- pub `mockCommandSequence` function L209-224 — `function mockCommandSequence( command: string, responses: ApiResponse<T>[], ): v...`
- pub `setInvokeFallback` function L230-232 — `function setInvokeFallback(handler: InvokeHandler): void`
- pub `getCommandCallCount` function L239-241 — `function getCommandCallCount(command: string): number`
- pub `expectCommandCalled` function L246-252 — `function expectCommandCalled(command: string): void`
- pub `expectCommandCalledWith` function L257-277 — `function expectCommandCalledWith(command: string, params: Record<string, unknown...`
- pub `getCommandCalls` function L282-285 — `function getCommandCalls(command: string): unknown[][]`
-  `CommandRegistration` interface L28-37 — `{ handler: : InvokeHandler, response: : unknown, persistent: : boolean, callCoun...`

#### crates/mimir/frontend/__tests__/helpers/mountHelpers.ts

- pub `MountOptions` interface L22-33 — `{ props: : Props, slots: : Record<string, string | Component>, global: : Mountin...`
- pub `mountWithPlugins` function L67-77 — `function mountWithPlugins( component: T, options: MountOptions = {}, ): VueWrapp...`
- pub `shallowMountWithPlugins` function L83-93 — `function shallowMountWithPlugins( component: T, options: MountOptions = {}, ): V...`
- pub `createTestPinia` function L105-115 — `function createTestPinia(initialState?: Record<string, unknown>): Pinia`
-  `buildGlobalConfig` function L37-60 — `function buildGlobalConfig(options: MountOptions = {}): MountingOptions<unknown>...`

### crates/mimir/frontend/__tests__/homebrew

> *Semantic summary to be generated by AI agent.*

#### crates/mimir/frontend/__tests__/homebrew/homebrewCrud.test.ts

-  `ItemFormState` interface L18-38 — `{ name: : string, item_type: : string, rarity: : string, description: : string, ...`
-  `emptyForm` function L40-47 — `function emptyForm(): ItemFormState`
-  `formToDataJson` function L50-95 — `function formToDataJson(form: ItemFormState): string`
-  `dataJsonToForm` function L98-142 — `function dataJsonToForm(data: Record<string, unknown>): Partial<ItemFormState>`
-  `HomebrewItem` interface L418-421 — `{ item_type: : string | null, rarity: : string | null }`
-  `CreateItem` interface L423-429 — `{ campaign_id: : string, name: : string, item_type: : string, rarity: : string, ...`
-  `UpdateItem` interface L431-436 — `{ name: : string, item_type: : string | null, rarity: : string | null, data: : s...`
-  `HomebrewMonster` interface L438-442 — `{ cr: : string | null, creature_type: : string | null, size: : string | null }`
-  `CreateMonster` interface L444-451 — `{ campaign_id: : string, name: : string, data: : string, cr: : string, creature_...`
-  `UpdateMonster` interface L453-459 — `{ name: : string, data: : string, cr: : string | null, creature_type: : string |...`
-  `HomebrewSpell` interface L461-464 — `{ level: : number | null, school: : string | null }`
-  `CreateSpell` interface L466-472 — `{ campaign_id: : string, name: : string, data: : string, level: : number, school...`
-  `UpdateSpell` interface L474-479 — `{ name: : string, data: : string, level: : number | null, school: : string | nul...`

### crates/mimir/frontend/__tests__/integration

> *Semantic summary to be generated by AI agent.*

#### crates/mimir/frontend/__tests__/integration/CharacterSheetIntegration.test.ts

-  `makeCharacter` function L29-80 — `function makeCharacter(overrides: Partial<Character> = {}): Character`
-  `makeRaceData` function L82-93 — `function makeRaceData(overrides: Record<string, unknown> = {}): Record<string, u...`
-  `makeClassData` function L95-120 — `function makeClassData(overrides: Record<string, unknown> = {}): Record<string, ...`
-  `makeBackgroundData` function L122-138 — `function makeBackgroundData(overrides: Record<string, unknown> = {}): Record<str...`
-  `makeSubclassData` function L140-153 — `function makeSubclassData(overrides: Record<string, unknown> = {}): Record<strin...`
-  `makeSubclassFeatures` function L155-162 — `function makeSubclassFeatures(): Array<Record<string, unknown>>`
-  `makeInventory` function L164-171 — `function makeInventory(): CharacterInventory[]`
-  `setupFullCharacterMocks` function L179-210 — `function setupFullCharacterMocks(character: Character = makeCharacter())`

### crates/mimir/frontend/__tests__/services

> *Semantic summary to be generated by AI agent.*

#### crates/mimir/frontend/__tests__/services/DocumentService.test.ts

-  `makeDocument` function L20-33 — `function makeDocument(overrides: Record<string, unknown> = {})`

#### crates/mimir/frontend/__tests__/services/ModuleService.test.ts

-  `makeModule` function L21-33 — `function makeModule(overrides: Record<string, unknown> = {})`

### crates/mimir/frontend/__tests__/utils

> *Semantic summary to be generated by AI agent.*

#### crates/mimir/frontend/__tests__/utils/contentRenderer.test.ts

-  `render` function L42-44 — `function render(entries: any[])`

### crates/mimir/frontend/src/composables/map/__tests__

> *Semantic summary to be generated by AI agent.*

#### crates/mimir/frontend/src/composables/map/__tests__/useFog.test.ts

-  `makeArea` function L18-28 — `function makeArea(overrides: Partial<FogRevealedArea> = {}): FogRevealedArea`

#### crates/mimir/frontend/src/composables/map/__tests__/useLightSources.test.ts

-  `makeSummary` function L18-34 — `function makeSummary(overrides: Partial<LightSourceSummary> = {}): LightSourceSu...`
-  `makeLightSource` function L36-53 — `function makeLightSource(overrides: Partial<LightSource> = {}): LightSource`

#### crates/mimir/frontend/src/composables/map/__tests__/useMapMarkers.test.ts

-  `makeTrap` function L24-40 — `function makeTrap(overrides: Partial<MapTrap> = {}): MapTrap`
-  `makePoi` function L42-57 — `function makePoi(overrides: Partial<MapPoi> = {}): MapPoi`
-  `createMarkers` function L59-65 — `function createMarkers(mapIdValue = 'map-1')`

#### crates/mimir/frontend/src/composables/map/__tests__/usePlayerDisplayEvents.test.ts

-  `createHandlerSpies` function L39-48 — `function createHandlerSpies(): PlayerDisplayEventHandlers`
-  `mountWithHandlers` function L50-57 — `function mountWithHandlers(handlers: PlayerDisplayEventHandlers)`

#### crates/mimir/frontend/src/composables/map/__tests__/useVisibilityPolygon.test.ts

-  `makeUvttData` function L17-26 — `function makeUvttData(overrides: Partial<UvttData> = {}): UvttData`

#### crates/mimir/frontend/src/composables/map/__tests__/useVisionCalculation.test.ts

-  `makeToken` function L18-43 — `function makeToken(overrides: Partial<Token> = {}): Token`
-  `makeLightSource` function L45-61 — `function makeLightSource(overrides: Partial<LightSourceSummary> = {}): LightSour...`
-  `createVision` function L63-76 — `function createVision( tokens: Token[] = [], lightSources: LightSourceSummary[] ...`

### crates/mimir/frontend/src/composables/map

> *Semantic summary to be generated by AI agent.*

#### crates/mimir/frontend/src/composables/map/useEntityDragDrop.ts

- pub `EntityDragOptions` interface L10-37 — `{ viewportRef: : Ref<HTMLElement | null>, baseScale: : Ref<number>, zoom: : Ref<...`
- pub `EntityDragReturn` interface L39-54 — `{ draggingEntity: : Ref<T | null>, onMouseDown: : (event: MouseEvent, entity: T)...`
- pub `useEntityDragDrop` function L97-271 — `function useEntityDragDrop( options: EntityDragOptions<T> ): EntityDragReturn<T>`
-  `getEntityPixelCoords` function L124-136 — `function getEntityPixelCoords(entity: T): { x: number; y: number }`
-  `onMouseDown` function L141-169 — `function onMouseDown(event: MouseEvent, entity: T): void`
-  `onMouseMove` function L176-213 — `function onMouseMove(event: MouseEvent): boolean`
-  `onMouseUp` function L219-263 — `function onMouseUp(): Promise<boolean>`

#### crates/mimir/frontend/src/composables/map/useFog.ts

- pub `FogRevealedArea` interface L14-21 — `{ id: : string, map_id: : string, x: : number, y: : number, width: : number, hei...`
- pub `FogState` interface L23-27 — `{ map_id: : string, fog_enabled: : boolean, revealed_areas: : FogRevealedArea[] ...`
- pub `RevealRectRequest` interface L29-35 — `{ map_id: : string, x: : number, y: : number, width: : number, height: : number ...`
- pub `RevealCircleRequest` interface L37-42 — `{ map_id: : string, center_x: : number, center_y: : number, radius: : number }`
- pub `useFog` function L44-231 — `function useFog(mapId: string)`
-  `ApiResponse` interface L8-12 — `{ success: : boolean, data: : T, error: : string }`
-  `loadFogState` function L54-71 — `function loadFogState(): Promise<void>`
-  `toggleFog` function L74-93 — `function toggleFog(): Promise<boolean>`
-  `enableFog` function L96-108 — `function enableFog(): Promise<boolean>`
-  `disableFog` function L111-123 — `function disableFog(): Promise<boolean>`
-  `revealRect` function L126-139 — `function revealRect(x: number, y: number, width: number, height: number): Promis...`
-  `revealCircle` function L142-155 — `function revealCircle(centerX: number, centerY: number, radius: number): Promise...`
-  `revealAll` function L158-171 — `function revealAll(mapWidth: number, mapHeight: number): Promise<FogRevealedArea...`
-  `deleteRevealedArea` function L174-186 — `function deleteRevealedArea(id: string): Promise<boolean>`
-  `resetFog` function L189-201 — `function resetFog(): Promise<boolean>`
-  `isPointRevealed` function L204-209 — `function isPointRevealed(px: number, py: number): boolean`

#### crates/mimir/frontend/src/composables/map/useLightSources.ts

- pub `LightType` type L15 — `= 'torch' | 'lantern' | 'candle' | 'spell' | 'custom'`
- pub `LightSource` interface L18-32 — `{ id: : string, map_id: : string, token_id: : string | null, name: : string, lig...`
- pub `LightSourceSummary` interface L35-48 — `{ id: : string, map_id: : string, token_id: : string | null, token_name: : strin...`
- pub `CreateLightSourceRequest` interface L51-61 — `{ map_id: : string, token_id: : string | null, name: : string, light_type: : Lig...`
- pub `UpdateLightSourceRequest` interface L64-73 — `{ name: : string, light_type: : LightType, x: : number, y: : number, bright_radi...`
- pub `useLightSources` function L84-294 — `function useLightSources(mapId: string)`
-  `ApiResponse` interface L8-12 — `{ success: : boolean, data: : T, error: : string }`
-  `loadLightSources` function L96-112 — `function loadLightSources(): Promise<void>`
-  `createLightSource` function L115-130 — `function createLightSource(request: CreateLightSourceRequest): Promise<LightSour...`
-  `createTorch` function L133-145 — `function createTorch(x: number, y: number): Promise<LightSource | null>`
-  `createLantern` function L148-160 — `function createLantern(x: number, y: number): Promise<LightSource | null>`
-  `updateLightSource` function L163-182 — `function updateLightSource(id: string, request: UpdateLightSourceRequest): Promi...`
-  `moveLightSource` function L185-201 — `function moveLightSource(id: string, x: number, y: number): Promise<LightSource ...`
-  `toggleLightSource` function L204-219 — `function toggleLightSource(id: string): Promise<LightSource | null>`
-  `deleteLightSource` function L222-234 — `function deleteLightSource(id: string): Promise<boolean>`
-  `deleteAllLightSources` function L237-249 — `function deleteAllLightSources(): Promise<boolean>`
-  `getLightSource` function L252-254 — `function getLightSource(id: string): LightSourceSummary | undefined`
-  `feetToPixels` function L257-260 — `function feetToPixels(feet: number, gridSizePx: number): number`
-  `pixelsToFeet` function L263-265 — `function pixelsToFeet(pixels: number, gridSizePx: number): number`

#### crates/mimir/frontend/src/composables/map/useMapMarkers.ts

- pub `MapTrap` interface L8-21 — `{ id: : string, map_id: : string, grid_x: : number, grid_y: : number, name: : st...`
- pub `MapPoi` interface L26-38 — `{ id: : string, map_id: : string, grid_x: : number, grid_y: : number, name: : st...`
- pub `useMapMarkers` function L49-297 — `function useMapMarkers(options: UseMapMarkersOptions)`
-  `UseMapMarkersOptions` interface L40-44 — `{ mapId: : ComputedRef<string | null>, gridSizePx: : ComputedRef<number>, isDisp...`
-  `loadMapTraps` function L78-91 — `function loadMapTraps(id?: string)`
-  `loadMapPois` function L96-109 — `function loadMapPois(id?: string)`
-  `toggleTrapVisibility` function L114-122 — `function toggleTrapVisibility(trap: MapTrap)`
-  `togglePoiVisibility` function L127-135 — `function togglePoiVisibility(poi: MapPoi)`
-  `showPoiContextMenuAt` function L140-148 — `function showPoiContextMenuAt(event: MouseEvent, poi: MapPoi)`
-  `openPoiEditModal` function L153-159 — `function openPoiEditModal()`
-  `closePoiEditModal` function L164-167 — `function closePoiEditModal()`
-  `handlePoiSaved` function L172-181 — `function handlePoiSaved(updatedPoi: MapPoi)`
-  `togglePoiVisibilityFromContext` function L186-191 — `function togglePoiVisibilityFromContext()`
-  `deletePoiFromContext` function L196-210 — `function deletePoiFromContext()`
-  `sendMarkersToDisplay` function L215-231 — `function sendMarkersToDisplay()`
-  `closePoiContextMenu` function L236-238 — `function closePoiContextMenu()`
-  `clearMarkers` function L243-248 — `function clearMarkers()`
-  `getPoiIcon` function L253-265 — `function getPoiIcon(icon: string): string`

#### crates/mimir/frontend/src/composables/map/usePlayerDisplayEvents.ts

- pub `MapUpdatePayload` interface L11-20 — `{ mapId: : string, gridType: : string, gridSizePx: : number | null, gridOffsetX:...`
- pub `TokensUpdatePayload` interface L25-29 — `{ mapId: : string, tokens: : Token[], deadTokenIds: : string[] }`
- pub `FogUpdatePayload` interface L34-45 — `{ mapId: : string, revealMap: : boolean, tokenOnlyLos: : boolean, visionCircles:...`
- pub `LightSourcesUpdatePayload` interface L50-53 — `{ mapId: : string, lightSources: : LightSourceSummary[] }`
- pub `MarkersUpdatePayload` interface L58-63 — `{ mapId: : string, traps: : { id: string; grid_x: number; grid_y: number; name: ...`
- pub `PlayerDisplayEventHandlers` interface L68-75 — `{ onMapUpdate: : (payload: MapUpdatePayload) => void | Promise<void>, onBlackout...`
- pub `usePlayerDisplayEvents` function L97-154 — `function usePlayerDisplayEvents(handlers: PlayerDisplayEventHandlers): void`

#### crates/mimir/frontend/src/composables/map/usePlayerViewport.ts

- pub `PlayerViewportOptions` interface L3-10 — `{ minZoom: : number, maxZoom: : number, zoomStep: : number }`
- pub `PlayerViewportReturn` interface L12-41 — `{ panX: : Ref<number>, panY: : Ref<number>, zoom: : Ref<number>, isPanning: : Re...`
- pub `usePlayerViewport` function L62-198 — `function usePlayerViewport(options: PlayerViewportOptions = {}): PlayerViewportR...`
-  `handleMouseDown` function L95-104 — `function handleMouseDown(event: MouseEvent): void`
-  `handleMouseMove` function L109-114 — `function handleMouseMove(event: MouseEvent): void`
-  `handleMouseUp` function L119-121 — `function handleMouseUp(): void`
-  `handleWheel` function L126-143 — `function handleWheel(event: WheelEvent): void`
-  `updateDisplayScale` function L148-171 — `function updateDisplayScale(imageEl: HTMLImageElement | null): void`
-  `reset` function L176-180 — `function reset(): void`

#### crates/mimir/frontend/src/composables/map/useTokenDrag.ts

- pub `useTokenDrag` function L81-286 — `function useTokenDrag(options: UseTokenDragOptions)`
-  `BackendToken` interface L10-32 — `{ id: : string, map_id: : string, name: : string, token_type: : string, size: : ...`
-  `transformToken` function L37-61 — `function transformToken(backendToken: BackendToken): Token`
-  `UseTokenDragOptions` interface L63-76 — `{ tokens: : Ref<Token[]>, selectedTokenId: : Ref<string | null>, zoom: : Ref<num...`
-  `snapToGrid` function L105-115 — `function snapToGrid(x: number, y: number): { x: number; y: number }`
-  `sendTokensToDisplayWithDragOffset` function L120-145 — `function sendTokensToDisplayWithDragOffset()`
-  `handleTokenDragStart` function L150-164 — `function handleTokenDragStart(event: MouseEvent, token: Token)`
-  `handleTokenDrag` function L169-187 — `function handleTokenDrag(event: MouseEvent)`
-  `handleTokenDragEnd` function L192-262 — `function handleTokenDragEnd()`
-  `cleanup` function L267-270 — `function cleanup()`

#### crates/mimir/frontend/src/composables/map/useTokenVision.ts

- pub `TokenVisionSettings` interface L14-19 — `{ vision_bright_ft: : number | null, vision_dim_ft: : number | null, vision_dark...`
- pub `VisionPresetKey` type L81 — `= keyof typeof VISION_PRESETS`
- pub `useTokenVision` function L92-219 — `function useTokenVision()`
-  `ApiResponse` interface L83-87 — `{ success: : boolean, data: : T, error: : string }`
-  `getVisionSettings` function L96-103 — `function getVisionSettings(token: Token): TokenVisionSettings`
-  `updateVisionSettings` function L109-130 — `function updateVisionSettings( tokenId: string, settings: TokenVisionSettings ):...`
-  `applyPreset` function L136-147 — `function applyPreset( tokenId: string, presetKey: VisionPresetKey ): Promise<Tok...`
-  `resetToDefault` function L152-154 — `function resetToDefault(tokenId: string): Promise<Token | null>`
-  `hasCustomSettings` function L159-166 — `function hasCustomSettings(token: Token): boolean`
-  `getVisionDescription` function L171-188 — `function getVisionDescription(token: Token): string`
-  `findMatchingPreset` function L193-205 — `function findMatchingPreset(token: Token): VisionPresetKey | null`

#### crates/mimir/frontend/src/composables/map/useTokens.ts

- pub `useTokens` function L16-255 — `function useTokens(mapId: string)`
-  `ApiResponse` interface L10-14 — `{ success: : boolean, data: : T, error: : string }`
-  `loadTokens` function L45-61 — `function loadTokens(): Promise<void>`
-  `loadTokenSummaries` function L64-82 — `function loadTokenSummaries(): Promise<void>`
-  `createToken` function L85-104 — `function createToken(request: CreateTokenRequest): Promise<Token | null>`
-  `updateToken` function L107-129 — `function updateToken(id: string, request: UpdateTokenRequest): Promise<Token | n...`
-  `updateTokenPosition` function L132-147 — `function updateTokenPosition(id: string, x: number, y: number): Promise<Token | ...`
-  `toggleVisibility` function L150-165 — `function toggleVisibility(id: string): Promise<Token | null>`
-  `deleteToken` function L168-187 — `function deleteToken(id: string): Promise<boolean>`
-  `getTokenDisplaySize` function L190-193 — `function getTokenDisplaySize(token: Token, gridSizePx: number): number`
-  `getTokenColor` function L196-198 — `function getTokenColor(token: Token): string`
-  `loadTokenImage` function L201-218 — `function loadTokenImage(tokenId: string): Promise<string | null>`
-  `loadAllTokenImages` function L221-224 — `function loadAllTokenImages(): Promise<void>`
-  `getTokenImage` function L227-229 — `function getTokenImage(tokenId: string): string | undefined`

#### crates/mimir/frontend/src/composables/map/useUvttMap.ts

- pub `UvttMapState` interface L26-49 — `{ uvttData: : UvttData | null, walls: : Wall[], portals: : Portal[], lights: : L...`
- pub `useUvttMap` function L59-215 — `function useUvttMap( mapId: Ref<string | null>, campaignId: Ref<string | null>, ...`
- pub `UvttMapComposable` type L221 — `= ReturnType<typeof useUvttMap>`
-  `ApiResponse` interface L20-24 — `{ success: : boolean, data: : T, error: : string }`
-  `loadUvttData` function L78-126 — `function loadUvttData()`
-  `togglePortal` function L129-134 — `function togglePortal(portalId: string)`
-  `setPortalState` function L137-142 — `function setPortalState(portalId: string, closed: boolean)`
-  `openAllPortals` function L145-147 — `function openAllPortals()`
-  `closeAllPortals` function L150-152 — `function closeAllPortals()`
-  `resetPortalStates` function L155-159 — `function resetPortalStates()`

#### crates/mimir/frontend/src/composables/map/useVisibilityPolygon.ts

- pub `Point` interface L12-15 — `{ x: : number, y: : number }`
- pub `Wall` interface L18-21 — `{ p1: : Point, p2: : Point }`
- pub `Portal` interface L24-28 — `{ id: : string, wall: : Wall, closed: : boolean }`
- pub `UvttPoint` interface L31-34 — `{ x: : number, y: : number }`
- pub `UvttPortal` interface L37-43 — `{ position: : UvttPoint, bounds: : [UvttPoint, UvttPoint], rotation: : number, c...`
- pub `UvttLight` interface L46-52 — `{ position: : UvttPoint, range: : number, intensity: : number, color: : string, ...`
- pub `UvttEnvironment` interface L55-58 — `{ baked_lighting: : boolean, ambient_light: : string }`
- pub `Light` interface L61-68 — `{ id: : string, position: : Point, range: : number, intensity: : number, color: ...`
- pub `UvttData` interface L71-82 — `{ format: : number, resolution: : { map_origin?: UvttPoint map_size: UvttPoint p...`
- pub `AmbientLightLevel` type L85 — `= 'bright' | 'dim' | 'darkness'`
- pub `uvttAmbientToLevel` function L91-109 — `function uvttAmbientToLevel(argb: string | undefined): AmbientLightLevel`
- pub `uvttWallsToPixels` function L112-129 — `function uvttWallsToPixels(uvtt: UvttData): Wall[]`
- pub `uvttPortalsToPixels` function L132-144 — `function uvttPortalsToPixels(uvtt: UvttData): Portal[]`
- pub `uvttLightsToPixels` function L168-183 — `function uvttLightsToPixels(uvtt: UvttData): Light[]`
- pub `calculateVisibilityPolygon` function L259-388 — `function calculateVisibilityPolygon( origin: Point, walls: Wall[], maxRadius: nu...`
- pub `polygonToSvgPath` function L394-402 — `function polygonToSvgPath(points: Point[]): string`
- pub `useVisibilityPolygon` function L407-448 — `function useVisibilityPolygon( origin: Ref<Point | null>, walls: Ref<Wall[]>, po...`
- pub `useMultiTokenVisibility` function L453-502 — `function useMultiTokenVisibility( tokens: Ref<Array<{ id: string; x: number; y: ...`
-  `argbToRgba` function L147-165 — `function argbToRgba(argb: string): string`
-  `RayHit` interface L186-190 — `{ point: : Point, angle: : number, dist: : number }`
-  `distance` function L193-197 — `function distance(p1: Point, p2: Point): number`
-  `raySegmentIntersection` function L203-247 — `function raySegmentIntersection( origin: Point, angle: number, seg: Wall, extend...`

#### crates/mimir/frontend/src/composables/map/useVisionCalculation.ts

- pub `AmbientLight` type L15 — `= 'bright' | 'dim' | 'darkness'`
- pub `LightLevel` type L18 — `= 'bright' | 'dim' | 'darkness'`
- pub `LightZone` interface L21-27 — `{ sourceId: : string, x: : number, y: : number, brightRadiusPx: : number, dimRad...`
- pub `TokenVision` interface L30-41 — `{ tokenId: : string, tokenColor: : string | null, x: : number, y: : number, visi...`
- pub `VisionCalculationConfig` interface L44-56 — `{ tokens: : Ref<Token[]>, lightSources: : Ref<LightSourceSummary[]>, ambientLigh...`
- pub `useVisionCalculation` function L190-301 — `function useVisionCalculation(config: VisionCalculationConfig)`
-  `feetToPixels` function L59-61 — `function feetToPixels(feet: number, gridSizePx: number): number`
-  `calculateLightZones` function L70-107 — `function calculateLightZones( mapLightSources: LightSourceSummary[], tokens: Tok...`
-  `getLightLevelAtPoint` function L112-133 — `function getLightLevelAtPoint( x: number, y: number, lightZones: LightZone[], am...`
-  `calculateTokenVision` function L141-185 — `function calculateTokenVision( token: Token, lightZones: LightZone[], ambientLig...`
-  `isPointInVision` function L227-236 — `function isPointInVision( pointX: number, pointY: number, vision: TokenVision ):...`
-  `canPartySeePoint` function L241-255 — `function canPartySeePoint(x: number, y: number): { canSee: boolean; isDim: boole...`
-  `getLightLevel` function L260-262 — `function getLightLevel(x: number, y: number): LightLevel`

### crates/mimir/frontend/src/composables

> *Semantic summary to be generated by AI agent.*

#### crates/mimir/frontend/src/composables/useApiCall.ts

- pub `ApiCallOptions` interface L5-10 — `{ retryCount: : number, retryDelay: : number, onSuccess: : (data: any) => void, ...`
- pub `useApiCall` function L12-84 — `function useApiCall()`
-  `execute` function L17-69 — `const execute = ( command: string, params?: any, options: ApiCallOptions = {} ):...`
-  `reset` function L71-75 — `const reset = ()`

#### crates/mimir/frontend/src/composables/useClipboard.ts

- pub `ClipboardReturn` interface L3-10 — `{ copiedText: : Ref<string>, copy: : (text: string) => Promise<boolean>, wasCopi...`
- pub `useClipboard` function L25-62 — `function useClipboard(feedbackDuration = 2000): ClipboardReturn`
-  `copy` function L29-51 — `function copy(text: string): Promise<boolean>`
-  `wasCopied` function L53-55 — `function wasCopied(text: string): boolean`

#### crates/mimir/frontend/src/composables/useDashboardLink.ts

- pub `EntityFocusEvent` interface L12-21 — `{ type: : 'monster' | 'npc' | 'pc' | 'trap' | 'poi', entityId: : string, tokenId...`
- pub `UseDashboardLinkOptions` interface L23-34 — `{ onFocusMonster: : (monsterId: string, tokenName: string) => void, onFocusNpc: ...`
- pub `useDashboardLink` function L60-109 — `function useDashboardLink(options: UseDashboardLinkOptions = {})`
-  `setupListener` function L64-92 — `function setupListener()`

#### crates/mimir/frontend/src/composables/useDataEvents.ts

- pub `useDataEvents` function L21-68 — `function useDataEvents()`
-  `on` function L27-34 — `function on( event: T, listener: (payload: DataEventPayloads[T]) => void ): () =...`
-  `once` function L39-46 — `function once( event: T, listener: (payload: DataEventPayloads[T]) => void ): ()...`
-  `emit` function L51-56 — `function emit( event: T, payload: DataEventPayloads[T] ): void`

#### crates/mimir/frontend/src/composables/useDebouncedSearch.ts

- pub `DebouncedSearchOptions` interface L4-17 — `{ command: : string, minLength: : number, debounceMs: : number, limit: : number,...`
- pub `DebouncedSearchReturn` interface L19-32 — `{ query: : Ref<string>, results: : Ref<TResult[]>, isSearching: : Ref<boolean>, ...`
- pub `useDebouncedSearch` function L56-125 — `function useDebouncedSearch( options: DebouncedSearchOptions<TResult, TRaw> ): D...`
-  `search` function L74-104 — `function search(): void`
-  `clear` function L106-110 — `function clear(): void`
-  `setQuery` function L112-115 — `function setQuery(value: string): void`

#### crates/mimir/frontend/src/composables/useDevTools.ts

- pub `SeedAction` type L4 — `= 'seed' | 'reseed' | 'clear'`
- pub `MessageType` type L5 — `= 'success' | 'error'`
- pub `DevToolsReturn` interface L7-26 — `{ isDevMode: : Ref<boolean>, isDevSeeded: : Ref<boolean>, isPending: : Ref<boole...`
- pub `useDevTools` function L46-138 — `function useDevTools(): DevToolsReturn`
-  `checkDevMode` function L53-61 — `function checkDevMode(): Promise<void>`
-  `checkDevSeeded` function L63-71 — `function checkDevSeeded(): Promise<void>`
-  `initialize` function L73-78 — `function initialize(): Promise<void>`
-  `executeSeedAction` function L83-113 — `function executeSeedAction( action: SeedAction, command: string, successMessage:...`
-  `seed` function L115-117 — `function seed(): Promise<void>`
-  `reseed` function L119-121 — `function reseed(): Promise<void>`
-  `clear` function L123-125 — `function clear(): Promise<void>`

#### crates/mimir/frontend/src/composables/usePagination.ts

- pub `PaginationOptions` interface L3-8 — `{ defaultPageSize: : number, pageSizeOptions: : number[] }`
- pub `PaginationResult` interface L10-43 — `{ currentPage: : Ref<number>, pageSize: : Ref<number>, totalItems: : ComputedRef...`
- pub `usePagination` function L49-147 — `function usePagination( items: Ref<T[]> | ComputedRef<T[]>, options: PaginationO...`
-  `goToPage` function L87-90 — `function goToPage(page: number)`
-  `nextPage` function L92-96 — `function nextPage()`
-  `previousPage` function L98-102 — `function previousPage()`
-  `firstPage` function L104-106 — `function firstPage()`
-  `lastPage` function L108-110 — `function lastPage()`
-  `reset` function L112-114 — `function reset()`

#### crates/mimir/frontend/src/composables/useWeaponRegistry.ts

- pub `useWeaponRegistry` function L59-65 — `function useWeaponRegistry()`
-  `loadWeaponNames` function L22-37 — `function loadWeaponNames(): Promise<void>`
-  `isWeapon` function L43-53 — `function isWeapon(itemName: string): boolean`

### crates/mimir/frontend/src/composables/windows/__tests__

> *Semantic summary to be generated by AI agent.*

#### crates/mimir/frontend/src/composables/windows/__tests__/usePlayerDisplay.test.ts

-  `getUsePlayerDisplay` function L18-21 — `function getUsePlayerDisplay()`

### crates/mimir/frontend/src/composables/windows

> *Semantic summary to be generated by AI agent.*

#### crates/mimir/frontend/src/composables/windows/useDmMapWindow.ts

- pub `useDmMapWindow` function L75-88 — `function useDmMapWindow()`
-  `checkWindowOpen` function L19-28 — `function checkWindowOpen(): Promise<boolean>`
-  `openWindow` function L33-43 — `function openWindow(moduleId: string, campaignId: string): Promise<void>`
-  `closeWindow` function L48-58 — `function closeWindow(): Promise<void>`
-  `toggleFullscreen` function L63-70 — `function toggleFullscreen(): Promise<boolean>`

#### crates/mimir/frontend/src/composables/windows/usePlayerDisplay.ts

- pub `usePlayerDisplay` function L144-161 — `function usePlayerDisplay()`
-  `checkDisplayOpen` function L20-29 — `function checkDisplayOpen(): Promise<boolean>`
-  `openDisplay` function L34-42 — `function openDisplay(): Promise<void>`
-  `closeDisplay` function L47-56 — `function closeDisplay(): Promise<void>`
-  `toggleDisplay` function L61-69 — `function toggleDisplay(): Promise<boolean>`
-  `toggleFullscreen` function L74-81 — `function toggleFullscreen(): Promise<boolean>`
-  `sendMapToDisplay` function L86-112 — `function sendMapToDisplay( mapId: number, gridType: string = 'none', gridSizePx:...`
-  `toggleBlackout` function L117-126 — `function toggleBlackout(): Promise<void>`
-  `setBlackout` function L131-139 — `function setBlackout(blackout: boolean): Promise<void>`

### crates/mimir/frontend/src/constants

> *Semantic summary to be generated by AI agent.*

#### crates/mimir/frontend/src/constants/tokenSizes.ts

- pub `normalizeSize` function L29-34 — `function normalizeSize(size: unknown): string`
- pub `sizeToTokenSize` function L40-43 — `function sizeToTokenSize(size: unknown): TokenSize`

### crates/mimir/frontend/src/features/campaigns/composables

> *Semantic summary to be generated by AI agent.*

#### crates/mimir/frontend/src/features/campaigns/composables/useDashboardState.ts

- pub `DashboardTab` type L4 — `= 'campaign' | 'modules' | 'npcs' | 'pcs' | 'homebrew'`
- pub `DashboardTabConfig` interface L6-11 — `{ id: : DashboardTab, label: : string, icon: : string, route: : string }`
- pub `useDashboardState` function L23-117 — `function useDashboardState(campaignId: string | number)`
-  `getStoredTab` function L28-34 — `const getStoredTab = (): DashboardTab`
-  `setTab` function L69-72 — `function setTab(tabId: DashboardTab)`
-  `selectModule` function L75-77 — `function selectModule(moduleId: string | number)`
-  `closeModuleDetail` function L80-82 — `function closeModuleDetail()`
-  `startSession` function L85-87 — `function startSession(moduleId: string | number)`
-  `endSession` function L90-92 — `function endSession()`
-  `getTabConfig` function L95-97 — `function getTabConfig(tabId: DashboardTab): DashboardTabConfig | undefined`

#### crates/mimir/frontend/src/features/campaigns/composables/useModalsState.ts

- pub `ModalName` type L7-17 — `= | 'createModal' // Create module modal | 'deleteModule' // Delete module confi...`
- pub `useModalsState` function L23-107 — `function useModalsState()`
-  `openModal` function L53-55 — `function openModal(name: ModalName)`
-  `closeModal` function L60-62 — `function closeModal(name: ModalName)`
-  `toggleModal` function L67-69 — `function toggleModal(name: ModalName)`
-  `closeAllModals` function L74-78 — `function closeAllModals()`
-  `isAnyModalOpen` function L83-85 — `function isAnyModalOpen(): boolean`

### crates/mimir/frontend/src/features/characters/composables/__tests__

> *Semantic summary to be generated by AI agent.*

#### crates/mimir/frontend/src/features/characters/composables/__tests__/useFeatureSelection.test.ts

-  `makeFeature` function L16-18 — `function makeFeature(name: string, source = 'PHB'): FeatureItem`

#### crates/mimir/frontend/src/features/characters/composables/__tests__/useLevelUp.test.ts

-  `makeCharacterClass` function L30-42 — `function makeCharacterClass(overrides: Partial<CharacterClass> = {}): CharacterC...`
-  `makeCharacter` function L44-70 — `function makeCharacter(overrides: Partial<Character> = {}): Character`
-  `makeClassInfo` function L72-84 — `function makeClassInfo(overrides: Partial<ClassInfo> = {}): ClassInfo`
-  `makeContext` function L86-101 — `function makeContext(overrides: Partial<LevelUpContext> = {}): LevelUpContext`
-  `createLevelUp` function L288-291 — `function createLevelUp(charOverrides: Partial<Character> = {})`

#### crates/mimir/frontend/src/features/characters/composables/__tests__/useSpellManagement.test.ts

-  `makeCharacterClass` function L23-35 — `function makeCharacterClass(overrides: Partial<CharacterClass> = {}): CharacterC...`
-  `makeCharacter` function L37-63 — `function makeCharacter(overrides: Partial<Character> = {}): Character`
-  `makeSpell` function L65-76 — `function makeSpell(overrides: Partial<SpellInfo> = {}): SpellInfo`
-  `createSpellManagement` function L78-82 — `function createSpellManagement(charOverrides: Partial<Character> = {})`

### crates/mimir/frontend/src/features/characters/composables

> *Semantic summary to be generated by AI agent.*

#### crates/mimir/frontend/src/features/characters/composables/useFeatureSelection.ts

- pub `FeatureRef` interface L6-9 — `{ name: : string, source: : string }`
- pub `FeatureItem` interface L14-19 — `{ description: : string, cost: : number | string, prereqs: : string, disabled: :...`
- pub `FeatureSelectionOptions` interface L24-33 — `{ maxSlots: : ComputedRef<number> | Ref<number> | number, getKey: : (item: T) =>...`
- pub `FeatureSelectionReturn` interface L38-57 — `{ selected: : Ref<T[]>, isSelected: : (item: T) => boolean, toggle: : (item: T) ...`
- pub `useFeatureSelection` function L81-171 — `function useFeatureSelection( options: FeatureSelectionOptions<T> = {} ): Featur...`
- pub `StringFeatureSelectionReturn` interface L176-185 — `{ selected: : Ref<string[]>, isSelected: : (item: string) => boolean, toggle: : ...`
- pub `useStringFeatureSelection` function L190-251 — `function useStringFeatureSelection( options: { maxSlots?: ComputedRef<number> | ...`
-  `isSelected` function L108-111 — `function isSelected(item: T): boolean`
-  `toggle` function L116-130 — `function toggle(item: T): void`
-  `select` function L135-138 — `function select(item: T): void`
-  `deselect` function L143-150 — `function deselect(item: T): void`
-  `clear` function L155-158 — `function clear(): void`
-  `isSelected` function L214-216 — `function isSelected(item: string): boolean`
-  `toggle` function L218-226 — `function toggle(item: string): void`
-  `deselect` function L228-234 — `function deselect(item: string): void`
-  `clear` function L236-239 — `function clear(): void`

#### crates/mimir/frontend/src/features/characters/composables/useLevelUp.ts

- pub `ClassInfo` interface L29-38 — `{ name: : string, source: : string, hit_die: : number, subclass_level: : number,...`
- pub `LevelUpContext` interface L43-57 — `{ character: : Character, selectedClass: : CharacterClass | null, isNewClass: : ...`
- pub `WizardStep` interface L62-67 — `{ id: : string, title: : string, isVisible: : (context: LevelUpContext) => boole...`
- pub `isAtSubclassLevel` function L76-84 — `function isAtSubclassLevel(context: LevelUpContext): boolean`
- pub `isAtAsiLevel` function L89-92 — `function isAtAsiLevel(context: LevelUpContext): boolean`
- pub `gainsSpellsAtLevel` function L97-101 — `function gainsSpellsAtLevel(context: LevelUpContext): boolean`
- pub `hasFeatureChoices` function L106-139 — `function hasFeatureChoices(context: LevelUpContext): boolean`
- pub `useLevelUp` function L148-443 — `function useLevelUp(character: Ref<Character>)`
- pub `LevelUpComposable` type L445 — `= ReturnType<typeof useLevelUp>`
-  `goToNextStep` function L264-268 — `function goToNextStep()`
-  `goToPreviousStep` function L270-274 — `function goToPreviousStep()`
-  `goToStep` function L276-280 — `function goToStep(index: number)`
-  `selectClass` function L283-327 — `function selectClass(charClass: CharacterClass, isNew: boolean)`
-  `buildRequest` function L330-358 — `function buildRequest(): LevelUpRequest | null`
-  `submit` function L361-392 — `function submit(): Promise<LevelUpResult | null>`
-  `reset` function L395-407 — `function reset()`

#### crates/mimir/frontend/src/features/characters/composables/useSpellManagement.ts

- pub `SpellInfo` interface L16-24 — `{ name: : string, source: : string, level: : number, school: : string | null, ri...`
- pub `SpellcastingStats` interface L29-34 — `{ className: : string, ability: : string, saveDC: : number, attackBonus: : numbe...`
- pub `useSpellManagement` function L39-488 — `function useSpellManagement( character: Ref<Character | null>, characterId: Comp...`
-  `getSchoolName` function L167-180 — `const getSchoolName = (code: string | null): string`
-  `getLevelDisplay` function L182-188 — `const getLevelDisplay = (level: number): string`
-  `toggleSpellDetails` function L190-198 — `const toggleSpellDetails = (name: string, source: string)`
-  `toggleSpellLevel` function L200-207 — `const toggleSpellLevel = (level: number)`
-  `isSpellLevelCollapsed` function L209-211 — `const isSpellLevelCollapsed = (level: number): boolean`
-  `isSpellExpanded` function L213-215 — `const isSpellExpanded = (name: string, source: string): boolean`
-  `getSpellCastingTime` function L217-222 — `const getSpellCastingTime = (spell: SpellInfo): string`
-  `getSpellRange` function L224-234 — `const getSpellRange = (spell: SpellInfo): string`
-  `getSpellComponents` function L236-244 — `const getSpellComponents = (spell: SpellInfo): string`
-  `getSpellDuration` function L246-257 — `const getSpellDuration = (spell: SpellInfo): string`
-  `getSpellDescription` function L259-284 — `const getSpellDescription = (spell: SpellInfo): string`
-  `loadClassSpells` function L287-453 — `const loadClassSpells = ()`
-  `getMaxSpellLevel` function L316-334 — `const getMaxSpellLevel = (className: string, level: number): number`

### crates/mimir/frontend/src/features/modules/composables

> *Semantic summary to be generated by AI agent.*

#### crates/mimir/frontend/src/features/modules/composables/useModuleMaps.ts

- pub `MapSummary` interface L6-20 — `{ id: : string, campaign_id: : string, module_id: : string | null, name: : strin...`
- pub `useModuleMaps` function L32-128 — `function useModuleMaps({ moduleId, campaignId, isDisplayOpen }: UseModuleMapsOpt...`
-  `UseModuleMapsOptions` interface L22-26 — `{ moduleId: : Ref<string>, campaignId: : ComputedRef<string | null | undefined>,...`
-  `loadMaps` function L45-67 — `function loadMaps()`
-  `sendMapToPlayerDisplay` function L70-91 — `function sendMapToPlayerDisplay(map: MapSummary)`
-  `setActiveMap` function L94-96 — `function setActiveMap(mapId: string | null)`

#### crates/mimir/frontend/src/features/modules/composables/useModuleMonsters.ts

- pub `MonsterWithData` interface L7-21 — `{ id: : string, module_id: : string, monster_name: : string | null, monster_sour...`
- pub `getMonsterDisplayName` function L24-30 — `function getMonsterDisplayName(monster: MonsterWithData): string`
- pub `isHomebrewMonster` function L33-35 — `function isHomebrewMonster(monster: MonsterWithData): boolean`
- pub `EncounterGroup` interface L37-40 — `{ encounter_tag: : string | null, monsters: : MonsterWithData[] }`
- pub `useModuleMonsters` function L46-212 — `function useModuleMonsters(moduleId: Ref<string>)`
- pub `formatCreatureType` function L252-283 — `function formatCreatureType(monsterData: any): string`
- pub `formatSpeed` function L288-309 — `function formatSpeed(monsterData: any): string`
- pub `formatModifier` function L314-317 — `function formatModifier(score: number): string`
- pub `formatSaves` function L322-333 — `function formatSaves(monsterData: any): string`
- pub `formatSkills` function L338-349 — `function formatSkills(monsterData: any): string`
- pub `formatSenses` function L354-372 — `function formatSenses(monsterData: any): string`
- pub `formatLanguages` function L377-385 — `function formatLanguages(monsterData: any): string`
- pub `formatDamageVulnerabilities` function L390-396 — `function formatDamageVulnerabilities(monsterData: any): string`
- pub `formatDamageResistances` function L401-411 — `function formatDamageResistances(monsterData: any): string`
- pub `formatDamageImmunities` function L416-426 — `function formatDamageImmunities(monsterData: any): string`
- pub `formatConditionImmunities` function L431-437 — `function formatConditionImmunities(monsterData: any): string`
- pub `formatCR` function L442-448 — `function formatCR(monsterData: any): string`
- pub `formatAC` function L453-473 — `function formatAC(monsterData: any): string`
- pub `formatHP` function L478-488 — `function formatHP(monsterData: any): string`
- pub `formatActionEntries` function L493-508 — `function formatActionEntries(entries: any[]): string`
- pub `getOrdinal` function L513-517 — `function getOrdinal(n: number): string`
- pub `getSpellcasting` function L522-566 — `function getSpellcasting(monsterData: any): string | null`
-  `loadEncounters` function L55-100 — `function loadEncounters(campaignId?: string)`
-  `loadMapMonsterTokens` function L103-153 — `function loadMapMonsterTokens(campaignId?: string): Promise<MonsterWithData[]>`
-  `selectEncounter` function L156-168 — `function selectEncounter(group: EncounterGroup)`
-  `selectMonster` function L171-173 — `function selectMonster(monster: MonsterWithData)`
-  `clearSelectedMonster` function L176-178 — `function clearSelectedMonster()`

#### crates/mimir/frontend/src/features/modules/composables/usePlayNotes.ts

- pub `usePlayNotes` function L15-125 — `function usePlayNotes()`
-  `ApiResponse` interface L5-9 — `{ success: : boolean, data: : T, error: : string }`
-  `toggleNotes` function L25-27 — `function toggleNotes()`
-  `loadNotesForModule` function L30-54 — `function loadNotesForModule(moduleId: string): Promise<void>`
-  `saveNotes` function L57-83 — `function saveNotes(): Promise<void>`
-  `handleNotesInput` function L86-96 — `function handleNotesInput()`
-  `cleanup` function L99-104 — `function cleanup()`

### crates/mimir/frontend/src/features/sources/composables/catalog

> *Semantic summary to be generated by AI agent.*

#### crates/mimir/frontend/src/features/sources/composables/catalog/useActions.ts

- pub `ActionSummary` interface L3-9 — `{ name: : string, source: : string, time: : string, description: : string, see_a...`
- pub `ActionFilters` interface L11-15 — `{ query: : string, sources: : string[], time_filter: : string }`
- pub `useActions` function L17-37 — `function useActions()`

#### crates/mimir/frontend/src/features/sources/composables/catalog/useBackgrounds.ts

- pub `BackgroundSummary` interface L3-10 — `{ name: : string, source: : string, skills: : string, languages: : string, tools...`
- pub `BackgroundFilters` interface L12-16 — `{ query: : string, sources: : string[], has_tools: : boolean }`
- pub `useBackgrounds` function L18-39 — `function useBackgrounds()`

#### crates/mimir/frontend/src/features/sources/composables/catalog/useCatalogSearch.ts

- pub `CatalogConfig` interface L5-20 — `{ name: : string, initializeCommand: : string, searchCommand: : string, detailsC...`
- pub `CatalogSearchResult` interface L22-30 — `{ isInitialized: : Ref<boolean>, isLoading: : Ref<boolean>, error: : Ref<string ...`
- pub `useCatalogSearch` function L36-131 — `function useCatalogSearch( config: CatalogConfig<TSummary, TDetails, TFilters> )...`
-  `initialize` function L48-65 — `function initialize(): Promise<void>`
-  `search` function L67-104 — `function search(filters: TFilters): Promise<TSummary[]>`
-  `getDetails` function L106-120 — `function getDetails(name: string, source: string): Promise<TDetails | null>`
-  `getEffectiveSources` function L139-158 — `function getEffectiveSources(filterSources: unknown): string[] | null`
-  `transformDefaultFilters` function L168-194 — `function transformDefaultFilters(filters: Record<string, unknown>): Record<strin...`

#### crates/mimir/frontend/src/features/sources/composables/catalog/useClasses.ts

- pub `ClassSummary` interface L5-19 — `{ name: : string, source: : string, page: : number, hitDice: : string, proficien...`
- pub `ClassFilters` interface L21-26 — `{ name: : string, sources: : string[], has_spellcasting: : boolean, primary_abil...`
- pub `Subclass` interface L28-41 — `{ name: : string, source: : string, className: : string, classSource: : string, ...`
- pub `ClassFeature` interface L43-51 — `{ name: : string, source: : string, class_name: : string, class_source: : string...`
- pub `SubclassFeature` interface L53-63 — `{ name: : string, source: : string, class_name: : string, class_source: : string...`
- pub `ClassFluff` interface L65-70 — `{ name: : string, source: : string, entries: : any[], images: : any[] }`
- pub `SubclassFluff` interface L72-80 — `{ name: : string, short_name: : string, source: : string, class_name: : string, ...`
- pub `ClassWithDetails` interface L82-89 — `{ class: : Class, subclasses: : Subclass[], features: : ClassFeature[], subclass...`
- pub `Class` interface L91-106 — `{ name: : string, source: : string, page: : number, hd: : any, proficiency: : an...`
- pub `useClasses` function L108-242 — `function useClasses()`
-  `getClassDetails` function L122-144 — `function getClassDetails(name: string, source: string): Promise<ClassWithDetails...`
-  `getSubclassDetails` function L146-161 — `function getSubclassDetails(subclassName: string, className: string, source: str...`
-  `getClassSubclasses` function L163-176 — `function getClassSubclasses(className: string): Promise<Subclass[]>`
-  `searchClassesWithSubclasses` function L180-227 — `function searchClassesWithSubclasses(filters?: ClassFilters): Promise<ClassSumma...`

#### crates/mimir/frontend/src/features/sources/composables/catalog/useConditions.ts

- pub `ConditionSummary` interface L3-8 — `{ name: : string, source: : string, item_type: : 'Condition' | 'Disease', descri...`
- pub `ConditionWithDetails` interface L10-17 — `{ item: : { type: 'Condition' | 'Disease' Condition?: any Disease?: any }, fluff...`
- pub `ConditionFilters` interface L19-23 — `{ query: : string, sources: : string[], type_filter: : string }`
- pub `useConditions` function L25-45 — `function useConditions()`

#### crates/mimir/frontend/src/features/sources/composables/catalog/useCults.ts

- pub `CultSummary` interface L4-7 — `{ name: : string, source: : string }`
- pub `CultFilters` interface L9-12 — `{ query: : string, sources: : string[] }`
- pub `Cult` interface L14-22 — `{ name: : string, source: : string, page: : number, entries: : any[], cultists: ...`
- pub `useCults` function L24-97 — `function useCults()`
-  `initializeCultCatalog` function L30-32 — `function initializeCultCatalog()`
-  `searchCults` function L34-64 — `function searchCults(filters: CultFilters = {}): Promise<CultSummary[]>`
-  `getCultDetails` function L66-80 — `function getCultDetails(name: string, source: string): Promise<Cult | null>`
-  `getBoonDetails` function L83-85 — `function getBoonDetails(name: string, source: string): Promise<Cult | null>`

#### crates/mimir/frontend/src/features/sources/composables/catalog/useDeities.ts

- pub `DeitySummary` interface L4-12 — `{ name: : string, source: : string, title: : string, pantheon: : string, alignme...`
- pub `Deity` interface L14-28 — `{ name: : string, source: : string, page: : number, title: : string, pantheon: :...`
- pub `DeityFilters` interface L30-34 — `{ query: : string, sources: : string[], pantheon: : string }`
- pub `useDeities` function L36-104 — `function useDeities()`
-  `initializeDeityCatalog` function L42-44 — `function initializeDeityCatalog()`
-  `searchDeities` function L46-77 — `function searchDeities(filters: DeityFilters): Promise<DeitySummary[]>`
-  `getDeityDetails` function L79-93 — `function getDeityDetails(name: string, source: string): Promise<Deity | null>`

#### crates/mimir/frontend/src/features/sources/composables/catalog/useFeats.ts

- pub `FeatSummary` interface L3-9 — `{ name: : string, source: : string, page: : number, prerequisites: : string, bri...`
- pub `FeatFilters` interface L11-15 — `{ query: : string, sources: : string[], has_prerequisites: : boolean }`
- pub `Feat` interface L17-37 — `{ name: : string, source: : string, page: : number, srd: : boolean, entries: : a...`
- pub `useFeats` function L39-77 — `function useFeats()`
-  `searchFeats` function L40-58 — `function searchFeats(params: FeatFilters = {}): Promise<FeatSummary[]>`
-  `getFeatDetails` function L60-71 — `function getFeatDetails(name: string, source: string): Promise<Feat | null>`

#### crates/mimir/frontend/src/features/sources/composables/catalog/useItems.ts

- pub `ItemSummary` interface L5-17 — `{ name: : string, itemType: : string, typeName: : string, source: : string, rari...`
- pub `ItemFilters` interface L19-26 — `{ query: : string, sources: : string[], types: : string[], rarities: : string[],...`
- pub `Item` interface L28-36 — `{ name: : string, source: : string, type: : string, rarity: : string, weight: : ...`
- pub `useItems` function L38-121 — `function useItems()`
-  `getEffectiveSources` function L46-59 — `const getEffectiveSources = (filterSources?: string[]): string[] | null`
-  `initializeItemCatalog` function L61-63 — `function initializeItemCatalog()`
-  `searchItems` function L65-94 — `function searchItems(filters: ItemFilters): Promise<ItemSummary[]>`
-  `getItemDetails` function L96-110 — `function getItemDetails(name: string, source: string): Promise<Item | null>`

#### crates/mimir/frontend/src/features/sources/composables/catalog/useLanguages.ts

- pub `LanguageSummary` interface L3-9 — `{ name: : string, source: : string, language_type: : string, script: : string, t...`
- pub `LanguageFilters` interface L11-16 — `{ query: : string, sources: : string[], types: : string[], scripts: : string[] }`
- pub `Language` interface L18-32 — `{ name: : string, source: : string, page: : number, language_type: : string, scr...`
- pub `useLanguages` function L34-97 — `function useLanguages()`
-  `initializeLanguageCatalog` function L35-37 — `function initializeLanguageCatalog()`
-  `searchLanguages` function L39-58 — `function searchLanguages(filters: LanguageFilters = {}): Promise<LanguageSummary...`
-  `getLanguageDetails` function L60-71 — `function getLanguageDetails(name: string, source: string): Promise<Language | nu...`
-  `getLanguageTypes` function L73-83 — `function getLanguageTypes(): Promise<string[]>`
-  `getLanguageScripts` function L85-88 — `function getLanguageScripts(): Promise<string[]>`

#### crates/mimir/frontend/src/features/sources/composables/catalog/useMonsters.ts

- pub `MonsterSummary` interface L5-26 — `{ name: : string, size: : string, type: : string, alignment: : string, cr: : str...`
- pub `MonsterFilters` interface L28-35 — `{ query: : string, sources: : string[], types: : string[], sizes: : string[], mi...`
- pub `Monster` interface L37-69 — `{ name: : string, source: : string, size: : string[], type: : unknown, alignment...`
- pub `useMonsters` function L71-161 — `function useMonsters()`
-  `getEffectiveSources` function L79-95 — `const getEffectiveSources = (filterSources?: string[]): string[] | null`
-  `initializeMonsterCatalog` function L97-99 — `function initializeMonsterCatalog()`
-  `searchMonsters` function L101-134 — `function searchMonsters(filters: MonsterFilters): Promise<MonsterSummary[]>`
-  `getMonsterDetails` function L136-150 — `function getMonsterDetails(name: string, source: string): Promise<Monster | null...`

#### crates/mimir/frontend/src/features/sources/composables/catalog/useObjects.ts

- pub `ObjectSummary` interface L4-8 — `{ name: : string, source: : string, object_type: : string }`
- pub `ObjectFilters` interface L10-14 — `{ query: : string, sources: : string[], object_type: : string }`
- pub `DndObject` interface L16-34 — `{ name: : string, source: : string, page: : number, objectType: : string, size: ...`
- pub `useObjects` function L36-104 — `function useObjects()`
-  `initializeObjectCatalog` function L42-44 — `function initializeObjectCatalog()`
-  `searchObjects` function L46-77 — `function searchObjects(filters: ObjectFilters = {}): Promise<ObjectSummary[]>`
-  `getObjectDetails` function L79-93 — `function getObjectDetails(name: string, source: string): Promise<DndObject | nul...`

#### crates/mimir/frontend/src/features/sources/composables/catalog/useOptionalFeatures.ts

- pub `OptionalFeatureSummary` interface L4-8 — `{ name: : string, source: : string, feature_type: : string }`
- pub `OptionalFeatureFilters` interface L10-14 — `{ query: : string, sources: : string[], feature_type: : string }`
- pub `OptionalFeature` interface L16-27 — `{ name: : string, source: : string, page: : number, feature_type: : string, entr...`
- pub `useOptionalFeatures` function L29-97 — `function useOptionalFeatures()`
-  `initializeOptionalFeatureCatalog` function L35-37 — `function initializeOptionalFeatureCatalog()`
-  `searchOptionalFeatures` function L39-70 — `function searchOptionalFeatures(filters: OptionalFeatureFilters = {}): Promise<O...`
-  `getOptionalFeatureDetails` function L72-86 — `function getOptionalFeatureDetails(name: string, source: string): Promise<Option...`

#### crates/mimir/frontend/src/features/sources/composables/catalog/usePsionics.ts

- pub `PsionicSummary` interface L4-9 — `{ name: : string, source: : string, psionic_type: : string, psionic_order: : str...`
- pub `PsionicFilters` interface L11-16 — `{ query: : string, sources: : string[], psionic_type: : string, psionic_order: :...`
- pub `Psionic` interface L18-27 — `{ name: : string, source: : string, psionic_type: : string, psionic_order: : str...`
- pub `PsionicMode` interface L29-40 — `{ name: : string, cost: : { min: number max?: number }, entries: : any[], concen...`
- pub `usePsionics` function L42-111 — `function usePsionics()`
-  `initializePsionicCatalog` function L48-50 — `function initializePsionicCatalog()`
-  `searchPsionics` function L52-84 — `function searchPsionics(filters: PsionicFilters = {}): Promise<PsionicSummary[]>`
-  `getPsionicDetails` function L86-100 — `function getPsionicDetails(name: string, source: string): Promise<Psionic | null...`

#### crates/mimir/frontend/src/features/sources/composables/catalog/useRaces.ts

- pub `RaceSummary` interface L5-14 — `{ name: : string, source: : string, size: : string, speed: : number, abilityBonu...`
- pub `RaceFilters` interface L16-22 — `{ query: : string, sources: : string[], sizes: : string[], has_darkvision: : boo...`
- pub `Race` interface L24-48 — `{ name: : string, source: : string, page: : number, size: : string[], speed: : a...`
- pub `Subrace` interface L50-68 — `{ name: : string, source: : string, page: : number, raceName: : string, raceSour...`
- pub `RaceWithDetails` interface L70-75 — `{ race: : Race, subrace: : Subrace, relatedSubraces: : Subrace[], fluff: : any }`
- pub `useRaces` function L77-121 — `function useRaces()`
-  `getRaceDetails` function L91-110 — `function getRaceDetails(name: string, source: string): Promise<RaceWithDetails |...`

#### crates/mimir/frontend/src/features/sources/composables/catalog/useRewards.ts

- pub `Reward` interface L4-17 — `{ name: : string, source: : string, page: : number, reward_type: : string, entri...`
- pub `RewardSummary` interface L19-23 — `{ name: : string, source: : string, reward_type: : string }`
- pub `RewardFilters` interface L25-29 — `{ query: : string, sources: : string[], reward_type: : string }`
- pub `useRewards` function L31-99 — `function useRewards()`
-  `initializeRewardCatalog` function L37-39 — `function initializeRewardCatalog()`
-  `searchRewards` function L41-72 — `function searchRewards(filters: RewardFilters = {}): Promise<RewardSummary[]>`
-  `getRewardDetails` function L74-88 — `function getRewardDetails(name: string, source: string): Promise<Reward | null>`

#### crates/mimir/frontend/src/features/sources/composables/catalog/useSpells.ts

- pub `SpellSummary` interface L3-15 — `{ name: : string, level: : number, school: : string, source: : string, concentra...`
- pub `SpellFilters` interface L17-25 — `{ query: : string, sources: : string[], levels: : number[], schools: : string[],...`
- pub `Spell` interface L27-44 — `{ name: : string, source: : string, level: : number, school: : string, time: : u...`
- pub `useSpells` function L46-71 — `function useSpells()`

#### crates/mimir/frontend/src/features/sources/composables/catalog/useTables.ts

- pub `Table` interface L4-20 — `{ name: : string, source: : string, page: : number, caption: : string, col_label...`
- pub `TableSummary` interface L22-26 — `{ name: : string, source: : string, caption: : string }`
- pub `TableFilters` interface L28-31 — `{ query: : string, sources: : string[] }`
- pub `useTables` function L33-100 — `function useTables()`
-  `initializeTableCatalog` function L39-41 — `function initializeTableCatalog()`
-  `searchTables` function L43-73 — `function searchTables(filters: TableFilters = {}): Promise<TableSummary[]>`
-  `getTableDetails` function L75-89 — `function getTableDetails(name: string, source: string): Promise<Table | null>`

#### crates/mimir/frontend/src/features/sources/composables/catalog/useTraps.ts

- pub `TrapSummary` interface L5-10 — `{ name: : string, source: : string, trap_type: : string, category: : string }`
- pub `TrapFilters` interface L12-17 — `{ query: : string, sources: : string[], categories: : string[], trap_types: : st...`
- pub `TrapOrHazard` interface L19-28 — `{ name: : string, source: : string, page: : number, trap_haz_type: : string, ent...`
- pub `useTraps` function L30-79 — `function useTraps()`
-  `getEffectiveSources` function L33-46 — `const getEffectiveSources = (filterSources?: string[]): string[] | null`
-  `getTrapTypes` function L59-69 — `function getTrapTypes(): Promise<string[]>`

#### crates/mimir/frontend/src/features/sources/composables/catalog/useVariantRules.ts

- pub `VariantRule` interface L4-10 — `{ name: : string, source: : string, rule_type: : string, page: : number, entries...`
- pub `VariantRuleSummary` interface L12-16 — `{ name: : string, source: : string, rule_type: : string }`
- pub `VariantRuleFilters` interface L18-22 — `{ query: : string, sources: : string[], rule_type: : string }`
- pub `useVariantRules` function L24-92 — `function useVariantRules()`
-  `initializeVariantRuleCatalog` function L30-32 — `function initializeVariantRuleCatalog()`
-  `searchVariantRules` function L34-65 — `function searchVariantRules(filters: VariantRuleFilters = {}): Promise<VariantRu...`
-  `getVariantRuleDetails` function L67-81 — `function getVariantRuleDetails(name: string, source: string): Promise<VariantRul...`

#### crates/mimir/frontend/src/features/sources/composables/catalog/useVehicles.ts

- pub `Vehicle` interface L4-24 — `{ name: : string, source: : string, vehicle_type: : string, size: : string, page...`
- pub `VehicleSummary` interface L26-30 — `{ name: : string, source: : string, vehicle_type: : string }`
- pub `VehicleFilters` interface L32-36 — `{ query: : string, sources: : string[], vehicle_type: : string }`
- pub `useVehicles` function L38-106 — `function useVehicles()`
-  `initializeVehicleCatalog` function L44-46 — `function initializeVehicleCatalog()`
-  `searchVehicles` function L48-79 — `function searchVehicles(filters: VehicleFilters = {}): Promise<VehicleSummary[]>`
-  `getVehicleDetails` function L81-95 — `function getVehicleDetails(name: string, source: string): Promise<Vehicle | null...`

### crates/mimir/frontend/src/features/sources/composables

> *Semantic summary to be generated by AI agent.*

#### crates/mimir/frontend/src/features/sources/composables/useBookContent.ts

- pub `useBookContent` function L8-88 — `function useBookContent()`
-  `loadBookContent` function L15-36 — `function loadBookContent(book: BookInfo)`
-  `getSectionName` function L39-48 — `function getSectionName(section: BookSection): string`
-  `jumpToEntry` function L51-60 — `function jumpToEntry(sectionIndex: number, entryId: string)`
-  `getCurrentSection` function L63-68 — `function getCurrentSection(): BookSection | null`
-  `clearContent` function L71-75 — `function clearContent()`

#### crates/mimir/frontend/src/features/sources/composables/useBookLibrary.ts

- pub `useBookLibrary` function L17-182 — `function useBookLibrary()`
-  `CatalogSourceInfo` interface L10-15 — `{ id: : string, name: : string, enabled: : boolean, imported_at: : string }`
-  `loadLibraryBooks` function L28-48 — `function loadLibraryBooks()`
-  `loadCatalogSources` function L51-73 — `function loadCatalogSources()`
-  `addBook` function L76-132 — `function addBook(): Promise<boolean>`
-  `removeBook` function L135-163 — `function removeBook(book: BookInfo): Promise<boolean>`
-  `selectBook` function L166-168 — `function selectBook(book: BookInfo | null)`

#### crates/mimir/frontend/src/features/sources/composables/useBookNavigation.ts

- pub `useBookNavigation` function L5-72 — `function useBookNavigation()`
-  `getSubEntries` function L8-46 — `function getSubEntries(section: BookSection): SubEntry[]`
-  `processEntries` function L11-39 — `function processEntries(items: any[], level: number = 0): SubEntry[]`
-  `scrollToTop` function L49-54 — `function scrollToTop()`
-  `scrollToElement` function L57-65 — `function scrollToElement(elementId: string, smooth: boolean = true)`

#### crates/mimir/frontend/src/features/sources/composables/useCrossReferences.ts

- pub `useCrossReferences` function L10-373 — `function useCrossReferences()`
-  `lookupReference` function L62-201 — `function lookupReference(refType: string, refName: string, refSource?: string, c...`
-  `handleCrossRefHover` function L204-235 — `function handleCrossRefHover(event: MouseEvent)`
-  `handleCrossRefClick` function L238-281 — `function handleCrossRefClick(event: MouseEvent)`
-  `hideTooltip` function L284-287 — `function hideTooltip()`
-  `closeModal` function L290-292 — `function closeModal()`
-  `formatTooltipContent` function L295-339 — `function formatTooltipContent(type: ReferenceType, data: any): string`
-  `formatModalContent` function L342-349 — `function formatModalContent(type: ReferenceType, data: any): Promise<string>`
-  `clearCache` function L352-354 — `function clearCache()`

#### crates/mimir/frontend/src/features/sources/composables/useImageLoader.ts

- pub `useImageLoader` function L5-37 — `function useImageLoader()`
-  `loadBookImage` function L8-32 — `function loadBookImage(bookId: string, imagePath: string, imageId: string)`

#### crates/mimir/frontend/src/features/sources/composables/useSearch.ts

- pub `useSearch` function L37-807 — `function useSearch(initialCategory: string, initialSources: MaybeRef<string[]>)`
-  `performSearch` function L92-107 — `function performSearch()`
-  `debouncedSearch` function L109-116 — `function debouncedSearch()`
-  `handleSort` function L118-125 — `function handleSort(column: string)`
-  `updateMonsterFilters` function L127-129 — `function updateMonsterFilters(newFilters: { sizes?: string[], types?: string[] }...`
-  `selectSpell` function L131-143 — `function selectSpell(spell: SpellSummary)`
-  `selectItem` function L145-158 — `function selectItem(item: ItemSummary)`
-  `selectMonster` function L160-173 — `function selectMonster(monster: MonsterSummary)`
-  `selectClass` function L175-214 — `function selectClass(classItem: ClassSummary)`
-  `selectFeat` function L216-229 — `function selectFeat(feat: FeatSummary)`
-  `selectRace` function L231-273 — `function selectRace(race: RaceSummary)`
-  `selectBackground` function L275-288 — `function selectBackground(background: BackgroundSummary)`
-  `selectAction` function L290-303 — `function selectAction(action: ActionSummary)`
-  `selectCondition` function L305-319 — `function selectCondition(condition: ConditionSummary)`
-  `selectOption` function L321-335 — `function selectOption(option: OptionalFeatureSummary)`
-  `selectDeity` function L337-351 — `function selectDeity(deity: DeitySummary)`
-  `selectObject` function L353-367 — `function selectObject(obj: ObjectSummary)`
-  `selectTrap` function L369-383 — `function selectTrap(trap: TrapSummary)`
-  `selectLanguage` function L385-399 — `function selectLanguage(lang: LanguageSummary)`
-  `selectReward` function L401-415 — `function selectReward(reward: RewardSummary)`
-  `selectTable` function L417-431 — `function selectTable(table: TableSummary)`
-  `selectVariantRule` function L433-447 — `function selectVariantRule(rule: any)`
-  `selectVehicle` function L449-463 — `function selectVehicle(vehicle: any)`
-  `selectCult` function L465-492 — `function selectCult(item: any)`
-  `selectPsionic` function L494-508 — `function selectPsionic(psionic: PsionicSummary)`
-  `closeModal` function L510-516 — `function closeModal(index?: number)`
-  `handleReferenceClick` function L518-735 — `function handleReferenceClick(event: { type: string; name: string; source?: stri...`
-  `initialize` function L737-740 — `function initialize()`

#### crates/mimir/frontend/src/features/sources/composables/useSourceSearch.ts

- pub `useSourceSearch` function L14-154 — `function useSourceSearch()`
-  `initializeForContentType` function L31-52 — `function initializeForContentType()`
-  `search` function L55-113 — `function search(sources: string[] = [])`
-  `clearSearch` function L116-122 — `function clearSearch()`
-  `getContentDetails` function L125-136 — `function getContentDetails(type: string, name: string, source: string)`

### crates/mimir/frontend/src/features/sources/formatters

> *Semantic summary to be generated by AI agent.*

#### crates/mimir/frontend/src/features/sources/formatters/actionFormatter.ts

- pub `formatActionDetails` function L3-133 — `function formatActionDetails(action: any): Promise<string>`
-  `formatTime` function L135-168 — `function formatTime(time: any): string`
-  `formatEntry` function L170-211 — `function formatEntry(entry: any): string`

#### crates/mimir/frontend/src/features/sources/formatters/backgroundFormatter.ts

- pub `formatBackgroundDetails` function L3-108 — `function formatBackgroundDetails(background: any): Promise<string>`
-  `formatProficiencies` function L110-142 — `function formatProficiencies(proficiencies: any[], type: string): string`
-  `formatEquipmentItem` function L145-181 — `function formatEquipmentItem(eq: any): string`
-  `formatEquipment` function L183-225 — `function formatEquipment(equipment: any[]): string`
-  `formatEntry` function L227-320 — `function formatEntry(entry: any): Promise<string>`

#### crates/mimir/frontend/src/features/sources/formatters/classFormatterEnhanced.ts

- pub `formatClassDetails` function L5-28 — `function formatClassDetails( classData: ClassWithDetails | ClassSummary | Subcla...`
- pub `formatTable` function L810-910 — `function formatTable(table: any): string`
- pub `formatClassFeatureDetails` function L1062-1093 — `function formatClassFeatureDetails(feature: any, className?: string, level?: str...`
-  `formatClassSummary` function L30-118 — `function formatClassSummary(classData: ClassSummary | any): string`
-  `formatClassWithSelectedSubclass` function L124-396 — `function formatClassWithSelectedSubclass( classData: ClassWithDetails | ClassSum...`
-  `formatFullClassDetails` function L398-614 — `function formatFullClassDetails(classDetails: ClassWithDetails): Promise<string>`
-  `formatEntry` function L616-648 — `function formatEntry(entry: any): string`
-  `formatSubclass` function L650-767 — `function formatSubclass(subclass: Subclass, fluff?: SubclassFluff, introFeature?...`
-  `formatFeaturesTable` function L769-809 — `function formatFeaturesTable(features: ClassFeature[]): string`
-  `formatAbilityScore` function L912-922 — `function formatAbilityScore(ability: string): string`
-  `formatOrdinal` function L924-928 — `function formatOrdinal(n: number): string`
-  `formatSubclassDetails` function L930-1057 — `function formatSubclassDetails(subclass: Subclass): Promise<string>`

#### crates/mimir/frontend/src/features/sources/formatters/conditionFormatter.ts

- pub `formatConditionDetails` function L29-348 — `function formatConditionDetails(details: ConditionWithDetails | ConditionSummary...`
-  `formatConditionSummary` function L4-27 — `function formatConditionSummary(summary: ConditionSummary): string`

#### crates/mimir/frontend/src/features/sources/formatters/cultFormatter.ts

- pub `formatCultDetails` function L24-84 — `function formatCultDetails(cult: Cult): string`
- pub `formatBoonDetails` function L86-138 — `function formatBoonDetails(boon: Boon): string`
-  `Cult` interface L3-12 — `{ name: : string, source: : string, cult_type: : string, page: : number, entries...`
-  `Boon` interface L14-22 — `{ name: : string, source: : string, boon_type: : string, page: : number, entries...`
-  `getCultBoonStyles` function L140-274 — `function getCultBoonStyles(): string`

#### crates/mimir/frontend/src/features/sources/formatters/deityFormatter.ts

- pub `formatDeityContent` function L4-405 — `function formatDeityContent(deity: Deity): string`
-  `formatAlignment` function L407-429 — `function formatAlignment(alignment: string[] | string): string`
-  `getAlignmentClass` function L431-447 — `function getAlignmentClass(alignment: string[] | string): string`
-  `getDomainClass` function L449-467 — `function getDomainClass(domain: string): string`

#### crates/mimir/frontend/src/features/sources/formatters/featFormatter.ts

- pub `formatFeatDetails` function L4-156 — `function formatFeatDetails(feat: Feat | FeatSummary): Promise<string>`
-  `formatProficiencies` function L159-183 — `function formatProficiencies(proficiencies: any[]): string`
-  `formatAdditionalSpells` function L185-226 — `function formatAdditionalSpells(spells: any[]): string`
-  `formatSpellList` function L228-234 — `function formatSpellList(label: string, spells: string[]): string`

#### crates/mimir/frontend/src/features/sources/formatters/itemFormatterEnhanced.ts

- pub `formatItemDetails` function L60-70 — `function formatItemDetails(item: any): Promise<string>`
-  `ItemDetails` interface L4-58 — `{ name: : string, type: : string, typeName: : string, rarity: : string, value: :...`
-  `formatItemSummary` function L72-136 — `function formatItemSummary(item: any): string`
-  `formatFullItemDetails` function L138-361 — `function formatFullItemDetails(item: ItemDetails): Promise<string>`
-  `formatCost` function L362-370 — `function formatCost(value: number): string`
-  `formatRarity` function L372-374 — `function formatRarity(rarity: string): string`
-  `formatDamageType` function L376-396 — `function formatDamageType(damageType?: string): string`
-  `formatWeaponProperties` function L398-472 — `function formatWeaponProperties(properties: string[]): string`
-  `formatItemTypeInfo` function L474-485 — `function formatItemTypeInfo(item: ItemDetails): string`
-  `formatWeaponCategory` function L487-489 — `function formatWeaponCategory(category: string): string`
-  `formatSpellcastingFocus` function L491-498 — `function formatSpellcastingFocus(focusType: string): string`
-  `formatItemType` function L500-551 — `function formatItemType(type: string | undefined | null): string`
-  `formatTier` function L553-555 — `function formatTier(tier: string): string`
-  `formatMiscTag` function L557-562 — `function formatMiscTag(tag: string): string`
-  `formatMagicItemSection` function L563-599 — `function formatMagicItemSection(item: ItemDetails): string`
-  `formatContainerSection` function L601-641 — `function formatContainerSection(item: ItemDetails): string`
-  `formatLightSection` function L643-674 — `function formatLightSection(item: ItemDetails): string`
-  `formatPackContents` function L676-692 — `function formatPackContents(contents: any[]): string`
-  `formatAmmoType` function L694-699 — `function formatAmmoType(ammoType: string): string`

#### crates/mimir/frontend/src/features/sources/formatters/languageFormatter.ts

- pub `formatLanguageDetails` function L4-95 — `function formatLanguageDetails(language: Language): Promise<string>`
-  `formatLanguageType` function L97-108 — `function formatLanguageType(type: string | undefined): string`
-  `formatSpeakers` function L110-118 — `function formatSpeakers(speakers: string[]): string`
-  `formatEntry` function L120-177 — `function formatEntry(entry: any): string`
-  `formatTable` function L179-210 — `function formatTable(table: any): string`

#### crates/mimir/frontend/src/features/sources/formatters/modalFormatters.ts

- pub `renderModalContent` function L10-66 — `function renderModalContent(data: any): Promise<string>`
-  `renderSpellContent` function L68-129 — `function renderSpellContent(data: any): string`
-  `renderItemContent` function L131-162 — `function renderItemContent(data: any): string`
-  `renderMonsterContent` function L164-206 — `function renderMonsterContent(data: any): string`
-  `renderConditionContent` function L208-256 — `function renderConditionContent(data: any): string`
-  `renderActionContent` function L258-305 — `function renderActionContent(data: any): string`
-  `renderFeatContent` function L307-361 — `function renderFeatContent(data: any): string`
-  `renderBackgroundContent` function L363-430 — `function renderBackgroundContent(data: any): string`
-  `renderRaceContent` function L432-483 — `function renderRaceContent(data: any): string`
-  `renderClassContent` function L485-547 — `function renderClassContent(data: any): string`
-  `renderClassFeatureContent` function L549-644 — `function renderClassFeatureContent(data: any): string`
-  `renderSubclassContent` function L646-696 — `function renderSubclassContent(data: any): string`
-  `renderSubclassFeatureContent` function L698-803 — `function renderSubclassFeatureContent(data: any): string`
-  `renderTableEntry` function L805-837 — `function renderTableEntry(table: any): string`
-  `renderGenericContent` function L839-856 — `function renderGenericContent(data: any): string`
-  `formatSpellSchool` function L873-879 — `function formatSpellSchool(school: any): string`
-  `formatSpellTime` function L881-902 — `function formatSpellTime(time: any): string`
-  `formatSpellRange` function L904-937 — `function formatSpellRange(range: any): string`
-  `formatSpellComponents` function L939-961 — `function formatSpellComponents(components: any): string`
-  `formatSpellDuration` function L963-972 — `function formatSpellDuration(duration: any): string`
-  `formatSingleDuration` function L974-1007 — `function formatSingleDuration(d: any): string`
-  `formatCurrency` function L1018-1029 — `function formatCurrency(copperValue: number): string`
-  `getOrdinalSuffix` function L1031-1045 — `function getOrdinalSuffix(num: number): string`

#### crates/mimir/frontend/src/features/sources/formatters/monsterFormatterEnhanced.ts

- pub `formatMonsterDetails` function L51-218 — `function formatMonsterDetails(monster: Monster | MonsterSummary): Promise<string...`
-  `Monster` interface L6-49 — `{ name: : string, size: : string | string[], type: : string | any, alignment: : ...`
-  `formatMonsterSummary` function L220-260 — `function formatMonsterSummary(monster: MonsterSummary): string`
-  `formatSize` function L262-273 — `function formatSize(size: string | string[]): string`
-  `formatSpeed` function L275-288 — `function formatSpeed(speed: any): string`
-  `formatAbilityScore` function L290-295 — `function formatAbilityScore(score?: number): string`
-  `formatSaves` function L297-308 — `function formatSaves(saves: any): string`
-  `formatSkills` function L310-322 — `function formatSkills(skills: any): string`
-  `formatAction` function L324-345 — `function formatAction(action: any): string`
-  `formatCreatureType` function L347-359 — `function formatCreatureType(type: any): string`
-  `formatAlignment` function L361-398 — `function formatAlignment(alignment: any): string`
-  `formatAC` function L400-410 — `function formatAC(ac: any): string`
-  `formatHP` function L412-420 — `function formatHP(hp: any): string`

#### crates/mimir/frontend/src/features/sources/formatters/objectFormatter.ts

- pub `formatObjectDetails` function L3-368 — `function formatObjectDetails(obj: any): Promise<string>`
-  `formatObjectType` function L370-378 — `function formatObjectType(type: string): string`
-  `formatHP` function L380-398 — `function formatHP(hp: any): string`
-  `formatSizes` function L400-412 — `function formatSizes(sizes: string[]): string`
-  `formatAC` function L414-423 — `function formatAC(ac: any): string`

#### crates/mimir/frontend/src/features/sources/formatters/optionalFeatureFormatter.ts

- pub `formatOptionalFeatureDetails` function L3-350 — `function formatOptionalFeatureDetails(feature: any): string`
-  `getFeatureTypeDisplay` function L352-373 — `function getFeatureTypeDisplay(type: string): string`
-  `formatPrerequisite` function L375-429 — `function formatPrerequisite(prereq: any): string`
-  `formatEntry` function L431-550 — `function formatEntry(entry: any): string`
-  `formatTable` function L552-583 — `function formatTable(table: any): string`
-  `formatAdditionalSpells` function L585-625 — `function formatAdditionalSpells(spellGroup: any): string`
-  `formatSpellLevel` function L627-634 — `function formatSpellLevel(level: string, spells: string[]): string`

#### crates/mimir/frontend/src/features/sources/formatters/psionicFormatter.ts

- pub `formatPsionicDetails` function L3-90 — `function formatPsionicDetails(psionic: any): string`
-  `formatConcentrationDuration` function L92-111 — `function formatConcentrationDuration(conc: { duration: number, unit: string }): ...`

#### crates/mimir/frontend/src/features/sources/formatters/raceFormatter.ts

- pub `formatRaceDetails` function L4-13 — `function formatRaceDetails(raceData: RaceWithDetails | RaceSummary): Promise<str...`
-  `formatRaceSummary` function L15-51 — `function formatRaceSummary(raceSummary: RaceSummary): string`
-  `formatFullRaceDetails` function L53-275 — `function formatFullRaceDetails(details: RaceWithDetails): Promise<string>`
-  `getSizeName` function L277-287 — `function getSizeName(size: string): string`
-  `formatSpeed` function L289-306 — `function formatSpeed(speed: any): string`
-  `formatAbilityScores` function L308-345 — `function formatAbilityScores(abilities: any[]): string`
-  `formatAge` function L347-359 — `function formatAge(age: any): string`
-  `formatLanguages` function L361-380 — `function formatLanguages(languages: any[]): string`
-  `formatResistances` function L382-398 — `function formatResistances(resistances: any[]): string`
-  `formatAbilityScoresSimple` function L400-419 — `function formatAbilityScoresSimple(abilities: any[]): string`
-  `formatWeaponProficiencies` function L421-442 — `function formatWeaponProficiencies(proficiencies: any[]): string`
-  `formatArmorProficiencies` function L444-464 — `function formatArmorProficiencies(proficiencies: any[]): string`
-  `formatToolProficiencies` function L466-494 — `function formatToolProficiencies(proficiencies: any[]): string`

#### crates/mimir/frontend/src/features/sources/formatters/rewardFormatter.ts

- pub `formatRewardDetails` function L4-200 — `function formatRewardDetails(reward: Reward): Promise<string>`
-  `formatRewardType` function L202-210 — `function formatRewardType(type: string): string`
-  `getRewardTypeClass` function L212-219 — `function getRewardTypeClass(type: string): string`
-  `formatEntry` function L221-278 — `function formatEntry(entry: any): string`
-  `formatTable` function L280-311 — `function formatTable(table: any): string`
-  `formatSpellList` function L313-328 — `function formatSpellList(spellData: any[]): string`
-  `formatInnateSpells` function L330-353 — `function formatInnateSpells(innate: any): string`
-  `formatDailySpells` function L355-366 — `function formatDailySpells(daily: any): string`
-  `formatRestSpells` function L368-379 — `function formatRestSpells(rest: any): string`
-  `formatFrequency` function L381-390 — `function formatFrequency(freq: string): string`
-  `formatSpellNames` function L392-396 — `function formatSpellNames(spells: string[]): string`

#### crates/mimir/frontend/src/features/sources/formatters/spellFormatterEnhanced.ts

- pub `formatSpellDetails` function L63-72 — `function formatSpellDetails(spell: any): string`
-  `SpellDetails` interface L4-61 — `{ name: : string, level: : number, school: : string, source: : string, page: : n...`
-  `formatSpellSummary` function L74-127 — `function formatSpellSummary(spell: any): string`
-  `formatFullSpellDetails` function L129-324 — `function formatFullSpellDetails(spell: SpellDetails): string`
-  `formatCastingTime` function L326-333 — `function formatCastingTime(time: any[]): string`
-  `formatRange` function L335-357 — `function formatRange(range: any): string`
-  `formatComponents` function L359-371 — `function formatComponents(components: any): string`
-  `formatDuration` function L373-389 — `function formatDuration(duration: any[]): string`
-  `formatList` function L391-400 — `function formatList(listObj: any): string`
-  `formatSaveType` function L402-412 — `function formatSaveType(save: string): string`
-  `formatAttackType` function L414-421 — `function formatAttackType(attack: string): string`
-  `formatDamageType` function L423-440 — `function formatDamageType(damage: string): string`
-  `formatCondition` function L442-460 — `function formatCondition(condition: string): string`
-  `formatAreaType` function L462-477 — `function formatAreaType(area: string): string`
-  `formatMiscTag` function L479-493 — `function formatMiscTag(tag: string): { label: string; class: string } | null`
-  `formatCantripScaling` function L495-519 — `function formatCantripScaling(scalingData: any): string`

#### crates/mimir/frontend/src/features/sources/formatters/tableFormatter.ts

- pub `formatTableDetails` function L4-208 — `function formatTableDetails(table: Table): Promise<string>`
-  `getCategoryBadge` function L210-214 — `function getCategoryBadge(name: string): string`
-  `categorizeTable` function L216-240 — `function categorizeTable(name: string): string`
-  `getCategoryClass` function L242-266 — `function getCategoryClass(category: string | undefined): string`
-  `formatTableContent` function L268-312 — `function formatTableContent(table: Table): string`
-  `formatEntry` function L314-379 — `function formatEntry(entry: any): string`

#### crates/mimir/frontend/src/features/sources/formatters/trapFormatter.ts

- pub `formatTrapDetails` function L4-39 — `function formatTrapDetails(trap: TrapOrHazard): Promise<string>`
-  `formatTrapType` function L41-50 — `function formatTrapType(type: string): string`
-  `formatEntry` function L52-113 — `function formatEntry(entry: any): string`
-  `formatTable` function L115-146 — `function formatTable(table: any): string`

#### crates/mimir/frontend/src/features/sources/formatters/variantRuleFormatter.ts

- pub `formatVariantRuleDetails` function L11-186 — `function formatVariantRuleDetails(rule: VariantRule): string`
-  `VariantRule` interface L3-9 — `{ name: : string, source: : string, rule_type: : string | null, page: : number |...`
-  `getTypeClass` function L188-192 — `function getTypeClass(type: string): string`

#### crates/mimir/frontend/src/features/sources/formatters/vehicleFormatter.ts

- pub `formatVehicleDetails` function L26-333 — `function formatVehicleDetails(vehicle: Vehicle): string`
-  `Vehicle` interface L3-24 — `{ name: : string, source: : string, vehicle_type: : string, size: : string, page...`
-  `formatSize` function L335-345 — `function formatSize(size: string): string`

### crates/mimir/frontend/src/features/sources/services

> *Semantic summary to be generated by AI agent.*

#### crates/mimir/frontend/src/features/sources/services/SearchService.ts

- pub `SearchFilters` interface L47-68 — `{ spells: : { school: string level: string class: string ritual: boolean concent...`
- pub `SearchParams` interface L70-75 — `{ query: : string, sources: : string[], category: : string, filters: : Partial<S...`
- pub `DetailFetchParams` interface L77-83 — `{ name: : string, source: : string, type: : 'spell' | 'item' | 'monster' | 'clas...`
- pub `initialize` method L107-176 — `initialize(category: string): Promise<void>`
- pub `search` method L178-233 — `search(params: Partial<SearchParams>): Promise<any[]>`
- pub `searchSpells` method L235-249 — `searchSpells( query?: string, sources?: string[], filters?: SearchFilters['spell...`
- pub `searchEquipment` method L251-266 — `searchEquipment( query?: string, sources?: string[], filters?: SearchFilters['eq...`
- pub `searchMagicItems` method L268-283 — `searchMagicItems( query?: string, sources?: string[], filters?: SearchFilters['m...`
- pub `searchMonsters` method L285-298 — `searchMonsters( query?: string, sources?: string[], filters?: SearchFilters['mon...`
- pub `searchClasses` method L300-305 — `searchClasses(query?: string): Promise<ClassSummary[]>`
- pub `searchFeats` method L307-312 — `searchFeats(query?: string, sources?: string[]): Promise<FeatSummary[]>`
- pub `searchRaces` method L314-322 — `searchRaces(query?: string, sources?: string[]): Promise<RaceSummary[]>`
- pub `searchBackgrounds` method L324-330 — `searchBackgrounds(query?: string, sources?: string[]): Promise<BackgroundSummary...`
- pub `searchActions` method L332-338 — `searchActions(query?: string, sources?: string[]): Promise<ActionSummary[]>`
- pub `searchConditions` method L340-346 — `searchConditions(query?: string, sources?: string[]): Promise<ConditionSummary[]...`
- pub `searchOptionalFeatures` method L348-354 — `searchOptionalFeatures(query?: string, sources?: string[]): Promise<OptionalFeat...`
- pub `searchDeities` method L356-362 — `searchDeities(query?: string, sources?: string[]): Promise<DeitySummary[]>`
- pub `searchObjects` method L364-370 — `searchObjects(query?: string, sources?: string[]): Promise<ObjectSummary[]>`
- pub `searchTraps` method L372-380 — `searchTraps(params: { query?: string sources?: string[] categories?: string[] tr...`
- pub `getTrapDetails` method L382-384 — `getTrapDetails(name: string, source: string): Promise<TrapOrHazard | null>`
- pub `getTrapTypes` method L386-388 — `getTrapTypes(): Promise<string[]>`
- pub `searchLanguages` method L390-398 — `searchLanguages(params: { query?: string sources?: string[] types?: string[] scr...`
- pub `getLanguageDetails` method L400-402 — `getLanguageDetails(name: string, source: string): Promise<Language | null>`
- pub `getLanguageTypes` method L404-406 — `getLanguageTypes(): Promise<string[]>`
- pub `getLanguageScripts` method L408-410 — `getLanguageScripts(): Promise<string[]>`
- pub `searchRewards` method L412-420 — `searchRewards(params: { query?: string sources?: string[] reward_types?: string[...`
- pub `getRewardDetails` method L422-424 — `getRewardDetails(name: string, source: string): Promise<Reward | null>`
- pub `searchTables` method L426-435 — `searchTables(params: { query?: string sources?: string[] categories?: string[] m...`
- pub `getTableDetails` method L437-439 — `getTableDetails(name: string, source: string): Promise<Table | null>`
- pub `searchVariantRules` method L441-448 — `searchVariantRules(params: { query?: string types?: string[] sources?: string[] ...`
- pub `getVariantRuleDetails` method L450-452 — `getVariantRuleDetails(name: string, source: string): Promise<any>`
- pub `searchVehicles` method L454-463 — `searchVehicles(params: { query?: string types?: string[] sources?: string[] terr...`
- pub `getVehicleDetails` method L465-467 — `getVehicleDetails(name: string, source: string): Promise<any>`
- pub `searchCults` method L469-477 — `searchCults(params: { query?: string item_types?: string[] subtypes?: string[] s...`
- pub `getCultDetails` method L479-481 — `getCultDetails(name: string, source: string): Promise<any>`
- pub `getBoonDetails` method L483-485 — `getBoonDetails(name: string, source: string): Promise<any>`
- pub `searchPsionics` method L487-495 — `searchPsionics(params: { query?: string psionic_types?: string[] orders?: string...`
- pub `getPsionicDetails` method L497-499 — `getPsionicDetails(name: string, source: string): Promise<any>`
- pub `getDetails` method L501-580 — `getDetails(params: DetailFetchParams): Promise<any>`
- pub `mapBookIdsToSources` method L582-587 — `mapBookIdsToSources(bookIds: string[]): string[]`
- pub `getClassSources` method L589-591 — `getClassSources(): string[]`
-  `SearchServiceClass` class L85-592 — `-`

### crates/mimir/frontend/src/features/sources/utils/renderers

> *Semantic summary to be generated by AI agent.*

#### crates/mimir/frontend/src/features/sources/utils/renderers/contentRenderer.ts

- pub `renderSection` function L9-24 — `function renderSection(section: BookSection): string`
-  `renderEntry` function L29-174 — `function renderEntry(entry: BookEntry, index: number = 0, depth: number = 0): st...`
-  `renderTable` function L179-215 — `function renderTable(table: ComplexEntry): string`
-  `renderTableCell` function L220-260 — `function renderTableCell(cell: any): string`

### crates/mimir/frontend/src/features/sources/utils

> *Semantic summary to be generated by AI agent.*

#### crates/mimir/frontend/src/features/sources/utils/textFormatting.ts

- pub `processFormattingTags` function L4-191 — `function processFormattingTags(text: string | any): string`
- pub `formatEntries` function L196-275 — `function formatEntries(entries: any[]): string`
-  `formatTable` function L277-341 — `function formatTable(table: any): string`

### crates/mimir/frontend/src/services

> *Semantic summary to be generated by AI agent.*

#### crates/mimir/frontend/src/services/DocumentService.ts

- pub `CreateDocumentRequest` interface L16-22 — `{ campaign_id: : string, module_id: : string, title: : string, doc_type: : strin...`
- pub `UpdateDocumentRequest` interface L24-28 — `{ title: : string, content: : string, doc_type: : string }`
- pub `DocumentSearchResult` interface L34-43 — `{ id: : string, campaign_id: : string, module_id: : string | null, title: : stri...`
- pub `listForCampaign` method L53-63 — `listForCampaign(campaignId: string): Promise<Document[]>`
- pub `listForModule` method L68-78 — `listForModule(moduleId: string): Promise<Document[]>`
- pub `get` method L83-91 — `get(id: string): Promise<Document>`
- pub `create` method L96-115 — `create(request: CreateDocumentRequest): Promise<Document>`
- pub `update` method L120-132 — `update(id: string, request: UpdateDocumentRequest): Promise<Document>`
- pub `delete` method L137-148 — `delete(id: string, moduleId?: string): Promise<void>`
- pub `search` method L153-164 — `search(campaignId: string, query: string): Promise<DocumentSearchResult[]>`
- pub `searchInModule` method L169-180 — `searchInModule(moduleId: string, query: string): Promise<DocumentSearchResult[]>`
- pub `reorder` method L185-197 — `reorder(documentId: string, swapWithId: string): Promise<Document[]>`
- pub `updateContent` method L202-204 — `updateContent(id: string, content: string): Promise<Document>`
- pub `updateTitle` method L209-211 — `updateTitle(id: string, title: string): Promise<Document>`
- pub `getByType` method L216-219 — `getByType(moduleId: string, docType: string): Promise<Document[]>`
-  `DocumentServiceClass` class L49-220 — `-`

#### crates/mimir/frontend/src/services/HomebrewMonsterService.ts

- pub `HomebrewMonster` type L11 — `= CampaignHomebrewMonster`
- pub `CreateHomebrewMonsterRequest` interface L13-22 — `{ campaign_id: : string, name: : string, cr: : string, creature_type: : string, ...`
- pub `UpdateHomebrewMonsterRequest` interface L24-30 — `{ name: : string, cr: : string | null, creature_type: : string | null, size: : s...`

#### crates/mimir/frontend/src/services/HomebrewService.ts

- pub `HomebrewItem` type L11 — `= CampaignHomebrewItem`
- pub `CreateHomebrewItemRequest` interface L13-21 — `{ campaign_id: : string, name: : string, item_type: : string, rarity: : string, ...`
- pub `UpdateHomebrewItemRequest` interface L23-28 — `{ name: : string, item_type: : string | null, rarity: : string | null, data: : s...`

#### crates/mimir/frontend/src/services/HomebrewSpellService.ts

- pub `HomebrewSpell` type L11 — `= CampaignHomebrewSpell`
- pub `CreateHomebrewSpellRequest` interface L13-21 — `{ campaign_id: : string, name: : string, level: : number, school: : string, data...`
- pub `UpdateHomebrewSpellRequest` interface L23-28 — `{ name: : string, level: : number | null, school: : string | null, data: : strin...`

#### crates/mimir/frontend/src/services/ModuleService.ts

- pub `CreateModuleRequest` interface L16-21 — `{ campaign_id: : string, name: : string, description: : string, module_type: : s...`
- pub `UpdateModuleRequest` interface L23-26 — `{ name: : string, description: : string | null }`
- pub `list` method L36-46 — `list(campaignId: string): Promise<Module[]>`
- pub `get` method L51-59 — `get(id: string): Promise<Module>`
- pub `getByNumber` method L64-75 — `getByNumber(campaignId: string, moduleNumber: number): Promise<Module>`
- pub `create` method L80-92 — `create(request: CreateModuleRequest): Promise<Module>`
- pub `update` method L97-109 — `update(id: string, request: UpdateModuleRequest): Promise<Module>`
- pub `delete` method L114-125 — `delete(id: string, campaignId?: string): Promise<void>`
- pub `reorder` method L131-143 — `reorder(moduleId: string, newPosition: number): Promise<Module[]>`
- pub `updateStatus` method L149-153 — `updateStatus(id: string, status: string): Promise<Module>`
- pub `transitionStage` method L159-163 — `transitionStage(id: string, newStage: string): Promise<Module>`
- pub `initializeDocuments` method L169-172 — `initializeDocuments(id: string): Promise<string[]>`
-  `ModuleServiceClass` class L32-173 — `-`

#### crates/mimir/frontend/src/services/PrintService.ts

- pub `PrintTemplateInfo` interface L10-17 — `{ id: : string, name: : string, category: : string }`
- pub `PrintResult` interface L19-24 — `{ pdf_base64: : string, size_bytes: : number }`
- pub `ApiResponse` interface L26-30 — `{ success: : boolean, data: : T, error: : string }`
- pub `MapPrintOptions` interface L33-52 — `{ include_preview: : boolean, preview_grid: : boolean, preview_los_walls: : bool...`
- pub `ModuleExportOptions` interface L55-87 — `{ include_documents: : boolean, include_monsters: : boolean, include_traps: : bo...`
- pub `CharacterExportOptions` interface L90-99 — `{ include_compact_sheet: : boolean, include_battle_card: : boolean, include_spel...`
- pub `CampaignExportOptions` interface L102-122 — `{ include_campaign_docs: : boolean, include_module_content: : boolean, include_n...`
- pub `listTemplates` method L128-136 — `listTemplates(): Promise<PrintTemplateInfo[]>`
- pub `generateCharacterSheet` method L145-161 — `generateCharacterSheet( characterId: string, template: 'sheet' | 'summary' = 'sh...`
- pub `generateCharacterExport` method L168-179 — `generateCharacterExport(characterId: string, options?: CharacterExportOptions): ...`
- pub `exportCampaignDocument` method L185-195 — `exportCampaignDocument(documentId: string): Promise<PrintResult>`
- pub `exportCampaignDocuments` method L202-213 — `exportCampaignDocuments(campaignId: string, options?: CampaignExportOptions): Pr...`
- pub `exportModuleDocuments` method L220-231 — `exportModuleDocuments(moduleId: string, options?: ModuleExportOptions): Promise<...`
- pub `printMap` method L238-249 — `printMap(mapId: string, options?: MapPrintOptions): Promise<PrintResult>`
- pub `pdfToBlob` method L254-261 — `pdfToBlob(result: PrintResult): Blob`
- pub `createPdfUrl` method L266-269 — `createPdfUrl(result: PrintResult): string`
- pub `savePdf` method L274-293 — `savePdf(result: PrintResult, defaultFileName: string = 'document.pdf'): Promise<...`
- pub `openPdf` method L298-302 — `openPdf(result: PrintResult): Promise<void>`
- pub `printPdf` method L307-322 — `printPdf(result: PrintResult): void`
-  `PrintServiceClass` class L124-323 — `-`

#### crates/mimir/frontend/src/services/createHomebrewService.ts

- pub `HomebrewBase` interface L13-22 — `{ id: : string, campaign_id: : string, name: : string, data: : string, cloned_fr...`
- pub `HomebrewService` interface L35-46 — `{ list(), get(), getByName(), create(), update(), delete() }`
- pub `createHomebrewService` function L48-108 — `function createHomebrewService(config: HomebrewServiceConfig): HomebrewService<T...`
-  `HomebrewServiceConfig` interface L24-33 — `{ commandSuffix: : string, eventPrefix: : string, label: : string, hasGetByName:...`

### crates/mimir/frontend/src/services/__tests__

> *Semantic summary to be generated by AI agent.*

#### crates/mimir/frontend/src/services/__tests__/PrintService.test.ts

-  `createMockPrintResult` function L36-42 — `function createMockPrintResult(overrides: Partial<PrintResult> = {}): PrintResul...`

#### crates/mimir/frontend/src/services/__tests__/createHomebrewService.test.ts

-  `TestMonster` interface L8-12 — `{ cr: : string | null, creature_type: : string | null, size: : string | null }`
-  `CreateTestMonster` interface L14-19 — `{ campaign_id: : string, name: : string, data: : string, cr: : string }`
-  `UpdateTestMonster` interface L21-25 — `{ name: : string, data: : string, cr: : string | null }`

### crates/mimir/frontend/src/shared/components/catalog/__tests__

> *Semantic summary to be generated by AI agent.*

#### crates/mimir/frontend/src/shared/components/catalog/__tests__/CatalogTable.test.ts

-  `createConfig` function L40-55 — `const createConfig = (overrides: Partial<CatalogConfig> = {}): CatalogConfig`
-  `createData` function L57-62 — `const createData = ()`

### crates/mimir/frontend/src/shared/components/catalog/config

> *Semantic summary to be generated by AI agent.*

#### crates/mimir/frontend/src/shared/components/catalog/config/actionConfig.ts

-  `formatTime` function L4-30 — `function formatTime(time: unknown): string`

#### crates/mimir/frontend/src/shared/components/catalog/config/backgroundConfig.ts

-  `formatSkills` function L4-28 — `function formatSkills(skillProficiencies: unknown): string`
-  `formatTools` function L31-48 — `function formatTools(toolProficiencies: unknown): string`
-  `formatLanguages` function L51-69 — `function formatLanguages(languageProficiencies: unknown): string`
-  `formatFeature` function L72-84 — `function formatFeature(entries: unknown): string`

#### crates/mimir/frontend/src/shared/components/catalog/config/classConfig.ts

-  `isSubclassRow` function L5-8 — `function isSubclassRow(item: unknown): boolean`
-  `skipOnSubclassRow` function L11-16 — `function skipOnSubclassRow(formatter: (value: unknown, item?: unknown) => T): (v...`
-  `formatHitDice` function L19-25 — `function formatHitDice(hd: unknown): string`
-  `formatPrimaryAbility` function L29-80 — `function formatPrimaryAbility(item: unknown): string`
-  `formatProficiencies` function L83-143 — `function formatProficiencies(startingProficiencies: unknown): string`
-  `formatSpellcasting` function L147-160 — `function formatSpellcasting(item: unknown): { text: string; variant: string } | ...`

#### crates/mimir/frontend/src/shared/components/catalog/config/conditionConfig.ts

-  `formatDescription` function L4-39 — `function formatDescription(_value: unknown, item: unknown): string`
-  `truncateText` function L42-45 — `function truncateText(text: string, maxLength: number): string`

#### crates/mimir/frontend/src/shared/components/catalog/config/deityConfig.ts

-  `formatDomains` function L4-7 — `function formatDomains(domains: string[] | null): string`
-  `formatAlignment` function L10-13 — `function formatAlignment(alignment: string | null): string`
-  `formatTitle` function L16-19 — `function formatTitle(title: string | null): string`

#### crates/mimir/frontend/src/shared/components/catalog/config/featConfig.ts

-  `formatPrerequisites` function L4-80 — `function formatPrerequisites(prerequisite: unknown): string`

#### crates/mimir/frontend/src/shared/components/catalog/config/itemConfig.ts

-  `formatItemType` function L45-52 — `function formatItemType(value: unknown): string`
-  `formatCost` function L55-66 — `function formatCost(value: unknown): string`
-  `formatRarity` function L69-75 — `function formatRarity(rarity: unknown): string`

#### crates/mimir/frontend/src/shared/components/catalog/config/languageConfig.ts

-  `getTypeClass` function L80-97 — `function getTypeClass(type: string): string`

#### crates/mimir/frontend/src/shared/components/catalog/config/monsterConfig.ts

-  `formatSize` function L4-22 — `const formatSize = (size: unknown): string`
-  `formatType` function L25-34 — `const formatType = (type: unknown): string`
-  `formatCR` function L37-48 — `const formatCR = (cr: unknown): string`
-  `formatHP` function L51-59 — `const formatHP = (hp: unknown): string`
-  `formatAC` function L62-76 — `const formatAC = (ac: unknown): string`
-  `formatAlignment` function L79-117 — `const formatAlignment = (alignment: unknown): string`

#### crates/mimir/frontend/src/shared/components/catalog/config/objectConfig.ts

-  `formatObjectType` function L22-28 — `function formatObjectType(value: unknown): string`
-  `formatSize` function L31-40 — `function formatSize(value: unknown): string`
-  `formatAC` function L43-52 — `function formatAC(value: unknown): string`
-  `formatHP` function L55-74 — `function formatHP(value: unknown): string`

#### crates/mimir/frontend/src/shared/components/catalog/config/optionalFeatureConfig.ts

-  `getFeatureTypeCode` function L58-77 — `function getFeatureTypeCode(item: unknown): string | null`
-  `formatFeatureType` function L80-86 — `function formatFeatureType(_value: unknown, item: unknown): string`
-  `formatClass` function L89-95 — `function formatClass(_value: unknown, item: unknown): string`
-  `formatPrerequisites` function L98-183 — `function formatPrerequisites(_value: unknown, item: unknown): string`

#### crates/mimir/frontend/src/shared/components/catalog/config/raceConfig.ts

-  `formatSize` function L17-31 — `function formatSize(size: unknown): string`
-  `formatSpeed` function L34-58 — `function formatSpeed(speed: unknown): string`
-  `formatAbilityBonuses` function L61-101 — `function formatAbilityBonuses(ability: unknown): string`
-  `countTraits` function L104-116 — `function countTraits(entries: unknown): string`
-  `formatRaceName` function L119-133 — `function formatRaceName(name: unknown, row: unknown): string`

#### crates/mimir/frontend/src/shared/components/catalog/config/rewardConfig.ts

-  `getTypeClass` function L83-99 — `function getTypeClass(type: string): string`

#### crates/mimir/frontend/src/shared/components/catalog/config/spellConfig.ts

-  `formatSchool` function L17-22 — `function formatSchool(school: unknown): string`
-  `formatCastingTime` function L25-35 — `function formatCastingTime(time: unknown): string`
-  `formatRange` function L38-53 — `function formatRange(range: unknown): string`
-  `formatComponents` function L56-67 — `function formatComponents(components: unknown): string`

#### crates/mimir/frontend/src/shared/components/catalog/config/types.ts

- pub `ColumnDef` interface L2-11 — `{ key: : string, label: : string, sortable: : boolean, width: : string, type: : ...`
- pub `FilterConfig` interface L14-26 — `{ type: : 'text' | 'select' | 'multiselect' | 'checkbox' | 'checkbox-group' | 'r...`
- pub `SelectOption` interface L28-31 — `{ value: : string, label: : string }`
- pub `GroupedOption` interface L33-36 — `{ label: : string, options: : SelectOption[] }`
- pub `CatalogConfig` interface L39-58 — `{ name: : string, title: : string, columns: : ColumnDef[], filters: : FilterConf...`
- pub `FilterValues` interface L61-63 — `-`
- pub `RangeValue` interface L66-69 — `{ min: : number, max: : number }`

#### crates/mimir/frontend/src/shared/components/catalog/config/variantRuleConfig.ts

-  `formatRuleType` function L5-19 — `function formatRuleType(item: unknown): { text: string; variant: string }`

#### crates/mimir/frontend/src/shared/components/catalog/config/vehicleConfig.ts

-  `formatSize` function L4-18 — `function formatSize(size: string | null): string`
-  `formatTerrain` function L22-28 — `function formatTerrain(terrain: string[] | null): string`
-  `formatVehicleType` function L31-47 — `function formatVehicleType(vehicleType: string | null): { text: string; variant:...`

### crates/mimir/frontend/src/stores

> *Semantic summary to be generated by AI agent.*

#### crates/mimir/frontend/src/stores/campaigns.ts

-  `fetchCampaigns` function L16-38 — `const fetchCampaigns = ()`
-  `getCampaign` function L41-60 — `const getCampaign = (id: string)`
-  `createCampaign` function L63-83 — `const createCampaign = (request: CreateCampaignRequest)`
-  `updateCampaign` function L86-113 — `const updateCampaign = (id: string, request: UpdateCampaignRequest)`
-  `fetchArchivedCampaigns` function L116-132 — `const fetchArchivedCampaigns = ()`
-  `archiveCampaign` function L135-160 — `const archiveCampaign = (id: string)`
-  `unarchiveCampaign` function L163-189 — `const unarchiveCampaign = (id: string)`
-  `deleteCampaign` function L192-219 — `const deleteCampaign = (id: string, deleteFiles = false)`
-  `fetchCampaignSources` function L222-237 — `const fetchCampaignSources = (campaignId: string)`
-  `setCurrentCampaign` function L240-247 — `const setCurrentCampaign = (campaign: Campaign | null)`
-  `refreshCampaignSources` function L250-254 — `const refreshCampaignSources = ()`
-  `clearError` function L257-259 — `const clearError = ()`
-  `exportCampaign` function L262-283 — `const exportCampaign = (campaignId: string, outputDirectory: string): Promise<{ ...`
-  `importCampaign` function L286-311 — `const importCampaign = (archivePath: string, newName?: string): Promise<Campaign...`
-  `previewArchive` function L314-334 — `const previewArchive = (archivePath: string): Promise<ArchivePreview | null>`
-  `getArchiveExtension` function L337-339 — `const getArchiveExtension = (): string`

#### crates/mimir/frontend/src/stores/characters.ts

-  `fetchCharacters` function L41-63 — `const fetchCharacters = (campaignId: string)`
-  `fetchPcs` function L68-95 — `const fetchPcs = (campaignId: string)`
-  `fetchAllPcs` function L100-132 — `const fetchAllPcs = (campaignIds: string[])`
-  `fetchNpcs` function L137-164 — `const fetchNpcs = (campaignId: string)`
-  `getCharacter` function L173-202 — `const getCharacter = (id: string)`
-  `createPc` function L207-231 — `const createPc = (request: CreatePcRequest)`
-  `createNpc` function L236-260 — `const createNpc = (request: CreateNpcRequest)`
-  `updateCharacter` function L265-293 — `const updateCharacter = (id: string, request: UpdateCharacterRequest)`
-  `deleteCharacter` function L298-328 — `const deleteCharacter = (id: string)`
-  `fetchInventory` function L337-359 — `const fetchInventory = (characterId: string)`
-  `fetchEquippedItems` function L364-385 — `const fetchEquippedItems = (characterId: string)`
-  `fetchAttunedItems` function L390-411 — `const fetchAttunedItems = (characterId: string)`
-  `addInventoryItem` function L416-439 — `const addInventoryItem = (characterId: string, request: AddInventoryRequest)`
-  `removeInventoryItem` function L444-464 — `const removeInventoryItem = (inventoryId: string)`
-  `updateInventoryItem` function L469-495 — `const updateInventoryItem = (inventoryId: string, request: UpdateInventoryReques...`
-  `setCurrentCharacter` function L504-506 — `const setCurrentCharacter = (character: Character | null)`
-  `reset` function L511-517 — `const reset = ()`
-  `clearError` function L522-524 — `const clearError = ()`

#### crates/mimir/frontend/src/stores/theme.ts

-  `loadThemes` function L14-22 — `const loadThemes = ()`
-  `getSavedTheme` function L25-27 — `const getSavedTheme = (): string`
-  `saveTheme` function L30-32 — `const saveTheme = (theme: string)`
-  `applyTheme` function L35-39 — `const applyTheme = ()`
-  `applyThemeToBody` function L42-47 — `const applyThemeToBody = (theme: string)`
-  `setTheme` function L50-62 — `const setTheme = (theme: string, broadcast = true)`
-  `initThemeSync` function L65-71 — `const initThemeSync = ()`
-  `cleanup` function L74-79 — `const cleanup = ()`

### crates/mimir/frontend/src/test

> *Semantic summary to be generated by AI agent.*

#### crates/mimir/frontend/src/test/test-env.d.ts

-  `Window` interface L3-7 — `{ __TAURI_INTERNALS__: : { invoke: any } }`

### crates/mimir/frontend/src/test/utils

> *Semantic summary to be generated by AI agent.*

#### crates/mimir/frontend/src/test/utils/mockTauri.ts

- pub `mockInvoke` function L3-16 — `function mockInvoke(responses: Record<string, any>)`
- pub `createMockModule` function L18-37 — `function createMockModule(overrides: any = {})`
- pub `createMockDocument` function L39-54 — `function createMockDocument(overrides: any = {})`
- pub `createMockSession` function L56-66 — `function createMockSession(overrides: any = {})`
- pub `createMockCampaign` function L68-80 — `function createMockCampaign(overrides: any = {})`

### crates/mimir/frontend/src/types

> *Semantic summary to be generated by AI agent.*

#### crates/mimir/frontend/src/types/api.ts

- pub `ApiResponse` interface L2-6 — `{ success: : boolean, data: : T, error: : string }`
- pub `Theme` interface L9-13 — `{ id: : string, name: : string, description: : string }`
- pub `CreateCampaignRequest` interface L21-24 — `{ name: : string, description: : string }`
- pub `UpdateCampaignRequest` interface L27-30 — `{ name: : string, description: : string | null }`
- pub `ArchiveCounts` interface L37-46 — `{ modules: : number, documents: : number, characters: : number, maps: : number, ...`
- pub `CatalogReference` interface L49-53 — `{ type: : string, name: : string, source: : string }`
- pub `ArchivePreview` interface L56-63 — `{ campaign_name: : string, counts: : ArchiveCounts, catalog_references: : Catalo...`
- pub `CreateModuleRequest` interface L71-76 — `{ campaign_id: : string, name: : string, description: : string, module_number: :...`
- pub `UpdateModuleRequest` interface L79-83 — `{ name: : string, description: : string | null, module_number: : number }`
- pub `CreateDocumentRequest` interface L91-97 — `{ campaign_id: : string, module_id: : string, title: : string, content: : string...`
- pub `UpdateDocumentRequest` interface L100-105 — `{ title: : string, content: : string, doc_type: : string, module_id: : string | ...`
- pub `TokenType` type L111 — `= 'monster' | 'pc' | 'npc' | 'trap' | 'marker'`
- pub `TokenSize` type L112 — `= 'tiny' | 'small' | 'medium' | 'large' | 'huge' | 'gargantuan'`
- pub `VisionType` type L113 — `= 'normal' | 'darkvision' | 'blindsight' | 'tremorsense' | 'truesight' | 'devils...`
- pub `Token` interface L115-138 — `{ id: : string, map_id: : string, name: : string, token_type: : TokenType, size:...`
- pub `TokenSummary` interface L140-143 — `{ monster_name: : string | null, character_name: : string | null }`
- pub `CreateTokenRequest` interface L145-160 — `{ map_id: : string, name: : string, token_type: : string, size: : string, x: : n...`
- pub `TokenConfigWithMonster` interface L166-169 — `{ monster_name: : string, monster_source: : string }`
- pub `UpdateTokenRequest` interface L171-182 — `{ name: : string, token_type: : string, size: : string, x: : number, y: : number...`

#### crates/mimir/frontend/src/types/book.ts

- pub `BookInfo` interface L3-14 — `{ id: : string, name: : string, enabled: : boolean, imported_at: : string, image...`
- pub `ImportResponse` interface L16-25 — `{ sources_imported: : number, sources_failed: : number, total_entities: : number...`
- pub `BookContent` interface L27-31 — `{ success: : boolean, data: : BookSection[] | null, message: : string }`
- pub `BookSection` interface L33-41 — `{ type: : string, name: : string, entries: : BookEntry[], page: : number, source...`
- pub `BookEntry` type L43 — `= string | ComplexEntry`
- pub `ComplexEntry` interface L45-69 — `{ type: : string, name: : string, entries: : BookEntry[], items: : any[], page: ...`
- pub `SubEntry` interface L71-76 — `{ id: : string, name: : string, level: : number, children: : SubEntry[] }`

#### crates/mimir/frontend/src/types/character.ts

- pub `Character` type L23 — `= CharacterResponse`
- pub `CreatePcRequest` interface L32-48 — `{ campaign_id: : string, name: : string, player_name: : string, race_name: : str...`
- pub `CreateNpcRequest` interface L53-61 — `{ campaign_id: : string, name: : string, race_name: : string, race_source: : str...`
- pub `UpdateCharacterRequest` interface L66-84 — `{ name: : string, player_name: : string | null, race_name: : string | null, race...`
- pub `AddInventoryRequest` interface L93-100 — `{ item_name: : string, item_source: : string, quantity: : number, equipped: : bo...`
- pub `UpdateInventoryRequest` interface L105-109 — `{ quantity: : number, equipped: : boolean, attuned: : boolean }`
- pub `isNpc` function L118-120 — `function isNpc(character: Character): boolean`
- pub `isPc` function L125-127 — `function isPc(character: Character): boolean`
- pub `abilityModifier` function L133-135 — `function abilityModifier(score: number): number`
- pub `totalGoldValue` function L140-148 — `function totalGoldValue(character: Character): number`
- pub `isEquipped` function L153-155 — `function isEquipped(item: CharacterInventory): boolean`
- pub `isAttuned` function L160-162 — `function isAttuned(item: CharacterInventory): boolean`
- pub `totalLevel` function L167-169 — `function totalLevel(character: Character): number`
- pub `classString` function L174-186 — `function classString(character: Character): string`
- pub `hasExpertise` function L195-197 — `function hasExpertise(prof: CharacterProficiency): boolean`
- pub `getSkillProficiencies` function L202-204 — `function getSkillProficiencies(character: Character): CharacterProficiency[]`
- pub `getSaveProficiencies` function L209-211 — `function getSaveProficiencies(character: Character): CharacterProficiency[]`
- pub `getToolProficiencies` function L216-218 — `function getToolProficiencies(character: Character): CharacterProficiency[]`
- pub `getWeaponProficiencies` function L223-225 — `function getWeaponProficiencies(character: Character): CharacterProficiency[]`
- pub `getArmorProficiencies` function L230-232 — `function getArmorProficiencies(character: Character): CharacterProficiency[]`
- pub `getLanguages` function L237-239 — `function getLanguages(character: Character): CharacterProficiency[]`
- pub `isProficientInSkill` function L244-248 — `function isProficientInSkill(character: Character, skillName: string): boolean`
- pub `hasSkillExpertise` function L253-257 — `function hasSkillExpertise(character: Character, skillName: string): boolean`
- pub `isProficientInSave` function L262-266 — `function isProficientInSave(character: Character, saveName: string): boolean`
- pub `proficiencyBonus` function L272-276 — `function proficiencyBonus(character: Character): number`
- pub `HpGainMethod` type L285-288 — `= | { type: 'Average' } | { type: 'Roll'; value: number } | { type: 'Manual'; va...`
- pub `AsiOrFeat` type L293-305 — `= | { type: 'AbilityScoreImprovement' ability1: string increase1: number ability...`
- pub `SubclassChoice` interface L310-313 — `{ name: : string, source: : string }`
- pub `SpellReference` interface L318-323 — `{ name: : string, source: : string, level: : number }`
- pub `SpellChanges` interface L328-333 — `{ new_spells: : SpellReference[], new_cantrips: : SpellReference[], swap_out: : ...`
- pub `FeatureReference` interface L338-341 — `{ name: : string, source: : string }`
- pub `ManeuverChoices` interface L346-350 — `{ new_maneuvers: : FeatureReference[], swap_out: : FeatureReference, swap_in: : ...`
- pub `InvocationChoices` interface L355-359 — `{ new_invocations: : FeatureReference[], swap_out: : FeatureReference, swap_in: ...`
- pub `FeatureChoices` interface L364-371 — `{ fighting_style: : FeatureReference, metamagic: : FeatureReference[], maneuvers...`
- pub `LevelUpRequest` interface L377-385 — `{ class_name: : string, class_source: : string, hit_points_method: : HpGainMetho...`
- pub `LevelUpResult` interface L390-396 — `{ character: : Character, class: : CharacterClass, hp_gained: : number, new_tota...`

#### crates/mimir/frontend/src/types/content.ts

- pub `TooltipPosition` interface L3-6 — `{ x: : number, y: : number }`
- pub `ModalContent` interface L8-12 — `{ title: : string, content: : string, visible: : boolean }`

#### crates/mimir/frontend/src/types/index.ts

- pub `Theme` type L24 — `= typeof THEMES[keyof typeof THEMES]`
- pub `EntityType` type L27 — `= 'campaign' | 'module'`
- pub `BaseEntity` interface L30-34 — `{ id: : string, name: : string, created_at: : string }`
- pub `ThemeIcons` interface L37-43 — `{ edit: : string, locked: : string, gear: : string, mimir: : string, new: : stri...`
- pub `IconMap` type L45 — `= Record<Theme, ThemeIcons>`

#### crates/mimir/frontend/src/types/reference.ts

- pub `ReferenceType` type L3 — `= 'spell' | 'item' | 'creature' | 'condition' | 'race' | 'class' | 'classFeature...`
- pub `ReferenceData` interface L5-9 — `{ success: : boolean, data: : any, message: : string }`
- pub `SpellData` interface L11-27 — `{ name: : string, source: : string, level: : number, school: : string, range: : ...`
- pub `ItemData` interface L29-47 — `{ name: : string, source: : string, type: : string, rarity: : string, weight: : ...`
- pub `CreatureData` interface L49-79 — `{ name: : string, source: : string, size: : string[], type: : string | { type: s...`
- pub `ConditionData` interface L81-85 — `{ name: : string, source: : string, entries: : string[] }`
- pub `RaceData` interface L87-100 — `{ name: : string, source: : string, ability: : any[], size: : string[], speed: :...`
- pub `ClassData` interface L102-113 — `{ name: : string, source: : string, hd: : { number: number; faces: number }, pro...`
- pub `BackgroundData` interface L115-125 — `{ name: : string, source: : string, entries: : any[], skillProficiencies: : any[...`
- pub `FeatData` interface L127-133 — `{ name: : string, source: : string, entries: : any[], prerequisite: : any[], abi...`
- pub `ClassFeatureData` interface L135-142 — `{ name: : string, source: : string, className: : string, classSource: : string, ...`
- pub `SubclassData` interface L144-152 — `{ name: : string, source: : string, className: : string, classSource: : string, ...`
- pub `SubclassFeatureData` interface L154-163 — `{ name: : string, source: : string, className: : string, classSource: : string, ...`

### crates/mimir/frontend/src/types/generated

> *Semantic summary to be generated by AI agent.*

#### crates/mimir/frontend/src/types/generated/Campaign.ts

- pub `Campaign` type L6-30 — `= { /** * Unique campaign ID (UUID) */ id: string, /** * Campaign name */ name: ...`

#### crates/mimir/frontend/src/types/generated/CampaignHomebrewItem.ts

- pub `CampaignHomebrewItem` type L6 — `= { id: string, campaign_id: string, name: string, item_type: string | null, rar...`

#### crates/mimir/frontend/src/types/generated/CampaignHomebrewMonster.ts

- pub `CampaignHomebrewMonster` type L6 — `= { id: string, campaign_id: string, name: string, cr: string | null, creature_t...`

#### crates/mimir/frontend/src/types/generated/CampaignHomebrewSpell.ts

- pub `CampaignHomebrewSpell` type L6 — `= { id: string, campaign_id: string, name: string, level: number | null, school:...`

#### crates/mimir/frontend/src/types/generated/Character.ts

- pub `Character` type L7-51 — `= { /** * Unique character ID (UUID) */ id: string, /** * Campaign this characte...`

#### crates/mimir/frontend/src/types/generated/CharacterClass.ts

- pub `CharacterClass` type L7-39 — `= { /** * Unique ID (UUID) */ id: string, /** * Character this class belongs to ...`

#### crates/mimir/frontend/src/types/generated/CharacterInventory.ts

- pub `CharacterInventory` type L6-38 — `= { /** * Unique ID (UUID) */ id: string, /** * Character who has this item */ c...`

#### crates/mimir/frontend/src/types/generated/CharacterProficiency.ts

- pub `CharacterProficiency` type L6-26 — `= { /** * Unique ID (UUID) */ id: string, /** * Character who has this proficien...`

#### crates/mimir/frontend/src/types/generated/CharacterResponse.ts

- pub `CharacterResponse` type L8-16 — `= { id: string, campaign_id: string | null, name: string, is_npc: number, player...`

#### crates/mimir/frontend/src/types/generated/Document.ts

- pub `Document` type L7-43 — `= { /** * Unique document ID (UUID) */ id: string, /** * Campaign this document ...`

#### crates/mimir/frontend/src/types/generated/Map.ts

- pub `Map` type L6-50 — `= { /** * Unique ID (UUID) */ id: string, /** * Campaign this map belongs to */ ...`

#### crates/mimir/frontend/src/types/generated/Module.ts

- pub `Module` type L6-34 — `= { /** * Unique module ID (UUID) */ id: string, /** * Campaign this module belo...`

### crates/mimir/frontend/src/utils/__tests__

> *Semantic summary to be generated by AI agent.*

#### crates/mimir/frontend/src/utils/__tests__/characterUtils.test.ts

-  `makeClass` function L47-59 — `function makeClass(overrides: Partial<CharacterClass> = {}): CharacterClass`
-  `makeProf` function L61-70 — `function makeProf(overrides: Partial<CharacterProficiency> = {}): CharacterProfi...`
-  `makeCharacter` function L72-98 — `function makeCharacter(overrides: Partial<Character> = {}): Character`

### crates/mimir/frontend/src/utils

> *Semantic summary to be generated by AI agent.*

#### crates/mimir/frontend/src/utils/api.ts

- pub `ApiError` class L12-17 — `extends Error`
- pub `constructor` method L13-16 — `constructor(message: string)`
- pub `unwrapResponse` function L36-41 — `function unwrapResponse(response: ApiResponse<T>): T`
- pub `unwrapResponseSafe` function L60-65 — `function unwrapResponseSafe(response: ApiResponse<T>): T | undefined`
- pub `isSuccess` function L73-75 — `function isSuccess(response: ApiResponse<T>): boolean`
- pub `getError` function L83-88 — `function getError(response: ApiResponse<T>): string | undefined`

#### crates/mimir/frontend/src/utils/characterUtils.ts

- pub `AbilityName` type L47 — `= (typeof ABILITIES)[number]`
- pub `getModifier` function L57-59 — `function getModifier(score: number): number`
- pub `formatModifier` function L64-66 — `function formatModifier(mod: number): string`
- pub `getProficiencyBonus` function L72-75 — `function getProficiencyBonus(totalLevel: number): number`
- pub `getTotalLevel` function L80-82 — `function getTotalLevel(character: Character): number`
- pub `getProficienciesByType` function L91-96 — `function getProficienciesByType( character: Character, type: CharacterProficienc...`
- pub `isProficientInSkill` function L101-105 — `function isProficientInSkill(character: Character, skillName: string): boolean`
- pub `hasSkillExpertise` function L110-114 — `function hasSkillExpertise(character: Character, skillName: string): boolean`
- pub `isProficientInSave` function L119-123 — `function isProficientInSave(character: Character, ability: string): boolean`
- pub `getSkillBonus` function L132-147 — `function getSkillBonus( character: Character, skillName: string, abilityScore: n...`
- pub `getSaveBonus` function L152-160 — `function getSaveBonus(character: Character, ability: string, score: number): num...`
- pub `getPassivePerception` function L165-179 — `function getPassivePerception(character: Character): number`
- pub `getArmorAC` function L189-238 — `function getArmorAC(armorName: string, dexMod: number): number`
- pub `getWeaponDamage` function L243-286 — `function getWeaponDamage(weaponName: string, abilityMod: number): string`
- pub `isWeapon` function L308-318 — `function isWeapon(itemName: string): boolean`
- pub `isFinesse` function L323-332 — `function isFinesse(weaponName: string): boolean`
- pub `isRanged` function L337-347 — `function isRanged(weaponName: string): boolean`
- pub `isSpellcaster` function L372-376 — `function isSpellcaster(character: Character): boolean`
- pub `getSpellcastingAbility` function L382-388 — `function getSpellcastingAbility(character: Character): AbilityName | null`
- pub `getSpellSaveDC` function L394-403 — `function getSpellSaveDC(character: Character): number | null`
- pub `getSpellAttackBonus` function L409-418 — `function getSpellAttackBonus(character: Character): number | null`
- pub `SpellcastingStats` interface L421-427 — `{ className: : string, ability: : AbilityName, abilityAbbrev: : string, saveDC: ...`
- pub `getAllSpellcastingStats` function L433-456 — `function getAllSpellcastingStats(character: Character): SpellcastingStats[]`
- pub `getMulticlassCasterLevel` function L466-491 — `function getMulticlassCasterLevel(character: Character): number`
- pub `getHitDiceString` function L518-529 — `function getHitDiceString(character: Character): string`
- pub `formatClassString` function L539-551 — `function formatClassString(character: Character): string`
- pub `getAbilityScore` function L556-558 — `function getAbilityScore(character: Character, ability: AbilityName): number`

#### crates/mimir/frontend/src/utils/dataEvents.ts

- pub `DataEventPayloads` interface L26-86 — `{ 'module:created': : { campaignId: string; moduleId: string }, 'module:updated'...`
- pub `DataEventName` type L88 — `= keyof DataEventPayloads`
- pub `setDebug` method L99-101 — `setDebug(enabled: boolean)`
- pub `on` method L106-116 — `on(event: T, listener: Listener<T>): () => void`
- pub `once` method L121-127 — `once(event: T, listener: Listener<T>): () => void`
- pub `off` method L132-134 — `off(event: T, listener: Listener<T>): void`
- pub `emit` method L139-154 — `emit(event: T, payload: DataEventPayloads[T]): void`
- pub `clear` method L159-165 — `clear(event?: DataEventName): void`
- pub `listenerCount` method L170-177 — `listenerCount(event?: DataEventName): number`
-  `Listener` type L90 — `= (payload: DataEventPayloads[T]) => void`
-  `DataEventBus` class L92-178 — `-`
-  `wrappedListener` function L122-125 — `const wrappedListener = (payload)`

#### crates/mimir/frontend/src/utils/debounce.ts

- pub `debounce` function L6-22 — `function debounce( func: T, delay: number ): (...args: Parameters<T>) => void`

#### crates/mimir/frontend/src/utils/debug.ts

- pub `debugDocument` function L5-12 — `function debugDocument(action: string, data: any)`

#### crates/mimir/frontend/src/utils/formatters.ts

- pub `formatSpellLevel` function L17-23 — `function formatSpellLevel(level: number): string`
- pub `formatSpellTags` function L30-35 — `function formatSpellTags(spell: any): string[]`
- pub `formatCR` function L47-53 — `function formatCR(cr: any): string`
- pub `formatWeight` function L64-67 — `function formatWeight(weight: number | null): string`

#### crates/mimir/frontend/src/utils/windows.ts

- pub `WindowConfig` interface L6-17 — `{ label: : string, url: : string, title: : string, width: : number, height: : nu...`
- pub `openExternalWindow` function L44-75 — `function openExternalWindow( config: WindowConfig | keyof typeof WINDOW_CONFIGS ...`
- pub `openSourcesReference` function L80-82 — `function openSourcesReference()`

### crates/mimir/src/commands

> *Semantic summary to be generated by AI agent.*

#### crates/mimir/src/commands/archive.rs

- pub `ExportResult` struct L15-20 — `{ archive_path: String, size_bytes: u64 }` — Result of exporting a campaign.
- pub `export_campaign` function L27-54 — `( state: State<'_, AppState>, campaign_id: String, output_dir: String, ) -> ApiR...` — Export a campaign to an archive file.
- pub `preview_archive` function L61-65 — `(archive_path: String) -> ApiResponse<ArchivePreview>` — Preview an archive without importing it.
- pub `import_campaign` function L72-91 — `( state: State<'_, AppState>, archive_path: String, new_name: Option<String>, ) ...` — Import a campaign from an archive file.

#### crates/mimir/src/commands/asset.rs

- pub `list_campaign_assets` function L18-29 — `( state: State<'_, AppState>, campaign_id: String, ) -> ApiResponse<Vec<Campaign...` — List all assets for a campaign.
- pub `list_module_assets` function L33-44 — `( state: State<'_, AppState>, module_id: String, ) -> ApiResponse<Vec<CampaignAs...` — List all assets for a module.
- pub `get_asset` function L52-64 — `(state: State<'_, AppState>, id: String) -> ApiResponse<CampaignAsset>` — Get an asset by ID.
- pub `UploadAssetRequest` struct L68-81 — `{ campaign_id: Option<String>, module_id: Option<String>, filename: String, desc...` — Request for uploading an asset.
- pub `upload_asset` function L85-115 — `( state: State<'_, AppState>, request: UploadAssetRequest, ) -> ApiResponse<Camp...` — Upload a new asset.
- pub `delete_asset` function L119-127 — `(state: State<'_, AppState>, id: String) -> ApiResponse<()>` — Delete an asset.
- pub `read_asset_file` function L135-155 — `(state: State<'_, AppState>, id: String) -> ApiResponse<String>` — Read the file data for an asset (returned as base64).
-  `base64_encode` function L162-165 — `(data: &[u8]) -> String` — Simple base64 encoding.
-  `base64_decode` function L168-171 — `(s: &str) -> Result<Vec<u8>, base64::DecodeError>` — Simple base64 decoding.

#### crates/mimir/src/commands/campaign.rs

- pub `list_campaigns` function L22-35 — `( state: State<'_, AppState>, include_archived: Option<bool>, ) -> ApiResponse<V...` — List all campaigns.
- pub `list_archived_campaigns` function L39-51 — `(state: State<'_, AppState>) -> ApiResponse<Vec<Campaign>>` — List only archived campaigns.
- pub `get_campaign` function L55-67 — `(state: State<'_, AppState>, id: String) -> ApiResponse<Campaign>` — Get a campaign by ID.
- pub `CreateCampaignRequest` struct L71-74 — `{ name: String, description: Option<String> }` — Request for creating a new campaign.
- pub `create_campaign` function L78-94 — `( state: State<'_, AppState>, request: CreateCampaignRequest, ) -> ApiResponse<C...` — Create a new campaign.
- pub `UpdateCampaignRequest` struct L98-101 — `{ name: Option<String>, description: Option<Option<String>> }` — Request for updating a campaign.
- pub `update_campaign` function L105-122 — `( state: State<'_, AppState>, id: String, request: UpdateCampaignRequest, ) -> A...` — Update a campaign.
- pub `archive_campaign` function L126-145 — `(state: State<'_, AppState>, campaign_id: String) -> ApiResponse<Campaign>` — Archive a campaign (soft delete).
- pub `unarchive_campaign` function L149-168 — `(state: State<'_, AppState>, campaign_id: String) -> ApiResponse<Campaign>` — Unarchive a campaign.
- pub `DeleteCampaignRequest` struct L172-176 — `{ campaign_id: String, delete_files: bool }` — Request for deleting a campaign.
- pub `delete_campaign` function L180-192 — `( state: State<'_, AppState>, request: DeleteCampaignRequest, ) -> ApiResponse<(...` — Delete a campaign permanently.
- pub `list_campaign_sources` function L200-211 — `( state: State<'_, AppState>, campaign_id: String, ) -> ApiResponse<Vec<String>>` — List all source codes enabled for a campaign.
- pub `add_campaign_source` function L215-235 — `( state: State<'_, AppState>, campaign_id: String, source_code: String, ) -> Api...` — Add a source to a campaign's allowed sources.
- pub `remove_campaign_source` function L239-254 — `( state: State<'_, AppState>, campaign_id: String, source_code: String, ) -> Api...` — Remove a source from a campaign's allowed sources.
- pub `set_campaign_sources` function L258-285 — `( state: State<'_, AppState>, campaign_id: String, source_codes: Vec<String>, ) ...` — Set all sources for a campaign (replaces existing).

#### crates/mimir/src/commands/character.rs

- pub `list_characters` function L24-43 — `( state: State<'_, AppState>, campaign_id: String, ) -> ApiResponse<Vec<Characte...` — List all characters for a campaign (with classes and proficiencies).
- pub `list_pcs` function L47-63 — `(state: State<'_, AppState>, campaign_id: String) -> ApiResponse<Vec<CharacterRe...` — List only player characters for a campaign (with classes and proficiencies).
- pub `list_npcs` function L67-83 — `(state: State<'_, AppState>, campaign_id: String) -> ApiResponse<Vec<CharacterRe...` — List only NPCs for a campaign (with classes and proficiencies).
- pub `list_unassigned_pcs` function L87-102 — `(state: State<'_, AppState>) -> ApiResponse<Vec<CharacterResponse>>` — List unassigned player characters (no campaign).
- pub `get_character` function L110-121 — `(state: State<'_, AppState>, id: String) -> ApiResponse<CharacterResponse>` — Get a character by ID (with classes and proficiencies).
- pub `CreatePcRequest` struct L125-137 — `{ campaign_id: Option<String>, name: String, player_name: String, race_name: Opt...` — Request for creating a new PC.
- pub `create_pc` function L141-185 — `(state: State<'_, AppState>, request: CreatePcRequest) -> ApiResponse<CharacterR...` — Create a new player character.
- pub `CreateNpcRequest` struct L189-197 — `{ campaign_id: Option<String>, name: String, race_name: Option<String>, race_sou...` — Request for creating a new NPC.
- pub `create_npc` function L201-236 — `(state: State<'_, AppState>, request: CreateNpcRequest) -> ApiResponse<Character...` — Create a new NPC.
- pub `UpdateCharacterRequest` struct L240-256 — `{ name: Option<String>, player_name: Option<Option<String>>, race_name: Option<O...` — Request for updating a character.
- pub `update_character` function L260-296 — `( state: State<'_, AppState>, id: String, request: UpdateCharacterRequest, ) -> ...` — Update a character.
- pub `delete_character` function L300-308 — `(state: State<'_, AppState>, id: String) -> ApiResponse<()>` — Delete a character permanently.
- pub `assign_character_to_campaign` function L312-326 — `( state: State<'_, AppState>, character_id: String, campaign_id: String, ) -> Ap...` — Assign a character to a campaign.
- pub `level_up_character` function L336-348 — `( state: State<'_, AppState>, character_id: String, request: LevelUpRequest, ) -...` — Level up a character.
- pub `get_character_inventory` function L356-367 — `( state: State<'_, AppState>, character_id: String, ) -> ApiResponse<Vec<Charact...` — Get a character's inventory.
- pub `get_equipped_items` function L371-382 — `( state: State<'_, AppState>, character_id: String, ) -> ApiResponse<Vec<Charact...` — Get equipped items for a character.
- pub `get_attuned_items` function L386-397 — `( state: State<'_, AppState>, character_id: String, ) -> ApiResponse<Vec<Charact...` — Get attuned items for a character.
- pub `AddInventoryRequest` struct L401-408 — `{ item_name: String, item_source: String, quantity: Option<i32>, equipped: Optio...` — Request for adding an item to inventory.
- pub `add_inventory_item` function L412-439 — `( state: State<'_, AppState>, character_id: String, request: AddInventoryRequest...` — Add an item to a character's inventory.
- pub `remove_inventory_item` function L443-451 — `(state: State<'_, AppState>, inventory_id: String) -> ApiResponse<()>` — Remove an item from a character's inventory.
- pub `UpdateInventoryRequest` struct L455-459 — `{ quantity: Option<i32>, equipped: Option<bool>, attuned: Option<bool> }` — Request for updating an inventory item.
- pub `update_inventory_item` function L463-480 — `( state: State<'_, AppState>, inventory_id: String, request: UpdateInventoryRequ...` — Update an inventory item (quantity, equipped, attuned).
- pub `list_character_sources` function L494-507 — `( state: State<'_, AppState>, character_id: String, ) -> ApiResponse<Vec<String>...` — List allowed source codes for a character.
- pub `add_character_source` function L511-532 — `( state: State<'_, AppState>, character_id: String, source_code: String, ) -> Ap...` — Add a source to a character's allowed sources.
- pub `remove_character_source` function L536-550 — `( state: State<'_, AppState>, character_id: String, source_code: String, ) -> Ap...` — Remove a source from a character's allowed sources.
- pub `set_character_sources` function L554-579 — `( state: State<'_, AppState>, character_id: String, source_codes: Vec<String>, )...` — Set the complete list of allowed sources for a character (replaces existing).
- pub `list_character_spells` function L587-600 — `( state: State<'_, AppState>, character_id: String, ) -> ApiResponse<Vec<Charact...` — List all spells a character knows.
- pub `add_character_spell` function L604-644 — `( state: State<'_, AppState>, character_id: String, spell_name: String, spell_so...` — Add a spell to a character's known spells.
- pub `remove_character_spell` function L648-692 — `( state: State<'_, AppState>, character_id: String, spell_name: String, source_c...` — Remove a spell from a character's known spells.
- pub `toggle_spell_prepared` function L696-722 — `( state: State<'_, AppState>, spell_id: String, ) -> ApiResponse<CharacterSpell>` — Toggle a spell's prepared status.

#### crates/mimir/src/commands/dev.rs

- pub `AppInfo` struct L15-17 — `{ database_path: String }` — Application info returned to the frontend.
- pub `get_app_info` function L21-25 — `(state: State<'_, AppState>) -> ApiResponse<AppInfo>` — Get application info (database path, etc.) for the settings UI.
- pub `is_dev_mode` function L29-31 — `() -> ApiResponse<bool>` — Check if the application is running in development mode.
- pub `is_dev_seeded` function L35-49 — `(state: State<'_, AppState>) -> ApiResponse<bool>` — Check if dev seed data already exists.
- pub `seed_dev_data` function L56-68 — `(state: State<'_, AppState>) -> ApiResponse<bool>` — Seed development test data.
- pub `reseed_dev_data` function L75-93 — `(state: State<'_, AppState>) -> ApiResponse<bool>` — Clear dev seed data and reseed fresh.
- pub `clear_dev_data` function L97-109 — `(state: State<'_, AppState>) -> ApiResponse<()>` — Clear dev seed data without reseeding.

#### crates/mimir/src/commands/dm_map.rs

- pub `is_dm_map_open` function L13-15 — `(app: AppHandle) -> bool` — Check if the DM map window is currently open.
- pub `open_dm_map_window` function L22-47 — `( app: AppHandle, module_id: String, campaign_id: String, ) -> Result<(), String...` — Open the DM map window for a specific module.
- pub `close_dm_map_window` function L51-58 — `(app: AppHandle) -> Result<(), String>` — Close the DM map window.
- pub `toggle_dm_map_fullscreen` function L63-77 — `(app: AppHandle) -> Result<bool, String>` — Toggle fullscreen mode on the DM map window.
-  `DM_MAP_LABEL` variable L9 — `: &str` — while the main window stays on the module dashboard.

#### crates/mimir/src/commands/document.rs

- pub `list_campaign_documents` function L19-30 — `( state: State<'_, AppState>, campaign_id: String, ) -> ApiResponse<Vec<Document...` — List campaign-level documents (not in any module).
- pub `list_module_documents` function L34-45 — `( state: State<'_, AppState>, module_id: String, ) -> ApiResponse<Vec<Document>>` — List all documents for a specific module.
- pub `get_document` function L53-65 — `(state: State<'_, AppState>, id: String) -> ApiResponse<Document>` — Get a document by ID.
- pub `CreateDocumentRequest` struct L69-75 — `{ campaign_id: String, module_id: Option<String>, title: String, doc_type: Optio...` — Request for creating a document.
- pub `create_document` function L79-104 — `( state: State<'_, AppState>, request: CreateDocumentRequest, ) -> ApiResponse<D...` — Create a new document.
- pub `UpdateDocumentRequest` struct L108-112 — `{ title: Option<String>, content: Option<String>, doc_type: Option<String> }` — Request for updating a document.
- pub `update_document` function L116-134 — `( state: State<'_, AppState>, id: String, request: UpdateDocumentRequest, ) -> A...` — Update a document.
- pub `delete_document` function L138-146 — `(state: State<'_, AppState>, id: String) -> ApiResponse<()>` — Delete a document permanently.
- pub `reorder_document` function L154-166 — `( state: State<'_, AppState>, document_id: String, swap_with_id: String, ) -> Ap...` — Swap sort_order between two documents.
- pub `search_documents` function L174-186 — `( state: State<'_, AppState>, campaign_id: String, query: String, ) -> ApiRespon...` — Search documents using full-text search.
- pub `search_module_documents` function L190-202 — `( state: State<'_, AppState>, module_id: String, query: String, ) -> ApiResponse...` — Search documents within a specific module.

#### crates/mimir/src/commands/homebrew.rs

- pub `TauriCreateHomebrewItemInput` struct L18-26 — `{ campaign_id: String, name: String, item_type: Option<String>, rarity: Option<S...` — Input for creating a homebrew item (Tauri-facing, Deserialize).
- pub `TauriUpdateHomebrewItemInput` struct L30-35 — `{ name: Option<String>, item_type: Option<Option<String>>, rarity: Option<Option...` — Input for updating a homebrew item (Tauri-facing, Deserialize).
- pub `list_homebrew_items` function L39-49 — `( state: State<'_, AppState>, campaign_id: String, ) -> ApiResponse<Vec<Campaign...` — List all homebrew items for a campaign.
- pub `get_homebrew_item` function L53-63 — `( state: State<'_, AppState>, id: String, ) -> ApiResponse<CampaignHomebrewItem>` — Get a homebrew item by ID.
- pub `get_homebrew_item_by_name` function L67-78 — `( state: State<'_, AppState>, campaign_id: String, name: String, ) -> ApiRespons...` — Get a homebrew item by campaign_id and name.
- pub `create_homebrew_item` function L82-102 — `( state: State<'_, AppState>, input: TauriCreateHomebrewItemInput, ) -> ApiRespo...` — Create a new homebrew item.
- pub `update_homebrew_item` function L106-124 — `( state: State<'_, AppState>, id: String, input: TauriUpdateHomebrewItemInput, )...` — Update a homebrew item.
- pub `delete_homebrew_item` function L128-138 — `( state: State<'_, AppState>, id: String, ) -> ApiResponse<bool>` — Delete a homebrew item.

#### crates/mimir/src/commands/homebrew_monster.rs

- pub `TauriCreateHomebrewMonsterInput` struct L15-24 — `{ campaign_id: String, name: String, cr: Option<String>, creature_type: Option<S...` — Input for creating a homebrew monster (Tauri-facing, Deserialize).
- pub `TauriUpdateHomebrewMonsterInput` struct L28-34 — `{ name: Option<String>, cr: Option<Option<String>>, creature_type: Option<Option...` — Input for updating a homebrew monster (Tauri-facing, Deserialize).
- pub `list_homebrew_monsters` function L38-48 — `( state: State<'_, AppState>, campaign_id: String, ) -> ApiResponse<Vec<Campaign...` — List all homebrew monsters for a campaign.
- pub `get_homebrew_monster` function L52-62 — `( state: State<'_, AppState>, id: String, ) -> ApiResponse<CampaignHomebrewMonst...` — Get a homebrew monster by ID.
- pub `create_homebrew_monster` function L66-87 — `( state: State<'_, AppState>, input: TauriCreateHomebrewMonsterInput, ) -> ApiRe...` — Create a new homebrew monster.
- pub `update_homebrew_monster` function L91-110 — `( state: State<'_, AppState>, id: String, input: TauriUpdateHomebrewMonsterInput...` — Update a homebrew monster.
- pub `delete_homebrew_monster` function L114-124 — `( state: State<'_, AppState>, id: String, ) -> ApiResponse<bool>` — Delete a homebrew monster.

#### crates/mimir/src/commands/homebrew_spell.rs

- pub `TauriCreateHomebrewSpellInput` struct L15-23 — `{ campaign_id: String, name: String, level: Option<i32>, school: Option<String>,...` — Input for creating a homebrew spell (Tauri-facing, Deserialize).
- pub `TauriUpdateHomebrewSpellInput` struct L27-32 — `{ name: Option<String>, level: Option<Option<i32>>, school: Option<Option<String...` — Input for updating a homebrew spell (Tauri-facing, Deserialize).
- pub `list_homebrew_spells` function L36-46 — `( state: State<'_, AppState>, campaign_id: String, ) -> ApiResponse<Vec<Campaign...` — List all homebrew spells for a campaign.
- pub `get_homebrew_spell` function L50-60 — `( state: State<'_, AppState>, id: String, ) -> ApiResponse<CampaignHomebrewSpell...` — Get a homebrew spell by ID.
- pub `create_homebrew_spell` function L64-84 — `( state: State<'_, AppState>, input: TauriCreateHomebrewSpellInput, ) -> ApiResp...` — Create a new homebrew spell.
- pub `update_homebrew_spell` function L88-106 — `( state: State<'_, AppState>, id: String, input: TauriUpdateHomebrewSpellInput, ...` — Update a homebrew spell.
- pub `delete_homebrew_spell` function L110-120 — `( state: State<'_, AppState>, id: String, ) -> ApiResponse<bool>` — Delete a homebrew spell.

#### crates/mimir/src/commands/mod.rs

- pub `archive` module L6 — `-` — This module contains all Tauri commands that bridge the Vue.js frontend
- pub `asset` module L7 — `-` — with the mimir-core service layer.
- pub `campaign` module L8 — `-` — with the mimir-core service layer.
- pub `catalog` module L9 — `-` — with the mimir-core service layer.
- pub `character` module L10 — `-` — with the mimir-core service layer.
- pub `dev` module L11 — `-` — with the mimir-core service layer.
- pub `dm_map` module L12 — `-` — with the mimir-core service layer.
- pub `homebrew` module L13 — `-` — with the mimir-core service layer.
- pub `homebrew_monster` module L14 — `-` — with the mimir-core service layer.
- pub `homebrew_spell` module L15 — `-` — with the mimir-core service layer.
- pub `document` module L16 — `-` — with the mimir-core service layer.
- pub `map` module L17 — `-` — with the mimir-core service layer.
- pub `module` module L18 — `-` — with the mimir-core service layer.
- pub `player_display` module L19 — `-` — with the mimir-core service layer.
- pub `print` module L20 — `-` — with the mimir-core service layer.
- pub `source` module L21 — `-` — with the mimir-core service layer.
- pub `ApiResponse` struct L30-36 — `{ success: bool, data: Option<T>, error: Option<String> }` — API Response wrapper for frontend compatibility.
- pub `ok` function L40-46 — `(data: T) -> Self` — Create a successful response with data.
- pub `err` function L49-55 — `(message: impl Into<String>) -> Self` — Create an error response.
- pub `to_api_response` function L59-66 — `( result: Result<T, E>, ) -> ApiResponse<T>` — Helper to convert service results to API responses.
- pub `CatalogEntity` interface L70-76 — `{ fn id(), fn name(), fn source(), fn data(), fn fluff() }` — Trait for catalog entities that store JSON data as a string.
- pub `entity_to_json` function L80-101 — `(entity: &E) -> Value` — Convert a catalog entity to a JSON Value with parsed data.
- pub `entities_to_json` function L104-106 — `(entities: Vec<E>) -> Vec<Value>` — Convert a vector of catalog entities to JSON Values.
-  `fluff` function L75 — `(&self) -> Option<&str>` — with the mimir-core service layer.

#### crates/mimir/src/commands/module.rs

- pub `list_modules` function L23-31 — `(state: State<'_, AppState>, campaign_id: String) -> ApiResponse<Vec<Module>>` — List all modules for a campaign.
- pub `get_module` function L35-47 — `(state: State<'_, AppState>, id: String) -> ApiResponse<Module>` — Get a module by ID.
- pub `CreateModuleRequest` struct L51-56 — `{ campaign_id: String, name: String, description: Option<String>, module_type: O...` — Request for creating a new module.
- pub `create_module` function L60-80 — `( state: State<'_, AppState>, request: CreateModuleRequest, ) -> ApiResponse<Mod...` — Create a new module.
- pub `UpdateModuleRequest` struct L84-87 — `{ name: Option<String>, description: Option<Option<String>> }` — Request for updating a module.
- pub `update_module` function L91-108 — `( state: State<'_, AppState>, id: String, request: UpdateModuleRequest, ) -> Api...` — Update a module.
- pub `delete_module` function L112-120 — `(state: State<'_, AppState>, id: String) -> ApiResponse<()>` — Delete a module permanently.
- pub `get_module_by_number` function L124-143 — `( state: State<'_, AppState>, campaign_id: String, module_number: i32, ) -> ApiR...` — Get a module by campaign ID and module number.
- pub `reorder_module` function L147-159 — `( state: State<'_, AppState>, module_id: String, new_position: i32, ) -> ApiResp...` — Reorder a module by moving it to a new position (1-indexed).
- pub `MonsterWithData` struct L167-171 — `{ monster: ModuleMonster, monster_data: Option<serde_json::Value> }` — Monster with optional catalog data for display.
- pub `list_module_monsters_with_data` function L175-234 — `( state: State<'_, AppState>, module_id: String, ) -> ApiResponse<Vec<MonsterWit...` — List all monsters for a module with catalog data.
- pub `AddModuleMonsterRequest` struct L239-250 — `{ module_id: String, monster_name: Option<String>, monster_source: Option<String...` — Request for adding a module monster.
- pub `add_module_monster` function L258-349 — `( state: State<'_, AppState>, request: AddModuleMonsterRequest, ) -> ApiResponse...` — Add a monster to a module (or increment quantity if it already exists).
- pub `UpdateModuleMonsterRequest` struct L353-357 — `{ display_name: Option<String>, notes: Option<String>, quantity: Option<i32> }` — Request for updating a module monster.
- pub `update_module_monster` function L361-392 — `( state: State<'_, AppState>, monster_id: String, request: UpdateModuleMonsterRe...` — Update a module monster.
- pub `remove_module_monster` function L396-410 — `( state: State<'_, AppState>, monster_id: String, ) -> ApiResponse<()>` — Remove a monster from a module.
- pub `list_module_npcs` function L418-428 — `(state: State<'_, AppState>, module_id: String) -> ApiResponse<Vec<ModuleNpc>>` — List all NPCs for a module.
- pub `list_tokens` function L436-446 — `(state: State<'_, AppState>, map_id: String) -> ApiResponse<Vec<TokenResponse>>` — List all tokens for a map with resolved names.
- pub `list_token_summaries` function L450-452 — `(state: State<'_, AppState>, map_id: String) -> ApiResponse<Vec<TokenResponse>>` — List token summaries (alias for list_tokens for frontend compatibility).
- pub `CreateTokenRequest` struct L457-466 — `{ map_id: String, module_monster_id: Option<String>, module_npc_id: Option<Strin...` — Request for creating a token placement.
- pub `create_token` function L470-494 — `( state: State<'_, AppState>, request: CreateTokenRequest, ) -> ApiResponse<Toke...` — Create a new token placement.
- pub `UpdateTokenRequest` struct L498-504 — `{ grid_x: Option<i32>, grid_y: Option<i32>, label: Option<Option<String>>, facti...` — Request for updating a token placement.
- pub `update_token` function L508-531 — `( state: State<'_, AppState>, id: String, request: UpdateTokenRequest, ) -> ApiR...` — Update a token placement.
- pub `update_token_position` function L535-550 — `( state: State<'_, AppState>, id: String, grid_x: i32, grid_y: i32, ) -> ApiResp...` — Update only the position of a token (optimized for drag operations).
- pub `update_token_vision` function L554-577 — `( state: State<'_, AppState>, id: String, vision_bright_ft: Option<i32>, vision_...` — Update a token's vision settings (D&D 5e vision rules).
- pub `toggle_token_visibility` function L581-594 — `( state: State<'_, AppState>, id: String, ) -> ApiResponse<TokenResponse>` — Toggle a token's visibility (hidden from players).
- pub `delete_token` function L598-608 — `(state: State<'_, AppState>, id: String) -> ApiResponse<()>` — Delete a token placement.
- pub `serve_token_image` function L620-734 — `( state: State<'_, AppState>, token_id: String, ) -> ApiResponse<Option<String>>` — Serve a token's image as a base64 data URL.

#### crates/mimir/src/commands/player_display.rs

- pub `MapUpdatePayload` struct L12-21 — `{ map_id: String, grid_type: String, grid_size_px: Option<i32>, grid_offset_x: i...` — Payload for map updates sent to the player display
- pub `ViewportPayload` struct L25-29 — `{ x: f64, y: f64, zoom: f64 }` — Payload for viewport updates sent to the player display
- pub `BlackoutPayload` struct L34-36 — `{ is_blackout: bool }` — Payload for blackout state updates
- pub `is_player_display_open` function L42-44 — `(app: AppHandle) -> bool` — Check if the player display window is currently open.
- pub `open_player_display_window` function L48-64 — `(app: AppHandle) -> Result<(), String>` — Open the player display window.
- pub `close_player_display_window` function L68-75 — `(app: AppHandle) -> Result<(), String>` — Close the player display window.
- pub `toggle_player_display_fullscreen` function L80-94 — `(app: AppHandle) -> Result<bool, String>` — Toggle fullscreen mode on the player display window.
- pub `send_map_to_display` function L98-129 — `( app: AppHandle, map_id: String, grid_type: String, grid_size_px: Option<i32>, ...` — Send a map to the player display window.
- pub `update_display_viewport` function L133-145 — `(app: AppHandle, x: f64, y: f64, zoom: f64) -> Result<(), String>` — Update the viewport on the player display window (pan/zoom).
- pub `toggle_display_blackout` function L149-161 — `(app: AppHandle, is_blackout: bool) -> Result<(), String>` — Toggle or set blackout mode on the player display.
-  `PLAYER_DISPLAY_LABEL` variable L38 — `: &str` — These commands handle window creation, destruction, fullscreen, and IPC events.

#### crates/mimir/src/commands/source.rs

- pub `SourceInfo` struct L24-33 — `{ id: String, name: String, enabled: bool, imported_at: String }` — Response for source listing with frontend-compatible fields.
- pub `ImportResponse` struct L48-57 — `{ sources_imported: usize, sources_failed: usize, total_entities: usize, message...` — Response for import operation.
- pub `list_catalog_sources` function L63-74 — `(state: State<'_, AppState>) -> ApiResponse<Vec<SourceInfo>>` — List all imported catalog sources (books).
- pub `import_catalog_from_zip` function L80-131 — `( state: State<'_, AppState>, archive_path: String, ) -> ApiResponse<ImportRespo...` — Import catalog data from a tar.gz archive containing 5etools data.
- pub `set_source_enabled` function L135-149 — `( state: State<'_, AppState>, source_code: String, enabled: bool, ) -> ApiRespon...` — Set the enabled status for a catalog source.
- pub `delete_catalog_source` function L156-172 — `( state: State<'_, AppState>, source_code: String, ) -> ApiResponse<()>` — Delete a catalog source and all its associated entities.
- pub `import_catalog_images` function L184-218 — `( state: State<'_, AppState>, archive_path: String, ) -> ApiResponse<ImportRespo...` — Import images from a tar.gz archive.
- pub `LibraryBookInfo` struct L329-341 — `{ id: String, name: String, enabled: bool, imported_at: String, cover_path: Opti...` — Book info for the library listing (Reading mode).
- pub `list_library_books` function L348-380 — `(state: State<'_, AppState>) -> ApiResponse<Vec<LibraryBookInfo>>` — List all books with readable content (for Reading mode).
- pub `get_book_content` function L387-402 — `( state: State<'_, AppState>, book_id: String, ) -> ApiResponse<BookContent>` — Get book content for reading.
- pub `serve_book_image` function L409-454 — `( state: State<'_, AppState>, _book_id: String, image_path: String, ) -> ApiResp...` — Serve a book image as a base64 data URL.
-  `SourceInfo` type L35-44 — `= SourceInfo` — Tauri commands for managing catalog sources (importing 5etools data, listing sources, etc.)
-  `from` function L36-43 — `(source: CatalogSource) -> Self` — Tauri commands for managing catalog sources (importing 5etools data, listing sources, etc.)
-  `stream_images_from_tarball` function L226-320 — `(archive_path: &Path, dest: &Path) -> Result<usize, String>` — Stream images from a tar.gz archive directly to the destination.

### crates/mimir/src/commands/catalog

> *Semantic summary to be generated by AI agent.*

#### crates/mimir/src/commands/catalog/characters.rs

- pub `search_races` function L25-46 — `( state: State<'_, AppState>, filter: Option<RaceFilter>, limit: Option<i64>, of...` — Search races with optional filters.
- pub `get_race` function L50-62 — `(state: State<'_, AppState>, id: i32) -> ApiResponse<Race>` — Get a race by database ID.
- pub `get_race_by_name` function L66-82 — `( state: State<'_, AppState>, name: String, source: String, ) -> ApiResponse<Val...` — Get a race by name and source.
- pub `list_race_sources` function L86-94 — `(state: State<'_, AppState>) -> ApiResponse<Vec<String>>` — List all race sources.
- pub `count_races` function L98-106 — `(state: State<'_, AppState>) -> ApiResponse<i64>` — Count total races.
- pub `search_backgrounds` function L114-135 — `( state: State<'_, AppState>, filter: Option<BackgroundFilter>, limit: Option<i6...` — Search backgrounds with optional filters.
- pub `get_background` function L139-151 — `(state: State<'_, AppState>, id: i32) -> ApiResponse<Background>` — Get a background by database ID.
- pub `get_background_by_name` function L155-171 — `( state: State<'_, AppState>, name: String, source: String, ) -> ApiResponse<Val...` — Get a background by name and source.
- pub `list_background_sources` function L175-183 — `(state: State<'_, AppState>) -> ApiResponse<Vec<String>>` — List all background sources.
- pub `count_backgrounds` function L187-195 — `(state: State<'_, AppState>) -> ApiResponse<i64>` — Count total backgrounds.
- pub `search_classes` function L203-224 — `( state: State<'_, AppState>, filter: Option<ClassFilter>, limit: Option<i64>, o...` — Search classes with optional filters.
- pub `get_class` function L228-240 — `(state: State<'_, AppState>, id: i32) -> ApiResponse<Class>` — Get a class by database ID.
- pub `get_class_by_name` function L244-260 — `( state: State<'_, AppState>, name: String, source: String, ) -> ApiResponse<Val...` — Get a class by name and source.
- pub `list_class_sources` function L264-272 — `(state: State<'_, AppState>) -> ApiResponse<Vec<String>>` — List all class sources.
- pub `count_classes` function L276-284 — `(state: State<'_, AppState>) -> ApiResponse<i64>` — Count total classes.
- pub `get_class_feature` function L292-308 — `( state: State<'_, AppState>, name: String, class_name: String, ) -> ApiResponse...` — Get a class feature by name and class.
- pub `list_class_features` function L312-327 — `( state: State<'_, AppState>, class_name: String, class_source: String, ) -> Api...` — List all class features for a class.
- pub `get_subclass` function L335-346 — `(state: State<'_, AppState>, id: i32) -> ApiResponse<Subclass>` — Get a subclass by database ID.
- pub `get_subclass_by_name` function L350-370 — `( state: State<'_, AppState>, name: String, class_name: String, source: String, ...` — Get a subclass by name, class, and source.
- pub `list_subclasses_by_class` function L374-388 — `( state: State<'_, AppState>, class_name: String, ) -> ApiResponse<Vec<Value>>` — List all subclasses for a class.
- pub `count_subclasses` function L392-400 — `(state: State<'_, AppState>) -> ApiResponse<i64>` — Count total subclasses.
- pub `get_subclass_feature` function L408-429 — `( state: State<'_, AppState>, name: String, subclass_name: String, subclass_sour...` — Get a subclass feature by name and subclass.
- pub `list_subclass_features` function L433-448 — `( state: State<'_, AppState>, subclass_name: String, subclass_source: String, ) ...` — List all subclass features for a subclass.
- pub `search_feats` function L456-477 — `( state: State<'_, AppState>, filter: Option<FeatFilter>, limit: Option<i64>, of...` — Search feats with optional filters.
- pub `get_feat` function L481-493 — `(state: State<'_, AppState>, id: i32) -> ApiResponse<Feat>` — Get a feat by database ID.
- pub `get_feat_by_name` function L497-513 — `( state: State<'_, AppState>, name: String, source: String, ) -> ApiResponse<Val...` — Get a feat by name and source.
- pub `list_feat_sources` function L517-525 — `(state: State<'_, AppState>) -> ApiResponse<Vec<String>>` — List all feat sources.
- pub `count_feats` function L529-537 — `(state: State<'_, AppState>) -> ApiResponse<i64>` — Count total feats.

#### crates/mimir/src/commands/catalog/helpers.rs

- pub `find_subclass_level` function L8-39 — `(data: &Value) -> i32` — Find the level at which a class gains its subclass.
- pub `extract_multiclass_prereqs` function L42-49 — `(data: &Value) -> Value` — Extract multiclass prerequisites from class data.
- pub `determine_asi_levels` function L52-94 — `(class_name: &str, data: &Value) -> Vec<i32>` — Determine ASI levels for a class.
- pub `extract_spell_slots_from_table` function L97-114 — `(table_groups: &[Value]) -> Vec<Value>` — Extract spell slot progression from class table groups.
- pub `generate_spell_slot_progression` function L117-214 — `(caster_type: &str) -> Vec<Vec<i32>>` — Generate standard spell slot progression based on caster type.
- pub `extract_fighting_style_classes` function L217-237 — `(feature_type: Option<&str>) -> Vec<String>` — Extract which classes can use a fighting style based on its type code.
- pub `extract_invocation_prereqs` function L240-275 — `(prereq: Option<&Value>) -> (Option<i32>, Option<String>, Option<String>)` — Extract invocation prerequisites (level, pact boon, spell requirements).
- pub `extract_feat_prereqs` function L278-339 — `(prereq: Option<&Value>) -> Vec<String>` — Extract feat prerequisites into a simple list.

#### crates/mimir/src/commands/catalog/items.rs

- pub `search_items` function L13-34 — `( state: State<'_, AppState>, filter: Option<ItemFilter>, limit: Option<i64>, of...` — Search items with optional filters.
- pub `get_item` function L38-50 — `(state: State<'_, AppState>, id: i32) -> ApiResponse<Item>` — Get an item by database ID.
- pub `get_item_by_name` function L54-100 — `( state: State<'_, AppState>, name: String, source: String, campaign_id: Option<...` — Get an item by name and source.
- pub `list_weapon_names` function L104-112 — `(state: State<'_, AppState>) -> ApiResponse<Vec<String>>` — List all weapon names from the catalog (melee + ranged).
- pub `list_item_sources` function L116-124 — `(state: State<'_, AppState>) -> ApiResponse<Vec<String>>` — List all item sources.
- pub `count_items` function L128-136 — `(state: State<'_, AppState>) -> ApiResponse<i64>` — Count total items.

#### crates/mimir/src/commands/catalog/level_up.rs

- pub `get_class_info` function L23-107 — `( state: State<'_, AppState>, name: String, source: String, ) -> ApiResponse<Val...` — Get class information needed for level-up decisions.
- pub `get_class_spellcasting` function L113-191 — `( state: State<'_, AppState>, name: String, source: String, ) -> ApiResponse<Val...` — Get spellcasting progression for a class.
- pub `list_fighting_styles` function L195-226 — `(state: State<'_, AppState>) -> ApiResponse<Vec<Value>>` — List all fighting styles.
- pub `list_metamagic` function L230-241 — `(state: State<'_, AppState>) -> ApiResponse<Vec<Value>>` — List all metamagic options.
- pub `list_maneuvers` function L245-268 — `(state: State<'_, AppState>) -> ApiResponse<Vec<Value>>` — List all Battle Master maneuvers.
- pub `list_invocations` function L272-306 — `(state: State<'_, AppState>) -> ApiResponse<Vec<Value>>` — List all Eldritch Invocations with their prerequisites.
- pub `list_feats_with_prereqs` function L310-347 — `( state: State<'_, AppState>, filter: Option<FeatFilter>, limit: Option<i64>, of...` — List all feats with their prerequisites parsed.

#### crates/mimir/src/commands/catalog/mod.rs

- pub `monsters` module L6 — `-` — Tauri commands for searching and retrieving D&D 5e catalog content.
- pub `spells` module L7 — `-` — Split into sub-modules by entity type for better organization.
- pub `items` module L8 — `-` — Split into sub-modules by entity type for better organization.
- pub `characters` module L9 — `-` — Split into sub-modules by entity type for better organization.
- pub `world` module L10 — `-` — Split into sub-modules by entity type for better organization.
- pub `other` module L11 — `-` — Split into sub-modules by entity type for better organization.
- pub `level_up` module L12 — `-` — Split into sub-modules by entity type for better organization.
-  `helpers` module L13 — `-` — Split into sub-modules by entity type for better organization.
-  `Monster` type L36-42 — `impl CatalogEntity for Monster` — Split into sub-modules by entity type for better organization.
-  `id` function L37 — `(&self) -> Option<i32>` — Split into sub-modules by entity type for better organization.
-  `name` function L38 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `source` function L39 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `data` function L40 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `fluff` function L41 — `(&self) -> Option<&str>` — Split into sub-modules by entity type for better organization.
-  `Spell` type L44-50 — `impl CatalogEntity for Spell` — Split into sub-modules by entity type for better organization.
-  `id` function L45 — `(&self) -> Option<i32>` — Split into sub-modules by entity type for better organization.
-  `name` function L46 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `source` function L47 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `data` function L48 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `fluff` function L49 — `(&self) -> Option<&str>` — Split into sub-modules by entity type for better organization.
-  `Item` type L52-58 — `impl CatalogEntity for Item` — Split into sub-modules by entity type for better organization.
-  `id` function L53 — `(&self) -> Option<i32>` — Split into sub-modules by entity type for better organization.
-  `name` function L54 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `source` function L55 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `data` function L56 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `fluff` function L57 — `(&self) -> Option<&str>` — Split into sub-modules by entity type for better organization.
-  `Race` type L60-66 — `impl CatalogEntity for Race` — Split into sub-modules by entity type for better organization.
-  `id` function L61 — `(&self) -> Option<i32>` — Split into sub-modules by entity type for better organization.
-  `name` function L62 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `source` function L63 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `data` function L64 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `fluff` function L65 — `(&self) -> Option<&str>` — Split into sub-modules by entity type for better organization.
-  `Background` type L68-74 — `impl CatalogEntity for Background` — Split into sub-modules by entity type for better organization.
-  `id` function L69 — `(&self) -> Option<i32>` — Split into sub-modules by entity type for better organization.
-  `name` function L70 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `source` function L71 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `data` function L72 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `fluff` function L73 — `(&self) -> Option<&str>` — Split into sub-modules by entity type for better organization.
-  `Class` type L76-82 — `impl CatalogEntity for Class` — Split into sub-modules by entity type for better organization.
-  `id` function L77 — `(&self) -> Option<i32>` — Split into sub-modules by entity type for better organization.
-  `name` function L78 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `source` function L79 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `data` function L80 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `fluff` function L81 — `(&self) -> Option<&str>` — Split into sub-modules by entity type for better organization.
-  `ClassFeature` type L84-90 — `impl CatalogEntity for ClassFeature` — Split into sub-modules by entity type for better organization.
-  `id` function L85 — `(&self) -> Option<i32>` — Split into sub-modules by entity type for better organization.
-  `name` function L86 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `source` function L87 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `data` function L88 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `fluff` function L89 — `(&self) -> Option<&str>` — Split into sub-modules by entity type for better organization.
-  `Subclass` type L92-98 — `impl CatalogEntity for Subclass` — Split into sub-modules by entity type for better organization.
-  `id` function L93 — `(&self) -> Option<i32>` — Split into sub-modules by entity type for better organization.
-  `name` function L94 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `source` function L95 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `data` function L96 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `fluff` function L97 — `(&self) -> Option<&str>` — Split into sub-modules by entity type for better organization.
-  `SubclassFeature` type L100-106 — `impl CatalogEntity for SubclassFeature` — Split into sub-modules by entity type for better organization.
-  `id` function L101 — `(&self) -> Option<i32>` — Split into sub-modules by entity type for better organization.
-  `name` function L102 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `source` function L103 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `data` function L104 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `fluff` function L105 — `(&self) -> Option<&str>` — Split into sub-modules by entity type for better organization.
-  `Feat` type L108-114 — `impl CatalogEntity for Feat` — Split into sub-modules by entity type for better organization.
-  `id` function L109 — `(&self) -> Option<i32>` — Split into sub-modules by entity type for better organization.
-  `name` function L110 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `source` function L111 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `data` function L112 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `fluff` function L113 — `(&self) -> Option<&str>` — Split into sub-modules by entity type for better organization.
-  `Condition` type L116-122 — `impl CatalogEntity for Condition` — Split into sub-modules by entity type for better organization.
-  `id` function L117 — `(&self) -> Option<i32>` — Split into sub-modules by entity type for better organization.
-  `name` function L118 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `source` function L119 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `data` function L120 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `fluff` function L121 — `(&self) -> Option<&str>` — Split into sub-modules by entity type for better organization.
-  `Disease` type L124-130 — `impl CatalogEntity for Disease` — Split into sub-modules by entity type for better organization.
-  `id` function L125 — `(&self) -> Option<i32>` — Split into sub-modules by entity type for better organization.
-  `name` function L126 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `source` function L127 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `data` function L128 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `fluff` function L129 — `(&self) -> Option<&str>` — Split into sub-modules by entity type for better organization.
-  `Language` type L132-137 — `impl CatalogEntity for Language` — Split into sub-modules by entity type for better organization.
-  `id` function L133 — `(&self) -> Option<i32>` — Split into sub-modules by entity type for better organization.
-  `name` function L134 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `source` function L135 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `data` function L136 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `Trap` type L139-144 — `impl CatalogEntity for Trap` — Split into sub-modules by entity type for better organization.
-  `id` function L140 — `(&self) -> Option<i32>` — Split into sub-modules by entity type for better organization.
-  `name` function L141 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `source` function L142 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `data` function L143 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `Hazard` type L146-151 — `impl CatalogEntity for Hazard` — Split into sub-modules by entity type for better organization.
-  `id` function L147 — `(&self) -> Option<i32>` — Split into sub-modules by entity type for better organization.
-  `name` function L148 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `source` function L149 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `data` function L150 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `Action` type L153-158 — `impl CatalogEntity for Action` — Split into sub-modules by entity type for better organization.
-  `id` function L154 — `(&self) -> Option<i32>` — Split into sub-modules by entity type for better organization.
-  `name` function L155 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `source` function L156 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `data` function L157 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `Deity` type L160-165 — `impl CatalogEntity for Deity` — Split into sub-modules by entity type for better organization.
-  `id` function L161 — `(&self) -> Option<i32>` — Split into sub-modules by entity type for better organization.
-  `name` function L162 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `source` function L163 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `data` function L164 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `OptionalFeature` type L167-172 — `impl CatalogEntity for OptionalFeature` — Split into sub-modules by entity type for better organization.
-  `id` function L168 — `(&self) -> Option<i32>` — Split into sub-modules by entity type for better organization.
-  `name` function L169 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `source` function L170 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `data` function L171 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `CatalogTable` type L174-179 — `impl CatalogEntity for CatalogTable` — Split into sub-modules by entity type for better organization.
-  `id` function L175 — `(&self) -> Option<i32>` — Split into sub-modules by entity type for better organization.
-  `name` function L176 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `source` function L177 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `data` function L178 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `VariantRule` type L181-186 — `impl CatalogEntity for VariantRule` — Split into sub-modules by entity type for better organization.
-  `id` function L182 — `(&self) -> Option<i32>` — Split into sub-modules by entity type for better organization.
-  `name` function L183 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `source` function L184 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `data` function L185 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `Vehicle` type L188-194 — `impl CatalogEntity for Vehicle` — Split into sub-modules by entity type for better organization.
-  `id` function L189 — `(&self) -> Option<i32>` — Split into sub-modules by entity type for better organization.
-  `name` function L190 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `source` function L191 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `data` function L192 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `fluff` function L193 — `(&self) -> Option<&str>` — Split into sub-modules by entity type for better organization.
-  `Cult` type L196-201 — `impl CatalogEntity for Cult` — Split into sub-modules by entity type for better organization.
-  `id` function L197 — `(&self) -> Option<i32>` — Split into sub-modules by entity type for better organization.
-  `name` function L198 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `source` function L199 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `data` function L200 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `Psionic` type L203-208 — `impl CatalogEntity for Psionic` — Split into sub-modules by entity type for better organization.
-  `id` function L204 — `(&self) -> Option<i32>` — Split into sub-modules by entity type for better organization.
-  `name` function L205 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `source` function L206 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `data` function L207 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `Reward` type L210-215 — `impl CatalogEntity for Reward` — Split into sub-modules by entity type for better organization.
-  `id` function L211 — `(&self) -> Option<i32>` — Split into sub-modules by entity type for better organization.
-  `name` function L212 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `source` function L213 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `data` function L214 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `Object` type L217-223 — `impl CatalogEntity for Object` — Split into sub-modules by entity type for better organization.
-  `id` function L218 — `(&self) -> Option<i32>` — Split into sub-modules by entity type for better organization.
-  `name` function L219 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `source` function L220 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `data` function L221 — `(&self) -> &str` — Split into sub-modules by entity type for better organization.
-  `fluff` function L222 — `(&self) -> Option<&str>` — Split into sub-modules by entity type for better organization.

#### crates/mimir/src/commands/catalog/monsters.rs

- pub `search_monsters` function L13-41 — `( state: State<'_, AppState>, filter: Option<MonsterFilter>, limit: Option<i64>,...` — Search monsters with optional filters.
- pub `get_monster` function L45-57 — `(state: State<'_, AppState>, id: i32) -> ApiResponse<Monster>` — Get a monster by database ID.
- pub `get_monster_by_name` function L61-77 — `( state: State<'_, AppState>, name: String, source: String, ) -> ApiResponse<Val...` — Get a monster by name and source.
- pub `list_monster_sources` function L81-89 — `(state: State<'_, AppState>) -> ApiResponse<Vec<String>>` — List all monster sources.
- pub `count_monsters` function L93-101 — `(state: State<'_, AppState>) -> ApiResponse<i64>` — Count total monsters.

#### crates/mimir/src/commands/catalog/other.rs

- pub `search_optional_features` function L26-47 — `( state: State<'_, AppState>, filter: Option<OptionalFeatureFilter>, limit: Opti...` — Search optional features with optional filters.
- pub `get_optional_feature` function L51-63 — `(state: State<'_, AppState>, id: i32) -> ApiResponse<OptionalFeature>` — Get an optional feature by database ID.
- pub `get_optional_feature_by_name` function L67-83 — `( state: State<'_, AppState>, name: String, source: String, ) -> ApiResponse<Val...` — Get an optional feature by name and source.
- pub `list_optional_feature_sources` function L87-95 — `(state: State<'_, AppState>) -> ApiResponse<Vec<String>>` — List all optional feature sources.
- pub `count_optional_features` function L99-107 — `(state: State<'_, AppState>) -> ApiResponse<i64>` — Count total optional features.
- pub `search_tables` function L115-136 — `( state: State<'_, AppState>, filter: Option<CatalogTableFilter>, limit: Option<...` — Search catalog tables with optional filters.
- pub `get_table` function L140-152 — `(state: State<'_, AppState>, id: i32) -> ApiResponse<CatalogTable>` — Get a catalog table by database ID.
- pub `get_table_by_name` function L156-172 — `( state: State<'_, AppState>, name: String, source: String, ) -> ApiResponse<Val...` — Get a catalog table by name and source.
- pub `list_table_sources` function L176-184 — `(state: State<'_, AppState>) -> ApiResponse<Vec<String>>` — List all table sources.
- pub `count_tables` function L188-196 — `(state: State<'_, AppState>) -> ApiResponse<i64>` — Count total tables.
- pub `search_variant_rules` function L204-225 — `( state: State<'_, AppState>, filter: Option<VariantRuleFilter>, limit: Option<i...` — Search variant rules with optional filters.
- pub `get_variant_rule` function L229-241 — `(state: State<'_, AppState>, id: i32) -> ApiResponse<VariantRule>` — Get a variant rule by database ID.
- pub `get_variant_rule_by_name` function L245-261 — `( state: State<'_, AppState>, name: String, source: String, ) -> ApiResponse<Val...` — Get a variant rule by name and source.
- pub `list_variant_rule_sources` function L265-273 — `(state: State<'_, AppState>) -> ApiResponse<Vec<String>>` — List all variant rule sources.
- pub `count_variant_rules` function L277-285 — `(state: State<'_, AppState>) -> ApiResponse<i64>` — Count total variant rules.
- pub `search_vehicles` function L293-314 — `( state: State<'_, AppState>, filter: Option<VehicleFilter>, limit: Option<i64>,...` — Search vehicles with optional filters.
- pub `get_vehicle` function L318-330 — `(state: State<'_, AppState>, id: i32) -> ApiResponse<Vehicle>` — Get a vehicle by database ID.
- pub `get_vehicle_by_name` function L334-350 — `( state: State<'_, AppState>, name: String, source: String, ) -> ApiResponse<Val...` — Get a vehicle by name and source.
- pub `list_vehicle_sources` function L354-362 — `(state: State<'_, AppState>) -> ApiResponse<Vec<String>>` — List all vehicle sources.
- pub `count_vehicles` function L366-374 — `(state: State<'_, AppState>) -> ApiResponse<i64>` — Count total vehicles.
- pub `search_cults` function L382-438 — `( state: State<'_, AppState>, filter: Option<CultFilter>, limit: Option<i64>, of...` — Search cults with optional filters.
- pub `get_cult` function L442-454 — `(state: State<'_, AppState>, id: i32) -> ApiResponse<Cult>` — Get a cult by database ID.
- pub `get_cult_by_name` function L458-502 — `( state: State<'_, AppState>, name: String, source: String, ) -> ApiResponse<Val...` — Get a cult by name and source.
- pub `list_cult_sources` function L506-514 — `(state: State<'_, AppState>) -> ApiResponse<Vec<String>>` — List all cult sources.
- pub `count_cults` function L518-526 — `(state: State<'_, AppState>) -> ApiResponse<i64>` — Count total cults.
- pub `search_psionics` function L534-555 — `( state: State<'_, AppState>, filter: Option<PsionicFilter>, limit: Option<i64>,...` — Search psionics with optional filters.
- pub `get_psionic` function L559-571 — `(state: State<'_, AppState>, id: i32) -> ApiResponse<Psionic>` — Get a psionic by database ID.
- pub `get_psionic_by_name` function L575-591 — `( state: State<'_, AppState>, name: String, source: String, ) -> ApiResponse<Val...` — Get a psionic by name and source.
- pub `list_psionic_sources` function L595-603 — `(state: State<'_, AppState>) -> ApiResponse<Vec<String>>` — List all psionic sources.
- pub `count_psionics` function L607-615 — `(state: State<'_, AppState>) -> ApiResponse<i64>` — Count total psionics.
- pub `search_rewards` function L623-644 — `( state: State<'_, AppState>, filter: Option<RewardFilter>, limit: Option<i64>, ...` — Search rewards with optional filters.
- pub `get_reward` function L648-660 — `(state: State<'_, AppState>, id: i32) -> ApiResponse<Reward>` — Get a reward by database ID.
- pub `get_reward_by_name` function L664-680 — `( state: State<'_, AppState>, name: String, source: String, ) -> ApiResponse<Val...` — Get a reward by name and source.
- pub `list_reward_sources` function L684-692 — `(state: State<'_, AppState>) -> ApiResponse<Vec<String>>` — List all reward sources.
- pub `count_rewards` function L696-704 — `(state: State<'_, AppState>) -> ApiResponse<i64>` — Count total rewards.
- pub `search_objects` function L712-733 — `( state: State<'_, AppState>, filter: Option<ObjectFilter>, limit: Option<i64>, ...` — Search objects with optional filters.
- pub `get_object` function L737-749 — `(state: State<'_, AppState>, id: i32) -> ApiResponse<Object>` — Get an object by database ID.
- pub `get_object_by_name` function L753-769 — `( state: State<'_, AppState>, name: String, source: String, ) -> ApiResponse<Val...` — Get an object by name and source.
- pub `list_object_sources` function L773-781 — `(state: State<'_, AppState>) -> ApiResponse<Vec<String>>` — List all object sources.
- pub `count_objects` function L785-793 — `(state: State<'_, AppState>) -> ApiResponse<i64>` — Count total objects.

#### crates/mimir/src/commands/catalog/spells.rs

- pub `search_spells` function L13-34 — `( state: State<'_, AppState>, filter: Option<SpellFilter>, limit: Option<i64>, o...` — Search spells with optional filters.
- pub `get_spell` function L38-50 — `(state: State<'_, AppState>, id: i32) -> ApiResponse<Spell>` — Get a spell by database ID.
- pub `get_spell_by_name` function L54-70 — `( state: State<'_, AppState>, name: String, source: String, ) -> ApiResponse<Val...` — Get a spell by name and source.
- pub `list_spell_sources` function L74-82 — `(state: State<'_, AppState>) -> ApiResponse<Vec<String>>` — List all spell sources.
- pub `count_spells` function L86-94 — `(state: State<'_, AppState>) -> ApiResponse<i64>` — Count total spells.
- pub `get_spells_by_class` function L100-120 — `( state: State<'_, AppState>, class_name: String, level: Option<i32>, ) -> ApiRe...` — Get spells available to a specific class.

#### crates/mimir/src/commands/catalog/world.rs

- pub `search_conditions` function L25-94 — `( state: State<'_, AppState>, filter: Option<ConditionFilter>, limit: Option<i64...` — Search conditions and diseases with optional filters.
- pub `get_condition` function L98-110 — `(state: State<'_, AppState>, id: i32) -> ApiResponse<Condition>` — Get a condition by database ID.
- pub `get_condition_by_name` function L114-130 — `( state: State<'_, AppState>, name: String, source: String, ) -> ApiResponse<Val...` — Get a condition by name and source.
- pub `list_condition_sources` function L134-142 — `(state: State<'_, AppState>) -> ApiResponse<Vec<String>>` — List all condition sources.
- pub `count_conditions` function L146-154 — `(state: State<'_, AppState>) -> ApiResponse<i64>` — Count total conditions.
- pub `search_languages` function L162-183 — `( state: State<'_, AppState>, filter: Option<LanguageFilter>, limit: Option<i64>...` — Search languages with optional filters.
- pub `get_language` function L187-199 — `(state: State<'_, AppState>, id: i32) -> ApiResponse<Language>` — Get a language by database ID.
- pub `get_language_by_name` function L203-219 — `( state: State<'_, AppState>, name: String, source: String, ) -> ApiResponse<Val...` — Get a language by name and source.
- pub `list_language_sources` function L223-231 — `(state: State<'_, AppState>) -> ApiResponse<Vec<String>>` — List all language sources.
- pub `count_languages` function L235-243 — `(state: State<'_, AppState>) -> ApiResponse<i64>` — Count total languages.
- pub `search_traps` function L269-335 — `( state: State<'_, AppState>, filter: Option<TrapFilter>, limit: Option<i64>, of...` — Search traps and hazards with optional filters.
- pub `get_trap` function L339-351 — `(state: State<'_, AppState>, id: i32) -> ApiResponse<Trap>` — Get a trap by database ID.
- pub `get_trap_by_name` function L355-371 — `( state: State<'_, AppState>, name: String, source: String, ) -> ApiResponse<Val...` — Get a trap by name and source.
- pub `list_trap_sources` function L375-383 — `(state: State<'_, AppState>) -> ApiResponse<Vec<String>>` — List all trap sources.
- pub `count_traps` function L387-395 — `(state: State<'_, AppState>) -> ApiResponse<i64>` — Count total traps.
- pub `search_hazards` function L403-424 — `( state: State<'_, AppState>, filter: Option<HazardFilter>, limit: Option<i64>, ...` — Search hazards with optional filters.
- pub `get_hazard` function L428-440 — `(state: State<'_, AppState>, id: i32) -> ApiResponse<Hazard>` — Get a hazard by database ID.
- pub `get_hazard_by_name` function L444-460 — `( state: State<'_, AppState>, name: String, source: String, ) -> ApiResponse<Val...` — Get a hazard by name and source.
- pub `list_hazard_sources` function L464-472 — `(state: State<'_, AppState>) -> ApiResponse<Vec<String>>` — List all hazard sources.
- pub `count_hazards` function L476-484 — `(state: State<'_, AppState>) -> ApiResponse<i64>` — Count total hazards.
- pub `search_actions` function L492-513 — `( state: State<'_, AppState>, filter: Option<ActionFilter>, limit: Option<i64>, ...` — Search actions with optional filters.
- pub `get_action` function L517-529 — `(state: State<'_, AppState>, id: i32) -> ApiResponse<Action>` — Get an action by database ID.
- pub `get_action_by_name` function L533-549 — `( state: State<'_, AppState>, name: String, source: String, ) -> ApiResponse<Val...` — Get an action by name and source.
- pub `list_action_sources` function L553-561 — `(state: State<'_, AppState>) -> ApiResponse<Vec<String>>` — List all action sources.
- pub `count_actions` function L565-573 — `(state: State<'_, AppState>) -> ApiResponse<i64>` — Count total actions.
- pub `search_deities` function L581-602 — `( state: State<'_, AppState>, filter: Option<DeityFilter>, limit: Option<i64>, o...` — Search deities with optional filters.
- pub `get_deity` function L606-618 — `(state: State<'_, AppState>, id: i32) -> ApiResponse<Deity>` — Get a deity by database ID.
- pub `get_deity_by_name` function L622-638 — `( state: State<'_, AppState>, name: String, source: String, ) -> ApiResponse<Val...` — Get a deity by name and source.
- pub `list_deity_sources` function L642-650 — `(state: State<'_, AppState>) -> ApiResponse<Vec<String>>` — List all deity sources.
- pub `count_deities` function L654-662 — `(state: State<'_, AppState>) -> ApiResponse<i64>` — Count total deities.
-  `format_trap_type` function L250-265 — `(type_code: Option<&str>) -> String` — Helper to format trap/hazard type codes.

### crates/mimir/src/commands/map

> *Semantic summary to be generated by AI agent.*

#### crates/mimir/src/commands/map/crud.rs

- pub `list_campaign_maps` function L19-30 — `(state: State<'_, AppState>, campaign_id: String) -> ApiResponse<Vec<MapResponse...` — List all maps for a campaign (including module maps).
- pub `list_campaign_level_maps` function L34-48 — `( state: State<'_, AppState>, campaign_id: String, ) -> ApiResponse<Vec<MapRespo...` — List only campaign-level maps (not in any module).
- pub `list_module_maps` function L52-63 — `(state: State<'_, AppState>, module_id: String) -> ApiResponse<Vec<MapResponse>>` — List all maps for a module.
- pub `get_map` function L71-83 — `(state: State<'_, AppState>, id: String) -> ApiResponse<MapResponse>` — Get a map by ID.
- pub `CreateMapRequest` struct L96-105 — `{ campaign_id: String, module_id: Option<String>, name: String, filename: String...` — Request for creating a map.
- pub `create_map` function L109-142 — `(state: State<'_, AppState>, request: CreateMapRequest) -> ApiResponse<Map>` — Create a new map from a UVTT file.
- pub `UpdateMapRequest` struct L146-151 — `{ name: Option<String>, description: Option<Option<String>>, sort_order: Option<...` — Request for updating a map.
- pub `update_map` function L155-175 — `( state: State<'_, AppState>, id: String, request: UpdateMapRequest, ) -> ApiRes...` — Update a map.
- pub `delete_map` function L179-187 — `(state: State<'_, AppState>, id: String) -> ApiResponse<()>` — Delete a map and its associated UVTT asset.
-  `parse_lighting_mode` function L86-92 — `(s: Option<&str>) -> LightingMode` — Parse lighting mode from string.

#### crates/mimir/src/commands/map/fog.rs

- pub `get_fog_state` function L21-44 — `(state: State<'_, AppState>, map_id: String) -> ApiResponse<FogState>` — Get the fog state for a map (enabled + revealed areas).
- pub `toggle_fog` function L48-69 — `(state: State<'_, AppState>, map_id: String) -> ApiResponse<bool>` — Toggle fog of war on/off for a map.
- pub `enable_fog` function L73-86 — `(state: State<'_, AppState>, map_id: String) -> ApiResponse<()>` — Enable fog of war for a map.
- pub `disable_fog` function L90-103 — `(state: State<'_, AppState>, map_id: String) -> ApiResponse<()>` — Disable fog of war for a map.
- pub `RevealRectRequest` struct L107-113 — `{ map_id: String, x: f64, y: f64, width: f64, height: f64 }` — Request for revealing a rectangular area.
- pub `reveal_rect` function L117-144 — `( state: State<'_, AppState>, request: RevealRectRequest, ) -> ApiResponse<FogRe...` — Reveal a rectangular area on the map.
- pub `RevealCircleRequest` struct L148-153 — `{ map_id: String, center_x: f64, center_y: f64, radius: f64 }` — Request for revealing a circular area.
- pub `reveal_circle` function L157-183 — `( state: State<'_, AppState>, request: RevealCircleRequest, ) -> ApiResponse<Fog...` — Reveal a circular area on the map (stored as bounding box).
- pub `RevealAllRequest` struct L187-191 — `{ map_id: String, width: f64, height: f64 }` — Request for revealing the entire map.
- pub `reveal_all` function L195-222 — `( state: State<'_, AppState>, request: RevealAllRequest, ) -> ApiResponse<FogRev...` — Reveal the entire map.
- pub `delete_revealed_area` function L226-236 — `(state: State<'_, AppState>, id: String) -> ApiResponse<()>` — Delete a revealed area.
- pub `reset_fog` function L240-250 — `(state: State<'_, AppState>, map_id: String) -> ApiResponse<i32>` — Reset fog by clearing all revealed areas for a map.

#### crates/mimir/src/commands/map/light.rs

- pub `list_light_sources` function L23-43 — `(state: State<'_, AppState>, map_id: String) -> ApiResponse<Vec<LightSourceRespo...` — List all light sources for a map.
- pub `CreateLightSourceRequest` struct L47-57 — `{ map_id: String, name: String, light_type: String, x: f64, y: f64, bright_radiu...` — Request for creating a light source.
- pub `create_light_source` function L73-120 — `( state: State<'_, AppState>, request: CreateLightSourceRequest, ) -> ApiRespons...` — Create a new light source.
- pub `toggle_light_source` function L124-151 — `(state: State<'_, AppState>, id: String) -> ApiResponse<LightSourceResponse>` — Toggle a light source on/off.
- pub `delete_light_source` function L155-165 — `(state: State<'_, AppState>, id: String) -> ApiResponse<()>` — Delete a light source.
- pub `delete_all_light_sources` function L169-179 — `(state: State<'_, AppState>, map_id: String) -> ApiResponse<i32>` — Delete all light sources on a map.
- pub `create_torch` function L183-205 — `( state: State<'_, AppState>, map_id: String, x: i32, y: i32, ) -> ApiResponse<L...` — Create a torch light source (20ft bright, 40ft dim).
- pub `create_lantern` function L209-231 — `( state: State<'_, AppState>, map_id: String, x: i32, y: i32, ) -> ApiResponse<L...` — Create a lantern light source (30ft bright, 60ft dim).
- pub `UpdateLightSourceRequest` struct L235-241 — `{ name: Option<Option<String>>, bright_radius_ft: Option<i32>, dim_radius_ft: Op...` — Request for updating a light source.
- pub `update_light_source` function L245-286 — `( state: State<'_, AppState>, id: String, request: UpdateLightSourceRequest, ) -...` — Update a light source.
- pub `move_light_source` function L290-312 — `( state: State<'_, AppState>, id: String, x: i32, y: i32, ) -> ApiResponse<Light...` — Move a light source to a new position.
-  `get_light_response` function L60-69 — `( db: &mut diesel::SqliteConnection, app_dir: &std::path::Path, light_id: &str, ...` — Helper to get a light source and return it as a response with proper coordinates.

#### crates/mimir/src/commands/map/mod.rs

- pub `crud` module L6 — `-` — Tauri commands for map management (UVTT files).
- pub `uvtt` module L7 — `-` — Split into sub-modules by functionality.
- pub `light` module L8 — `-` — Split into sub-modules by functionality.
- pub `fog` module L9 — `-` — Split into sub-modules by functionality.
- pub `traps` module L10 — `-` — Split into sub-modules by functionality.
- pub `pois` module L11 — `-` — Split into sub-modules by functionality.
- pub `MapResponse` struct L32-55 — `{ id: String, campaign_id: String, module_id: Option<String>, name: String, desc...` — Map response with UVTT-derived fields for the frontend.
- pub `LightSourceResponse` struct L123-136 — `{ id: String, map_id: String, token_id: Option<String>, token_name: Option<Strin...` — Light source response format for frontend.
-  `enrich_map_with_uvtt` function L58-114 — `(map: &Map, service: &mut MapService, _app_dir: &Path) -> MapResponse` — Enrich a Map with UVTT data to create a MapResponse.
-  `enrich_maps_with_uvtt` function L117-119 — `(maps: Vec<Map>, service: &mut MapService, app_dir: &Path) -> Vec<MapResponse>` — Enrich multiple maps with UVTT data.
-  `transform_light_source` function L139-159 — `(ls: LightSource, grid_size_px: i32) -> LightSourceResponse` — Transform a LightSource to LightSourceResponse with pixel coordinates.
-  `get_map_grid_size_for_lights` function L162-175 — `(service: &mut MapService, map_id: &str) -> i32` — Get the grid size (pixels per grid) from a map's UVTT file.
-  `base64_encode` function L182-185 — `(data: &[u8]) -> String` — Simple base64 encoding using the standard library.
-  `base64_decode` function L188-191 — `(s: &str) -> Result<Vec<u8>, base64::DecodeError>` — Simple base64 decoding.

#### crates/mimir/src/commands/map/pois.rs

- pub `list_map_pois` function L21-31 — `(state: State<'_, AppState>, map_id: String) -> ApiResponse<Vec<MapPoi>>` — List all POIs for a map.
- pub `get_map_poi` function L35-45 — `(state: State<'_, AppState>, id: String) -> ApiResponse<MapPoi>` — Get a map POI by ID.
- pub `CreateMapPoiRequest` struct L50-59 — `{ map_id: String, name: String, grid_x: i32, grid_y: i32, description: Option<St...` — Request for creating a new map POI.
- pub `create_map_poi` function L63-96 — `( state: State<'_, AppState>, request: CreateMapPoiRequest, ) -> ApiResponse<Map...` — Create a new map POI.
- pub `UpdateMapPoiRequest` struct L101-106 — `{ name: Option<String>, description: Option<String>, icon: Option<String>, color...` — Request for updating a map POI.
- pub `update_map_poi` function L110-143 — `( state: State<'_, AppState>, id: String, request: UpdateMapPoiRequest, ) -> Api...` — Update a map POI.
- pub `move_map_poi` function L147-169 — `( state: State<'_, AppState>, id: String, grid_x: i32, grid_y: i32, ) -> ApiResp...` — Move a map POI to a new position.
- pub `toggle_map_poi_visibility` function L173-196 — `(state: State<'_, AppState>, id: String) -> ApiResponse<MapPoi>` — Toggle POI visibility for players.
- pub `delete_map_poi` function L200-210 — `(state: State<'_, AppState>, id: String) -> ApiResponse<()>` — Delete a map POI.

#### crates/mimir/src/commands/map/traps.rs

- pub `list_map_traps` function L21-31 — `(state: State<'_, AppState>, map_id: String) -> ApiResponse<Vec<MapTrap>>` — List all traps for a map.
- pub `get_map_trap` function L35-45 — `(state: State<'_, AppState>, id: String) -> ApiResponse<MapTrap>` — Get a map trap by ID.
- pub `CreateMapTrapRequest` struct L50-60 — `{ map_id: String, name: String, grid_x: i32, grid_y: i32, description: Option<St...` — Request for creating a new map trap.
- pub `create_map_trap` function L64-100 — `( state: State<'_, AppState>, request: CreateMapTrapRequest, ) -> ApiResponse<Ma...` — Create a new map trap.
- pub `UpdateMapTrapRequest` struct L105-111 — `{ name: Option<String>, description: Option<String>, trigger_description: Option...` — Request for updating a map trap.
- pub `update_map_trap` function L115-149 — `( state: State<'_, AppState>, id: String, request: UpdateMapTrapRequest, ) -> Ap...` — Update a map trap.
- pub `move_map_trap` function L153-175 — `( state: State<'_, AppState>, id: String, grid_x: i32, grid_y: i32, ) -> ApiResp...` — Move a map trap to a new position.
- pub `toggle_map_trap_visibility` function L179-202 — `(state: State<'_, AppState>, id: String) -> ApiResponse<MapTrap>` — Toggle trap visibility for players.
- pub `trigger_map_trap` function L206-223 — `(state: State<'_, AppState>, id: String) -> ApiResponse<MapTrap>` — Trigger a trap.
- pub `reset_map_trap` function L227-244 — `(state: State<'_, AppState>, id: String) -> ApiResponse<MapTrap>` — Reset (re-arm) a triggered trap.
- pub `delete_map_trap` function L248-258 — `(state: State<'_, AppState>, id: String) -> ApiResponse<()>` — Delete a map trap.

#### crates/mimir/src/commands/map/uvtt.rs

- pub `UvttPoint` struct L19-22 — `{ x: f64, y: f64 }` — A 2D point in grid units.
- pub `UvttPortal` struct L26-32 — `{ position: UvttPoint, bounds: [UvttPoint; 2], rotation: f64, closed: bool, free...` — UVTT portal (door) structure.
- pub `UvttLight` struct L36-42 — `{ position: UvttPoint, range: f64, intensity: f64, color: String, shadows: bool ...` — UVTT light source structure.
- pub `UvttEnvironment` struct L46-49 — `{ baked_lighting: bool, ambient_light: String }` — UVTT environment settings.
- pub `UvttMapSize` struct L53-56 — `{ x: f64, y: f64 }` — UVTT map size.
- pub `UvttResolution` struct L60-63 — `{ pixels_per_grid: i32, map_size: UvttMapSize }` — UVTT resolution data.
- pub `UvttData` struct L67-77 — `{ resolution: UvttResolution, line_of_sight: Option<Vec<Vec<UvttPoint>>>, portal...` — Parsed UVTT data with resolution, walls, portals, lights, and environment.
- pub `read_map_uvtt` function L85-105 — `(state: State<'_, AppState>, map_id: String) -> ApiResponse<String>` — Read the UVTT file data for a map (returned as base64).
- pub `get_uvtt_map` function L109-264 — `(state: State<'_, AppState>, id: String) -> ApiResponse<UvttData>` — Get parsed UVTT data for a map (resolution, grid size, etc).
- pub `serve_map_image` function L268-304 — `(state: State<'_, AppState>, id: String) -> ApiResponse<String>` — Serve the map image as a data URL.

### crates/mimir/src/commands/print

> *Semantic summary to be generated by AI agent.*

#### crates/mimir/src/commands/print/character.rs

- pub `export_character` function L66-640 — `( app_state: State<'_, AppState>, print_state: State<'_, PrintState>, character_...` — Export a character to PDF
- pub `generate_character_sheet` function L774-785 — `( _app_state: State<'_, AppState>, _print_state: State<'_, PrintState>, characte...` — Generate character sheet (legacy API)
-  `homebrew_item_type_to_code` function L29-62 — `(item_type: &str, data: &serde_json::Map<String, Value>) -> Option<String>` — Map human-readable homebrew item_type (from frontend UI) to 5etools type codes.
-  `tests` module L643-770 — `-` — Tauri commands for exporting character sheets to PDF.
-  `test_homebrew_weapon_melee_default` function L648-652 — `()` — Tauri commands for exporting character sheets to PDF.
-  `test_homebrew_weapon_ranged_with_range` function L655-660 — `()` — Tauri commands for exporting character sheets to PDF.
-  `test_homebrew_weapon_ranged_with_ammo` function L663-669 — `()` — Tauri commands for exporting character sheets to PDF.
-  `test_homebrew_weapon_case_insensitive` function L672-676 — `()` — Tauri commands for exporting character sheets to PDF.
-  `test_homebrew_armor` function L679-682 — `()` — Tauri commands for exporting character sheets to PDF.
-  `test_homebrew_shield` function L685-688 — `()` — Tauri commands for exporting character sheets to PDF.
-  `test_homebrew_potion` function L691-694 — `()` — Tauri commands for exporting character sheets to PDF.
-  `test_homebrew_ring` function L697-700 — `()` — Tauri commands for exporting character sheets to PDF.
-  `test_homebrew_rod` function L703-706 — `()` — Tauri commands for exporting character sheets to PDF.
-  `test_homebrew_wand` function L709-712 — `()` — Tauri commands for exporting character sheets to PDF.
-  `test_homebrew_scroll` function L715-718 — `()` — Tauri commands for exporting character sheets to PDF.
-  `test_homebrew_staff` function L721-724 — `()` — Tauri commands for exporting character sheets to PDF.
-  `test_homebrew_wondrous_item` function L727-730 — `()` — Tauri commands for exporting character sheets to PDF.
-  `test_homebrew_adventuring_gear` function L733-736 — `()` — Tauri commands for exporting character sheets to PDF.
-  `test_passthrough_raw_5etools_codes` function L739-746 — `()` — Tauri commands for exporting character sheets to PDF.
-  `test_passthrough_lowercase_5etools_codes` function L749-754 — `()` — Tauri commands for exporting character sheets to PDF.
-  `test_unknown_type_returns_none` function L757-761 — `()` — Tauri commands for exporting character sheets to PDF.
-  `test_weapon_with_empty_range_is_melee` function L764-769 — `()` — Tauri commands for exporting character sheets to PDF.

#### crates/mimir/src/commands/print/document.rs

- pub `list_print_templates` function L207-227 — `( print_state: State<'_, PrintState>, ) -> ApiResponse<Vec<PrintTemplateInfo>>` — List available print templates
- pub `export_campaign_document` function L231-293 — `( app_state: State<'_, AppState>, print_state: State<'_, PrintState>, document_i...` — Export a single campaign document to PDF
- pub `export_campaign_documents` function L297-1025 — `( app_state: State<'_, AppState>, print_state: State<'_, PrintState>, campaign_i...` — Export all campaign documents to PDF
- pub `export_module_documents` function L1029-1471 — `( app_state: State<'_, AppState>, print_state: State<'_, PrintState>, module_id:...` — Export module documents to PDF
-  `lookup_monster_data` function L24-35 — `( db: &mut diesel::SqliteConnection, monster_name: &str, monster_source: &str, c...` — Look up monster data from catalog or homebrew, returning parsed JSON.
-  `lookup_homebrew_monster_by_name` function L37-53 — `( db: &mut diesel::SqliteConnection, campaign_id: &str, monster_name: &str, ) ->...` — Tauri commands for exporting campaign and module documents to PDF.
-  `lookup_homebrew_monster_by_id` function L55-66 — `( db: &mut diesel::SqliteConnection, homebrew_monster_id: &str, ) -> Option<Valu...` — Tauri commands for exporting campaign and module documents to PDF.
-  `parse_homebrew_monster_data` function L68-82 — `(hb_monster: mimir_core::models::campaign::CampaignHomebrewMonster) -> Option<Va...` — Tauri commands for exporting campaign and module documents to PDF.
-  `lookup_catalog_monster` function L84-108 — `( db: &mut diesel::SqliteConnection, monster_name: &str, monster_source: &str, )...` — Tauri commands for exporting campaign and module documents to PDF.
-  `resolve_module_monster_data` function L111-124 — `( db: &mut diesel::SqliteConnection, mm: &mimir_core::models::campaign::ModuleMo...` — Resolve monster data for a module monster, handling both catalog and homebrew paths.
-  `resolve_monster_size` function L127-172 — `( db: &mut diesel::SqliteConnection, mm: &mimir_core::models::campaign::ModuleMo...` — Resolve monster size from catalog or homebrew.
-  `load_monster_token_image` function L175-203 — `( mm: &mimir_core::models::campaign::ModuleMonster, assets_dir: &std::path::Path...` — Load token image for a catalog monster (homebrew monsters don't have catalog images).

#### crates/mimir/src/commands/print/helpers.rs

- pub `hit_die_for_class` function L10-18 — `(class_name: &str) -> i32` — Get the hit die size for a class (e.g.
- pub `spellcasting_ability_for_class` function L21-28 — `(class_name: &str) -> Option<&'static str>` — Get the spellcasting ability abbreviation for a class, if it's a caster
- pub `caster_level_multiplier` function L31-39 — `(class_name: &str) -> f64` — Get the caster level multiplier for multiclass spell slot calculation
- pub `spell_slots_for_caster_level` function L42-67 — `(caster_level: i32) -> Vec<i32>` — Standard 5e spell slot table indexed by caster level (1-20), returning slots for levels 1-9
- pub `compute_hp_max` function L70-98 — `(classes: &[ClassInfo], con_mod: i32) -> i32` — Compute hit points max: level 1 = max die + CON mod, subsequent = avg die + CON mod
- pub `compute_hit_die_string` function L101-107 — `(classes: &[ClassInfo]) -> String` — Build hit die string like "5d10 + 3d8"
- pub `enrich_inventory_item` function L110-189 — `( db: &mut diesel::SqliteConnection, inv_item: &mimir_core::models::CharacterInv...` — Enrich an inventory item with catalog data (weapon stats, armor AC, etc.)
- pub `compute_ac` function L192-228 — `(inventory: &[InventoryItem], dex_mod: i32) -> i32` — Compute AC from equipped armor and DEX modifier
- pub `max_spell_level_for_class` function L232-295 — `(class_name: &str, class_level: i32) -> i32` — Calculate the maximum spell level a class can cast at a given class level.
-  `tests` module L298-720 — `-` — spell slots, and armor class for PDF character sheets.
-  `hit_die_barbarian_d12` function L304-306 — `()` — spell slots, and armor class for PDF character sheets.
-  `hit_die_fighter_d10` function L309-311 — `()` — spell slots, and armor class for PDF character sheets.
-  `hit_die_paladin_d10` function L314-316 — `()` — spell slots, and armor class for PDF character sheets.
-  `hit_die_ranger_d10` function L319-321 — `()` — spell slots, and armor class for PDF character sheets.
-  `hit_die_d8_classes` function L324-328 — `()` — spell slots, and armor class for PDF character sheets.
-  `hit_die_wizard_d6` function L331-333 — `()` — spell slots, and armor class for PDF character sheets.
-  `hit_die_sorcerer_d6` function L336-338 — `()` — spell slots, and armor class for PDF character sheets.
-  `hit_die_unknown_defaults_d8` function L341-343 — `()` — spell slots, and armor class for PDF character sheets.
-  `spell_ability_int_casters` function L348-351 — `()` — spell slots, and armor class for PDF character sheets.
-  `spell_ability_wis_casters` function L354-358 — `()` — spell slots, and armor class for PDF character sheets.
-  `spell_ability_cha_casters` function L361-366 — `()` — spell slots, and armor class for PDF character sheets.
-  `spell_ability_non_casters` function L369-374 — `()` — spell slots, and armor class for PDF character sheets.
-  `caster_multiplier_full_casters` function L379-383 — `()` — spell slots, and armor class for PDF character sheets.
-  `caster_multiplier_half_casters` function L386-390 — `()` — spell slots, and armor class for PDF character sheets.
-  `caster_multiplier_third_casters` function L393-397 — `()` — spell slots, and armor class for PDF character sheets.
-  `caster_multiplier_non_casters` function L400-404 — `()` — spell slots, and armor class for PDF character sheets.
-  `spell_slots_level_0` function L409-411 — `()` — spell slots, and armor class for PDF character sheets.
-  `spell_slots_level_1` function L414-416 — `()` — spell slots, and armor class for PDF character sheets.
-  `spell_slots_level_5` function L419-421 — `()` — spell slots, and armor class for PDF character sheets.
-  `spell_slots_level_9` function L424-426 — `()` — spell slots, and armor class for PDF character sheets.
-  `spell_slots_level_20` function L429-431 — `()` — spell slots, and armor class for PDF character sheets.
-  `spell_slots_above_20_caps` function L434-436 — `()` — spell slots, and armor class for PDF character sheets.
-  `spell_slots_negative_level` function L439-441 — `()` — spell slots, and armor class for PDF character sheets.
-  `hp_single_class_level_1` function L446-456 — `()` — spell slots, and armor class for PDF character sheets.
-  `hp_single_class_level_5` function L459-471 — `()` — spell slots, and armor class for PDF character sheets.
-  `hp_multiclass` function L474-495 — `()` — spell slots, and armor class for PDF character sheets.
-  `hp_wizard_low_con` function L498-508 — `()` — spell slots, and armor class for PDF character sheets.
-  `hp_empty_classes` function L511-513 — `()` — spell slots, and armor class for PDF character sheets.
-  `hp_minimum_is_1` function L516-526 — `()` — spell slots, and armor class for PDF character sheets.
-  `hit_die_string_single_class` function L531-540 — `()` — spell slots, and armor class for PDF character sheets.
-  `hit_die_string_multiclass` function L543-561 — `()` — spell slots, and armor class for PDF character sheets.
-  `hit_die_string_empty` function L564-566 — `()` — spell slots, and armor class for PDF character sheets.
-  `ac_no_armor` function L571-574 — `()` — spell slots, and armor class for PDF character sheets.
-  `ac_light_armor` function L577-588 — `()` — spell slots, and armor class for PDF character sheets.
-  `ac_medium_armor_dex_capped` function L591-602 — `()` — spell slots, and armor class for PDF character sheets.
-  `ac_medium_armor_low_dex` function L605-616 — `()` — spell slots, and armor class for PDF character sheets.
-  `ac_heavy_armor_ignores_dex` function L619-630 — `()` — spell slots, and armor class for PDF character sheets.
-  `ac_shield_adds_bonus` function L633-653 — `()` — spell slots, and armor class for PDF character sheets.
-  `ac_unequipped_armor_ignored` function L656-667 — `()` — spell slots, and armor class for PDF character sheets.
-  `max_spell_level_wizard` function L672-679 — `()` — spell slots, and armor class for PDF character sheets.
-  `max_spell_level_paladin` function L682-688 — `()` — spell slots, and armor class for PDF character sheets.
-  `max_spell_level_warlock` function L691-697 — `()` — spell slots, and armor class for PDF character sheets.
-  `max_spell_level_artificer` function L700-704 — `()` — spell slots, and armor class for PDF character sheets.
-  `max_spell_level_third_caster` function L707-712 — `()` — spell slots, and armor class for PDF character sheets.
-  `max_spell_level_non_caster` function L715-719 — `()` — spell slots, and armor class for PDF character sheets.

#### crates/mimir/src/commands/print/map.rs

- pub `print_map` function L21-210 — `( app_state: State<'_, AppState>, print_state: State<'_, PrintState>, map_id: St...` — Print a map to PDF
- pub `save_pdf` function L214-239 — `(pdf_base64: String, path: String) -> ApiResponse<()>` — Save PDF to file

#### crates/mimir/src/commands/print/mod.rs

- pub `ApiResponse` struct L28-34 — `{ success: bool, data: Option<T>, error: Option<String> }` — API response wrapper
- pub `ok` function L37-43 — `(data: T) -> Self` — Split into sub-modules by print domain.
- pub `err` function L45-51 — `(error: impl Into<String>) -> Self` — Split into sub-modules by print domain.
- pub `PrintResult` struct L56-59 — `{ pdf_base64: String, size_bytes: usize }` — Print result containing PDF data
- pub `CharacterExportOptions` struct L66-71 — `{ include_compact_sheet: Option<bool>, include_battle_card: Option<bool>, includ...` — Split into sub-modules by print domain.
- pub `ModuleExportOptions` struct L74-89 — `{ include_documents: Option<bool>, include_monsters: Option<bool>, include_traps...` — Split into sub-modules by print domain.
- pub `CampaignExportOptions` struct L92-101 — `{ include_campaign_docs: Option<bool>, include_module_content: Option<bool>, inc...` — Split into sub-modules by print domain.
- pub `MapPrintOptions` struct L104-113 — `{ include_preview: Option<bool>, preview_grid: Option<bool>, preview_los_walls: ...` — Split into sub-modules by print domain.
- pub `PrintTemplateInfo` struct L117-121 — `{ id: String, name: String, category: String }` — Template info for listing
-  `character` module L6 — `-` — Tauri commands for PDF export using mimir-print infrastructure.
-  `document` module L7 — `-` — Split into sub-modules by print domain.
-  `helpers` module L8 — `-` — Split into sub-modules by print domain.
-  `map` module L9 — `-` — Split into sub-modules by print domain.
-  `monster` module L10 — `-` — Split into sub-modules by print domain.
-  `trap` module L11 — `-` — Split into sub-modules by print domain.

#### crates/mimir/src/commands/print/monster.rs

- pub `MonsterExportOptions` struct L21-23 — `{ show_cut_lines: Option<bool> }` — Export options for monster cards
- pub `export_module_monsters` function L27-222 — `( app_state: State<'_, AppState>, print_state: State<'_, PrintState>, module_id:...` — Export module monsters as cards to PDF
- pub `export_monster_card` function L226-310 — `( app_state: State<'_, AppState>, print_state: State<'_, PrintState>, monster_na...` — Export a single monster as a card to PDF

#### crates/mimir/src/commands/print/trap.rs

- pub `TrapExportOptions` struct L20-22 — `{ show_cut_lines: Option<bool> }` — Export options for trap cards
- pub `export_trap_card` function L26-109 — `( app_state: State<'_, AppState>, print_state: State<'_, PrintState>, trap_name:...` — Export a single trap as a card to PDF
- pub `export_trap_cards` function L113-212 — `( app_state: State<'_, AppState>, print_state: State<'_, PrintState>, traps: Vec...` — Export multiple traps as cards to PDF

### crates/mimir/src

> *Semantic summary to be generated by AI agent.*

#### crates/mimir/src/error.rs

- pub `CommandError` struct L12-17 — `{ message: String, code: String }` — Error type returned from Tauri commands.
- pub `CommandResult` type L52 — `= Result<T, CommandError>` — Result type for Tauri commands.
-  `CommandError` type L19-31 — `= CommandError` — Error handling for Tauri commands, converting service errors to serializable responses.
-  `from` function L20-30 — `(err: ServiceError) -> Self` — Error handling for Tauri commands, converting service errors to serializable responses.
-  `CommandError` type L33-40 — `= CommandError` — Error handling for Tauri commands, converting service errors to serializable responses.
-  `from` function L34-39 — `(err: diesel::result::Error) -> Self` — Error handling for Tauri commands, converting service errors to serializable responses.
-  `CommandError` type L42-49 — `= CommandError` — Error handling for Tauri commands, converting service errors to serializable responses.
-  `from` function L43-48 — `(err: std::io::Error) -> Self` — Error handling for Tauri commands, converting service errors to serializable responses.

#### crates/mimir/src/lib.rs

- pub `commands` module L8 — `-` — Desktop application for managing D&D 5e campaigns, built with Tauri v2.
- pub `error` module L9 — `-` — with the mimir-core service layer.
- pub `state` module L10 — `-` — with the mimir-core service layer.

#### crates/mimir/src/main.rs

-  `main` function L18-439 — `()` — Initializes the Tauri application with database connection and runs the event loop.

#### crates/mimir/src/state.rs

- pub `is_dev_mode` function L20-22 — `() -> bool` — Check if running in development mode.
- pub `AppPaths` struct L29-44 — `{ app_dir: PathBuf, config_dir: PathBuf, data_dir: PathBuf, logs_dir: PathBuf, a...` — Application paths configuration.
- pub `from_tauri_path` function L50-89 — `(tauri_app_data_dir: PathBuf) -> std::io::Result<Self>` — Initialize application paths from Tauri's app data directory.
- pub `is_new_database` function L102-104 — `(&self) -> bool` — Check if this is a fresh database (for seeding/first-run logic).
- pub `database_url` function L107-109 — `(&self) -> String` — Get the database path as a string for Diesel.
- pub `AppState` struct L119-126 — `{ db_url: String, paths: AppPaths, active_campaign_id: Mutex<Option<String>> }` — Application state shared across all Tauri commands.
- pub `new` function L132-138 — `(paths: AppPaths) -> Self` — Create a new AppState with the given paths.
- pub `connect` function L144-146 — `(&self) -> Result<SqliteConnection, String>` — Create a new database connection.
- pub `is_dev` function L149-151 — `(&self) -> bool` — Check if running in development mode.
- pub `get_active_campaign_id` function L154-159 — `(&self) -> Option<String>` — Get the active campaign ID, if set.
- pub `set_active_campaign_id` function L162-166 — `(&self, campaign_id: Option<String>)` — Set the active campaign ID.
-  `AppPaths` type L46-110 — `= AppPaths` — This allows concurrent read operations with SQLite WAL mode.
-  `ensure_directories` function L92-99 — `(&self) -> std::io::Result<()>` — Ensure all required directories exist.
-  `AppState` type L128-167 — `= AppState` — This allows concurrent read operations with SQLite WAL mode.

### crates/mimir-core/bindings/bindings

> *Semantic summary to be generated by AI agent.*

#### crates/mimir-core/bindings/bindings/Campaign.ts

- pub `Campaign` type L6-30 — `= { /** * Unique campaign ID (UUID) */ id: string, /** * Campaign name */ name: ...`

#### crates/mimir-core/bindings/bindings/CampaignHomebrewItem.ts

- pub `CampaignHomebrewItem` type L6 — `= { id: string, campaign_id: string, name: string, item_type: string | null, rar...`

#### crates/mimir-core/bindings/bindings/CampaignHomebrewMonster.ts

- pub `CampaignHomebrewMonster` type L6 — `= { id: string, campaign_id: string, name: string, cr: string | null, creature_t...`

#### crates/mimir-core/bindings/bindings/CampaignHomebrewSpell.ts

- pub `CampaignHomebrewSpell` type L6 — `= { id: string, campaign_id: string, name: string, level: number | null, school:...`

#### crates/mimir-core/bindings/bindings/Character.ts

- pub `Character` type L7-51 — `= { /** * Unique character ID (UUID) */ id: string, /** * Campaign this characte...`

#### crates/mimir-core/bindings/bindings/CharacterClass.ts

- pub `CharacterClass` type L7-39 — `= { /** * Unique ID (UUID) */ id: string, /** * Character this class belongs to ...`

#### crates/mimir-core/bindings/bindings/CharacterInventory.ts

- pub `CharacterInventory` type L6-38 — `= { /** * Unique ID (UUID) */ id: string, /** * Character who has this item */ c...`

#### crates/mimir-core/bindings/bindings/CharacterProficiency.ts

- pub `CharacterProficiency` type L6-26 — `= { /** * Unique ID (UUID) */ id: string, /** * Character who has this proficien...`

#### crates/mimir-core/bindings/bindings/CharacterResponse.ts

- pub `CharacterResponse` type L8-16 — `= { id: string, campaign_id: string | null, name: string, is_npc: number, player...`

#### crates/mimir-core/bindings/bindings/Document.ts

- pub `Document` type L7-43 — `= { /** * Unique document ID (UUID) */ id: string, /** * Campaign this document ...`

#### crates/mimir-core/bindings/bindings/Map.ts

- pub `Map` type L6-50 — `= { /** * Unique ID (UUID) */ id: string, /** * Campaign this map belongs to */ ...`

#### crates/mimir-core/bindings/bindings/Module.ts

- pub `Module` type L6-34 — `= { /** * Unique module ID (UUID) */ id: string, /** * Campaign this module belo...`

### crates/mimir-core/schema

> *Semantic summary to be generated by AI agent.*

#### crates/mimir-core/schema/bundle-schemas.mjs

-  `bundleSchema` function L54-73 — `function bundleSchema(inputPath, outputPath)`
-  `main` function L75-107 — `function main()`

### crates/mimir-core/src/catalog

> *Semantic summary to be generated by AI agent.*

#### crates/mimir-core/src/catalog/action.rs

- pub `Action` struct L11-34 — `{ name: String, source: String, page: Option<u32>, time: Vec<ActionTime>, entrie...` — A D&D 5e action from 5etools data.
- pub `ActionTime` enum L39-49 — `Structured | Simple` — Action time specification.
- pub `ActionData` struct L53-56 — `{ action: Vec<Action> }` — Container for action data from 5etools JSON files.
-  `tests` module L59-118 — `-` — Types for deserializing 5etools action JSON data.
-  `test_action_structured_time` function L64-75 — `()` — Types for deserializing 5etools action JSON data.
-  `test_action_reaction` function L78-92 — `()` — Types for deserializing 5etools action JSON data.
-  `test_action_with_see_also` function L95-105 — `()` — Types for deserializing 5etools action JSON data.
-  `test_action_data` function L108-117 — `()` — Types for deserializing 5etools action JSON data.

#### crates/mimir-core/src/catalog/background.rs

- pub `Background` struct L11-52 — `{ name: String, source: String, page: Option<u32>, skill_proficiencies: Vec<Prof...` — A D&D 5e character background from 5etools data.
- pub `BackgroundData` struct L56-59 — `{ background: Vec<Background> }` — Container for background data from 5etools JSON files.
- pub `BackgroundFluff` struct L63-70 — `{ name: String, source: String, entries: Option<Vec<serde_json::Value>>, images:...` — Background fluff (lore/description) data.
- pub `BackgroundFluffData` struct L75-78 — `{ background_fluff: Vec<BackgroundFluff> }` — Container for background fluff data.
-  `tests` module L81-216 — `-` — Types for deserializing 5etools background JSON data.
-  `test_minimal_background` function L86-95 — `()` — Types for deserializing 5etools background JSON data.
-  `test_background_with_proficiencies` function L98-117 — `()` — Types for deserializing 5etools background JSON data.
-  `test_background_with_tool_proficiencies` function L120-136 — `()` — Types for deserializing 5etools background JSON data.
-  `test_starting_equipment_with_items` function L139-169 — `()` — Types for deserializing 5etools background JSON data.
-  `test_starting_equipment_with_choices` function L172-191 — `()` — Types for deserializing 5etools background JSON data.
-  `test_background_data_container` function L194-203 — `()` — Types for deserializing 5etools background JSON data.
-  `test_background_fluff` function L206-215 — `()` — Types for deserializing 5etools background JSON data.

#### crates/mimir-core/src/catalog/class.rs

- pub `Class` struct L12-69 — `{ name: String, source: String, page: Option<u32>, hd: Option<HitDice>, proficie...` — A D&D 5e character class from 5etools data.
- pub `HitDice` struct L73-76 — `{ number: u8, faces: u8 }` — Hit dice specification.
- pub `ClassFeatureRef` enum L81-86 — `Simple | Object` — A class feature reference - can be string or object.
- pub `ClassFeatureRefObject` struct L91-97 — `{ class_feature: String, gain_subclass_feature: Option<bool>, table_display_name...` — Class feature reference object.
- pub `StartingProficiencies` struct L102-113 — `{ armor: Option<Vec<serde_json::Value>>, weapons: Option<Vec<serde_json::Value>>...` — Starting proficiencies for a class.
- pub `StartingEquipment` struct L118-127 — `{ additional_from_background: Option<bool>, default: Option<Vec<String>>, defaul...` — Starting equipment options.
- pub `ClassTableGroup` struct L132-143 — `{ col_labels: Option<Vec<String>>, col_styles: Option<Vec<String>>, rows: Option...` — Class table group for progression tables.
- pub `Multiclassing` struct L148-153 — `{ requirements: Option<MulticlassingRequirements>, proficiencies_gained: Option<...` — Multiclassing requirements and benefits.
- pub `MulticlassingRequirements` struct L157-172 — `{ strength: Option<i32>, dexterity: Option<i32>, constitution: Option<i32>, inte...` — Multiclassing ability score requirements.
- pub `MulticlassingProficiencies` struct L177-186 — `{ armor: Option<Vec<serde_json::Value>>, weapons: Option<Vec<serde_json::Value>>...` — Proficiencies gained from multiclassing.
- pub `OptionalFeatureProgression` struct L191-198 — `{ feature_type: Option<Vec<String>>, name: Option<String>, progression: Option<O...` — Optional feature progression (invocations, metamagic, etc.).
- pub `OptionalFeatureProgressionValue` enum L203-206 — `Array | Object` — Progression value - array or level-keyed object.
- pub `Subclass` struct L215-252 — `{ name: String, short_name: Option<String>, source: String, class_name: String, ...` — A character subclass.
- pub `ClassFeature` struct L261-275 — `{ name: String, source: String, class_name: String, class_source: String, level:...` — A class feature.
- pub `SubclassFeature` struct L280-297 — `{ name: String, source: String, class_name: String, class_source: String, subcla...` — A subclass feature.
- pub `ClassData` struct L306-315 — `{ classes: Vec<Class>, subclass: Option<Vec<Subclass>>, class_features: Option<V...` — Container for class data from 5etools JSON files.
-  `tests` module L318-469 — `-` — Types for deserializing 5etools class JSON data.
-  `test_hit_dice` function L323-328 — `()` — Types for deserializing 5etools class JSON data.
-  `test_class_feature_ref_simple` function L331-335 — `()` — Types for deserializing 5etools class JSON data.
-  `test_class_feature_ref_object` function L338-350 — `()` — Types for deserializing 5etools class JSON data.
-  `test_multiclassing_requirements` function L353-359 — `()` — Types for deserializing 5etools class JSON data.
-  `test_optional_feature_progression_array` function L362-371 — `()` — Types for deserializing 5etools class JSON data.
-  `test_optional_feature_progression_object` function L374-386 — `()` — Types for deserializing 5etools class JSON data.
-  `test_minimal_class` function L389-402 — `()` — Types for deserializing 5etools class JSON data.
-  `test_class_with_features` function L405-418 — `()` — Types for deserializing 5etools class JSON data.
-  `test_subclass` function L421-436 — `()` — Types for deserializing 5etools class JSON data.
-  `test_class_feature` function L439-453 — `()` — Types for deserializing 5etools class JSON data.
-  `test_class_data_container` function L456-468 — `()` — Types for deserializing 5etools class JSON data.

#### crates/mimir-core/src/catalog/condition.rs

- pub `Condition` struct L11-34 — `{ name: String, source: String, page: Option<u32>, entries: Vec<serde_json::Valu...` — A D&D 5e condition from 5etools data.
- pub `Disease` struct L39-52 — `{ name: String, source: String, page: Option<u32>, entries: Vec<serde_json::Valu...` — A D&D 5e disease from 5etools data.
- pub `ConditionData` struct L56-59 — `{ condition: Vec<Condition> }` — Container for condition data from 5etools JSON files.
- pub `DiseaseData` struct L63-66 — `{ disease: Vec<Disease> }` — Container for disease data from 5etools JSON files.
- pub `ConditionFluff` struct L70-77 — `{ name: String, source: String, entries: Option<Vec<serde_json::Value>>, images:...` — Condition fluff data.
- pub `ConditionFluffData` struct L82-85 — `{ condition_fluff: Vec<ConditionFluff> }` — Container for condition fluff data.
-  `tests` module L88-145 — `-` — Types for deserializing 5etools condition and disease JSON data.
-  `test_condition` function L93-108 — `()` — Types for deserializing 5etools condition and disease JSON data.
-  `test_disease` function L111-120 — `()` — Types for deserializing 5etools condition and disease JSON data.
-  `test_condition_data` function L123-132 — `()` — Types for deserializing 5etools condition and disease JSON data.
-  `test_disease_data` function L135-144 — `()` — Types for deserializing 5etools condition and disease JSON data.

#### crates/mimir-core/src/catalog/cult.rs

- pub `CultistInfo` struct L9-11 — `{ entry: String }` — Cultist information entry.
- pub `GoalInfo` struct L15-17 — `{ entry: String }` — Goal information entry.
- pub `SignatureSpellInfo` struct L21-23 — `{ entry: String }` — Signature spell information entry.
- pub `AbilityInfo` struct L27-29 — `{ entry: String }` — Ability information entry.
- pub `Cult` struct L34-63 — `{ name: String, source: String, page: Option<u32>, cult_type: Option<String>, en...` — A D&D 5e cult from 5etools data.
- pub `Boon` struct L68-93 — `{ name: String, source: String, page: Option<u32>, boon_type: Option<String>, en...` — A D&D 5e demonic boon from 5etools data.
- pub `CultData` struct L97-100 — `{ cult: Vec<Cult> }` — Container for cult data from 5etools JSON files.
- pub `BoonData` struct L104-107 — `{ boon: Vec<Boon> }` — Container for boon data from 5etools JSON files.
-  `tests` module L110-181 — `-` — Types for deserializing 5etools cult and demonic boon JSON data.
-  `test_cult` function L115-129 — `()` — Types for deserializing 5etools cult and demonic boon JSON data.
-  `test_cult_with_signature_spells` function L132-142 — `()` — Types for deserializing 5etools cult and demonic boon JSON data.
-  `test_boon` function L145-156 — `()` — Types for deserializing 5etools cult and demonic boon JSON data.
-  `test_cult_data` function L159-168 — `()` — Types for deserializing 5etools cult and demonic boon JSON data.
-  `test_boon_data` function L171-180 — `()` — Types for deserializing 5etools cult and demonic boon JSON data.

#### crates/mimir-core/src/catalog/deity.rs

- pub `Deity` struct L10-53 — `{ name: String, source: String, page: Option<u32>, title: Option<String>, panthe...` — A D&D 5e deity from 5etools data.
- pub `SourceReference` struct L57-61 — `{ source: String, page: Option<u32> }` — Source reference for additional sources.
- pub `DeityData` struct L65-68 — `{ deity: Vec<Deity> }` — Container for deity data from 5etools JSON files.
- pub `DeityFluff` struct L73-80 — `{ name: String, source: String, entries: Option<Vec<serde_json::Value>>, images:...` — Deity fluff data.
- pub `DeityFluffData` struct L85-88 — `{ deity_fluff: Vec<DeityFluff> }` — Container for deity fluff data.
- pub `format_alignment` function L91-106 — `(codes: &[String]) -> String` — Get human-readable alignment from codes.
-  `tests` module L109-166 — `-` — Types for deserializing 5etools deity JSON data.
-  `test_deity` function L114-128 — `()` — Types for deserializing 5etools deity JSON data.
-  `test_deity_with_entries` function L131-141 — `()` — Types for deserializing 5etools deity JSON data.
-  `test_deity_data` function L144-153 — `()` — Types for deserializing 5etools deity JSON data.
-  `test_format_alignment` function L156-165 — `()` — Types for deserializing 5etools deity JSON data.

#### crates/mimir-core/src/catalog/feat.rs

- pub `Feat` struct L11-82 — `{ name: String, source: String, page: Option<u32>, entries: Vec<serde_json::Valu...` — A D&D 5e feat from 5etools data.
- pub `FeatData` struct L86-89 — `{ feat: Vec<Feat> }` — Container for feat data from 5etools JSON files.
-  `tests` module L92-249 — `-` — Types for deserializing 5etools feat JSON data.
-  `test_minimal_feat` function L97-106 — `()` — Types for deserializing 5etools feat JSON data.
-  `test_feat_with_ability_bonus` function L109-127 — `()` — Types for deserializing 5etools feat JSON data.
-  `test_feat_with_prerequisites` function L130-143 — `()` — Types for deserializing 5etools feat JSON data.
-  `test_feat_with_proficiencies` function L146-161 — `()` — Types for deserializing 5etools feat JSON data.
-  `test_feat_with_additional_spells` function L164-181 — `()` — Types for deserializing 5etools feat JSON data.
-  `test_feat_with_resistance` function L184-202 — `()` — Types for deserializing 5etools feat JSON data.
-  `test_feat_repeatable` function L205-220 — `()` — Types for deserializing 5etools feat JSON data.
-  `test_feat_data_container` function L223-233 — `()` — Types for deserializing 5etools feat JSON data.
-  `test_feat_with_category` function L236-248 — `()` — Types for deserializing 5etools feat JSON data.

#### crates/mimir-core/src/catalog/item.rs

- pub `Item` struct L11-98 — `{ name: String, source: String, page: Option<u32>, item_type: Option<String>, ra...` — A D&D 5e item from 5etools data.
- pub `ItemRarity` enum L103-116 — `None | Common | Uncommon | Rare | VeryRare | Legendary | Artifact | Unknown | Va...` — Item rarity - can be string or "none".
- pub `as_str` function L119-132 — `(&self) -> &str` — Types for deserializing 5etools item JSON data.
- pub `ItemData` struct L137-142 — `{ item: Vec<Item>, item_group: Option<Vec<serde_json::Value>> }` — Container for item data from 5etools JSON files.
- pub `BaseItemData` struct L146-149 — `{ base_item: Vec<Item> }` — Base item data (weapons, armor without magic properties).
- pub `get_item_type_name` function L173-218 — `(item_type: &str) -> &'static str` — Get human-readable type name from 5etools type code.
-  `ItemRarity` type L118-133 — `= ItemRarity` — Types for deserializing 5etools item JSON data.
-  `deserialize_attunement` function L152-170 — `(deserializer: D) -> Result<Option<String>, D::Error>` — Custom deserializer for reqAttune field that can be boolean or string.
-  `tests` module L221-338 — `-` — Types for deserializing 5etools item JSON data.
-  `test_simple_weapon` function L226-246 — `()` — Types for deserializing 5etools item JSON data.
-  `test_magic_item_with_attunement` function L249-268 — `()` — Types for deserializing 5etools item JSON data.
-  `test_attunement_boolean` function L271-282 — `()` — Types for deserializing 5etools item JSON data.
-  `test_armor` function L285-302 — `()` — Types for deserializing 5etools item JSON data.
-  `test_item_type_name` function L305-311 — `()` — Types for deserializing 5etools item JSON data.
-  `test_srd_values` function L314-324 — `()` — Types for deserializing 5etools item JSON data.
-  `test_item_data_container` function L327-337 — `()` — Types for deserializing 5etools item JSON data.

#### crates/mimir-core/src/catalog/language.rs

- pub `Language` struct L11-52 — `{ name: String, source: String, page: Option<u32>, language_type: Option<String>...` — A D&D 5e language from 5etools data.
- pub `LanguageData` struct L56-59 — `{ language: Vec<Language> }` — Container for language data from 5etools JSON files.
- pub `LanguageFluff` struct L64-71 — `{ name: String, source: String, entries: Option<Vec<serde_json::Value>>, images:...` — Language fluff data.
- pub `LanguageFluffData` struct L76-79 — `{ language_fluff: Vec<LanguageFluff> }` — Container for language fluff data.
-  `tests` module L82-139 — `-` — Types for deserializing 5etools language JSON data.
-  `test_standard_language` function L87-100 — `()` — Types for deserializing 5etools language JSON data.
-  `test_exotic_language` function L103-114 — `()` — Types for deserializing 5etools language JSON data.
-  `test_secret_language` function L117-126 — `()` — Types for deserializing 5etools language JSON data.
-  `test_language_data` function L129-138 — `()` — Types for deserializing 5etools language JSON data.

#### crates/mimir-core/src/catalog/mod.rs

-  `action` module L18 — `-` — Hand-written extraction types for deserializing 5etools JSON data.
-  `background` module L19 — `-` — See <https://github.com/TheGiddyLimit/5etools-utils/tree/master/schema/brew-fast>
-  `class` module L20 — `-` — See <https://github.com/TheGiddyLimit/5etools-utils/tree/master/schema/brew-fast>
-  `condition` module L21 — `-` — See <https://github.com/TheGiddyLimit/5etools-utils/tree/master/schema/brew-fast>
-  `cult` module L22 — `-` — See <https://github.com/TheGiddyLimit/5etools-utils/tree/master/schema/brew-fast>
-  `deity` module L23 — `-` — See <https://github.com/TheGiddyLimit/5etools-utils/tree/master/schema/brew-fast>
-  `feat` module L24 — `-` — See <https://github.com/TheGiddyLimit/5etools-utils/tree/master/schema/brew-fast>
-  `item` module L25 — `-` — See <https://github.com/TheGiddyLimit/5etools-utils/tree/master/schema/brew-fast>
-  `language` module L26 — `-` — See <https://github.com/TheGiddyLimit/5etools-utils/tree/master/schema/brew-fast>
-  `monster` module L27 — `-` — See <https://github.com/TheGiddyLimit/5etools-utils/tree/master/schema/brew-fast>
-  `object` module L28 — `-` — See <https://github.com/TheGiddyLimit/5etools-utils/tree/master/schema/brew-fast>
-  `optionalfeature` module L29 — `-` — See <https://github.com/TheGiddyLimit/5etools-utils/tree/master/schema/brew-fast>
-  `psionic` module L30 — `-` — See <https://github.com/TheGiddyLimit/5etools-utils/tree/master/schema/brew-fast>
-  `race` module L31 — `-` — See <https://github.com/TheGiddyLimit/5etools-utils/tree/master/schema/brew-fast>
-  `reward` module L32 — `-` — See <https://github.com/TheGiddyLimit/5etools-utils/tree/master/schema/brew-fast>
-  `spell` module L33 — `-` — See <https://github.com/TheGiddyLimit/5etools-utils/tree/master/schema/brew-fast>
-  `table` module L34 — `-` — See <https://github.com/TheGiddyLimit/5etools-utils/tree/master/schema/brew-fast>
-  `trap` module L35 — `-` — See <https://github.com/TheGiddyLimit/5etools-utils/tree/master/schema/brew-fast>
-  `types` module L36 — `-` — See <https://github.com/TheGiddyLimit/5etools-utils/tree/master/schema/brew-fast>
-  `variantrule` module L37 — `-` — See <https://github.com/TheGiddyLimit/5etools-utils/tree/master/schema/brew-fast>
-  `vehicle` module L38 — `-` — See <https://github.com/TheGiddyLimit/5etools-utils/tree/master/schema/brew-fast>

#### crates/mimir-core/src/catalog/monster.rs

- pub `Monster` struct L14-117 — `{ name: String, source: String, page: Option<u32>, size: Option<Vec<String>>, cr...` — A D&D 5e monster/creature from 5etools bestiary data.
- pub `MonsterSpeed` struct L122-137 — `{ walk: Option<SpeedValue>, burrow: Option<SpeedValue>, climb: Option<SpeedValue...` — Monster speed object with multiple movement types.
- pub `MonsterSaves` struct L141-154 — `{ strength: Option<String>, dexterity: Option<String>, constitution: Option<Stri...` — Monster saving throw bonuses.
- pub `MonsterSkills` struct L158-195 — `{ acrobatics: Option<String>, animal_handling: Option<String>, arcana: Option<St...` — Monster skill bonuses.
- pub `MonsterAction` struct L203-208 — `{ name: Option<String>, entries: Option<Vec<serde_json::Value>> }` — A monster action, trait, reaction, or legendary action.
- pub `MonsterData` struct L212-215 — `{ monster: Vec<Monster> }` — Container for monster data from 5etools bestiary JSON files.
- pub `MonsterFluff` struct L219-226 — `{ name: String, source: String, entries: Option<Vec<serde_json::Value>>, images:...` — Monster fluff (lore/description) data.
- pub `MonsterFluffData` struct L230-233 — `{ monster_fluff: Vec<MonsterFluff> }` — Container for monster fluff data.
-  `tests` module L236-350 — `-` — Types for deserializing 5etools bestiary JSON data.
-  `test_simple_monster` function L241-272 — `()` — Types for deserializing 5etools bestiary JSON data.
-  `test_monster_with_legendary_group` function L275-289 — `()` — Types for deserializing 5etools bestiary JSON data.
-  `test_full_monster` function L292-334 — `()` — Types for deserializing 5etools bestiary JSON data.
-  `test_monster_data_container` function L337-349 — `()` — Types for deserializing 5etools bestiary JSON data.

#### crates/mimir-core/src/catalog/object.rs

- pub `ArmorClass` enum L10-20 — `Number | Object` — Armor class can be a simple number or a complex object.
- pub `HitPoints` enum L25-33 — `Number | Object` — Hit points can be a simple number or a complex object.
- pub `DamageType` enum L38-41 — `Simple | Special` — Damage type can be simple string or special object.
- pub `DndObject` struct L46-97 — `{ name: String, source: String, page: Option<u32>, object_type: Option<String>, ...` — A D&D 5e object from 5etools data.
- pub `ObjectData` struct L101-104 — `{ object: Vec<DndObject> }` — Container for object data from 5etools JSON files.
- pub `get_object_type_name` function L107-114 — `(code: &str) -> &'static str` — Get human-readable name for object type code.
- pub `get_size_name` function L117-127 — `(code: &str) -> &'static str` — Get human-readable name for size code.
-  `tests` module L130-201 — `-` — Types for deserializing 5etools object JSON data (siege weapons, etc.).
-  `test_siege_weapon` function L135-149 — `()` — Types for deserializing 5etools object JSON data (siege weapons, etc.).
-  `test_complex_ac` function L152-165 — `()` — Types for deserializing 5etools object JSON data (siege weapons, etc.).
-  `test_immunities` function L168-176 — `()` — Types for deserializing 5etools object JSON data (siege weapons, etc.).
-  `test_object_data` function L179-188 — `()` — Types for deserializing 5etools object JSON data (siege weapons, etc.).
-  `test_object_type_name` function L191-194 — `()` — Types for deserializing 5etools object JSON data (siege weapons, etc.).
-  `test_size_name` function L197-200 — `()` — Types for deserializing 5etools object JSON data (siege weapons, etc.).

#### crates/mimir-core/src/catalog/optionalfeature.rs

- pub `OptionalFeature` struct L12-49 — `{ name: String, source: String, page: Option<u32>, feature_type: Vec<String>, pr...` — A D&D 5e optional feature from 5etools data.
- pub `Consumes` struct L53-57 — `{ name: String, amount: Option<i32> }` — Resource consumption for optional features.
- pub `OptionalFeatureData` struct L61-64 — `{ optional_features: Vec<OptionalFeature> }` — Container for optional feature data from 5etools JSON files.
- pub `get_feature_type_name` function L67-91 — `(code: &str) -> &'static str` — Get human-readable name for feature type code.
-  `tests` module L94-171 — `-` — Includes Eldritch Invocations, Metamagic, Fighting Styles, etc.
-  `test_eldritch_invocation` function L99-111 — `()` — Includes Eldritch Invocations, Metamagic, Fighting Styles, etc.
-  `test_metamagic` function L114-126 — `()` — Includes Eldritch Invocations, Metamagic, Fighting Styles, etc.
-  `test_fighting_style` function L129-138 — `()` — Includes Eldritch Invocations, Metamagic, Fighting Styles, etc.
-  `test_with_additional_spells` function L141-151 — `()` — Includes Eldritch Invocations, Metamagic, Fighting Styles, etc.
-  `test_feature_type_name` function L154-158 — `()` — Includes Eldritch Invocations, Metamagic, Fighting Styles, etc.
-  `test_data_container` function L161-170 — `()` — Includes Eldritch Invocations, Metamagic, Fighting Styles, etc.

#### crates/mimir-core/src/catalog/psionic.rs

- pub `PsionicCost` struct L9-13 — `{ min: i32, max: Option<i32> }` — Psionic power cost range.
- pub `ConcentrationDuration` struct L17-20 — `{ duration: i32, unit: String }` — Concentration duration for psionic modes.
- pub `PsionicMode` struct L24-31 — `{ name: String, cost: PsionicCost, entries: Vec<serde_json::Value>, concentratio...` — A psionic mode (for disciplines).
- pub `Psionic` struct L36-61 — `{ name: String, source: String, page: Option<u32>, psionic_type: String, order: ...` — A D&D 5e psionic power from 5etools data.
- pub `PsionicData` struct L65-68 — `{ psionic: Vec<Psionic> }` — Container for psionic data from 5etools JSON files.
- pub `get_psionic_type_name` function L71-77 — `(code: &str) -> &'static str` — Get human-readable name for psionic type code.
- pub `get_order_name` function L80-89 — `(order: &str) -> &'static str` — Get human-readable name for psionic order.
-  `tests` module L92-181 — `-` — Types for deserializing 5etools psionic JSON data (UA psionics).
-  `test_talent` function L97-107 — `()` — Types for deserializing 5etools psionic JSON data (UA psionics).
-  `test_discipline` function L110-133 — `()` — Types for deserializing 5etools psionic JSON data (UA psionics).
-  `test_discipline_with_concentration` function L136-156 — `()` — Types for deserializing 5etools psionic JSON data (UA psionics).
-  `test_psionic_data` function L159-168 — `()` — Types for deserializing 5etools psionic JSON data (UA psionics).
-  `test_psionic_type_name` function L171-174 — `()` — Types for deserializing 5etools psionic JSON data (UA psionics).
-  `test_order_name` function L177-180 — `()` — Types for deserializing 5etools psionic JSON data (UA psionics).

#### crates/mimir-core/src/catalog/race.rs

- pub `Race` struct L13-104 — `{ name: String, source: String, page: Option<u32>, size: Option<Vec<String>>, sp...` — A D&D 5e player race from 5etools data.
- pub `Subrace` struct L109-165 — `{ name: Option<String>, source: String, page: Option<u32>, race_name: String, ra...` — A subrace variant.
- pub `Age` struct L169-176 — `{ mature: Option<i32>, max: Option<i32> }` — Age information for a race.
- pub `SoundClip` struct L180-184 — `{ clip_type: String, path: String }` — Sound clip reference.
- pub `Overwrite` struct L192-199 — `{ ability: Option<bool>, language_proficiencies: Option<bool>, skill_proficienci...` — Overwrite settings for subraces.
- pub `RaceData` struct L203-208 — `{ race: Vec<Race>, subrace: Option<Vec<Subrace>> }` — Container for race data from 5etools JSON files.
- pub `RaceFluff` struct L212-219 — `{ name: String, source: String, entries: Option<Vec<serde_json::Value>>, images:...` — Race fluff (lore/description) data.
- pub `RaceFluffData` struct L224-227 — `{ race_fluff: Vec<RaceFluff> }` — Container for race fluff data.
-  `tests` module L230-423 — `-` — Types for deserializing 5etools race JSON data.
-  `test_minimal_race` function L235-243 — `()` — Types for deserializing 5etools race JSON data.
-  `test_race_speed_number` function L246-252 — `()` — Types for deserializing 5etools race JSON data.
-  `test_race_speed_object` function L255-266 — `()` — Types for deserializing 5etools race JSON data.
-  `test_race_speed_with_swim_true` function L269-280 — `()` — Types for deserializing 5etools race JSON data.
-  `test_lineage_string` function L283-290 — `()` — Types for deserializing 5etools race JSON data.
-  `test_lineage_bool` function L293-300 — `()` — Types for deserializing 5etools race JSON data.
-  `test_height_and_weight` function L303-320 — `()` — Types for deserializing 5etools race JSON data.
-  `test_full_race` function L323-344 — `()` — Types for deserializing 5etools race JSON data.
-  `test_subrace` function L347-362 — `()` — Types for deserializing 5etools race JSON data.
-  `test_subrace_overwrite` function L365-380 — `()` — Types for deserializing 5etools race JSON data.
-  `test_race_data_container` function L383-396 — `()` — Types for deserializing 5etools race JSON data.
-  `test_race_with_srd_alternate_name` function L399-409 — `()` — Types for deserializing 5etools race JSON data.
-  `test_age` function L412-422 — `()` — Types for deserializing 5etools race JSON data.

#### crates/mimir-core/src/catalog/reward.rs

- pub `Reward` struct L11-48 — `{ name: String, source: String, page: Option<u32>, reward_type: Option<String>, ...` — A D&D 5e reward from 5etools data.
- pub `RewardData` struct L52-55 — `{ reward: Vec<Reward> }` — Container for reward data from 5etools JSON files.
- pub `RewardFluff` struct L60-67 — `{ name: String, source: String, entries: Option<Vec<serde_json::Value>>, images:...` — Reward fluff data.
- pub `RewardFluffData` struct L72-75 — `{ reward_fluff: Vec<RewardFluff> }` — Container for reward fluff data.
- pub `get_reward_type_name` function L78-86 — `(reward_type: Option<&str>) -> &'static str` — Get human-readable name for reward type.
-  `tests` module L89-163 — `-` — Types for deserializing 5etools reward JSON data (blessings, boons, charms).
-  `test_blessing` function L94-104 — `()` — Types for deserializing 5etools reward JSON data (blessings, boons, charms).
-  `test_epic_boon` function L107-116 — `()` — Types for deserializing 5etools reward JSON data (blessings, boons, charms).
-  `test_charm` function L119-130 — `()` — Types for deserializing 5etools reward JSON data (blessings, boons, charms).
-  `test_reward_with_prerequisites` function L133-143 — `()` — Types for deserializing 5etools reward JSON data (blessings, boons, charms).
-  `test_reward_data` function L146-155 — `()` — Types for deserializing 5etools reward JSON data (blessings, boons, charms).
-  `test_reward_type_name` function L158-162 — `()` — Types for deserializing 5etools reward JSON data (blessings, boons, charms).

#### crates/mimir-core/src/catalog/spell.rs

- pub `Spell` struct L10-53 — `{ name: String, source: String, page: Option<u32>, level: u8, school: SpellSchoo...` — A D&D 5e spell from 5etools data.
- pub `SpellSchool` enum L57-76 — `Abjuration | Conjuration | Divination | Enchantment | Evocation | Illusion | Nec...` — Spell school codes (single letter in 5etools).
- pub `as_str` function L79-91 — `(&self) -> &str` — Types for deserializing 5etools spell JSON data.
- pub `CastingTime` struct L96-101 — `{ number: u32, unit: String, condition: Option<String> }` — Casting time specification.
- pub `SpellRange` enum L106-116 — `Point | Special` — Spell range - point-based or special.
- pub `SpellDistance` struct L120-125 — `{ distance_type: String, amount: Option<u32> }` — Distance specification.
- pub `SpellComponents` struct L129-138 — `{ v: Option<bool>, s: Option<bool>, m: Option<MaterialComponent>, r: Option<bool...` — Spell components (V, S, M, R).
- pub `MaterialComponent` enum L143-153 — `Text | Object | Bool` — Material component - can be text, object with cost, or bool.
- pub `ConsumeValue` enum L158-161 — `Flag | Text` — Material component consumption indicator.
- pub `is_consumed` function L164-166 — `(&self) -> bool` — Types for deserializing 5etools spell JSON data.
- pub `is_optional` function L168-170 — `(&self) -> bool` — Types for deserializing 5etools spell JSON data.
- pub `SpellDuration` struct L175-184 — `{ duration_type: String, duration: Option<DurationValue>, concentration: Option<...` — Spell duration specification.
- pub `DurationValue` struct L188-195 — `{ value_type: String, amount: Option<u32>, up_to: Option<bool> }` — Duration value (amount and type).
- pub `SpellClasses` struct L199-204 — `{ from_class_list: Option<Vec<ClassReference>>, from_subclass: Option<Vec<Subcla...` — Classes that can cast the spell.
- pub `ClassReference` struct L208-211 — `{ name: String, source: String }` — Class reference for spell lists.
- pub `SubclassSpellRef` struct L215-218 — `{ class: ClassReference, subclass: SubclassReference }` — Subclass reference for spell lists.
- pub `SubclassReference` struct L222-227 — `{ name: String, source: String, sub_subclass: Option<String> }` — Subclass name and source.
- pub `SpellMeta` struct L231-234 — `{ ritual: bool }` — Spell metadata (ritual, etc.).
- pub `SpellData` struct L238-241 — `{ spell: Vec<Spell> }` — Container for spell data from 5etools JSON files.
-  `SpellSchool` type L78-92 — `= SpellSchool` — Types for deserializing 5etools spell JSON data.
-  `ConsumeValue` type L163-171 — `= ConsumeValue` — Types for deserializing 5etools spell JSON data.
-  `tests` module L244-389 — `-` — Types for deserializing 5etools spell JSON data.
-  `test_simple_spell` function L249-269 — `()` — Types for deserializing 5etools spell JSON data.
-  `test_concentration_spell` function L272-288 — `()` — Types for deserializing 5etools spell JSON data.
-  `test_ritual_spell` function L291-307 — `()` — Types for deserializing 5etools spell JSON data.
-  `test_material_component_with_cost` function L310-338 — `()` — Types for deserializing 5etools spell JSON data.
-  `test_spell_with_classes` function L341-365 — `()` — Types for deserializing 5etools spell JSON data.
-  `test_spell_data_container` function L368-388 — `()` — Types for deserializing 5etools spell JSON data.

#### crates/mimir-core/src/catalog/table.rs

- pub `TableCell` enum L11-15 — `Text | Number | Object` — Table cell content - can be a simple string or complex object.
- pub `Table` struct L20-69 — `{ name: String, source: String, page: Option<u32>, caption: Option<String>, col_...` — A D&D 5e table from 5etools data.
- pub `TableData` struct L73-76 — `{ table: Vec<Table> }` — Container for table data from 5etools JSON files.
- pub `TableFluff` struct L81-88 — `{ name: String, source: String, entries: Option<Vec<serde_json::Value>>, images:...` — Table fluff data.
- pub `TableFluffData` struct L93-96 — `{ table_fluff: Vec<TableFluff> }` — Container for table fluff data.
- pub `categorize_table` function L99-132 — `(name: &str) -> &'static str` — Categorize a table by its name.
-  `tests` module L135-204 — `-` — Types for deserializing 5etools table JSON data (random tables, etc.).
-  `test_table` function L140-155 — `()` — Types for deserializing 5etools table JSON data (random tables, etc.).
-  `test_table_with_intro` function L158-168 — `()` — Types for deserializing 5etools table JSON data (random tables, etc.).
-  `test_table_data` function L171-180 — `()` — Types for deserializing 5etools table JSON data (random tables, etc.).
-  `test_categorize_table` function L183-189 — `()` — Types for deserializing 5etools table JSON data (random tables, etc.).
-  `test_table_cell_types` function L192-203 — `()` — Types for deserializing 5etools table JSON data (random tables, etc.).

#### crates/mimir-core/src/catalog/trap.rs

- pub `Trap` struct L11-34 — `{ name: String, source: String, page: Option<u32>, trap_haz_type: Option<String>...` — A D&D 5e trap from 5etools data.
- pub `Hazard` struct L39-62 — `{ name: String, source: String, page: Option<u32>, trap_haz_type: Option<String>...` — A D&D 5e hazard from 5etools data.
- pub `TrapData` struct L66-69 — `{ trap: Vec<Trap> }` — Container for trap data from 5etools JSON files.
- pub `HazardData` struct L73-76 — `{ hazard: Vec<Hazard> }` — Container for hazard data from 5etools JSON files.
- pub `get_trap_type_name` function L79-88 — `(code: &str) -> &'static str` — Get human-readable name for trap/hazard type code.
-  `tests` module L91-163 — `-` — Types for deserializing 5etools trap and hazard JSON data.
-  `test_mechanical_trap` function L96-106 — `()` — Types for deserializing 5etools trap and hazard JSON data.
-  `test_magical_trap` function L109-118 — `()` — Types for deserializing 5etools trap and hazard JSON data.
-  `test_hazard` function L121-131 — `()` — Types for deserializing 5etools trap and hazard JSON data.
-  `test_trap_data` function L134-143 — `()` — Types for deserializing 5etools trap and hazard JSON data.
-  `test_hazard_data` function L146-155 — `()` — Types for deserializing 5etools trap and hazard JSON data.
-  `test_trap_type_name` function L158-162 — `()` — Types for deserializing 5etools trap and hazard JSON data.

#### crates/mimir-core/src/catalog/types.rs

- pub `ArmorClassValue` enum L16-21 — `Number | Array` — Armor Class value - can be a number or array of AC entries.
- pub `ArmorClassEntry` enum L26-31 — `Number | Object` — An AC entry - can be a plain number or an object with details.
- pub `ac` function L35-40 — `(&self) -> Option<i32>` — Get the AC value from this entry
- pub `ArmorClassEntryObject` struct L45-54 — `{ ac: Option<i32>, from: Option<Vec<String>>, condition: Option<String>, braces:...` — An AC entry object with optional source and condition.
- pub `HitPointsValue` enum L63-74 — `Standard | Special | Number` — Hit points - standard formula or special text.
- pub `average` function L78-84 — `(&self) -> Option<i32>` — Get the average HP value
- pub `ChallengeRatingValue` enum L94-105 — `Simple | Complex` — Challenge Rating - simple string or complex object.
- pub `cr` function L109-114 — `(&self) -> &str` — Get the base CR string
- pub `SpeedValue` enum L124-131 — `Number | WithCondition | Flag` — Speed value - number or object with condition.
- pub `as_number` function L135-141 — `(&self) -> i32` — Get the numeric speed value
- pub `SpeedObject` struct L147-160 — `{ walk: Option<SpeedValue>, fly: Option<SpeedValue>, swim: Option<SpeedValue>, c...` — Creature speeds object with multiple movement types.
- pub `CreatureTypeValue` enum L169-181 — `Simple | Complex` — Creature type - simple string or object with tags.
- pub `base_type` function L185-190 — `(&self) -> &str` — Get the base creature type
- pub `CreatureTag` enum L196-201 — `Simple | WithPrefix` — Creature type tag - string or object.
- pub `AlignmentValue` enum L210-215 — `Array | Single` — Alignment value - array of strings or objects.
- pub `AlignmentComponent` enum L220-227 — `Abbr | Special | Choice` — Alignment component - abbreviation or special.
- pub `DamageModifier` enum L236-254 — `Simple | Conditional` — Damage resistance/immunity/vulnerability - string or complex.
- pub `OtherSource` struct L262-266 — `{ source: String, page: Option<i32> }` — Other source reference.
- pub `SrdValue` enum L271-276 — `Flag | Name` — SRD status indicator.
- pub `LegendaryGroup` struct L284-287 — `{ name: String, source: String }` — Reference to a legendary group definition.
- pub `ChooseSpec` struct L295-304 — `{ from: Option<Vec<String>>, from_filter: Option<String>, count: Option<i32>, am...` — Specification for a choice selection.
- pub `ProficiencyItem` enum L309-318 — `Simple | Flag | Choice | Keyed` — A proficiency item that can be a simple name or a choice.
- pub `ProficiencyChoice` struct L322-331 — `{ choose: Option<ChooseSpec>, any: Option<i32>, from: Option<Vec<String>>, count...` — A choice of proficiencies from a list.
- pub `AbilityBonus` enum L340-345 — `Fixed | Choice` — Ability score bonus - can be object with scores or choice.
- pub `AbilityChoice` struct L349-358 — `{ choose: Option<ChooseSpec>, from: Option<Vec<String>>, count: Option<i32>, amo...` — Choice of ability score improvements.
- pub `CopyDirective` struct L366-375 — `{ name: String, source: String, modifications: Option<serde_json::Value>, traits...` — Copy directive for inheriting from another entity.
- pub `RaceSpeed` enum L388-393 — `Number | Object` — Race speed - can be a simple number or a structured object with multiple movement types.
- pub `walk_speed` function L397-402 — `(&self) -> i32` — Get the walking speed (or base speed if only a number)
- pub `RaceSpeedObject` struct L408-422 — `{ walk: Option<SpeedValue>, fly: Option<SpeedValue>, swim: Option<SpeedValue>, c...` — Structured speed object with optional movement types.
- pub `is_equal_to_walk` function L426-428 — `(&self) -> bool` — Check if this is a "true" flag meaning "equal to walk"
- pub `Lineage` enum L438-443 — `Flag | Source` — Lineage indicator - boolean or source string for legacy races.
- pub `HeightAndWeight` struct L448-461 — `{ base_height: Option<i32>, height_mod: Option<String>, base_weight: Option<i32>...` — Height and weight generation parameters for a race.
- pub `StartingEquipmentEntry` struct L471-484 — `{ default_items: Vec<StartingEquipmentItem>, a: Option<Vec<StartingEquipmentItem...` — A choice group for starting equipment.
- pub `StartingEquipmentItem` enum L491-496 — `ItemRef | Object` — A single item in starting equipment.
- pub `StartingEquipmentItemObject` struct L501-517 — `{ item: Option<String>, special: Option<String>, display_name: Option<String>, q...` — A structured starting equipment item with optional metadata.
-  `ArmorClassEntry` type L33-41 — `= ArmorClassEntry` — multiple entity types (monsters, spells, items, etc.).
-  `HitPointsValue` type L76-85 — `= HitPointsValue` — multiple entity types (monsters, spells, items, etc.).
-  `ChallengeRatingValue` type L107-115 — `= ChallengeRatingValue` — multiple entity types (monsters, spells, items, etc.).
-  `SpeedValue` type L133-142 — `= SpeedValue` — multiple entity types (monsters, spells, items, etc.).
-  `CreatureTypeValue` type L183-191 — `= CreatureTypeValue` — multiple entity types (monsters, spells, items, etc.).
-  `RaceSpeed` type L395-403 — `= RaceSpeed` — multiple entity types (monsters, spells, items, etc.).
-  `SpeedValue` type L424-429 — `= SpeedValue` — multiple entity types (monsters, spells, items, etc.).

#### crates/mimir-core/src/catalog/variantrule.rs

- pub `VariantRule` struct L11-36 — `{ name: String, source: String, page: Option<u32>, rule_type: Option<String>, en...` — A D&D 5e variant rule from 5etools data.
- pub `VariantRuleData` struct L40-43 — `{ variantrule: Vec<VariantRule> }` — Container for variant rule data from 5etools JSON files.
-  `tests` module L46-100 — `-` — Types for deserializing 5etools variant rule JSON data.
-  `test_variant_rule` function L51-63 — `()` — Types for deserializing 5etools variant rule JSON data.
-  `test_variant_rule_with_srd` function L66-75 — `()` — Types for deserializing 5etools variant rule JSON data.
-  `test_variant_rule_data` function L78-87 — `()` — Types for deserializing 5etools variant rule JSON data.
-  `test_variant_rule_minimal` function L90-99 — `()` — Types for deserializing 5etools variant rule JSON data.

#### crates/mimir-core/src/catalog/vehicle.rs

- pub `VehicleSpeed` struct L9-22 — `{ walk: Option<i32>, swim: Option<i32>, fly: Option<i32>, burrow: Option<i32>, c...` — Vehicle speed information.
- pub `VehicleWeapon` struct L26-32 — `{ name: String, count: Option<i32>, entries: Vec<serde_json::Value> }` — Vehicle weapon entry.
- pub `Vehicle` struct L37-104 — `{ name: String, source: String, page: Option<u32>, vehicle_type: Option<String>,...` — A D&D 5e vehicle from 5etools data.
- pub `VehicleData` struct L108-111 — `{ vehicle: Vec<Vehicle> }` — Container for vehicle data from 5etools JSON files.
- pub `VehicleFluff` struct L116-123 — `{ name: String, source: String, entries: Option<Vec<serde_json::Value>>, images:...` — Vehicle fluff data.
- pub `VehicleFluffData` struct L128-131 — `{ vehicle_fluff: Vec<VehicleFluff> }` — Container for vehicle fluff data.
- pub `get_vehicle_type_name` function L134-145 — `(code: &str) -> &'static str` — Get human-readable name for vehicle type code.
-  `tests` module L148-221 — `-` — Types for deserializing 5etools vehicle JSON data.
-  `test_ship` function L153-168 — `()` — Types for deserializing 5etools vehicle JSON data.
-  `test_infernal_war_machine` function L171-185 — `()` — Types for deserializing 5etools vehicle JSON data.
-  `test_vehicle_with_weapons` function L188-201 — `()` — Types for deserializing 5etools vehicle JSON data.
-  `test_vehicle_data` function L204-213 — `()` — Types for deserializing 5etools vehicle JSON data.
-  `test_vehicle_type_name` function L216-220 — `()` — Types for deserializing 5etools vehicle JSON data.

### crates/mimir-core/src/dal/campaign

> *Semantic summary to be generated by AI agent.*

#### crates/mimir-core/src/dal/campaign/campaign.rs

- pub `insert_campaign` function L11-17 — `(conn: &mut SqliteConnection, campaign: &NewCampaign) -> QueryResult<String>` — Insert a new campaign.
- pub `get_campaign` function L20-22 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<Campaign>` — Get a campaign by ID.
- pub `get_campaign_optional` function L25-30 — `( conn: &mut SqliteConnection, id: &str, ) -> QueryResult<Option<Campaign>>` — Get a campaign by ID, returning None if not found.
- pub `list_campaigns` function L33-44 — `( conn: &mut SqliteConnection, include_archived: bool, ) -> QueryResult<Vec<Camp...` — List all campaigns, optionally including archived.
- pub `list_archived_campaigns` function L47-52 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<Campaign>>` — List only archived campaigns.
- pub `update_campaign` function L55-63 — `( conn: &mut SqliteConnection, id: &str, update: &UpdateCampaign, ) -> QueryResu...` — Update a campaign.
- pub `delete_campaign` function L68-70 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<usize>` — Delete a campaign by ID.
- pub `campaign_exists` function L73-78 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<bool>` — Check if a campaign exists.
- pub `count_campaigns` function L81-86 — `(conn: &mut SqliteConnection) -> QueryResult<i64>` — Count all campaigns (excluding archived).
- pub `count_all_campaigns` function L89-91 — `(conn: &mut SqliteConnection) -> QueryResult<i64>` — Count all campaigns including archived.
-  `tests` module L94-250 — `-` — Database operations for campaigns.
-  `test_insert_and_get_campaign` function L99-111 — `()` — Database operations for campaigns.
-  `test_insert_campaign_with_description` function L114-123 — `()` — Database operations for campaigns.
-  `test_list_campaigns_excludes_archived` function L126-146 — `()` — Database operations for campaigns.
-  `test_list_archived_campaigns` function L149-164 — `()` — Database operations for campaigns.
-  `test_update_campaign_name` function L167-178 — `()` — Database operations for campaigns.
-  `test_archive_and_unarchive_campaign` function L181-200 — `()` — Database operations for campaigns.
-  `test_delete_campaign` function L203-214 — `()` — Database operations for campaigns.
-  `test_get_campaign_optional` function L217-228 — `()` — Database operations for campaigns.
-  `test_count_campaigns` function L231-249 — `()` — Database operations for campaigns.

#### crates/mimir-core/src/dal/campaign/campaign_asset.rs

- pub `insert_campaign_asset` function L11-20 — `( conn: &mut SqliteConnection, asset: &NewCampaignAsset, ) -> QueryResult<String...` — Insert a new campaign asset.
- pub `get_campaign_asset` function L23-25 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<CampaignAsset>` — Get a campaign asset by ID.
- pub `get_campaign_asset_optional` function L28-33 — `( conn: &mut SqliteConnection, id: &str, ) -> QueryResult<Option<CampaignAsset>>` — Get a campaign asset by ID, returning None if not found.
- pub `list_campaign_assets` function L36-44 — `( conn: &mut SqliteConnection, campaign_id: &str, ) -> QueryResult<Vec<CampaignA...` — List all assets for a campaign.
- pub `list_module_assets` function L47-55 — `( conn: &mut SqliteConnection, module_id: &str, ) -> QueryResult<Vec<CampaignAss...` — List all assets for a module.
- pub `list_campaign_assets_by_mime` function L58-68 — `( conn: &mut SqliteConnection, campaign_id: &str, mime_type: &str, ) -> QueryRes...` — List assets by MIME type for a campaign.
- pub `list_module_assets_by_mime` function L71-81 — `( conn: &mut SqliteConnection, module_id: &str, mime_type: &str, ) -> QueryResul...` — List assets by MIME type for a module.
- pub `delete_campaign_asset` function L87-89 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<usize>` — Delete a campaign asset by ID.
- pub `campaign_asset_exists` function L92-97 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<bool>` — Check if a campaign asset exists.
- pub `count_campaign_assets` function L100-105 — `(conn: &mut SqliteConnection, campaign_id: &str) -> QueryResult<i64>` — Count assets for a campaign.
- pub `count_module_assets` function L108-113 — `(conn: &mut SqliteConnection, module_id: &str) -> QueryResult<i64>` — Count assets for a module.
-  `tests` module L116-354 — `-` — Database operations for campaign assets (user-uploaded files).
-  `setup_test_data` function L122-128 — `(conn: &mut SqliteConnection)` — Database operations for campaign assets (user-uploaded files).
-  `test_insert_and_get_campaign_asset` function L131-151 — `()` — Database operations for campaign assets (user-uploaded files).
-  `test_insert_module_asset` function L154-170 — `()` — Database operations for campaign assets (user-uploaded files).
-  `test_list_campaign_assets` function L173-199 — `()` — Database operations for campaign assets (user-uploaded files).
-  `test_list_module_assets` function L202-225 — `()` — Database operations for campaign assets (user-uploaded files).
-  `test_list_assets_by_mime_type` function L228-253 — `()` — Database operations for campaign assets (user-uploaded files).
-  `test_delete_campaign_asset` function L256-274 — `()` — Database operations for campaign assets (user-uploaded files).
-  `test_get_campaign_asset_optional` function L277-295 — `()` — Database operations for campaign assets (user-uploaded files).
-  `test_count_campaign_assets` function L298-328 — `()` — Database operations for campaign assets (user-uploaded files).
-  `test_count_module_assets` function L331-353 — `()` — Database operations for campaign assets (user-uploaded files).

#### crates/mimir-core/src/dal/campaign/campaign_source.rs

- pub `insert_campaign_source` function L11-20 — `( conn: &mut SqliteConnection, source: &NewCampaignSource, ) -> QueryResult<Stri...` — Insert a new campaign source.
- pub `insert_campaign_sources` function L23-30 — `( conn: &mut SqliteConnection, sources: &[NewCampaignSource], ) -> QueryResult<u...` — Insert multiple campaign sources in a batch.
- pub `get_campaign_source` function L33-35 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<CampaignSource>` — Get a campaign source by ID.
- pub `list_campaign_sources` function L38-46 — `( conn: &mut SqliteConnection, campaign_id: &str, ) -> QueryResult<Vec<CampaignS...` — List all sources for a campaign.
- pub `list_campaign_source_codes` function L49-58 — `( conn: &mut SqliteConnection, campaign_id: &str, ) -> QueryResult<Vec<String>>` — List source codes for a campaign (just the codes, not full objects).
- pub `delete_campaign_source` function L61-63 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<usize>` — Delete a campaign source by ID.
- pub `delete_campaign_source_by_code` function L66-77 — `( conn: &mut SqliteConnection, campaign_id: &str, source_code: &str, ) -> QueryR...` — Delete a campaign source by campaign and source code.
- pub `delete_all_campaign_sources` function L80-86 — `( conn: &mut SqliteConnection, campaign_id: &str, ) -> QueryResult<usize>` — Delete all sources for a campaign.
- pub `campaign_has_source` function L89-103 — `( conn: &mut SqliteConnection, campaign_id: &str, source_code: &str, ) -> QueryR...` — Check if a campaign has a specific source enabled.
- pub `count_campaign_sources` function L106-111 — `(conn: &mut SqliteConnection, campaign_id: &str) -> QueryResult<i64>` — Count sources for a campaign.
-  `tests` module L114-273 — `-` — Database operations for campaign sources (allowed books).
-  `setup_test_data` function L122-132 — `(conn: &mut SqliteConnection)` — Database operations for campaign sources (allowed books).
-  `test_insert_and_get_campaign_source` function L135-147 — `()` — Database operations for campaign sources (allowed books).
-  `test_insert_multiple_campaign_sources` function L150-165 — `()` — Database operations for campaign sources (allowed books).
-  `test_list_campaign_source_codes` function L168-180 — `()` — Database operations for campaign sources (allowed books).
-  `test_campaign_has_source` function L183-194 — `()` — Database operations for campaign sources (allowed books).
-  `test_delete_campaign_source` function L197-209 — `()` — Database operations for campaign sources (allowed books).
-  `test_delete_campaign_source_by_code` function L212-226 — `()` — Database operations for campaign sources (allowed books).
-  `test_delete_all_campaign_sources` function L229-250 — `()` — Database operations for campaign sources (allowed books).
-  `test_count_campaign_sources` function L253-272 — `()` — Database operations for campaign sources (allowed books).

#### crates/mimir-core/src/dal/campaign/character.rs

- pub `insert_character` function L11-17 — `(conn: &mut SqliteConnection, character: &NewCharacter) -> QueryResult<String>` — Insert a new character.
- pub `get_character` function L20-22 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<Character>` — Get a character by ID.
- pub `get_character_optional` function L25-30 — `( conn: &mut SqliteConnection, id: &str, ) -> QueryResult<Option<Character>>` — Get a character by ID, returning None if not found.
- pub `list_campaign_characters` function L33-41 — `( conn: &mut SqliteConnection, campaign_id: &str, ) -> QueryResult<Vec<Character...` — List all characters for a campaign.
- pub `list_pcs` function L44-50 — `(conn: &mut SqliteConnection, campaign_id: &str) -> QueryResult<Vec<Character>>` — List all player characters for a campaign.
- pub `list_unassigned_pcs` function L53-59 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<Character>>` — List all unassigned player characters (no campaign).
- pub `list_npcs` function L62-68 — `(conn: &mut SqliteConnection, campaign_id: &str) -> QueryResult<Vec<Character>>` — List all NPCs for a campaign.
- pub `list_npcs_by_location` function L71-82 — `( conn: &mut SqliteConnection, campaign_id: &str, location: &str, ) -> QueryResu...` — List NPCs by location.
- pub `list_npcs_by_faction` function L85-96 — `( conn: &mut SqliteConnection, campaign_id: &str, faction: &str, ) -> QueryResul...` — List NPCs by faction.
- pub `update_character` function L99-107 — `( conn: &mut SqliteConnection, id: &str, update: &UpdateCharacter, ) -> QueryRes...` — Update a character.
- pub `delete_character` function L110-112 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<usize>` — Delete a character by ID.
- pub `assign_character_to_campaign` function L115-123 — `( conn: &mut SqliteConnection, character_id: &str, campaign_id: &str, ) -> Query...` — Assign a character to a campaign.
- pub `character_exists` function L126-131 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<bool>` — Check if a character exists.
- pub `count_campaign_characters` function L134-139 — `(conn: &mut SqliteConnection, campaign_id: &str) -> QueryResult<i64>` — Count all characters for a campaign.
- pub `count_pcs` function L142-148 — `(conn: &mut SqliteConnection, campaign_id: &str) -> QueryResult<i64>` — Count PCs for a campaign.
- pub `count_npcs` function L151-157 — `(conn: &mut SqliteConnection, campaign_id: &str) -> QueryResult<i64>` — Count NPCs for a campaign.
-  `tests` module L160-395 — `-` — Database operations for characters (PCs and NPCs).
-  `setup_test_data` function L166-169 — `(conn: &mut SqliteConnection)` — Database operations for characters (PCs and NPCs).
-  `test_insert_and_get_character` function L172-185 — `()` — Database operations for characters (PCs and NPCs).
-  `test_insert_npc` function L188-201 — `()` — Database operations for characters (PCs and NPCs).
-  `test_list_campaign_characters` function L204-217 — `()` — Database operations for characters (PCs and NPCs).
-  `test_list_pcs_and_npcs` function L220-240 — `()` — Database operations for characters (PCs and NPCs).
-  `test_list_npcs_by_location` function L243-264 — `()` — Database operations for characters (PCs and NPCs).
-  `test_list_npcs_by_faction` function L267-284 — `()` — Database operations for characters (PCs and NPCs).
-  `test_update_character_name` function L287-299 — `()` — Database operations for characters (PCs and NPCs).
-  `test_update_ability_scores` function L302-320 — `()` — Database operations for characters (PCs and NPCs).
-  `test_update_currency` function L323-339 — `()` — Database operations for characters (PCs and NPCs).
-  `test_delete_character` function L342-354 — `()` — Database operations for characters (PCs and NPCs).
-  `test_count_characters` function L357-379 — `()` — Database operations for characters (PCs and NPCs).
-  `test_get_character_optional` function L382-394 — `()` — Database operations for characters (PCs and NPCs).

#### crates/mimir-core/src/dal/campaign/character_class.rs

- pub `insert_character_class` function L11-20 — `( conn: &mut SqliteConnection, class: &NewCharacterClass, ) -> QueryResult<Strin...` — Insert a new character class.
- pub `get_character_class` function L23-25 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<CharacterClass>` — Get a character class by ID.
- pub `get_character_class_optional` function L28-33 — `( conn: &mut SqliteConnection, id: &str, ) -> QueryResult<Option<CharacterClass>...` — Get a character class by ID, returning None if not found.
- pub `list_character_classes` function L36-45 — `( conn: &mut SqliteConnection, character_id: &str, ) -> QueryResult<Vec<Characte...` — List all classes for a character.
- pub `get_starting_class` function L48-57 — `( conn: &mut SqliteConnection, character_id: &str, ) -> QueryResult<Option<Chara...` — Get the starting class for a character.
- pub `get_total_level` function L60-68 — `(conn: &mut SqliteConnection, character_id: &str) -> QueryResult<i64>` — Calculate total character level (sum of all class levels).
- pub `update_character_class` function L71-79 — `( conn: &mut SqliteConnection, id: &str, update: &UpdateCharacterClass, ) -> Que...` — Update a character class.
- pub `delete_character_class` function L82-84 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<usize>` — Delete a character class by ID.
- pub `delete_character_classes` function L87-95 — `( conn: &mut SqliteConnection, character_id: &str, ) -> QueryResult<usize>` — Delete all classes for a character.
- pub `character_class_exists` function L98-103 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<bool>` — Check if a character class exists.
- pub `count_character_classes` function L106-114 — `( conn: &mut SqliteConnection, character_id: &str, ) -> QueryResult<i64>` — Count classes for a character.
- pub `find_character_class_by_name` function L118-130 — `( conn: &mut SqliteConnection, character_id: &str, class_name: &str, class_sourc...` — Find a character's class entry by class name and source.
-  `tests` module L133-267 — `-` — Database operations for character class levels.
-  `setup_test_data` function L139-145 — `(conn: &mut SqliteConnection)` — Database operations for character class levels.
-  `test_insert_and_get_character_class` function L148-160 — `()` — Database operations for character class levels.
-  `test_multiclass_character` function L163-182 — `()` — Database operations for character class levels.
-  `test_get_starting_class` function L185-198 — `()` — Database operations for character class levels.
-  `test_update_class_level` function L201-213 — `()` — Database operations for character class levels.
-  `test_update_subclass` function L216-230 — `()` — Database operations for character class levels.
-  `test_delete_character_class` function L233-245 — `()` — Database operations for character class levels.
-  `test_count_character_classes` function L248-266 — `()` — Database operations for character class levels.

#### crates/mimir-core/src/dal/campaign/character_feat.rs

- pub `insert_character_feat` function L11-20 — `( conn: &mut SqliteConnection, feat: &NewCharacterFeat, ) -> QueryResult<String>` — Insert a new character feat.
- pub `get_character_feat` function L23-25 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<CharacterFeat>` — Get a character feat by ID.
- pub `get_character_feat_optional` function L28-33 — `( conn: &mut SqliteConnection, id: &str, ) -> QueryResult<Option<CharacterFeat>>` — Get a character feat by ID, returning None if not found.
- pub `list_character_feats` function L36-44 — `( conn: &mut SqliteConnection, character_id: &str, ) -> QueryResult<Vec<Characte...` — List all feats for a character.
- pub `list_feats_by_source_type` function L47-57 — `( conn: &mut SqliteConnection, character_id: &str, source_type: &str, ) -> Query...` — List feats by source type (asi, race, class, bonus).
- pub `character_has_feat` function L60-74 — `( conn: &mut SqliteConnection, character_id: &str, feat_name: &str, ) -> QueryRe...` — Check if a character has a specific feat (by name).
- pub `delete_character_feat` function L77-79 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<usize>` — Delete a character feat by ID.
- pub `delete_character_feats` function L82-90 — `( conn: &mut SqliteConnection, character_id: &str, ) -> QueryResult<usize>` — Delete all feats for a character.
- pub `character_feat_exists` function L93-98 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<bool>` — Check if a character feat exists.
- pub `count_character_feats` function L101-106 — `(conn: &mut SqliteConnection, character_id: &str) -> QueryResult<i64>` — Count feats for a character.
-  `tests` module L109-270 — `-` — Database operations for character feats.
-  `setup_test_data` function L115-121 — `(conn: &mut SqliteConnection)` — Database operations for character feats.
-  `test_insert_and_get_feat` function L124-136 — `()` — Database operations for character feats.
-  `test_list_character_feats` function L139-156 — `()` — Database operations for character feats.
-  `test_list_feats_by_source_type` function L159-177 — `()` — Database operations for character feats.
-  `test_character_has_feat` function L180-190 — `()` — Database operations for character feats.
-  `test_different_source_types` function L193-233 — `()` — Database operations for character feats.
-  `test_delete_character_feat` function L236-248 — `()` — Database operations for character feats.
-  `test_count_character_feats` function L251-269 — `()` — Database operations for character feats.

#### crates/mimir-core/src/dal/campaign/character_feature.rs

- pub `insert_character_feature` function L11-20 — `( conn: &mut SqliteConnection, feature: &NewCharacterFeature, ) -> QueryResult<S...` — Insert a new character feature.
- pub `get_character_feature` function L23-25 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<CharacterFeature>` — Get a character feature by ID.
- pub `get_character_feature_optional` function L28-33 — `( conn: &mut SqliteConnection, id: &str, ) -> QueryResult<Option<CharacterFeatur...` — Get a character feature by ID, returning None if not found.
- pub `list_character_features` function L36-44 — `( conn: &mut SqliteConnection, character_id: &str, ) -> QueryResult<Vec<Characte...` — List all features for a character.
- pub `list_features_by_type` function L47-57 — `( conn: &mut SqliteConnection, character_id: &str, feature_type: &str, ) -> Quer...` — List features by type (fighting_style, metamagic, maneuver, invocation, pact_boon).
- pub `list_features_by_class` function L60-70 — `( conn: &mut SqliteConnection, character_id: &str, source_class: &str, ) -> Quer...` — List features by source class.
- pub `character_has_feature` function L73-89 — `( conn: &mut SqliteConnection, character_id: &str, feature_type: &str, feature_n...` — Check if a character has a specific feature (by type and name).
- pub `find_feature_by_name` function L92-104 — `( conn: &mut SqliteConnection, character_id: &str, feature_type: &str, feature_n...` — Find a feature by type and name (for swap operations).
- pub `delete_character_feature` function L107-109 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<usize>` — Delete a character feature by ID.
- pub `delete_all_character_features` function L112-120 — `( conn: &mut SqliteConnection, character_id: &str, ) -> QueryResult<usize>` — Delete all features for a character.
- pub `delete_features_by_type` function L123-134 — `( conn: &mut SqliteConnection, character_id: &str, feature_type: &str, ) -> Quer...` — Delete features by type for a character.
- pub `character_feature_exists` function L137-142 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<bool>` — Check if a character feature exists.
- pub `count_character_features` function L145-150 — `(conn: &mut SqliteConnection, character_id: &str) -> QueryResult<i64>` — Count features for a character.
- pub `count_features_by_type` function L153-163 — `( conn: &mut SqliteConnection, character_id: &str, feature_type: &str, ) -> Quer...` — Count features by type for a character.
-  `tests` module L166-300 — `-` — Database operations for character class feature choices (fighting styles, metamagic, etc).
-  `setup_test_data` function L172-178 — `(conn: &mut SqliteConnection)` — Database operations for character class feature choices (fighting styles, metamagic, etc).
-  `test_insert_and_get_feature` function L181-195 — `()` — Database operations for character class feature choices (fighting styles, metamagic, etc).
-  `test_list_features_by_type` function L198-216 — `()` — Database operations for character class feature choices (fighting styles, metamagic, etc).
-  `test_character_has_feature` function L219-233 — `()` — Database operations for character class feature choices (fighting styles, metamagic, etc).
-  `test_find_feature_by_name` function L236-253 — `()` — Database operations for character class feature choices (fighting styles, metamagic, etc).
-  `test_delete_feature` function L256-268 — `()` — Database operations for character class feature choices (fighting styles, metamagic, etc).
-  `test_count_features_by_type` function L271-285 — `()` — Database operations for character class feature choices (fighting styles, metamagic, etc).
-  `test_pact_boon` function L288-299 — `()` — Database operations for character class feature choices (fighting styles, metamagic, etc).

#### crates/mimir-core/src/dal/campaign/character_inventory.rs

- pub `insert_character_inventory` function L11-20 — `( conn: &mut SqliteConnection, item: &NewCharacterInventory, ) -> QueryResult<St...` — Insert a new inventory item.
- pub `get_character_inventory` function L23-28 — `( conn: &mut SqliteConnection, id: &str, ) -> QueryResult<CharacterInventory>` — Get an inventory item by ID.
- pub `get_character_inventory_optional` function L31-36 — `( conn: &mut SqliteConnection, id: &str, ) -> QueryResult<Option<CharacterInvent...` — Get an inventory item by ID, returning None if not found.
- pub `list_character_inventory` function L39-47 — `( conn: &mut SqliteConnection, character_id: &str, ) -> QueryResult<Vec<Characte...` — List all inventory items for a character.
- pub `list_equipped_items` function L50-59 — `( conn: &mut SqliteConnection, character_id: &str, ) -> QueryResult<Vec<Characte...` — List equipped items for a character.
- pub `list_attuned_items` function L62-71 — `( conn: &mut SqliteConnection, character_id: &str, ) -> QueryResult<Vec<Characte...` — List attuned items for a character.
- pub `count_attuned_items` function L74-80 — `(conn: &mut SqliteConnection, character_id: &str) -> QueryResult<i64>` — Count attuned items for a character (max 3 in D&D 5e).
- pub `update_character_inventory` function L83-91 — `( conn: &mut SqliteConnection, id: &str, update: &UpdateCharacterInventory, ) ->...` — Update an inventory item.
- pub `delete_character_inventory` function L94-96 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<usize>` — Delete an inventory item by ID.
- pub `delete_all_character_inventory` function L99-107 — `( conn: &mut SqliteConnection, character_id: &str, ) -> QueryResult<usize>` — Delete all inventory for a character.
- pub `character_inventory_exists` function L110-115 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<bool>` — Check if an inventory item exists.
- pub `count_character_inventory` function L118-126 — `( conn: &mut SqliteConnection, character_id: &str, ) -> QueryResult<i64>` — Count inventory items for a character.
-  `tests` module L129-244 — `-` — Database operations for character inventory items.
-  `setup_test_data` function L135-141 — `(conn: &mut SqliteConnection)` — Database operations for character inventory items.
-  `test_insert_and_get_inventory` function L144-155 — `()` — Database operations for character inventory items.
-  `test_list_character_inventory` function L158-170 — `()` — Database operations for character inventory items.
-  `test_equipped_items` function L173-188 — `()` — Database operations for character inventory items.
-  `test_attuned_items` function L191-212 — `()` — Database operations for character inventory items.
-  `test_update_inventory` function L215-228 — `()` — Database operations for character inventory items.
-  `test_delete_inventory` function L231-243 — `()` — Database operations for character inventory items.

#### crates/mimir-core/src/dal/campaign/character_proficiency.rs

- pub `insert_character_proficiency` function L13-22 — `( conn: &mut SqliteConnection, proficiency: &NewCharacterProficiency, ) -> Query...` — Insert a new proficiency.
- pub `get_character_proficiency` function L25-30 — `( conn: &mut SqliteConnection, id: &str, ) -> QueryResult<CharacterProficiency>` — Get a proficiency by ID.
- pub `get_character_proficiency_optional` function L33-38 — `( conn: &mut SqliteConnection, id: &str, ) -> QueryResult<Option<CharacterProfic...` — Get a proficiency by ID, returning None if not found.
- pub `list_character_proficiencies` function L41-50 — `( conn: &mut SqliteConnection, character_id: &str, ) -> QueryResult<Vec<Characte...` — List all proficiencies for a character.
- pub `list_proficiencies_by_type` function L53-63 — `( conn: &mut SqliteConnection, character_id: &str, proficiency_type: &str, ) -> ...` — List proficiencies by type.
- pub `list_skill_proficiencies` function L66-71 — `( conn: &mut SqliteConnection, character_id: &str, ) -> QueryResult<Vec<Characte...` — List skill proficiencies.
- pub `list_save_proficiencies` function L74-79 — `( conn: &mut SqliteConnection, character_id: &str, ) -> QueryResult<Vec<Characte...` — List saving throw proficiencies.
- pub `list_languages` function L82-87 — `( conn: &mut SqliteConnection, character_id: &str, ) -> QueryResult<Vec<Characte...` — List languages.
- pub `list_expertise` function L90-99 — `( conn: &mut SqliteConnection, character_id: &str, ) -> QueryResult<Vec<Characte...` — List expertise proficiencies.
- pub `character_has_proficiency` function L102-118 — `( conn: &mut SqliteConnection, character_id: &str, proficiency_type: &str, name:...` — Check if character has a specific proficiency.
- pub `update_character_proficiency` function L121-129 — `( conn: &mut SqliteConnection, id: &str, update: &UpdateCharacterProficiency, ) ...` — Update a proficiency.
- pub `delete_character_proficiency` function L132-134 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<usize>` — Delete a proficiency by ID.
- pub `delete_all_character_proficiencies` function L137-146 — `( conn: &mut SqliteConnection, character_id: &str, ) -> QueryResult<usize>` — Delete all proficiencies for a character.
- pub `character_proficiency_exists` function L149-154 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<bool>` — Check if a proficiency exists.
- pub `count_character_proficiencies` function L157-165 — `( conn: &mut SqliteConnection, character_id: &str, ) -> QueryResult<i64>` — Count proficiencies for a character.
-  `tests` module L168-307 — `-` — Database operations for character proficiencies.
-  `setup_test_data` function L174-180 — `(conn: &mut SqliteConnection)` — Database operations for character proficiencies.
-  `test_insert_and_get_proficiency` function L183-194 — `()` — Database operations for character proficiencies.
-  `test_list_character_proficiencies` function L197-210 — `()` — Database operations for character proficiencies.
-  `test_list_by_type` function L213-229 — `()` — Database operations for character proficiencies.
-  `test_expertise` function L232-245 — `()` — Database operations for character proficiencies.
-  `test_character_has_proficiency` function L248-260 — `()` — Database operations for character proficiencies.
-  `test_update_expertise` function L263-275 — `()` — Database operations for character proficiencies.
-  `test_languages` function L278-291 — `()` — Database operations for character proficiencies.
-  `test_delete_proficiency` function L294-306 — `()` — Database operations for character proficiencies.

#### crates/mimir-core/src/dal/campaign/character_source.rs

- pub `insert_character_source` function L11-20 — `( conn: &mut SqliteConnection, source: &NewCharacterSource, ) -> QueryResult<Str...` — Insert a new character source.
- pub `get_character_source` function L23-25 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<CharacterSource>` — Get a character source by ID.
- pub `list_character_sources` function L28-36 — `( conn: &mut SqliteConnection, character_id: &str, ) -> QueryResult<Vec<Characte...` — List all sources for a character.
- pub `list_character_source_codes` function L39-48 — `( conn: &mut SqliteConnection, character_id: &str, ) -> QueryResult<Vec<String>>` — List source codes for a character (just the codes, not full objects).
- pub `delete_character_source` function L51-53 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<usize>` — Delete a character source by ID.
- pub `delete_character_source_by_code` function L56-67 — `( conn: &mut SqliteConnection, character_id: &str, source_code: &str, ) -> Query...` — Delete a character source by character and source code.
- pub `delete_all_character_sources` function L70-78 — `( conn: &mut SqliteConnection, character_id: &str, ) -> QueryResult<usize>` — Delete all sources for a character.
- pub `character_has_source` function L81-95 — `( conn: &mut SqliteConnection, character_id: &str, source_code: &str, ) -> Query...` — Check if a character has a specific source enabled.
- pub `count_character_sources` function L98-103 — `(conn: &mut SqliteConnection, character_id: &str) -> QueryResult<i64>` — Count sources for a character.

#### crates/mimir-core/src/dal/campaign/character_spell.rs

- pub `insert_character_spell` function L11-20 — `( conn: &mut SqliteConnection, spell: &NewCharacterSpell, ) -> QueryResult<Strin...` — Insert a new character spell.
- pub `get_character_spell` function L23-25 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<CharacterSpell>` — Get a character spell by ID.
- pub `get_character_spell_optional` function L28-33 — `( conn: &mut SqliteConnection, id: &str, ) -> QueryResult<Option<CharacterSpell>...` — Get a character spell by ID, returning None if not found.
- pub `list_character_spells` function L36-44 — `( conn: &mut SqliteConnection, character_id: &str, ) -> QueryResult<Vec<Characte...` — List all spells for a character.
- pub `list_spells_by_class` function L47-57 — `( conn: &mut SqliteConnection, character_id: &str, source_class: &str, ) -> Quer...` — List spells by source class.
- pub `list_prepared_spells` function L60-69 — `( conn: &mut SqliteConnection, character_id: &str, ) -> QueryResult<Vec<Characte...` — List prepared spells for a character.
- pub `count_prepared_spells` function L72-78 — `(conn: &mut SqliteConnection, character_id: &str) -> QueryResult<i64>` — Count prepared spells for a character.
- pub `character_knows_spell` function L81-95 — `( conn: &mut SqliteConnection, character_id: &str, spell_name: &str, ) -> QueryR...` — Check if character knows a specific spell.
- pub `update_character_spell` function L98-106 — `( conn: &mut SqliteConnection, id: &str, update: &UpdateCharacterSpell, ) -> Que...` — Update a character spell.
- pub `delete_character_spell` function L109-111 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<usize>` — Delete a character spell by ID.
- pub `delete_all_character_spells` function L114-122 — `( conn: &mut SqliteConnection, character_id: &str, ) -> QueryResult<usize>` — Delete all spells for a character.
- pub `character_spell_exists` function L125-130 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<bool>` — Check if a character spell exists.
- pub `count_character_spells` function L133-138 — `(conn: &mut SqliteConnection, character_id: &str) -> QueryResult<i64>` — Count spells for a character.
- pub `find_character_spell_by_name` function L142-154 — `( conn: &mut SqliteConnection, character_id: &str, spell_name: &str, source_clas...` — Find a character spell by name and source class.
- pub `count_spells_by_class` function L157-167 — `( conn: &mut SqliteConnection, character_id: &str, source_class: &str, ) -> Quer...` — Count spells for a character by source class.
-  `tests` module L170-299 — `-` — Database operations for character spells.
-  `setup_test_data` function L176-182 — `(conn: &mut SqliteConnection)` — Database operations for character spells.
-  `test_insert_and_get_spell` function L185-196 — `()` — Database operations for character spells.
-  `test_list_character_spells` function L199-212 — `()` — Database operations for character spells.
-  `test_prepared_spells` function L215-233 — `()` — Database operations for character spells.
-  `test_spells_by_class` function L236-255 — `()` — Database operations for character spells.
-  `test_character_knows_spell` function L258-268 — `()` — Database operations for character spells.
-  `test_update_prepared_status` function L271-283 — `()` — Database operations for character spells.
-  `test_delete_spell` function L286-298 — `()` — Database operations for character spells.

#### crates/mimir-core/src/dal/campaign/document.rs

- pub `insert_document` function L14-20 — `(conn: &mut SqliteConnection, doc: &NewDocument) -> QueryResult<String>` — Insert a new document.
- pub `get_document` function L23-25 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<Document>` — Get a document by ID.
- pub `get_document_optional` function L28-33 — `( conn: &mut SqliteConnection, id: &str, ) -> QueryResult<Option<Document>>` — Get a document by ID, returning None if not found.
- pub `list_campaign_documents` function L36-44 — `( conn: &mut SqliteConnection, campaign_id: &str, ) -> QueryResult<Vec<Document>...` — List all documents for a campaign (including module documents).
- pub `list_campaign_level_documents` function L47-56 — `( conn: &mut SqliteConnection, campaign_id: &str, ) -> QueryResult<Vec<Document>...` — List only campaign-level documents (not in any module).
- pub `list_module_documents` function L59-67 — `( conn: &mut SqliteConnection, module_id: &str, ) -> QueryResult<Vec<Document>>` — List all documents for a module.
- pub `list_documents_by_type` function L70-80 — `( conn: &mut SqliteConnection, campaign_id: &str, doc_type: &str, ) -> QueryResu...` — List documents by type for a campaign.
- pub `update_document` function L83-91 — `( conn: &mut SqliteConnection, id: &str, update: &UpdateDocument, ) -> QueryResu...` — Update a document.
- pub `delete_document` function L94-96 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<usize>` — Delete a document by ID.
- pub `document_exists` function L99-104 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<bool>` — Check if a document exists.
- pub `count_campaign_documents` function L107-112 — `(conn: &mut SqliteConnection, campaign_id: &str) -> QueryResult<i64>` — Count documents for a campaign.
- pub `count_module_documents` function L115-120 — `(conn: &mut SqliteConnection, module_id: &str) -> QueryResult<i64>` — Count documents for a module.
- pub `next_campaign_document_sort_order` function L123-134 — `( conn: &mut SqliteConnection, campaign_id: &str, ) -> QueryResult<i32>` — Get the next available sort_order for campaign-level documents.
- pub `next_module_document_sort_order` function L137-147 — `( conn: &mut SqliteConnection, module_id: &str, ) -> QueryResult<i32>` — Get the next available sort_order for module documents.
- pub `swap_document_order` function L150-178 — `( conn: &mut SqliteConnection, doc_id_a: &str, doc_id_b: &str, ) -> QueryResult<...` — Swap sort_order between two documents.
- pub `DocumentSearchResult` struct L182-199 — `{ id: String, campaign_id: String, module_id: Option<String>, title: String, con...` — FTS search result with relevance ranking.
- pub `search_documents` function L204-224 — `( conn: &mut SqliteConnection, campaign_id: &str, query: &str, ) -> QueryResult<...` — Search documents using FTS5 full-text search.
- pub `search_module_documents` function L227-246 — `( conn: &mut SqliteConnection, module_id: &str, query: &str, ) -> QueryResult<Ve...` — Search documents within a specific module using FTS5.
-  `tests` module L249-534 — `-` — Database operations for documents (markdown content).
-  `setup_test_data` function L255-261 — `(conn: &mut SqliteConnection)` — Database operations for documents (markdown content).
-  `test_insert_and_get_document` function L264-280 — `()` — Database operations for documents (markdown content).
-  `test_insert_module_document` function L283-293 — `()` — Database operations for documents (markdown content).
-  `test_list_campaign_documents` function L296-315 — `()` — Database operations for documents (markdown content).
-  `test_list_module_documents` function L318-331 — `()` — Database operations for documents (markdown content).
-  `test_list_documents_by_type` function L334-348 — `()` — Database operations for documents (markdown content).
-  `test_update_document_content` function L351-364 — `()` — Database operations for documents (markdown content).
-  `test_update_document_move_to_module` function L367-379 — `()` — Database operations for documents (markdown content).
-  `test_delete_document` function L382-394 — `()` — Database operations for documents (markdown content).
-  `test_count_documents` function L397-419 — `()` — Database operations for documents (markdown content).
-  `test_fts_search` function L422-444 — `()` — Database operations for documents (markdown content).
-  `test_fts_search_module` function L447-463 — `()` — Database operations for documents (markdown content).
-  `test_next_sort_order` function L466-479 — `()` — Database operations for documents (markdown content).
-  `test_swap_document_order` function L482-506 — `()` — Database operations for documents (markdown content).
-  `test_swap_document_order_non_adjacent` function L509-533 — `()` — Database operations for documents (markdown content).

#### crates/mimir-core/src/dal/campaign/fog.rs

- pub `insert_fog_revealed_area` function L11-19 — `( conn: &mut SqliteConnection, area: &NewFogRevealedArea, ) -> QueryResult<Strin...` — Insert a new revealed area.
- pub `get_fog_revealed_area` function L22-27 — `( conn: &mut SqliteConnection, id: &str, ) -> QueryResult<FogRevealedArea>` — Get a revealed area by ID.
- pub `list_fog_revealed_areas` function L30-38 — `( conn: &mut SqliteConnection, map_id: &str, ) -> QueryResult<Vec<FogRevealedAre...` — List all revealed areas for a map.
- pub `delete_fog_revealed_area` function L41-46 — `( conn: &mut SqliteConnection, id: &str, ) -> QueryResult<usize>` — Delete a revealed area.
- pub `delete_all_fog_revealed_areas` function L49-56 — `( conn: &mut SqliteConnection, map_id: &str, ) -> QueryResult<usize>` — Delete all revealed areas for a map.
- pub `count_fog_revealed_areas` function L59-67 — `( conn: &mut SqliteConnection, map_id: &str, ) -> QueryResult<i64>` — Count revealed areas for a map.
-  `tests` module L70-149 — `-` — Database operations for fog of war revealed areas.
-  `setup_test_map` function L76-87 — `(conn: &mut SqliteConnection) -> String` — Database operations for fog of war revealed areas.
-  `test_insert_and_get_revealed_area` function L90-102 — `()` — Database operations for fog of war revealed areas.
-  `test_list_revealed_areas` function L105-116 — `()` — Database operations for fog of war revealed areas.
-  `test_delete_revealed_area` function L119-131 — `()` — Database operations for fog of war revealed areas.
-  `test_delete_all_revealed_areas` function L134-148 — `()` — Database operations for fog of war revealed areas.

#### crates/mimir-core/src/dal/campaign/homebrew_crud.rs

-  `homebrew_crud` macro L9-101 — `-` — Generate standard homebrew CRUD functions for a given entity type.

#### crates/mimir-core/src/dal/campaign/light_source.rs

- pub `insert_light_source` function L11-20 — `( conn: &mut SqliteConnection, light: &NewLightSource, ) -> QueryResult<String>` — Insert a new light source.
- pub `get_light_source` function L23-25 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<LightSource>` — Get a light source by ID.
- pub `get_light_source_optional` function L28-33 — `( conn: &mut SqliteConnection, id: &str, ) -> QueryResult<Option<LightSource>>` — Get a light source by ID, returning None if not found.
- pub `list_light_sources` function L36-43 — `( conn: &mut SqliteConnection, map_id: &str, ) -> QueryResult<Vec<LightSource>>` — List all light sources for a map.
- pub `list_active_light_sources` function L46-54 — `( conn: &mut SqliteConnection, map_id: &str, ) -> QueryResult<Vec<LightSource>>` — List active light sources for a map.
- pub `update_light_source` function L57-65 — `( conn: &mut SqliteConnection, id: &str, update: &UpdateLightSource, ) -> QueryR...` — Update a light source.
- pub `delete_light_source` function L68-70 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<usize>` — Delete a light source by ID.
- pub `delete_all_light_sources` function L73-75 — `(conn: &mut SqliteConnection, map_id: &str) -> QueryResult<usize>` — Delete all light sources for a map.
- pub `light_source_exists` function L78-83 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<bool>` — Check if a light source exists.
- pub `count_light_sources` function L86-91 — `(conn: &mut SqliteConnection, map_id: &str) -> QueryResult<i64>` — Count light sources for a map.
-  `tests` module L94-285 — `-` — Database operations for dynamic light sources on maps.
-  `setup_test_data` function L100-109 — `(conn: &mut SqliteConnection)` — Database operations for dynamic light sources on maps.
-  `test_insert_and_get_light_source` function L112-124 — `()` — Database operations for dynamic light sources on maps.
-  `test_insert_torch` function L127-139 — `()` — Database operations for dynamic light sources on maps.
-  `test_insert_lantern` function L142-152 — `()` — Database operations for dynamic light sources on maps.
-  `test_insert_inactive` function L155-164 — `()` — Database operations for dynamic light sources on maps.
-  `test_list_light_sources` function L167-178 — `()` — Database operations for dynamic light sources on maps.
-  `test_list_active_light_sources` function L181-193 — `()` — Database operations for dynamic light sources on maps.
-  `test_turn_on_off` function L196-216 — `()` — Database operations for dynamic light sources on maps.
-  `test_update_position` function L219-232 — `()` — Database operations for dynamic light sources on maps.
-  `test_update_radii` function L235-248 — `()` — Database operations for dynamic light sources on maps.
-  `test_delete_light_source` function L251-263 — `()` — Database operations for dynamic light sources on maps.
-  `test_count_light_sources` function L266-284 — `()` — Database operations for dynamic light sources on maps.

#### crates/mimir-core/src/dal/campaign/map.rs

- pub `insert_map` function L11-17 — `(conn: &mut SqliteConnection, map: &NewMap) -> QueryResult<String>` — Insert a new map.
- pub `get_map` function L20-22 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<Map>` — Get a map by ID.
- pub `get_map_optional` function L25-27 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<Option<Map>>` — Get a map by ID, returning None if not found.
- pub `list_campaign_maps` function L30-36 — `(conn: &mut SqliteConnection, campaign_id: &str) -> QueryResult<Vec<Map>>` — List all maps for a campaign (including module maps).
- pub `list_campaign_level_maps` function L39-49 — `( conn: &mut SqliteConnection, campaign_id: &str, ) -> QueryResult<Vec<Map>>` — List only campaign-level maps (not in any module).
- pub `list_module_maps` function L52-58 — `(conn: &mut SqliteConnection, module_id: &str) -> QueryResult<Vec<Map>>` — List all maps for a module.
- pub `update_map` function L61-65 — `(conn: &mut SqliteConnection, id: &str, update: &UpdateMap) -> QueryResult<usize...` — Update a map.
- pub `delete_map` function L68-70 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<usize>` — Delete a map by ID.
- pub `map_exists` function L73-78 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<bool>` — Check if a map exists.
- pub `count_campaign_maps` function L81-86 — `(conn: &mut SqliteConnection, campaign_id: &str) -> QueryResult<i64>` — Count maps for a campaign.
- pub `count_module_maps` function L89-94 — `(conn: &mut SqliteConnection, module_id: &str) -> QueryResult<i64>` — Count maps for a module.
- pub `get_next_campaign_sort_order` function L97-109 — `( conn: &mut SqliteConnection, campaign_id: &str, ) -> QueryResult<i32>` — Get the next sort order for a new map in a campaign.
- pub `get_next_module_sort_order` function L112-123 — `( conn: &mut SqliteConnection, module_id: &str, ) -> QueryResult<i32>` — Get the next sort order for a new map in a module.
-  `tests` module L126-306 — `-` — Database operations for maps.
-  `setup_test_data` function L132-144 — `(conn: &mut SqliteConnection)` — Database operations for maps.
-  `test_insert_and_get_map` function L147-159 — `()` — Database operations for maps.
-  `test_insert_module_map` function L162-173 — `()` — Database operations for maps.
-  `test_list_campaign_maps` function L176-193 — `()` — Database operations for maps.
-  `test_list_module_maps` function L196-214 — `()` — Database operations for maps.
-  `test_update_map_lighting` function L217-232 — `()` — Database operations for maps.
-  `test_update_map_move_to_module` function L235-247 — `()` — Database operations for maps.
-  `test_delete_map` function L250-262 — `()` — Database operations for maps.
-  `test_count_maps` function L265-287 — `()` — Database operations for maps.
-  `test_get_next_sort_order` function L290-305 — `()` — Database operations for maps.

#### crates/mimir-core/src/dal/campaign/map_poi.rs

- pub `insert_map_poi` function L11-17 — `(conn: &mut SqliteConnection, poi: &NewMapPoi) -> QueryResult<String>` — Insert a new map POI.
- pub `get_map_poi` function L20-22 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<MapPoi>` — Get a map POI by ID.
- pub `get_map_poi_optional` function L25-30 — `( conn: &mut SqliteConnection, id: &str, ) -> QueryResult<Option<MapPoi>>` — Get a map POI by ID, returning None if not found.
- pub `list_map_pois` function L33-37 — `(conn: &mut SqliteConnection, map_id: &str) -> QueryResult<Vec<MapPoi>>` — List all POIs for a map.
- pub `list_visible_map_pois` function L40-48 — `( conn: &mut SqliteConnection, map_id: &str, ) -> QueryResult<Vec<MapPoi>>` — List visible POIs for a map (for player view).
- pub `update_map_poi` function L51-59 — `( conn: &mut SqliteConnection, id: &str, update: &UpdateMapPoi, ) -> QueryResult...` — Update a map POI.
- pub `delete_map_poi` function L62-64 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<usize>` — Delete a map POI by ID.
- pub `delete_all_map_pois` function L67-69 — `(conn: &mut SqliteConnection, map_id: &str) -> QueryResult<usize>` — Delete all POIs for a map.
- pub `map_poi_exists` function L72-77 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<bool>` — Check if a map POI exists.
- pub `count_map_pois` function L80-85 — `(conn: &mut SqliteConnection, map_id: &str) -> QueryResult<i64>` — Count POIs for a map.
-  `tests` module L88-235 — `-` — Database operations for POI (Point of Interest) placements on maps.
-  `setup_test_data` function L94-103 — `(conn: &mut SqliteConnection)` — Database operations for POI (Point of Interest) placements on maps.
-  `test_insert_and_get_map_poi` function L106-119 — `()` — Database operations for POI (Point of Interest) placements on maps.
-  `test_insert_with_options` function L122-138 — `()` — Database operations for POI (Point of Interest) placements on maps.
-  `test_list_map_pois` function L141-152 — `()` — Database operations for POI (Point of Interest) placements on maps.
-  `test_list_visible_pois` function L155-167 — `()` — Database operations for POI (Point of Interest) placements on maps.
-  `test_update_position` function L170-183 — `()` — Database operations for POI (Point of Interest) placements on maps.
-  `test_update_visible` function L186-198 — `()` — Database operations for POI (Point of Interest) placements on maps.
-  `test_delete_map_poi` function L201-213 — `()` — Database operations for POI (Point of Interest) placements on maps.
-  `test_count_map_pois` function L216-234 — `()` — Database operations for POI (Point of Interest) placements on maps.

#### crates/mimir-core/src/dal/campaign/map_trap.rs

- pub `insert_map_trap` function L11-17 — `(conn: &mut SqliteConnection, trap: &NewMapTrap) -> QueryResult<String>` — Insert a new map trap.
- pub `get_map_trap` function L20-22 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<MapTrap>` — Get a map trap by ID.
- pub `get_map_trap_optional` function L25-30 — `( conn: &mut SqliteConnection, id: &str, ) -> QueryResult<Option<MapTrap>>` — Get a map trap by ID, returning None if not found.
- pub `list_map_traps` function L33-37 — `(conn: &mut SqliteConnection, map_id: &str) -> QueryResult<Vec<MapTrap>>` — List all traps for a map.
- pub `list_visible_map_traps` function L40-48 — `( conn: &mut SqliteConnection, map_id: &str, ) -> QueryResult<Vec<MapTrap>>` — List visible traps for a map (for player view).
- pub `list_armed_map_traps` function L51-59 — `( conn: &mut SqliteConnection, map_id: &str, ) -> QueryResult<Vec<MapTrap>>` — List armed (not triggered) traps for a map.
- pub `update_map_trap` function L62-70 — `( conn: &mut SqliteConnection, id: &str, update: &UpdateMapTrap, ) -> QueryResul...` — Update a map trap.
- pub `delete_map_trap` function L73-75 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<usize>` — Delete a map trap by ID.
- pub `delete_all_map_traps` function L78-80 — `(conn: &mut SqliteConnection, map_id: &str) -> QueryResult<usize>` — Delete all traps for a map.
- pub `map_trap_exists` function L83-88 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<bool>` — Check if a map trap exists.
- pub `count_map_traps` function L91-96 — `(conn: &mut SqliteConnection, map_id: &str) -> QueryResult<i64>` — Count traps for a map.
-  `tests` module L99-275 — `-` — Database operations for trap placements on maps.
-  `setup_test_data` function L105-114 — `(conn: &mut SqliteConnection)` — Database operations for trap placements on maps.
-  `test_insert_and_get_map_trap` function L117-131 — `()` — Database operations for trap placements on maps.
-  `test_insert_with_details` function L134-150 — `()` — Database operations for trap placements on maps.
-  `test_list_map_traps` function L153-164 — `()` — Database operations for trap placements on maps.
-  `test_list_visible_traps` function L167-179 — `()` — Database operations for trap placements on maps.
-  `test_list_armed_traps` function L182-199 — `()` — Database operations for trap placements on maps.
-  `test_trigger_and_reset` function L202-223 — `()` — Database operations for trap placements on maps.
-  `test_update_visible` function L226-238 — `()` — Database operations for trap placements on maps.
-  `test_delete_map_trap` function L241-253 — `()` — Database operations for trap placements on maps.
-  `test_count_map_traps` function L256-274 — `()` — Database operations for trap placements on maps.

#### crates/mimir-core/src/dal/campaign/mod.rs

-  `campaign` module L5 — `-` — Database operations for campaigns, modules, campaign sources, assets, documents, characters, maps, module entities, and map overlays.
-  `campaign_asset` module L6 — `-` — Database operations for campaigns, modules, campaign sources, assets, documents, characters, maps, module entities, and map overlays.
-  `homebrew_crud` module L7 — `-` — Database operations for campaigns, modules, campaign sources, assets, documents, characters, maps, module entities, and map overlays.
-  `campaign_homebrew_item` module L8 — `-` — Database operations for campaigns, modules, campaign sources, assets, documents, characters, maps, module entities, and map overlays.
-  `campaign_homebrew_monster` module L9 — `-` — Database operations for campaigns, modules, campaign sources, assets, documents, characters, maps, module entities, and map overlays.
-  `campaign_homebrew_spell` module L10 — `-` — Database operations for campaigns, modules, campaign sources, assets, documents, characters, maps, module entities, and map overlays.
-  `campaign_source` module L11 — `-` — Database operations for campaigns, modules, campaign sources, assets, documents, characters, maps, module entities, and map overlays.
-  `character` module L12 — `-` — Database operations for campaigns, modules, campaign sources, assets, documents, characters, maps, module entities, and map overlays.
-  `character_class` module L13 — `-` — Database operations for campaigns, modules, campaign sources, assets, documents, characters, maps, module entities, and map overlays.
-  `character_feat` module L14 — `-` — Database operations for campaigns, modules, campaign sources, assets, documents, characters, maps, module entities, and map overlays.
-  `character_feature` module L15 — `-` — Database operations for campaigns, modules, campaign sources, assets, documents, characters, maps, module entities, and map overlays.
-  `character_inventory` module L16 — `-` — Database operations for campaigns, modules, campaign sources, assets, documents, characters, maps, module entities, and map overlays.
-  `character_proficiency` module L17 — `-` — Database operations for campaigns, modules, campaign sources, assets, documents, characters, maps, module entities, and map overlays.
-  `character_source` module L18 — `-` — Database operations for campaigns, modules, campaign sources, assets, documents, characters, maps, module entities, and map overlays.
-  `character_spell` module L19 — `-` — Database operations for campaigns, modules, campaign sources, assets, documents, characters, maps, module entities, and map overlays.
-  `document` module L20 — `-` — Database operations for campaigns, modules, campaign sources, assets, documents, characters, maps, module entities, and map overlays.
-  `fog` module L21 — `-` — Database operations for campaigns, modules, campaign sources, assets, documents, characters, maps, module entities, and map overlays.
-  `light_source` module L22 — `-` — Database operations for campaigns, modules, campaign sources, assets, documents, characters, maps, module entities, and map overlays.
-  `map` module L23 — `-` — Database operations for campaigns, modules, campaign sources, assets, documents, characters, maps, module entities, and map overlays.
-  `map_poi` module L24 — `-` — Database operations for campaigns, modules, campaign sources, assets, documents, characters, maps, module entities, and map overlays.
-  `map_trap` module L25 — `-` — Database operations for campaigns, modules, campaign sources, assets, documents, characters, maps, module entities, and map overlays.
-  `module` module L26 — `-` — Database operations for campaigns, modules, campaign sources, assets, documents, characters, maps, module entities, and map overlays.
-  `module_monster` module L27 — `-` — Database operations for campaigns, modules, campaign sources, assets, documents, characters, maps, module entities, and map overlays.
-  `module_npc` module L28 — `-` — Database operations for campaigns, modules, campaign sources, assets, documents, characters, maps, module entities, and map overlays.
-  `token_placement` module L29 — `-` — Database operations for campaigns, modules, campaign sources, assets, documents, characters, maps, module entities, and map overlays.

#### crates/mimir-core/src/dal/campaign/module.rs

- pub `insert_module` function L12-18 — `(conn: &mut SqliteConnection, module: &NewModule) -> QueryResult<String>` — Insert a new module.
- pub `get_module` function L21-23 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<Module>` — Get a module by ID.
- pub `get_module_optional` function L26-28 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<Option<Module>>` — Get a module by ID, returning None if not found.
- pub `get_module_by_number` function L31-41 — `( conn: &mut SqliteConnection, campaign_id: &str, module_number: i32, ) -> Query...` — Get a module by campaign ID and module number.
- pub `list_modules` function L44-49 — `(conn: &mut SqliteConnection, campaign_id: &str) -> QueryResult<Vec<Module>>` — List all modules for a campaign, ordered by module_number.
- pub `update_module` function L52-60 — `( conn: &mut SqliteConnection, id: &str, update: &UpdateModule, ) -> QueryResult...` — Update a module.
- pub `delete_module` function L65-67 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<usize>` — Delete a module by ID.
- pub `module_exists` function L70-75 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<bool>` — Check if a module exists.
- pub `count_modules` function L78-83 — `(conn: &mut SqliteConnection, campaign_id: &str) -> QueryResult<i64>` — Count modules for a campaign.
- pub `reorder_module` function L89-163 — `( conn: &mut SqliteConnection, campaign_id: &str, module_id: &str, new_position:...` — Reorder a module by moving it to a new position (1-indexed).
- pub `next_module_number` function L166-173 — `(conn: &mut SqliteConnection, campaign_id: &str) -> QueryResult<i32>` — Get the next available module number for a campaign.
-  `tests` module L176-441 — `-` — Database operations for modules (adventure chapters).
-  `setup_test_data` function L182-185 — `(conn: &mut SqliteConnection)` — Database operations for modules (adventure chapters).
-  `test_insert_and_get_module` function L188-202 — `()` — Database operations for modules (adventure chapters).
-  `test_insert_module_with_description` function L205-218 — `()` — Database operations for modules (adventure chapters).
-  `test_list_modules_ordered_by_number` function L221-238 — `()` — Database operations for modules (adventure chapters).
-  `test_get_module_by_number` function L241-254 — `()` — Database operations for modules (adventure chapters).
-  `test_update_module_name` function L257-269 — `()` — Database operations for modules (adventure chapters).
-  `test_update_module_number` function L272-284 — `()` — Database operations for modules (adventure chapters).
-  `test_delete_module` function L287-299 — `()` — Database operations for modules (adventure chapters).
-  `test_get_module_optional` function L302-314 — `()` — Database operations for modules (adventure chapters).
-  `test_count_modules` function L317-329 — `()` — Database operations for modules (adventure chapters).
-  `test_reorder_module_move_down` function L332-353 — `()` — Database operations for modules (adventure chapters).
-  `test_reorder_module_move_up` function L356-377 — `()` — Database operations for modules (adventure chapters).
-  `test_reorder_module_no_op` function L380-395 — `()` — Database operations for modules (adventure chapters).
-  `test_reorder_module_invalid_position` function L398-412 — `()` — Database operations for modules (adventure chapters).
-  `test_next_module_number` function L415-440 — `()` — Database operations for modules (adventure chapters).

#### crates/mimir-core/src/dal/campaign/module_monster.rs

- pub `insert_module_monster` function L11-20 — `( conn: &mut SqliteConnection, monster: &NewModuleMonster, ) -> QueryResult<Stri...` — Insert a new module monster.
- pub `get_module_monster` function L23-25 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<ModuleMonster>` — Get a module monster by ID.
- pub `get_module_monster_optional` function L28-33 — `( conn: &mut SqliteConnection, id: &str, ) -> QueryResult<Option<ModuleMonster>>` — Get a module monster by ID, returning None if not found.
- pub `list_module_monsters` function L36-44 — `( conn: &mut SqliteConnection, module_id: &str, ) -> QueryResult<Vec<ModuleMonst...` — List all monsters for a module.
- pub `update_module_monster` function L47-55 — `( conn: &mut SqliteConnection, id: &str, update: &UpdateModuleMonster, ) -> Quer...` — Update a module monster.
- pub `delete_module_monster` function L58-60 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<usize>` — Delete a module monster by ID.
- pub `delete_all_module_monsters` function L63-69 — `( conn: &mut SqliteConnection, module_id: &str, ) -> QueryResult<usize>` — Delete all monsters for a module.
- pub `module_monster_exists` function L72-77 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<bool>` — Check if a module monster exists.
- pub `count_module_monsters` function L80-85 — `(conn: &mut SqliteConnection, module_id: &str) -> QueryResult<i64>` — Count monsters for a module.
- pub `get_total_monster_count` function L88-96 — `(conn: &mut SqliteConnection, module_id: &str) -> QueryResult<i64>` — Get total monster count for a module (sum of quantities).
-  `tests` module L99-298 — `-` — Database operations for module monsters (catalog monster instances).
-  `setup_test_data` function L105-111 — `(conn: &mut SqliteConnection)` — Database operations for module monsters (catalog monster instances).
-  `test_insert_and_get_module_monster` function L114-127 — `()` — Database operations for module monsters (catalog monster instances).
-  `test_insert_with_customizations` function L130-143 — `()` — Database operations for module monsters (catalog monster instances).
-  `test_list_module_monsters` function L146-163 — `()` — Database operations for module monsters (catalog monster instances).
-  `test_update_module_monster` function L166-178 — `()` — Database operations for module monsters (catalog monster instances).
-  `test_delete_module_monster` function L181-193 — `()` — Database operations for module monsters (catalog monster instances).
-  `test_count_and_total` function L196-218 — `()` — Database operations for module monsters (catalog monster instances).
-  `test_insert_homebrew_module_monster` function L221-246 — `()` — Database operations for module monsters (catalog monster instances).
-  `test_list_mixed_catalog_and_homebrew` function L249-278 — `()` — Database operations for module monsters (catalog monster instances).
-  `test_delete_homebrew_module_monster` function L281-297 — `()` — Database operations for module monsters (catalog monster instances).

#### crates/mimir-core/src/dal/campaign/module_npc.rs

- pub `insert_module_npc` function L11-17 — `(conn: &mut SqliteConnection, npc: &NewModuleNpc) -> QueryResult<String>` — Insert a new module NPC.
- pub `get_module_npc` function L20-22 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<ModuleNpc>` — Get a module NPC by ID.
- pub `get_module_npc_optional` function L25-30 — `( conn: &mut SqliteConnection, id: &str, ) -> QueryResult<Option<ModuleNpc>>` — Get a module NPC by ID, returning None if not found.
- pub `list_module_npcs` function L33-38 — `(conn: &mut SqliteConnection, module_id: &str) -> QueryResult<Vec<ModuleNpc>>` — List all NPCs for a module.
- pub `list_module_npcs_by_role` function L41-51 — `( conn: &mut SqliteConnection, module_id: &str, role: &str, ) -> QueryResult<Vec...` — List NPCs by role.
- pub `update_module_npc` function L54-62 — `( conn: &mut SqliteConnection, id: &str, update: &UpdateModuleNpc, ) -> QueryRes...` — Update a module NPC.
- pub `delete_module_npc` function L65-67 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<usize>` — Delete a module NPC by ID.
- pub `delete_all_module_npcs` function L70-72 — `(conn: &mut SqliteConnection, module_id: &str) -> QueryResult<usize>` — Delete all NPCs for a module.
- pub `module_npc_exists` function L75-80 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<bool>` — Check if a module NPC exists.
- pub `count_module_npcs` function L83-88 — `(conn: &mut SqliteConnection, module_id: &str) -> QueryResult<i64>` — Count NPCs for a module.
-  `tests` module L91-230 — `-` — Database operations for module NPCs (custom DM-created characters).
-  `setup_test_data` function L97-103 — `(conn: &mut SqliteConnection)` — Database operations for module NPCs (custom DM-created characters).
-  `test_insert_and_get_module_npc` function L106-116 — `()` — Database operations for module NPCs (custom DM-created characters).
-  `test_insert_with_details` function L119-134 — `()` — Database operations for module NPCs (custom DM-created characters).
-  `test_list_module_npcs` function L137-154 — `()` — Database operations for module NPCs (custom DM-created characters).
-  `test_list_by_role` function L157-178 — `()` — Database operations for module NPCs (custom DM-created characters).
-  `test_update_module_npc` function L181-193 — `()` — Database operations for module NPCs (custom DM-created characters).
-  `test_delete_module_npc` function L196-208 — `()` — Database operations for module NPCs (custom DM-created characters).
-  `test_count_module_npcs` function L211-229 — `()` — Database operations for module NPCs (custom DM-created characters).

#### crates/mimir-core/src/dal/campaign/token_placement.rs

- pub `insert_token_placement` function L11-20 — `( conn: &mut SqliteConnection, placement: &NewTokenPlacement, ) -> QueryResult<S...` — Insert a new token placement.
- pub `get_token_placement` function L23-25 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<TokenPlacement>` — Get a token placement by ID.
- pub `get_token_placement_optional` function L28-33 — `( conn: &mut SqliteConnection, id: &str, ) -> QueryResult<Option<TokenPlacement>...` — Get a token placement by ID, returning None if not found.
- pub `list_token_placements` function L36-43 — `( conn: &mut SqliteConnection, map_id: &str, ) -> QueryResult<Vec<TokenPlacement...` — List all token placements for a map.
- pub `list_visible_token_placements` function L46-54 — `( conn: &mut SqliteConnection, map_id: &str, ) -> QueryResult<Vec<TokenPlacement...` — List visible token placements for a map (for player view).
- pub `list_token_placements_for_monster` function L57-64 — `( conn: &mut SqliteConnection, module_monster_id: &str, ) -> QueryResult<Vec<Tok...` — List token placements for a specific monster.
- pub `list_token_placements_for_npc` function L67-74 — `( conn: &mut SqliteConnection, module_npc_id: &str, ) -> QueryResult<Vec<TokenPl...` — List token placements for a specific NPC.
- pub `update_token_placement` function L77-85 — `( conn: &mut SqliteConnection, id: &str, update: &UpdateTokenPlacement, ) -> Que...` — Update a token placement.
- pub `delete_token_placement` function L88-90 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<usize>` — Delete a token placement by ID.
- pub `delete_all_token_placements` function L93-99 — `( conn: &mut SqliteConnection, map_id: &str, ) -> QueryResult<usize>` — Delete all token placements for a map.
- pub `token_placement_exists` function L102-107 — `(conn: &mut SqliteConnection, id: &str) -> QueryResult<bool>` — Check if a token placement exists.
- pub `count_token_placements` function L110-115 — `(conn: &mut SqliteConnection, map_id: &str) -> QueryResult<i64>` — Count token placements for a map.
-  `tests` module L118-285 — `-` — Database operations for token placements on maps.
-  `setup_test_data` function L124-142 — `(conn: &mut SqliteConnection)` — Database operations for token placements on maps.
-  `test_insert_and_get_monster_placement` function L145-158 — `()` — Database operations for token placements on maps.
-  `test_insert_and_get_npc_placement` function L161-171 — `()` — Database operations for token placements on maps.
-  `test_insert_with_options` function L174-188 — `()` — Database operations for token placements on maps.
-  `test_list_token_placements` function L191-202 — `()` — Database operations for token placements on maps.
-  `test_list_visible_placements` function L205-217 — `()` — Database operations for token placements on maps.
-  `test_update_position` function L220-233 — `()` — Database operations for token placements on maps.
-  `test_update_hidden` function L236-248 — `()` — Database operations for token placements on maps.
-  `test_delete_token_placement` function L251-263 — `()` — Database operations for token placements on maps.
-  `test_count_token_placements` function L266-284 — `()` — Database operations for token placements on maps.

### crates/mimir-core/src/dal/catalog

> *Semantic summary to be generated by AI agent.*

#### crates/mimir-core/src/dal/catalog/action.rs

- pub `insert_action` function L11-18 — `(conn: &mut SqliteConnection, action: &NewAction) -> QueryResult<i32>` — Insert a new action.
- pub `insert_actions` function L21-28 — `( conn: &mut SqliteConnection, actions: &[NewAction], ) -> QueryResult<usize>` — Insert multiple actions in a batch.
- pub `get_action` function L31-35 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<Action>` — Get an action by its ID.
- pub `get_action_optional` function L38-43 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<Option<Action>>` — Get an action by its ID, returning None if not found.
- pub `get_action_by_name` function L49-60 — `( conn: &mut SqliteConnection, name: &str, source: &str, ) -> QueryResult<Option...` — Get an action by name and source (case-insensitive name matching).
- pub `list_actions` function L63-65 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<Action>>` — List all actions, ordered by name.
- pub `list_actions_by_source` function L68-76 — `( conn: &mut SqliteConnection, source: &str, ) -> QueryResult<Vec<Action>>` — List actions from a specific source.
- pub `delete_action` function L79-81 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<usize>` — Delete an action by its ID.
- pub `delete_actions_by_source` function L84-89 — `( conn: &mut SqliteConnection, source: &str, ) -> QueryResult<usize>` — Delete all actions from a specific source.
- pub `count_actions` function L92-94 — `(conn: &mut SqliteConnection) -> QueryResult<i64>` — Count all actions.
- pub `count_actions_by_source` function L97-102 — `(conn: &mut SqliteConnection, source: &str) -> QueryResult<i64>` — Count actions from a specific source.
- pub `list_action_sources` function L105-111 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<String>>` — List all distinct sources that have actions.
- pub `search_actions` function L114-133 — `(conn: &mut SqliteConnection, filter: &ActionFilter) -> QueryResult<Vec<Action>>` — Search actions with filters.
- pub `search_actions_paginated` function L136-164 — `( conn: &mut SqliteConnection, filter: &ActionFilter, limit: i64, offset: i64, )...` — Search actions with pagination.
-  `tests` module L167-214 — `-` — Database operations for actions.
-  `setup_test_data` function L173-176 — `(conn: &mut SqliteConnection)` — Database operations for actions.
-  `test_action_crud` function L179-196 — `()` — Database operations for actions.
-  `test_list_actions` function L199-213 — `()` — Database operations for actions.

#### crates/mimir-core/src/dal/catalog/background.rs

- pub `insert_background` function L11-18 — `(conn: &mut SqliteConnection, background: &NewBackground) -> QueryResult<i32>` — Insert a new background.
- pub `insert_backgrounds` function L21-28 — `( conn: &mut SqliteConnection, backgrounds: &[NewBackground], ) -> QueryResult<u...` — Insert multiple backgrounds in a batch.
- pub `get_background` function L31-35 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<Background>` — Get a background by its ID.
- pub `get_background_optional` function L38-46 — `( conn: &mut SqliteConnection, id: i32, ) -> QueryResult<Option<Background>>` — Get a background by its ID, returning None if not found.
- pub `get_background_by_name` function L52-63 — `( conn: &mut SqliteConnection, name: &str, source: &str, ) -> QueryResult<Option...` — Get a background by name and source (case-insensitive name matching).
- pub `list_backgrounds` function L66-68 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<Background>>` — List all backgrounds, ordered by name.
- pub `list_backgrounds_by_source` function L71-79 — `( conn: &mut SqliteConnection, source: &str, ) -> QueryResult<Vec<Background>>` — List backgrounds from a specific source.
- pub `delete_background` function L82-84 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<usize>` — Delete a background by its ID.
- pub `delete_backgrounds_by_source` function L87-92 — `( conn: &mut SqliteConnection, source: &str, ) -> QueryResult<usize>` — Delete all backgrounds from a specific source.
- pub `count_backgrounds` function L95-97 — `(conn: &mut SqliteConnection) -> QueryResult<i64>` — Count all backgrounds.
- pub `count_backgrounds_by_source` function L100-105 — `(conn: &mut SqliteConnection, source: &str) -> QueryResult<i64>` — Count backgrounds from a specific source.
- pub `list_background_sources` function L108-114 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<String>>` — List all distinct sources that have backgrounds.
- pub `search_backgrounds` function L117-139 — `( conn: &mut SqliteConnection, filter: &BackgroundFilter, ) -> QueryResult<Vec<B...` — Search backgrounds with filters.
- pub `search_backgrounds_paginated` function L142-170 — `( conn: &mut SqliteConnection, filter: &BackgroundFilter, limit: i64, offset: i6...` — Search backgrounds with pagination.
-  `tests` module L173-211 — `-` — Database operations for character backgrounds.
-  `test_background_crud` function L178-194 — `()` — Database operations for character backgrounds.
-  `test_list_backgrounds` function L197-210 — `()` — Database operations for character backgrounds.

#### crates/mimir-core/src/dal/catalog/book.rs

- pub `insert_book` function L13-23 — `(conn: &mut SqliteConnection, book: &NewBook) -> QueryResult<i32>` — Insert a new book.
- pub `upsert_book` function L28-49 — `(conn: &mut SqliteConnection, book: &NewBook) -> QueryResult<i32>` — Insert or replace a book (upsert).
- pub `get_book_by_source` function L52-57 — `(conn: &mut SqliteConnection, source: &str) -> QueryResult<Option<Book>>` — Get a book by its source code.
- pub `list_books` function L60-62 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<Book>>` — List all books.
- pub `list_books_by_sources` function L65-73 — `( conn: &mut SqliteConnection, sources: &[String], ) -> QueryResult<Vec<Book>>` — List books by source codes.
- pub `delete_book` function L76-78 — `(conn: &mut SqliteConnection, source: &str) -> QueryResult<usize>` — Delete a book by its source code.
- pub `book_exists` function L81-86 — `(conn: &mut SqliteConnection, source: &str) -> QueryResult<bool>` — Check if a book exists for a source.
- pub `count_books` function L89-91 — `(conn: &mut SqliteConnection) -> QueryResult<i64>` — Count all books.
-  `tests` module L94-186 — `-` — Database operations for book content.
-  `setup_test_data` function L100-103 — `(conn: &mut SqliteConnection)` — Database operations for book content.
-  `test_insert_and_get_book` function L106-120 — `()` — Database operations for book content.
-  `test_insert_book_with_contents` function L123-138 — `()` — Database operations for book content.
-  `test_list_books` function L141-157 — `()` — Database operations for book content.
-  `test_delete_book` function L160-172 — `()` — Database operations for book content.
-  `test_count_books` function L175-185 — `()` — Database operations for book content.

#### crates/mimir-core/src/dal/catalog/class.rs

- pub `insert_class` function L14-27 — `(conn: &mut SqliteConnection, class: &NewClass) -> QueryResult<i32>` — Insert a new class, ignoring duplicates.
- pub `insert_classes` function L30-34 — `(conn: &mut SqliteConnection, classes: &[NewClass]) -> QueryResult<usize>` — Insert multiple classes in a batch.
- pub `get_class` function L37-39 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<Class>` — Get a class by its ID.
- pub `get_class_optional` function L42-44 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<Option<Class>>` — Get a class by its ID, returning None if not found.
- pub `get_class_by_name` function L50-61 — `( conn: &mut SqliteConnection, name: &str, source: &str, ) -> QueryResult<Option...` — Get a class by name and source (case-insensitive name matching).
- pub `list_classes` function L64-66 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<Class>>` — List all classes, ordered by name.
- pub `list_classes_by_source` function L69-74 — `(conn: &mut SqliteConnection, source: &str) -> QueryResult<Vec<Class>>` — List classes from a specific source.
- pub `delete_class` function L77-79 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<usize>` — Delete a class by its ID.
- pub `delete_classes_by_source` function L82-84 — `(conn: &mut SqliteConnection, source: &str) -> QueryResult<usize>` — Delete all classes from a specific source.
- pub `count_classes` function L87-89 — `(conn: &mut SqliteConnection) -> QueryResult<i64>` — Count all classes.
- pub `count_classes_by_source` function L92-97 — `(conn: &mut SqliteConnection, source: &str) -> QueryResult<i64>` — Count classes from a specific source.
- pub `list_class_sources` function L100-106 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<String>>` — List all distinct sources that have classes.
- pub `search_classes` function L109-131 — `( conn: &mut SqliteConnection, filter: &ClassFilter, ) -> QueryResult<Vec<Class>...` — Search classes with filters.
- pub `search_classes_paginated` function L134-162 — `( conn: &mut SqliteConnection, filter: &ClassFilter, limit: i64, offset: i64, ) ...` — Search classes with pagination.
-  `tests` module L165-205 — `-` — Database operations for character classes.
-  `test_class_crud` function L170-188 — `()` — Database operations for character classes.
-  `test_list_classes` function L191-204 — `()` — Database operations for character classes.

#### crates/mimir-core/src/dal/catalog/class_feature.rs

- pub `insert_class_feature` function L11-24 — `(conn: &mut SqliteConnection, feature: &NewClassFeature) -> QueryResult<i32>` — Insert a new class feature, ignoring duplicates.
- pub `insert_class_features` function L27-31 — `(conn: &mut SqliteConnection, features: &[NewClassFeature]) -> QueryResult<usize...` — Insert multiple class features in a batch.
- pub `get_class_feature` function L34-36 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<ClassFeature>` — Get a class feature by its ID.
- pub `get_class_feature_by_name` function L42-58 — `( conn: &mut SqliteConnection, name: &str, source: &str, class_name: &str, class...` — Get a class feature by name, source, and class (case-insensitive name matching).
- pub `get_class_feature_by_name_and_class` function L61-73 — `( conn: &mut SqliteConnection, name: &str, class_name: &str, ) -> QueryResult<Op...` — Get a class feature by name and class (any source, case-insensitive name matching).
- pub `list_class_features_by_class` function L76-86 — `( conn: &mut SqliteConnection, class_name: &str, class_source: &str, ) -> QueryR...` — List all class features for a specific class.
- pub `delete_class_features_by_source` function L89-91 — `(conn: &mut SqliteConnection, source: &str) -> QueryResult<usize>` — Delete all class features from a specific source.
- pub `count_class_features` function L94-96 — `(conn: &mut SqliteConnection) -> QueryResult<i64>` — Count all class features.
- pub `count_class_features_by_source` function L99-104 — `(conn: &mut SqliteConnection, source: &str) -> QueryResult<i64>` — Count class features from a specific source.
- pub `list_class_feature_sources` function L107-113 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<String>>` — List all distinct sources that have class features.
- pub `search_class_features` function L116-150 — `( conn: &mut SqliteConnection, filter: &ClassFeatureFilter, ) -> QueryResult<Vec...` — Search class features with filters.

#### crates/mimir-core/src/dal/catalog/condition.rs

- pub `insert_condition` function L11-18 — `(conn: &mut SqliteConnection, condition: &NewCondition) -> QueryResult<i32>` — Insert a new condition.
- pub `insert_conditions` function L21-28 — `( conn: &mut SqliteConnection, conditions: &[NewCondition], ) -> QueryResult<usi...` — Insert multiple conditions in a batch.
- pub `get_condition` function L31-35 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<Condition>` — Get a condition by its ID.
- pub `get_condition_optional` function L38-43 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<Option<Condition>>` — Get a condition by its ID, returning None if not found.
- pub `get_condition_by_name` function L49-60 — `( conn: &mut SqliteConnection, name: &str, source: &str, ) -> QueryResult<Option...` — Get a condition by name and source (case-insensitive name matching).
- pub `list_conditions` function L63-65 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<Condition>>` — List all conditions, ordered by name.
- pub `list_conditions_by_source` function L68-76 — `( conn: &mut SqliteConnection, source: &str, ) -> QueryResult<Vec<Condition>>` — List conditions from a specific source.
- pub `delete_condition` function L79-81 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<usize>` — Delete a condition by its ID.
- pub `delete_conditions_by_source` function L84-89 — `( conn: &mut SqliteConnection, source: &str, ) -> QueryResult<usize>` — Delete all conditions from a specific source.
- pub `count_conditions` function L92-94 — `(conn: &mut SqliteConnection) -> QueryResult<i64>` — Count all conditions.
- pub `count_conditions_by_source` function L97-102 — `(conn: &mut SqliteConnection, source: &str) -> QueryResult<i64>` — Count conditions from a specific source.
- pub `list_condition_sources` function L105-111 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<String>>` — List all distinct sources that have conditions.
- pub `search_conditions` function L114-133 — `(conn: &mut SqliteConnection, filter: &ConditionFilter) -> QueryResult<Vec<Condi...` — Search conditions with filters.
- pub `search_conditions_paginated` function L136-164 — `( conn: &mut SqliteConnection, filter: &ConditionFilter, limit: i64, offset: i64...` — Search conditions with pagination.
-  `tests` module L167-205 — `-` — Database operations for conditions.
-  `test_condition_crud` function L172-188 — `()` — Database operations for conditions.
-  `test_list_conditions` function L191-204 — `()` — Database operations for conditions.

#### crates/mimir-core/src/dal/catalog/cult.rs

- pub `insert_cult` function L11-18 — `(conn: &mut SqliteConnection, cult: &NewCult) -> QueryResult<i32>` — Insert a new cult.
- pub `insert_cults` function L21-28 — `( conn: &mut SqliteConnection, cults: &[NewCult], ) -> QueryResult<usize>` — Insert multiple cults in a batch.
- pub `get_cult` function L31-35 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<Cult>` — Get a cult by its ID.
- pub `get_cult_by_name` function L38-48 — `( conn: &mut SqliteConnection, name: &str, source: &str, ) -> QueryResult<Option...` — Get a cult by name and source.
- pub `list_cults` function L51-53 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<Cult>>` — List all cults, ordered by name.
- pub `list_cults_by_source` function L56-64 — `( conn: &mut SqliteConnection, source: &str, ) -> QueryResult<Vec<Cult>>` — List cults from a specific source.
- pub `delete_cult` function L67-69 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<usize>` — Delete a cult by its ID.
- pub `delete_cults_by_source` function L72-77 — `( conn: &mut SqliteConnection, source: &str, ) -> QueryResult<usize>` — Delete all cults from a specific source.
- pub `count_cults` function L80-82 — `(conn: &mut SqliteConnection) -> QueryResult<i64>` — Count all cults.
- pub `count_cults_by_source` function L85-90 — `(conn: &mut SqliteConnection, source: &str) -> QueryResult<i64>` — Count cults from a specific source.
- pub `get_cult_optional` function L93-98 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<Option<Cult>>` — Get a cult by its ID, returning None if not found.
- pub `list_cult_sources` function L101-107 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<String>>` — List all distinct sources that have cults.
- pub `search_cults` function L110-130 — `( conn: &mut SqliteConnection, filter: &CultFilter, ) -> QueryResult<Vec<Cult>>` — Search cults with filters.
- pub `search_cults_paginated` function L133-159 — `( conn: &mut SqliteConnection, filter: &CultFilter, limit: i64, offset: i64, ) -...` — Search cults with pagination.
-  `tests` module L162-209 — `-` — Database operations for cults and supernatural gifts.
-  `setup_test_data` function L168-171 — `(conn: &mut SqliteConnection)` — Database operations for cults and supernatural gifts.
-  `test_cult_crud` function L174-191 — `()` — Database operations for cults and supernatural gifts.
-  `test_list_cults` function L194-208 — `()` — Database operations for cults and supernatural gifts.

#### crates/mimir-core/src/dal/catalog/deity.rs

- pub `insert_deity` function L14-27 — `(conn: &mut SqliteConnection, deity: &NewDeity) -> QueryResult<i32>` — Insert a new deity, ignoring duplicates.
- pub `insert_deities` function L30-37 — `( conn: &mut SqliteConnection, deities: &[NewDeity], ) -> QueryResult<usize>` — Insert multiple deities in a batch.
- pub `get_deity` function L40-44 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<Deity>` — Get a deity by its ID.
- pub `get_deity_by_name` function L47-57 — `( conn: &mut SqliteConnection, name: &str, source: &str, ) -> QueryResult<Option...` — Get a deity by name and source.
- pub `list_deities` function L60-62 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<Deity>>` — List all deities, ordered by name.
- pub `list_deities_by_source` function L65-73 — `( conn: &mut SqliteConnection, source: &str, ) -> QueryResult<Vec<Deity>>` — List deities from a specific source.
- pub `list_deities_by_pantheon` function L76-84 — `( conn: &mut SqliteConnection, pantheon: &str, ) -> QueryResult<Vec<Deity>>` — List deities by pantheon.
- pub `delete_deity` function L87-89 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<usize>` — Delete a deity by its ID.
- pub `delete_deities_by_source` function L92-97 — `( conn: &mut SqliteConnection, source: &str, ) -> QueryResult<usize>` — Delete all deities from a specific source.
- pub `count_deities` function L100-102 — `(conn: &mut SqliteConnection) -> QueryResult<i64>` — Count all deities.
- pub `count_deities_by_source` function L105-110 — `(conn: &mut SqliteConnection, source: &str) -> QueryResult<i64>` — Count deities from a specific source.
- pub `get_deity_optional` function L113-118 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<Option<Deity>>` — Get a deity by its ID, returning None if not found.
- pub `list_deity_sources` function L121-127 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<String>>` — List all distinct sources that have deities.
- pub `search_deities` function L130-151 — `(conn: &mut SqliteConnection, filter: &DeityFilter) -> QueryResult<Vec<Deity>>` — Search deities with filters.
- pub `search_deities_paginated` function L154-184 — `( conn: &mut SqliteConnection, filter: &DeityFilter, limit: i64, offset: i64, ) ...` — Search deities with pagination.
-  `tests` module L187-239 — `-` — Database operations for deities.
-  `setup_test_data` function L193-196 — `(conn: &mut SqliteConnection)` — Database operations for deities.
-  `test_deity_crud` function L199-218 — `()` — Database operations for deities.
-  `test_list_deities` function L221-238 — `()` — Database operations for deities.

#### crates/mimir-core/src/dal/catalog/disease.rs

- pub `insert_disease` function L11-18 — `(conn: &mut SqliteConnection, disease: &NewDisease) -> QueryResult<i32>` — Insert a new disease.
- pub `insert_diseases` function L21-28 — `( conn: &mut SqliteConnection, diseases: &[NewDisease], ) -> QueryResult<usize>` — Insert multiple diseases in a batch.
- pub `get_disease` function L31-35 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<Disease>` — Get a disease by its ID.
- pub `get_disease_by_name` function L38-48 — `( conn: &mut SqliteConnection, name: &str, source: &str, ) -> QueryResult<Option...` — Get a disease by name and source.
- pub `list_diseases` function L51-53 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<Disease>>` — List all diseases, ordered by name.
- pub `list_diseases_by_source` function L56-64 — `( conn: &mut SqliteConnection, source: &str, ) -> QueryResult<Vec<Disease>>` — List diseases from a specific source.
- pub `delete_disease` function L67-69 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<usize>` — Delete a disease by its ID.
- pub `delete_diseases_by_source` function L72-77 — `( conn: &mut SqliteConnection, source: &str, ) -> QueryResult<usize>` — Delete all diseases from a specific source.
- pub `count_diseases` function L80-82 — `(conn: &mut SqliteConnection) -> QueryResult<i64>` — Count all diseases.
-  `tests` module L85-123 — `-` — Database operations for diseases.
-  `test_disease_crud` function L90-106 — `()` — Database operations for diseases.
-  `test_list_diseases` function L109-122 — `()` — Database operations for diseases.

#### crates/mimir-core/src/dal/catalog/feat.rs

- pub `insert_feat` function L11-18 — `(conn: &mut SqliteConnection, feat: &NewFeat) -> QueryResult<i32>` — Insert a new feat.
- pub `insert_feats` function L21-25 — `(conn: &mut SqliteConnection, feats: &[NewFeat]) -> QueryResult<usize>` — Insert multiple feats in a batch.
- pub `get_feat` function L28-30 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<Feat>` — Get a feat by its ID.
- pub `get_feat_optional` function L33-35 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<Option<Feat>>` — Get a feat by its ID, returning None if not found.
- pub `get_feat_by_name` function L41-52 — `( conn: &mut SqliteConnection, name: &str, source: &str, ) -> QueryResult<Option...` — Get a feat by name and source (case-insensitive name matching).
- pub `list_feats` function L55-57 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<Feat>>` — List all feats, ordered by name.
- pub `list_feats_by_source` function L60-65 — `(conn: &mut SqliteConnection, source: &str) -> QueryResult<Vec<Feat>>` — List feats from a specific source.
- pub `search_feats_by_name` function L68-74 — `(conn: &mut SqliteConnection, pattern: &str) -> QueryResult<Vec<Feat>>` — Search feats by name pattern.
- pub `delete_feat` function L77-79 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<usize>` — Delete a feat by its ID.
- pub `delete_feats_by_source` function L82-84 — `(conn: &mut SqliteConnection, source: &str) -> QueryResult<usize>` — Delete all feats from a specific source.
- pub `count_feats` function L87-89 — `(conn: &mut SqliteConnection) -> QueryResult<i64>` — Count all feats.
- pub `count_feats_by_source` function L92-97 — `(conn: &mut SqliteConnection, source: &str) -> QueryResult<i64>` — Count feats from a specific source.
- pub `list_feat_sources` function L100-106 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<String>>` — List all distinct sources that have feats.
- pub `search_feats` function L109-128 — `(conn: &mut SqliteConnection, filter: &FeatFilter) -> QueryResult<Vec<Feat>>` — Search feats with filters.
- pub `search_feats_paginated` function L131-159 — `( conn: &mut SqliteConnection, filter: &FeatFilter, limit: i64, offset: i64, ) -...` — Search feats with pagination.
-  `tests` module L162-200 — `-` — Database operations for character feats.
-  `test_feat_crud` function L167-183 — `()` — Database operations for character feats.
-  `test_search_feats` function L186-199 — `()` — Database operations for character feats.

#### crates/mimir-core/src/dal/catalog/hazard.rs

- pub `insert_hazard` function L11-18 — `(conn: &mut SqliteConnection, hazard: &NewHazard) -> QueryResult<i32>` — Insert a new hazard.
- pub `insert_hazards` function L21-28 — `( conn: &mut SqliteConnection, hazards: &[NewHazard], ) -> QueryResult<usize>` — Insert multiple hazards in a batch.
- pub `get_hazard` function L31-35 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<Hazard>` — Get a hazard by its ID.
- pub `get_hazard_optional` function L38-43 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<Option<Hazard>>` — Get a hazard by its ID, returning None if not found.
- pub `get_hazard_by_name` function L49-60 — `( conn: &mut SqliteConnection, name: &str, source: &str, ) -> QueryResult<Option...` — Get a hazard by name and source (case-insensitive name matching).
- pub `list_hazards` function L63-65 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<Hazard>>` — List all hazards, ordered by name.
- pub `list_hazards_by_source` function L68-76 — `( conn: &mut SqliteConnection, source: &str, ) -> QueryResult<Vec<Hazard>>` — List hazards from a specific source.
- pub `delete_hazard` function L79-81 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<usize>` — Delete a hazard by its ID.
- pub `delete_hazards_by_source` function L84-89 — `( conn: &mut SqliteConnection, source: &str, ) -> QueryResult<usize>` — Delete all hazards from a specific source.
- pub `count_hazards` function L92-94 — `(conn: &mut SqliteConnection) -> QueryResult<i64>` — Count all hazards.
- pub `count_hazards_by_source` function L97-102 — `(conn: &mut SqliteConnection, source: &str) -> QueryResult<i64>` — Count hazards from a specific source.
- pub `list_hazard_sources` function L105-111 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<String>>` — List all distinct sources that have hazards.
- pub `search_hazards` function L114-133 — `(conn: &mut SqliteConnection, filter: &HazardFilter) -> QueryResult<Vec<Hazard>>` — Search hazards with filters.
- pub `search_hazards_paginated` function L136-164 — `( conn: &mut SqliteConnection, filter: &HazardFilter, limit: i64, offset: i64, )...` — Search hazards with pagination.
-  `tests` module L167-205 — `-` — Database operations for environmental hazards.
-  `test_hazard_crud` function L172-188 — `()` — Database operations for environmental hazards.
-  `test_list_hazards` function L191-204 — `()` — Database operations for environmental hazards.

#### crates/mimir-core/src/dal/catalog/item.rs

- pub `insert_item` function L13-21 — `(conn: &mut SqliteConnection, item: &NewItem) -> QueryResult<i32>` — Insert a new item.
- pub `insert_items` function L24-28 — `(conn: &mut SqliteConnection, items: &[NewItem]) -> QueryResult<usize>` — Insert multiple items in a batch.
- pub `get_item` function L31-35 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<Item>` — Get an item by its ID.
- pub `get_item_optional` function L38-43 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<Option<Item>>` — Get an item by its ID, returning None if not found.
- pub `get_item_by_name` function L49-60 — `( conn: &mut SqliteConnection, name: &str, source: &str, ) -> QueryResult<Option...` — Get an item by name and source (case-insensitive name matching).
- pub `list_items` function L63-65 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<Item>>` — List all items, ordered by name.
- pub `list_items_by_source` function L68-76 — `( conn: &mut SqliteConnection, source: &str, ) -> QueryResult<Vec<Item>>` — List items from a specific source.
- pub `search_items` function L79-109 — `( conn: &mut SqliteConnection, filter: &ItemFilter, ) -> QueryResult<Vec<Item>>` — Search items with filters.
- pub `search_items_paginated` function L112-148 — `( conn: &mut SqliteConnection, filter: &ItemFilter, limit: i64, offset: i64, ) -...` — Search items with pagination.
- pub `list_weapon_names` function L151-158 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<String>>` — List all weapon names (item_type 'M' or 'R'), lowercased and deduplicated.
- pub `delete_item` function L161-163 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<usize>` — Delete an item by its ID.
- pub `delete_items_by_source` function L166-168 — `(conn: &mut SqliteConnection, source: &str) -> QueryResult<usize>` — Delete all items from a specific source.
- pub `count_items` function L171-173 — `(conn: &mut SqliteConnection) -> QueryResult<i64>` — Count all items.
- pub `count_items_by_source` function L176-181 — `(conn: &mut SqliteConnection, source: &str) -> QueryResult<i64>` — Count items from a specific source.
- pub `list_item_sources` function L184-190 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<String>>` — List all distinct sources that have items.
-  `tests` module L193-352 — `-` — Database operations for items.
-  `test_insert_and_get_item` function L198-214 — `()` — Database operations for items.
-  `test_get_item_by_name` function L217-231 — `()` — Database operations for items.
-  `test_list_items_by_source` function L234-245 — `()` — Database operations for items.
-  `test_search_items` function L248-281 — `()` — Database operations for items.
-  `test_search_items_paginated` function L284-301 — `()` — Database operations for items.
-  `test_delete_item` function L304-315 — `()` — Database operations for items.
-  `test_delete_items_by_source` function L318-332 — `()` — Database operations for items.
-  `test_count_items` function L335-351 — `()` — Database operations for items.

#### crates/mimir-core/src/dal/catalog/item_attunement.rs

- pub `insert_item_attunement_class` function L11-21 — `( conn: &mut SqliteConnection, attunement: &NewItemAttunementClass, ) -> QueryRe...` — Insert an item attunement class requirement.
- pub `insert_item_attunement_classes` function L24-31 — `( conn: &mut SqliteConnection, attunements: &[NewItemAttunementClass], ) -> Quer...` — Insert multiple item attunement class requirements in a batch.
- pub `get_item_attunement_classes` function L34-42 — `( conn: &mut SqliteConnection, item_id: i32, ) -> QueryResult<Vec<ItemAttunement...` — Get all class requirements for an item.
- pub `get_items_attuneable_by_class` function L45-52 — `( conn: &mut SqliteConnection, class_name: &str, ) -> QueryResult<Vec<ItemAttune...` — Get all items that can be attuned by a specific class.
- pub `get_attunement_class_names_for_item` function L55-64 — `( conn: &mut SqliteConnection, item_id: i32, ) -> QueryResult<Vec<String>>` — Get the class names that can attune to a specific item.
- pub `item_has_class_attunement` function L67-73 — `(conn: &mut SqliteConnection, item_id: i32) -> QueryResult<bool>` — Check if an item has class-specific attunement requirements.
- pub `delete_item_attunement_classes` function L76-84 — `( conn: &mut SqliteConnection, item_id: i32, ) -> QueryResult<usize>` — Delete all class requirements for an item.
- pub `count_item_attunement_classes` function L87-89 — `(conn: &mut SqliteConnection) -> QueryResult<i64>` — Count item attunement class associations.
-  `tests` module L92-194 — `-` — Database operations for class-specific item attunement requirements.
-  `setup_test_data` function L98-101 — `(conn: &mut SqliteConnection)` — Database operations for class-specific item attunement requirements.
-  `insert_test_item` function L103-107 — `(conn: &mut SqliteConnection, name: &str) -> i32` — Database operations for class-specific item attunement requirements.
-  `test_item_attunement_crud` function L110-140 — `()` — Database operations for class-specific item attunement requirements.
-  `test_multiple_class_attunement` function L143-158 — `()` — Database operations for class-specific item attunement requirements.
-  `test_get_items_by_class` function L161-193 — `()` — Database operations for class-specific item attunement requirements.

#### crates/mimir-core/src/dal/catalog/language.rs

- pub `insert_language` function L11-18 — `(conn: &mut SqliteConnection, language: &NewLanguage) -> QueryResult<i32>` — Insert a new language.
- pub `insert_languages` function L21-28 — `( conn: &mut SqliteConnection, languages: &[NewLanguage], ) -> QueryResult<usize...` — Insert multiple languages in a batch.
- pub `get_language` function L31-35 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<Language>` — Get a language by its ID.
- pub `get_language_optional` function L38-43 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<Option<Language>>` — Get a language by its ID, returning None if not found.
- pub `get_language_by_name` function L49-60 — `( conn: &mut SqliteConnection, name: &str, source: &str, ) -> QueryResult<Option...` — Get a language by name and source (case-insensitive name matching).
- pub `list_languages` function L63-65 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<Language>>` — List all languages, ordered by name.
- pub `list_languages_by_source` function L68-76 — `( conn: &mut SqliteConnection, source: &str, ) -> QueryResult<Vec<Language>>` — List languages from a specific source.
- pub `list_languages_by_type` function L79-87 — `( conn: &mut SqliteConnection, language_type: &str, ) -> QueryResult<Vec<Languag...` — List languages by type (e.g., "Standard", "Exotic").
- pub `delete_language` function L90-92 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<usize>` — Delete a language by its ID.
- pub `delete_languages_by_source` function L95-100 — `( conn: &mut SqliteConnection, source: &str, ) -> QueryResult<usize>` — Delete all languages from a specific source.
- pub `count_languages` function L103-105 — `(conn: &mut SqliteConnection) -> QueryResult<i64>` — Count all languages.
- pub `count_languages_by_source` function L108-113 — `(conn: &mut SqliteConnection, source: &str) -> QueryResult<i64>` — Count languages from a specific source.
- pub `list_language_sources` function L116-122 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<String>>` — List all distinct sources that have languages.
- pub `search_languages` function L125-148 — `(conn: &mut SqliteConnection, filter: &LanguageFilter) -> QueryResult<Vec<Langua...` — Search languages with filters.
- pub `search_languages_paginated` function L151-183 — `( conn: &mut SqliteConnection, filter: &LanguageFilter, limit: i64, offset: i64,...` — Search languages with pagination.
-  `tests` module L186-229 — `-` — Database operations for languages.
-  `test_language_crud` function L191-209 — `()` — Database operations for languages.
-  `test_list_languages` function L212-228 — `()` — Database operations for languages.

#### crates/mimir-core/src/dal/catalog/mod.rs

-  `action` module L7 — `-` — Database operations for catalog entities.
-  `background` module L8 — `-` — Database operations for catalog entities.
-  `book` module L9 — `-` — Database operations for catalog entities.
-  `class` module L10 — `-` — Database operations for catalog entities.
-  `class_feature` module L11 — `-` — Database operations for catalog entities.
-  `condition` module L12 — `-` — Database operations for catalog entities.
-  `cult` module L13 — `-` — Database operations for catalog entities.
-  `deity` module L14 — `-` — Database operations for catalog entities.
-  `disease` module L15 — `-` — Database operations for catalog entities.
-  `feat` module L16 — `-` — Database operations for catalog entities.
-  `hazard` module L17 — `-` — Database operations for catalog entities.
-  `item` module L18 — `-` — Database operations for catalog entities.
-  `item_attunement` module L19 — `-` — Database operations for catalog entities.
-  `language` module L20 — `-` — Database operations for catalog entities.
-  `monster` module L21 — `-` — Database operations for catalog entities.
-  `object` module L22 — `-` — Database operations for catalog entities.
-  `optional_feature` module L23 — `-` — Database operations for catalog entities.
-  `psionic` module L24 — `-` — Database operations for catalog entities.
-  `race` module L25 — `-` — Database operations for catalog entities.
-  `reward` module L26 — `-` — Database operations for catalog entities.
-  `sense` module L27 — `-` — Database operations for catalog entities.
-  `skill` module L28 — `-` — Database operations for catalog entities.
-  `source` module L29 — `-` — Database operations for catalog entities.
-  `spell` module L30 — `-` — Database operations for catalog entities.
-  `spell_list` module L31 — `-` — Database operations for catalog entities.
-  `subclass` module L32 — `-` — Database operations for catalog entities.
-  `subclass_feature` module L33 — `-` — Database operations for catalog entities.
-  `table` module L34 — `-` — Database operations for catalog entities.
-  `trap` module L35 — `-` — Database operations for catalog entities.
-  `variant_rule` module L36 — `-` — Database operations for catalog entities.
-  `vehicle` module L37 — `-` — Database operations for catalog entities.

#### crates/mimir-core/src/dal/catalog/monster.rs

- pub `insert_monster` function L13-21 — `(conn: &mut SqliteConnection, monster: &NewMonster) -> QueryResult<i32>` — Insert a new monster.
- pub `insert_monsters` function L24-28 — `(conn: &mut SqliteConnection, monsters: &[NewMonster]) -> QueryResult<usize>` — Insert multiple monsters in a batch.
- pub `get_monster` function L31-35 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<Monster>` — Get a monster by its ID.
- pub `get_monster_optional` function L38-43 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<Option<Monster>>` — Get a monster by its ID, returning None if not found.
- pub `get_monster_by_name` function L49-60 — `( conn: &mut SqliteConnection, name: &str, source: &str, ) -> QueryResult<Option...` — Get a monster by name and source (case-insensitive name matching).
- pub `list_monsters` function L63-65 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<Monster>>` — List all monsters, ordered by name.
- pub `list_monsters_by_source` function L68-76 — `( conn: &mut SqliteConnection, source: &str, ) -> QueryResult<Vec<Monster>>` — List monsters from a specific source.
- pub `search_monsters` function L79-113 — `( conn: &mut SqliteConnection, filter: &MonsterFilter, ) -> QueryResult<Vec<Mons...` — Search monsters with filters.
- pub `search_monsters_paginated` function L116-156 — `( conn: &mut SqliteConnection, filter: &MonsterFilter, limit: i64, offset: i64, ...` — Search monsters with pagination.
- pub `delete_monster` function L159-161 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<usize>` — Delete a monster by its ID.
- pub `delete_monsters_by_source` function L164-166 — `(conn: &mut SqliteConnection, source: &str) -> QueryResult<usize>` — Delete all monsters from a specific source.
- pub `count_monsters` function L169-171 — `(conn: &mut SqliteConnection) -> QueryResult<i64>` — Count all monsters.
- pub `count_monsters_by_source` function L174-179 — `(conn: &mut SqliteConnection, source: &str) -> QueryResult<i64>` — Count monsters from a specific source.
- pub `list_monster_sources` function L182-188 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<String>>` — List all distinct sources that have monsters.
- pub `set_token_image_path` function L191-199 — `( conn: &mut SqliteConnection, id: i32, path: Option<&str>, ) -> QueryResult<usi...` — Update a monster's token image path.
-  `tests` module L202-381 — `-` — Database operations for monsters.
-  `test_insert_and_get_monster` function L207-225 — `()` — Database operations for monsters.
-  `test_get_monster_by_name` function L228-242 — `()` — Database operations for monsters.
-  `test_list_monsters_by_source` function L245-256 — `()` — Database operations for monsters.
-  `test_search_monsters` function L259-294 — `()` — Database operations for monsters.
-  `test_search_monsters_paginated` function L297-314 — `()` — Database operations for monsters.
-  `test_delete_monster` function L317-328 — `()` — Database operations for monsters.
-  `test_delete_monsters_by_source` function L331-345 — `()` — Database operations for monsters.
-  `test_set_token_image_path` function L348-361 — `()` — Database operations for monsters.
-  `test_count_monsters` function L364-380 — `()` — Database operations for monsters.

#### crates/mimir-core/src/dal/catalog/object.rs

- pub `insert_object` function L11-18 — `(conn: &mut SqliteConnection, object: &NewObject) -> QueryResult<i32>` — Insert a new object.
- pub `insert_objects` function L21-28 — `( conn: &mut SqliteConnection, objects: &[NewObject], ) -> QueryResult<usize>` — Insert multiple objects in a batch.
- pub `get_object` function L31-35 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<Object>` — Get an object by its ID.
- pub `get_object_by_name` function L38-48 — `( conn: &mut SqliteConnection, name: &str, source: &str, ) -> QueryResult<Option...` — Get an object by name and source.
- pub `list_objects` function L51-53 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<Object>>` — List all objects, ordered by name.
- pub `list_objects_by_source` function L56-64 — `( conn: &mut SqliteConnection, source: &str, ) -> QueryResult<Vec<Object>>` — List objects from a specific source.
- pub `list_objects_by_type` function L67-75 — `( conn: &mut SqliteConnection, object_type: &str, ) -> QueryResult<Vec<Object>>` — List objects by type.
- pub `delete_object` function L78-80 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<usize>` — Delete an object by its ID.
- pub `delete_objects_by_source` function L83-88 — `( conn: &mut SqliteConnection, source: &str, ) -> QueryResult<usize>` — Delete all objects from a specific source.
- pub `count_objects` function L91-93 — `(conn: &mut SqliteConnection) -> QueryResult<i64>` — Count all objects.
- pub `count_objects_by_source` function L96-101 — `(conn: &mut SqliteConnection, source: &str) -> QueryResult<i64>` — Count objects from a specific source.
- pub `get_object_optional` function L104-109 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<Option<Object>>` — Get an object by its ID, returning None if not found.
- pub `list_object_sources` function L112-118 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<String>>` — List all distinct sources that have objects.
- pub `search_objects` function L121-145 — `( conn: &mut SqliteConnection, filter: &ObjectFilter, ) -> QueryResult<Vec<Objec...` — Search objects with filters.
- pub `search_objects_paginated` function L148-178 — `( conn: &mut SqliteConnection, filter: &ObjectFilter, limit: i64, offset: i64, )...` — Search objects with pagination.
-  `tests` module L181-224 — `-` — Database operations for objects.
-  `test_object_crud` function L186-204 — `()` — Database operations for objects.
-  `test_list_objects` function L207-223 — `()` — Database operations for objects.

#### crates/mimir-core/src/dal/catalog/optional_feature.rs

- pub `insert_optional_feature` function L11-21 — `( conn: &mut SqliteConnection, feature: &NewOptionalFeature, ) -> QueryResult<i3...` — Insert a new optional feature.
- pub `insert_optional_features` function L24-31 — `( conn: &mut SqliteConnection, features: &[NewOptionalFeature], ) -> QueryResult...` — Insert multiple optional features in a batch.
- pub `get_optional_feature` function L34-38 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<OptionalFeature>` — Get an optional feature by its ID.
- pub `get_optional_feature_by_name` function L41-51 — `( conn: &mut SqliteConnection, name: &str, source: &str, ) -> QueryResult<Option...` — Get an optional feature by name and source.
- pub `list_optional_features` function L54-58 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<OptionalFeature>>` — List all optional features, ordered by name.
- pub `list_optional_features_by_source` function L61-69 — `( conn: &mut SqliteConnection, source: &str, ) -> QueryResult<Vec<OptionalFeatur...` — List optional features from a specific source.
- pub `list_optional_features_by_type` function L72-80 — `( conn: &mut SqliteConnection, feature_type: &str, ) -> QueryResult<Vec<Optional...` — List optional features by type (e.g., "EI" for Eldritch Invocations).
- pub `delete_optional_feature` function L83-85 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<usize>` — Delete an optional feature by its ID.
- pub `delete_optional_features_by_source` function L88-94 — `( conn: &mut SqliteConnection, source: &str, ) -> QueryResult<usize>` — Delete all optional features from a specific source.
- pub `count_optional_features` function L97-99 — `(conn: &mut SqliteConnection) -> QueryResult<i64>` — Count all optional features.
- pub `count_optional_features_by_source` function L102-107 — `(conn: &mut SqliteConnection, source: &str) -> QueryResult<i64>` — Count optional features from a specific source.
- pub `get_optional_feature_optional` function L110-115 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<Option<OptionalFeature>>` — Get an optional feature by its ID, returning None if not found.
- pub `list_optional_feature_sources` function L118-124 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<String>>` — List all distinct sources that have optional features.
- pub `search_optional_features` function L127-151 — `( conn: &mut SqliteConnection, filter: &OptionalFeatureFilter, ) -> QueryResult<...` — Search optional features with filters.
- pub `search_optional_features_paginated` function L154-184 — `( conn: &mut SqliteConnection, filter: &OptionalFeatureFilter, limit: i64, offse...` — Search optional features with pagination.
-  `tests` module L187-233 — `-` — Database operations for optional features.
-  `setup_test_data` function L193-196 — `(conn: &mut SqliteConnection)` — Database operations for optional features.
-  `test_optional_feature_crud` function L199-213 — `()` — Database operations for optional features.
-  `test_list_by_type` function L216-232 — `()` — Database operations for optional features.

#### crates/mimir-core/src/dal/catalog/psionic.rs

- pub `insert_psionic` function L11-18 — `(conn: &mut SqliteConnection, psionic: &NewPsionic) -> QueryResult<i32>` — Insert a new psionic.
- pub `insert_psionics` function L21-25 — `(conn: &mut SqliteConnection, psionics: &[NewPsionic]) -> QueryResult<usize>` — Insert multiple psionics in a batch.
- pub `get_psionic` function L28-30 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<Psionic>` — Get a psionic by its ID.
- pub `get_psionic_by_name` function L33-43 — `( conn: &mut SqliteConnection, name: &str, source: &str, ) -> QueryResult<Option...` — Get a psionic by name and source.
- pub `list_psionics` function L46-48 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<Psionic>>` — List all psionics, ordered by name.
- pub `list_psionics_by_source` function L51-59 — `( conn: &mut SqliteConnection, source: &str, ) -> QueryResult<Vec<Psionic>>` — List psionics from a specific source.
- pub `list_psionics_by_type` function L62-70 — `( conn: &mut SqliteConnection, psionic_type: &str, ) -> QueryResult<Vec<Psionic>...` — List psionics by type (e.g., "D" for Discipline, "T" for Talent).
- pub `list_psionics_by_order` function L73-81 — `( conn: &mut SqliteConnection, order: &str, ) -> QueryResult<Vec<Psionic>>` — List psionics by order (e.g., "Avatar", "Wu Jen").
- pub `delete_psionic` function L84-86 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<usize>` — Delete a psionic by its ID.
- pub `delete_psionics_by_source` function L89-94 — `( conn: &mut SqliteConnection, source: &str, ) -> QueryResult<usize>` — Delete all psionics from a specific source.
- pub `count_psionics` function L97-99 — `(conn: &mut SqliteConnection) -> QueryResult<i64>` — Count all psionics.
- pub `count_psionics_by_source` function L102-107 — `(conn: &mut SqliteConnection, source: &str) -> QueryResult<i64>` — Count psionics from a specific source.
- pub `get_psionic_optional` function L110-115 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<Option<Psionic>>` — Get a psionic by its ID, returning None if not found.
- pub `list_psionic_sources` function L118-124 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<String>>` — List all distinct sources that have psionics.
- pub `search_psionics` function L127-155 — `( conn: &mut SqliteConnection, filter: &PsionicFilter, ) -> QueryResult<Vec<Psio...` — Search psionics with filters.
- pub `search_psionics_paginated` function L158-192 — `( conn: &mut SqliteConnection, filter: &PsionicFilter, limit: i64, offset: i64, ...` — Search psionics with pagination.
-  `tests` module L195-250 — `-` — Database operations for psionics.
-  `setup_test_data` function L201-204 — `(conn: &mut SqliteConnection)` — Database operations for psionics.
-  `test_psionic_crud` function L207-223 — `()` — Database operations for psionics.
-  `test_list_by_type_and_order` function L226-249 — `()` — Database operations for psionics.

#### crates/mimir-core/src/dal/catalog/race.rs

- pub `insert_race` function L14-27 — `(conn: &mut SqliteConnection, race: &NewRace) -> QueryResult<i32>` — Insert a new race, ignoring duplicates.
- pub `insert_races` function L30-34 — `(conn: &mut SqliteConnection, races: &[NewRace]) -> QueryResult<usize>` — Insert multiple races in a batch.
- pub `get_race` function L37-39 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<Race>` — Get a race by its ID.
- pub `get_race_optional` function L42-44 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<Option<Race>>` — Get a race by its ID, returning None if not found.
- pub `get_race_by_name` function L50-61 — `( conn: &mut SqliteConnection, name: &str, source: &str, ) -> QueryResult<Option...` — Get a race by name and source (case-insensitive name matching).
- pub `list_races` function L64-66 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<Race>>` — List all races, ordered by name.
- pub `list_races_by_source` function L69-74 — `(conn: &mut SqliteConnection, source: &str) -> QueryResult<Vec<Race>>` — List races from a specific source.
- pub `search_races_by_name` function L77-83 — `(conn: &mut SqliteConnection, pattern: &str) -> QueryResult<Vec<Race>>` — Search races by name pattern.
- pub `delete_race` function L86-88 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<usize>` — Delete a race by its ID.
- pub `delete_races_by_source` function L91-93 — `(conn: &mut SqliteConnection, source: &str) -> QueryResult<usize>` — Delete all races from a specific source.
- pub `count_races` function L96-98 — `(conn: &mut SqliteConnection) -> QueryResult<i64>` — Count all races.
- pub `count_races_by_source` function L101-106 — `(conn: &mut SqliteConnection, source: &str) -> QueryResult<i64>` — Count races from a specific source.
- pub `list_race_sources` function L109-115 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<String>>` — List all distinct sources that have races.
- pub `search_races` function L118-137 — `(conn: &mut SqliteConnection, filter: &RaceFilter) -> QueryResult<Vec<Race>>` — Search races with filters.
- pub `search_races_paginated` function L140-168 — `( conn: &mut SqliteConnection, filter: &RaceFilter, limit: i64, offset: i64, ) -...` — Search races with pagination.
-  `tests` module L171-208 — `-` — Database operations for character races.
-  `test_race_crud` function L176-192 — `()` — Database operations for character races.
-  `test_search_races` function L195-207 — `()` — Database operations for character races.

#### crates/mimir-core/src/dal/catalog/reward.rs

- pub `insert_reward` function L11-18 — `(conn: &mut SqliteConnection, reward: &NewReward) -> QueryResult<i32>` — Insert a new reward.
- pub `insert_rewards` function L21-25 — `(conn: &mut SqliteConnection, rewards: &[NewReward]) -> QueryResult<usize>` — Insert multiple rewards in a batch.
- pub `get_reward` function L28-30 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<Reward>` — Get a reward by its ID.
- pub `get_reward_by_name` function L33-43 — `( conn: &mut SqliteConnection, name: &str, source: &str, ) -> QueryResult<Option...` — Get a reward by name and source.
- pub `list_rewards` function L46-48 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<Reward>>` — List all rewards, ordered by name.
- pub `list_rewards_by_source` function L51-59 — `( conn: &mut SqliteConnection, source: &str, ) -> QueryResult<Vec<Reward>>` — List rewards from a specific source.
- pub `list_rewards_by_type` function L62-70 — `( conn: &mut SqliteConnection, reward_type: &str, ) -> QueryResult<Vec<Reward>>` — List rewards by type (e.g., "blessing", "boon", "charm").
- pub `delete_reward` function L73-75 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<usize>` — Delete a reward by its ID.
- pub `delete_rewards_by_source` function L78-80 — `(conn: &mut SqliteConnection, source: &str) -> QueryResult<usize>` — Delete all rewards from a specific source.
- pub `count_rewards` function L83-85 — `(conn: &mut SqliteConnection) -> QueryResult<i64>` — Count all rewards.
- pub `count_rewards_by_source` function L88-93 — `(conn: &mut SqliteConnection, source: &str) -> QueryResult<i64>` — Count rewards from a specific source.
- pub `get_reward_optional` function L96-101 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<Option<Reward>>` — Get a reward by its ID, returning None if not found.
- pub `list_reward_sources` function L104-110 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<String>>` — List all distinct sources that have rewards.
- pub `search_rewards` function L113-137 — `( conn: &mut SqliteConnection, filter: &RewardFilter, ) -> QueryResult<Vec<Rewar...` — Search rewards with filters.
- pub `search_rewards_paginated` function L140-170 — `( conn: &mut SqliteConnection, filter: &RewardFilter, limit: i64, offset: i64, )...` — Search rewards with pagination.
-  `tests` module L173-219 — `-` — Database operations for rewards.
-  `setup_test_data` function L179-182 — `(conn: &mut SqliteConnection)` — Database operations for rewards.
-  `test_reward_crud` function L185-199 — `()` — Database operations for rewards.
-  `test_list_by_type` function L202-218 — `()` — Database operations for rewards.

#### crates/mimir-core/src/dal/catalog/sense.rs

- pub `insert_sense` function L11-18 — `(conn: &mut SqliteConnection, sense: &NewSense) -> QueryResult<i32>` — Insert a new sense.
- pub `insert_senses` function L21-28 — `( conn: &mut SqliteConnection, senses: &[NewSense], ) -> QueryResult<usize>` — Insert multiple senses in a batch.
- pub `get_sense` function L31-35 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<Sense>` — Get a sense by its ID.
- pub `get_sense_by_name` function L38-48 — `( conn: &mut SqliteConnection, name: &str, source: &str, ) -> QueryResult<Option...` — Get a sense by name and source.
- pub `list_senses` function L51-53 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<Sense>>` — List all senses, ordered by name.
- pub `list_senses_by_source` function L56-64 — `( conn: &mut SqliteConnection, source: &str, ) -> QueryResult<Vec<Sense>>` — List senses from a specific source.
- pub `delete_sense` function L67-69 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<usize>` — Delete a sense by its ID.
- pub `delete_senses_by_source` function L72-77 — `( conn: &mut SqliteConnection, source: &str, ) -> QueryResult<usize>` — Delete all senses from a specific source.
- pub `count_senses` function L80-82 — `(conn: &mut SqliteConnection) -> QueryResult<i64>` — Count all senses.
-  `tests` module L85-132 — `-` — Database operations for senses.
-  `setup_test_data` function L91-94 — `(conn: &mut SqliteConnection)` — Database operations for senses.
-  `test_sense_crud` function L97-114 — `()` — Database operations for senses.
-  `test_list_senses` function L117-131 — `()` — Database operations for senses.

#### crates/mimir-core/src/dal/catalog/skill.rs

- pub `insert_skill` function L11-18 — `(conn: &mut SqliteConnection, skill: &NewSkill) -> QueryResult<i32>` — Insert a new skill.
- pub `insert_skills` function L21-28 — `( conn: &mut SqliteConnection, skills: &[NewSkill], ) -> QueryResult<usize>` — Insert multiple skills in a batch.
- pub `get_skill` function L31-35 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<Skill>` — Get a skill by its ID.
- pub `get_skill_by_name` function L38-48 — `( conn: &mut SqliteConnection, name: &str, source: &str, ) -> QueryResult<Option...` — Get a skill by name and source.
- pub `list_skills` function L51-53 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<Skill>>` — List all skills, ordered by name.
- pub `list_skills_by_source` function L56-64 — `( conn: &mut SqliteConnection, source: &str, ) -> QueryResult<Vec<Skill>>` — List skills from a specific source.
- pub `list_skills_by_ability` function L67-75 — `( conn: &mut SqliteConnection, ability: &str, ) -> QueryResult<Vec<Skill>>` — List skills by ability score.
- pub `delete_skill` function L78-80 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<usize>` — Delete a skill by its ID.
- pub `delete_skills_by_source` function L83-88 — `( conn: &mut SqliteConnection, source: &str, ) -> QueryResult<usize>` — Delete all skills from a specific source.
- pub `count_skills` function L91-93 — `(conn: &mut SqliteConnection) -> QueryResult<i64>` — Count all skills.
-  `tests` module L96-148 — `-` — Database operations for skills.
-  `setup_test_data` function L102-105 — `(conn: &mut SqliteConnection)` — Database operations for skills.
-  `test_skill_crud` function L108-127 — `()` — Database operations for skills.
-  `test_list_skills` function L130-147 — `()` — Database operations for skills.

#### crates/mimir-core/src/dal/catalog/source.rs

- pub `insert_source` function L13-19 — `(conn: &mut SqliteConnection, source: &NewCatalogSource) -> QueryResult<String>` — Insert a new catalog source.
- pub `insert_sources` function L22-29 — `( conn: &mut SqliteConnection, sources: &[NewCatalogSource], ) -> QueryResult<us...` — Insert multiple catalog sources in a batch.
- pub `get_source` function L32-34 — `(conn: &mut SqliteConnection, code: &str) -> QueryResult<CatalogSource>` — Get a catalog source by its code.
- pub `get_source_optional` function L37-42 — `( conn: &mut SqliteConnection, code: &str, ) -> QueryResult<Option<CatalogSource...` — Get a catalog source by its code, returning None if not found.
- pub `list_sources` function L45-49 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<CatalogSource>>` — List all catalog sources.
- pub `list_enabled_sources` function L52-57 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<CatalogSource>>` — List only enabled catalog sources.
- pub `set_enabled` function L60-65 — `(conn: &mut SqliteConnection, code: &str, enabled: bool) -> QueryResult<usize>` — Set the enabled status for a catalog source.
- pub `delete_source` function L71-73 — `(conn: &mut SqliteConnection, code: &str) -> QueryResult<usize>` — Delete a catalog source by its code.
- pub `delete_source_cascade` function L79-158 — `(conn: &mut SqliteConnection, code: &str) -> QueryResult<usize>` — Delete a catalog source and all associated entities.
- pub `source_exists` function L161-166 — `(conn: &mut SqliteConnection, code: &str) -> QueryResult<bool>` — Check if a source exists.
- pub `count_sources` function L169-171 — `(conn: &mut SqliteConnection) -> QueryResult<i64>` — Count all catalog sources.
- pub `count_enabled_sources` function L174-179 — `(conn: &mut SqliteConnection) -> QueryResult<i64>` — Count enabled catalog sources.
-  `tests` module L182-313 — `-` — Database operations for catalog sources.
-  `test_insert_and_get_source` function L187-204 — `()` — Database operations for catalog sources.
-  `test_insert_multiple_sources` function L207-221 — `()` — Database operations for catalog sources.
-  `test_list_enabled_sources` function L224-237 — `()` — Database operations for catalog sources.
-  `test_set_enabled` function L240-255 — `()` — Database operations for catalog sources.
-  `test_get_source_optional` function L258-269 — `()` — Database operations for catalog sources.
-  `test_source_exists` function L272-281 — `()` — Database operations for catalog sources.
-  `test_delete_source` function L284-295 — `()` — Database operations for catalog sources.
-  `test_count_sources` function L298-312 — `()` — Database operations for catalog sources.

#### crates/mimir-core/src/dal/catalog/spell.rs

- pub `insert_spell` function L13-21 — `(conn: &mut SqliteConnection, spell: &NewSpell) -> QueryResult<i32>` — Insert a new spell.
- pub `insert_spells` function L24-28 — `(conn: &mut SqliteConnection, spells: &[NewSpell]) -> QueryResult<usize>` — Insert multiple spells in a batch.
- pub `get_spell` function L31-35 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<Spell>` — Get a spell by its ID.
- pub `get_spell_optional` function L38-43 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<Option<Spell>>` — Get a spell by its ID, returning None if not found.
- pub `get_spell_by_name` function L49-60 — `( conn: &mut SqliteConnection, name: &str, source: &str, ) -> QueryResult<Option...` — Get a spell by name and source (case-insensitive name matching).
- pub `list_spells` function L63-67 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<Spell>>` — List all spells, ordered by level then name.
- pub `list_spells_by_source` function L70-78 — `( conn: &mut SqliteConnection, source: &str, ) -> QueryResult<Vec<Spell>>` — List spells from a specific source.
- pub `list_spells_by_level` function L81-86 — `(conn: &mut SqliteConnection, level: i32) -> QueryResult<Vec<Spell>>` — List spells of a specific level.
- pub `list_cantrips` function L89-91 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<Spell>>` — List all cantrips.
- pub `list_ritual_spells` function L94-99 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<Spell>>` — List all ritual spells.
- pub `list_spells_by_class` function L105-113 — `(conn: &mut SqliteConnection, class_name: &str) -> QueryResult<Vec<Spell>>` — List spells available to a specific class.
- pub `list_spells_by_class_and_level` function L116-129 — `( conn: &mut SqliteConnection, class_name: &str, level: i32, ) -> QueryResult<Ve...` — List spells available to a specific class at a specific level.
- pub `search_spells` function L132-174 — `( conn: &mut SqliteConnection, filter: &SpellFilter, ) -> QueryResult<Vec<Spell>...` — Search spells with filters.
- pub `search_spells_paginated` function L177-223 — `( conn: &mut SqliteConnection, filter: &SpellFilter, limit: i64, offset: i64, ) ...` — Search spells with pagination.
- pub `delete_spell` function L226-228 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<usize>` — Delete a spell by its ID.
- pub `delete_spells_by_source` function L231-233 — `(conn: &mut SqliteConnection, source: &str) -> QueryResult<usize>` — Delete all spells from a specific source.
- pub `count_spells` function L236-238 — `(conn: &mut SqliteConnection) -> QueryResult<i64>` — Count all spells.
- pub `count_spells_by_source` function L241-246 — `(conn: &mut SqliteConnection, source: &str) -> QueryResult<i64>` — Count spells from a specific source.
- pub `count_spells_by_level` function L249-254 — `(conn: &mut SqliteConnection, level: i32) -> QueryResult<i64>` — Count spells by level.
- pub `list_spell_sources` function L257-263 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<String>>` — List all distinct sources that have spells.
-  `tests` module L266-483 — `-` — Database operations for spells.
-  `test_insert_and_get_spell` function L271-290 — `()` — Database operations for spells.
-  `test_get_spell_by_name` function L293-307 — `()` — Database operations for spells.
-  `test_list_spells_by_source` function L310-324 — `()` — Database operations for spells.
-  `test_list_spells_by_level` function L327-343 — `()` — Database operations for spells.
-  `test_list_ritual_spells` function L346-361 — `()` — Database operations for spells.
-  `test_search_spells` function L364-406 — `()` — Database operations for spells.
-  `test_search_spells_paginated` function L409-427 — `()` — Database operations for spells.
-  `test_delete_spell` function L430-441 — `()` — Database operations for spells.
-  `test_delete_spells_by_source` function L444-458 — `()` — Database operations for spells.
-  `test_count_spells` function L461-482 — `()` — Database operations for spells.

#### crates/mimir-core/src/dal/catalog/spell_list.rs

- pub `insert_spell_class` function L15-22 — `(conn: &mut SqliteConnection, assoc: &NewSpellClass) -> QueryResult<i32>` — Insert a spell-class association.
- pub `insert_spell_classes` function L25-32 — `( conn: &mut SqliteConnection, assocs: &[NewSpellClass], ) -> QueryResult<usize>` — Insert multiple spell-class associations in a batch.
- pub `get_spell_classes` function L35-40 — `(conn: &mut SqliteConnection, spell_id: i32) -> QueryResult<Vec<SpellClass>>` — Get all class associations for a spell.
- pub `get_class_spells` function L43-47 — `(conn: &mut SqliteConnection, class_name: &str) -> QueryResult<Vec<SpellClass>>` — Get all spell associations for a class.
- pub `get_class_names_for_spell` function L50-60 — `( conn: &mut SqliteConnection, spell_id: i32, ) -> QueryResult<Vec<String>>` — Get the class names that have a specific spell.
- pub `delete_spell_classes` function L63-65 — `(conn: &mut SqliteConnection, spell_id: i32) -> QueryResult<usize>` — Delete all class associations for a spell.
- pub `delete_spell_classes_by_source` function L68-73 — `( conn: &mut SqliteConnection, source: &str, ) -> QueryResult<usize>` — Delete all spell associations from a specific source.
- pub `count_spell_classes` function L76-78 — `(conn: &mut SqliteConnection) -> QueryResult<i64>` — Count spell-class associations.
- pub `insert_spell_subclass` function L85-95 — `( conn: &mut SqliteConnection, assoc: &NewSpellSubclass, ) -> QueryResult<i32>` — Insert a spell-subclass association.
- pub `insert_spell_subclasses` function L98-105 — `( conn: &mut SqliteConnection, assocs: &[NewSpellSubclass], ) -> QueryResult<usi...` — Insert multiple spell-subclass associations in a batch.
- pub `get_spell_subclasses` function L108-119 — `( conn: &mut SqliteConnection, spell_id: i32, ) -> QueryResult<Vec<SpellSubclass...` — Get all subclass associations for a spell.
- pub `get_subclass_spells` function L122-131 — `( conn: &mut SqliteConnection, subclass_name: &str, class_name: &str, ) -> Query...` — Get all spell associations for a subclass.
- pub `delete_spell_subclasses` function L134-137 — `(conn: &mut SqliteConnection, spell_id: i32) -> QueryResult<usize>` — Delete all subclass associations for a spell.
- pub `delete_spell_subclasses_by_source` function L140-146 — `( conn: &mut SqliteConnection, source: &str, ) -> QueryResult<usize>` — Delete all spell-subclass associations from a specific source.
- pub `count_spell_subclasses` function L149-151 — `(conn: &mut SqliteConnection) -> QueryResult<i64>` — Count spell-subclass associations.
-  `tests` module L154-282 — `-` — Database operations for spell-class and spell-subclass relationships.
-  `setup_test_data` function L160-163 — `(conn: &mut SqliteConnection)` — Database operations for spell-class and spell-subclass relationships.
-  `insert_test_spell` function L165-169 — `(conn: &mut SqliteConnection, name: &str, level: i32) -> i32` — Database operations for spell-class and spell-subclass relationships.
-  `test_spell_class_crud` function L172-197 — `()` — Database operations for spell-class and spell-subclass relationships.
-  `test_get_class_spells` function L200-221 — `()` — Database operations for spell-class and spell-subclass relationships.
-  `test_spell_subclass_crud` function L224-252 — `()` — Database operations for spell-class and spell-subclass relationships.
-  `test_delete_by_source` function L255-281 — `()` — Database operations for spell-class and spell-subclass relationships.

#### crates/mimir-core/src/dal/catalog/subclass.rs

- pub `insert_subclass` function L14-28 — `(conn: &mut SqliteConnection, subclass: &NewSubclass) -> QueryResult<i32>` — Insert a new subclass, ignoring duplicates.
- pub `insert_subclasses` function L31-38 — `( conn: &mut SqliteConnection, subclasses: &[NewSubclass], ) -> QueryResult<usiz...` — Insert multiple subclasses in a batch.
- pub `get_subclass` function L41-43 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<Subclass>` — Get a subclass by its ID.
- pub `get_subclass_by_name` function L49-63 — `( conn: &mut SqliteConnection, name: &str, class_name: &str, source: &str, ) -> ...` — Get a subclass by name, class, and source (case-insensitive name matching).
- pub `list_subclasses` function L66-70 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<Subclass>>` — List all subclasses, ordered by class then name.
- pub `list_subclasses_by_class` function L73-81 — `( conn: &mut SqliteConnection, class_name: &str, ) -> QueryResult<Vec<Subclass>>` — List subclasses for a specific class.
- pub `list_subclasses_by_source` function L84-92 — `( conn: &mut SqliteConnection, source: &str, ) -> QueryResult<Vec<Subclass>>` — List subclasses from a specific source.
- pub `delete_subclass` function L95-97 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<usize>` — Delete a subclass by its ID.
- pub `delete_subclasses_by_source` function L100-105 — `( conn: &mut SqliteConnection, source: &str, ) -> QueryResult<usize>` — Delete all subclasses from a specific source.
- pub `count_subclasses` function L108-110 — `(conn: &mut SqliteConnection) -> QueryResult<i64>` — Count all subclasses.
-  `tests` module L113-155 — `-` — Database operations for character subclasses.
-  `test_subclass_crud` function L118-136 — `()` — Database operations for character subclasses.
-  `test_list_subclasses_by_class` function L139-154 — `()` — Database operations for character subclasses.

#### crates/mimir-core/src/dal/catalog/subclass_feature.rs

- pub `insert_subclass_feature` function L11-26 — `(conn: &mut SqliteConnection, feature: &NewSubclassFeature) -> QueryResult<i32>` — Insert a new subclass feature, ignoring duplicates.
- pub `insert_subclass_features` function L29-33 — `(conn: &mut SqliteConnection, features: &[NewSubclassFeature]) -> QueryResult<us...` — Insert multiple subclass features in a batch.
- pub `get_subclass_feature` function L36-38 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<SubclassFeature>` — Get a subclass feature by its ID.
- pub `get_subclass_feature_by_name` function L44-58 — `( conn: &mut SqliteConnection, name: &str, subclass_name: &str, subclass_source:...` — Get a subclass feature by name and subclass (case-insensitive name matching).
- pub `list_subclass_features_by_subclass` function L61-71 — `( conn: &mut SqliteConnection, subclass_name: &str, subclass_source: &str, ) -> ...` — List all subclass features for a specific subclass.
- pub `delete_subclass_features_by_source` function L74-76 — `(conn: &mut SqliteConnection, source: &str) -> QueryResult<usize>` — Delete all subclass features from a specific source.
- pub `count_subclass_features` function L79-81 — `(conn: &mut SqliteConnection) -> QueryResult<i64>` — Count all subclass features.
- pub `count_subclass_features_by_source` function L84-89 — `(conn: &mut SqliteConnection, source: &str) -> QueryResult<i64>` — Count subclass features from a specific source.
- pub `list_subclass_feature_sources` function L92-98 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<String>>` — List all distinct sources that have subclass features.
- pub `search_subclass_features` function L101-143 — `( conn: &mut SqliteConnection, filter: &SubclassFeatureFilter, ) -> QueryResult<...` — Search subclass features with filters.

#### crates/mimir-core/src/dal/catalog/table.rs

- pub `insert_catalog_table` function L11-21 — `( conn: &mut SqliteConnection, table: &NewCatalogTable, ) -> QueryResult<i32>` — Insert a new catalog table.
- pub `insert_catalog_tables` function L24-31 — `( conn: &mut SqliteConnection, tables: &[NewCatalogTable], ) -> QueryResult<usiz...` — Insert multiple catalog tables in a batch.
- pub `get_catalog_table` function L34-38 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<CatalogTable>` — Get a catalog table by its ID.
- pub `get_catalog_table_by_name` function L41-51 — `( conn: &mut SqliteConnection, name: &str, source: &str, ) -> QueryResult<Option...` — Get a catalog table by name and source.
- pub `list_catalog_tables` function L54-58 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<CatalogTable>>` — List all catalog tables, ordered by name.
- pub `list_catalog_tables_by_source` function L61-69 — `( conn: &mut SqliteConnection, source: &str, ) -> QueryResult<Vec<CatalogTable>>` — List catalog tables from a specific source.
- pub `delete_catalog_table` function L72-74 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<usize>` — Delete a catalog table by its ID.
- pub `delete_catalog_tables_by_source` function L77-82 — `( conn: &mut SqliteConnection, source: &str, ) -> QueryResult<usize>` — Delete all catalog tables from a specific source.
- pub `count_catalog_tables` function L85-87 — `(conn: &mut SqliteConnection) -> QueryResult<i64>` — Count all catalog tables.
- pub `count_catalog_tables_by_source` function L90-95 — `(conn: &mut SqliteConnection, source: &str) -> QueryResult<i64>` — Count catalog tables from a specific source.
- pub `get_catalog_table_optional` function L98-103 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<Option<CatalogTable>>` — Get a catalog table by its ID, returning None if not found.
- pub `list_catalog_table_sources` function L106-112 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<String>>` — List all distinct sources that have catalog tables.
- pub `search_catalog_tables` function L115-135 — `( conn: &mut SqliteConnection, filter: &CatalogTableFilter, ) -> QueryResult<Vec...` — Search catalog tables with filters.
- pub `search_catalog_tables_paginated` function L138-164 — `( conn: &mut SqliteConnection, filter: &CatalogTableFilter, limit: i64, offset: ...` — Search catalog tables with pagination.
-  `tests` module L167-208 — `-` — Database operations for catalog tables.
-  `setup_test_data` function L173-176 — `(conn: &mut SqliteConnection)` — Database operations for catalog tables.
-  `test_catalog_table_crud` function L179-191 — `()` — Database operations for catalog tables.
-  `test_list_catalog_tables` function L194-207 — `()` — Database operations for catalog tables.

#### crates/mimir-core/src/dal/catalog/trap.rs

- pub `insert_trap` function L11-18 — `(conn: &mut SqliteConnection, trap: &NewTrap) -> QueryResult<i32>` — Insert a new trap.
- pub `insert_traps` function L21-28 — `( conn: &mut SqliteConnection, traps: &[NewTrap], ) -> QueryResult<usize>` — Insert multiple traps in a batch.
- pub `get_trap` function L31-35 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<Trap>` — Get a trap by its ID.
- pub `get_trap_optional` function L38-43 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<Option<Trap>>` — Get a trap by its ID, returning None if not found.
- pub `get_trap_by_name` function L49-60 — `( conn: &mut SqliteConnection, name: &str, source: &str, ) -> QueryResult<Option...` — Get a trap by name and source (case-insensitive name matching).
- pub `list_traps` function L63-65 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<Trap>>` — List all traps, ordered by name.
- pub `list_traps_by_source` function L68-76 — `( conn: &mut SqliteConnection, source: &str, ) -> QueryResult<Vec<Trap>>` — List traps from a specific source.
- pub `list_traps_by_tier` function L79-87 — `( conn: &mut SqliteConnection, tier: &str, ) -> QueryResult<Vec<Trap>>` — List traps by tier (e.g., "simple", "complex").
- pub `list_simple_traps` function L90-92 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<Trap>>` — List simple traps.
- pub `list_complex_traps` function L95-97 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<Trap>>` — List complex traps.
- pub `delete_trap` function L100-102 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<usize>` — Delete a trap by its ID.
- pub `delete_traps_by_source` function L105-110 — `( conn: &mut SqliteConnection, source: &str, ) -> QueryResult<usize>` — Delete all traps from a specific source.
- pub `count_traps` function L113-115 — `(conn: &mut SqliteConnection) -> QueryResult<i64>` — Count all traps.
- pub `count_traps_by_source` function L118-123 — `(conn: &mut SqliteConnection, source: &str) -> QueryResult<i64>` — Count traps from a specific source.
- pub `list_trap_sources` function L126-132 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<String>>` — List all distinct sources that have traps.
- pub `search_traps` function L135-158 — `(conn: &mut SqliteConnection, filter: &TrapFilter) -> QueryResult<Vec<Trap>>` — Search traps with filters.
- pub `search_traps_paginated` function L161-193 — `( conn: &mut SqliteConnection, filter: &TrapFilter, limit: i64, offset: i64, ) -...` — Search traps with pagination.
-  `tests` module L196-242 — `-` — Database operations for traps.
-  `test_trap_crud` function L201-219 — `()` — Database operations for traps.
-  `test_list_traps` function L222-241 — `()` — Database operations for traps.

#### crates/mimir-core/src/dal/catalog/variant_rule.rs

- pub `insert_variant_rule` function L11-21 — `( conn: &mut SqliteConnection, rule: &NewVariantRule, ) -> QueryResult<i32>` — Insert a new variant rule.
- pub `insert_variant_rules` function L24-31 — `( conn: &mut SqliteConnection, rules: &[NewVariantRule], ) -> QueryResult<usize>` — Insert multiple variant rules in a batch.
- pub `get_variant_rule` function L34-38 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<VariantRule>` — Get a variant rule by its ID.
- pub `get_variant_rule_by_name` function L41-51 — `( conn: &mut SqliteConnection, name: &str, source: &str, ) -> QueryResult<Option...` — Get a variant rule by name and source.
- pub `list_variant_rules` function L54-58 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<VariantRule>>` — List all variant rules, ordered by name.
- pub `list_variant_rules_by_source` function L61-69 — `( conn: &mut SqliteConnection, source: &str, ) -> QueryResult<Vec<VariantRule>>` — List variant rules from a specific source.
- pub `delete_variant_rule` function L72-74 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<usize>` — Delete a variant rule by its ID.
- pub `delete_variant_rules_by_source` function L77-82 — `( conn: &mut SqliteConnection, source: &str, ) -> QueryResult<usize>` — Delete all variant rules from a specific source.
- pub `count_variant_rules` function L85-87 — `(conn: &mut SqliteConnection) -> QueryResult<i64>` — Count all variant rules.
- pub `count_variant_rules_by_source` function L90-95 — `(conn: &mut SqliteConnection, source: &str) -> QueryResult<i64>` — Count variant rules from a specific source.
- pub `get_variant_rule_optional` function L98-103 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<Option<VariantRule>>` — Get a variant rule by its ID, returning None if not found.
- pub `list_variant_rule_sources` function L106-112 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<String>>` — List all distinct sources that have variant rules.
- pub `search_variant_rules` function L115-139 — `( conn: &mut SqliteConnection, filter: &VariantRuleFilter, ) -> QueryResult<Vec<...` — Search variant rules with filters.
- pub `search_variant_rules_paginated` function L142-172 — `( conn: &mut SqliteConnection, filter: &VariantRuleFilter, limit: i64, offset: i...` — Search variant rules with pagination.
-  `tests` module L175-219 — `-` — Database operations for variant rules.
-  `setup_test_data` function L181-184 — `(conn: &mut SqliteConnection)` — Database operations for variant rules.
-  `test_variant_rule_crud` function L187-201 — `()` — Database operations for variant rules.
-  `test_list_variant_rules` function L204-218 — `()` — Database operations for variant rules.

#### crates/mimir-core/src/dal/catalog/vehicle.rs

- pub `insert_vehicle` function L11-18 — `(conn: &mut SqliteConnection, vehicle: &NewVehicle) -> QueryResult<i32>` — Insert a new vehicle.
- pub `insert_vehicles` function L21-28 — `( conn: &mut SqliteConnection, vehicles: &[NewVehicle], ) -> QueryResult<usize>` — Insert multiple vehicles in a batch.
- pub `get_vehicle` function L31-35 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<Vehicle>` — Get a vehicle by its ID.
- pub `get_vehicle_by_name` function L38-48 — `( conn: &mut SqliteConnection, name: &str, source: &str, ) -> QueryResult<Option...` — Get a vehicle by name and source.
- pub `list_vehicles` function L51-53 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<Vehicle>>` — List all vehicles, ordered by name.
- pub `list_vehicles_by_source` function L56-64 — `( conn: &mut SqliteConnection, source: &str, ) -> QueryResult<Vec<Vehicle>>` — List vehicles from a specific source.
- pub `list_vehicles_by_type` function L67-75 — `( conn: &mut SqliteConnection, vehicle_type: &str, ) -> QueryResult<Vec<Vehicle>...` — List vehicles by type (e.g., "land", "water", "air").
- pub `delete_vehicle` function L78-80 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<usize>` — Delete a vehicle by its ID.
- pub `delete_vehicles_by_source` function L83-88 — `( conn: &mut SqliteConnection, source: &str, ) -> QueryResult<usize>` — Delete all vehicles from a specific source.
- pub `count_vehicles` function L91-93 — `(conn: &mut SqliteConnection) -> QueryResult<i64>` — Count all vehicles.
- pub `count_vehicles_by_source` function L96-101 — `(conn: &mut SqliteConnection, source: &str) -> QueryResult<i64>` — Count vehicles from a specific source.
- pub `get_vehicle_optional` function L104-109 — `(conn: &mut SqliteConnection, id: i32) -> QueryResult<Option<Vehicle>>` — Get a vehicle by its ID, returning None if not found.
- pub `list_vehicle_sources` function L112-118 — `(conn: &mut SqliteConnection) -> QueryResult<Vec<String>>` — List all distinct sources that have vehicles.
- pub `search_vehicles` function L121-145 — `( conn: &mut SqliteConnection, filter: &VehicleFilter, ) -> QueryResult<Vec<Vehi...` — Search vehicles with filters.
- pub `search_vehicles_paginated` function L148-178 — `( conn: &mut SqliteConnection, filter: &VehicleFilter, limit: i64, offset: i64, ...` — Search vehicles with pagination.
-  `tests` module L181-224 — `-` — Database operations for vehicles.
-  `test_vehicle_crud` function L186-204 — `()` — Database operations for vehicles.
-  `test_list_vehicles` function L207-223 — `()` — Database operations for vehicles.

### crates/mimir-core/src/dal

> *Semantic summary to be generated by AI agent.*

#### crates/mimir-core/src/dal/mod.rs

- pub `campaign` module L11 — `-` — functions to implement business logic.
- pub `catalog` module L12 — `-` — functions to implement business logic.

### crates/mimir-core/src

> *Semantic summary to be generated by AI agent.*

#### crates/mimir-core/src/db.rs

- pub `MIGRATIONS` variable L12 — `: EmbeddedMigrations` — Embed all migrations at compile time.
- pub `run_migrations` function L40-43 — `(conn: &mut SqliteConnection) -> Result<Vec<String>, Box<dyn std::error::Error +...` — Run all pending migrations on the given connection.
- pub `init_database` function L49-59 — `(db_url: &str) -> Result<SqliteConnection, Box<dyn std::error::Error + Send + Sy...` — Establish a database connection, run migrations, and configure pragmas.
- pub `create_connection` function L65-69 — `(db_url: &str) -> Result<SqliteConnection, Box<dyn std::error::Error + Send + Sy...` — Create a new database connection with pragmas configured.
- pub `test_connection` function L73-75 — `() -> SqliteConnection` — Create an in-memory SQLite connection for testing.
-  `configure_connection` function L20-35 — `(conn: &mut SqliteConnection)` — Configure a connection with pragmas for optimal operation.

#### crates/mimir-core/src/lib.rs

- pub `catalog` module L5 — `-` — Core types, models, and services for Mimir v0.5.
- pub `dal` module L6 — `-` — Core types, models, and services for Mimir v0.5.
- pub `db` module L7 — `-` — Core types, models, and services for Mimir v0.5.
- pub `fts` module L8 — `-` — Core types, models, and services for Mimir v0.5.
- pub `import` module L9 — `-` — Core types, models, and services for Mimir v0.5.
- pub `models` module L10 — `-` — Core types, models, and services for Mimir v0.5.
- pub `seed` module L12 — `-` — Core types, models, and services for Mimir v0.5.
- pub `services` module L13 — `-` — Core types, models, and services for Mimir v0.5.
- pub `templates` module L14 — `-` — Core types, models, and services for Mimir v0.5.
- pub `tokens` module L15 — `-` — Core types, models, and services for Mimir v0.5.
- pub `utils` module L16 — `-` — Core types, models, and services for Mimir v0.5.
- pub `test_utils` module L19 — `-` — Core types, models, and services for Mimir v0.5.
-  `schema` module L11 — `-` — Core types, models, and services for Mimir v0.5.

#### crates/mimir-core/src/test_utils.rs

- pub `MIGRATIONS` variable L12 — `: EmbeddedMigrations` — Embed all migrations at compile time.
- pub `setup_test_db` function L17-26 — `() -> SqliteConnection` — Set up an in-memory database with all migrations applied.
- pub `setup_test_db_with_sources` function L31-52 — `() -> SqliteConnection` — Set up an in-memory database with common test sources pre-inserted.

#### crates/mimir-core/src/tokens.rs

- pub `slugify` function L13-33 — `(name: &str) -> String` — Slugify a monster name for use in filesystem paths.
- pub `token_relative_path` function L38-41 — `(source: &str, monster_name: &str, extension: &str) -> String` — Generate the relative token path for storage in the database.
- pub `resolve_token_path` function L44-46 — `(app_data_dir: &Path, token_image_path: &str) -> PathBuf` — Resolve a relative token path to an absolute filesystem path.
- pub `find_source_token` function L52-73 — `(img_dir: &Path, source: &str, monster_name: &str) -> Option<(PathBuf, String)>` — Find a token image in the 5etools image directory.
- pub `copy_token` function L78-104 — `( img_dir: &Path, app_data_dir: &Path, source: &str, monster_name: &str, ) -> Re...` — Copy a token image from the source directory to the app data directory.
- pub `copy_tokens_batch` function L109-130 — `( img_dir: &Path, app_data_dir: &Path, monsters: &[(String, String)], // (source...` — Batch copy tokens for multiple monsters.
-  `tests` module L133-264 — `-` — Handles token image paths, slugification, and file copying for monster tokens.
-  `test_slugify_simple` function L138-140 — `()` — Handles token image paths, slugification, and file copying for monster tokens.
-  `test_slugify_spaces` function L143-145 — `()` — Handles token image paths, slugification, and file copying for monster tokens.
-  `test_slugify_special_chars` function L148-150 — `()` — Handles token image paths, slugification, and file copying for monster tokens.
-  `test_slugify_apostrophe` function L153-156 — `()` — Handles token image paths, slugification, and file copying for monster tokens.
-  `test_slugify_multiple_spaces` function L159-161 — `()` — Handles token image paths, slugification, and file copying for monster tokens.
-  `test_slugify_hyphens` function L164-166 — `()` — Handles token image paths, slugification, and file copying for monster tokens.
-  `test_token_relative_path` function L169-172 — `()` — Handles token image paths, slugification, and file copying for monster tokens.
-  `test_resolve_token_path` function L175-179 — `()` — Handles token image paths, slugification, and file copying for monster tokens.
-  `test_find_source_token_png` function L182-196 — `()` — Handles token image paths, slugification, and file copying for monster tokens.
-  `test_find_source_token_webp` function L199-212 — `()` — Handles token image paths, slugification, and file copying for monster tokens.
-  `test_find_source_token_not_found` function L215-221 — `()` — Handles token image paths, slugification, and file copying for monster tokens.
-  `test_copy_token` function L224-248 — `()` — Handles token image paths, slugification, and file copying for monster tokens.
-  `test_copy_token_not_found` function L251-263 — `()` — Handles token image paths, slugification, and file copying for monster tokens.

#### crates/mimir-core/src/utils.rs

- pub `now_rfc3339` function L19-21 — `() -> String` — Returns the current UTC timestamp in RFC 3339 format.
-  `tests` module L24-40 — `-` — Common helper functions used across services and DAL.
-  `test_now_rfc3339_format` function L28-32 — `()` — Common helper functions used across services and DAL.
-  `test_now_rfc3339_is_utc` function L35-39 — `()` — Common helper functions used across services and DAL.

### crates/mimir-core/src/fts

> *Semantic summary to be generated by AI agent.*

#### crates/mimir-core/src/fts/entry_flattener.rs

- pub `flatten_entries` function L40-46 — `(entries: &[Value]) -> String` — Flattens 5etools entry arrays into searchable plain text.
- pub `flatten_entry` function L51-55 — `(entry: &Value) -> String` — Flattens a single 5etools entry value into searchable plain text.
- pub `strip_5etools_tags` function L145-147 — `(s: &str) -> String` — Strips 5etools tag markers like {@spell fireball} -> "fireball".
-  `TAG_REGEX` variable L12-14 — `: Lazy<Regex>` — Regex to match 5etools tag markers like {@spell fireball|PHB}
-  `flatten_entry_recursive` function L59-124 — `(entry: &Value, output: &mut Vec<String>)` — Recursively flattens an entry value into text fragments.
-  `tests` module L150-315 — `-` — Handles nested entry structures and strips 5etools tag markers.
-  `test_strip_simple_tag` function L155-157 — `()` — Handles nested entry structures and strips 5etools tag markers.
-  `test_strip_tag_with_source` function L160-162 — `()` — Handles nested entry structures and strips 5etools tag markers.
-  `test_strip_multiple_tags` function L165-169 — `()` — Handles nested entry structures and strips 5etools tag markers.
-  `test_strip_dice_tag` function L172-174 — `()` — Handles nested entry structures and strips 5etools tag markers.
-  `test_strip_damage_tag` function L177-179 — `()` — Handles nested entry structures and strips 5etools tag markers.
-  `test_strip_hit_tag` function L182-184 — `()` — Handles nested entry structures and strips 5etools tag markers.
-  `test_strip_dc_tag` function L187-189 — `()` — Handles nested entry structures and strips 5etools tag markers.
-  `test_strip_condition_tag` function L192-194 — `()` — Handles nested entry structures and strips 5etools tag markers.
-  `test_no_tags` function L197-200 — `()` — Handles nested entry structures and strips 5etools tag markers.
-  `test_flatten_plain_string` function L203-206 — `()` — Handles nested entry structures and strips 5etools tag markers.
-  `test_flatten_multiple_strings` function L209-212 — `()` — Handles nested entry structures and strips 5etools tag markers.
-  `test_flatten_nested_entries` function L215-224 — `()` — Handles nested entry structures and strips 5etools tag markers.
-  `test_flatten_deeply_nested` function L227-239 — `()` — Handles nested entry structures and strips 5etools tag markers.
-  `test_flatten_preserves_tags` function L242-248 — `()` — Handles nested entry structures and strips 5etools tag markers.
-  `test_flatten_list` function L251-260 — `()` — Handles nested entry structures and strips 5etools tag markers.
-  `test_flatten_empty` function L263-266 — `()` — Handles nested entry structures and strips 5etools tag markers.
-  `test_flatten_quote` function L269-278 — `()` — Handles nested entry structures and strips 5etools tag markers.
-  `test_flatten_table_caption` function L281-289 — `()` — Handles nested entry structures and strips 5etools tag markers.
-  `test_flatten_mixed_content` function L292-314 — `()` — Handles nested entry structures and strips 5etools tag markers.

#### crates/mimir-core/src/fts/mod.rs

-  `entry_flattener` module L5 — `-` — Provides full-text search capabilities for catalog entities using SQLite FTS5.
-  `search` module L6 — `-` — Provides full-text search capabilities for catalog entities using SQLite FTS5.

#### crates/mimir-core/src/fts/search.rs

- pub `ContentType` enum L11-16 — `Rules | Fluff` — Content type for FTS entries.
- pub `as_str` function L20-25 — `(&self) -> &'static str` — Convert to string for database storage.
- pub `FtsSearchResult` struct L30-50 — `{ entity_type: String, entity_id: String, content_type: String, name: String, ra...` — A search result from the FTS table.
- pub `index_entity` function L62-79 — `( conn: &mut SqliteConnection, entity_type: &str, entity_id: i32, content_type: ...` — Index a catalog entity in the FTS table.
- pub `remove_entity_from_index` function L88-97 — `( conn: &mut SqliteConnection, entity_type: &str, entity_id: i32, ) -> QueryResu...` — Remove all FTS entries for an entity.
- pub `clear_entity_type_from_index` function L105-112 — `( conn: &mut SqliteConnection, entity_type: &str, ) -> QueryResult<usize>` — Remove all FTS entries of a specific type.
- pub `clear_index` function L115-117 — `(conn: &mut SqliteConnection) -> QueryResult<usize>` — Clear the entire FTS index.
- pub `search` function L130-145 — `( conn: &mut SqliteConnection, query: &str, limit: i32, ) -> QueryResult<Vec<Fts...` — Search the catalog using full-text search.
- pub `search_by_entity_type` function L155-173 — `( conn: &mut SqliteConnection, query: &str, entity_type: &str, limit: i32, ) -> ...` — Search the catalog filtering by entity type.
- pub `search_by_content_type` function L183-200 — `( conn: &mut SqliteConnection, query: &str, content_type: ContentType, limit: i3...` — Search the catalog filtering by content type.
- pub `search_filtered` function L211-230 — `( conn: &mut SqliteConnection, query: &str, entity_type: &str, content_type: Con...` — Search with both entity type and content type filters.
- pub `count_indexed` function L233-243 — `(conn: &mut SqliteConnection) -> QueryResult<i64>` — Count total indexed entries.
- pub `count_indexed_by_type` function L246-258 — `(conn: &mut SqliteConnection, entity_type: &str) -> QueryResult<i64>` — Count indexed entries by entity type.
-  `ContentType` type L18-26 — `= ContentType` — Database operations for searching the unified FTS5 catalog_fts table.
-  `CountResult` struct L235-238 — `{ count: i64 }` — Database operations for searching the unified FTS5 catalog_fts table.
-  `CountResult` struct L248-251 — `{ count: i64 }` — Database operations for searching the unified FTS5 catalog_fts table.
-  `tests` module L261-447 — `-` — Database operations for searching the unified FTS5 catalog_fts table.
-  `setup_test_db` function L265-282 — `() -> SqliteConnection` — Database operations for searching the unified FTS5 catalog_fts table.
-  `test_index_and_search` function L285-302 — `()` — Database operations for searching the unified FTS5 catalog_fts table.
-  `test_search_by_entity_type` function L305-322 — `()` — Database operations for searching the unified FTS5 catalog_fts table.
-  `test_search_by_content_type` function L325-342 — `()` — Database operations for searching the unified FTS5 catalog_fts table.
-  `test_remove_entity` function L345-358 — `()` — Database operations for searching the unified FTS5 catalog_fts table.
-  `test_clear_entity_type` function L361-378 — `()` — Database operations for searching the unified FTS5 catalog_fts table.
-  `test_phrase_search` function L381-401 — `()` — Database operations for searching the unified FTS5 catalog_fts table.
-  `test_porter_stemming` function L404-424 — `()` — Database operations for searching the unified FTS5 catalog_fts table.
-  `test_count_functions` function L427-446 — `()` — Database operations for searching the unified FTS5 catalog_fts table.

### crates/mimir-core/src/import

> *Semantic summary to be generated by AI agent.*

#### crates/mimir-core/src/import/collector.rs

- pub `FluffKey` struct L18-22 — `{ entity_type: String, name: String, source: String }` — Key for looking up fluff data.
- pub `new` function L25-31 — `(entity_type: &str, name: &str, source: &str) -> Self` — Adapted from mimir-5etools-splitter/src/collector.rs.
- pub `CollectedEntities` struct L36-50 — `{ source: String, entities: HashMap<String, Vec<Value>>, fluff: HashMap<FluffKey...` — Collected entities from a source, organized by entity type.
- pub `new` function L54-63 — `(source: impl Into<String>) -> Self` — Create a new empty collection for a source.
- pub `add` function L66-73 — `(&mut self, entity_type: &str, entities: Vec<Value>)` — Add entities of a specific type.
- pub `get` function L76-78 — `(&self, entity_type: &str) -> Option<&Vec<Value>>` — Get entities of a specific type.
- pub `count` function L81-83 — `(&self, entity_type: &str) -> usize` — Get count for a specific entity type.
- pub `is_empty` function L86-88 — `(&self) -> bool` — Check if empty.
- pub `entity_types` function L91-93 — `(&self) -> Vec<&str>` — Get all entity types that have been collected.
- pub `add_fluff` function L96-99 — `(&mut self, entity_type: &str, name: &str, source: &str, fluff: Value)` — Add fluff data for an entity.
- pub `get_fluff` function L102-105 — `(&self, entity_type: &str, name: &str, source: &str) -> Option<&Value>` — Get fluff data for an entity.
- pub `fluff_count` function L108-110 — `(&self) -> usize` — Get fluff count.
- pub `set_book_content` function L113-115 — `(&mut self, content: Value)` — Set book content data.
- pub `set_book_toc` function L118-120 — `(&mut self, toc: Value)` — Set book table of contents.
- pub `has_book_content` function L123-125 — `(&self) -> bool` — Check if book content is available.
- pub `CollectedSrdContent` struct L130-135 — `{ content: HashMap<String, Vec<SrdItem>>, total_count: usize }` — Collected SRD entities, organized by content type.
- pub `new` function L139-141 — `() -> Self` — Create a new empty SRD collection.
- pub `add` function L144-151 — `(&mut self, content_type: &str, items: Vec<SrdItem>)` — Add SRD items of a specific content type.
- pub `get` function L154-156 — `(&self, content_type: &str) -> Option<&Vec<SrdItem>>` — Get SRD items of a specific content type.
- pub `is_empty` function L159-161 — `(&self) -> bool` — Check if empty.
- pub `content_types` function L164-166 — `(&self) -> Vec<&str>` — Get all content types that have been collected.
- pub `collect_source_entities` function L183-205 — `(repo_path: &Path, source: &str) -> Result<CollectedEntities>` — Collect all entities from a source (book).
- pub `discover_available_sources` function L528-530 — `(repo_path: &Path) -> Result<Vec<Book>>` — Collect all available books/sources from a 5etools directory.
- pub `collect_all_of_type` function L539-592 — `(repo_path: &Path, entity_type: &str) -> Result<Vec<Value>>` — Collect entities of a specific type from any source.
- pub `collect_srd_content` function L598-651 — `(repo_path: &Path) -> Result<CollectedSrdContent>` — Collect all SRD content from a 5etools repository.
- pub `filter_srd_only` function L684-686 — `(entities: &[Value]) -> Vec<SrdItem>` — Filter a list of entities to only include SRD content.
- pub `entity_is_srd` function L689-691 — `(entity: &Value) -> bool` — Check if a single entity is SRD content.
- pub `summarize_collection` function L694-707 — `(collected: &CollectedEntities) -> String` — Get a summary of collected entities.
-  `FluffKey` type L24-32 — `= FluffKey` — Adapted from mimir-5etools-splitter/src/collector.rs.
-  `CollectedEntities` type L52-126 — `= CollectedEntities` — Adapted from mimir-5etools-splitter/src/collector.rs.
-  `CollectedSrdContent` type L137-167 — `= CollectedSrdContent` — Adapted from mimir-5etools-splitter/src/collector.rs.
-  `collect_entities_from_files` function L208-227 — `( repo_path: &Path, info: &EntityFileInfo, source: &str, ) -> Result<Vec<Value>>` — Collect entities from files matching the entity file info pattern.
-  `collect_root_level_entities` function L230-278 — `( collected: &mut CollectedEntities, data_dir: &Path, source: &str, ) -> Result<...` — Collect entities from root-level data files (not in subdirectories).
-  `collect_classes_from_directory` function L285-332 — `( collected: &mut CollectedEntities, data_dir: &Path, source: &str, ) -> Result<...` — Collect classes, subclasses, and their features from the class directory.
-  `collect_fluff` function L335-376 — `(collected: &mut CollectedEntities, data_dir: &Path, source: &str) -> Result<()>` — Collect fluff (lore, flavor text, images) for all entity types from a source.
-  `collect_fluff_from_file` function L379-403 — `( collected: &mut CollectedEntities, file_path: &Path, json_key: &str, entity_ty...` — Collect fluff from a single fluff file.
-  `collect_class_fluff` function L409-474 — `( collected: &mut CollectedEntities, data_dir: &Path, source: &str, ) -> Result<...` — Collect class fluff from class directory.
-  `collect_book_content` function L480-523 — `( collected: &mut CollectedEntities, data_dir: &Path, source: &str, ) -> Result<...` — Collect book content (readable chapters/sections) for a source.
-  `collect_srd_from_directory` function L654-679 — `( collected: &mut CollectedSrdContent, dir: &Path, prefix: &str, ) -> Result<()>` — Collect SRD content from all JSON files in a directory with a given prefix.
-  `tests` module L710-805 — `-` — Adapted from mimir-5etools-splitter/src/collector.rs.
-  `test_collected_entities_new` function L715-720 — `()` — Adapted from mimir-5etools-splitter/src/collector.rs.
-  `test_collected_entities_add` function L723-734 — `()` — Adapted from mimir-5etools-splitter/src/collector.rs.
-  `test_collected_entities_get` function L737-747 — `()` — Adapted from mimir-5etools-splitter/src/collector.rs.
-  `test_collected_entities_entity_types` function L750-759 — `()` — Adapted from mimir-5etools-splitter/src/collector.rs.
-  `test_collected_srd_content_new` function L762-766 — `()` — Adapted from mimir-5etools-splitter/src/collector.rs.
-  `test_filter_srd_only` function L769-779 — `()` — Adapted from mimir-5etools-splitter/src/collector.rs.
-  `test_entity_is_srd` function L782-788 — `()` — Adapted from mimir-5etools-splitter/src/collector.rs.
-  `test_summarize_collection` function L791-804 — `()` — Adapted from mimir-5etools-splitter/src/collector.rs.

#### crates/mimir-core/src/import/discovery.rs

- pub `Book` struct L13-35 — `{ name: String, id: String, source: String, group: Option<String>, published: Op...` — Metadata for a 5etools book or sourcebook.
- pub `Cover` struct L39-45 — `{ cover_type: String, path: String }` — Book cover image information.
- pub `load_all_books` function L67-76 — `(repo_path: &Path) -> Result<Vec<Book>>` — Load all books from a 5etools repository/directory.
- pub `get_all_source_codes` function L81-84 — `(repo_path: &Path) -> Result<Vec<String>>` — Get all unique source codes from the available books.
- pub `file_exists_for_source` function L95-99 — `(repo_path: &Path, dir: &str, prefix: &str, source: &str) -> bool` — Check if a data file exists for a given source.
- pub `load_json_file` function L102-107 — `(path: &Path) -> Result<serde_json::Value>` — Load and parse a JSON file.
- pub `get_matching_files` function L121-162 — `( repo_path: &Path, dir: &str, prefix: &str, source: &str, ) -> Vec<PathBuf>` — Get all file paths matching a source pattern.
- pub `EntityFileInfo` struct L168-175 — `{ dir: &'static str, prefix: &'static str, json_key: &'static str }` — Entity type to file location mapping.
- pub `ENTITY_FILE_INFO` variable L178-387 — `: &[(&str, EntityFileInfo)]` — Get file info for each entity type.
-  `BooksJson` struct L49-51 — `{ book: Vec<Book> }` — Internal struct for parsing books.json.
-  `tests` module L390-422 — `-` — Adapted from mimir-5etools-splitter/src/parser.rs.
-  `test_book_deserialization` function L395-407 — `()` — Adapted from mimir-5etools-splitter/src/parser.rs.
-  `test_entity_file_info` function L410-421 — `()` — Adapted from mimir-5etools-splitter/src/parser.rs.

#### crates/mimir-core/src/import/filter.rs

- pub `SourceFilter` interface L14-28 — `{ fn filter_by_source(), fn filter_key_by_source() }` — Trait for filtering content by source.
- pub `ENTITY_KEYS` variable L64-92 — `: &[&str]` — Common JSON keys containing entity arrays in 5etools data.
- pub `matches_source` function L100-133 — `(item: &Value, source: &str) -> bool` — Check if an item matches the given source.
- pub `get_all_sources` function L139-143 — `(data: &Value) -> HashSet<String>` — Get all unique sources from a JSON value.
- pub `filter_by_sources` function L167-173 — `(items: &[Value], sources: &[&str]) -> Vec<Value>` — Filter entities by a list of allowed sources.
-  `Value` type L30-61 — `impl SourceFilter for Value` — Adapted from mimir-5etools-splitter/src/filter.rs.
-  `filter_by_source` function L31-46 — `(&self, source: &str) -> Vec<Value>` — Adapted from mimir-5etools-splitter/src/filter.rs.
-  `filter_key_by_source` function L48-60 — `(&self, key: &str, source: &str) -> Vec<Value>` — Adapted from mimir-5etools-splitter/src/filter.rs.
-  `collect_sources_recursive` function L145-162 — `(value: &Value, sources: &mut HashSet<String>)` — Adapted from mimir-5etools-splitter/src/filter.rs.
-  `tests` module L176-279 — `-` — Adapted from mimir-5etools-splitter/src/filter.rs.
-  `test_direct_source_match` function L181-188 — `()` — Adapted from mimir-5etools-splitter/src/filter.rs.
-  `test_inherits_from_match` function L191-200 — `()` — Adapted from mimir-5etools-splitter/src/filter.rs.
-  `test_sources_array_match` function L203-214 — `()` — Adapted from mimir-5etools-splitter/src/filter.rs.
-  `test_filter_by_source` function L217-229 — `()` — Adapted from mimir-5etools-splitter/src/filter.rs.
-  `test_filter_key_by_source` function L232-246 — `()` — Adapted from mimir-5etools-splitter/src/filter.rs.
-  `test_get_all_sources` function L249-266 — `()` — Adapted from mimir-5etools-splitter/src/filter.rs.
-  `test_filter_by_sources` function L269-278 — `()` — Adapted from mimir-5etools-splitter/src/filter.rs.

#### crates/mimir-core/src/import/images.rs

- pub `copy_images` function L20-97 — `(source_dir: &Path, dest_dir: &Path) -> Result<usize>` — Copy all images from source directory to destination, preserving structure.
- pub `image_exists` function L100-102 — `(img_dir: &Path, relative_path: &str) -> bool` — Check if an image exists at the given path relative to the images directory.
- pub `get_token_path` function L108-135 — `(img_dir: &Path, source: &str, monster_name: &str) -> Option<String>` — Get the expected token path for a monster.
-  `slugify` function L138-147 — `(name: &str) -> String` — Convert a name to a URL-safe slug.
-  `tests` module L150-264 — `-` — preserving the directory structure so paths in imported data work as-is.
-  `test_slugify` function L157-162 — `()` — preserving the directory structure so paths in imported data work as-is.
-  `test_copy_images_empty_source` function L165-171 — `()` — preserving the directory structure so paths in imported data work as-is.
-  `test_copy_images_preserves_structure` function L174-193 — `()` — preserving the directory structure so paths in imported data work as-is.
-  `test_copy_images_skips_existing` function L196-213 — `()` — preserving the directory structure so paths in imported data work as-is.
-  `test_copy_images_nonexistent_source` function L216-222 — `()` — preserving the directory structure so paths in imported data work as-is.
-  `test_get_token_path` function L225-243 — `()` — preserving the directory structure so paths in imported data work as-is.
-  `test_get_token_path_slugified` function L246-263 — `()` — preserving the directory structure so paths in imported data work as-is.

#### crates/mimir-core/src/import/mod.rs

-  `collector` module L30 — `-` — This module provides functionality for importing D&D 5e content directly from
-  `discovery` module L31 — `-` — ```
-  `filter` module L32 — `-` — ```
-  `images` module L33 — `-` — ```
-  `service` module L34 — `-` — ```
-  `srd` module L35 — `-` — ```

#### crates/mimir-core/src/import/service.rs

- pub `ImportResult` struct L24-35 — `{ sources_imported: Vec<String>, sources_failed: Vec<(String, String)>, entity_c...` — Result of an import operation.
- pub `is_success` function L39-41 — `(&self) -> bool` — Check if the import was fully successful (no failures).
- pub `summary` function L44-70 — `(&self) -> String` — Get a summary of the import.
- pub `CatalogImportService` struct L86-96 — `{ conn: &'a mut SqliteConnection, source_img_dir: Option<PathBuf>, dest_img_dir:...` — Catalog import service for importing 5etools data.
- pub `new` function L103-116 — `(conn: &'a mut SqliteConnection) -> Self` — Create a new import service with a database connection.
- pub `with_all_groups` function L119-122 — `(mut self) -> Self` — Import all groups (including adventures, screens, etc.)
- pub `with_groups` function L127-130 — `(mut self, groups: Vec<String>) -> Self` — Set specific groups to import.
- pub `with_image_copy` function L139-143 — `(mut self, source_img_dir: PathBuf, dest_img_dir: PathBuf) -> Self` — Configure image copying from source to destination.
- pub `import_from_directory` function L152-250 — `(&mut self, repo_path: &Path) -> Result<ImportResult>` — Import all available sources from a 5etools data directory.
- pub `import_from_tarball` function L302-390 — `(&mut self, tarball_path: &Path) -> Result<ImportResult>` — Import all sources from a tar.gz archive, streaming directly without extraction.
-  `ImportResult` type L37-71 — `= ImportResult` — transaction handling, and FTS indexing.
-  `DEFAULT_ALLOWED_GROUPS` variable L83 — `: &[&str]` — Default groups to include when importing.
-  `expand_magic_variants_from_disk` function L253-296 — `(&mut self, repo_path: &Path) -> Result<usize>` — Expand magic variant templates from disk-based 5etools data.
-  `expand_magic_variants_from_memory` function L395-442 — `( &mut self, json_files: &HashMap<String, String>, ) -> Result<usize>` — Expand magic variant templates from in-memory JSON files.
-  `import_source_from_memory` function L445-472 — `( &mut self, json_files: &HashMap<String, String>, source_code: &str, source_nam...` — Import a single source from in-memory JSON files.
-  `import_source_from_memory_internal` function L474-521 — `( &mut self, json_files: &HashMap<String, String>, source_code: &str, source_nam...` — transaction handling, and FTS indexing.
-  `import_source_with_transaction` function L524-551 — `( &mut self, repo_path: &Path, source_code: &str, source_name: &str, ) -> Result...` — Import a single source with transaction handling.
-  `import_source_internal` function L554-602 — `( &mut self, repo_path: &Path, source_code: &str, source_name: &str, ) -> Result...` — Internal import logic for a single source.
-  `import_entities` function L605-634 — `( &mut self, entity_type: &str, entities: &[Value], source: &str, collected: &Co...` — Import entities of a specific type.
-  `import_single_entity` function L637-691 — `( &mut self, entity_type: &str, entity: &Value, source: &str, collected: &Collec...` — Import a single entity based on its type.
-  `import_monster` function L695-724 — `(&mut self, entity: &Value, name: &str, source: &str, data: &str, fluff: Option<...` — transaction handling, and FTS indexing.
-  `import_spell` function L726-752 — `(&mut self, entity: &Value, name: &str, source: &str, data: &str, fluff: Option<...` — transaction handling, and FTS indexing.
-  `import_spell_classes` function L754-815 — `(&mut self, spell_id: i32, entity: &Value, source: &str) -> Result<()>` — transaction handling, and FTS indexing.
-  `import_spell_class_associations` function L821-905 — `(&mut self, repo_path: &Path, source_code: &str) -> Result<usize>` — Import spell-class associations from the 5etools sources.json file.
-  `import_spell_class_associations_from_memory` function L910-1003 — `( &mut self, json_files: &HashMap<String, String>, source_code: &str, ) -> Resul...` — Import spell-class associations from in-memory JSON files.
-  `import_item` function L1005-1030 — `(&mut self, entity: &Value, name: &str, source: &str, data: &str, fluff: Option<...` — transaction handling, and FTS indexing.
-  `import_item_attunement` function L1032-1051 — `(&mut self, item_id: i32, entity: &Value) -> Result<()>` — transaction handling, and FTS indexing.
-  `expand_and_import_magic_variants` function L1058-1189 — `( &mut self, collected: &CollectedEntities, source_code: &str, ) -> Result<usize...` — Expand magic variant templates against base items and import the resulting concrete items.
-  `import_class` function L1191-1195 — `(&mut self, name: &str, source: &str, data: &str, fluff: Option<&str>) -> Result...` — transaction handling, and FTS indexing.
-  `import_class_feature` function L1197-1219 — `( &mut self, entity: &Value, name: &str, source: &str, data: &str, ) -> Result<i...` — transaction handling, and FTS indexing.
-  `import_subclass` function L1221-1237 — `( &mut self, entity: &Value, name: &str, source: &str, data: &str, fluff: Option...` — transaction handling, and FTS indexing.
-  `import_subclass_feature` function L1239-1272 — `( &mut self, entity: &Value, name: &str, source: &str, data: &str, ) -> Result<i...` — transaction handling, and FTS indexing.
-  `import_race` function L1274-1278 — `(&mut self, name: &str, source: &str, data: &str, fluff: Option<&str>) -> Result...` — transaction handling, and FTS indexing.
-  `import_background` function L1280-1284 — `(&mut self, name: &str, source: &str, data: &str, fluff: Option<&str>) -> Result...` — transaction handling, and FTS indexing.
-  `import_feat` function L1286-1290 — `(&mut self, name: &str, source: &str, data: &str, fluff: Option<&str>) -> Result...` — transaction handling, and FTS indexing.
-  `import_condition` function L1292-1296 — `(&mut self, name: &str, source: &str, data: &str, fluff: Option<&str>) -> Result...` — transaction handling, and FTS indexing.
-  `import_disease` function L1298-1302 — `(&mut self, name: &str, source: &str, data: &str, fluff: Option<&str>) -> Result...` — transaction handling, and FTS indexing.
-  `import_action` function L1304-1307 — `(&mut self, name: &str, source: &str, data: &str) -> Result<i32>` — transaction handling, and FTS indexing.
-  `import_language` function L1309-1328 — `( &mut self, entity: &Value, name: &str, source: &str, data: &str, fluff: Option...` — transaction handling, and FTS indexing.
-  `import_vehicle` function L1330-1349 — `( &mut self, entity: &Value, name: &str, source: &str, data: &str, fluff: Option...` — transaction handling, and FTS indexing.
-  `import_object` function L1351-1370 — `( &mut self, entity: &Value, name: &str, source: &str, data: &str, fluff: Option...` — transaction handling, and FTS indexing.
-  `import_trap` function L1372-1386 — `(&mut self, entity: &Value, name: &str, source: &str, data: &str, fluff: Option<...` — transaction handling, and FTS indexing.
-  `import_hazard` function L1388-1392 — `(&mut self, name: &str, source: &str, data: &str, fluff: Option<&str>) -> Result...` — transaction handling, and FTS indexing.
-  `import_cult` function L1394-1397 — `(&mut self, name: &str, source: &str, data: &str) -> Result<i32>` — transaction handling, and FTS indexing.
-  `import_deity` function L1399-1418 — `( &mut self, entity: &Value, name: &str, source: &str, data: &str, ) -> Result<i...` — transaction handling, and FTS indexing.
-  `import_sense` function L1420-1423 — `(&mut self, name: &str, source: &str, data: &str) -> Result<i32>` — transaction handling, and FTS indexing.
-  `import_skill` function L1425-1438 — `(&mut self, entity: &Value, name: &str, source: &str, data: &str) -> Result<i32>` — transaction handling, and FTS indexing.
-  `import_optional_feature` function L1440-1462 — `( &mut self, entity: &Value, name: &str, source: &str, data: &str, ) -> Result<i...` — transaction handling, and FTS indexing.
-  `import_psionic` function L1464-1488 — `( &mut self, entity: &Value, name: &str, source: &str, data: &str, ) -> Result<i...` — transaction handling, and FTS indexing.
-  `import_reward` function L1490-1509 — `( &mut self, entity: &Value, name: &str, source: &str, data: &str, ) -> Result<i...` — transaction handling, and FTS indexing.
-  `import_variant_rule` function L1511-1530 — `( &mut self, entity: &Value, name: &str, source: &str, data: &str, ) -> Result<i...` — transaction handling, and FTS indexing.
-  `import_catalog_table` function L1532-1535 — `(&mut self, name: &str, source: &str, data: &str) -> Result<i32>` — transaction handling, and FTS indexing.
-  `import_book` function L1541-1594 — `( &mut self, collected: &CollectedEntities, source: &str, name: &str, ) -> Resul...` — Import book content (readable chapters/sections).
-  `index_entity_fts` function L1598-1639 — `(&mut self, entity_type: &str, entity_id: i32, entity: &Value) -> Result<()>` — transaction handling, and FTS indexing.
-  `read_json_from_tarball` function L1648-1719 — `(tarball_path: &Path) -> Result<HashMap<String, String>>` — Read all JSON files from a tar.gz archive into memory.
-  `BookMeta` struct L1723-1727 — `{ id: String, name: String, group: Option<String> }` — Book metadata for source discovery (simplified).
-  `parse_books_from_memory` function L1730-1753 — `(json_files: &HashMap<String, String>) -> Result<Vec<BookMeta>>` — Parse books.json from in-memory files to discover available sources.
-  `collect_entities_from_memory` function L1756-1887 — `( json_files: &HashMap<String, String>, source_code: &str, ) -> Result<Collected...` — Collect entities from in-memory JSON files for a specific source.
-  `extract_cr` function L1893-1900 — `(entity: &Value) -> Option<String>` — Extract CR from a monster entity.
-  `extract_creature_type` function L1904-1913 — `(entity: &Value) -> Option<String>` — Extract creature type from a monster entity.
-  `extract_size` function L1917-1923 — `(entity: &Value) -> Option<String>` — Extract size from a monster entity.
-  `extract_ritual` function L1926-1932 — `(entity: &Value) -> bool` — Extract ritual flag from a spell entity.
-  `extract_concentration` function L1935-1944 — `(entity: &Value) -> bool` — Extract concentration flag from a spell entity.
-  `rewrite_book_image_paths` function L1951-1989 — `(content: &Value) -> Value` — Rewrite image paths in book content to use local asset paths.
-  `extract_attunement_classes` function L1992-2015 — `(attune_str: &str) -> Vec<String>` — Extract class names from an attunement requirement string.
-  `base_item_matches_field` function L2020-2050 — `(base_item: &Value, key: &str, expected: &Value) -> bool` — Check if a base item field matches a requirement key-value pair.
-  `base_item_excluded` function L2053-2103 — `(base_item: &Value, excludes: &Value) -> bool` — Check if a base item should be excluded based on the variant's excludes block.
-  `build_expanded_item` function L2106-2143 — `( base_item: &Value, inherits: &Value, expanded_name: &str, variant_name: &str, ...` — Build an expanded item JSON by merging base item data with variant inherits.
-  `expand_damage_type` function L2146-2163 — `(code: &str) -> &str` — Map 5etools damage type codes to human-readable names for use in prose text.
-  `resolve_template_variables` function L2168-2193 — `(obj: &mut serde_json::Map<String, Value>)` — Resolve 5etools template variables ({=fieldName}) throughout a JSON object.
-  `resolve_value_templates` function L2196-2218 — `(val: &Value, lookup: &HashMap<String, String>) -> Value` — Recursively resolve {=key} templates in a JSON value.
-  `tests` module L2221-2429 — `-` — transaction handling, and FTS indexing.
-  `test_extract_cr_string` function L2226-2229 — `()` — transaction handling, and FTS indexing.
-  `test_extract_cr_object` function L2232-2235 — `()` — transaction handling, and FTS indexing.
-  `test_extract_cr_number` function L2238-2241 — `()` — transaction handling, and FTS indexing.
-  `test_extract_creature_type_string` function L2244-2247 — `()` — transaction handling, and FTS indexing.
-  `test_extract_creature_type_object` function L2250-2253 — `()` — transaction handling, and FTS indexing.
-  `test_extract_size_array` function L2256-2259 — `()` — transaction handling, and FTS indexing.
-  `test_extract_size_string` function L2262-2265 — `()` — transaction handling, and FTS indexing.
-  `test_extract_ritual` function L2268-2274 — `()` — transaction handling, and FTS indexing.
-  `test_extract_concentration` function L2277-2283 — `()` — transaction handling, and FTS indexing.
-  `test_extract_attunement_classes` function L2286-2291 — `()` — transaction handling, and FTS indexing.
-  `test_base_item_matches_boolean_field` function L2296-2301 — `()` — transaction handling, and FTS indexing.
-  `test_base_item_matches_string_field` function L2304-2309 — `()` — transaction handling, and FTS indexing.
-  `test_base_item_matches_array_field` function L2312-2317 — `()` — transaction handling, and FTS indexing.
-  `test_base_item_excluded_boolean` function L2320-2326 — `()` — transaction handling, and FTS indexing.
-  `test_base_item_excluded_string` function L2329-2333 — `()` — transaction handling, and FTS indexing.
-  `test_base_item_excluded_array` function L2336-2340 — `()` — transaction handling, and FTS indexing.
-  `test_build_expanded_item` function L2343-2376 — `()` — transaction handling, and FTS indexing.
-  `test_template_variable_resolution` function L2379-2395 — `()` — transaction handling, and FTS indexing.
-  `test_build_expanded_item_suffix` function L2398-2409 — `()` — transaction handling, and FTS indexing.
-  `test_import_result_summary` function L2412-2428 — `()` — transaction handling, and FTS indexing.

#### crates/mimir-core/src/import/srd.rs

- pub `SrdFilter` interface L14-19 — `{ fn filter_srd_content() }` — Trait for filtering content by SRD status.
- pub `SrdItem` struct L23-32 — `{ data: Value, original_name: Option<String>, srd_name: Option<String>, was_rena...` — An item that has been identified as SRD content.
- pub `SrdStatus` enum L36-43 — `Include | IncludeRenamed | Exclude` — SRD status for an entity.
- pub `check_srd_status` function L99-119 — `(item: &Value) -> SrdStatus` — Check the SRD status of an item.
- pub `is_srd` function L122-124 — `(item: &Value) -> bool` — Check if an item is SRD content (quick check without transformation).
- pub `process_srd_item` function L129-153 — `(item: &Value) -> Option<SrdItem>` — Process a single item for SRD inclusion.
- pub `transform_item_for_srd` function L161-185 — `(item: &Value, new_name: Option<&str>) -> Value` — Transform an item for SRD inclusion.
- pub `extract_all_srd_content` function L190-237 — `(data: &Value) -> HashMap<String, Vec<SrdItem>>` — Get all SRD content from a complete 5etools dataset.
- pub `count_srd_items` function L240-250 — `(data: &Value) -> usize` — Count SRD items in a dataset without transforming them.
-  `Value` type L45-61 — `impl SrdFilter for Value` — Adapted from mimir-5etools-splitter/src/srd_filter.rs.
-  `filter_srd_content` function L46-60 — `(&self) -> Vec<SrdItem>` — Adapted from mimir-5etools-splitter/src/srd_filter.rs.
-  `SRD_ENTITY_KEYS` variable L64-91 — `: &[&str]` — Entity keys that may contain SRD content.
-  `tests` module L253-394 — `-` — Adapted from mimir-5etools-splitter/src/srd_filter.rs.
-  `test_srd_true` function L258-272 — `()` — Adapted from mimir-5etools-splitter/src/srd_filter.rs.
-  `test_srd_renamed` function L275-293 — `()` — Adapted from mimir-5etools-splitter/src/srd_filter.rs.
-  `test_basic_rules` function L296-311 — `()` — Adapted from mimir-5etools-splitter/src/srd_filter.rs.
-  `test_exclude` function L314-323 — `()` — Adapted from mimir-5etools-splitter/src/srd_filter.rs.
-  `test_no_srd_field` function L326-334 — `()` — Adapted from mimir-5etools-splitter/src/srd_filter.rs.
-  `test_is_srd` function L337-343 — `()` — Adapted from mimir-5etools-splitter/src/srd_filter.rs.
-  `test_check_srd_status` function L346-363 — `()` — Adapted from mimir-5etools-splitter/src/srd_filter.rs.
-  `test_extract_all_srd_content` function L366-380 — `()` — Adapted from mimir-5etools-splitter/src/srd_filter.rs.
-  `test_count_srd_items` function L383-393 — `()` — Adapted from mimir-5etools-splitter/src/srd_filter.rs.

### crates/mimir-core/src/models/campaign

> *Semantic summary to be generated by AI agent.*

#### crates/mimir-core/src/models/campaign/campaign.rs

- pub `Campaign` struct L17-30 — `{ id: String, name: String, description: Option<String>, archived_at: Option<Str...` — A campaign - the top-level container for D&D campaign data.
- pub `is_archived` function L34-36 — `(&self) -> bool` — Check if the campaign is archived.
- pub `NewCampaign` struct L42-46 — `{ id: &'a str, name: &'a str, description: Option<&'a str> }` — Data for inserting a new campaign.
- pub `new` function L50-56 — `(id: &'a str, name: &'a str) -> Self` — Create a new campaign.
- pub `with_description` function L59-62 — `(mut self, description: &'a str) -> Self` — Set the description.
- pub `UpdateCampaign` struct L68-73 — `{ name: Option<&'a str>, description: Option<Option<&'a str>>, archived_at: Opti...` — Data for updating a campaign.
- pub `set_name` function L77-83 — `(name: &'a str, updated_at: &'a str) -> Self` — Create an update to change the name.
- pub `set_description` function L86-92 — `(description: Option<&'a str>, updated_at: &'a str) -> Self` — Create an update to change the description.
- pub `archive` function L95-101 — `(archived_at: &'a str) -> Self` — Create an update to archive the campaign.
- pub `unarchive` function L104-110 — `(updated_at: &'a str) -> Self` — Create an update to unarchive the campaign.
-  `Campaign` type L32-37 — `= Campaign` — Top-level container for all campaign data.
-  `tests` module L114-150 — `-` — Top-level container for all campaign data.
-  `test_new_campaign` function L118-123 — `()` — Top-level container for all campaign data.
-  `test_new_campaign_with_description` function L126-130 — `()` — Top-level container for all campaign data.
-  `test_update_campaign_name` function L133-137 — `()` — Top-level container for all campaign data.
-  `test_update_campaign_archive` function L140-143 — `()` — Top-level container for all campaign data.
-  `test_update_campaign_unarchive` function L146-149 — `()` — Top-level container for all campaign data.

#### crates/mimir-core/src/models/campaign/campaign_asset.rs

- pub `ALLOWED_MIME_TYPES` variable L10-17 — `: &[&str]` — Allowed MIME types for campaign assets.
- pub `CampaignAsset` struct L23-42 — `{ id: String, campaign_id: Option<String>, module_id: Option<String>, filename: ...` — A campaign asset - user-uploaded file (image, UVTT map, etc.).
- pub `is_image` function L46-48 — `(&self) -> bool` — Check if the asset is an image.
- pub `is_uvtt` function L51-53 — `(&self) -> bool` — Check if the asset is a UVTT map file.
- pub `extension` function L56-58 — `(&self) -> Option<&str>` — Get the file extension from the filename.
- pub `is_campaign_asset` function L61-63 — `(&self) -> bool` — Check if this is a campaign-level asset.
- pub `is_module_asset` function L66-68 — `(&self) -> bool` — Check if this is a module-level asset.
- pub `NewCampaignAsset` struct L74-83 — `{ id: &'a str, campaign_id: Option<&'a str>, module_id: Option<&'a str>, filenam...` — Data for inserting a new campaign asset.
- pub `for_campaign` function L87-104 — `( id: &'a str, campaign_id: &'a str, filename: &'a str, mime_type: &'a str, blob...` — Create a new campaign-level asset.
- pub `for_module` function L107-124 — `( id: &'a str, module_id: &'a str, filename: &'a str, mime_type: &'a str, blob_p...` — Create a new module-level asset.
- pub `with_description` function L127-130 — `(mut self, description: &'a str) -> Self` — Set the description.
- pub `with_file_size` function L133-136 — `(mut self, size: i32) -> Self` — Set the file size.
- pub `is_allowed_mime_type` function L140-142 — `(mime_type: &str) -> bool` — Validate that a MIME type is allowed.
- pub `extension_for_mime_type` function L145-155 — `(mime_type: &str) -> Option<&'static str>` — Get file extension for a MIME type.
-  `CampaignAsset` type L44-69 — `= CampaignAsset` — User-uploaded images for campaigns or modules (maps, props, puzzles, etc.).
-  `tests` module L158-223 — `-` — User-uploaded images for campaigns or modules (maps, props, puzzles, etc.).
-  `test_new_campaign_asset_for_campaign` function L162-176 — `()` — User-uploaded images for campaigns or modules (maps, props, puzzles, etc.).
-  `test_new_campaign_asset_for_module` function L179-191 — `()` — User-uploaded images for campaigns or modules (maps, props, puzzles, etc.).
-  `test_new_campaign_asset_with_size` function L194-204 — `()` — User-uploaded images for campaigns or modules (maps, props, puzzles, etc.).
-  `test_is_allowed_mime_type` function L207-214 — `()` — User-uploaded images for campaigns or modules (maps, props, puzzles, etc.).
-  `test_extension_for_mime_type` function L217-222 — `()` — User-uploaded images for campaigns or modules (maps, props, puzzles, etc.).

#### crates/mimir-core/src/models/campaign/campaign_homebrew_item.rs

- pub `CampaignHomebrewItem` struct L17-28 — `{ id: String, campaign_id: String, name: String, item_type: Option<String>, rari...` — A homebrew item belonging to a campaign.
- pub `NewCampaignHomebrewItem` struct L33-42 — `{ id: &'a str, campaign_id: &'a str, name: &'a str, item_type: Option<&'a str>, ...` — Data for inserting a new homebrew item.
- pub `new` function L45-56 — `(id: &'a str, campaign_id: &'a str, name: &'a str, data: &'a str) -> Self` — Custom items created by DMs within a campaign, optionally cloned from catalog items.
- pub `with_item_type` function L58-61 — `(mut self, item_type: &'a str) -> Self` — Custom items created by DMs within a campaign, optionally cloned from catalog items.
- pub `with_rarity` function L63-66 — `(mut self, rarity: &'a str) -> Self` — Custom items created by DMs within a campaign, optionally cloned from catalog items.
- pub `cloned_from` function L68-72 — `(mut self, name: &'a str, source: &'a str) -> Self` — Custom items created by DMs within a campaign, optionally cloned from catalog items.
- pub `UpdateCampaignHomebrewItem` struct L78-84 — `{ name: Option<&'a str>, item_type: Option<Option<&'a str>>, rarity: Option<Opti...` — Data for updating a homebrew item.

#### crates/mimir-core/src/models/campaign/campaign_homebrew_monster.rs

- pub `CampaignHomebrewMonster` struct L17-29 — `{ id: String, campaign_id: String, name: String, cr: Option<String>, creature_ty...` — A homebrew monster belonging to a campaign.
- pub `NewCampaignHomebrewMonster` struct L34-44 — `{ id: &'a str, campaign_id: &'a str, name: &'a str, cr: Option<&'a str>, creatur...` — Data for inserting a new homebrew monster.
- pub `new` function L47-59 — `(id: &'a str, campaign_id: &'a str, name: &'a str, data: &'a str) -> Self` — Custom monsters created by DMs within a campaign, optionally cloned from catalog monsters.
- pub `with_cr` function L61-64 — `(mut self, cr: &'a str) -> Self` — Custom monsters created by DMs within a campaign, optionally cloned from catalog monsters.
- pub `with_creature_type` function L66-69 — `(mut self, creature_type: &'a str) -> Self` — Custom monsters created by DMs within a campaign, optionally cloned from catalog monsters.
- pub `with_size` function L71-74 — `(mut self, size: &'a str) -> Self` — Custom monsters created by DMs within a campaign, optionally cloned from catalog monsters.
- pub `cloned_from` function L76-80 — `(mut self, name: &'a str, source: &'a str) -> Self` — Custom monsters created by DMs within a campaign, optionally cloned from catalog monsters.
- pub `UpdateCampaignHomebrewMonster` struct L86-93 — `{ name: Option<&'a str>, cr: Option<Option<&'a str>>, creature_type: Option<Opti...` — Data for updating a homebrew monster.

#### crates/mimir-core/src/models/campaign/campaign_homebrew_spell.rs

- pub `CampaignHomebrewSpell` struct L17-28 — `{ id: String, campaign_id: String, name: String, level: Option<i32>, school: Opt...` — A homebrew spell belonging to a campaign.
- pub `NewCampaignHomebrewSpell` struct L33-42 — `{ id: &'a str, campaign_id: &'a str, name: &'a str, level: Option<i32>, school: ...` — Data for inserting a new homebrew spell.
- pub `new` function L45-56 — `(id: &'a str, campaign_id: &'a str, name: &'a str, data: &'a str) -> Self` — Custom spells created by DMs within a campaign, optionally cloned from catalog spells.
- pub `with_level` function L58-61 — `(mut self, level: i32) -> Self` — Custom spells created by DMs within a campaign, optionally cloned from catalog spells.
- pub `with_school` function L63-66 — `(mut self, school: &'a str) -> Self` — Custom spells created by DMs within a campaign, optionally cloned from catalog spells.
- pub `cloned_from` function L68-72 — `(mut self, name: &'a str, source: &'a str) -> Self` — Custom spells created by DMs within a campaign, optionally cloned from catalog spells.
- pub `UpdateCampaignHomebrewSpell` struct L78-84 — `{ name: Option<&'a str>, level: Option<Option<i32>>, school: Option<Option<&'a s...` — Data for updating a homebrew spell.

#### crates/mimir-core/src/models/campaign/campaign_source.rs

- pub `CampaignSource` struct L12-19 — `{ id: String, campaign_id: String, source_code: String }` — A campaign source - links a campaign to an allowed source book.
- pub `NewCampaignSource` struct L24-28 — `{ id: &'a str, campaign_id: &'a str, source_code: &'a str }` — Data for inserting a new campaign source.
- pub `new` function L32-38 — `(id: &'a str, campaign_id: &'a str, source_code: &'a str) -> Self` — Create a new campaign source link.
-  `tests` module L42-52 — `-` — Links campaigns to allowed source books.
-  `test_new_campaign_source` function L46-51 — `()` — Links campaigns to allowed source books.

#### crates/mimir-core/src/models/campaign/character.rs

- pub `Character` struct L20-72 — `{ id: String, campaign_id: Option<String>, name: String, is_npc: i32, player_nam...` — A character - either a player character or NPC in a campaign.
- pub `is_npc` function L76-78 — `(&self) -> bool` — Check if this character is an NPC.
- pub `is_pc` function L81-83 — `(&self) -> bool` — Check if this is a player character.
- pub `ability_modifier` function L87-89 — `(score: i32) -> i32` — Calculate ability modifier for a given score.
- pub `str_mod` function L92-94 — `(&self) -> i32` — Get strength modifier.
- pub `dex_mod` function L97-99 — `(&self) -> i32` — Get dexterity modifier.
- pub `con_mod` function L102-104 — `(&self) -> i32` — Get constitution modifier.
- pub `int_mod` function L107-109 — `(&self) -> i32` — Get intelligence modifier.
- pub `wis_mod` function L112-114 — `(&self) -> i32` — Get wisdom modifier.
- pub `cha_mod` function L117-119 — `(&self) -> i32` — Get charisma modifier.
- pub `total_gold_value` function L122-128 — `(&self) -> f64` — Calculate total gold value of all currency.
- pub `NewCharacter` struct L134-162 — `{ id: &'a str, campaign_id: Option<&'a str>, name: &'a str, is_npc: i32, player_...` — Data for inserting a new character.
- pub `new_pc` function L166-196 — `(id: &'a str, campaign_id: Option<&'a str>, name: &'a str, player_name: &'a str)...` — Create a new player character with default ability scores.
- pub `new_npc` function L199-229 — `(id: &'a str, campaign_id: Option<&'a str>, name: &'a str) -> Self` — Create a new NPC with default ability scores.
- pub `with_race` function L232-236 — `(mut self, name: &'a str, source: &'a str) -> Self` — Set the race.
- pub `with_background` function L239-243 — `(mut self, name: &'a str, source: &'a str) -> Self` — Set the background.
- pub `with_ability_scores` function L246-262 — `( mut self, str: i32, dex: i32, con: i32, int: i32, wis: i32, cha: i32, ) -> Sel...` — Set ability scores.
- pub `with_currency` function L265-272 — `(mut self, cp: i32, sp: i32, ep: i32, gp: i32, pp: i32) -> Self` — Set starting currency.
- pub `with_roleplay` function L275-287 — `( mut self, traits: Option<&'a str>, ideals: Option<&'a str>, bonds: Option<&'a ...` — Set roleplay elements.
- pub `with_npc_info` function L290-300 — `( mut self, role: Option<&'a str>, location: Option<&'a str>, faction: Option<&'...` — Set NPC-specific fields.
- pub `UpdateCharacter` struct L306-332 — `{ name: Option<&'a str>, player_name: Option<Option<&'a str>>, race_name: Option...` — Data for updating a character.
- pub `set_name` function L336-342 — `(name: &'a str, updated_at: &'a str) -> Self` — Update character name.
- pub `set_ability_scores` function L345-364 — `( str: i32, dex: i32, con: i32, int: i32, wis: i32, cha: i32, updated_at: &'a st...` — Update ability scores.
- pub `set_currency` function L367-377 — `(cp: i32, sp: i32, ep: i32, gp: i32, pp: i32, updated_at: &'a str) -> Self` — Update currency.
- pub `set_roleplay` function L380-395 — `( traits: Option<&'a str>, ideals: Option<&'a str>, bonds: Option<&'a str>, flaw...` — Update roleplay elements.
- pub `set_npc_info` function L398-411 — `( role: Option<&'a str>, location: Option<&'a str>, faction: Option<&'a str>, up...` — Update NPC info.
- pub `set_race` function L414-421 — `(name: Option<&'a str>, source: Option<&'a str>, updated_at: &'a str) -> Self` — Update race.
- pub `set_background` function L424-435 — `( name: Option<&'a str>, source: Option<&'a str>, updated_at: &'a str, ) -> Self` — Update background.
- pub `CharacterResponse` struct L535-569 — `{ id: String, campaign_id: Option<String>, name: String, is_npc: i32, player_nam...` — Character with classes included - used for API responses.
- pub `from_character` function L573-611 — `( character: Character, classes: Vec<CharacterClass>, proficiencies: Vec<Charact...` — Create from a Character, its classes, and proficiencies.
- pub `total_level` function L614-616 — `(&self) -> i32` — Get total character level across all classes.
- pub `class_string` function L619-634 — `(&self) -> String` — Format class string (e.g., "Fighter 5 / Rogue 3").
-  `Character` type L74-129 — `= Character` — Player characters and NPCs for campaigns.
-  `tests` module L439-525 — `-` — Player characters and NPCs for campaigns.
-  `test_new_pc` function L443-450 — `()` — Player characters and NPCs for campaigns.
-  `test_new_npc` function L453-458 — `()` — Player characters and NPCs for campaigns.
-  `test_with_ability_scores` function L461-470 — `()` — Player characters and NPCs for campaigns.
-  `test_with_currency` function L473-481 — `()` — Player characters and NPCs for campaigns.
-  `test_with_npc_info` function L484-490 — `()` — Player characters and NPCs for campaigns.
-  `test_ability_modifier` function L493-502 — `()` — Player characters and NPCs for campaigns.
-  `test_update_name` function L505-509 — `()` — Player characters and NPCs for campaigns.
-  `test_update_ability_scores` function L512-517 — `()` — Player characters and NPCs for campaigns.
-  `test_update_currency` function L520-524 — `()` — Player characters and NPCs for campaigns.
-  `CharacterResponse` type L571-635 — `= CharacterResponse` — Player characters and NPCs for campaigns.

#### crates/mimir-core/src/models/campaign/character_class.rs

- pub `CharacterClass` struct L18-35 — `{ id: String, character_id: String, class_name: String, class_source: String, le...` — A character's class level entry.
- pub `is_starting_class` function L39-41 — `(&self) -> bool` — Check if this is the character's starting class.
- pub `has_subclass` function L44-46 — `(&self) -> bool` — Check if a subclass has been selected.
- pub `NewCharacterClass` struct L52-61 — `{ id: &'a str, character_id: &'a str, class_name: &'a str, class_source: &'a str...` — Data for inserting a new character class.
- pub `starting` function L65-81 — `( id: &'a str, character_id: &'a str, class_name: &'a str, class_source: &'a str...` — Create a new starting class entry.
- pub `multiclass` function L84-100 — `( id: &'a str, character_id: &'a str, class_name: &'a str, class_source: &'a str...` — Create a new multiclass entry.
- pub `with_level` function L103-106 — `(mut self, level: i32) -> Self` — Set the class level.
- pub `with_subclass` function L109-113 — `(mut self, name: &'a str, source: &'a str) -> Self` — Set the subclass.
- pub `UpdateCharacterClass` struct L119-123 — `{ level: Option<i32>, subclass_name: Option<Option<&'a str>>, subclass_source: O...` — Data for updating a character class.
- pub `set_level` function L127-132 — `(level: i32) -> Self` — Update the class level.
- pub `set_subclass` function L135-141 — `(name: &'a str, source: &'a str) -> Self` — Set or update the subclass.
- pub `set_level_and_subclass` function L144-150 — `(level: i32, subclass_name: &'a str, subclass_source: &'a str) -> Self` — Update level and subclass together.
-  `CharacterClass` type L37-47 — `= CharacterClass` — Tracks character class levels for both single-class and multiclass characters.
-  `tests` module L154-196 — `-` — Tracks character class levels for both single-class and multiclass characters.
-  `test_new_starting_class` function L158-163 — `()` — Tracks character class levels for both single-class and multiclass characters.
-  `test_new_multiclass` function L166-172 — `()` — Tracks character class levels for both single-class and multiclass characters.
-  `test_with_subclass` function L175-181 — `()` — Tracks character class levels for both single-class and multiclass characters.
-  `test_update_level` function L184-188 — `()` — Tracks character class levels for both single-class and multiclass characters.
-  `test_update_subclass` function L191-195 — `()` — Tracks character class levels for both single-class and multiclass characters.

#### crates/mimir-core/src/models/campaign/character_feat.rs

- pub `CharacterFeat` struct L12-23 — `{ id: String, character_id: String, feat_name: String, feat_source: String, sour...` — A feat selected by a character.
- pub `is_from_asi` function L27-29 — `(&self) -> bool` — Check if this feat was taken via ASI.
- pub `is_from_race` function L32-34 — `(&self) -> bool` — Check if this feat is a racial feat.
- pub `is_from_class` function L37-39 — `(&self) -> bool` — Check if this feat is from a class feature.
- pub `is_bonus` function L42-44 — `(&self) -> bool` — Check if this is a bonus feat (DM granted, etc).
- pub `FeatSourceType` enum L49-58 — `Asi | Race | Class | Bonus` — Source type for how a feat was obtained.
- pub `as_str` function L62-69 — `(&self) -> &'static str` — Convert to string for database storage.
- pub `NewCharacterFeat` struct L75-81 — `{ id: &'a str, character_id: &'a str, feat_name: &'a str, feat_source: &'a str, ...` — Data for inserting a new character feat.
- pub `new` function L85-99 — `( id: &'a str, character_id: &'a str, feat_name: &'a str, feat_source: &'a str, ...` — Create a new feat entry.
- pub `from_asi` function L102-109 — `( id: &'a str, character_id: &'a str, feat_name: &'a str, feat_source: &'a str, ...` — Create an ASI feat (most common).
- pub `from_race` function L112-119 — `( id: &'a str, character_id: &'a str, feat_name: &'a str, feat_source: &'a str, ...` — Create a racial feat.
- pub `from_class` function L122-129 — `( id: &'a str, character_id: &'a str, feat_name: &'a str, feat_source: &'a str, ...` — Create a class-granted feat.
- pub `bonus` function L132-139 — `( id: &'a str, character_id: &'a str, feat_name: &'a str, feat_source: &'a str, ...` — Create a bonus feat (DM granted).
-  `CharacterFeat` type L25-45 — `= CharacterFeat` — Tracks feats selected by characters.
-  `FeatSourceType` type L60-70 — `= FeatSourceType` — Tracks feats selected by characters.
-  `tests` module L143-178 — `-` — Tracks feats selected by characters.
-  `test_new_asi_feat` function L147-151 — `()` — Tracks feats selected by characters.
-  `test_new_racial_feat` function L154-157 — `()` — Tracks feats selected by characters.
-  `test_new_class_feat` function L160-163 — `()` — Tracks feats selected by characters.
-  `test_new_bonus_feat` function L166-169 — `()` — Tracks feats selected by characters.
-  `test_feat_source_type_as_str` function L172-177 — `()` — Tracks feats selected by characters.

#### crates/mimir-core/src/models/campaign/character_feature.rs

- pub `CharacterFeature` struct L12-25 — `{ id: String, character_id: String, feature_type: String, feature_name: String, ...` — A class feature choice made by a character.
- pub `is_fighting_style` function L29-31 — `(&self) -> bool` — Check if this is a fighting style.
- pub `is_metamagic` function L34-36 — `(&self) -> bool` — Check if this is a metamagic option.
- pub `is_maneuver` function L39-41 — `(&self) -> bool` — Check if this is a battle master maneuver.
- pub `is_invocation` function L44-46 — `(&self) -> bool` — Check if this is a warlock invocation.
- pub `is_pact_boon` function L49-51 — `(&self) -> bool` — Check if this is a pact boon.
- pub `FeatureType` enum L56-67 — `FightingStyle | Metamagic | Maneuver | Invocation | PactBoon` — Type of class feature choice.
- pub `as_str` function L71-79 — `(&self) -> &'static str` — Convert to string for database storage.
- pub `from_str` function L82-91 — `(s: &str) -> Option<Self>` — Parse from string.
- pub `NewCharacterFeature` struct L97-104 — `{ id: &'a str, character_id: &'a str, feature_type: &'a str, feature_name: &'a s...` — Data for inserting a new character feature.
- pub `new` function L108-124 — `( id: &'a str, character_id: &'a str, feature_type: FeatureType, feature_name: &...` — Create a new feature entry.
- pub `fighting_style` function L127-135 — `( id: &'a str, character_id: &'a str, name: &'a str, source: &'a str, class: &'a...` — Create a fighting style feature.
- pub `metamagic` function L138-145 — `( id: &'a str, character_id: &'a str, name: &'a str, source: &'a str, ) -> Self` — Create a metamagic option.
- pub `maneuver` function L148-155 — `( id: &'a str, character_id: &'a str, name: &'a str, source: &'a str, ) -> Self` — Create a battle master maneuver.
- pub `invocation` function L158-165 — `( id: &'a str, character_id: &'a str, name: &'a str, source: &'a str, ) -> Self` — Create a warlock invocation.
- pub `pact_boon` function L168-175 — `( id: &'a str, character_id: &'a str, name: &'a str, source: &'a str, ) -> Self` — Create a warlock pact boon.
-  `CharacterFeature` type L27-52 — `= CharacterFeature` — Tracks class feature choices: Fighting Style, Metamagic, Maneuvers, Invocations, Pact Boon.
-  `FeatureType` type L69-92 — `= FeatureType` — Tracks class feature choices: Fighting Style, Metamagic, Maneuvers, Invocations, Pact Boon.
-  `tests` module L179-246 — `-` — Tracks class feature choices: Fighting Style, Metamagic, Maneuvers, Invocations, Pact Boon.
-  `test_feature_type_as_str` function L183-189 — `()` — Tracks class feature choices: Fighting Style, Metamagic, Maneuvers, Invocations, Pact Boon.
-  `test_feature_type_from_str` function L192-199 — `()` — Tracks class feature choices: Fighting Style, Metamagic, Maneuvers, Invocations, Pact Boon.
-  `test_new_fighting_style` function L202-209 — `()` — Tracks class feature choices: Fighting Style, Metamagic, Maneuvers, Invocations, Pact Boon.
-  `test_new_metamagic` function L212-218 — `()` — Tracks class feature choices: Fighting Style, Metamagic, Maneuvers, Invocations, Pact Boon.
-  `test_new_maneuver` function L221-227 — `()` — Tracks class feature choices: Fighting Style, Metamagic, Maneuvers, Invocations, Pact Boon.
-  `test_new_invocation` function L230-236 — `()` — Tracks class feature choices: Fighting Style, Metamagic, Maneuvers, Invocations, Pact Boon.
-  `test_new_pact_boon` function L239-245 — `()` — Tracks class feature choices: Fighting Style, Metamagic, Maneuvers, Invocations, Pact Boon.

#### crates/mimir-core/src/models/campaign/character_inventory.rs

- pub `CharacterInventory` struct L17-34 — `{ id: String, character_id: String, item_name: String, item_source: String, quan...` — An item in a character's inventory.
- pub `is_equipped` function L38-40 — `(&self) -> bool` — Check if this item is equipped.
- pub `is_attuned` function L43-45 — `(&self) -> bool` — Check if this item is attuned.
- pub `NewCharacterInventory` struct L51-60 — `{ id: &'a str, character_id: &'a str, item_name: &'a str, item_source: &'a str, ...` — Data for inserting a new inventory item.
- pub `new` function L64-80 — `( id: &'a str, character_id: &'a str, item_name: &'a str, item_source: &'a str, ...` — Create a new inventory item.
- pub `with_quantity` function L83-86 — `(mut self, quantity: i32) -> Self` — Set the quantity.
- pub `equipped` function L89-92 — `(mut self) -> Self` — Mark as equipped.
- pub `attuned` function L95-98 — `(mut self) -> Self` — Mark as attuned.
- pub `with_notes` function L101-104 — `(mut self, notes: &'a str) -> Self` — Add notes.
- pub `UpdateCharacterInventory` struct L110-115 — `{ quantity: Option<i32>, equipped: Option<i32>, attuned: Option<i32>, notes: Opt...` — Data for updating an inventory item.
- pub `set_quantity` function L119-124 — `(quantity: i32) -> Self` — Update quantity.
- pub `set_equipped` function L127-132 — `(equipped: bool) -> Self` — Set equipped status.
- pub `set_attuned` function L135-140 — `(attuned: bool) -> Self` — Set attuned status.
- pub `set_notes` function L143-148 — `(notes: Option<&'a str>) -> Self` — Set notes.
-  `CharacterInventory` type L36-46 — `= CharacterInventory` — Tracks items held by characters.
-  `tests` module L152-201 — `-` — Tracks items held by characters.
-  `test_new_inventory_item` function L156-162 — `()` — Tracks items held by characters.
-  `test_with_quantity` function L165-169 — `()` — Tracks items held by characters.
-  `test_equipped_and_attuned` function L172-178 — `()` — Tracks items held by characters.
-  `test_with_notes` function L181-185 — `()` — Tracks items held by characters.
-  `test_update_quantity` function L188-191 — `()` — Tracks items held by characters.
-  `test_update_equipped` function L194-200 — `()` — Tracks items held by characters.

#### crates/mimir-core/src/models/campaign/character_proficiency.rs

- pub `CharacterProficiency` struct L17-28 — `{ id: String, character_id: String, proficiency_type: String, name: String, expe...` — A proficiency held by a character.
- pub `has_expertise` function L32-34 — `(&self) -> bool` — Check if this is expertise (double proficiency).
- pub `is_skill` function L37-39 — `(&self) -> bool` — Check if this is a skill proficiency.
- pub `is_save` function L42-44 — `(&self) -> bool` — Check if this is a saving throw proficiency.
- pub `is_tool` function L47-49 — `(&self) -> bool` — Check if this is a tool proficiency.
- pub `is_weapon` function L52-54 — `(&self) -> bool` — Check if this is a weapon proficiency.
- pub `is_armor` function L57-59 — `(&self) -> bool` — Check if this is an armor proficiency.
- pub `is_language` function L62-64 — `(&self) -> bool` — Check if this is a language.
- pub `ProficiencyType` enum L69-76 — `Skill | Save | Tool | Weapon | Armor | Language` — Type of proficiency.
- pub `as_str` function L80-89 — `(&self) -> &'static str` — Convert to string for database storage.
- pub `NewCharacterProficiency` struct L95-101 — `{ id: &'a str, character_id: &'a str, proficiency_type: &'a str, name: &'a str, ...` — Data for inserting a new proficiency.
- pub `new` function L105-118 — `( id: &'a str, character_id: &'a str, proficiency_type: ProficiencyType, name: &...` — Create a new proficiency.
- pub `skill` function L121-123 — `(id: &'a str, character_id: &'a str, name: &'a str) -> Self` — Create a skill proficiency.
- pub `save` function L126-128 — `(id: &'a str, character_id: &'a str, name: &'a str) -> Self` — Create a saving throw proficiency.
- pub `tool` function L131-133 — `(id: &'a str, character_id: &'a str, name: &'a str) -> Self` — Create a tool proficiency.
- pub `weapon` function L136-138 — `(id: &'a str, character_id: &'a str, name: &'a str) -> Self` — Create a weapon proficiency.
- pub `armor` function L141-143 — `(id: &'a str, character_id: &'a str, name: &'a str) -> Self` — Create an armor proficiency.
- pub `language` function L146-148 — `(id: &'a str, character_id: &'a str, name: &'a str) -> Self` — Create a language.
- pub `with_expertise` function L151-154 — `(mut self) -> Self` — Mark as expertise.
- pub `UpdateCharacterProficiency` struct L160-162 — `{ expertise: Option<i32> }` — Data for updating a proficiency.
- pub `set_expertise` function L166-170 — `(has_expertise: bool) -> Self` — Set expertise status.
-  `CharacterProficiency` type L30-65 — `= CharacterProficiency` — Tracks character proficiencies in skills, saves, tools, weapons, armor, and languages.
-  `ProficiencyType` type L78-90 — `= ProficiencyType` — Tracks character proficiencies in skills, saves, tools, weapons, armor, and languages.
-  `UpdateCharacterProficiency` type L164-171 — `= UpdateCharacterProficiency` — Tracks character proficiencies in skills, saves, tools, weapons, armor, and languages.
-  `tests` module L174-241 — `-` — Tracks character proficiencies in skills, saves, tools, weapons, armor, and languages.
-  `test_new_skill_proficiency` function L178-183 — `()` — Tracks character proficiencies in skills, saves, tools, weapons, armor, and languages.
-  `test_new_save_proficiency` function L186-190 — `()` — Tracks character proficiencies in skills, saves, tools, weapons, armor, and languages.
-  `test_skill_with_expertise` function L193-197 — `()` — Tracks character proficiencies in skills, saves, tools, weapons, armor, and languages.
-  `test_tool_proficiency` function L200-203 — `()` — Tracks character proficiencies in skills, saves, tools, weapons, armor, and languages.
-  `test_weapon_proficiency` function L206-209 — `()` — Tracks character proficiencies in skills, saves, tools, weapons, armor, and languages.
-  `test_armor_proficiency` function L212-215 — `()` — Tracks character proficiencies in skills, saves, tools, weapons, armor, and languages.
-  `test_language` function L218-221 — `()` — Tracks character proficiencies in skills, saves, tools, weapons, armor, and languages.
-  `test_proficiency_type_as_str` function L224-231 — `()` — Tracks character proficiencies in skills, saves, tools, weapons, armor, and languages.
-  `test_update_expertise` function L234-240 — `()` — Tracks character proficiencies in skills, saves, tools, weapons, armor, and languages.

#### crates/mimir-core/src/models/campaign/character_source.rs

- pub `CharacterSource` struct L12-19 — `{ id: String, character_id: String, source_code: String }` — A character source - links a character to an allowed source book.
- pub `NewCharacterSource` struct L24-28 — `{ id: &'a str, character_id: &'a str, source_code: &'a str }` — Data for inserting a new character source.
- pub `new` function L32-38 — `(id: &'a str, character_id: &'a str, source_code: &'a str) -> Self` — Create a new character source link.
-  `tests` module L42-52 — `-` — Links characters to allowed source books.
-  `test_new_character_source` function L46-51 — `()` — Links characters to allowed source books.

#### crates/mimir-core/src/models/campaign/character_spell.rs

- pub `CharacterSpell` struct L12-25 — `{ id: String, character_id: String, spell_name: String, spell_source: String, so...` — A spell known or prepared by a character.
- pub `is_prepared` function L29-31 — `(&self) -> bool` — Check if this spell is prepared.
- pub `NewCharacterSpell` struct L37-44 — `{ id: &'a str, character_id: &'a str, spell_name: &'a str, spell_source: &'a str...` — Data for inserting a new character spell.
- pub `new` function L48-63 — `( id: &'a str, character_id: &'a str, spell_name: &'a str, spell_source: &'a str...` — Create a new spell entry.
- pub `prepared` function L66-69 — `(mut self) -> Self` — Mark as prepared.
- pub `UpdateCharacterSpell` struct L75-77 — `{ prepared: Option<i32> }` — Data for updating a character spell.
- pub `set_prepared` function L81-85 — `(is_prepared: bool) -> Self` — Set prepared status.
-  `CharacterSpell` type L27-32 — `= CharacterSpell` — Tracks spells known or prepared by characters.
-  `UpdateCharacterSpell` type L79-86 — `= UpdateCharacterSpell` — Tracks spells known or prepared by characters.
-  `tests` module L89-116 — `-` — Tracks spells known or prepared by characters.
-  `test_new_spell` function L93-99 — `()` — Tracks spells known or prepared by characters.
-  `test_prepared_spell` function L102-106 — `()` — Tracks spells known or prepared by characters.
-  `test_update_prepared` function L109-115 — `()` — Tracks spells known or prepared by characters.

#### crates/mimir-core/src/models/campaign/document.rs

- pub `Document` struct L18-38 — `{ id: String, campaign_id: String, module_id: Option<String>, title: String, con...` — A document - markdown content within a campaign.
- pub `is_module_document` function L42-44 — `(&self) -> bool` — Check if this document belongs to a module.
- pub `is_campaign_document` function L47-49 — `(&self) -> bool` — Check if this is a campaign-level document (not in a module).
- pub `NewDocument` struct L55-63 — `{ id: &'a str, campaign_id: &'a str, module_id: Option<&'a str>, title: &'a str,...` — Data for inserting a new document.
- pub `for_campaign` function L67-82 — `( id: &'a str, campaign_id: &'a str, title: &'a str, doc_type: &'a str, ) -> Sel...` — Create a new campaign-level document.
- pub `for_module` function L85-101 — `( id: &'a str, campaign_id: &'a str, module_id: &'a str, title: &'a str, doc_typ...` — Create a new module-level document.
- pub `with_sort_order` function L104-107 — `(mut self, sort_order: i32) -> Self` — Set the sort order.
- pub `with_content` function L110-113 — `(mut self, content: &'a str) -> Self` — Set the content.
- pub `UpdateDocument` struct L119-125 — `{ title: Option<&'a str>, content: Option<&'a str>, doc_type: Option<&'a str>, m...` — Data for updating a document.
- pub `set_title` function L129-135 — `(title: &'a str, updated_at: &'a str) -> Self` — Create an update to change the title.
- pub `set_content` function L138-144 — `(content: &'a str, updated_at: &'a str) -> Self` — Create an update to change the content.
- pub `set_title_and_content` function L147-154 — `(title: &'a str, content: &'a str, updated_at: &'a str) -> Self` — Create an update to change title and content together.
- pub `set_doc_type` function L157-163 — `(doc_type: &'a str, updated_at: &'a str) -> Self` — Create an update to change the document type.
- pub `move_to_module` function L166-172 — `(module_id: &'a str, updated_at: &'a str) -> Self` — Create an update to move document to a module.
- pub `move_to_campaign` function L175-181 — `(updated_at: &'a str) -> Self` — Create an update to move document out of a module (to campaign level).
-  `Document` type L40-50 — `= Document` — Markdown content storage for campaigns and modules.
-  `tests` module L185-242 — `-` — Markdown content storage for campaigns and modules.
-  `test_new_campaign_document` function L189-197 — `()` — Markdown content storage for campaigns and modules.
-  `test_new_module_document` function L200-207 — `()` — Markdown content storage for campaigns and modules.
-  `test_new_document_with_content` function L210-214 — `()` — Markdown content storage for campaigns and modules.
-  `test_update_document_title` function L217-222 — `()` — Markdown content storage for campaigns and modules.
-  `test_update_document_content` function L225-229 — `()` — Markdown content storage for campaigns and modules.
-  `test_update_document_move_to_module` function L232-235 — `()` — Markdown content storage for campaigns and modules.
-  `test_update_document_move_to_campaign` function L238-241 — `()` — Markdown content storage for campaigns and modules.

#### crates/mimir-core/src/models/campaign/fog.rs

- pub `FogRevealedArea` struct L12-27 — `{ id: String, map_id: String, x: f64, y: f64, width: f64, height: f64, created_a...` — A revealed area in the fog of war.
- pub `NewFogRevealedArea` struct L32-39 — `{ id: &'a str, map_id: &'a str, x: f64, y: f64, width: f64, height: f64 }` — Data for inserting a new revealed area.
- pub `new` function L43-52 — `(id: &'a str, map_id: &'a str, x: f64, y: f64, width: f64, height: f64) -> Self` — Create a new revealed area.
- pub `rect` function L55-57 — `(id: &'a str, map_id: &'a str, x: f64, y: f64, width: f64, height: f64) -> Self` — Create a revealed area from a rectangle.
- pub `circle` function L60-69 — `(id: &'a str, map_id: &'a str, center_x: f64, center_y: f64, radius: f64) -> Sel...` — Create a revealed area from a circle (converted to bounding box).
- pub `FogState` struct L74-81 — `{ map_id: String, fog_enabled: bool, revealed_areas: Vec<FogRevealedArea> }` — Fog state for a map.
- pub `new` function L85-91 — `(map_id: String, fog_enabled: bool, revealed_areas: Vec<FogRevealedArea>) -> Sel...` — Create a new fog state.
-  `FogState` type L83-92 — `= FogState` — Models for managing fog of war revealed areas on maps.
-  `tests` module L95-123 — `-` — Models for managing fog of war revealed areas on maps.
-  `test_new_revealed_area` function L99-105 — `()` — Models for managing fog of war revealed areas on maps.
-  `test_circle_to_rect` function L108-115 — `()` — Models for managing fog of war revealed areas on maps.
-  `test_fog_state` function L118-122 — `()` — Models for managing fog of war revealed areas on maps.

#### crates/mimir-core/src/models/campaign/light_source.rs

- pub `LightSource` struct L12-35 — `{ id: String, map_id: String, grid_x: i32, grid_y: i32, name: Option<String>, br...` — A dynamic light source on a map.
- pub `is_active` function L39-41 — `(&self) -> bool` — Check if this light source is active.
- pub `total_radius` function L44-46 — `(&self) -> i32` — Get the total light radius (dim extends beyond bright).
- pub `presets` module L50-75 — `-` — Common light source presets
- pub `TORCH_BRIGHT` variable L52 — `: i32` — Torch: 20 ft bright, 40 ft dim, warm orange
- pub `TORCH_DIM` variable L53 — `: i32` — Dynamic light sources on maps (beyond UVTT static lights).
- pub `TORCH_COLOR` variable L54 — `: &str` — Dynamic light sources on maps (beyond UVTT static lights).
- pub `LANTERN_BRIGHT` variable L57 — `: i32` — Lantern: 30 ft bright, 60 ft dim, warm yellow
- pub `LANTERN_DIM` variable L58 — `: i32` — Dynamic light sources on maps (beyond UVTT static lights).
- pub `LANTERN_COLOR` variable L59 — `: &str` — Dynamic light sources on maps (beyond UVTT static lights).
- pub `CANDLE_BRIGHT` variable L62 — `: i32` — Candle: 5 ft bright, 10 ft dim, warm orange
- pub `CANDLE_DIM` variable L63 — `: i32` — Dynamic light sources on maps (beyond UVTT static lights).
- pub `CANDLE_COLOR` variable L64 — `: &str` — Dynamic light sources on maps (beyond UVTT static lights).
- pub `LIGHT_BRIGHT` variable L67 — `: i32` — Light spell: 20 ft bright, 40 ft dim, white
- pub `LIGHT_DIM` variable L68 — `: i32` — Dynamic light sources on maps (beyond UVTT static lights).
- pub `LIGHT_COLOR` variable L69 — `: &str` — Dynamic light sources on maps (beyond UVTT static lights).
- pub `DAYLIGHT_BRIGHT` variable L72 — `: i32` — Daylight spell: 60 ft bright, 120 ft dim, bright white
- pub `DAYLIGHT_DIM` variable L73 — `: i32` — Dynamic light sources on maps (beyond UVTT static lights).
- pub `DAYLIGHT_COLOR` variable L74 — `: &str` — Dynamic light sources on maps (beyond UVTT static lights).
- pub `NewLightSource` struct L80-90 — `{ id: &'a str, map_id: &'a str, grid_x: i32, grid_y: i32, name: Option<&'a str>,...` — Data for inserting a new light source.
- pub `new` function L94-113 — `( id: &'a str, map_id: &'a str, grid_x: i32, grid_y: i32, bright_radius: i32, di...` — Create a new light source.
- pub `torch` function L116-127 — `(id: &'a str, map_id: &'a str, grid_x: i32, grid_y: i32) -> Self` — Create a torch light source.
- pub `lantern` function L130-141 — `(id: &'a str, map_id: &'a str, grid_x: i32, grid_y: i32) -> Self` — Create a lantern light source.
- pub `candle` function L144-155 — `(id: &'a str, map_id: &'a str, grid_x: i32, grid_y: i32) -> Self` — Create a candle light source.
- pub `with_name` function L158-161 — `(mut self, name: &'a str) -> Self` — Set the name.
- pub `with_color` function L164-167 — `(mut self, color: &'a str) -> Self` — Set the color.
- pub `inactive` function L170-173 — `(mut self) -> Self` — Create in inactive state.
- pub `UpdateLightSource` struct L179-188 — `{ grid_x: Option<i32>, grid_y: Option<i32>, name: Option<Option<&'a str>>, brigh...` — Data for updating a light source.
- pub `set_position` function L192-199 — `(grid_x: i32, grid_y: i32, updated_at: &'a str) -> Self` — Update position.
- pub `set_radii` function L202-209 — `(bright_radius: i32, dim_radius: i32, updated_at: &'a str) -> Self` — Update radii.
- pub `set_color` function L212-218 — `(color: Option<&'a str>, updated_at: &'a str) -> Self` — Update color.
- pub `turn_on` function L221-227 — `(updated_at: &'a str) -> Self` — Turn light on.
- pub `turn_off` function L230-236 — `(updated_at: &'a str) -> Self` — Turn light off.
-  `LightSource` type L37-47 — `= LightSource` — Dynamic light sources on maps (beyond UVTT static lights).
-  `tests` module L240-305 — `-` — Dynamic light sources on maps (beyond UVTT static lights).
-  `test_new_light_source` function L244-249 — `()` — Dynamic light sources on maps (beyond UVTT static lights).
-  `test_torch_preset` function L252-258 — `()` — Dynamic light sources on maps (beyond UVTT static lights).
-  `test_lantern_preset` function L261-266 — `()` — Dynamic light sources on maps (beyond UVTT static lights).
-  `test_candle_preset` function L269-273 — `()` — Dynamic light sources on maps (beyond UVTT static lights).
-  `test_inactive` function L276-279 — `()` — Dynamic light sources on maps (beyond UVTT static lights).
-  `test_with_name_and_color` function L282-288 — `()` — Dynamic light sources on maps (beyond UVTT static lights).
-  `test_update_turn_on_off` function L291-297 — `()` — Dynamic light sources on maps (beyond UVTT static lights).
-  `test_update_position` function L300-304 — `()` — Dynamic light sources on maps (beyond UVTT static lights).

#### crates/mimir-core/src/models/campaign/map.rs

- pub `Map` struct L18-41 — `{ id: String, campaign_id: String, module_id: Option<String>, name: String, desc...` — A map in a campaign or module.
- pub `is_module_map` function L45-47 — `(&self) -> bool` — Check if this is a module-level map.
- pub `is_campaign_map` function L50-52 — `(&self) -> bool` — Check if this is a campaign-level map (e.g., world map).
- pub `is_bright` function L55-57 — `(&self) -> bool` — Check if lighting mode is bright.
- pub `is_dim` function L60-62 — `(&self) -> bool` — Check if lighting mode is dim.
- pub `is_dark` function L65-67 — `(&self) -> bool` — Check if lighting mode is dark.
- pub `is_fog_enabled` function L70-72 — `(&self) -> bool` — Check if fog of war is enabled.
- pub `LightingMode` enum L77-84 — `Bright | Dim | Dark` — Lighting mode for initial play state.
- pub `as_str` function L88-94 — `(&self) -> &'static str` — Convert to string for database storage.
- pub `from_str` function L97-104 — `(s: &str) -> Option<Self>` — Parse from string.
- pub `NewMap` struct L116-126 — `{ id: &'a str, campaign_id: &'a str, module_id: Option<&'a str>, name: &'a str, ...` — Data for inserting a new map.
- pub `for_campaign` function L130-147 — `( id: &'a str, campaign_id: &'a str, name: &'a str, uvtt_asset_id: &'a str, ) ->...` — Create a new campaign-level map (e.g., world map).
- pub `for_module` function L150-168 — `( id: &'a str, campaign_id: &'a str, module_id: &'a str, name: &'a str, uvtt_ass...` — Create a new module-level map (e.g., dungeon map).
- pub `with_description` function L171-174 — `(mut self, description: &'a str) -> Self` — Set description.
- pub `with_sort_order` function L177-180 — `(mut self, sort_order: i32) -> Self` — Set sort order.
- pub `with_lighting_mode` function L183-186 — `(mut self, mode: LightingMode) -> Self` — Set lighting mode.
- pub `UpdateMap` struct L192-200 — `{ name: Option<&'a str>, description: Option<Option<&'a str>>, sort_order: Optio...` — Data for updating a map.
- pub `set_name` function L204-210 — `(name: &'a str, updated_at: &'a str) -> Self` — Update the name.
- pub `set_description` function L213-219 — `(description: Option<&'a str>, updated_at: &'a str) -> Self` — Update the description.
- pub `set_sort_order` function L222-228 — `(sort_order: i32, updated_at: &'a str) -> Self` — Update the sort order.
- pub `set_lighting_mode` function L231-237 — `(mode: LightingMode, updated_at: &'a str) -> Self` — Update the lighting mode.
- pub `enable_fog` function L240-246 — `(updated_at: &'a str) -> Self` — Enable fog of war.
- pub `disable_fog` function L249-255 — `(updated_at: &'a str) -> Self` — Disable fog of war.
- pub `set_fog_enabled` function L258-264 — `(enabled: bool, updated_at: &'a str) -> Self` — Set fog enabled state.
- pub `move_to_module` function L267-273 — `(module_id: &'a str, updated_at: &'a str) -> Self` — Move map to a module.
- pub `move_to_campaign` function L276-282 — `(updated_at: &'a str) -> Self` — Move map to campaign level (out of module).
-  `Map` type L43-73 — `= Map` — for grid, walls, and lighting geometry.
-  `LightingMode` type L86-105 — `= LightingMode` — for grid, walls, and lighting geometry.
-  `LightingMode` type L107-111 — `impl Default for LightingMode` — for grid, walls, and lighting geometry.
-  `default` function L108-110 — `() -> Self` — for grid, walls, and lighting geometry.
-  `tests` module L286-350 — `-` — for grid, walls, and lighting geometry.
-  `test_new_campaign_map` function L290-295 — `()` — for grid, walls, and lighting geometry.
-  `test_new_module_map` function L298-302 — `()` — for grid, walls, and lighting geometry.
-  `test_with_lighting_mode` function L305-309 — `()` — for grid, walls, and lighting geometry.
-  `test_with_sort_order` function L312-316 — `()` — for grid, walls, and lighting geometry.
-  `test_lighting_mode_as_str` function L319-323 — `()` — for grid, walls, and lighting geometry.
-  `test_lighting_mode_from_str` function L326-331 — `()` — for grid, walls, and lighting geometry.
-  `test_update_lighting_mode` function L334-337 — `()` — for grid, walls, and lighting geometry.
-  `test_update_move_to_module` function L340-343 — `()` — for grid, walls, and lighting geometry.
-  `test_update_move_to_campaign` function L346-349 — `()` — for grid, walls, and lighting geometry.

#### crates/mimir-core/src/models/campaign/map_poi.rs

- pub `MapPoi` struct L13-25 — `{ id: String, map_id: String, grid_x: i32, grid_y: i32, name: String, descriptio...` — A point of interest placed on a map.
- pub `is_visible` function L29-31 — `(&self) -> bool` — Check if POI is visible to players.
- pub `NewMapPoi` struct L37-47 — `{ id: &'a str, map_id: &'a str, grid_x: i32, grid_y: i32, name: &'a str, descrip...` — Data for creating a new POI.
- pub `new` function L51-63 — `(id: &'a str, map_id: &'a str, name: &'a str, grid_x: i32, grid_y: i32) -> Self` — Create a new POI with required fields.
- pub `with_description` function L66-69 — `(mut self, description: &'a str) -> Self` — Set description.
- pub `with_icon` function L72-75 — `(mut self, icon: &'a str) -> Self` — Set icon type.
- pub `with_color` function L78-81 — `(mut self, color: &'a str) -> Self` — Set color.
- pub `visible` function L84-87 — `(mut self) -> Self` — Set as visible to players.
- pub `UpdateMapPoi` struct L93-102 — `{ grid_x: Option<i32>, grid_y: Option<i32>, name: Option<&'a str>, description: ...` — Data for updating an existing POI.
- pub `set_position` function L106-113 — `(grid_x: i32, grid_y: i32, updated_at: &'a str) -> Self` — Update position.
- pub `set_visible` function L116-122 — `(visible: bool, updated_at: &'a str) -> Self` — Set visibility.
-  `MapPoi` type L27-32 — `= MapPoi` — Points of Interest (POIs) placed on maps - markers for locations, secrets, notes, etc.
-  `tests` module L126-170 — `-` — Points of Interest (POIs) placed on maps - markers for locations, secrets, notes, etc.
-  `test_new_poi` function L130-139 — `()` — Points of Interest (POIs) placed on maps - markers for locations, secrets, notes, etc.
-  `test_new_poi_with_options` function L142-153 — `()` — Points of Interest (POIs) placed on maps - markers for locations, secrets, notes, etc.
-  `test_update_position` function L156-160 — `()` — Points of Interest (POIs) placed on maps - markers for locations, secrets, notes, etc.
-  `test_update_visible` function L163-169 — `()` — Points of Interest (POIs) placed on maps - markers for locations, secrets, notes, etc.

#### crates/mimir-core/src/models/campaign/map_trap.rs

- pub `MapTrap` struct L12-39 — `{ id: String, map_id: String, grid_x: i32, grid_y: i32, name: String, descriptio...` — A trap placed on a map.
- pub `is_triggered` function L43-45 — `(&self) -> bool` — Check if this trap has been triggered.
- pub `is_visible` function L48-50 — `(&self) -> bool` — Check if this trap is visible to players.
- pub `is_armed` function L53-55 — `(&self) -> bool` — Check if this trap is still armed (not triggered).
- pub `NewMapTrap` struct L61-73 — `{ id: &'a str, map_id: &'a str, grid_x: i32, grid_y: i32, name: &'a str, descrip...` — Data for inserting a new map trap.
- pub `new` function L77-91 — `(id: &'a str, map_id: &'a str, name: &'a str, grid_x: i32, grid_y: i32) -> Self` — Create a new map trap.
- pub `with_description` function L94-97 — `(mut self, description: &'a str) -> Self` — Set description.
- pub `with_trigger` function L100-103 — `(mut self, trigger: &'a str) -> Self` — Set trigger description.
- pub `with_effect` function L106-109 — `(mut self, effect: &'a str) -> Self` — Set effect description.
- pub `with_dc` function L112-115 — `(mut self, dc: i32) -> Self` — Set detection/disarm DC.
- pub `visible` function L118-121 — `(mut self) -> Self` — Mark as visible to players.
- pub `UpdateMapTrap` struct L127-138 — `{ grid_x: Option<i32>, grid_y: Option<i32>, name: Option<&'a str>, description: ...` — Data for updating a map trap.
- pub `set_position` function L142-149 — `(grid_x: i32, grid_y: i32, updated_at: &'a str) -> Self` — Update position.
- pub `trigger` function L152-158 — `(updated_at: &'a str) -> Self` — Mark trap as triggered.
- pub `reset` function L161-167 — `(updated_at: &'a str) -> Self` — Reset trap to armed state.
- pub `set_visible` function L170-176 — `(visible: bool, updated_at: &'a str) -> Self` — Set visibility to players.
- pub `set_dc` function L179-185 — `(dc: Option<i32>, updated_at: &'a str) -> Self` — Update DC.
-  `MapTrap` type L41-56 — `= MapTrap` — Trap placements on maps for encounter building.
-  `tests` module L189-245 — `-` — Trap placements on maps for encounter building.
-  `test_new_map_trap` function L193-200 — `()` — Trap placements on maps for encounter building.
-  `test_with_descriptions` function L203-211 — `()` — Trap placements on maps for encounter building.
-  `test_with_dc` function L214-217 — `()` — Trap placements on maps for encounter building.
-  `test_visible` function L220-223 — `()` — Trap placements on maps for encounter building.
-  `test_update_trigger` function L226-229 — `()` — Trap placements on maps for encounter building.
-  `test_update_reset` function L232-235 — `()` — Trap placements on maps for encounter building.
-  `test_update_visible` function L238-244 — `()` — Trap placements on maps for encounter building.

#### crates/mimir-core/src/models/campaign/mod.rs

-  `campaign` module L5 — `-` — Models for campaign management including campaigns, modules, sources, assets, documents, characters, maps, module entities, and map overlays.
-  `campaign_asset` module L6 — `-` — Models for campaign management including campaigns, modules, sources, assets, documents, characters, maps, module entities, and map overlays.
-  `campaign_homebrew_item` module L7 — `-` — Models for campaign management including campaigns, modules, sources, assets, documents, characters, maps, module entities, and map overlays.
-  `campaign_homebrew_monster` module L8 — `-` — Models for campaign management including campaigns, modules, sources, assets, documents, characters, maps, module entities, and map overlays.
-  `campaign_homebrew_spell` module L9 — `-` — Models for campaign management including campaigns, modules, sources, assets, documents, characters, maps, module entities, and map overlays.
-  `campaign_source` module L10 — `-` — Models for campaign management including campaigns, modules, sources, assets, documents, characters, maps, module entities, and map overlays.
-  `character` module L11 — `-` — Models for campaign management including campaigns, modules, sources, assets, documents, characters, maps, module entities, and map overlays.
-  `character_class` module L12 — `-` — Models for campaign management including campaigns, modules, sources, assets, documents, characters, maps, module entities, and map overlays.
-  `character_feat` module L13 — `-` — Models for campaign management including campaigns, modules, sources, assets, documents, characters, maps, module entities, and map overlays.
-  `character_feature` module L14 — `-` — Models for campaign management including campaigns, modules, sources, assets, documents, characters, maps, module entities, and map overlays.
-  `character_inventory` module L15 — `-` — Models for campaign management including campaigns, modules, sources, assets, documents, characters, maps, module entities, and map overlays.
-  `character_proficiency` module L16 — `-` — Models for campaign management including campaigns, modules, sources, assets, documents, characters, maps, module entities, and map overlays.
-  `character_source` module L17 — `-` — Models for campaign management including campaigns, modules, sources, assets, documents, characters, maps, module entities, and map overlays.
-  `character_spell` module L18 — `-` — Models for campaign management including campaigns, modules, sources, assets, documents, characters, maps, module entities, and map overlays.
-  `document` module L19 — `-` — Models for campaign management including campaigns, modules, sources, assets, documents, characters, maps, module entities, and map overlays.
-  `fog` module L20 — `-` — Models for campaign management including campaigns, modules, sources, assets, documents, characters, maps, module entities, and map overlays.
-  `light_source` module L21 — `-` — Models for campaign management including campaigns, modules, sources, assets, documents, characters, maps, module entities, and map overlays.
-  `map` module L22 — `-` — Models for campaign management including campaigns, modules, sources, assets, documents, characters, maps, module entities, and map overlays.
-  `map_poi` module L23 — `-` — Models for campaign management including campaigns, modules, sources, assets, documents, characters, maps, module entities, and map overlays.
-  `map_trap` module L24 — `-` — Models for campaign management including campaigns, modules, sources, assets, documents, characters, maps, module entities, and map overlays.
-  `module` module L25 — `-` — Models for campaign management including campaigns, modules, sources, assets, documents, characters, maps, module entities, and map overlays.
-  `module_monster` module L26 — `-` — Models for campaign management including campaigns, modules, sources, assets, documents, characters, maps, module entities, and map overlays.
-  `module_npc` module L27 — `-` — Models for campaign management including campaigns, modules, sources, assets, documents, characters, maps, module entities, and map overlays.
-  `token_placement` module L28 — `-` — Models for campaign management including campaigns, modules, sources, assets, documents, characters, maps, module entities, and map overlays.

#### crates/mimir-core/src/models/campaign/module.rs

- pub `Module` struct L17-32 — `{ id: String, campaign_id: String, name: String, description: Option<String>, mo...` — A module - an adventure chapter container within a campaign.
- pub `NewModule` struct L37-43 — `{ id: &'a str, campaign_id: &'a str, name: &'a str, description: Option<&'a str>...` — Data for inserting a new module.
- pub `new` function L47-55 — `(id: &'a str, campaign_id: &'a str, name: &'a str, module_number: i32) -> Self` — Create a new module.
- pub `with_description` function L58-61 — `(mut self, description: &'a str) -> Self` — Set the description.
- pub `UpdateModule` struct L67-72 — `{ name: Option<&'a str>, description: Option<Option<&'a str>>, module_number: Op...` — Data for updating a module.
- pub `set_name` function L76-82 — `(name: &'a str, updated_at: &'a str) -> Self` — Create an update to change the name.
- pub `set_description` function L85-91 — `(description: Option<&'a str>, updated_at: &'a str) -> Self` — Create an update to change the description.
- pub `set_module_number` function L94-100 — `(module_number: i32, updated_at: &'a str) -> Self` — Create an update to change the module number.
-  `tests` module L104-136 — `-` — Organizational container for adventure chapters within a campaign.
-  `test_new_module` function L108-115 — `()` — Organizational container for adventure chapters within a campaign.
-  `test_new_module_with_description` function L118-122 — `()` — Organizational container for adventure chapters within a campaign.
-  `test_update_module_name` function L125-129 — `()` — Organizational container for adventure chapters within a campaign.
-  `test_update_module_number` function L132-135 — `()` — Organizational container for adventure chapters within a campaign.

#### crates/mimir-core/src/models/campaign/module_monster.rs

- pub `ModuleMonster` struct L12-33 — `{ id: String, module_id: String, monster_name: Option<String>, monster_source: O...` — A monster instance in a module, referencing either the catalog or a homebrew monster.
- pub `effective_name` function L37-42 — `(&self) -> &str` — Get the effective display name (custom name, or catalog name if available).
- pub `has_custom_name` function L45-47 — `(&self) -> bool` — Check if this monster has a custom display name.
- pub `is_homebrew` function L50-52 — `(&self) -> bool` — Check if this is a homebrew monster reference.
- pub `is_catalog` function L55-57 — `(&self) -> bool` — Check if this is a catalog monster reference.
- pub `NewModuleMonster` struct L63-72 — `{ id: &'a str, module_id: &'a str, monster_name: Option<&'a str>, monster_source...` — Data for inserting a new module monster.
- pub `new` function L76-92 — `( id: &'a str, module_id: &'a str, monster_name: &'a str, monster_source: &'a st...` — Create a new module monster from catalog reference.
- pub `from_homebrew` function L95-110 — `( id: &'a str, module_id: &'a str, homebrew_monster_id: &'a str, ) -> Self` — Create a new module monster from a homebrew monster reference.
- pub `with_display_name` function L113-116 — `(mut self, name: &'a str) -> Self` — Set a custom display name.
- pub `with_notes` function L119-122 — `(mut self, notes: &'a str) -> Self` — Set DM notes.
- pub `with_quantity` function L125-128 — `(mut self, quantity: i32) -> Self` — Set quantity for encounters.
- pub `UpdateModuleMonster` struct L134-139 — `{ display_name: Option<Option<&'a str>>, notes: Option<Option<&'a str>>, quantit...` — Data for updating a module monster.
- pub `set_display_name` function L143-149 — `(name: Option<&'a str>, updated_at: &'a str) -> Self` — Update the display name.
- pub `set_notes` function L152-158 — `(notes: Option<&'a str>, updated_at: &'a str) -> Self` — Update the notes.
- pub `set_quantity` function L161-167 — `(quantity: i32, updated_at: &'a str) -> Self` — Update the quantity.
-  `ModuleMonster` type L35-58 — `= ModuleMonster` — Catalog monster instances with optional customizations for use in modules.
-  `tests` module L171-231 — `-` — Catalog monster instances with optional customizations for use in modules.
-  `test_new_catalog_module_monster` function L175-182 — `()` — Catalog monster instances with optional customizations for use in modules.
-  `test_new_homebrew_module_monster` function L185-191 — `()` — Catalog monster instances with optional customizations for use in modules.
-  `test_with_display_name` function L194-198 — `()` — Catalog monster instances with optional customizations for use in modules.
-  `test_with_quantity` function L201-205 — `()` — Catalog monster instances with optional customizations for use in modules.
-  `test_with_notes` function L208-212 — `()` — Catalog monster instances with optional customizations for use in modules.
-  `test_homebrew_with_builders` function L215-224 — `()` — Catalog monster instances with optional customizations for use in modules.
-  `test_update_quantity` function L227-230 — `()` — Catalog monster instances with optional customizations for use in modules.

#### crates/mimir-core/src/models/campaign/module_npc.rs

- pub `ModuleNpc` struct L12-39 — `{ id: String, module_id: String, name: String, role: Option<String>, description...` — A custom NPC created by the DM for a module.
- pub `has_stat_block` function L43-45 — `(&self) -> bool` — Check if this NPC has a stat block.
- pub `has_token` function L48-50 — `(&self) -> bool` — Check if this NPC has a custom token.
- pub `NewModuleNpc` struct L56-68 — `{ id: &'a str, module_id: &'a str, name: &'a str, role: Option<&'a str>, descrip...` — Data for inserting a new module NPC.
- pub `new` function L72-86 — `(id: &'a str, module_id: &'a str, name: &'a str) -> Self` — Create a new module NPC with just a name.
- pub `with_role` function L89-92 — `(mut self, role: &'a str) -> Self` — Set the role.
- pub `with_description` function L95-98 — `(mut self, description: &'a str) -> Self` — Set the description.
- pub `with_appearance` function L101-104 — `(mut self, appearance: &'a str) -> Self` — Set the appearance.
- pub `with_personality` function L107-110 — `(mut self, personality: &'a str) -> Self` — Set personality traits.
- pub `with_motivation` function L113-116 — `(mut self, motivation: &'a str) -> Self` — Set motivation.
- pub `with_secrets` function L119-122 — `(mut self, secrets: &'a str) -> Self` — Set secrets (DM-only).
- pub `with_stat_block` function L125-128 — `(mut self, stat_block: &'a str) -> Self` — Set the stat block JSON.
- pub `with_token` function L131-134 — `(mut self, token_asset_id: &'a str) -> Self` — Set the token asset.
- pub `UpdateModuleNpc` struct L140-151 — `{ name: Option<&'a str>, role: Option<Option<&'a str>>, description: Option<Opti...` — Data for updating a module NPC.
- pub `set_name` function L155-161 — `(name: &'a str, updated_at: &'a str) -> Self` — Update the name.
- pub `set_role` function L164-170 — `(role: Option<&'a str>, updated_at: &'a str) -> Self` — Update the role.
- pub `set_description` function L173-179 — `(description: Option<&'a str>, updated_at: &'a str) -> Self` — Update the description.
- pub `set_secrets` function L182-188 — `(secrets: Option<&'a str>, updated_at: &'a str) -> Self` — Update secrets.
- pub `set_token` function L191-197 — `(token_asset_id: Option<&'a str>, updated_at: &'a str) -> Self` — Update the token asset.
-  `ModuleNpc` type L41-51 — `= ModuleNpc` — Custom NPCs created by the DM for use in modules.
-  `tests` module L201-246 — `-` — Custom NPCs created by the DM for use in modules.
-  `test_new_module_npc` function L205-209 — `()` — Custom NPCs created by the DM for use in modules.
-  `test_with_role` function L212-216 — `()` — Custom NPCs created by the DM for use in modules.
-  `test_with_description` function L219-223 — `()` — Custom NPCs created by the DM for use in modules.
-  `test_with_personality_and_motivation` function L226-232 — `()` — Custom NPCs created by the DM for use in modules.
-  `test_with_secrets` function L235-239 — `()` — Custom NPCs created by the DM for use in modules.
-  `test_update_name` function L242-245 — `()` — Custom NPCs created by the DM for use in modules.

#### crates/mimir-core/src/models/campaign/token_placement.rs

- pub `TokenPlacement` struct L13-42 — `{ id: String, map_id: String, module_monster_id: Option<String>, module_npc_id: ...` — A token placement on a map, representing either a monster or NPC.
- pub `is_monster` function L46-48 — `(&self) -> bool` — Check if this is a monster token.
- pub `is_npc` function L51-53 — `(&self) -> bool` — Check if this is an NPC token.
- pub `is_hidden` function L56-58 — `(&self) -> bool` — Check if this token is hidden from players.
- pub `NewTokenPlacement` struct L64-78 — `{ id: &'a str, map_id: &'a str, module_monster_id: Option<&'a str>, module_npc_i...` — Data for inserting a new token placement.
- pub `for_monster` function L82-104 — `( id: &'a str, map_id: &'a str, module_monster_id: &'a str, grid_x: i32, grid_y:...` — Create a new token placement for a monster.
- pub `for_npc` function L107-129 — `( id: &'a str, map_id: &'a str, module_npc_id: &'a str, grid_x: i32, grid_y: i32...` — Create a new token placement for an NPC.
- pub `with_label` function L132-135 — `(mut self, label: &'a str) -> Self` — Set a custom label.
- pub `with_faction_color` function L138-141 — `(mut self, color: &'a str) -> Self` — Set faction color.
- pub `hidden` function L144-147 — `(mut self) -> Self` — Mark as hidden from players.
- pub `UpdateTokenPlacement` struct L153-163 — `{ grid_x: Option<i32>, grid_y: Option<i32>, label: Option<Option<&'a str>>, fact...` — Data for updating a token placement.
- pub `set_position` function L167-173 — `(grid_x: i32, grid_y: i32) -> Self` — Update position.
- pub `set_label` function L176-181 — `(label: Option<&'a str>) -> Self` — Update label.
- pub `set_faction_color` function L184-189 — `(color: Option<&'a str>) -> Self` — Update faction color.
- pub `set_hidden` function L192-197 — `(hidden: bool) -> Self` — Update hidden state.
- pub `set_vision` function L200-213 — `( vision_bright_ft: Option<i32>, vision_dim_ft: Option<i32>, vision_dark_ft: i32...` — Update vision settings.
-  `TokenPlacement` type L44-59 — `= TokenPlacement` — PCs are placed at runtime via frontend state, not persisted here.
-  `tests` module L217-272 — `-` — PCs are placed at runtime via frontend state, not persisted here.
-  `test_new_monster_placement` function L221-228 — `()` — PCs are placed at runtime via frontend state, not persisted here.
-  `test_new_npc_placement` function L231-235 — `()` — PCs are placed at runtime via frontend state, not persisted here.
-  `test_with_label` function L238-242 — `()` — PCs are placed at runtime via frontend state, not persisted here.
-  `test_with_faction_color` function L245-249 — `()` — PCs are placed at runtime via frontend state, not persisted here.
-  `test_hidden` function L252-255 — `()` — PCs are placed at runtime via frontend state, not persisted here.
-  `test_update_position` function L258-262 — `()` — PCs are placed at runtime via frontend state, not persisted here.
-  `test_update_hidden` function L265-271 — `()` — PCs are placed at runtime via frontend state, not persisted here.

### crates/mimir-core/src/models/catalog

> *Semantic summary to be generated by AI agent.*

#### crates/mimir-core/src/models/catalog/action.rs

- pub `Action` struct L13-18 — `{ id: Option<i32>, name: String, source: String, data: String }` — A combat action from the catalog.
- pub `parse_data` function L21-23 — `(&self) -> Result<serde_json::Value, serde_json::Error>` — Represents a combat action (Dash, Dodge, Help, Hide, etc.).
- pub `NewAction` struct L28-32 — `{ name: &'a str, source: &'a str, data: &'a str }` — Represents a combat action (Dash, Dodge, Help, Hide, etc.).
- pub `new` function L35-37 — `(name: &'a str, source: &'a str, data: &'a str) -> Self` — Represents a combat action (Dash, Dodge, Help, Hide, etc.).
- pub `ActionFilter` struct L42-48 — `{ name_contains: Option<String>, source: Option<String>, sources: Option<Vec<Str...` — Filters for searching actions.
- pub `new` function L51-53 — `() -> Self` — Represents a combat action (Dash, Dodge, Help, Hide, etc.).
- pub `with_name_contains` function L55-58 — `(mut self, name: impl Into<String>) -> Self` — Represents a combat action (Dash, Dodge, Help, Hide, etc.).
- pub `with_source` function L60-63 — `(mut self, source: impl Into<String>) -> Self` — Represents a combat action (Dash, Dodge, Help, Hide, etc.).
- pub `with_sources` function L65-68 — `(mut self, sources: Vec<String>) -> Self` — Represents a combat action (Dash, Dodge, Help, Hide, etc.).
- pub `has_empty_sources_filter` function L71-73 — `(&self) -> bool` — Returns true if sources filter is explicitly set to an empty array.
- pub `effective_sources` function L76-82 — `(&self) -> Option<Vec<String>>` — Get effective sources list (combines single source and sources array).
-  `Action` type L20-24 — `= Action` — Represents a combat action (Dash, Dodge, Help, Hide, etc.).
-  `ActionFilter` type L50-83 — `= ActionFilter` — Represents a combat action (Dash, Dodge, Help, Hide, etc.).
-  `tests` module L86-94 — `-` — Represents a combat action (Dash, Dodge, Help, Hide, etc.).
-  `test_new_action` function L90-93 — `()` — Represents a combat action (Dash, Dodge, Help, Hide, etc.).

#### crates/mimir-core/src/models/catalog/background.rs

- pub `Background` struct L13-24 — `{ id: Option<i32>, name: String, source: String, data: String, fluff: Option<Str...` — A character background from the catalog.
- pub `parse_data` function L28-30 — `(&self) -> Result<serde_json::Value, serde_json::Error>` — Parse the JSON data blob into a serde_json::Value.
- pub `NewBackground` struct L36-41 — `{ name: &'a str, source: &'a str, data: &'a str, fluff: Option<&'a str> }` — Data for inserting a new background.
- pub `new` function L45-47 — `(name: &'a str, source: &'a str, data: &'a str) -> Self` — Create a new background entry.
- pub `BackgroundFilter` struct L52-58 — `{ name_contains: Option<String>, source: Option<String>, sources: Option<Vec<Str...` — Filters for searching backgrounds.
- pub `new` function L61-63 — `() -> Self` — Represents a character background in the catalog (Acolyte, Criminal, etc.).
- pub `with_name_contains` function L65-68 — `(mut self, name: impl Into<String>) -> Self` — Represents a character background in the catalog (Acolyte, Criminal, etc.).
- pub `with_source` function L70-73 — `(mut self, source: impl Into<String>) -> Self` — Represents a character background in the catalog (Acolyte, Criminal, etc.).
- pub `with_sources` function L75-78 — `(mut self, sources: Vec<String>) -> Self` — Represents a character background in the catalog (Acolyte, Criminal, etc.).
- pub `has_empty_sources_filter` function L81-83 — `(&self) -> bool` — Returns true if sources filter is explicitly set to an empty array.
- pub `effective_sources` function L86-92 — `(&self) -> Option<Vec<String>>` — Get effective sources list (combines single source and sources array).
-  `Background` type L26-31 — `= Background` — Represents a character background in the catalog (Acolyte, Criminal, etc.).
-  `BackgroundFilter` type L60-93 — `= BackgroundFilter` — Represents a character background in the catalog (Acolyte, Criminal, etc.).
-  `tests` module L96-106 — `-` — Represents a character background in the catalog (Acolyte, Criminal, etc.).
-  `test_new_background` function L100-105 — `()` — Represents a character background in the catalog (Acolyte, Criminal, etc.).

#### crates/mimir-core/src/models/catalog/book.rs

- pub `Book` struct L12-25 — `{ id: Option<i32>, source: String, name: String, data: String, contents: Option<...` — A book with its full content (chapters, sections, entries).
- pub `NewBook` struct L30-36 — `{ source: &'a str, name: &'a str, data: &'a str, contents: Option<&'a str>, cove...` — Data for inserting a new book.
- pub `new` function L45-53 — `(source: &'a str, name: &'a str, data: &'a str) -> Self` — Create a new book entry.
- pub `with_contents` function L56-59 — `(mut self, contents: &'a str) -> Self` — Set the table of contents.
- pub `with_cover_path` function L62-65 — `(mut self, cover_path: &'a str) -> Self` — Set the cover image path.
- pub `BookContent` struct L70-81 — `{ id: String, name: String, data: serde_json::Value, contents: Option<serde_json...` — Response structure for book content (what frontend expects).
-  `BookContent` type L83-93 — `= BookContent` — Represents book content from 5etools (PHB, DMG, etc.
-  `from` function L84-92 — `(book: Book) -> Self` — Represents book content from 5etools (PHB, DMG, etc.
-  `tests` module L96-118 — `-` — Represents book content from 5etools (PHB, DMG, etc.
-  `test_new_book` function L100-107 — `()` — Represents book content from 5etools (PHB, DMG, etc.
-  `test_new_book_with_contents` function L110-117 — `()` — Represents book content from 5etools (PHB, DMG, etc.

#### crates/mimir-core/src/models/catalog/class.rs

- pub `Class` struct L13-24 — `{ id: Option<i32>, name: String, source: String, data: String, fluff: Option<Str...` — A character class from the catalog.
- pub `parse_data` function L28-30 — `(&self) -> Result<serde_json::Value, serde_json::Error>` — Parse the JSON data blob into a serde_json::Value.
- pub `NewClass` struct L36-41 — `{ name: &'a str, source: &'a str, data: &'a str, fluff: Option<&'a str> }` — Data for inserting a new class.
- pub `new` function L45-47 — `(name: &'a str, source: &'a str, data: &'a str) -> Self` — Create a new class entry.
- pub `ClassFilter` struct L52-58 — `{ name_contains: Option<String>, source: Option<String>, sources: Option<Vec<Str...` — Filters for searching classes.
- pub `new` function L61-63 — `() -> Self` — Represents a character class in the catalog (Wizard, Fighter, etc.).
- pub `with_name_contains` function L65-68 — `(mut self, name: impl Into<String>) -> Self` — Represents a character class in the catalog (Wizard, Fighter, etc.).
- pub `with_source` function L70-73 — `(mut self, source: impl Into<String>) -> Self` — Represents a character class in the catalog (Wizard, Fighter, etc.).
- pub `with_sources` function L75-78 — `(mut self, sources: Vec<String>) -> Self` — Represents a character class in the catalog (Wizard, Fighter, etc.).
- pub `has_empty_sources_filter` function L81-83 — `(&self) -> bool` — Returns true if sources filter is explicitly set to an empty array.
- pub `effective_sources` function L86-92 — `(&self) -> Option<Vec<String>>` — Get effective sources list (combines single source and sources array).
-  `Class` type L26-31 — `= Class` — Represents a character class in the catalog (Wizard, Fighter, etc.).
-  `ClassFilter` type L60-93 — `= ClassFilter` — Represents a character class in the catalog (Wizard, Fighter, etc.).
-  `tests` module L96-106 — `-` — Represents a character class in the catalog (Wizard, Fighter, etc.).
-  `test_new_class` function L100-105 — `()` — Represents a character class in the catalog (Wizard, Fighter, etc.).

#### crates/mimir-core/src/models/catalog/class_feature.rs

- pub `ClassFeature` struct L13-28 — `{ id: Option<i32>, name: String, source: String, class_name: String, class_sourc...` — A class feature from the catalog.
- pub `parse_data` function L32-34 — `(&self) -> Result<serde_json::Value, serde_json::Error>` — Parse the JSON data blob into a serde_json::Value.
- pub `NewClassFeature` struct L40-47 — `{ name: &'a str, source: &'a str, class_name: &'a str, class_source: &'a str, le...` — Data for inserting a new class feature.
- pub `new` function L51-60 — `( name: &'a str, source: &'a str, class_name: &'a str, class_source: &'a str, le...` — Create a new class feature entry.
- pub `ClassFeatureFilter` struct L65-74 — `{ name_contains: Option<String>, class_name: Option<String>, class_source: Optio...` — Filters for searching class features.
- pub `new` function L77-79 — `() -> Self` — Represents a class feature in the catalog (Spellcasting, Extra Attack, etc.).
- pub `with_name_contains` function L81-84 — `(mut self, name: impl Into<String>) -> Self` — Represents a class feature in the catalog (Spellcasting, Extra Attack, etc.).
- pub `with_class` function L86-90 — `(mut self, class_name: impl Into<String>, class_source: impl Into<String>) -> Se...` — Represents a class feature in the catalog (Spellcasting, Extra Attack, etc.).
- pub `with_level` function L92-95 — `(mut self, level: i32) -> Self` — Represents a class feature in the catalog (Spellcasting, Extra Attack, etc.).
- pub `with_source` function L97-100 — `(mut self, source: impl Into<String>) -> Self` — Represents a class feature in the catalog (Spellcasting, Extra Attack, etc.).
- pub `with_sources` function L102-105 — `(mut self, sources: Vec<String>) -> Self` — Represents a class feature in the catalog (Spellcasting, Extra Attack, etc.).
- pub `has_empty_sources_filter` function L108-110 — `(&self) -> bool` — Returns true if sources filter is explicitly set to an empty array.
- pub `effective_sources` function L113-119 — `(&self) -> Option<Vec<String>>` — Get effective sources list (combines single source and sources array).
-  `ClassFeature` type L30-35 — `= ClassFeature` — Represents a class feature in the catalog (Spellcasting, Extra Attack, etc.).
-  `ClassFeatureFilter` type L76-120 — `= ClassFeatureFilter` — Represents a class feature in the catalog (Spellcasting, Extra Attack, etc.).
-  `tests` module L123-134 — `-` — Represents a class feature in the catalog (Spellcasting, Extra Attack, etc.).
-  `test_new_class_feature` function L127-133 — `()` — Represents a class feature in the catalog (Spellcasting, Extra Attack, etc.).

#### crates/mimir-core/src/models/catalog/condition.rs

- pub `Condition` struct L13-19 — `{ id: Option<i32>, name: String, source: String, data: String, fluff: Option<Str...` — A game condition from the catalog.
- pub `parse_data` function L22-24 — `(&self) -> Result<serde_json::Value, serde_json::Error>` — Represents a game condition (blinded, charmed, frightened, etc.).
- pub `NewCondition` struct L29-34 — `{ name: &'a str, source: &'a str, data: &'a str, fluff: Option<&'a str> }` — Represents a game condition (blinded, charmed, frightened, etc.).
- pub `new` function L37-39 — `(name: &'a str, source: &'a str, data: &'a str) -> Self` — Represents a game condition (blinded, charmed, frightened, etc.).
- pub `ConditionFilter` struct L44-50 — `{ name_contains: Option<String>, source: Option<String>, sources: Option<Vec<Str...` — Filters for searching conditions.
- pub `new` function L53-55 — `() -> Self` — Represents a game condition (blinded, charmed, frightened, etc.).
- pub `with_name_contains` function L57-60 — `(mut self, name: impl Into<String>) -> Self` — Represents a game condition (blinded, charmed, frightened, etc.).
- pub `with_source` function L62-65 — `(mut self, source: impl Into<String>) -> Self` — Represents a game condition (blinded, charmed, frightened, etc.).
- pub `with_sources` function L67-70 — `(mut self, sources: Vec<String>) -> Self` — Represents a game condition (blinded, charmed, frightened, etc.).
- pub `has_empty_sources_filter` function L73-75 — `(&self) -> bool` — Returns true if sources filter is explicitly set to an empty array.
- pub `effective_sources` function L78-84 — `(&self) -> Option<Vec<String>>` — Get effective sources list (combines single source and sources array).
-  `Condition` type L21-25 — `= Condition` — Represents a game condition (blinded, charmed, frightened, etc.).
-  `ConditionFilter` type L52-85 — `= ConditionFilter` — Represents a game condition (blinded, charmed, frightened, etc.).
-  `tests` module L88-96 — `-` — Represents a game condition (blinded, charmed, frightened, etc.).
-  `test_new_condition` function L92-95 — `()` — Represents a game condition (blinded, charmed, frightened, etc.).

#### crates/mimir-core/src/models/catalog/cult.rs

- pub `Cult` struct L13-18 — `{ id: Option<i32>, name: String, source: String, data: String }` — A cult or supernatural gift from the catalog.
- pub `parse_data` function L21-23 — `(&self) -> Result<serde_json::Value, serde_json::Error>` — Represents a cult or supernatural gift/boon.
- pub `NewCult` struct L28-32 — `{ name: &'a str, source: &'a str, data: &'a str }` — Represents a cult or supernatural gift/boon.
- pub `new` function L35-37 — `(name: &'a str, source: &'a str, data: &'a str) -> Self` — Represents a cult or supernatural gift/boon.
- pub `CultFilter` struct L42-46 — `{ name_contains: Option<String>, source: Option<String>, sources: Option<Vec<Str...` — Filters for searching cults.
- pub `new` function L49-51 — `() -> Self` — Represents a cult or supernatural gift/boon.
- pub `has_empty_sources_filter` function L53-55 — `(&self) -> bool` — Represents a cult or supernatural gift/boon.
- pub `effective_sources` function L57-63 — `(&self) -> Option<Vec<String>>` — Represents a cult or supernatural gift/boon.
-  `Cult` type L20-24 — `= Cult` — Represents a cult or supernatural gift/boon.
-  `CultFilter` type L48-64 — `= CultFilter` — Represents a cult or supernatural gift/boon.
-  `tests` module L67-75 — `-` — Represents a cult or supernatural gift/boon.
-  `test_new_cult` function L71-74 — `()` — Represents a cult or supernatural gift/boon.

#### crates/mimir-core/src/models/catalog/deity.rs

- pub `Deity` struct L13-19 — `{ id: Option<i32>, name: String, source: String, pantheon: Option<String>, data:...` — A deity from the catalog.
- pub `parse_data` function L22-24 — `(&self) -> Result<serde_json::Value, serde_json::Error>` — Represents a deity (god) from various pantheons.
- pub `NewDeity` struct L29-34 — `{ name: &'a str, source: &'a str, pantheon: Option<&'a str>, data: &'a str }` — Represents a deity (god) from various pantheons.
- pub `new` function L37-39 — `(name: &'a str, source: &'a str, data: &'a str) -> Self` — Represents a deity (god) from various pantheons.
- pub `with_pantheon` function L41-44 — `(mut self, pantheon: &'a str) -> Self` — Represents a deity (god) from various pantheons.
- pub `DeityFilter` struct L49-54 — `{ name_contains: Option<String>, source: Option<String>, sources: Option<Vec<Str...` — Filters for searching deities.
- pub `new` function L57-59 — `() -> Self` — Represents a deity (god) from various pantheons.
- pub `has_empty_sources_filter` function L61-63 — `(&self) -> bool` — Represents a deity (god) from various pantheons.
- pub `effective_sources` function L65-71 — `(&self) -> Option<Vec<String>>` — Represents a deity (god) from various pantheons.
-  `Deity` type L21-25 — `= Deity` — Represents a deity (god) from various pantheons.
-  `DeityFilter` type L56-72 — `= DeityFilter` — Represents a deity (god) from various pantheons.
-  `tests` module L75-85 — `-` — Represents a deity (god) from various pantheons.
-  `test_new_deity` function L79-84 — `()` — Represents a deity (god) from various pantheons.

#### crates/mimir-core/src/models/catalog/disease.rs

- pub `Disease` struct L13-19 — `{ id: Option<i32>, name: String, source: String, data: String, fluff: Option<Str...` — A disease from the catalog.
- pub `parse_data` function L22-24 — `(&self) -> Result<serde_json::Value, serde_json::Error>` — Represents a disease in the catalog.
- pub `NewDisease` struct L29-34 — `{ name: &'a str, source: &'a str, data: &'a str, fluff: Option<&'a str> }` — Represents a disease in the catalog.
- pub `new` function L37-39 — `(name: &'a str, source: &'a str, data: &'a str) -> Self` — Represents a disease in the catalog.
-  `Disease` type L21-25 — `= Disease` — Represents a disease in the catalog.
-  `tests` module L43-51 — `-` — Represents a disease in the catalog.
-  `test_new_disease` function L47-50 — `()` — Represents a disease in the catalog.

#### crates/mimir-core/src/models/catalog/feat.rs

- pub `Feat` struct L13-24 — `{ id: Option<i32>, name: String, source: String, data: String, fluff: Option<Str...` — A character feat from the catalog.
- pub `parse_data` function L28-30 — `(&self) -> Result<serde_json::Value, serde_json::Error>` — Parse the JSON data blob into a serde_json::Value.
- pub `NewFeat` struct L36-41 — `{ name: &'a str, source: &'a str, data: &'a str, fluff: Option<&'a str> }` — Data for inserting a new feat.
- pub `new` function L45-47 — `(name: &'a str, source: &'a str, data: &'a str) -> Self` — Create a new feat entry.
- pub `FeatFilter` struct L52-58 — `{ name_contains: Option<String>, source: Option<String>, sources: Option<Vec<Str...` — Filters for searching feats.
- pub `new` function L61-63 — `() -> Self` — Represents a character feat in the catalog (Alert, Sharpshooter, etc.).
- pub `with_name_contains` function L65-68 — `(mut self, name: impl Into<String>) -> Self` — Represents a character feat in the catalog (Alert, Sharpshooter, etc.).
- pub `with_source` function L70-73 — `(mut self, source: impl Into<String>) -> Self` — Represents a character feat in the catalog (Alert, Sharpshooter, etc.).
- pub `with_sources` function L75-78 — `(mut self, sources: Vec<String>) -> Self` — Represents a character feat in the catalog (Alert, Sharpshooter, etc.).
- pub `has_empty_sources_filter` function L81-83 — `(&self) -> bool` — Returns true if sources filter is explicitly set to an empty array.
- pub `effective_sources` function L86-92 — `(&self) -> Option<Vec<String>>` — Get effective sources list (combines single source and sources array).
-  `Feat` type L26-31 — `= Feat` — Represents a character feat in the catalog (Alert, Sharpshooter, etc.).
-  `FeatFilter` type L60-93 — `= FeatFilter` — Represents a character feat in the catalog (Alert, Sharpshooter, etc.).
-  `tests` module L96-106 — `-` — Represents a character feat in the catalog (Alert, Sharpshooter, etc.).
-  `test_new_feat` function L100-105 — `()` — Represents a character feat in the catalog (Alert, Sharpshooter, etc.).

#### crates/mimir-core/src/models/catalog/hazard.rs

- pub `Hazard` struct L13-19 — `{ id: Option<i32>, name: String, source: String, data: String, fluff: Option<Str...` — A hazard from the catalog.
- pub `parse_data` function L22-24 — `(&self) -> Result<serde_json::Value, serde_json::Error>` — Represents an environmental hazard.
- pub `NewHazard` struct L29-34 — `{ name: &'a str, source: &'a str, data: &'a str, fluff: Option<&'a str> }` — Represents an environmental hazard.
- pub `new` function L37-39 — `(name: &'a str, source: &'a str, data: &'a str) -> Self` — Represents an environmental hazard.
- pub `HazardFilter` struct L44-50 — `{ name_contains: Option<String>, source: Option<String>, sources: Option<Vec<Str...` — Filters for searching hazards.
- pub `new` function L53-55 — `() -> Self` — Represents an environmental hazard.
- pub `with_name_contains` function L57-60 — `(mut self, name: impl Into<String>) -> Self` — Represents an environmental hazard.
- pub `with_source` function L62-65 — `(mut self, source: impl Into<String>) -> Self` — Represents an environmental hazard.
- pub `with_sources` function L67-70 — `(mut self, sources: Vec<String>) -> Self` — Represents an environmental hazard.
- pub `has_empty_sources_filter` function L73-75 — `(&self) -> bool` — Returns true if sources filter is explicitly set to an empty array.
- pub `effective_sources` function L78-84 — `(&self) -> Option<Vec<String>>` — Get effective sources list (combines single source and sources array).
-  `Hazard` type L21-25 — `= Hazard` — Represents an environmental hazard.
-  `HazardFilter` type L52-85 — `= HazardFilter` — Represents an environmental hazard.
-  `tests` module L88-96 — `-` — Represents an environmental hazard.
-  `test_new_hazard` function L92-95 — `()` — Represents an environmental hazard.

#### crates/mimir-core/src/models/catalog/item.rs

- pub `Item` struct L13-28 — `{ id: Option<i32>, name: String, source: String, item_type: Option<String>, rari...` — An item from the catalog.
- pub `parse_data` function L32-34 — `(&self) -> Result<serde_json::Value, serde_json::Error>` — Parse the JSON data blob into a serde_json::Value.
- pub `type_name` function L37-76 — `(&self) -> &str` — Get the human-readable type name.
- pub `is_magic` function L79-81 — `(&self) -> bool` — Check if this is a magic item.
- pub `NewItem` struct L87-94 — `{ name: &'a str, source: &'a str, item_type: Option<&'a str>, rarity: Option<&'a...` — Data for inserting a new item.
- pub `new` function L98-107 — `(name: &'a str, source: &'a str, data: &'a str) -> Self` — Create a new item entry.
- pub `with_type` function L110-113 — `(mut self, item_type: &'a str) -> Self` — Set the item type.
- pub `with_rarity` function L116-119 — `(mut self, rarity: &'a str) -> Self` — Set the rarity.
- pub `ItemFilter` struct L124-132 — `{ name_contains: Option<String>, source: Option<String>, sources: Option<Vec<Str...` — Filters for searching items.
- pub `new` function L135-137 — `() -> Self` — Represents an item in the catalog (weapons, armor, magic items, equipment).
- pub `with_name_contains` function L139-142 — `(mut self, name: impl Into<String>) -> Self` — Represents an item in the catalog (weapons, armor, magic items, equipment).
- pub `with_source` function L144-147 — `(mut self, source: impl Into<String>) -> Self` — Represents an item in the catalog (weapons, armor, magic items, equipment).
- pub `with_sources` function L149-152 — `(mut self, sources: Vec<String>) -> Self` — Represents an item in the catalog (weapons, armor, magic items, equipment).
- pub `has_empty_sources_filter` function L156-158 — `(&self) -> bool` — Returns true if sources filter is explicitly set to an empty array.
- pub `effective_sources` function L163-169 — `(&self) -> Option<Vec<String>>` — Get effective sources list (combines single source and sources array).
- pub `with_type` function L171-174 — `(mut self, item_type: impl Into<String>) -> Self` — Represents an item in the catalog (weapons, armor, magic items, equipment).
- pub `with_rarity` function L176-179 — `(mut self, rarity: impl Into<String>) -> Self` — Represents an item in the catalog (weapons, armor, magic items, equipment).
-  `Item` type L30-82 — `= Item` — Represents an item in the catalog (weapons, armor, magic items, equipment).
-  `ItemFilter` type L134-180 — `= ItemFilter` — Represents an item in the catalog (weapons, armor, magic items, equipment).
-  `tests` module L183-217 — `-` — Represents an item in the catalog (weapons, armor, magic items, equipment).
-  `test_new_item` function L187-193 — `()` — Represents an item in the catalog (weapons, armor, magic items, equipment).
-  `test_new_item_with_fields` function L196-204 — `()` — Represents an item in the catalog (weapons, armor, magic items, equipment).
-  `test_item_filter` function L207-216 — `()` — Represents an item in the catalog (weapons, armor, magic items, equipment).

#### crates/mimir-core/src/models/catalog/item_attunement.rs

- pub `ItemAttunementClass` struct L15-22 — `{ id: Option<i32>, item_id: i32, class_name: String }` — An item-class attunement association.
- pub `NewItemAttunementClass` struct L27-30 — `{ item_id: i32, class_name: &'a str }` — Data for inserting a new item attunement class requirement.
- pub `new` function L34-36 — `(item_id: i32, class_name: &'a str) -> Self` — Create a new item attunement class requirement.
-  `tests` module L40-49 — `-` — attunement by a Cleric or Paladin").
-  `test_new_item_attunement_class` function L44-48 — `()` — attunement by a Cleric or Paladin").

#### crates/mimir-core/src/models/catalog/language.rs

- pub `Language` struct L13-20 — `{ id: Option<i32>, name: String, source: String, language_type: Option<String>, ...` — A language from the catalog.
- pub `parse_data` function L23-25 — `(&self) -> Result<serde_json::Value, serde_json::Error>` — Represents a language (Common, Elvish, Dwarvish, etc.).
- pub `type_name` function L28-35 — `(&self) -> &str` — Get the human-readable type name.
- pub `NewLanguage` struct L40-46 — `{ name: &'a str, source: &'a str, language_type: Option<&'a str>, data: &'a str,...` — Represents a language (Common, Elvish, Dwarvish, etc.).
- pub `new` function L49-51 — `(name: &'a str, source: &'a str, data: &'a str) -> Self` — Represents a language (Common, Elvish, Dwarvish, etc.).
- pub `with_type` function L53-56 — `(mut self, language_type: &'a str) -> Self` — Represents a language (Common, Elvish, Dwarvish, etc.).
- pub `LanguageFilter` struct L61-68 — `{ name_contains: Option<String>, source: Option<String>, sources: Option<Vec<Str...` — Filters for searching languages.
- pub `new` function L71-73 — `() -> Self` — Represents a language (Common, Elvish, Dwarvish, etc.).
- pub `with_name_contains` function L75-78 — `(mut self, name: impl Into<String>) -> Self` — Represents a language (Common, Elvish, Dwarvish, etc.).
- pub `with_source` function L80-83 — `(mut self, source: impl Into<String>) -> Self` — Represents a language (Common, Elvish, Dwarvish, etc.).
- pub `with_sources` function L85-88 — `(mut self, sources: Vec<String>) -> Self` — Represents a language (Common, Elvish, Dwarvish, etc.).
- pub `has_empty_sources_filter` function L91-93 — `(&self) -> bool` — Returns true if sources filter is explicitly set to an empty array.
- pub `effective_sources` function L96-102 — `(&self) -> Option<Vec<String>>` — Get effective sources list (combines single source and sources array).
- pub `with_type` function L104-107 — `(mut self, language_type: impl Into<String>) -> Self` — Represents a language (Common, Elvish, Dwarvish, etc.).
-  `Language` type L22-36 — `= Language` — Represents a language (Common, Elvish, Dwarvish, etc.).
-  `LanguageFilter` type L70-108 — `= LanguageFilter` — Represents a language (Common, Elvish, Dwarvish, etc.).
-  `tests` module L111-121 — `-` — Represents a language (Common, Elvish, Dwarvish, etc.).
-  `test_new_language` function L115-120 — `()` — Represents a language (Common, Elvish, Dwarvish, etc.).

#### crates/mimir-core/src/models/catalog/mod.rs

-  `action` module L5 — `-` — Database models for catalog entities (sources, monsters, spells, items, etc.)
-  `background` module L6 — `-` — Database models for catalog entities (sources, monsters, spells, items, etc.)
-  `book` module L7 — `-` — Database models for catalog entities (sources, monsters, spells, items, etc.)
-  `class` module L8 — `-` — Database models for catalog entities (sources, monsters, spells, items, etc.)
-  `class_feature` module L9 — `-` — Database models for catalog entities (sources, monsters, spells, items, etc.)
-  `condition` module L10 — `-` — Database models for catalog entities (sources, monsters, spells, items, etc.)
-  `cult` module L11 — `-` — Database models for catalog entities (sources, monsters, spells, items, etc.)
-  `deity` module L12 — `-` — Database models for catalog entities (sources, monsters, spells, items, etc.)
-  `disease` module L13 — `-` — Database models for catalog entities (sources, monsters, spells, items, etc.)
-  `feat` module L14 — `-` — Database models for catalog entities (sources, monsters, spells, items, etc.)
-  `hazard` module L15 — `-` — Database models for catalog entities (sources, monsters, spells, items, etc.)
-  `item` module L16 — `-` — Database models for catalog entities (sources, monsters, spells, items, etc.)
-  `item_attunement` module L17 — `-` — Database models for catalog entities (sources, monsters, spells, items, etc.)
-  `language` module L18 — `-` — Database models for catalog entities (sources, monsters, spells, items, etc.)
-  `monster` module L19 — `-` — Database models for catalog entities (sources, monsters, spells, items, etc.)
-  `object` module L20 — `-` — Database models for catalog entities (sources, monsters, spells, items, etc.)
-  `optional_feature` module L21 — `-` — Database models for catalog entities (sources, monsters, spells, items, etc.)
-  `psionic` module L22 — `-` — Database models for catalog entities (sources, monsters, spells, items, etc.)
-  `race` module L23 — `-` — Database models for catalog entities (sources, monsters, spells, items, etc.)
-  `reward` module L24 — `-` — Database models for catalog entities (sources, monsters, spells, items, etc.)
-  `sense` module L25 — `-` — Database models for catalog entities (sources, monsters, spells, items, etc.)
-  `skill` module L26 — `-` — Database models for catalog entities (sources, monsters, spells, items, etc.)
-  `source` module L27 — `-` — Database models for catalog entities (sources, monsters, spells, items, etc.)
-  `spell` module L28 — `-` — Database models for catalog entities (sources, monsters, spells, items, etc.)
-  `spell_list` module L29 — `-` — Database models for catalog entities (sources, monsters, spells, items, etc.)
-  `subclass` module L30 — `-` — Database models for catalog entities (sources, monsters, spells, items, etc.)
-  `subclass_feature` module L31 — `-` — Database models for catalog entities (sources, monsters, spells, items, etc.)
-  `table` module L32 — `-` — Database models for catalog entities (sources, monsters, spells, items, etc.)
-  `trap` module L33 — `-` — Database models for catalog entities (sources, monsters, spells, items, etc.)
-  `variant_rule` module L34 — `-` — Database models for catalog entities (sources, monsters, spells, items, etc.)
-  `vehicle` module L35 — `-` — Database models for catalog entities (sources, monsters, spells, items, etc.)

#### crates/mimir-core/src/models/catalog/monster.rs

- pub `Monster` struct L14-33 — `{ id: Option<i32>, name: String, source: String, cr: Option<String>, creature_ty...` — A monster from the catalog.
- pub `parse_data` function L37-39 — `(&self) -> Result<serde_json::Value, serde_json::Error>` — Parse the JSON data blob into a serde_json::Value.
- pub `cr_numeric` function L42-44 — `(&self) -> Option<f32>` — Get the numeric CR value if it can be parsed.
- pub `size_name` function L47-57 — `(&self) -> &str` — Get a display-friendly size name.
- pub `NewMonster` struct L74-83 — `{ name: &'a str, source: &'a str, cr: Option<&'a str>, creature_type: Option<&'a...` — Data for inserting a new monster.
- pub `new` function L87-98 — `(name: &'a str, source: &'a str, data: &'a str) -> Self` — Create a new monster entry.
- pub `with_cr` function L101-104 — `(mut self, cr: &'a str) -> Self` — Set the challenge rating.
- pub `with_creature_type` function L107-110 — `(mut self, creature_type: &'a str) -> Self` — Set the creature type.
- pub `with_size` function L113-116 — `(mut self, size: &'a str) -> Self` — Set the size.
- pub `with_token_image_path` function L119-122 — `(mut self, path: &'a str) -> Self` — Set the token image path.
- pub `from_json` function L130-167 — `(name: &'a str, source: &'a str, data: &'a str, json: &serde_json::Value) -> Sel...` — Extract indexed fields from a JSON value.
- pub `MonsterFilter` struct L173-182 — `{ name_contains: Option<String>, source: Option<String>, sources: Option<Vec<Str...` — Filters for searching monsters.
- pub `new` function L185-187 — `() -> Self` — and a full JSON data blob.
- pub `with_name_contains` function L189-192 — `(mut self, name: impl Into<String>) -> Self` — and a full JSON data blob.
- pub `with_source` function L194-197 — `(mut self, source: impl Into<String>) -> Self` — and a full JSON data blob.
- pub `with_sources` function L199-202 — `(mut self, sources: Vec<String>) -> Self` — and a full JSON data blob.
- pub `has_empty_sources_filter` function L205-207 — `(&self) -> bool` — Returns true if sources filter is explicitly set to an empty array.
- pub `effective_sources` function L210-216 — `(&self) -> Option<Vec<String>>` — Get effective sources list (combines single source and sources array).
- pub `with_cr` function L218-221 — `(mut self, cr: impl Into<String>) -> Self` — and a full JSON data blob.
- pub `with_creature_type` function L223-226 — `(mut self, creature_type: impl Into<String>) -> Self` — and a full JSON data blob.
- pub `with_size` function L228-231 — `(mut self, size: impl Into<String>) -> Self` — and a full JSON data blob.
-  `Monster` type L35-58 — `= Monster` — and a full JSON data blob.
-  `parse_cr` function L61-69 — `(cr: &str) -> Option<f32>` — Parse a CR string to a numeric value.
-  `MonsterFilter` type L184-232 — `= MonsterFilter` — and a full JSON data blob.
-  `tests` module L235-319 — `-` — and a full JSON data blob.
-  `test_new_monster` function L240-246 — `()` — and a full JSON data blob.
-  `test_new_monster_with_fields` function L249-259 — `()` — and a full JSON data blob.
-  `test_parse_cr` function L262-270 — `()` — and a full JSON data blob.
-  `test_monster_filter` function L273-282 — `()` — and a full JSON data blob.
-  `test_from_json_simple_type` function L285-299 — `()` — and a full JSON data blob.
-  `test_from_json_complex_type` function L302-318 — `()` — and a full JSON data blob.

#### crates/mimir-core/src/models/catalog/object.rs

- pub `Object` struct L13-20 — `{ id: Option<i32>, name: String, source: String, object_type: Option<String>, da...` — An object from the catalog.
- pub `parse_data` function L23-25 — `(&self) -> Result<serde_json::Value, serde_json::Error>` — Represents an interactive object (doors, chests, siege weapons, etc.).
- pub `NewObject` struct L30-36 — `{ name: &'a str, source: &'a str, object_type: Option<&'a str>, data: &'a str, f...` — Represents an interactive object (doors, chests, siege weapons, etc.).
- pub `new` function L39-41 — `(name: &'a str, source: &'a str, data: &'a str) -> Self` — Represents an interactive object (doors, chests, siege weapons, etc.).
- pub `with_type` function L43-46 — `(mut self, object_type: &'a str) -> Self` — Represents an interactive object (doors, chests, siege weapons, etc.).
- pub `ObjectFilter` struct L51-56 — `{ name_contains: Option<String>, source: Option<String>, sources: Option<Vec<Str...` — Filters for searching objects.
- pub `new` function L59-61 — `() -> Self` — Represents an interactive object (doors, chests, siege weapons, etc.).
- pub `has_empty_sources_filter` function L63-65 — `(&self) -> bool` — Represents an interactive object (doors, chests, siege weapons, etc.).
- pub `effective_sources` function L67-73 — `(&self) -> Option<Vec<String>>` — Represents an interactive object (doors, chests, siege weapons, etc.).
-  `Object` type L22-26 — `= Object` — Represents an interactive object (doors, chests, siege weapons, etc.).
-  `ObjectFilter` type L58-74 — `= ObjectFilter` — Represents an interactive object (doors, chests, siege weapons, etc.).
-  `tests` module L77-87 — `-` — Represents an interactive object (doors, chests, siege weapons, etc.).
-  `test_new_object` function L81-86 — `()` — Represents an interactive object (doors, chests, siege weapons, etc.).

#### crates/mimir-core/src/models/catalog/optional_feature.rs

- pub `OptionalFeature` struct L14-21 — `{ id: Option<i32>, name: String, source: String, feature_type: Option<String>, d...` — An optional feature from the catalog.
- pub `parse_data` function L24-26 — `(&self) -> Result<serde_json::Value, serde_json::Error>` — Fighting Styles, Maneuvers, etc.
- pub `is_eldritch_invocation` function L29-31 — `(&self) -> bool` — Check if this is an Eldritch Invocation.
- pub `is_metamagic` function L34-36 — `(&self) -> bool` — Check if this is a Metamagic option.
- pub `is_fighting_style` function L39-41 — `(&self) -> bool` — Check if this is a Fighting Style.
- pub `is_maneuver` function L44-46 — `(&self) -> bool` — Check if this is a Battle Master Maneuver.
- pub `NewOptionalFeature` struct L51-56 — `{ name: &'a str, source: &'a str, feature_type: Option<&'a str>, data: &'a str }` — Fighting Styles, Maneuvers, etc.
- pub `new` function L59-66 — `(name: &'a str, source: &'a str, data: &'a str) -> Self` — Fighting Styles, Maneuvers, etc.
- pub `with_feature_type` function L68-71 — `(mut self, feature_type: &'a str) -> Self` — Fighting Styles, Maneuvers, etc.
- pub `OptionalFeatureFilter` struct L76-81 — `{ name_contains: Option<String>, source: Option<String>, sources: Option<Vec<Str...` — Filters for searching optional features.
- pub `new` function L84-86 — `() -> Self` — Fighting Styles, Maneuvers, etc.
- pub `has_empty_sources_filter` function L88-90 — `(&self) -> bool` — Fighting Styles, Maneuvers, etc.
- pub `effective_sources` function L92-98 — `(&self) -> Option<Vec<String>>` — Fighting Styles, Maneuvers, etc.
-  `OptionalFeature` type L23-47 — `= OptionalFeature` — Fighting Styles, Maneuvers, etc.
-  `OptionalFeatureFilter` type L83-99 — `= OptionalFeatureFilter` — Fighting Styles, Maneuvers, etc.
-  `tests` module L102-112 — `-` — Fighting Styles, Maneuvers, etc.
-  `test_new_optional_feature` function L106-111 — `()` — Fighting Styles, Maneuvers, etc.

#### crates/mimir-core/src/models/catalog/psionic.rs

- pub `Psionic` struct L13-22 — `{ id: Option<i32>, name: String, source: String, psionic_type: Option<String>, p...` — A psionic discipline or talent from the catalog.
- pub `parse_data` function L25-27 — `(&self) -> Result<serde_json::Value, serde_json::Error>` — Represents psionic disciplines and talents from UA/homebrew content.
- pub `is_discipline` function L30-32 — `(&self) -> bool` — Check if this is a discipline.
- pub `is_talent` function L35-37 — `(&self) -> bool` — Check if this is a talent.
- pub `NewPsionic` struct L42-48 — `{ name: &'a str, source: &'a str, psionic_type: Option<&'a str>, psionic_order: ...` — Represents psionic disciplines and talents from UA/homebrew content.
- pub `new` function L51-59 — `(name: &'a str, source: &'a str, data: &'a str) -> Self` — Represents psionic disciplines and talents from UA/homebrew content.
- pub `with_type` function L61-64 — `(mut self, psionic_type: &'a str) -> Self` — Represents psionic disciplines and talents from UA/homebrew content.
- pub `with_order` function L66-69 — `(mut self, order: &'a str) -> Self` — Represents psionic disciplines and talents from UA/homebrew content.
- pub `PsionicFilter` struct L74-80 — `{ name_contains: Option<String>, source: Option<String>, sources: Option<Vec<Str...` — Filters for searching psionics.
- pub `new` function L83-85 — `() -> Self` — Represents psionic disciplines and talents from UA/homebrew content.
- pub `has_empty_sources_filter` function L87-89 — `(&self) -> bool` — Represents psionic disciplines and talents from UA/homebrew content.
- pub `effective_sources` function L91-97 — `(&self) -> Option<Vec<String>>` — Represents psionic disciplines and talents from UA/homebrew content.
-  `Psionic` type L24-38 — `= Psionic` — Represents psionic disciplines and talents from UA/homebrew content.
-  `PsionicFilter` type L82-98 — `= PsionicFilter` — Represents psionic disciplines and talents from UA/homebrew content.
-  `tests` module L101-113 — `-` — Represents psionic disciplines and talents from UA/homebrew content.
-  `test_new_psionic` function L105-112 — `()` — Represents psionic disciplines and talents from UA/homebrew content.

#### crates/mimir-core/src/models/catalog/race.rs

- pub `Race` struct L13-24 — `{ id: Option<i32>, name: String, source: String, data: String, fluff: Option<Str...` — A character race from the catalog.
- pub `parse_data` function L28-30 — `(&self) -> Result<serde_json::Value, serde_json::Error>` — Parse the JSON data blob into a serde_json::Value.
- pub `NewRace` struct L36-41 — `{ name: &'a str, source: &'a str, data: &'a str, fluff: Option<&'a str> }` — Data for inserting a new race.
- pub `new` function L45-47 — `(name: &'a str, source: &'a str, data: &'a str) -> Self` — Create a new race entry.
- pub `RaceFilter` struct L52-58 — `{ name_contains: Option<String>, source: Option<String>, sources: Option<Vec<Str...` — Filters for searching races.
- pub `new` function L61-63 — `() -> Self` — Represents a character race/lineage in the catalog (Elf, Dwarf, etc.).
- pub `with_name_contains` function L65-68 — `(mut self, name: impl Into<String>) -> Self` — Represents a character race/lineage in the catalog (Elf, Dwarf, etc.).
- pub `with_source` function L70-73 — `(mut self, source: impl Into<String>) -> Self` — Represents a character race/lineage in the catalog (Elf, Dwarf, etc.).
- pub `with_sources` function L75-78 — `(mut self, sources: Vec<String>) -> Self` — Represents a character race/lineage in the catalog (Elf, Dwarf, etc.).
- pub `has_empty_sources_filter` function L81-83 — `(&self) -> bool` — Returns true if sources filter is explicitly set to an empty array.
- pub `effective_sources` function L86-92 — `(&self) -> Option<Vec<String>>` — Get effective sources list (combines single source and sources array).
-  `Race` type L26-31 — `= Race` — Represents a character race/lineage in the catalog (Elf, Dwarf, etc.).
-  `RaceFilter` type L60-93 — `= RaceFilter` — Represents a character race/lineage in the catalog (Elf, Dwarf, etc.).
-  `tests` module L96-106 — `-` — Represents a character race/lineage in the catalog (Elf, Dwarf, etc.).
-  `test_new_race` function L100-105 — `()` — Represents a character race/lineage in the catalog (Elf, Dwarf, etc.).

#### crates/mimir-core/src/models/catalog/reward.rs

- pub `Reward` struct L13-20 — `{ id: Option<i32>, name: String, source: String, reward_type: Option<String>, da...` — A supernatural reward from the catalog.
- pub `parse_data` function L23-25 — `(&self) -> Result<serde_json::Value, serde_json::Error>` — Represents supernatural rewards: blessings, boons, charms, and gifts.
- pub `is_blessing` function L28-32 — `(&self) -> bool` — Check if this is a blessing.
- pub `is_boon` function L35-39 — `(&self) -> bool` — Check if this is a boon.
- pub `is_charm` function L42-46 — `(&self) -> bool` — Check if this is a charm.
- pub `NewReward` struct L51-56 — `{ name: &'a str, source: &'a str, reward_type: Option<&'a str>, data: &'a str }` — Represents supernatural rewards: blessings, boons, charms, and gifts.
- pub `new` function L59-66 — `(name: &'a str, source: &'a str, data: &'a str) -> Self` — Represents supernatural rewards: blessings, boons, charms, and gifts.
- pub `with_type` function L68-71 — `(mut self, reward_type: &'a str) -> Self` — Represents supernatural rewards: blessings, boons, charms, and gifts.
- pub `RewardFilter` struct L76-81 — `{ name_contains: Option<String>, source: Option<String>, sources: Option<Vec<Str...` — Filters for searching rewards.
- pub `new` function L84-86 — `() -> Self` — Represents supernatural rewards: blessings, boons, charms, and gifts.
- pub `has_empty_sources_filter` function L88-90 — `(&self) -> bool` — Represents supernatural rewards: blessings, boons, charms, and gifts.
- pub `effective_sources` function L92-98 — `(&self) -> Option<Vec<String>>` — Represents supernatural rewards: blessings, boons, charms, and gifts.
-  `Reward` type L22-47 — `= Reward` — Represents supernatural rewards: blessings, boons, charms, and gifts.
-  `RewardFilter` type L83-99 — `= RewardFilter` — Represents supernatural rewards: blessings, boons, charms, and gifts.
-  `tests` module L102-112 — `-` — Represents supernatural rewards: blessings, boons, charms, and gifts.
-  `test_new_reward` function L106-111 — `()` — Represents supernatural rewards: blessings, boons, charms, and gifts.

#### crates/mimir-core/src/models/catalog/sense.rs

- pub `Sense` struct L13-18 — `{ id: Option<i32>, name: String, source: String, data: String }` — A sense from the catalog.
- pub `parse_data` function L21-23 — `(&self) -> Result<serde_json::Value, serde_json::Error>` — Represents a sense (darkvision, tremorsense, blindsight, etc.).
- pub `NewSense` struct L28-32 — `{ name: &'a str, source: &'a str, data: &'a str }` — Represents a sense (darkvision, tremorsense, blindsight, etc.).
- pub `new` function L35-37 — `(name: &'a str, source: &'a str, data: &'a str) -> Self` — Represents a sense (darkvision, tremorsense, blindsight, etc.).
-  `Sense` type L20-24 — `= Sense` — Represents a sense (darkvision, tremorsense, blindsight, etc.).
-  `tests` module L41-49 — `-` — Represents a sense (darkvision, tremorsense, blindsight, etc.).
-  `test_new_sense` function L45-48 — `()` — Represents a sense (darkvision, tremorsense, blindsight, etc.).

#### crates/mimir-core/src/models/catalog/skill.rs

- pub `Skill` struct L13-19 — `{ id: Option<i32>, name: String, source: String, ability: Option<String>, data: ...` — A skill from the catalog.
- pub `parse_data` function L22-24 — `(&self) -> Result<serde_json::Value, serde_json::Error>` — Represents a skill (Athletics, Perception, Stealth, etc.).
- pub `NewSkill` struct L29-34 — `{ name: &'a str, source: &'a str, ability: Option<&'a str>, data: &'a str }` — Represents a skill (Athletics, Perception, Stealth, etc.).
- pub `new` function L37-39 — `(name: &'a str, source: &'a str, data: &'a str) -> Self` — Represents a skill (Athletics, Perception, Stealth, etc.).
- pub `with_ability` function L41-44 — `(mut self, ability: &'a str) -> Self` — Represents a skill (Athletics, Perception, Stealth, etc.).
-  `Skill` type L21-25 — `= Skill` — Represents a skill (Athletics, Perception, Stealth, etc.).
-  `tests` module L48-58 — `-` — Represents a skill (Athletics, Perception, Stealth, etc.).
-  `test_new_skill` function L52-57 — `()` — Represents a skill (Athletics, Perception, Stealth, etc.).

#### crates/mimir-core/src/models/catalog/source.rs

- pub `CatalogSource` struct L13-22 — `{ code: String, name: String, enabled: i32, imported_at: String }` — A catalog source (book) that has been imported.
- pub `is_enabled` function L26-28 — `(&self) -> bool` — Check if the source is enabled.
- pub `NewCatalogSource` struct L34-39 — `{ code: &'a str, name: &'a str, enabled: i32, imported_at: &'a str }` — Data for inserting a new catalog source.
- pub `new` function L49-56 — `(code: &'a str, name: &'a str, enabled: bool, imported_at: &'a str) -> Self` — Create a new catalog source entry.
- pub `UpdateCatalogSource` struct L62-64 — `{ enabled: Option<i32> }` — Data for updating a catalog source's enabled status.
- pub `set_enabled` function L68-72 — `(enabled: bool) -> Self` — Create an update to change the enabled status.
-  `CatalogSource` type L24-29 — `= CatalogSource` — Represents a source book in the catalog (e.g., Player's Handbook, Monster Manual).
-  `UpdateCatalogSource` type L66-73 — `= UpdateCatalogSource` — Represents a source book in the catalog (e.g., Player's Handbook, Monster Manual).
-  `tests` module L76-101 — `-` — Represents a source book in the catalog (e.g., Player's Handbook, Monster Manual).
-  `test_new_catalog_source` function L80-85 — `()` — Represents a source book in the catalog (e.g., Player's Handbook, Monster Manual).
-  `test_new_catalog_source_disabled` function L88-91 — `()` — Represents a source book in the catalog (e.g., Player's Handbook, Monster Manual).
-  `test_update_catalog_source` function L94-100 — `()` — Represents a source book in the catalog (e.g., Player's Handbook, Monster Manual).

#### crates/mimir-core/src/models/catalog/spell.rs

- pub `Spell` struct L13-32 — `{ id: Option<i32>, name: String, source: String, level: i32, school: Option<Stri...` — A spell from the catalog.
- pub `parse_data` function L36-38 — `(&self) -> Result<serde_json::Value, serde_json::Error>` — Parse the JSON data blob into a serde_json::Value.
- pub `is_cantrip` function L41-43 — `(&self) -> bool` — Check if this is a cantrip.
- pub `is_ritual` function L46-48 — `(&self) -> bool` — Check if this is a ritual spell.
- pub `requires_concentration` function L51-53 — `(&self) -> bool` — Check if this spell requires concentration.
- pub `school_name` function L56-68 — `(&self) -> &str` — Get the human-readable school name.
- pub `level_display` function L71-79 — `(&self) -> String` — Get the level as a display string (e.g., "Cantrip", "1st", "2nd").
- pub `NewSpell` struct L85-94 — `{ name: &'a str, source: &'a str, level: i32, school: Option<&'a str>, ritual: i...` — Data for inserting a new spell.
- pub `new` function L98-109 — `(name: &'a str, source: &'a str, level: i32, data: &'a str) -> Self` — Create a new spell entry.
- pub `with_school` function L112-115 — `(mut self, school: &'a str) -> Self` — Set the school.
- pub `with_ritual` function L118-121 — `(mut self, ritual: bool) -> Self` — Mark as a ritual spell.
- pub `with_concentration` function L124-127 — `(mut self, concentration: bool) -> Self` — Mark as requiring concentration.
- pub `SpellFilter` struct L132-142 — `{ name_contains: Option<String>, source: Option<String>, sources: Option<Vec<Str...` — Filters for searching spells.
- pub `new` function L145-147 — `() -> Self` — Represents a spell in the catalog.
- pub `with_name_contains` function L149-152 — `(mut self, name: impl Into<String>) -> Self` — Represents a spell in the catalog.
- pub `with_source` function L154-157 — `(mut self, source: impl Into<String>) -> Self` — Represents a spell in the catalog.
- pub `with_sources` function L159-162 — `(mut self, sources: Vec<String>) -> Self` — Represents a spell in the catalog.
- pub `has_empty_sources_filter` function L165-167 — `(&self) -> bool` — Returns true if sources filter is explicitly set to an empty array.
- pub `effective_sources` function L170-176 — `(&self) -> Option<Vec<String>>` — Get effective sources list (combines single source and sources array).
- pub `with_level` function L178-181 — `(mut self, level: i32) -> Self` — Represents a spell in the catalog.
- pub `with_school` function L183-186 — `(mut self, school: impl Into<String>) -> Self` — Represents a spell in the catalog.
- pub `with_ritual` function L188-191 — `(mut self, ritual: bool) -> Self` — Represents a spell in the catalog.
- pub `with_concentration` function L193-196 — `(mut self, concentration: bool) -> Self` — Represents a spell in the catalog.
-  `Spell` type L34-80 — `= Spell` — Represents a spell in the catalog.
-  `SpellFilter` type L144-197 — `= SpellFilter` — Represents a spell in the catalog.
-  `tests` module L200-352 — `-` — Represents a spell in the catalog.
-  `test_new_spell` function L204-213 — `()` — Represents a spell in the catalog.
-  `test_new_spell_with_fields` function L216-226 — `()` — Represents a spell in the catalog.
-  `test_new_spell_ritual_concentration` function L229-238 — `()` — Represents a spell in the catalog.
-  `test_spell_filter` function L241-252 — `()` — Represents a spell in the catalog.
-  `test_school_name` function L255-269 — `()` — Represents a spell in the catalog.
-  `test_level_display` function L272-291 — `()` — Represents a spell in the catalog.
-  `test_is_cantrip` function L294-320 — `()` — Represents a spell in the catalog.
-  `test_is_ritual` function L323-351 — `()` — Represents a spell in the catalog.

#### crates/mimir-core/src/models/catalog/spell_list.rs

- pub `SpellClass` struct L15-24 — `{ id: Option<i32>, spell_id: i32, class_name: String, source: String }` — A spell-class association indicating a spell is on a class's spell list.
- pub `NewSpellClass` struct L29-33 — `{ spell_id: i32, class_name: &'a str, source: &'a str }` — Data for inserting a new spell-class association.
- pub `new` function L37-43 — `(spell_id: i32, class_name: &'a str, source: &'a str) -> Self` — Create a new spell-class association.
- pub `SpellSubclass` struct L50-61 — `{ id: Option<i32>, spell_id: i32, subclass_name: String, class_name: String, sou...` — A spell-subclass association indicating a spell is on a subclass's expanded spell list.
- pub `NewSpellSubclass` struct L66-71 — `{ spell_id: i32, subclass_name: &'a str, class_name: &'a str, source: &'a str }` — Data for inserting a new spell-subclass association.
- pub `new` function L75-87 — `( spell_id: i32, subclass_name: &'a str, class_name: &'a str, source: &'a str, )...` — Create a new spell-subclass association.
-  `tests` module L91-110 — `-` — classes/subclasses that have access to them.
-  `test_new_spell_class` function L95-100 — `()` — classes/subclasses that have access to them.
-  `test_new_spell_subclass` function L103-109 — `()` — classes/subclasses that have access to them.

#### crates/mimir-core/src/models/catalog/subclass.rs

- pub `Subclass` struct L13-26 — `{ id: Option<i32>, name: String, class_name: String, source: String, data: Strin...` — A character subclass from the catalog.
- pub `parse_data` function L30-32 — `(&self) -> Result<serde_json::Value, serde_json::Error>` — Parse the JSON data blob into a serde_json::Value.
- pub `NewSubclass` struct L38-44 — `{ name: &'a str, class_name: &'a str, source: &'a str, data: &'a str, fluff: Opt...` — Data for inserting a new subclass.
- pub `new` function L48-56 — `(name: &'a str, class_name: &'a str, source: &'a str, data: &'a str) -> Self` — Create a new subclass entry.
-  `Subclass` type L28-33 — `= Subclass` — Represents a character subclass in the catalog (School of Evocation, Champion, etc.).
-  `tests` module L60-71 — `-` — Represents a character subclass in the catalog (School of Evocation, Champion, etc.).
-  `test_new_subclass` function L64-70 — `()` — Represents a character subclass in the catalog (School of Evocation, Champion, etc.).

#### crates/mimir-core/src/models/catalog/subclass_feature.rs

- pub `SubclassFeature` struct L13-32 — `{ id: Option<i32>, name: String, source: String, class_name: String, class_sourc...` — A subclass feature from the catalog.
- pub `parse_data` function L36-38 — `(&self) -> Result<serde_json::Value, serde_json::Error>` — Parse the JSON data blob into a serde_json::Value.
- pub `NewSubclassFeature` struct L44-53 — `{ name: &'a str, source: &'a str, class_name: &'a str, class_source: &'a str, su...` — Data for inserting a new subclass feature.
- pub `new` function L57-71 — `( name: &'a str, source: &'a str, class_name: &'a str, class_source: &'a str, su...` — Create a new subclass feature entry.
- pub `SubclassFeatureFilter` struct L76-87 — `{ name_contains: Option<String>, class_name: Option<String>, class_source: Optio...` — Filters for searching subclass features.
- pub `new` function L90-92 — `() -> Self` — Represents a subclass feature in the catalog (Channel Divinity, etc.).
- pub `with_name_contains` function L94-97 — `(mut self, name: impl Into<String>) -> Self` — Represents a subclass feature in the catalog (Channel Divinity, etc.).
- pub `with_class` function L99-103 — `(mut self, class_name: impl Into<String>, class_source: impl Into<String>) -> Se...` — Represents a subclass feature in the catalog (Channel Divinity, etc.).
- pub `with_subclass` function L105-109 — `(mut self, subclass_name: impl Into<String>, subclass_source: impl Into<String>)...` — Represents a subclass feature in the catalog (Channel Divinity, etc.).
- pub `with_level` function L111-114 — `(mut self, level: i32) -> Self` — Represents a subclass feature in the catalog (Channel Divinity, etc.).
- pub `has_empty_sources_filter` function L117-119 — `(&self) -> bool` — Returns true if sources filter is explicitly set to an empty array.
- pub `effective_sources` function L122-128 — `(&self) -> Option<Vec<String>>` — Get effective sources list.
-  `SubclassFeature` type L34-39 — `= SubclassFeature` — Represents a subclass feature in the catalog (Channel Divinity, etc.).
-  `SubclassFeatureFilter` type L89-129 — `= SubclassFeatureFilter` — Represents a subclass feature in the catalog (Channel Divinity, etc.).

#### crates/mimir-core/src/models/catalog/table.rs

- pub `CatalogTable` struct L13-18 — `{ id: Option<i32>, name: String, source: String, data: String }` — A table (random table, encounter table, etc.) from the catalog.
- pub `parse_data` function L21-23 — `(&self) -> Result<serde_json::Value, serde_json::Error>` — Represents random tables, encounter tables, and other game tables.
- pub `NewCatalogTable` struct L28-32 — `{ name: &'a str, source: &'a str, data: &'a str }` — Represents random tables, encounter tables, and other game tables.
- pub `new` function L35-37 — `(name: &'a str, source: &'a str, data: &'a str) -> Self` — Represents random tables, encounter tables, and other game tables.
- pub `CatalogTableFilter` struct L42-46 — `{ name_contains: Option<String>, source: Option<String>, sources: Option<Vec<Str...` — Filters for searching tables.
- pub `new` function L49-51 — `() -> Self` — Represents random tables, encounter tables, and other game tables.
- pub `has_empty_sources_filter` function L53-55 — `(&self) -> bool` — Represents random tables, encounter tables, and other game tables.
- pub `effective_sources` function L57-63 — `(&self) -> Option<Vec<String>>` — Represents random tables, encounter tables, and other game tables.
-  `CatalogTable` type L20-24 — `= CatalogTable` — Represents random tables, encounter tables, and other game tables.
-  `CatalogTableFilter` type L48-64 — `= CatalogTableFilter` — Represents random tables, encounter tables, and other game tables.
-  `tests` module L67-76 — `-` — Represents random tables, encounter tables, and other game tables.
-  `test_new_catalog_table` function L71-75 — `()` — Represents random tables, encounter tables, and other game tables.

#### crates/mimir-core/src/models/catalog/trap.rs

- pub `Trap` struct L13-20 — `{ id: Option<i32>, name: String, source: String, trap_tier: Option<String>, data...` — A trap from the catalog.
- pub `parse_data` function L23-25 — `(&self) -> Result<serde_json::Value, serde_json::Error>` — Represents a trap.
- pub `is_simple` function L28-30 — `(&self) -> bool` — Check if this is a simple trap.
- pub `is_complex` function L33-35 — `(&self) -> bool` — Check if this is a complex trap.
- pub `NewTrap` struct L40-46 — `{ name: &'a str, source: &'a str, trap_tier: Option<&'a str>, data: &'a str, flu...` — Represents a trap.
- pub `new` function L49-51 — `(name: &'a str, source: &'a str, data: &'a str) -> Self` — Represents a trap.
- pub `with_tier` function L53-56 — `(mut self, tier: &'a str) -> Self` — Represents a trap.
- pub `TrapFilter` struct L62-69 — `{ name_contains: Option<String>, source: Option<String>, sources: Option<Vec<Str...` — Filters for searching traps.
- pub `new` function L72-74 — `() -> Self` — Represents a trap.
- pub `with_name_contains` function L76-79 — `(mut self, name: impl Into<String>) -> Self` — Represents a trap.
- pub `with_source` function L81-84 — `(mut self, source: impl Into<String>) -> Self` — Represents a trap.
- pub `with_sources` function L86-89 — `(mut self, sources: Vec<String>) -> Self` — Represents a trap.
- pub `has_empty_sources_filter` function L92-94 — `(&self) -> bool` — Returns true if sources filter is explicitly set to an empty array.
- pub `effective_sources` function L97-103 — `(&self) -> Option<Vec<String>>` — Get effective sources list (combines single source and sources array).
- pub `with_tier` function L105-108 — `(mut self, tier: impl Into<String>) -> Self` — Represents a trap.
-  `Trap` type L22-36 — `= Trap` — Represents a trap.
-  `TrapFilter` type L71-109 — `= TrapFilter` — Represents a trap.
-  `tests` module L112-122 — `-` — Represents a trap.
-  `test_new_trap` function L116-121 — `()` — Represents a trap.

#### crates/mimir-core/src/models/catalog/variant_rule.rs

- pub `VariantRule` struct L13-20 — `{ id: Option<i32>, name: String, source: String, rule_type: Option<String>, data...` — A variant or optional rule from the catalog.
- pub `parse_data` function L23-25 — `(&self) -> Result<serde_json::Value, serde_json::Error>` — Represents optional and variant rules from rulebooks.
- pub `is_optional` function L28-30 — `(&self) -> bool` — Check if this is an optional rule.
- pub `is_variant` function L33-35 — `(&self) -> bool` — Check if this is a variant rule.
- pub `NewVariantRule` struct L40-45 — `{ name: &'a str, source: &'a str, rule_type: Option<&'a str>, data: &'a str }` — Represents optional and variant rules from rulebooks.
- pub `new` function L48-55 — `(name: &'a str, source: &'a str, data: &'a str) -> Self` — Represents optional and variant rules from rulebooks.
- pub `with_type` function L57-60 — `(mut self, rule_type: &'a str) -> Self` — Represents optional and variant rules from rulebooks.
- pub `VariantRuleFilter` struct L65-70 — `{ name_contains: Option<String>, source: Option<String>, sources: Option<Vec<Str...` — Filters for searching variant rules.
- pub `new` function L73-75 — `() -> Self` — Represents optional and variant rules from rulebooks.
- pub `has_empty_sources_filter` function L77-79 — `(&self) -> bool` — Represents optional and variant rules from rulebooks.
- pub `effective_sources` function L81-87 — `(&self) -> Option<Vec<String>>` — Represents optional and variant rules from rulebooks.
-  `VariantRule` type L22-36 — `= VariantRule` — Represents optional and variant rules from rulebooks.
-  `VariantRuleFilter` type L72-88 — `= VariantRuleFilter` — Represents optional and variant rules from rulebooks.
-  `tests` module L91-101 — `-` — Represents optional and variant rules from rulebooks.
-  `test_new_variant_rule` function L95-100 — `()` — Represents optional and variant rules from rulebooks.

#### crates/mimir-core/src/models/catalog/vehicle.rs

- pub `Vehicle` struct L13-20 — `{ id: Option<i32>, name: String, source: String, vehicle_type: Option<String>, d...` — A vehicle from the catalog.
- pub `parse_data` function L23-25 — `(&self) -> Result<serde_json::Value, serde_json::Error>` — Represents a vehicle (ships, airships, infernal war machines, etc.).
- pub `type_name` function L28-37 — `(&self) -> &str` — Get the human-readable type name.
- pub `NewVehicle` struct L42-48 — `{ name: &'a str, source: &'a str, vehicle_type: Option<&'a str>, data: &'a str, ...` — Represents a vehicle (ships, airships, infernal war machines, etc.).
- pub `new` function L51-53 — `(name: &'a str, source: &'a str, data: &'a str) -> Self` — Represents a vehicle (ships, airships, infernal war machines, etc.).
- pub `with_type` function L55-58 — `(mut self, vehicle_type: &'a str) -> Self` — Represents a vehicle (ships, airships, infernal war machines, etc.).
- pub `VehicleFilter` struct L63-68 — `{ name_contains: Option<String>, source: Option<String>, sources: Option<Vec<Str...` — Filters for searching vehicles.
- pub `new` function L71-73 — `() -> Self` — Represents a vehicle (ships, airships, infernal war machines, etc.).
- pub `has_empty_sources_filter` function L75-77 — `(&self) -> bool` — Represents a vehicle (ships, airships, infernal war machines, etc.).
- pub `effective_sources` function L79-85 — `(&self) -> Option<Vec<String>>` — Represents a vehicle (ships, airships, infernal war machines, etc.).
-  `Vehicle` type L22-38 — `= Vehicle` — Represents a vehicle (ships, airships, infernal war machines, etc.).
-  `VehicleFilter` type L70-86 — `= VehicleFilter` — Represents a vehicle (ships, airships, infernal war machines, etc.).
-  `tests` module L89-99 — `-` — Represents a vehicle (ships, airships, infernal war machines, etc.).
-  `test_new_vehicle` function L93-98 — `()` — Represents a vehicle (ships, airships, infernal war machines, etc.).

### crates/mimir-core/src/models

> *Semantic summary to be generated by AI agent.*

#### crates/mimir-core/src/models/mod.rs

- pub `campaign` module L5 — `-` — Diesel models for the Mimir database.
- pub `catalog` module L6 — `-` — Diesel models for the Mimir database.

### crates/mimir-core/src/seed

> *Semantic summary to be generated by AI agent.*

#### crates/mimir-core/src/seed/dev.rs

- pub `TEST_CAMPAIGN_NAME` variable L45 — `: &str` — Name of the test campaign for detection.
- pub `is_already_seeded` function L48-51 — `(conn: &mut SqliteConnection) -> ServiceResult<bool>` — Check if dev seed data already exists.
- pub `seed_dev_data` function L61-124 — `(conn: &mut SqliteConnection, app_data_dir: &Path) -> ServiceResult<bool>` — Seed development data into the database.
- pub `clear_dev_seed_data` function L127-141 — `(conn: &mut SqliteConnection) -> ServiceResult<()>` — Clear existing dev seed data.
-  `SeededMonsters` struct L22-31 — `{ entrance_guards: String, bridge_archers: String, main_chamber: String, klarg: ...` — IDs of seeded monsters for token placement.
-  `SeededHomebrewMonsters` struct L34-36 — `{ cragmaw_mutant: String }` — IDs of seeded homebrew monsters for module monster creation.
-  `SeededNpcs` struct L39-42 — `{ sildar: String, yeemik: String }` — IDs of seeded NPCs for token placement.
-  `seed_campaign` function L147-152 — `(conn: &mut SqliteConnection) -> ServiceResult<crate::models::campaign::Campaign...` — Seeds the database with "The Lost Mine of Phandelver" test data.
-  `seed_characters` function L158-517 — `(conn: &mut SqliteConnection, campaign_id: &str) -> ServiceResult<Vec<Character>...` — Seeds the database with "The Lost Mine of Phandelver" test data.
-  `seed_homebrew_items` function L523-647 — `(conn: &mut SqliteConnection, campaign_id: &str) -> ServiceResult<usize>` — Seeds the database with "The Lost Mine of Phandelver" test data.
-  `seed_homebrew_monsters` function L653-729 — `( conn: &mut SqliteConnection, campaign_id: &str, ) -> ServiceResult<SeededHomeb...` — Seeds the database with "The Lost Mine of Phandelver" test data.
-  `seed_module` function L735-741 — `(conn: &mut SqliteConnection, campaign_id: &str) -> ServiceResult<Module>` — Seeds the database with "The Lost Mine of Phandelver" test data.
-  `seed_monsters` function L747-809 — `( conn: &mut SqliteConnection, module_id: &str, homebrew_monsters: &SeededHomebr...` — Seeds the database with "The Lost Mine of Phandelver" test data.
-  `seed_npcs` function L815-860 — `(conn: &mut SqliteConnection, module_id: &str) -> ServiceResult<SeededNpcs>` — Seeds the database with "The Lost Mine of Phandelver" test data.
-  `get_seed_assets_dir` function L867-898 — `() -> Option<PathBuf>` — Get the seed assets directory path.
-  `seed_maps` function L900-922 — `( conn: &mut SqliteConnection, campaign_id: &str, module_id: &str, assets_dir: &...` — Seeds the database with "The Lost Mine of Phandelver" test data.
-  `seed_uvtt_map` function L924-964 — `( conn: &mut SqliteConnection, campaign_id: &str, module_id: Option<&str>, uvtt_...` — Seeds the database with "The Lost Mine of Phandelver" test data.
-  `seed_png_map` function L966-1026 — `( conn: &mut SqliteConnection, campaign_id: &str, module_id: Option<&str>, png_p...` — Seeds the database with "The Lost Mine of Phandelver" test data.
-  `seed_tokens` function L1032-1140 — `( conn: &mut SqliteConnection, map_id: &str, monsters: &SeededMonsters, npcs: &S...` — Seeds the database with "The Lost Mine of Phandelver" test data.
-  `seed_lights` function L1146-1173 — `(conn: &mut SqliteConnection, map_id: &str) -> ServiceResult<usize>` — Seeds the database with "The Lost Mine of Phandelver" test data.
-  `seed_traps` function L1179-1197 — `(conn: &mut SqliteConnection, map_id: &str) -> ServiceResult<usize>` — Seeds the database with "The Lost Mine of Phandelver" test data.
-  `seed_pois` function L1203-1216 — `(conn: &mut SqliteConnection, map_id: &str) -> ServiceResult<usize>` — Seeds the database with "The Lost Mine of Phandelver" test data.

#### crates/mimir-core/src/seed/mod.rs

-  `dev` module L9 — `-` — Seeds the database with "The Lost Mine of Phandelver" test data

### crates/mimir-core/src/services

> *Semantic summary to be generated by AI agent.*

#### crates/mimir-core/src/services/archive.rs

- pub `ARCHIVE_EXTENSION` variable L37 — `: &str` — File extension for campaign archives
- pub `ArchiveManifest` struct L45-53 — `{ version: String, format: String, created_at: DateTime<Utc>, campaign_name: Str...` — Manifest file containing archive metadata
- pub `ArchiveCounts` struct L57-72 — `{ modules: usize, documents: usize, characters: usize, maps: usize, tokens: usiz...` — Counts of entities in the archive
- pub `CatalogReference` struct L76-81 — `{ ref_type: String, name: String, source: String }` — A reference to a catalog item found in campaign content
- pub `ArchivePreview` struct L85-92 — `{ campaign_name: String, counts: ArchiveCounts, catalog_references: Vec<CatalogR...` — Preview information about an archive without importing
- pub `ImportResult` struct L96-100 — `{ campaign_id: String, campaign_name: String, counts: ArchiveCounts }` — Result of importing an archive
- pub `ArchiveData` struct L118-135 — `{ campaign: Campaign, sources: Vec<String>, modules: Vec<Module>, documents: Vec...` — Complete archive data - uses existing models directly
- pub `CharacterWithRelated` struct L139-148 — `{ character: Character, classes: Vec<CharacterClass>, inventory: Vec<CharacterIn...` — Character with all related data aggregated
- pub `MapWithRelated` struct L152-159 — `{ map: Map, pois: Vec<MapPoi>, traps: Vec<MapTrap>, light_sources: Vec<LightSour...` — Map with all related data aggregated
- pub `ArchiveService` struct L166-168 — `{ conn: &'a mut SqliteConnection }` — Service for campaign archive operations
- pub `new` function L171-173 — `(conn: &'a mut SqliteConnection) -> Self` — and importing them back into new campaign instances.
- pub `export_campaign` function L176-223 — `( &mut self, campaign_id: &str, output_dir: &Path, assets_dir: &Path, ) -> Servi...` — Export a campaign to a tar.gz archive.
- pub `import_campaign` function L229-328 — `( &mut self, archive_path: &Path, assets_dir: &Path, campaign_name_override: Opt...` — Import a campaign from an archive.
- pub `preview_archive` function L331-366 — `(archive_path: &Path) -> ServiceResult<ArchivePreview>` — Preview an archive without importing.
-  `ARCHIVE_VERSION` variable L33 — `: &str` — Archive format version
-  `ARCHIVE_FORMAT` variable L35 — `: &str` — Archive format identifier
-  `IdMaps` struct L104-114 — `{ campaign: HashMap<String, String>, modules: HashMap<String, String>, documents...` — ID mapping tables for import (old ID -> new ID)
-  `collect_campaign_data` function L368-443 — `( &mut self, campaign_id: &str, campaign: Campaign, ) -> ServiceResult<ArchiveDa...` — and importing them back into new campaign instances.
-  `collect_asset_files` function L445-481 — `( &self, data: &ArchiveData, assets_dir: &Path, ) -> ServiceResult<HashMap<Strin...` — and importing them back into new campaign instances.
-  `extract_catalog_references` function L483-504 — `(&self, documents: &[Document]) -> Vec<CatalogReference>` — and importing them back into new campaign instances.
-  `create_archive` function L506-536 — `( &self, archive_path: &Path, manifest: &ArchiveManifest, data: &ArchiveData, as...` — and importing them back into new campaign instances.
-  `add_file_to_archive` function L538-560 — `( archive: &mut Builder<W>, path: &str, data: &[u8], ) -> ServiceResult<()>` — and importing them back into new campaign instances.
-  `extract_archive` function L566-572 — `(archive_path: &Path, dest: &Path) -> ServiceResult<()>` — and importing them back into new campaign instances.
-  `generate_unique_campaign_name` function L574-590 — `(&mut self, base_name: &str) -> String` — and importing them back into new campaign instances.
-  `import_assets` function L592-626 — `( &mut self, data: &ArchiveData, id_maps: &mut IdMaps, campaign_id: &str, assets...` — and importing them back into new campaign instances.
-  `import_modules` function L628-645 — `( &mut self, data: &ArchiveData, id_maps: &mut IdMaps, campaign_id: &str, ) -> S...` — and importing them back into new campaign instances.
-  `import_characters` function L647-794 — `( &mut self, data: &ArchiveData, id_maps: &mut IdMaps, campaign_id: &str, ) -> S...` — and importing them back into new campaign instances.
-  `import_documents` function L796-817 — `( &mut self, data: &ArchiveData, id_maps: &mut IdMaps, campaign_id: &str, ) -> S...` — and importing them back into new campaign instances.
-  `import_maps` function L819-928 — `( &mut self, data: &ArchiveData, id_maps: &mut IdMaps, campaign_id: &str, ) -> S...` — and importing them back into new campaign instances.
-  `import_module_entities` function L930-985 — `( &mut self, data: &ArchiveData, id_maps: &mut IdMaps, ) -> ServiceResult<()>` — and importing them back into new campaign instances.
-  `import_tokens` function L987-1019 — `(&mut self, data: &ArchiveData, id_maps: &IdMaps) -> ServiceResult<()>` — and importing them back into new campaign instances.
-  `import_homebrew_items` function L1021-1037 — `( &mut self, data: &ArchiveData, campaign_id: &str, ) -> ServiceResult<()>` — and importing them back into new campaign instances.
-  `import_homebrew_spells` function L1039-1055 — `( &mut self, data: &ArchiveData, campaign_id: &str, ) -> ServiceResult<()>` — and importing them back into new campaign instances.
-  `import_homebrew_monsters` function L1057-1076 — `( &mut self, data: &ArchiveData, campaign_id: &str, id_maps: &mut IdMaps, ) -> S...` — and importing them back into new campaign instances.
-  `slugify` function L1078-1087 — `(name: &str) -> String` — and importing them back into new campaign instances.
-  `tests` module L1091-1778 — `-` — and importing them back into new campaign instances.
-  `create_test_campaign` function L1104-1109 — `(conn: &mut SqliteConnection) -> String` — and importing them back into new campaign instances.
-  `create_test_module` function L1111-1117 — `(conn: &mut SqliteConnection, campaign_id: &str) -> String` — and importing them back into new campaign instances.
-  `seed_campaign` function L1120-1153 — `(conn: &mut SqliteConnection) -> (String, String)` — Seed a campaign with a module, document, and character for testing.
-  `test_slugify` function L1156-1160 — `()` — and importing them back into new campaign instances.
-  `test_catalog_reference_hash` function L1163-1180 — `()` — and importing them back into new campaign instances.
-  `test_export_creates_file` function L1183-1196 — `()` — and importing them back into new campaign instances.
-  `test_export_archive_contains_manifest_and_data` function L1199-1230 — `()` — and importing them back into new campaign instances.
-  `test_preview_archive` function L1233-1251 — `()` — and importing them back into new campaign instances.
-  `test_preview_extracts_catalog_references` function L1254-1273 — `()` — and importing them back into new campaign instances.
-  `test_export_nonexistent_campaign` function L1276-1285 — `()` — and importing them back into new campaign instances.
-  `test_preview_nonexistent_file` function L1288-1291 — `()` — and importing them back into new campaign instances.
-  `test_import_round_trip` function L1294-1338 — `()` — and importing them back into new campaign instances.
-  `test_import_generates_new_uuids` function L1341-1365 — `()` — and importing them back into new campaign instances.
-  `test_import_name_collision_auto_increments` function L1368-1396 — `()` — and importing them back into new campaign instances.
-  `test_import_invalid_archive` function L1399-1410 — `()` — and importing them back into new campaign instances.
-  `test_export_empty_campaign` function L1413-1429 — `()` — and importing them back into new campaign instances.
-  `test_import_preserves_document_content` function L1432-1453 — `()` — and importing them back into new campaign instances.
-  `seed_campaign_with_homebrew` function L1456-1499 — `(conn: &mut SqliteConnection) -> String` — Seed a campaign with homebrew items, monsters, and spells for testing.
-  `seed_campaign_with_detailed_character` function L1502-1580 — `(conn: &mut SqliteConnection) -> (String, String)` — Seed a campaign with a character that has classes, inventory, and proficiencies.
-  `test_homebrew_item_round_trip` function L1583-1635 — `()` — and importing them back into new campaign instances.
-  `test_character_details_round_trip` function L1638-1686 — `()` — and importing them back into new campaign instances.
-  `test_module_entities_round_trip` function L1689-1726 — `()` — and importing them back into new campaign instances.
-  `test_import_preserves_module_document_associations` function L1729-1752 — `()` — and importing them back into new campaign instances.
-  `test_export_import_empty_round_trip` function L1755-1777 — `()` — and importing them back into new campaign instances.

#### crates/mimir-core/src/services/asset.rs

- pub `UploadAssetInput` struct L19-32 — `{ campaign_id: Option<String>, module_id: Option<String>, filename: String, desc...` — Input for uploading an image asset.
- pub `for_campaign` function L36-50 — `( campaign_id: impl Into<String>, filename: impl Into<String>, mime_type: impl I...` — Create input for a campaign-level asset.
- pub `for_module` function L53-67 — `( module_id: impl Into<String>, filename: impl Into<String>, mime_type: impl Int...` — Create input for a module-level asset.
- pub `with_description` function L70-73 — `(mut self, description: impl Into<String>) -> Self` — Set the description/title for the asset.
- pub `AssetService` struct L79-82 — `{ conn: &'a mut SqliteConnection, app_data_dir: PathBuf }` — Service for managing binary assets.
- pub `new` function L86-91 — `(conn: &'a mut SqliteConnection, app_data_dir: impl Into<PathBuf>) -> Self` — Create a new asset service.
- pub `upload` function L96-163 — `(&mut self, input: UploadAssetInput) -> ServiceResult<CampaignAsset>` — Upload an asset (image or other file).
- pub `get` function L166-168 — `(&mut self, id: &str) -> ServiceResult<Option<CampaignAsset>>` — Get an asset by ID.
- pub `get_file_path` function L171-173 — `(&self, asset: &CampaignAsset) -> PathBuf` — Get the full file path for an asset.
- pub `read_file` function L176-179 — `(&self, asset: &CampaignAsset) -> ServiceResult<Vec<u8>>` — Read the file data for an asset.
- pub `delete` function L182-197 — `(&mut self, id: &str) -> ServiceResult<()>` — Delete an asset (removes from disk and database).
- pub `list_for_campaign` function L200-202 — `(&mut self, campaign_id: &str) -> ServiceResult<Vec<CampaignAsset>>` — List assets for a campaign.
- pub `list_for_module` function L205-207 — `(&mut self, module_id: &str) -> ServiceResult<Vec<CampaignAsset>>` — List assets for a module.
- pub `list_images_for_campaign` function L210-217 — `( &mut self, campaign_id: &str, ) -> ServiceResult<Vec<CampaignAsset>>` — List image assets for a campaign.
- pub `exists` function L220-222 — `(&mut self, id: &str) -> ServiceResult<bool>` — Check if an asset exists.
- pub `count_for_campaign` function L225-227 — `(&mut self, campaign_id: &str) -> ServiceResult<i64>` — Count assets for a campaign.
- pub `count_for_module` function L230-232 — `(&mut self, module_id: &str) -> ServiceResult<i64>` — Count assets for a module.
- pub `app_data_dir` function L235-237 — `(&self) -> &Path` — Get the app data directory.
-  `UploadAssetInput` type L34-74 — `= UploadAssetInput` — Assets are stored on disk with metadata in the database.
-  `tests` module L241-496 — `-` — Assets are stored on disk with metadata in the database.
-  `setup_test_env` function L248-252 — `() -> (SqliteConnection, TempDir)` — Assets are stored on disk with metadata in the database.
-  `create_test_campaign` function L254-259 — `(conn: &mut SqliteConnection) -> String` — Assets are stored on disk with metadata in the database.
-  `create_test_module` function L261-266 — `(conn: &mut SqliteConnection, campaign_id: &str) -> String` — Assets are stored on disk with metadata in the database.
-  `test_upload_campaign_asset` function L269-285 — `()` — Assets are stored on disk with metadata in the database.
-  `test_upload_module_asset` function L288-302 — `()` — Assets are stored on disk with metadata in the database.
-  `test_upload_invalid_mime_type` function L305-316 — `()` — Assets are stored on disk with metadata in the database.
-  `test_upload_no_owner` function L319-335 — `()` — Assets are stored on disk with metadata in the database.
-  `test_file_written_to_disk` function L338-355 — `()` — Assets are stored on disk with metadata in the database.
-  `test_read_file` function L358-371 — `()` — Assets are stored on disk with metadata in the database.
-  `test_get_asset` function L374-390 — `()` — Assets are stored on disk with metadata in the database.
-  `test_get_asset_not_found` function L393-400 — `()` — Assets are stored on disk with metadata in the database.
-  `test_delete_asset` function L403-420 — `()` — Assets are stored on disk with metadata in the database.
-  `test_delete_asset_not_found` function L423-430 — `()` — Assets are stored on disk with metadata in the database.
-  `test_list_for_campaign` function L433-448 — `()` — Assets are stored on disk with metadata in the database.
-  `test_list_for_module` function L451-467 — `()` — Assets are stored on disk with metadata in the database.
-  `test_count_assets` function L470-495 — `()` — Assets are stored on disk with metadata in the database.

#### crates/mimir-core/src/services/campaign.rs

- pub `CreateCampaignInput` struct L16-21 — `{ name: String, description: Option<String> }` — Input for creating a new campaign.
- pub `new` function L25-30 — `(name: impl Into<String>) -> Self` — Create a new campaign input.
- pub `with_description` function L33-36 — `(mut self, description: impl Into<String>) -> Self` — Set the description.
- pub `UpdateCampaignInput` struct L41-46 — `{ name: Option<String>, description: Option<Option<String>> }` — Input for updating a campaign.
- pub `set_name` function L50-55 — `(name: impl Into<String>) -> Self` — Create an update to change the name.
- pub `set_description` function L58-63 — `(description: Option<String>) -> Self` — Create an update to change the description.
- pub `CampaignService` struct L69-71 — `{ conn: &'a mut SqliteConnection }` — Service for campaign management.
- pub `new` function L75-77 — `(conn: &'a mut SqliteConnection) -> Self` — Create a new campaign service.
- pub `create` function L83-112 — `(&mut self, input: CreateCampaignInput) -> ServiceResult<Campaign>` — Create a new campaign with all 11 initial documents.
- pub `list` function L118-120 — `(&mut self, include_archived: bool) -> ServiceResult<Vec<Campaign>>` — List campaigns.
- pub `get` function L125-127 — `(&mut self, id: &str) -> ServiceResult<Option<Campaign>>` — Get a campaign by ID.
- pub `update` function L132-152 — `(&mut self, id: &str, input: UpdateCampaignInput) -> ServiceResult<Campaign>` — Update a campaign.
- pub `archive` function L157-167 — `(&mut self, id: &str) -> ServiceResult<()>` — Archive a campaign (soft delete).
- pub `unarchive` function L170-180 — `(&mut self, id: &str) -> ServiceResult<()>` — Unarchive a campaign.
- pub `delete` function L185-192 — `(&mut self, id: &str) -> ServiceResult<()>` — Delete a campaign permanently.
- pub `count` function L198-204 — `(&mut self, include_archived: bool) -> ServiceResult<i64>` — Count campaigns.
- pub `exists` function L207-209 — `(&mut self, id: &str) -> ServiceResult<bool>` — Check if a campaign exists.
-  `CreateCampaignInput` type L23-37 — `= CreateCampaignInput` — Business logic for campaign management including automatic document creation.
-  `UpdateCampaignInput` type L48-64 — `= UpdateCampaignInput` — Business logic for campaign management including automatic document creation.
-  `tests` module L213-424 — `-` — Business logic for campaign management including automatic document creation.
-  `test_create_campaign` function L219-229 — `()` — Business logic for campaign management including automatic document creation.
-  `test_create_campaign_with_description` function L232-241 — `()` — Business logic for campaign management including automatic document creation.
-  `test_create_campaign_creates_11_documents` function L244-266 — `()` — Business logic for campaign management including automatic document creation.
-  `test_list_campaigns` function L269-291 — `()` — Business logic for campaign management including automatic document creation.
-  `test_get_campaign` function L294-308 — `()` — Business logic for campaign management including automatic document creation.
-  `test_get_campaign_not_found` function L311-319 — `()` — Business logic for campaign management including automatic document creation.
-  `test_update_campaign_name` function L322-335 — `()` — Business logic for campaign management including automatic document creation.
-  `test_update_campaign_not_found` function L338-346 — `()` — Business logic for campaign management including automatic document creation.
-  `test_archive_and_unarchive` function L349-375 — `()` — Business logic for campaign management including automatic document creation.
-  `test_delete_campaign` function L378-392 — `()` — Business logic for campaign management including automatic document creation.
-  `test_delete_campaign_not_found` function L395-401 — `()` — Business logic for campaign management including automatic document creation.
-  `test_count_campaigns` function L404-423 — `()` — Business logic for campaign management including automatic document creation.

#### crates/mimir-core/src/services/character.rs

- pub `CreateCharacterInput` struct L23-48 — `{ campaign_id: Option<String>, name: String, is_npc: bool, player_name: Option<S...` — Input for creating a new character.
- pub `new_pc` function L52-71 — `( campaign_id: Option<impl Into<String>>, name: impl Into<String>, player_name: ...` — Create input for a new player character.
- pub `new_npc` function L74-89 — `(campaign_id: Option<impl Into<String>>, name: impl Into<String>) -> Self` — Create input for a new NPC.
- pub `with_race` function L92-96 — `(mut self, name: impl Into<String>, source: impl Into<String>) -> Self` — Set the race.
- pub `with_background` function L99-103 — `(mut self, name: impl Into<String>, source: impl Into<String>) -> Self` — Set the background.
- pub `with_ability_scores` function L106-109 — `(mut self, scores: [i32; 6]) -> Self` — Set ability scores [STR, DEX, CON, INT, WIS, CHA].
- pub `with_class` function L112-116 — `(mut self, name: impl Into<String>, source: impl Into<String>) -> Self` — Set starting class.
- pub `with_skills` function L119-122 — `(mut self, skills: Vec<String>) -> Self` — Set selected skill proficiencies.
- pub `UpdateCharacterInput` struct L127-143 — `{ name: Option<String>, player_name: Option<Option<String>>, race_name: Option<O...` — Input for updating a character.
- pub `set_name` function L147-152 — `(name: impl Into<String>) -> Self` — Update character name.
- pub `set_race` function L155-161 — `(name: Option<String>, source: Option<String>) -> Self` — Update race.
- pub `set_background` function L164-170 — `(name: Option<String>, source: Option<String>) -> Self` — Update background.
- pub `set_ability_scores` function L173-178 — `(scores: [i32; 6]) -> Self` — Update ability scores [STR, DEX, CON, INT, WIS, CHA].
- pub `set_currency` function L181-186 — `(currency: [i32; 5]) -> Self` — Update currency [CP, SP, EP, GP, PP].
- pub `set_roleplay` function L189-202 — `( traits: Option<String>, ideals: Option<String>, bonds: Option<String>, flaws: ...` — Update roleplay elements.
- pub `set_npc_info` function L205-216 — `( role: Option<String>, location: Option<String>, faction: Option<String>, ) -> ...` — Update NPC info.
- pub `AddInventoryInput` struct L221-234 — `{ item_name: String, item_source: String, quantity: Option<i32>, equipped: bool,...` — Input for adding an item to inventory.
- pub `new` function L238-247 — `(name: impl Into<String>, source: impl Into<String>) -> Self` — Create input for adding an item.
- pub `with_quantity` function L250-253 — `(mut self, quantity: i32) -> Self` — Set quantity.
- pub `equipped` function L256-259 — `(mut self) -> Self` — Mark as equipped.
- pub `attuned` function L262-265 — `(mut self) -> Self` — Mark as attuned.
- pub `with_notes` function L268-271 — `(mut self, notes: impl Into<String>) -> Self` — Add notes.
- pub `LevelUpRequest` struct L280-295 — `{ class_name: String, class_source: String, hit_points_method: HpGainMethod, sub...` — Request for leveling up a character.
- pub `SpellChanges` struct L299-308 — `{ new_spells: Vec<SpellReference>, new_cantrips: Vec<SpellReference>, swap_out: ...` — Spell changes during level up.
- pub `SpellReference` struct L312-317 — `{ name: String, source: String }` — Reference to a spell from the catalog.
- pub `FeatureChoices` struct L321-334 — `{ fighting_style: Option<FeatureReference>, metamagic: Option<Vec<FeatureReferen...` — Class feature choices during level up.
- pub `FeatureReference` struct L338-343 — `{ name: String, source: String }` — Reference to a class feature option from the catalog.
- pub `ManeuverChoices` struct L347-354 — `{ new_maneuvers: Vec<FeatureReference>, swap_out: Option<FeatureReference>, swap...` — Maneuver choices with optional swap for Battle Master.
- pub `InvocationChoices` struct L358-365 — `{ new_invocations: Vec<FeatureReference>, swap_out: Option<FeatureReference>, sw...` — Invocation choices with optional swap for Warlock.
- pub `HpGainMethod` enum L370-377 — `Average | Roll | Manual` — Method for gaining HP on level up.
- pub `AsiOrFeat` enum L382-401 — `AbilityScoreImprovement | Feat` — Ability Score Improvement or Feat selection.
- pub `SubclassChoice` struct L405-410 — `{ name: String, source: String }` — Subclass choice for level up.
- pub `LevelUpResult` struct L414-425 — `{ character: Character, class: CharacterClass, hp_gained: i32, new_total_level: ...` — Response from level up operation.
- pub `CharacterService` struct L794-796 — `{ conn: &'a mut SqliteConnection }` — Service for character management.
- pub `new` function L800-802 — `(conn: &'a mut SqliteConnection) -> Self` — Create a new character service.
- pub `create` function L805-886 — `(&mut self, input: CreateCharacterInput) -> ServiceResult<Character>` — Create a new character.
- pub `list_for_campaign` function L889-891 — `(&mut self, campaign_id: &str) -> ServiceResult<Vec<Character>>` — List all characters for a campaign.
- pub `list_pcs` function L894-896 — `(&mut self, campaign_id: &str) -> ServiceResult<Vec<Character>>` — List only player characters for a campaign.
- pub `list_npcs` function L899-901 — `(&mut self, campaign_id: &str) -> ServiceResult<Vec<Character>>` — List only NPCs for a campaign.
- pub `list_npcs_by_location` function L904-911 — `( &mut self, campaign_id: &str, location: &str, ) -> ServiceResult<Vec<Character...` — List NPCs by location.
- pub `list_npcs_by_faction` function L914-920 — `( &mut self, campaign_id: &str, faction: &str, ) -> ServiceResult<Vec<Character>...` — List NPCs by faction.
- pub `get` function L923-925 — `(&mut self, id: &str) -> ServiceResult<Option<Character>>` — Get a character by ID.
- pub `get_enriched` function L930-936 — `(&mut self, id: &str) -> ServiceResult<Option<CharacterResponse>>` — Get an enriched character by ID (includes classes and proficiencies).
- pub `enrich` function L942-951 — `(&mut self, character: Character) -> ServiceResult<CharacterResponse>` — Enrich a character with classes and proficiencies.
- pub `enrich_many` function L957-966 — `( &mut self, characters: Vec<Character>, ) -> ServiceResult<Vec<CharacterRespons...` — Enrich multiple characters with classes and proficiencies.
- pub `update` function L969-1021 — `(&mut self, id: &str, input: UpdateCharacterInput) -> ServiceResult<Character>` — Update a character.
- pub `delete` function L1024-1030 — `(&mut self, id: &str) -> ServiceResult<()>` — Delete a character and all related data.
- pub `exists` function L1033-1035 — `(&mut self, id: &str) -> ServiceResult<bool>` — Check if a character exists.
- pub `count_for_campaign` function L1038-1040 — `(&mut self, campaign_id: &str) -> ServiceResult<i64>` — Count all characters for a campaign.
- pub `count_pcs` function L1043-1045 — `(&mut self, campaign_id: &str) -> ServiceResult<i64>` — Count PCs for a campaign.
- pub `count_npcs` function L1048-1050 — `(&mut self, campaign_id: &str) -> ServiceResult<i64>` — Count NPCs for a campaign.
- pub `level_up` function L1058-1509 — `( &mut self, character_id: &str, request: LevelUpRequest, ) -> ServiceResult<Lev...` — Level up a character.
- pub `add_to_inventory` function L1514-1545 — `( &mut self, character_id: &str, input: AddInventoryInput, ) -> ServiceResult<Ch...` — Add an item to a character's inventory.
- pub `remove_from_inventory` function L1548-1554 — `(&mut self, inventory_id: &str) -> ServiceResult<()>` — Remove an item from a character's inventory.
- pub `get_inventory` function L1557-1559 — `(&mut self, character_id: &str) -> ServiceResult<Vec<CharacterInventory>>` — Get a character's inventory.
- pub `get_equipped_items` function L1562-1567 — `( &mut self, character_id: &str, ) -> ServiceResult<Vec<CharacterInventory>>` — Get equipped items for a character.
- pub `get_attuned_items` function L1570-1575 — `( &mut self, character_id: &str, ) -> ServiceResult<Vec<CharacterInventory>>` — Get attuned items for a character.
- pub `update_inventory_item` function L1578-1598 — `( &mut self, inventory_id: &str, quantity: Option<i32>, equipped: Option<bool>, ...` — Update an inventory item (quantity, equipped, attuned, notes).
- pub `count_attuned_items` function L1601-1603 — `(&mut self, character_id: &str) -> ServiceResult<i64>` — Count attuned items for a character (D&D 5e max is 3).
-  `CreateCharacterInput` type L50-123 — `= CreateCharacterInput` — Business logic for character management (PCs and NPCs).
-  `UpdateCharacterInput` type L145-217 — `= UpdateCharacterInput` — Business logic for character management (PCs and NPCs).
-  `AddInventoryInput` type L236-272 — `= AddInventoryInput` — Business logic for character management (PCs and NPCs).
-  `get_multiclass_prerequisites` function L434-456 — `(class_name: &str) -> Option<Vec<(&'static str, i32, bool)>>` — Multiclass prerequisites for D&D 5e classes.
-  `check_multiclass_prerequisites` function L459-502 — `( character: &Character, class_name: &str, ) -> Result<(), ServiceError>` — Check if a character meets multiclass prerequisites for a class.
-  `get_ability_score` function L505-515 — `(character: &Character, ability: &str) -> i32` — Get an ability score by name.
-  `set_ability_score` function L518-537 — `(character: &Character, ability: &str, new_value: i32) -> [i32; 6]` — Set an ability score by name, returning the new scores array.
-  `calculate_hp_gain` function L540-548 — `(method: &HpGainMethod, hit_die: i32, con_mod: i32) -> i32` — Calculate HP gain for a level up.
-  `get_class_hit_die` function L551-570 — `(conn: &mut SqliteConnection, class_name: &str, class_source: &str) -> i32` — Get hit die value for a class from catalog, returns d8 as default.
-  `ProficiencyEntry` struct L577-580 — `{ prof_type: ProficiencyType, name: String }` — A proficiency to be inserted during character creation.
-  `extract_keyed_proficiencies` function L586-617 — `( items: &[serde_json::Value], prof_type: ProficiencyType, ) -> Vec<ProficiencyE...` — Extract deterministic (non-choice) proficiencies from a keyed JSON object.
-  `capitalize_proficiency` function L620-629 — `(s: &str) -> String` — Capitalize a proficiency name for display.
-  `extract_class_proficiencies` function L632-686 — `( class_data: &serde_json::Value, selected_skills: &[String], ) -> Vec<Proficien...` — Extract proficiencies from class catalog data.
-  `extract_background_proficiencies` function L689-708 — `(bg_data: &serde_json::Value) -> Vec<ProficiencyEntry>` — Extract proficiencies from background catalog data.
-  `extract_race_proficiencies` function L711-740 — `(race_data: &serde_json::Value) -> Vec<ProficiencyEntry>` — Extract proficiencies from race catalog data.
-  `extract_multiclass_proficiencies` function L743-761 — `(class_data: &serde_json::Value) -> Vec<ProficiencyEntry>` — Extract multiclass proficiencies from class catalog data.
-  `insert_proficiencies` function L764-789 — `( conn: &mut SqliteConnection, character_id: &str, entries: &[ProficiencyEntry],...` — Insert proficiency entries for a character, skipping duplicates.
-  `tests` module L1607-2151 — `-` — Business logic for character management (PCs and NPCs).
-  `create_test_campaign` function L1613-1618 — `(conn: &mut SqliteConnection) -> String` — Business logic for character management (PCs and NPCs).
-  `test_create_pc` function L1621-1633 — `()` — Business logic for character management (PCs and NPCs).
-  `test_create_npc` function L1636-1648 — `()` — Business logic for character management (PCs and NPCs).
-  `test_create_with_race_and_background` function L1651-1666 — `()` — Business logic for character management (PCs and NPCs).
-  `test_create_with_ability_scores` function L1669-1685 — `()` — Business logic for character management (PCs and NPCs).
-  `test_list_for_campaign` function L1688-1711 — `()` — Business logic for character management (PCs and NPCs).
-  `test_get_character` function L1714-1730 — `()` — Business logic for character management (PCs and NPCs).
-  `test_get_character_not_found` function L1733-1740 — `()` — Business logic for character management (PCs and NPCs).
-  `test_update_character_name` function L1743-1758 — `()` — Business logic for character management (PCs and NPCs).
-  `test_update_character_race` function L1761-1778 — `()` — Business logic for character management (PCs and NPCs).
-  `test_update_character_not_found` function L1781-1790 — `()` — Business logic for character management (PCs and NPCs).
-  `test_delete_character` function L1793-1807 — `()` — Business logic for character management (PCs and NPCs).
-  `test_delete_character_not_found` function L1810-1817 — `()` — Business logic for character management (PCs and NPCs).
-  `test_count_characters` function L1820-1854 — `()` — Business logic for character management (PCs and NPCs).
-  `test_add_to_inventory` function L1857-1876 — `()` — Business logic for character management (PCs and NPCs).
-  `test_add_to_inventory_with_options` function L1879-1899 — `()` — Business logic for character management (PCs and NPCs).
-  `test_get_inventory` function L1902-1922 — `()` — Business logic for character management (PCs and NPCs).
-  `test_remove_from_inventory` function L1925-1946 — `()` — Business logic for character management (PCs and NPCs).
-  `test_update_inventory_item` function L1949-1972 — `()` — Business logic for character management (PCs and NPCs).
-  `test_extract_keyed_proficiencies_boolean_keys` function L1977-1984 — `()` — Business logic for character management (PCs and NPCs).
-  `test_extract_keyed_proficiencies_ignores_choice_keys` function L1987-1991 — `()` — Business logic for character management (PCs and NPCs).
-  `test_extract_keyed_proficiencies_ignores_any_prefix_keys` function L1994-1998 — `()` — Business logic for character management (PCs and NPCs).
-  `test_extract_keyed_proficiencies_string_values` function L2001-2011 — `()` — Business logic for character management (PCs and NPCs).
-  `test_extract_keyed_proficiencies_strips_source_suffix` function L2014-2020 — `()` — Business logic for character management (PCs and NPCs).
-  `test_extract_class_proficiencies_saves` function L2023-2039 — `()` — Business logic for character management (PCs and NPCs).
-  `test_extract_class_proficiencies_with_skills` function L2042-2056 — `()` — Business logic for character management (PCs and NPCs).
-  `test_extract_background_proficiencies` function L2059-2086 — `()` — Business logic for character management (PCs and NPCs).
-  `test_extract_race_proficiencies` function L2089-2109 — `()` — Business logic for character management (PCs and NPCs).
-  `test_extract_multiclass_proficiencies` function L2112-2135 — `()` — Business logic for character management (PCs and NPCs).
-  `test_extract_multiclass_proficiencies_no_multiclass` function L2138-2142 — `()` — Business logic for character management (PCs and NPCs).
-  `test_capitalize_proficiency` function L2145-2150 — `()` — Business logic for character management (PCs and NPCs).

#### crates/mimir-core/src/services/document.rs

- pub `CreateDocumentInput` struct L15-26 — `{ campaign_id: String, module_id: Option<String>, title: String, doc_type: Optio...` — Input for creating a blank document.
- pub `for_campaign` function L30-38 — `(campaign_id: impl Into<String>, title: impl Into<String>) -> Self` — Create input for a campaign-level document.
- pub `for_module` function L41-53 — `( campaign_id: impl Into<String>, module_id: impl Into<String>, title: impl Into...` — Create input for a module-level document.
- pub `with_type` function L56-59 — `(mut self, doc_type: impl Into<String>) -> Self` — Set the document type.
- pub `with_content` function L62-65 — `(mut self, content: impl Into<String>) -> Self` — Set initial content.
- pub `UpdateDocumentInput` struct L70-77 — `{ title: Option<String>, content: Option<String>, doc_type: Option<String> }` — Input for updating a document.
- pub `set_title` function L81-87 — `(title: impl Into<String>) -> Self` — Create an update to change the title.
- pub `set_content` function L90-96 — `(content: impl Into<String>) -> Self` — Create an update to change the content.
- pub `set_title_and_content` function L99-105 — `(title: impl Into<String>, content: impl Into<String>) -> Self` — Create an update to change both title and content.
- pub `DocumentService` struct L111-113 — `{ conn: &'a mut SqliteConnection }` — Service for document management.
- pub `new` function L117-119 — `(conn: &'a mut SqliteConnection) -> Self` — Create a new document service.
- pub `create` function L126-149 — `(&mut self, input: CreateDocumentInput) -> ServiceResult<Document>` — Create a new document.
- pub `list_for_campaign` function L152-154 — `(&mut self, campaign_id: &str) -> ServiceResult<Vec<Document>>` — List all campaign-level documents (not in any module).
- pub `list_for_module` function L157-159 — `(&mut self, module_id: &str) -> ServiceResult<Vec<Document>>` — List all documents for a specific module.
- pub `list_by_type` function L162-168 — `( &mut self, campaign_id: &str, doc_type: &str, ) -> ServiceResult<Vec<Document>...` — List documents by type for a campaign.
- pub `get` function L173-175 — `(&mut self, id: &str) -> ServiceResult<Option<Document>>` — Get a document by ID.
- pub `update` function L180-202 — `(&mut self, id: &str, input: UpdateDocumentInput) -> ServiceResult<Document>` — Update a document.
- pub `delete` function L205-212 — `(&mut self, id: &str) -> ServiceResult<()>` — Delete a document permanently.
- pub `move_to_module` function L215-225 — `(&mut self, id: &str, module_id: &str) -> ServiceResult<Document>` — Move a document to a different module.
- pub `move_to_campaign` function L228-238 — `(&mut self, id: &str) -> ServiceResult<Document>` — Move a document out of a module to campaign level.
- pub `exists` function L241-243 — `(&mut self, id: &str) -> ServiceResult<bool>` — Check if a document exists.
- pub `count_for_campaign` function L246-248 — `(&mut self, campaign_id: &str) -> ServiceResult<i64>` — Count documents for a campaign (all documents, including module docs).
- pub `count_for_module` function L251-253 — `(&mut self, module_id: &str) -> ServiceResult<i64>` — Count documents for a module.
- pub `search` function L256-262 — `( &mut self, campaign_id: &str, query: &str, ) -> ServiceResult<Vec<dal::Documen...` — Search documents using full-text search.
- pub `swap_order` function L267-286 — `( &mut self, doc_id_a: &str, doc_id_b: &str, ) -> ServiceResult<Vec<Document>>` — Swap sort_order between two documents.
- pub `search_in_module` function L289-295 — `( &mut self, module_id: &str, query: &str, ) -> ServiceResult<Vec<dal::DocumentS...` — Search documents within a specific module.
-  `CreateDocumentInput` type L28-66 — `= CreateDocumentInput` — Business logic for document management (campaign and module markdown content).
-  `UpdateDocumentInput` type L79-106 — `= UpdateDocumentInput` — Business logic for document management (campaign and module markdown content).
-  `tests` module L299-612 — `-` — Business logic for document management (campaign and module markdown content).
-  `create_test_campaign` function L305-310 — `(conn: &mut SqliteConnection) -> String` — Business logic for document management (campaign and module markdown content).
-  `create_test_module` function L312-317 — `(conn: &mut SqliteConnection, campaign_id: &str) -> String` — Business logic for document management (campaign and module markdown content).
-  `test_create_campaign_document` function L320-334 — `()` — Business logic for document management (campaign and module markdown content).
-  `test_create_module_document` function L337-349 — `()` — Business logic for document management (campaign and module markdown content).
-  `test_create_document_with_content` function L352-365 — `()` — Business logic for document management (campaign and module markdown content).
-  `test_list_for_campaign` function L368-388 — `()` — Business logic for document management (campaign and module markdown content).
-  `test_list_for_module` function L391-409 — `()` — Business logic for document management (campaign and module markdown content).
-  `test_get_document` function L412-428 — `()` — Business logic for document management (campaign and module markdown content).
-  `test_get_document_not_found` function L431-438 — `()` — Business logic for document management (campaign and module markdown content).
-  `test_update_document_title` function L441-456 — `()` — Business logic for document management (campaign and module markdown content).
-  `test_update_document_content` function L459-474 — `()` — Business logic for document management (campaign and module markdown content).
-  `test_update_document_not_found` function L477-486 — `()` — Business logic for document management (campaign and module markdown content).
-  `test_delete_document` function L489-505 — `()` — Business logic for document management (campaign and module markdown content).
-  `test_delete_document_not_found` function L508-515 — `()` — Business logic for document management (campaign and module markdown content).
-  `test_move_document_to_module` function L518-535 — `()` — Business logic for document management (campaign and module markdown content).
-  `test_move_document_to_campaign` function L538-555 — `()` — Business logic for document management (campaign and module markdown content).
-  `test_count_documents` function L558-584 — `()` — Business logic for document management (campaign and module markdown content).
-  `test_list_by_type` function L587-611 — `()` — Business logic for document management (campaign and module markdown content).

#### crates/mimir-core/src/services/homebrew.rs

- pub `CreateHomebrewItemInput` struct L29-37 — `{ campaign_id: String, name: String, data: Option<String>, item_type: Option<Str...` — Input for creating a homebrew item.
- pub `UpdateHomebrewItemInput` struct L41-46 — `{ name: Option<String>, data: Option<String>, item_type: Option<Option<String>>,...` — Input for updating a homebrew item.
- pub `CreateHomebrewMonsterInput` struct L54-63 — `{ campaign_id: String, name: String, data: Option<String>, cr: Option<String>, c...` — Input for creating a homebrew monster.
- pub `UpdateHomebrewMonsterInput` struct L67-73 — `{ name: Option<String>, data: Option<String>, cr: Option<Option<String>>, creatu...` — Input for updating a homebrew monster.
- pub `CreateHomebrewSpellInput` struct L81-89 — `{ campaign_id: String, name: String, data: Option<String>, level: Option<i32>, s...` — Input for creating a homebrew spell.
- pub `UpdateHomebrewSpellInput` struct L93-98 — `{ name: Option<String>, data: Option<String>, level: Option<Option<i32>>, school...` — Input for updating a homebrew spell.
- pub `HomebrewService` struct L103-105 — `{ conn: &'a mut SqliteConnection }` — Service for homebrew content management.
- pub `new` function L109-111 — `(conn: &'a mut SqliteConnection) -> Self` — Create a new homebrew service.
- pub `list_items` function L116-118 — `(&mut self, campaign_id: &str) -> ServiceResult<Vec<CampaignHomebrewItem>>` — List all homebrew items for a campaign.
- pub `get_item` function L121-123 — `(&mut self, id: &str) -> ServiceResult<CampaignHomebrewItem>` — Get a homebrew item by ID.
- pub `get_item_by_name` function L126-133 — `( &mut self, campaign_id: &str, name: &str, ) -> ServiceResult<Option<CampaignHo...` — Get a homebrew item by campaign ID and name.
- pub `create_item` function L139-170 — `( &mut self, input: CreateHomebrewItemInput, ) -> ServiceResult<CampaignHomebrew...` — Create a homebrew item.
- pub `update_item` function L173-202 — `( &mut self, id: &str, input: UpdateHomebrewItemInput, ) -> ServiceResult<Campai...` — Update a homebrew item.
- pub `delete_item` function L205-211 — `(&mut self, id: &str) -> ServiceResult<()>` — Delete a homebrew item.
- pub `list_monsters` function L216-221 — `( &mut self, campaign_id: &str, ) -> ServiceResult<Vec<CampaignHomebrewMonster>>` — List all homebrew monsters for a campaign.
- pub `get_monster` function L224-226 — `(&mut self, id: &str) -> ServiceResult<CampaignHomebrewMonster>` — Get a homebrew monster by ID.
- pub `get_monster_by_name` function L229-236 — `( &mut self, campaign_id: &str, name: &str, ) -> ServiceResult<Option<CampaignHo...` — Get a homebrew monster by campaign ID and name.
- pub `create_monster` function L242-278 — `( &mut self, input: CreateHomebrewMonsterInput, ) -> ServiceResult<CampaignHomeb...` — Create a homebrew monster.
- pub `update_monster` function L281-322 — `( &mut self, id: &str, input: UpdateHomebrewMonsterInput, ) -> ServiceResult<Cam...` — Update a homebrew monster.
- pub `delete_monster` function L325-331 — `(&mut self, id: &str) -> ServiceResult<()>` — Delete a homebrew monster.
- pub `list_spells` function L336-341 — `( &mut self, campaign_id: &str, ) -> ServiceResult<Vec<CampaignHomebrewSpell>>` — List all homebrew spells for a campaign.
- pub `get_spell` function L344-346 — `(&mut self, id: &str) -> ServiceResult<CampaignHomebrewSpell>` — Get a homebrew spell by ID.
- pub `get_spell_by_name` function L349-356 — `( &mut self, campaign_id: &str, name: &str, ) -> ServiceResult<Option<CampaignHo...` — Get a homebrew spell by campaign ID and name.
- pub `create_spell` function L362-394 — `( &mut self, input: CreateHomebrewSpellInput, ) -> ServiceResult<CampaignHomebre...` — Create a homebrew spell.
- pub `update_spell` function L397-426 — `( &mut self, id: &str, input: UpdateHomebrewSpellInput, ) -> ServiceResult<Campa...` — Update a homebrew spell.
- pub `delete_spell` function L429-435 — `(&mut self, id: &str) -> ServiceResult<()>` — Delete a homebrew spell.
-  `resolve_clone_data` function L447-489 — `( user_data: Option<&str>, cloned_from_name: Option<&str>, cloned_from_source: O...` — Resolve the data blob for a homebrew entity, handling clone-from-catalog.
-  `deep_merge` function L493-505 — `(target: &mut Value, source: &Value)` — Deep merge `source` into `target`.
-  `validate_json` function L508-512 — `(data: &str) -> ServiceResult<()>` — Validate that a string is valid JSON.
-  `enrich_monster_data` function L517-555 — `( data: &str, cr: Option<&str>, creature_type: Option<&str>, size: Option<&str>,...` — Ensure the monster data JSON blob includes cr, type, and size fields
-  `tests` module L558-1410 — `-` — that was previously duplicated across Tauri commands and MCP tools.
-  `create_test_campaign` function L564-569 — `(conn: &mut SqliteConnection) -> String` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_enrich_adds_missing_cr` function L574-579 — `()` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_enrich_preserves_existing_cr` function L582-587 — `()` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_enrich_adds_missing_type` function L590-595 — `()` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_enrich_preserves_existing_type` function L598-603 — `()` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_enrich_adds_missing_size` function L606-611 — `()` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_enrich_preserves_existing_size` function L614-619 — `()` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_enrich_adds_all_fields` function L622-629 — `()` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_enrich_no_args_is_noop` function L632-639 — `()` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_enrich_invalid_json` function L642-645 — `()` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_validate_json_valid` function L650-652 — `()` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_validate_json_invalid` function L655-657 — `()` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_create_item` function L662-683 — `()` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_create_item_invalid_json` function L686-702 — `()` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_list_items` function L705-735 — `()` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_get_item` function L738-757 — `()` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_update_item` function L760-792 — `()` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_update_item_not_found` function L795-807 — `()` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_delete_item` function L810-829 — `()` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_delete_item_not_found` function L832-838 — `()` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_create_monster` function L843-872 — `()` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_create_monster_enrichment_preserves_existing` function L875-899 — `()` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_create_monster_empty_data_gets_enriched` function L902-925 — `()` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_list_monsters` function L928-948 — `()` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_update_monster` function L951-984 — `()` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_update_monster_not_found` function L987-999 — `()` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_delete_monster` function L1002-1022 — `()` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_delete_monster_not_found` function L1025-1031 — `()` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_create_spell` function L1036-1056 — `()` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_create_spell_invalid_json` function L1059-1075 — `()` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_list_spells` function L1078-1108 — `()` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_update_spell` function L1111-1143 — `()` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_update_spell_not_found` function L1146-1158 — `()` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_delete_spell` function L1161-1180 — `()` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_delete_spell_not_found` function L1183-1189 — `()` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_get_item_by_name` function L1194-1217 — `()` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_get_monster_by_name` function L1220-1243 — `()` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_get_spell_by_name` function L1246-1268 — `()` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_clone_item_catalog_not_found` function L1273-1289 — `()` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_clone_monster_catalog_not_found` function L1292-1309 — `()` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_clone_spell_catalog_not_found` function L1312-1328 — `()` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_create_item_requires_data_when_not_cloning` function L1331-1347 — `()` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_create_monster_requires_data_when_not_cloning` function L1350-1367 — `()` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_create_spell_requires_data_when_not_cloning` function L1370-1386 — `()` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_deep_merge_objects` function L1389-1399 — `()` — that was previously duplicated across Tauri commands and MCP tools.
-  `test_deep_merge_replaces_non_objects` function L1402-1409 — `()` — that was previously duplicated across Tauri commands and MCP tools.

#### crates/mimir-core/src/services/map.rs

- pub `CreateMapInput` struct L19-34 — `{ campaign_id: String, module_id: Option<String>, name: String, description: Opt...` — Input for creating a map from a UVTT file.
- pub `for_campaign` function L38-53 — `( campaign_id: impl Into<String>, name: impl Into<String>, filename: impl Into<S...` — Create input for a campaign-level map.
- pub `for_module` function L56-72 — `( campaign_id: impl Into<String>, module_id: impl Into<String>, name: impl Into<...` — Create input for a module-level map.
- pub `with_description` function L75-78 — `(mut self, description: impl Into<String>) -> Self` — Set the description.
- pub `with_lighting_mode` function L81-84 — `(mut self, mode: LightingMode) -> Self` — Set the lighting mode.
- pub `UpdateMapInput` struct L89-100 — `{ name: Option<String>, description: Option<Option<String>>, sort_order: Option<...` — Input for updating a map.
- pub `set_name` function L104-109 — `(name: impl Into<String>) -> Self` — Update only the name.
- pub `set_description` function L112-117 — `(description: Option<String>) -> Self` — Update only the description.
- pub `set_sort_order` function L120-125 — `(sort_order: i32) -> Self` — Update only the sort order.
- pub `set_lighting_mode` function L128-133 — `(mode: LightingMode) -> Self` — Update only the lighting mode.
- pub `move_to_module` function L136-141 — `(module_id: impl Into<String>) -> Self` — Move map to a module.
- pub `move_to_campaign` function L144-149 — `() -> Self` — Move map to campaign level (out of any module).
- pub `MapService` struct L155-158 — `{ conn: &'a mut SqliteConnection, app_data_dir: PathBuf }` — Service for managing maps.
- pub `new` function L162-167 — `(conn: &'a mut SqliteConnection, app_data_dir: impl Into<PathBuf>) -> Self` — Create a new map service.
- pub `create` function L172-221 — `(&mut self, input: CreateMapInput) -> ServiceResult<Map>` — Create a new map from a UVTT file.
- pub `get` function L275-277 — `(&mut self, id: &str) -> ServiceResult<Option<Map>>` — Get a map by ID.
- pub `get_required` function L280-282 — `(&mut self, id: &str) -> ServiceResult<Map>` — Get a map by ID, returning an error if not found.
- pub `list_for_campaign` function L285-287 — `(&mut self, campaign_id: &str) -> ServiceResult<Vec<Map>>` — List all maps for a campaign (including module maps).
- pub `list_campaign_level` function L290-292 — `(&mut self, campaign_id: &str) -> ServiceResult<Vec<Map>>` — List only campaign-level maps (not in any module).
- pub `list_for_module` function L295-297 — `(&mut self, module_id: &str) -> ServiceResult<Vec<Map>>` — List all maps for a module.
- pub `update` function L300-333 — `(&mut self, id: &str, input: UpdateMapInput) -> ServiceResult<Map>` — Update a map.
- pub `delete` function L336-353 — `(&mut self, id: &str) -> ServiceResult<()>` — Delete a map and its associated UVTT asset.
- pub `exists` function L356-358 — `(&mut self, id: &str) -> ServiceResult<bool>` — Check if a map exists.
- pub `count_for_campaign` function L361-363 — `(&mut self, campaign_id: &str) -> ServiceResult<i64>` — Count maps for a campaign.
- pub `count_for_module` function L366-368 — `(&mut self, module_id: &str) -> ServiceResult<i64>` — Count maps for a module.
- pub `get_uvtt_asset` function L371-373 — `(&mut self, map: &Map) -> ServiceResult<Option<CampaignAsset>>` — Get the UVTT asset for a map.
- pub `read_uvtt_file` function L376-382 — `(&mut self, map: &Map) -> ServiceResult<Vec<u8>>` — Read the UVTT file data for a map.
-  `CreateMapInput` type L36-85 — `= CreateMapInput` — with metadata in the database.
-  `UpdateMapInput` type L102-150 — `= UpdateMapInput` — with metadata in the database.
-  `upload_uvtt_asset` function L224-272 — `( &mut self, campaign_id: &str, module_id: Option<&str>, filename: &str, data: &...` — Helper to upload a UVTT file as an asset.
-  `tests` module L386-742 — `-` — with metadata in the database.
-  `setup_test_env` function L393-397 — `() -> (SqliteConnection, TempDir)` — with metadata in the database.
-  `create_test_campaign` function L399-404 — `(conn: &mut SqliteConnection) -> String` — with metadata in the database.
-  `create_test_module` function L406-411 — `(conn: &mut SqliteConnection, campaign_id: &str) -> String` — with metadata in the database.
-  `fake_uvtt_data` function L413-416 — `() -> Vec<u8>` — with metadata in the database.
-  `test_create_campaign_map` function L419-434 — `()` — with metadata in the database.
-  `test_create_module_map` function L437-452 — `()` — with metadata in the database.
-  `test_create_map_with_description` function L455-467 — `()` — with metadata in the database.
-  `test_create_map_invalid_campaign` function L470-479 — `()` — with metadata in the database.
-  `test_create_map_invalid_module` function L482-492 — `()` — with metadata in the database.
-  `test_uvtt_file_stored_on_disk` function L495-509 — `()` — with metadata in the database.
-  `test_get_map` function L512-524 — `()` — with metadata in the database.
-  `test_get_map_not_found` function L527-534 — `()` — with metadata in the database.
-  `test_list_for_campaign` function L537-557 — `()` — with metadata in the database.
-  `test_list_for_module` function L560-576 — `()` — with metadata in the database.
-  `test_update_map_name` function L579-592 — `()` — with metadata in the database.
-  `test_update_lighting_mode` function L595-609 — `()` — with metadata in the database.
-  `test_update_move_to_module` function L612-627 — `()` — with metadata in the database.
-  `test_update_move_to_campaign` function L630-645 — `()` — with metadata in the database.
-  `test_update_map_not_found` function L648-656 — `()` — with metadata in the database.
-  `test_delete_map` function L659-677 — `()` — with metadata in the database.
-  `test_delete_map_not_found` function L680-687 — `()` — with metadata in the database.
-  `test_count_maps` function L690-706 — `()` — with metadata in the database.
-  `test_auto_increment_sort_order` function L709-726 — `()` — with metadata in the database.
-  `test_get_uvtt_asset` function L729-741 — `()` — with metadata in the database.

#### crates/mimir-core/src/services/mod.rs

- pub `catalog` module L9 — `-` — Services encapsulate validation, transactions, and orchestration of database operations.
- pub `DEFAULT_QUERY_LIMIT` variable L47 — `: i64` — Default query limit to prevent memory issues on large result sets.
- pub `ServiceError` enum L53-74 — `NotFound | Validation | Database | Io` — Service layer error type.
- pub `ServiceResult` type L77 — `= Result<T, ServiceError>` — Result type for service operations.
- pub `not_found` function L81-86 — `(entity_type: impl Into<String>, id: impl Into<String>) -> Self` — Create a NotFound error.
- pub `validation` function L89-91 — `(message: impl Into<String>) -> Self` — Create a Validation error.
-  `archive` module L6 — `-` — Business logic services that sit between consumers (MCP, Tauri) and the DAL layer.
-  `asset` module L7 — `-` — Services encapsulate validation, transactions, and orchestration of database operations.
-  `campaign` module L8 — `-` — Services encapsulate validation, transactions, and orchestration of database operations.
-  `character` module L10 — `-` — Services encapsulate validation, transactions, and orchestration of database operations.
-  `document` module L11 — `-` — Services encapsulate validation, transactions, and orchestration of database operations.
-  `homebrew` module L12 — `-` — Services encapsulate validation, transactions, and orchestration of database operations.
-  `map` module L13 — `-` — Services encapsulate validation, transactions, and orchestration of database operations.
-  `module` module L14 — `-` — Services encapsulate validation, transactions, and orchestration of database operations.
-  `token` module L15 — `-` — Services encapsulate validation, transactions, and orchestration of database operations.
-  `ServiceError` type L79-92 — `= ServiceError` — Services encapsulate validation, transactions, and orchestration of database operations.
-  `tests` module L95-123 — `-` — Services encapsulate validation, transactions, and orchestration of database operations.
-  `test_not_found_error` function L99-102 — `()` — Services encapsulate validation, transactions, and orchestration of database operations.
-  `test_validation_error` function L105-108 — `()` — Services encapsulate validation, transactions, and orchestration of database operations.
-  `test_database_error_conversion` function L111-115 — `()` — Services encapsulate validation, transactions, and orchestration of database operations.
-  `test_io_error_conversion` function L118-122 — `()` — Services encapsulate validation, transactions, and orchestration of database operations.

#### crates/mimir-core/src/services/module.rs

- pub `ModuleType` enum L16-30 — `General | Mystery | Dungeon | Heist | Horror | Political` — Module types that determine which template is used for the overview document.
- pub `template_key` function L34-43 — `(&self) -> &'static str` — Get the template key for this module type.
- pub `display_name` function L46-55 — `(&self) -> &'static str` — Get the display name for this module type.
- pub `CreateModuleInput` struct L79-88 — `{ campaign_id: String, name: String, description: Option<String>, module_type: M...` — Input for creating a new module.
- pub `new` function L92-99 — `(campaign_id: impl Into<String>, name: impl Into<String>) -> Self` — Create a new module input.
- pub `with_description` function L102-105 — `(mut self, description: impl Into<String>) -> Self` — Set the description.
- pub `with_type` function L108-111 — `(mut self, module_type: ModuleType) -> Self` — Set the module type.
- pub `UpdateModuleInput` struct L116-121 — `{ name: Option<String>, description: Option<Option<String>> }` — Input for updating a module.
- pub `set_name` function L125-130 — `(name: impl Into<String>) -> Self` — Create an update to change the name.
- pub `set_description` function L133-138 — `(description: Option<String>) -> Self` — Create an update to change the description.
- pub `ModuleService` struct L144-146 — `{ conn: &'a mut SqliteConnection }` — Service for module management.
- pub `new` function L150-152 — `(conn: &'a mut SqliteConnection) -> Self` — Create a new module service.
- pub `create` function L159-211 — `(&mut self, input: CreateModuleInput) -> ServiceResult<Module>` — Create a new module with type-specific overview and play notes documents.
- pub `list_for_campaign` function L214-216 — `(&mut self, campaign_id: &str) -> ServiceResult<Vec<Module>>` — List all modules for a campaign, ordered by module number.
- pub `get` function L221-223 — `(&mut self, id: &str) -> ServiceResult<Option<Module>>` — Get a module by ID.
- pub `get_by_number` function L226-232 — `( &mut self, campaign_id: &str, module_number: i32, ) -> ServiceResult<Option<Mo...` — Get a module by campaign ID and module number.
- pub `update` function L237-257 — `(&mut self, id: &str, input: UpdateModuleInput) -> ServiceResult<Module>` — Update a module.
- pub `delete` function L262-269 — `(&mut self, id: &str) -> ServiceResult<()>` — Delete a module permanently.
- pub `count_for_campaign` function L272-274 — `(&mut self, campaign_id: &str) -> ServiceResult<i64>` — Count modules for a campaign.
- pub `reorder` function L279-300 — `( &mut self, module_id: &str, new_position: i32, ) -> ServiceResult<Vec<Module>>` — Reorder a module by moving it to a new position (1-indexed).
- pub `exists` function L303-305 — `(&mut self, id: &str) -> ServiceResult<bool>` — Check if a module exists.
-  `ModuleType` type L32-56 — `= ModuleType` — Business logic for module management including type-based document creation.
-  `ModuleType` type L58-62 — `= ModuleType` — Business logic for module management including type-based document creation.
-  `fmt` function L59-61 — `(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result` — Business logic for module management including type-based document creation.
-  `ModuleType` type L64-75 — `= ModuleType` — Business logic for module management including type-based document creation.
-  `from` function L65-74 — `(s: Option<&str>) -> Self` — Business logic for module management including type-based document creation.
-  `CreateModuleInput` type L90-112 — `= CreateModuleInput` — Business logic for module management including type-based document creation.
-  `UpdateModuleInput` type L123-139 — `= UpdateModuleInput` — Business logic for module management including type-based document creation.
-  `tests` module L309-652 — `-` — Business logic for module management including type-based document creation.
-  `create_test_campaign` function L315-320 — `(conn: &mut SqliteConnection) -> String` — Business logic for module management including type-based document creation.
-  `test_create_module` function L323-336 — `()` — Business logic for module management including type-based document creation.
-  `test_create_module_with_description` function L339-353 — `()` — Business logic for module management including type-based document creation.
-  `test_create_module_creates_2_documents` function L356-378 — `()` — Business logic for module management including type-based document creation.
-  `test_create_module_with_type` function L381-401 — `()` — Business logic for module management including type-based document creation.
-  `test_create_modules_auto_increment_number` function L404-421 — `()` — Business logic for module management including type-based document creation.
-  `test_list_modules_for_campaign` function L424-441 — `()` — Business logic for module management including type-based document creation.
-  `test_get_module` function L444-460 — `()` — Business logic for module management including type-based document creation.
-  `test_get_module_not_found` function L463-470 — `()` — Business logic for module management including type-based document creation.
-  `test_get_module_by_number` function L473-488 — `()` — Business logic for module management including type-based document creation.
-  `test_update_module_name` function L491-506 — `()` — Business logic for module management including type-based document creation.
-  `test_update_module_not_found` function L509-518 — `()` — Business logic for module management including type-based document creation.
-  `test_delete_module` function L521-535 — `()` — Business logic for module management including type-based document creation.
-  `test_delete_module_not_found` function L538-545 — `()` — Business logic for module management including type-based document creation.
-  `test_count_modules` function L548-572 — `()` — Business logic for module management including type-based document creation.
-  `test_reorder_module` function L575-594 — `()` — Business logic for module management including type-based document creation.
-  `test_reorder_module_not_found` function L597-603 — `()` — Business logic for module management including type-based document creation.
-  `test_module_type_display` function L606-613 — `()` — Business logic for module management including type-based document creation.
-  `test_module_type_from_str` function L616-625 — `()` — Business logic for module management including type-based document creation.
-  `test_all_module_types_have_templates` function L628-651 — `()` — Business logic for module management including type-based document creation.

#### crates/mimir-core/src/services/token.rs

- pub `CreateTokenInput` struct L16-33 — `{ map_id: String, module_monster_id: Option<String>, module_npc_id: Option<Strin...` — Input for creating a token placement.
- pub `for_monster` function L37-53 — `( map_id: impl Into<String>, module_monster_id: impl Into<String>, grid_x: i32, ...` — Create input for a monster token.
- pub `for_npc` function L56-72 — `( map_id: impl Into<String>, module_npc_id: impl Into<String>, grid_x: i32, grid...` — Create input for an NPC token.
- pub `for_pc` function L75-86 — `(map_id: impl Into<String>, label: impl Into<String>, grid_x: i32, grid_y: i32) ...` — Create input for a PC token (requires label).
- pub `with_label` function L89-92 — `(mut self, label: impl Into<String>) -> Self` — Set a label.
- pub `with_faction_color` function L95-98 — `(mut self, color: impl Into<String>) -> Self` — Set a faction color.
- pub `hidden` function L101-104 — `(mut self) -> Self` — Mark as hidden.
- pub `UpdateTokenInput` struct L109-128 — `{ grid_x: Option<i32>, grid_y: Option<i32>, label: Option<Option<String>>, facti...` — Input for updating a token placement.
- pub `set_position` function L132-138 — `(grid_x: i32, grid_y: i32) -> Self` — Set position.
- pub `set_hidden` function L141-146 — `(hidden: bool) -> Self` — Set visibility.
- pub `set_vision` function L149-162 — `( vision_bright_ft: Option<i32>, vision_dim_ft: Option<i32>, vision_dark_ft: i32...` — Set vision settings.
- pub `TokenResponse` struct L167-187 — `{ id: String, map_id: String, name: String, token_type: String, size: String, gr...` — Token with resolved name and type information.
- pub `TokenService` struct L190-193 — `{ conn: &'a mut SqliteConnection, app_dir: &'a Path }` — Service for token management.
- pub `new` function L197-199 — `(conn: &'a mut SqliteConnection, app_dir: &'a Path) -> Self` — Create a new token service.
- pub `list` function L202-211 — `(&mut self, map_id: &str) -> ServiceResult<Vec<TokenResponse>>` — List all tokens for a map.
- pub `list_visible` function L214-223 — `(&mut self, map_id: &str) -> ServiceResult<Vec<TokenResponse>>` — List visible tokens for a map (player view).
- pub `get` function L226-233 — `(&mut self, id: &str) -> ServiceResult<Option<TokenResponse>>` — Get a token by ID.
- pub `create` function L236-281 — `(&mut self, input: CreateTokenInput) -> ServiceResult<TokenResponse>` — Create a new token.
- pub `update` function L284-308 — `(&mut self, id: &str, input: UpdateTokenInput) -> ServiceResult<TokenResponse>` — Update a token.
- pub `update_position` function L311-322 — `(&mut self, id: &str, grid_x: i32, grid_y: i32) -> ServiceResult<TokenResponse>` — Update just the position (optimized for drag operations).
- pub `update_vision` function L325-348 — `( &mut self, id: &str, vision_bright_ft: Option<i32>, vision_dim_ft: Option<i32>...` — Update vision settings.
- pub `toggle_visibility` function L351-362 — `(&mut self, id: &str) -> ServiceResult<TokenResponse>` — Toggle visibility.
- pub `delete` function L365-371 — `(&mut self, id: &str) -> ServiceResult<()>` — Delete a token.
- pub `count` function L374-376 — `(&mut self, map_id: &str) -> ServiceResult<i64>` — Count tokens for a map.
-  `CreateTokenInput` type L35-105 — `= CreateTokenInput` — Business logic for token placements on maps.
-  `UpdateTokenInput` type L130-163 — `= UpdateTokenInput` — Business logic for token placements on maps.
-  `get_grid_size` function L381-396 — `(&mut self, map_id: &str) -> i32` — Get the grid size (pixels per grid) from a map's UVTT file.
-  `enrich` function L399-427 — `(&mut self, placement: TokenPlacement, grid_size_px: i32) -> ServiceResult<Token...` — Enrich a TokenPlacement with resolved names and computed coordinates.
-  `resolve_names` function L430-480 — `(&mut self, token: &TokenPlacement) -> (String, Option<String>, String)` — Resolve token type and name from monster/NPC references.
-  `normalize_size_code` function L484-494 — `(size: &str) -> String` — Normalize size codes (T, S, M, L, H, G) to full names.
-  `tests` module L497-1252 — `-` — Business logic for token placements on maps.
-  `setup_test_env` function L510-514 — `() -> (SqliteConnection, TempDir)` — Set up a test environment with an in-memory DB and temp directory.
-  `create_prerequisites` function L518-547 — `(conn: &mut SqliteConnection) -> (String, String, String)` — Create prerequisite entities: campaign -> module -> asset -> map.
-  `create_module_monster` function L550-555 — `(conn: &mut SqliteConnection, module_id: &str) -> String` — Create a module monster in the DB and return its ID.
-  `create_module_npc` function L558-563 — `(conn: &mut SqliteConnection, module_id: &str) -> String` — Create a module NPC in the DB and return its ID.
-  `test_create_pc_token` function L568-584 — `()` — Business logic for token placements on maps.
-  `test_create_monster_token` function L587-600 — `()` — Business logic for token placements on maps.
-  `test_create_npc_token` function L603-617 — `()` — Business logic for token placements on maps.
-  `test_create_token_with_label_and_color` function L620-634 — `()` — Business logic for token placements on maps.
-  `test_create_hidden_token` function L637-646 — `()` — Business logic for token placements on maps.
-  `test_get_token` function L651-668 — `()` — Business logic for token placements on maps.
-  `test_get_token_not_found` function L671-677 — `()` — Business logic for token placements on maps.
-  `test_list_tokens` function L682-700 — `()` — Business logic for token placements on maps.
-  `test_list_tokens_different_maps` function L703-733 — `()` — Business logic for token placements on maps.
-  `test_list_visible_filters_hidden` function L738-757 — `()` — Business logic for token placements on maps.
-  `test_list_visible_empty_when_all_hidden` function L760-771 — `()` — Business logic for token placements on maps.
-  `test_update_token_position` function L776-789 — `()` — Business logic for token placements on maps.
-  `test_update_token_hidden` function L792-804 — `()` — Business logic for token placements on maps.
-  `test_update_token_label` function L807-821 — `()` — Business logic for token placements on maps.
-  `test_update_token_faction_color` function L824-838 — `()` — Business logic for token placements on maps.
-  `test_update_not_found` function L841-848 — `()` — Business logic for token placements on maps.
-  `test_update_position` function L853-868 — `()` — Business logic for token placements on maps.
-  `test_update_position_not_found` function L871-877 — `()` — Business logic for token placements on maps.
-  `test_update_vision` function L882-898 — `()` — Business logic for token placements on maps.
-  `test_update_vision_not_found` function L901-907 — `()` — Business logic for token placements on maps.
-  `test_toggle_visibility` function L912-932 — `()` — Business logic for token placements on maps.
-  `test_toggle_visibility_not_found` function L935-941 — `()` — Business logic for token placements on maps.
-  `test_delete_token` function L946-958 — `()` — Business logic for token placements on maps.
-  `test_delete_not_found` function L961-967 — `()` — Business logic for token placements on maps.
-  `test_count_tokens` function L972-986 — `()` — Business logic for token placements on maps.
-  `test_count_after_delete` function L989-1001 — `()` — Business logic for token placements on maps.
-  `test_create_pc_without_label_fails` function L1006-1024 — `()` — Business logic for token placements on maps.
-  `test_create_both_monster_and_npc_fails` function L1027-1047 — `()` — Business logic for token placements on maps.
-  `test_pixel_coordinates_use_default_grid_size` function L1052-1065 — `()` — Business logic for token placements on maps.
-  `test_pc_token_defaults_to_medium` function L1070-1079 — `()` — Business logic for token placements on maps.
-  `test_npc_token_defaults_to_medium` function L1082-1092 — `()` — Business logic for token placements on maps.
-  `test_monster_token_with_missing_reference_errors` function L1097-1107 — `()` — Business logic for token placements on maps.
-  `test_npc_token_with_missing_reference_errors` function L1110-1120 — `()` — Business logic for token placements on maps.
-  `test_normalize_size_code_values` function L1125-1132 — `()` — Business logic for token placements on maps.
-  `test_normalize_size_code_case_insensitive` function L1135-1139 — `()` — Business logic for token placements on maps.
-  `test_normalize_size_code_unknown` function L1142-1145 — `()` — Business logic for token placements on maps.
-  `test_default_vision_values` function L1150-1162 — `()` — Business logic for token placements on maps.
-  `test_create_update_delete_sequence` function L1167-1199 — `()` — Business logic for token placements on maps.
-  `test_negative_coordinates_allowed` function L1204-1214 — `()` — Business logic for token placements on maps.
-  `test_create_token_input_builders` function L1219-1233 — `()` — Business logic for token placements on maps.
-  `test_update_token_input_builders` function L1236-1251 — `()` — Business logic for token placements on maps.

### crates/mimir-core/src/services/catalog

> *Semantic summary to be generated by AI agent.*

#### crates/mimir-core/src/services/catalog/action.rs

- pub `ActionService` struct L14-16 — `{ conn: &'a mut SqliteConnection }` — Service for accessing action catalog data.
- pub `new` function L20-22 — `(conn: &'a mut SqliteConnection) -> Self` — Create a new action service.
- pub `list_by_source` function L25-27 — `(&mut self, source: &str) -> ServiceResult<Vec<Action>>` — List all actions from a specific source.
- pub `list_all` function L30-32 — `(&mut self) -> ServiceResult<Vec<Action>>` — List all actions (up to DEFAULT_QUERY_LIMIT).
-  `Entity` type L36 — `= Action` — Service layer for accessing action catalog data.
-  `Filter` type L37 — `= ActionFilter` — Service layer for accessing action catalog data.
-  `search` function L39-41 — `(&mut self, filter: &Self::Filter) -> ServiceResult<Vec<Self::Entity>>` — Service layer for accessing action catalog data.
-  `search_paginated` function L43-50 — `( &mut self, filter: &Self::Filter, limit: i64, offset: i64, ) -> ServiceResult<...` — Service layer for accessing action catalog data.
-  `get` function L52-54 — `(&mut self, id: i32) -> ServiceResult<Option<Self::Entity>>` — Service layer for accessing action catalog data.
-  `get_by_name_and_source` function L56-62 — `( &mut self, name: &str, source: &str, ) -> ServiceResult<Option<Self::Entity>>` — Service layer for accessing action catalog data.
-  `list_sources` function L64-66 — `(&mut self) -> ServiceResult<Vec<String>>` — Service layer for accessing action catalog data.
-  `count` function L68-70 — `(&mut self) -> ServiceResult<i64>` — Service layer for accessing action catalog data.
-  `count_by_source` function L72-74 — `(&mut self, source: &str) -> ServiceResult<i64>` — Service layer for accessing action catalog data.
-  `tests` module L78-130 — `-` — Service layer for accessing action catalog data.
-  `insert_test_actions` function L84-92 — `(conn: &mut SqliteConnection)` — Service layer for accessing action catalog data.
-  `test_action_service_search` function L95-105 — `()` — Service layer for accessing action catalog data.
-  `test_action_service_get` function L108-119 — `()` — Service layer for accessing action catalog data.
-  `test_action_service_count` function L122-129 — `()` — Service layer for accessing action catalog data.

#### crates/mimir-core/src/services/catalog/background.rs

- pub `BackgroundService` struct L14-16 — `{ conn: &'a mut SqliteConnection }` — Service for accessing background catalog data.
- pub `new` function L20-22 — `(conn: &'a mut SqliteConnection) -> Self` — Create a new background service.
- pub `list_by_source` function L25-27 — `(&mut self, source: &str) -> ServiceResult<Vec<Background>>` — List all backgrounds from a specific source.
- pub `list_all` function L30-32 — `(&mut self) -> ServiceResult<Vec<Background>>` — List all backgrounds (up to DEFAULT_QUERY_LIMIT).
-  `Entity` type L36 — `= Background` — Service layer for accessing background catalog data.
-  `Filter` type L37 — `= BackgroundFilter` — Service layer for accessing background catalog data.
-  `search` function L39-41 — `(&mut self, filter: &Self::Filter) -> ServiceResult<Vec<Self::Entity>>` — Service layer for accessing background catalog data.
-  `search_paginated` function L43-51 — `( &mut self, filter: &Self::Filter, limit: i64, offset: i64, ) -> ServiceResult<...` — Service layer for accessing background catalog data.
-  `get` function L53-55 — `(&mut self, id: i32) -> ServiceResult<Option<Self::Entity>>` — Service layer for accessing background catalog data.
-  `get_by_name_and_source` function L57-63 — `( &mut self, name: &str, source: &str, ) -> ServiceResult<Option<Self::Entity>>` — Service layer for accessing background catalog data.
-  `list_sources` function L65-67 — `(&mut self) -> ServiceResult<Vec<String>>` — Service layer for accessing background catalog data.
-  `count` function L69-71 — `(&mut self) -> ServiceResult<i64>` — Service layer for accessing background catalog data.
-  `count_by_source` function L73-75 — `(&mut self, source: &str) -> ServiceResult<i64>` — Service layer for accessing background catalog data.
-  `tests` module L79-153 — `-` — Service layer for accessing background catalog data.
-  `insert_test_backgrounds` function L85-94 — `(conn: &mut SqliteConnection)` — Service layer for accessing background catalog data.
-  `test_background_service_search` function L97-113 — `()` — Service layer for accessing background catalog data.
-  `test_background_service_get` function L116-127 — `()` — Service layer for accessing background catalog data.
-  `test_background_service_list_sources` function L130-140 — `()` — Service layer for accessing background catalog data.
-  `test_background_service_count` function L143-152 — `()` — Service layer for accessing background catalog data.

#### crates/mimir-core/src/services/catalog/class.rs

- pub `ClassService` struct L14-16 — `{ conn: &'a mut SqliteConnection }` — Service for accessing class catalog data.
- pub `new` function L20-22 — `(conn: &'a mut SqliteConnection) -> Self` — Create a new class service.
- pub `list_by_source` function L25-27 — `(&mut self, source: &str) -> ServiceResult<Vec<Class>>` — List all classes from a specific source.
- pub `list_all` function L30-32 — `(&mut self) -> ServiceResult<Vec<Class>>` — List all classes (up to DEFAULT_QUERY_LIMIT).
-  `Entity` type L36 — `= Class` — Service layer for accessing class catalog data.
-  `Filter` type L37 — `= ClassFilter` — Service layer for accessing class catalog data.
-  `search` function L39-41 — `(&mut self, filter: &Self::Filter) -> ServiceResult<Vec<Self::Entity>>` — Service layer for accessing class catalog data.
-  `search_paginated` function L43-50 — `( &mut self, filter: &Self::Filter, limit: i64, offset: i64, ) -> ServiceResult<...` — Service layer for accessing class catalog data.
-  `get` function L52-54 — `(&mut self, id: i32) -> ServiceResult<Option<Self::Entity>>` — Service layer for accessing class catalog data.
-  `get_by_name_and_source` function L56-62 — `( &mut self, name: &str, source: &str, ) -> ServiceResult<Option<Self::Entity>>` — Service layer for accessing class catalog data.
-  `list_sources` function L64-66 — `(&mut self) -> ServiceResult<Vec<String>>` — Service layer for accessing class catalog data.
-  `count` function L68-70 — `(&mut self) -> ServiceResult<i64>` — Service layer for accessing class catalog data.
-  `count_by_source` function L72-74 — `(&mut self, source: &str) -> ServiceResult<i64>` — Service layer for accessing class catalog data.
-  `tests` module L78-150 — `-` — Service layer for accessing class catalog data.
-  `insert_test_classes` function L84-93 — `(conn: &mut SqliteConnection)` — Service layer for accessing class catalog data.
-  `test_class_service_search` function L96-110 — `()` — Service layer for accessing class catalog data.
-  `test_class_service_get` function L113-124 — `()` — Service layer for accessing class catalog data.
-  `test_class_service_list_sources` function L127-137 — `()` — Service layer for accessing class catalog data.
-  `test_class_service_count` function L140-149 — `()` — Service layer for accessing class catalog data.

#### crates/mimir-core/src/services/catalog/class_feature.rs

- pub `ClassFeatureService` struct L12-14 — `{ conn: &'a mut SqliteConnection }` — Service for accessing class feature catalog data.
- pub `new` function L18-20 — `(conn: &'a mut SqliteConnection) -> Self` — Create a new class feature service.
- pub `get` function L23-25 — `(&mut self, id: i32) -> ServiceResult<ClassFeature>` — Get a class feature by ID.
- pub `get_by_name_and_source` function L28-37 — `( &mut self, name: &str, source: &str, class_name: &str, class_source: &str, ) -...` — Get a class feature by name, source, and class.
- pub `get_by_name_and_class` function L40-47 — `( &mut self, name: &str, class_name: &str, ) -> ServiceResult<Option<ClassFeatur...` — Get a class feature by name and class (any source).
- pub `list_by_class` function L50-57 — `( &mut self, class_name: &str, class_source: &str, ) -> ServiceResult<Vec<ClassF...` — List all class features for a specific class.
- pub `search` function L60-62 — `(&mut self, filter: &ClassFeatureFilter) -> ServiceResult<Vec<ClassFeature>>` — Search class features with filters.
- pub `list_sources` function L65-67 — `(&mut self) -> ServiceResult<Vec<String>>` — List all distinct sources.
- pub `count` function L70-72 — `(&mut self) -> ServiceResult<i64>` — Count all class features.
- pub `count_by_source` function L75-77 — `(&mut self, source: &str) -> ServiceResult<i64>` — Count class features by source.

#### crates/mimir-core/src/services/catalog/condition.rs

- pub `ConditionService` struct L14-16 — `{ conn: &'a mut SqliteConnection }` — Service for accessing condition catalog data.
- pub `new` function L20-22 — `(conn: &'a mut SqliteConnection) -> Self` — Create a new condition service.
- pub `list_by_source` function L25-27 — `(&mut self, source: &str) -> ServiceResult<Vec<Condition>>` — List all conditions from a specific source.
- pub `list_all` function L30-32 — `(&mut self) -> ServiceResult<Vec<Condition>>` — List all conditions (up to DEFAULT_QUERY_LIMIT).
-  `Entity` type L36 — `= Condition` — Service layer for accessing condition catalog data.
-  `Filter` type L37 — `= ConditionFilter` — Service layer for accessing condition catalog data.
-  `search` function L39-41 — `(&mut self, filter: &Self::Filter) -> ServiceResult<Vec<Self::Entity>>` — Service layer for accessing condition catalog data.
-  `search_paginated` function L43-51 — `( &mut self, filter: &Self::Filter, limit: i64, offset: i64, ) -> ServiceResult<...` — Service layer for accessing condition catalog data.
-  `get` function L53-55 — `(&mut self, id: i32) -> ServiceResult<Option<Self::Entity>>` — Service layer for accessing condition catalog data.
-  `get_by_name_and_source` function L57-63 — `( &mut self, name: &str, source: &str, ) -> ServiceResult<Option<Self::Entity>>` — Service layer for accessing condition catalog data.
-  `list_sources` function L65-67 — `(&mut self) -> ServiceResult<Vec<String>>` — Service layer for accessing condition catalog data.
-  `count` function L69-71 — `(&mut self) -> ServiceResult<i64>` — Service layer for accessing condition catalog data.
-  `count_by_source` function L73-75 — `(&mut self, source: &str) -> ServiceResult<i64>` — Service layer for accessing condition catalog data.
-  `tests` module L79-131 — `-` — Service layer for accessing condition catalog data.
-  `insert_test_conditions` function L85-93 — `(conn: &mut SqliteConnection)` — Service layer for accessing condition catalog data.
-  `test_condition_service_search` function L96-106 — `()` — Service layer for accessing condition catalog data.
-  `test_condition_service_get` function L109-120 — `()` — Service layer for accessing condition catalog data.
-  `test_condition_service_count` function L123-130 — `()` — Service layer for accessing condition catalog data.

#### crates/mimir-core/src/services/catalog/cult.rs

- pub `CultService` struct L14-16 — `{ conn: &'a mut SqliteConnection }` — Service for accessing cult catalog data.
- pub `new` function L20-22 — `(conn: &'a mut SqliteConnection) -> Self` — Create a new cult service.
- pub `list_by_source` function L25-27 — `(&mut self, source: &str) -> ServiceResult<Vec<Cult>>` — List all cults from a specific source.
- pub `list_all` function L30-32 — `(&mut self) -> ServiceResult<Vec<Cult>>` — List all cults (up to DEFAULT_QUERY_LIMIT).
-  `Entity` type L36 — `= Cult` — Service layer for accessing cult catalog data.
-  `Filter` type L37 — `= CultFilter` — Service layer for accessing cult catalog data.
-  `search` function L39-41 — `(&mut self, filter: &Self::Filter) -> ServiceResult<Vec<Self::Entity>>` — Service layer for accessing cult catalog data.
-  `search_paginated` function L43-50 — `( &mut self, filter: &Self::Filter, limit: i64, offset: i64, ) -> ServiceResult<...` — Service layer for accessing cult catalog data.
-  `get` function L52-54 — `(&mut self, id: i32) -> ServiceResult<Option<Self::Entity>>` — Service layer for accessing cult catalog data.
-  `get_by_name_and_source` function L56-62 — `( &mut self, name: &str, source: &str, ) -> ServiceResult<Option<Self::Entity>>` — Service layer for accessing cult catalog data.
-  `list_sources` function L64-66 — `(&mut self) -> ServiceResult<Vec<String>>` — Service layer for accessing cult catalog data.
-  `count` function L68-70 — `(&mut self) -> ServiceResult<i64>` — Service layer for accessing cult catalog data.
-  `count_by_source` function L72-74 — `(&mut self, source: &str) -> ServiceResult<i64>` — Service layer for accessing cult catalog data.

#### crates/mimir-core/src/services/catalog/deity.rs

- pub `DeityService` struct L14-16 — `{ conn: &'a mut SqliteConnection }` — Service for accessing deity catalog data.
- pub `new` function L20-22 — `(conn: &'a mut SqliteConnection) -> Self` — Create a new deity service.
- pub `list_by_source` function L25-27 — `(&mut self, source: &str) -> ServiceResult<Vec<Deity>>` — List all deities from a specific source.
- pub `list_all` function L30-32 — `(&mut self) -> ServiceResult<Vec<Deity>>` — List all deities (up to DEFAULT_QUERY_LIMIT).
- pub `list_by_pantheon` function L35-37 — `(&mut self, pantheon: &str) -> ServiceResult<Vec<Deity>>` — List deities by pantheon.
-  `Entity` type L41 — `= Deity` — Service layer for accessing deity catalog data.
-  `Filter` type L42 — `= DeityFilter` — Service layer for accessing deity catalog data.
-  `search` function L44-46 — `(&mut self, filter: &Self::Filter) -> ServiceResult<Vec<Self::Entity>>` — Service layer for accessing deity catalog data.
-  `search_paginated` function L48-55 — `( &mut self, filter: &Self::Filter, limit: i64, offset: i64, ) -> ServiceResult<...` — Service layer for accessing deity catalog data.
-  `get` function L57-59 — `(&mut self, id: i32) -> ServiceResult<Option<Self::Entity>>` — Service layer for accessing deity catalog data.
-  `get_by_name_and_source` function L61-67 — `( &mut self, name: &str, source: &str, ) -> ServiceResult<Option<Self::Entity>>` — Service layer for accessing deity catalog data.
-  `list_sources` function L69-71 — `(&mut self) -> ServiceResult<Vec<String>>` — Service layer for accessing deity catalog data.
-  `count` function L73-75 — `(&mut self) -> ServiceResult<i64>` — Service layer for accessing deity catalog data.
-  `count_by_source` function L77-79 — `(&mut self, source: &str) -> ServiceResult<i64>` — Service layer for accessing deity catalog data.

#### crates/mimir-core/src/services/catalog/feat.rs

- pub `FeatService` struct L14-16 — `{ conn: &'a mut SqliteConnection }` — Service for accessing feat catalog data.
- pub `new` function L20-22 — `(conn: &'a mut SqliteConnection) -> Self` — Create a new feat service.
- pub `list_by_source` function L25-27 — `(&mut self, source: &str) -> ServiceResult<Vec<Feat>>` — List all feats from a specific source.
- pub `list_all` function L30-32 — `(&mut self) -> ServiceResult<Vec<Feat>>` — List all feats (up to DEFAULT_QUERY_LIMIT).
-  `Entity` type L36 — `= Feat` — Service layer for accessing feat catalog data.
-  `Filter` type L37 — `= FeatFilter` — Service layer for accessing feat catalog data.
-  `search` function L39-41 — `(&mut self, filter: &Self::Filter) -> ServiceResult<Vec<Self::Entity>>` — Service layer for accessing feat catalog data.
-  `search_paginated` function L43-50 — `( &mut self, filter: &Self::Filter, limit: i64, offset: i64, ) -> ServiceResult<...` — Service layer for accessing feat catalog data.
-  `get` function L52-54 — `(&mut self, id: i32) -> ServiceResult<Option<Self::Entity>>` — Service layer for accessing feat catalog data.
-  `get_by_name_and_source` function L56-62 — `( &mut self, name: &str, source: &str, ) -> ServiceResult<Option<Self::Entity>>` — Service layer for accessing feat catalog data.
-  `list_sources` function L64-66 — `(&mut self) -> ServiceResult<Vec<String>>` — Service layer for accessing feat catalog data.
-  `count` function L68-70 — `(&mut self) -> ServiceResult<i64>` — Service layer for accessing feat catalog data.
-  `count_by_source` function L72-74 — `(&mut self, source: &str) -> ServiceResult<i64>` — Service layer for accessing feat catalog data.
-  `tests` module L78-133 — `-` — Service layer for accessing feat catalog data.
-  `insert_test_feats` function L84-92 — `(conn: &mut SqliteConnection)` — Service layer for accessing feat catalog data.
-  `test_feat_service_search` function L95-107 — `()` — Service layer for accessing feat catalog data.
-  `test_feat_service_get` function L110-121 — `()` — Service layer for accessing feat catalog data.
-  `test_feat_service_count` function L124-132 — `()` — Service layer for accessing feat catalog data.

#### crates/mimir-core/src/services/catalog/hazard.rs

- pub `HazardService` struct L14-16 — `{ conn: &'a mut SqliteConnection }` — Service for accessing hazard catalog data.
- pub `new` function L20-22 — `(conn: &'a mut SqliteConnection) -> Self` — Create a new hazard service.
- pub `list_by_source` function L25-27 — `(&mut self, source: &str) -> ServiceResult<Vec<Hazard>>` — List all hazards from a specific source.
- pub `list_all` function L30-32 — `(&mut self) -> ServiceResult<Vec<Hazard>>` — List all hazards (up to DEFAULT_QUERY_LIMIT).
-  `Entity` type L36 — `= Hazard` — Service layer for accessing hazard catalog data.
-  `Filter` type L37 — `= HazardFilter` — Service layer for accessing hazard catalog data.
-  `search` function L39-41 — `(&mut self, filter: &Self::Filter) -> ServiceResult<Vec<Self::Entity>>` — Service layer for accessing hazard catalog data.
-  `search_paginated` function L43-50 — `( &mut self, filter: &Self::Filter, limit: i64, offset: i64, ) -> ServiceResult<...` — Service layer for accessing hazard catalog data.
-  `get` function L52-54 — `(&mut self, id: i32) -> ServiceResult<Option<Self::Entity>>` — Service layer for accessing hazard catalog data.
-  `get_by_name_and_source` function L56-62 — `( &mut self, name: &str, source: &str, ) -> ServiceResult<Option<Self::Entity>>` — Service layer for accessing hazard catalog data.
-  `list_sources` function L64-66 — `(&mut self) -> ServiceResult<Vec<String>>` — Service layer for accessing hazard catalog data.
-  `count` function L68-70 — `(&mut self) -> ServiceResult<i64>` — Service layer for accessing hazard catalog data.
-  `count_by_source` function L72-74 — `(&mut self, source: &str) -> ServiceResult<i64>` — Service layer for accessing hazard catalog data.
-  `tests` module L78-133 — `-` — Service layer for accessing hazard catalog data.
-  `insert_test_hazards` function L84-91 — `(conn: &mut SqliteConnection)` — Service layer for accessing hazard catalog data.
-  `test_hazard_service_search` function L94-108 — `()` — Service layer for accessing hazard catalog data.
-  `test_hazard_service_get` function L111-122 — `()` — Service layer for accessing hazard catalog data.
-  `test_hazard_service_count` function L125-132 — `()` — Service layer for accessing hazard catalog data.

#### crates/mimir-core/src/services/catalog/item.rs

- pub `ItemService` struct L14-16 — `{ conn: &'a mut SqliteConnection }` — Service for accessing item catalog data.
- pub `new` function L20-22 — `(conn: &'a mut SqliteConnection) -> Self` — Create a new item service.
- pub `list_weapon_names` function L25-27 — `(&mut self) -> ServiceResult<Vec<String>>` — List all weapon names from the catalog (item_type 'M' or 'R').
- pub `list_by_source` function L30-32 — `(&mut self, source: &str) -> ServiceResult<Vec<Item>>` — List all items from a specific source.
- pub `list_all` function L35-37 — `(&mut self) -> ServiceResult<Vec<Item>>` — List all items (up to DEFAULT_QUERY_LIMIT).
-  `Entity` type L41 — `= Item` — Service layer for accessing item catalog data.
-  `Filter` type L42 — `= ItemFilter` — Service layer for accessing item catalog data.
-  `search` function L44-46 — `(&mut self, filter: &Self::Filter) -> ServiceResult<Vec<Self::Entity>>` — Service layer for accessing item catalog data.
-  `search_paginated` function L48-55 — `( &mut self, filter: &Self::Filter, limit: i64, offset: i64, ) -> ServiceResult<...` — Service layer for accessing item catalog data.
-  `get` function L57-59 — `(&mut self, id: i32) -> ServiceResult<Option<Self::Entity>>` — Service layer for accessing item catalog data.
-  `get_by_name_and_source` function L61-67 — `( &mut self, name: &str, source: &str, ) -> ServiceResult<Option<Self::Entity>>` — Service layer for accessing item catalog data.
-  `list_sources` function L69-71 — `(&mut self) -> ServiceResult<Vec<String>>` — Service layer for accessing item catalog data.
-  `count` function L73-75 — `(&mut self) -> ServiceResult<i64>` — Service layer for accessing item catalog data.
-  `count_by_source` function L77-79 — `(&mut self, source: &str) -> ServiceResult<i64>` — Service layer for accessing item catalog data.
-  `tests` module L83-169 — `-` — Service layer for accessing item catalog data.
-  `insert_test_items` function L89-105 — `(conn: &mut SqliteConnection)` — Service layer for accessing item catalog data.
-  `test_item_service_search` function L108-129 — `()` — Service layer for accessing item catalog data.
-  `test_item_service_get` function L132-143 — `()` — Service layer for accessing item catalog data.
-  `test_item_service_list_sources` function L146-156 — `()` — Service layer for accessing item catalog data.
-  `test_item_service_count` function L159-168 — `()` — Service layer for accessing item catalog data.

#### crates/mimir-core/src/services/catalog/language.rs

- pub `LanguageService` struct L14-16 — `{ conn: &'a mut SqliteConnection }` — Service for accessing language catalog data.
- pub `new` function L20-22 — `(conn: &'a mut SqliteConnection) -> Self` — Create a new language service.
- pub `list_by_source` function L25-27 — `(&mut self, source: &str) -> ServiceResult<Vec<Language>>` — List all languages from a specific source.
- pub `list_all` function L30-32 — `(&mut self) -> ServiceResult<Vec<Language>>` — List all languages (up to DEFAULT_QUERY_LIMIT).
- pub `list_by_type` function L35-38 — `(&mut self, language_type: &str) -> ServiceResult<Vec<Language>>` — List languages by type (standard, exotic, secret).
-  `Entity` type L42 — `= Language` — Service layer for accessing language catalog data.
-  `Filter` type L43 — `= LanguageFilter` — Service layer for accessing language catalog data.
-  `search` function L45-47 — `(&mut self, filter: &Self::Filter) -> ServiceResult<Vec<Self::Entity>>` — Service layer for accessing language catalog data.
-  `search_paginated` function L49-57 — `( &mut self, filter: &Self::Filter, limit: i64, offset: i64, ) -> ServiceResult<...` — Service layer for accessing language catalog data.
-  `get` function L59-61 — `(&mut self, id: i32) -> ServiceResult<Option<Self::Entity>>` — Service layer for accessing language catalog data.
-  `get_by_name_and_source` function L63-69 — `( &mut self, name: &str, source: &str, ) -> ServiceResult<Option<Self::Entity>>` — Service layer for accessing language catalog data.
-  `list_sources` function L71-73 — `(&mut self) -> ServiceResult<Vec<String>>` — Service layer for accessing language catalog data.
-  `count` function L75-77 — `(&mut self) -> ServiceResult<i64>` — Service layer for accessing language catalog data.
-  `count_by_source` function L79-81 — `(&mut self, source: &str) -> ServiceResult<i64>` — Service layer for accessing language catalog data.
-  `tests` module L85-151 — `-` — Service layer for accessing language catalog data.
-  `insert_test_languages` function L91-99 — `(conn: &mut SqliteConnection)` — Service layer for accessing language catalog data.
-  `test_language_service_search` function L102-112 — `()` — Service layer for accessing language catalog data.
-  `test_language_service_get` function L115-126 — `()` — Service layer for accessing language catalog data.
-  `test_language_service_count` function L129-136 — `()` — Service layer for accessing language catalog data.
-  `test_language_service_by_type` function L139-150 — `()` — Service layer for accessing language catalog data.

#### crates/mimir-core/src/services/catalog/mod.rs

- pub `CatalogEntityService` interface L61-99 — `{ fn search(), fn search_paginated(), fn get(), fn get_by_name_and_source(), fn ...` — Trait for catalog entity services.
-  `action` module L5 — `-` — Services for accessing D&D 5e catalog content (monsters, spells, items, etc.).
-  `background` module L6 — `-` — Services for accessing D&D 5e catalog content (monsters, spells, items, etc.).
-  `class` module L7 — `-` — Services for accessing D&D 5e catalog content (monsters, spells, items, etc.).
-  `class_feature` module L8 — `-` — Services for accessing D&D 5e catalog content (monsters, spells, items, etc.).
-  `condition` module L9 — `-` — Services for accessing D&D 5e catalog content (monsters, spells, items, etc.).
-  `cult` module L10 — `-` — Services for accessing D&D 5e catalog content (monsters, spells, items, etc.).
-  `deity` module L11 — `-` — Services for accessing D&D 5e catalog content (monsters, spells, items, etc.).
-  `feat` module L12 — `-` — Services for accessing D&D 5e catalog content (monsters, spells, items, etc.).
-  `hazard` module L13 — `-` — Services for accessing D&D 5e catalog content (monsters, spells, items, etc.).
-  `item` module L14 — `-` — Services for accessing D&D 5e catalog content (monsters, spells, items, etc.).
-  `language` module L15 — `-` — Services for accessing D&D 5e catalog content (monsters, spells, items, etc.).
-  `monster` module L16 — `-` — Services for accessing D&D 5e catalog content (monsters, spells, items, etc.).
-  `object` module L17 — `-` — Services for accessing D&D 5e catalog content (monsters, spells, items, etc.).
-  `optional_feature` module L18 — `-` — Services for accessing D&D 5e catalog content (monsters, spells, items, etc.).
-  `psionic` module L19 — `-` — Services for accessing D&D 5e catalog content (monsters, spells, items, etc.).
-  `race` module L20 — `-` — Services for accessing D&D 5e catalog content (monsters, spells, items, etc.).
-  `reward` module L21 — `-` — Services for accessing D&D 5e catalog content (monsters, spells, items, etc.).
-  `spell` module L22 — `-` — Services for accessing D&D 5e catalog content (monsters, spells, items, etc.).
-  `subclass` module L23 — `-` — Services for accessing D&D 5e catalog content (monsters, spells, items, etc.).
-  `subclass_feature` module L24 — `-` — Services for accessing D&D 5e catalog content (monsters, spells, items, etc.).
-  `table` module L25 — `-` — Services for accessing D&D 5e catalog content (monsters, spells, items, etc.).
-  `trap` module L26 — `-` — Services for accessing D&D 5e catalog content (monsters, spells, items, etc.).
-  `variant_rule` module L27 — `-` — Services for accessing D&D 5e catalog content (monsters, spells, items, etc.).
-  `vehicle` module L28 — `-` — Services for accessing D&D 5e catalog content (monsters, spells, items, etc.).

#### crates/mimir-core/src/services/catalog/monster.rs

- pub `MonsterService` struct L29-31 — `{ conn: &'a mut SqliteConnection }` — Service for accessing monster catalog data.
- pub `new` function L35-37 — `(conn: &'a mut SqliteConnection) -> Self` — Create a new monster service.
- pub `list_by_source` function L40-42 — `(&mut self, source: &str) -> ServiceResult<Vec<Monster>>` — List all monsters from a specific source.
- pub `list_all` function L47-49 — `(&mut self) -> ServiceResult<Vec<Monster>>` — List all monsters (up to DEFAULT_QUERY_LIMIT).
-  `Entity` type L53 — `= Monster` — Service layer for accessing monster catalog data.
-  `Filter` type L54 — `= MonsterFilter` — Service layer for accessing monster catalog data.
-  `search` function L56-59 — `(&mut self, filter: &Self::Filter) -> ServiceResult<Vec<Self::Entity>>` — Service layer for accessing monster catalog data.
-  `search_paginated` function L61-68 — `( &mut self, filter: &Self::Filter, limit: i64, offset: i64, ) -> ServiceResult<...` — Service layer for accessing monster catalog data.
-  `get` function L70-72 — `(&mut self, id: i32) -> ServiceResult<Option<Self::Entity>>` — Service layer for accessing monster catalog data.
-  `get_by_name_and_source` function L74-80 — `( &mut self, name: &str, source: &str, ) -> ServiceResult<Option<Self::Entity>>` — Service layer for accessing monster catalog data.
-  `list_sources` function L82-84 — `(&mut self) -> ServiceResult<Vec<String>>` — Service layer for accessing monster catalog data.
-  `count` function L86-88 — `(&mut self) -> ServiceResult<i64>` — Service layer for accessing monster catalog data.
-  `count_by_source` function L90-92 — `(&mut self, source: &str) -> ServiceResult<i64>` — Service layer for accessing monster catalog data.
-  `tests` module L96-241 — `-` — Service layer for accessing monster catalog data.
-  `insert_test_monsters` function L102-126 — `(conn: &mut SqliteConnection)` — Service layer for accessing monster catalog data.
-  `test_monster_service_search` function L129-150 — `()` — Service layer for accessing monster catalog data.
-  `test_monster_service_search_paginated` function L153-174 — `()` — Service layer for accessing monster catalog data.
-  `test_monster_service_get` function L177-204 — `()` — Service layer for accessing monster catalog data.
-  `test_monster_service_list_sources` function L207-217 — `()` — Service layer for accessing monster catalog data.
-  `test_monster_service_count` function L220-229 — `()` — Service layer for accessing monster catalog data.
-  `test_monster_service_list_by_source` function L232-240 — `()` — Service layer for accessing monster catalog data.

#### crates/mimir-core/src/services/catalog/object.rs

- pub `ObjectService` struct L14-16 — `{ conn: &'a mut SqliteConnection }` — Service for accessing object catalog data.
- pub `new` function L20-22 — `(conn: &'a mut SqliteConnection) -> Self` — Create a new object service.
- pub `list_by_source` function L25-27 — `(&mut self, source: &str) -> ServiceResult<Vec<Object>>` — List all objects from a specific source.
- pub `list_all` function L30-32 — `(&mut self) -> ServiceResult<Vec<Object>>` — List all objects (up to DEFAULT_QUERY_LIMIT).
- pub `list_by_type` function L35-37 — `(&mut self, object_type: &str) -> ServiceResult<Vec<Object>>` — List objects by type.
-  `Entity` type L41 — `= Object` — Service layer for accessing object catalog data.
-  `Filter` type L42 — `= ObjectFilter` — Service layer for accessing object catalog data.
-  `search` function L44-46 — `(&mut self, filter: &Self::Filter) -> ServiceResult<Vec<Self::Entity>>` — Service layer for accessing object catalog data.
-  `search_paginated` function L48-55 — `( &mut self, filter: &Self::Filter, limit: i64, offset: i64, ) -> ServiceResult<...` — Service layer for accessing object catalog data.
-  `get` function L57-59 — `(&mut self, id: i32) -> ServiceResult<Option<Self::Entity>>` — Service layer for accessing object catalog data.
-  `get_by_name_and_source` function L61-67 — `( &mut self, name: &str, source: &str, ) -> ServiceResult<Option<Self::Entity>>` — Service layer for accessing object catalog data.
-  `list_sources` function L69-71 — `(&mut self) -> ServiceResult<Vec<String>>` — Service layer for accessing object catalog data.
-  `count` function L73-75 — `(&mut self) -> ServiceResult<i64>` — Service layer for accessing object catalog data.
-  `count_by_source` function L77-79 — `(&mut self, source: &str) -> ServiceResult<i64>` — Service layer for accessing object catalog data.

#### crates/mimir-core/src/services/catalog/optional_feature.rs

- pub `OptionalFeatureService` struct L14-16 — `{ conn: &'a mut SqliteConnection }` — Service for accessing optional feature catalog data.
- pub `new` function L20-22 — `(conn: &'a mut SqliteConnection) -> Self` — Create a new optional feature service.
- pub `list_by_source` function L25-27 — `(&mut self, source: &str) -> ServiceResult<Vec<OptionalFeature>>` — List all optional features from a specific source.
- pub `list_all` function L30-32 — `(&mut self) -> ServiceResult<Vec<OptionalFeature>>` — List all optional features (up to DEFAULT_QUERY_LIMIT).
- pub `list_by_type` function L35-37 — `(&mut self, feature_type: &str) -> ServiceResult<Vec<OptionalFeature>>` — List optional features by type.
-  `Entity` type L41 — `= OptionalFeature` — Service layer for accessing optional feature catalog data.
-  `Filter` type L42 — `= OptionalFeatureFilter` — Service layer for accessing optional feature catalog data.
-  `search` function L44-46 — `(&mut self, filter: &Self::Filter) -> ServiceResult<Vec<Self::Entity>>` — Service layer for accessing optional feature catalog data.
-  `search_paginated` function L48-55 — `( &mut self, filter: &Self::Filter, limit: i64, offset: i64, ) -> ServiceResult<...` — Service layer for accessing optional feature catalog data.
-  `get` function L57-59 — `(&mut self, id: i32) -> ServiceResult<Option<Self::Entity>>` — Service layer for accessing optional feature catalog data.
-  `get_by_name_and_source` function L61-67 — `( &mut self, name: &str, source: &str, ) -> ServiceResult<Option<Self::Entity>>` — Service layer for accessing optional feature catalog data.
-  `list_sources` function L69-71 — `(&mut self) -> ServiceResult<Vec<String>>` — Service layer for accessing optional feature catalog data.
-  `count` function L73-75 — `(&mut self) -> ServiceResult<i64>` — Service layer for accessing optional feature catalog data.
-  `count_by_source` function L77-79 — `(&mut self, source: &str) -> ServiceResult<i64>` — Service layer for accessing optional feature catalog data.

#### crates/mimir-core/src/services/catalog/psionic.rs

- pub `PsionicService` struct L14-16 — `{ conn: &'a mut SqliteConnection }` — Service for accessing psionic catalog data.
- pub `new` function L20-22 — `(conn: &'a mut SqliteConnection) -> Self` — Create a new psionic service.
- pub `list_by_source` function L25-27 — `(&mut self, source: &str) -> ServiceResult<Vec<Psionic>>` — List all psionics from a specific source.
- pub `list_all` function L30-32 — `(&mut self) -> ServiceResult<Vec<Psionic>>` — List all psionics (up to DEFAULT_QUERY_LIMIT).
- pub `list_by_type` function L35-37 — `(&mut self, psionic_type: &str) -> ServiceResult<Vec<Psionic>>` — List psionics by type.
- pub `list_by_order` function L40-42 — `(&mut self, order: &str) -> ServiceResult<Vec<Psionic>>` — List psionics by order.
-  `Entity` type L46 — `= Psionic` — Service layer for accessing psionic catalog data.
-  `Filter` type L47 — `= PsionicFilter` — Service layer for accessing psionic catalog data.
-  `search` function L49-51 — `(&mut self, filter: &Self::Filter) -> ServiceResult<Vec<Self::Entity>>` — Service layer for accessing psionic catalog data.
-  `search_paginated` function L53-60 — `( &mut self, filter: &Self::Filter, limit: i64, offset: i64, ) -> ServiceResult<...` — Service layer for accessing psionic catalog data.
-  `get` function L62-64 — `(&mut self, id: i32) -> ServiceResult<Option<Self::Entity>>` — Service layer for accessing psionic catalog data.
-  `get_by_name_and_source` function L66-72 — `( &mut self, name: &str, source: &str, ) -> ServiceResult<Option<Self::Entity>>` — Service layer for accessing psionic catalog data.
-  `list_sources` function L74-76 — `(&mut self) -> ServiceResult<Vec<String>>` — Service layer for accessing psionic catalog data.
-  `count` function L78-80 — `(&mut self) -> ServiceResult<i64>` — Service layer for accessing psionic catalog data.
-  `count_by_source` function L82-84 — `(&mut self, source: &str) -> ServiceResult<i64>` — Service layer for accessing psionic catalog data.

#### crates/mimir-core/src/services/catalog/race.rs

- pub `RaceService` struct L14-16 — `{ conn: &'a mut SqliteConnection }` — Service for accessing race catalog data.
- pub `new` function L20-22 — `(conn: &'a mut SqliteConnection) -> Self` — Create a new race service.
- pub `list_by_source` function L25-27 — `(&mut self, source: &str) -> ServiceResult<Vec<Race>>` — List all races from a specific source.
- pub `list_all` function L30-32 — `(&mut self) -> ServiceResult<Vec<Race>>` — List all races (up to DEFAULT_QUERY_LIMIT).
-  `Entity` type L36 — `= Race` — Service layer for accessing race catalog data.
-  `Filter` type L37 — `= RaceFilter` — Service layer for accessing race catalog data.
-  `search` function L39-41 — `(&mut self, filter: &Self::Filter) -> ServiceResult<Vec<Self::Entity>>` — Service layer for accessing race catalog data.
-  `search_paginated` function L43-50 — `( &mut self, filter: &Self::Filter, limit: i64, offset: i64, ) -> ServiceResult<...` — Service layer for accessing race catalog data.
-  `get` function L52-54 — `(&mut self, id: i32) -> ServiceResult<Option<Self::Entity>>` — Service layer for accessing race catalog data.
-  `get_by_name_and_source` function L56-62 — `( &mut self, name: &str, source: &str, ) -> ServiceResult<Option<Self::Entity>>` — Service layer for accessing race catalog data.
-  `list_sources` function L64-66 — `(&mut self) -> ServiceResult<Vec<String>>` — Service layer for accessing race catalog data.
-  `count` function L68-70 — `(&mut self) -> ServiceResult<i64>` — Service layer for accessing race catalog data.
-  `count_by_source` function L72-74 — `(&mut self, source: &str) -> ServiceResult<i64>` — Service layer for accessing race catalog data.
-  `tests` module L78-150 — `-` — Service layer for accessing race catalog data.
-  `insert_test_races` function L84-93 — `(conn: &mut SqliteConnection)` — Service layer for accessing race catalog data.
-  `test_race_service_search` function L96-110 — `()` — Service layer for accessing race catalog data.
-  `test_race_service_get` function L113-124 — `()` — Service layer for accessing race catalog data.
-  `test_race_service_list_sources` function L127-137 — `()` — Service layer for accessing race catalog data.
-  `test_race_service_count` function L140-149 — `()` — Service layer for accessing race catalog data.

#### crates/mimir-core/src/services/catalog/reward.rs

- pub `RewardService` struct L14-16 — `{ conn: &'a mut SqliteConnection }` — Service for accessing reward catalog data.
- pub `new` function L20-22 — `(conn: &'a mut SqliteConnection) -> Self` — Create a new reward service.
- pub `list_by_source` function L25-27 — `(&mut self, source: &str) -> ServiceResult<Vec<Reward>>` — List all rewards from a specific source.
- pub `list_all` function L30-32 — `(&mut self) -> ServiceResult<Vec<Reward>>` — List all rewards (up to DEFAULT_QUERY_LIMIT).
- pub `list_by_type` function L35-37 — `(&mut self, reward_type: &str) -> ServiceResult<Vec<Reward>>` — List rewards by type.
-  `Entity` type L41 — `= Reward` — Service layer for accessing reward catalog data.
-  `Filter` type L42 — `= RewardFilter` — Service layer for accessing reward catalog data.
-  `search` function L44-46 — `(&mut self, filter: &Self::Filter) -> ServiceResult<Vec<Self::Entity>>` — Service layer for accessing reward catalog data.
-  `search_paginated` function L48-55 — `( &mut self, filter: &Self::Filter, limit: i64, offset: i64, ) -> ServiceResult<...` — Service layer for accessing reward catalog data.
-  `get` function L57-59 — `(&mut self, id: i32) -> ServiceResult<Option<Self::Entity>>` — Service layer for accessing reward catalog data.
-  `get_by_name_and_source` function L61-67 — `( &mut self, name: &str, source: &str, ) -> ServiceResult<Option<Self::Entity>>` — Service layer for accessing reward catalog data.
-  `list_sources` function L69-71 — `(&mut self) -> ServiceResult<Vec<String>>` — Service layer for accessing reward catalog data.
-  `count` function L73-75 — `(&mut self) -> ServiceResult<i64>` — Service layer for accessing reward catalog data.
-  `count_by_source` function L77-79 — `(&mut self, source: &str) -> ServiceResult<i64>` — Service layer for accessing reward catalog data.

#### crates/mimir-core/src/services/catalog/spell.rs

- pub `SpellService` struct L14-16 — `{ conn: &'a mut SqliteConnection }` — Service for accessing spell catalog data.
- pub `new` function L20-22 — `(conn: &'a mut SqliteConnection) -> Self` — Create a new spell service.
- pub `list_by_source` function L25-27 — `(&mut self, source: &str) -> ServiceResult<Vec<Spell>>` — List all spells from a specific source.
- pub `list_all` function L30-32 — `(&mut self) -> ServiceResult<Vec<Spell>>` — List all spells (up to DEFAULT_QUERY_LIMIT).
- pub `list_cantrips` function L35-37 — `(&mut self) -> ServiceResult<Vec<Spell>>` — List all cantrips.
- pub `list_rituals` function L40-42 — `(&mut self) -> ServiceResult<Vec<Spell>>` — List all ritual spells.
- pub `list_by_level` function L45-47 — `(&mut self, level: i32) -> ServiceResult<Vec<Spell>>` — List spells by level.
- pub `list_by_class` function L50-52 — `(&mut self, class_name: &str) -> ServiceResult<Vec<Spell>>` — List spells available to a specific class.
- pub `list_by_class_and_level` function L55-57 — `(&mut self, class_name: &str, level: i32) -> ServiceResult<Vec<Spell>>` — List spells available to a specific class at a specific level.
-  `Entity` type L61 — `= Spell` — Service layer for accessing spell catalog data.
-  `Filter` type L62 — `= SpellFilter` — Service layer for accessing spell catalog data.
-  `search` function L64-66 — `(&mut self, filter: &Self::Filter) -> ServiceResult<Vec<Self::Entity>>` — Service layer for accessing spell catalog data.
-  `search_paginated` function L68-75 — `( &mut self, filter: &Self::Filter, limit: i64, offset: i64, ) -> ServiceResult<...` — Service layer for accessing spell catalog data.
-  `get` function L77-79 — `(&mut self, id: i32) -> ServiceResult<Option<Self::Entity>>` — Service layer for accessing spell catalog data.
-  `get_by_name_and_source` function L81-87 — `( &mut self, name: &str, source: &str, ) -> ServiceResult<Option<Self::Entity>>` — Service layer for accessing spell catalog data.
-  `list_sources` function L89-91 — `(&mut self) -> ServiceResult<Vec<String>>` — Service layer for accessing spell catalog data.
-  `count` function L93-95 — `(&mut self) -> ServiceResult<i64>` — Service layer for accessing spell catalog data.
-  `count_by_source` function L97-99 — `(&mut self, source: &str) -> ServiceResult<i64>` — Service layer for accessing spell catalog data.
-  `tests` module L103-204 — `-` — Service layer for accessing spell catalog data.
-  `insert_test_spells` function L109-126 — `(conn: &mut SqliteConnection)` — Service layer for accessing spell catalog data.
-  `test_spell_service_search` function L129-148 — `()` — Service layer for accessing spell catalog data.
-  `test_spell_service_get` function L151-163 — `()` — Service layer for accessing spell catalog data.
-  `test_spell_service_list_sources` function L166-176 — `()` — Service layer for accessing spell catalog data.
-  `test_spell_service_cantrips_and_rituals` function L179-191 — `()` — Service layer for accessing spell catalog data.
-  `test_spell_service_count` function L194-203 — `()` — Service layer for accessing spell catalog data.

#### crates/mimir-core/src/services/catalog/subclass.rs

- pub `SubclassService` struct L12-14 — `{ conn: &'a mut SqliteConnection }` — Service for accessing subclass catalog data.
- pub `new` function L18-20 — `(conn: &'a mut SqliteConnection) -> Self` — Create a new subclass service.
- pub `get` function L23-25 — `(&mut self, id: i32) -> ServiceResult<Subclass>` — Get a subclass by ID.
- pub `get_by_name_and_class` function L28-35 — `( &mut self, name: &str, class_name: &str, source: &str, ) -> ServiceResult<Opti...` — Get a subclass by name, class, and source.
- pub `list_by_class` function L38-40 — `(&mut self, class_name: &str) -> ServiceResult<Vec<Subclass>>` — List all subclasses for a specific class.
- pub `list_by_source` function L43-45 — `(&mut self, source: &str) -> ServiceResult<Vec<Subclass>>` — List all subclasses from a specific source.
- pub `list_all` function L48-50 — `(&mut self) -> ServiceResult<Vec<Subclass>>` — List all subclasses.
- pub `count` function L53-55 — `(&mut self) -> ServiceResult<i64>` — Count all subclasses.

#### crates/mimir-core/src/services/catalog/subclass_feature.rs

- pub `SubclassFeatureService` struct L12-14 — `{ conn: &'a mut SqliteConnection }` — Service for accessing subclass feature catalog data.
- pub `new` function L18-20 — `(conn: &'a mut SqliteConnection) -> Self` — Create a new subclass feature service.
- pub `get` function L23-25 — `(&mut self, id: i32) -> ServiceResult<SubclassFeature>` — Get a subclass feature by ID.
- pub `get_by_name_and_subclass` function L28-36 — `( &mut self, name: &str, subclass_name: &str, subclass_source: &str, ) -> Servic...` — Get a subclass feature by name and subclass.
- pub `list_by_subclass` function L39-46 — `( &mut self, subclass_name: &str, subclass_source: &str, ) -> ServiceResult<Vec<...` — List all subclass features for a specific subclass.
- pub `search` function L49-51 — `(&mut self, filter: &SubclassFeatureFilter) -> ServiceResult<Vec<SubclassFeature...` — Search subclass features with filters.
- pub `list_sources` function L54-56 — `(&mut self) -> ServiceResult<Vec<String>>` — List all distinct sources.
- pub `count` function L59-61 — `(&mut self) -> ServiceResult<i64>` — Count all subclass features.
- pub `count_by_source` function L64-66 — `(&mut self, source: &str) -> ServiceResult<i64>` — Count subclass features by source.

#### crates/mimir-core/src/services/catalog/table.rs

- pub `CatalogTableService` struct L14-16 — `{ conn: &'a mut SqliteConnection }` — Service for accessing catalog table data.
- pub `new` function L20-22 — `(conn: &'a mut SqliteConnection) -> Self` — Create a new catalog table service.
- pub `list_by_source` function L25-27 — `(&mut self, source: &str) -> ServiceResult<Vec<CatalogTable>>` — List all catalog tables from a specific source.
- pub `list_all` function L30-32 — `(&mut self) -> ServiceResult<Vec<CatalogTable>>` — List all catalog tables (up to DEFAULT_QUERY_LIMIT).
-  `Entity` type L36 — `= CatalogTable` — Service layer for accessing catalog table data.
-  `Filter` type L37 — `= CatalogTableFilter` — Service layer for accessing catalog table data.
-  `search` function L39-41 — `(&mut self, filter: &Self::Filter) -> ServiceResult<Vec<Self::Entity>>` — Service layer for accessing catalog table data.
-  `search_paginated` function L43-50 — `( &mut self, filter: &Self::Filter, limit: i64, offset: i64, ) -> ServiceResult<...` — Service layer for accessing catalog table data.
-  `get` function L52-54 — `(&mut self, id: i32) -> ServiceResult<Option<Self::Entity>>` — Service layer for accessing catalog table data.
-  `get_by_name_and_source` function L56-62 — `( &mut self, name: &str, source: &str, ) -> ServiceResult<Option<Self::Entity>>` — Service layer for accessing catalog table data.
-  `list_sources` function L64-66 — `(&mut self) -> ServiceResult<Vec<String>>` — Service layer for accessing catalog table data.
-  `count` function L68-70 — `(&mut self) -> ServiceResult<i64>` — Service layer for accessing catalog table data.
-  `count_by_source` function L72-74 — `(&mut self, source: &str) -> ServiceResult<i64>` — Service layer for accessing catalog table data.

#### crates/mimir-core/src/services/catalog/trap.rs

- pub `TrapService` struct L14-16 — `{ conn: &'a mut SqliteConnection }` — Service for accessing trap catalog data.
- pub `new` function L20-22 — `(conn: &'a mut SqliteConnection) -> Self` — Create a new trap service.
- pub `list_by_source` function L25-27 — `(&mut self, source: &str) -> ServiceResult<Vec<Trap>>` — List all traps from a specific source.
- pub `list_all` function L30-32 — `(&mut self) -> ServiceResult<Vec<Trap>>` — List all traps (up to DEFAULT_QUERY_LIMIT).
- pub `list_simple` function L35-38 — `(&mut self) -> ServiceResult<Vec<Trap>>` — List simple traps.
- pub `list_complex` function L41-44 — `(&mut self) -> ServiceResult<Vec<Trap>>` — List complex traps.
-  `Entity` type L48 — `= Trap` — Service layer for accessing trap catalog data.
-  `Filter` type L49 — `= TrapFilter` — Service layer for accessing trap catalog data.
-  `search` function L51-53 — `(&mut self, filter: &Self::Filter) -> ServiceResult<Vec<Self::Entity>>` — Service layer for accessing trap catalog data.
-  `search_paginated` function L55-62 — `( &mut self, filter: &Self::Filter, limit: i64, offset: i64, ) -> ServiceResult<...` — Service layer for accessing trap catalog data.
-  `get` function L64-66 — `(&mut self, id: i32) -> ServiceResult<Option<Self::Entity>>` — Service layer for accessing trap catalog data.
-  `get_by_name_and_source` function L68-74 — `( &mut self, name: &str, source: &str, ) -> ServiceResult<Option<Self::Entity>>` — Service layer for accessing trap catalog data.
-  `list_sources` function L76-78 — `(&mut self) -> ServiceResult<Vec<String>>` — Service layer for accessing trap catalog data.
-  `count` function L80-82 — `(&mut self) -> ServiceResult<i64>` — Service layer for accessing trap catalog data.
-  `count_by_source` function L84-86 — `(&mut self, source: &str) -> ServiceResult<i64>` — Service layer for accessing trap catalog data.
-  `tests` module L90-154 — `-` — Service layer for accessing trap catalog data.
-  `insert_test_traps` function L96-104 — `(conn: &mut SqliteConnection)` — Service layer for accessing trap catalog data.
-  `test_trap_service_search` function L107-115 — `()` — Service layer for accessing trap catalog data.
-  `test_trap_service_get` function L118-129 — `()` — Service layer for accessing trap catalog data.
-  `test_trap_service_count` function L132-139 — `()` — Service layer for accessing trap catalog data.
-  `test_trap_service_by_tier` function L142-153 — `()` — Service layer for accessing trap catalog data.

#### crates/mimir-core/src/services/catalog/variant_rule.rs

- pub `VariantRuleService` struct L14-16 — `{ conn: &'a mut SqliteConnection }` — Service for accessing variant rule catalog data.
- pub `new` function L20-22 — `(conn: &'a mut SqliteConnection) -> Self` — Create a new variant rule service.
- pub `list_by_source` function L25-27 — `(&mut self, source: &str) -> ServiceResult<Vec<VariantRule>>` — List all variant rules from a specific source.
- pub `list_all` function L30-32 — `(&mut self) -> ServiceResult<Vec<VariantRule>>` — List all variant rules (up to DEFAULT_QUERY_LIMIT).
-  `Entity` type L36 — `= VariantRule` — Service layer for accessing variant rule catalog data.
-  `Filter` type L37 — `= VariantRuleFilter` — Service layer for accessing variant rule catalog data.
-  `search` function L39-41 — `(&mut self, filter: &Self::Filter) -> ServiceResult<Vec<Self::Entity>>` — Service layer for accessing variant rule catalog data.
-  `search_paginated` function L43-50 — `( &mut self, filter: &Self::Filter, limit: i64, offset: i64, ) -> ServiceResult<...` — Service layer for accessing variant rule catalog data.
-  `get` function L52-54 — `(&mut self, id: i32) -> ServiceResult<Option<Self::Entity>>` — Service layer for accessing variant rule catalog data.
-  `get_by_name_and_source` function L56-62 — `( &mut self, name: &str, source: &str, ) -> ServiceResult<Option<Self::Entity>>` — Service layer for accessing variant rule catalog data.
-  `list_sources` function L64-66 — `(&mut self) -> ServiceResult<Vec<String>>` — Service layer for accessing variant rule catalog data.
-  `count` function L68-70 — `(&mut self) -> ServiceResult<i64>` — Service layer for accessing variant rule catalog data.
-  `count_by_source` function L72-74 — `(&mut self, source: &str) -> ServiceResult<i64>` — Service layer for accessing variant rule catalog data.

#### crates/mimir-core/src/services/catalog/vehicle.rs

- pub `VehicleService` struct L14-16 — `{ conn: &'a mut SqliteConnection }` — Service for accessing vehicle catalog data.
- pub `new` function L20-22 — `(conn: &'a mut SqliteConnection) -> Self` — Create a new vehicle service.
- pub `list_by_source` function L25-27 — `(&mut self, source: &str) -> ServiceResult<Vec<Vehicle>>` — List all vehicles from a specific source.
- pub `list_all` function L30-32 — `(&mut self) -> ServiceResult<Vec<Vehicle>>` — List all vehicles (up to DEFAULT_QUERY_LIMIT).
- pub `list_by_type` function L35-37 — `(&mut self, vehicle_type: &str) -> ServiceResult<Vec<Vehicle>>` — List vehicles by type.
-  `Entity` type L41 — `= Vehicle` — Service layer for accessing vehicle catalog data.
-  `Filter` type L42 — `= VehicleFilter` — Service layer for accessing vehicle catalog data.
-  `search` function L44-46 — `(&mut self, filter: &Self::Filter) -> ServiceResult<Vec<Self::Entity>>` — Service layer for accessing vehicle catalog data.
-  `search_paginated` function L48-55 — `( &mut self, filter: &Self::Filter, limit: i64, offset: i64, ) -> ServiceResult<...` — Service layer for accessing vehicle catalog data.
-  `get` function L57-59 — `(&mut self, id: i32) -> ServiceResult<Option<Self::Entity>>` — Service layer for accessing vehicle catalog data.
-  `get_by_name_and_source` function L61-67 — `( &mut self, name: &str, source: &str, ) -> ServiceResult<Option<Self::Entity>>` — Service layer for accessing vehicle catalog data.
-  `list_sources` function L69-71 — `(&mut self) -> ServiceResult<Vec<String>>` — Service layer for accessing vehicle catalog data.
-  `count` function L73-75 — `(&mut self) -> ServiceResult<i64>` — Service layer for accessing vehicle catalog data.
-  `count_by_source` function L77-79 — `(&mut self, source: &str) -> ServiceResult<i64>` — Service layer for accessing vehicle catalog data.

### crates/mimir-core/src/templates

> *Semantic summary to be generated by AI agent.*

#### crates/mimir-core/src/templates/mod.rs

- pub `CAMPAIGN_PITCH` variable L12 — `: &str` — Campaign pitch template - the initial hook and premise.
- pub `STARTING_SCENARIO` variable L15 — `: &str` — Starting scenario template - where the adventure begins.
- pub `WORLD_PRIMER` variable L18 — `: &str` — World primer template - essential setting information for players.
- pub `CHARACTER_GUIDELINES` variable L21 — `: &str` — Character guidelines template - rules and options for character creation.
- pub `TABLE_EXPECTATIONS` variable L24 — `: &str` — Table expectations template - social contract and gameplay norms.
- pub `CHARACTER_INTEGRATION` variable L27 — `: &str` — Character integration template - weaving PCs into the story.
- pub `CAMPAIGN_BIBLE` variable L30 — `: &str` — Campaign bible template - the DM's reference for campaign lore.
- pub `SAFETY_TOOLS` variable L33 — `: &str` — Safety tools template - lines, veils, and safety mechanics.
- pub `HOUSE_RULES` variable L36 — `: &str` — House rules template - custom rules and rulings.
- pub `PLAYER_SECRETS` variable L39 — `: &str` — Player secrets template - information known only to specific players.
- pub `FACTION_OVERVIEW` variable L42 — `: &str` — Faction overview template - major factions and their relationships.
- pub `MODULE_OVERVIEW` variable L49 — `: &str` — General module overview template - works for any adventure type.
- pub `MODULE_MYSTERY` variable L52 — `: &str` — Mystery module template - investigation and clue-based adventures.
- pub `MODULE_DUNGEON` variable L55 — `: &str` — Dungeon crawl template - exploration and combat-focused adventures.
- pub `MODULE_HEIST` variable L58 — `: &str` — Heist module template - planning and execution of complex operations.
- pub `MODULE_HORROR` variable L61 — `: &str` — Horror module template - tension, dread, and survival.
- pub `MODULE_POLITICAL` variable L64 — `: &str` — Political intrigue template - factions, diplomacy, and scheming.
- pub `PLAY_NOTES` variable L67 — `: &str` — Play notes template - session-by-session DM notes.
- pub `CampaignTemplateInfo` struct L75-82 — `{ doc_type: &'static str, title: &'static str, content: &'static str }` — Campaign document type with its template and display title.
- pub `ModuleTemplateInfo` struct L86-93 — `{ module_type: &'static str, title: &'static str, content: &'static str }` — Module type with its template and display title.
- pub `CAMPAIGN_TEMPLATES` variable L96-152 — `: &[CampaignTemplateInfo]` — All campaign templates with metadata.
- pub `MODULE_TEMPLATES` variable L155-186 — `: &[ModuleTemplateInfo]` — All module templates with metadata.
- pub `get_campaign_template` function L195-210 — `(doc_type: &str) -> Option<&'static str>` — Get a campaign template by document type.
- pub `get_module_template` function L219-229 — `(module_type: &str) -> Option<&'static str>` — Get a module template by module type.
- pub `get_play_notes_template` function L234-236 — `() -> &'static str` — Get the play notes template.
- pub `list_campaign_templates` function L242-247 — `() -> Vec<(&'static str, &'static str)>` — List all campaign templates with their types and titles.
- pub `list_module_templates` function L253-258 — `() -> Vec<(&'static str, &'static str)>` — List all module templates with their types and titles.
-  `tests` module L261-345 — `-` — campaigns and modules are set up.
-  `test_campaign_templates_count` function L265-267 — `()` — campaigns and modules are set up.
-  `test_module_templates_count` function L270-272 — `()` — campaigns and modules are set up.
-  `test_get_campaign_template` function L275-279 — `()` — campaigns and modules are set up.
-  `test_get_module_template` function L282-287 — `()` — campaigns and modules are set up.
-  `test_play_notes_template` function L290-294 — `()` — campaigns and modules are set up.
-  `test_campaign_templates_not_empty` function L297-311 — `()` — campaigns and modules are set up.
-  `test_module_templates_not_empty` function L314-328 — `()` — campaigns and modules are set up.
-  `test_list_campaign_templates` function L331-336 — `()` — campaigns and modules are set up.
-  `test_list_module_templates` function L339-344 — `()` — campaigns and modules are set up.

### crates/mimir-core/tests

> *Semantic summary to be generated by AI agent.*

#### crates/mimir-core/tests/catalog_import.rs

- pub `MIGRATIONS` variable L12 — `: EmbeddedMigrations` — Embed all migrations at compile time.
-  `FIVETOOLS_DATA_PATH` variable L15 — `: &str` — Path to 5etools data directory.
-  `setup_test_db` function L18-27 — `() -> SqliteConnection` — Set up an in-memory database with all migrations applied.
-  `fivetools_data_exists` function L30-33 — `() -> bool` — Check if 5etools data is available for testing.
-  `test_import_phb_source` function L36-65 — `()` — These tests use the actual 5etools data from the data directory.
-  `test_import_spells_from_phb` function L68-109 — `()` — These tests use the actual 5etools data from the data directory.
-  `test_import_monsters_from_mm` function L112-155 — `()` — These tests use the actual 5etools data from the data directory.
-  `test_import_items_with_attunement` function L158-180 — `()` — These tests use the actual 5etools data from the data directory.
-  `test_import_classes_and_subclasses` function L183-212 — `()` — These tests use the actual 5etools data from the data directory.
-  `test_import_optional_features` function L215-245 — `()` — These tests use the actual 5etools data from the data directory.
-  `test_import_preserves_full_json_data` function L248-280 — `()` — These tests use the actual 5etools data from the data directory.
-  `test_spell_class_relationships` function L283-311 — `()` — These tests use the actual 5etools data from the data directory.
-  `test_import_summary_accuracy` function L314-369 — `()` — These tests use the actual 5etools data from the data directory.

#### crates/mimir-core/tests/srd_smoke_test.rs

-  `common` module L6 — `-` — Verifies that the test harness correctly loads all SRD fixture data
-  `test_srd_db_has_all_classes` function L11-27 — `()` — and that basic queries work against the seeded database.
-  `test_srd_db_has_subclasses` function L30-52 — `()` — and that basic queries work against the seeded database.
-  `test_srd_db_has_class_features` function L55-67 — `()` — and that basic queries work against the seeded database.
-  `test_srd_db_has_subclass_features_with_children` function L70-92 — `()` — and that basic queries work against the seeded database.
-  `test_srd_db_has_backgrounds` function L95-107 — `()` — and that basic queries work against the seeded database.
-  `test_srd_db_has_races` function L110-122 — `()` — and that basic queries work against the seeded database.
-  `test_srd_db_has_spells` function L125-142 — `()` — and that basic queries work against the seeded database.
-  `test_srd_db_has_items` function L145-152 — `()` — and that basic queries work against the seeded database.
-  `test_srd_db_has_monsters` function L155-169 — `()` — and that basic queries work against the seeded database.
-  `test_srd_db_data_blobs_are_valid_json` function L172-198 — `()` — and that basic queries work against the seeded database.
-  `test_srd_db_entity_to_json_roundtrip` function L201-212 — `()` — and that basic queries work against the seeded database.

### crates/mimir-core/tests/common

> *Semantic summary to be generated by AI agent.*

#### crates/mimir-core/tests/common/mod.rs

- pub `MIGRATIONS` variable L19 — `: EmbeddedMigrations` — SRD content is published under the OGL and safe to commit.
- pub `setup_srd_db` function L55-72 — `() -> SqliteConnection` — Set up an in-memory database seeded with SRD fixture data.
- pub `setup_srd_db_with_extra_sources` function L75-88 — `() -> SqliteConnection` — Set up the SRD database plus additional sources for broader testing.
-  `FIXTURES_DIR` variable L22 — `: &str` — Path to the fixtures directory relative to the crate root.
-  `create_db` function L25-31 — `() -> SqliteConnection` — Create an in-memory database with migrations applied.
-  `load_fixture` function L34-40 — `(filename: &str) -> Vec<Value>` — Load a JSON fixture file and parse it as a Vec<Value>.
-  `seed_sources` function L93-143 — `(conn: &mut SqliteConnection)` — Scan all fixture files for unique source codes and insert them.
-  `seed_classes` function L145-167 — `(conn: &mut SqliteConnection)` — SRD content is published under the OGL and safe to commit.
-  `seed_subclasses` function L169-193 — `(conn: &mut SqliteConnection)` — SRD content is published under the OGL and safe to commit.
-  `seed_class_features` function L195-221 — `(conn: &mut SqliteConnection)` — SRD content is published under the OGL and safe to commit.
-  `seed_subclass_features` function L223-255 — `(conn: &mut SqliteConnection)` — SRD content is published under the OGL and safe to commit.
-  `seed_backgrounds` function L257-278 — `(conn: &mut SqliteConnection)` — SRD content is published under the OGL and safe to commit.
-  `seed_races` function L280-301 — `(conn: &mut SqliteConnection)` — SRD content is published under the OGL and safe to commit.
-  `seed_items` function L303-330 — `(conn: &mut SqliteConnection)` — SRD content is published under the OGL and safe to commit.
-  `seed_spells` function L332-365 — `(conn: &mut SqliteConnection)` — SRD content is published under the OGL and safe to commit.
-  `seed_monsters` function L367-398 — `(conn: &mut SqliteConnection)` — SRD content is published under the OGL and safe to commit.

### crates/mimir-mapgen/src

> *Semantic summary to be generated by AI agent.*

#### crates/mimir-mapgen/src/biomes.rs

- pub `BiomePreset` struct L10-19 — `{ name: &'static str, description: &'static str, default_size: (u32, u32), confi...` — A biome preset with sensible defaults for all generation parameters.
- pub `get_preset` function L22-46 — `(name: &str) -> Option<BiomePreset>` — Get a biome preset by name.
- pub `list_presets` function L49-64 — `() -> Vec<BiomePreset>` — List all available presets.
-  `forest_preset` function L66-180 — `() -> BiomePreset` — for terrain textures, object palettes, and noise parameters.
-  `grassland_preset` function L182-258 — `() -> BiomePreset` — for terrain textures, object palettes, and noise parameters.
-  `cave_preset` function L260-325 — `() -> BiomePreset` — for terrain textures, object palettes, and noise parameters.
-  `desert_preset` function L327-414 — `() -> BiomePreset` — for terrain textures, object palettes, and noise parameters.
-  `lake_preset` function L416-522 — `() -> BiomePreset` — for terrain textures, object palettes, and noise parameters.
-  `ice_lake_preset` function L524-594 — `() -> BiomePreset` — for terrain textures, object palettes, and noise parameters.
-  `arctic_preset` function L596-673 — `() -> BiomePreset` — for terrain textures, object palettes, and noise parameters.
-  `island_tropical_preset` function L675-781 — `() -> BiomePreset` — for terrain textures, object palettes, and noise parameters.
-  `island_forest_preset` function L783-890 — `() -> BiomePreset` — for terrain textures, object palettes, and noise parameters.
-  `island_arctic_preset` function L892-962 — `() -> BiomePreset` — for terrain textures, object palettes, and noise parameters.
-  `swamp_preset` function L964-1074 — `() -> BiomePreset` — for terrain textures, object palettes, and noise parameters.
-  `forest_river_preset` function L1076-1178 — `() -> BiomePreset` — for terrain textures, object palettes, and noise parameters.
-  `tests` module L1181-1314 — `-` — for terrain textures, object palettes, and noise parameters.
-  `test_get_preset` function L1185-1206 — `()` — for terrain textures, object palettes, and noise parameters.
-  `test_list_presets` function L1209-1225 — `()` — for terrain textures, object palettes, and noise parameters.
-  `test_forest_has_terrain` function L1228-1232 — `()` — for terrain textures, object palettes, and noise parameters.
-  `test_cave_has_lighting` function L1235-1240 — `()` — for terrain textures, object palettes, and noise parameters.
-  `test_lake_has_water` function L1243-1247 — `()` — for terrain textures, object palettes, and noise parameters.
-  `test_ice_lake_has_water` function L1250-1254 — `()` — for terrain textures, object palettes, and noise parameters.
-  `test_desert_has_elevation` function L1257-1261 — `()` — for terrain textures, object palettes, and noise parameters.
-  `test_arctic_has_elevation` function L1264-1268 — `()` — for terrain textures, object palettes, and noise parameters.
-  `test_island_tropical_has_water_and_trees` function L1271-1276 — `()` — for terrain textures, object palettes, and noise parameters.
-  `test_island_forest_has_water_and_trees` function L1279-1284 — `()` — for terrain textures, object palettes, and noise parameters.
-  `test_island_arctic_has_water_no_trees` function L1287-1292 — `()` — for terrain textures, object palettes, and noise parameters.
-  `test_swamp_has_water_and_lighting` function L1295-1304 — `()` — for terrain textures, object palettes, and noise parameters.
-  `test_forest_river_has_river_and_trees` function L1307-1312 — `()` — for terrain textures, object palettes, and noise parameters.

#### crates/mimir-mapgen/src/contour.rs

- pub `Contour` type L8 — `= Vec<(f64, f64)>` — A contour polyline — a sequence of 2D points.
- pub `find_contours` function L14-50 — `(noise_map: &NoiseMap, threshold: f64) -> Vec<Contour>` — Extract contour lines from a noise map at the given threshold.
- pub `filter_contours` function L151-156 — `(contours: Vec<Contour>, min_points: usize) -> Vec<Contour>` — Filter contours by minimum length (number of points).
- pub `smooth_contours` function L159-164 — `(contours: Vec<Contour>, iterations: usize) -> Vec<Contour>` — Smooth contours by averaging each point with its neighbors.
-  `merge_contours` function L56-148 — `(mut fragments: Vec<Contour>) -> Vec<Contour>` — Merge contour fragments that share endpoints.
-  `smooth_polyline` function L166-185 — `(points: &[(f64, f64)], iterations: usize) -> Contour` — Extracts contour polylines from 2D scalar fields at configurable thresholds.
-  `cell_case` function L188-210 — `(noise_map: &NoiseMap, x: usize, y: usize, threshold: f64) -> u8` — Compute the Marching Squares case index (0-15) for a cell.
-  `interpolate` function L213-219 — `(v1: f64, v2: f64, threshold: f64) -> f64` — Interpolate position along an edge between two values crossing the threshold.
-  `cell_segments` function L223-263 — `( noise_map: &NoiseMap, x: usize, y: usize, threshold: f64, ) -> Vec<((f64, f64)...` — Get the edge crossing point(s) for a given marching squares case.
-  `trace_contour` function L266-319 — `( noise_map: &NoiseMap, visited: &mut [Vec<bool>], start_x: usize, start_y: usiz...` — Trace a contour starting from the given cell.
-  `next_cell` function L322-348 — `( cx: usize, cy: usize, exit: (f64, f64), w: usize, h: usize, ) -> Option<(usize...` — Determine the next cell to visit based on the exit point.
-  `tests` module L351-437 — `-` — Extracts contour polylines from 2D scalar fields at configurable thresholds.
-  `test_find_contours_basic` function L356-370 — `()` — Extracts contour polylines from 2D scalar fields at configurable thresholds.
-  `test_no_contours_at_extremes` function L373-382 — `()` — Extracts contour polylines from 2D scalar fields at configurable thresholds.
-  `test_filter_contours` function L385-393 — `()` — Extracts contour polylines from 2D scalar fields at configurable thresholds.
-  `test_smooth_contours` function L396-412 — `()` — Extracts contour polylines from 2D scalar fields at configurable thresholds.
-  `test_cell_case` function L415-436 — `()` — Extracts contour polylines from 2D scalar fields at configurable thresholds.

#### crates/mimir-mapgen/src/curves.rs

- pub `cubic_bezier` function L8-25 — `( p0: (f64, f64), p1: (f64, f64), p2: (f64, f64), p3: (f64, f64), t: f64, ) -> (...` — Evaluate a cubic Bezier curve at parameter `t` (0.0–1.0).
- pub `bezier_smooth` function L36-82 — `(points: &[(f64, f64)], density: usize) -> Vec<(f64, f64)>` — Smooth a polyline using cubic Bezier interpolation.
- pub `polyline_length` function L85-94 — `(points: &[(f64, f64)]) -> f64` — Compute the total length of a polyline.
- pub `offset_polyline` function L100-129 — `(points: &[(f64, f64)], distance: f64) -> Vec<(f64, f64)>` — Compute the perpendicular offset of a polyline.
-  `segment_normal` function L132-141 — `(a: (f64, f64), b: (f64, f64)) -> (f64, f64)` — Compute the unit normal (perpendicular) of a line segment.
-  `tests` module L144-204 — `-` — Cubic Bezier evaluation for smoothing polylines into natural curves.
-  `test_cubic_bezier_endpoints` function L148-161 — `()` — Cubic Bezier evaluation for smoothing polylines into natural curves.
-  `test_bezier_smooth_preserves_endpoints` function L164-171 — `()` — Cubic Bezier evaluation for smoothing polylines into natural curves.
-  `test_bezier_smooth_two_points` function L174-178 — `()` — Cubic Bezier evaluation for smoothing polylines into natural curves.
-  `test_polyline_length` function L181-185 — `()` — Cubic Bezier evaluation for smoothing polylines into natural curves.
-  `test_offset_polyline` function L188-195 — `()` — Cubic Bezier evaluation for smoothing polylines into natural curves.
-  `test_segment_normal` function L198-203 — `()` — Cubic Bezier evaluation for smoothing polylines into natural curves.

#### crates/mimir-mapgen/src/distribution.rs

- pub `Point` type L11 — `= (f64, f64)` — A point in 2D space.
- pub `PoissonDisc` struct L16 — `-` — Poisson Disc sampler using Bridson's algorithm.
- pub `sample` function L27-89 — `( width: f64, height: f64, min_distance: f64, rng: &mut impl Rng, k: u32, ) -> V...` — Generate uniformly distributed points with minimum spacing.
- pub `sample_gated` function L101-121 — `( noise_map: &NoiseMap, lower: f64, upper: f64, min_distance: f64, rng: &mut imp...` — Generate noise-gated points — only places points where the noise value
-  `PoissonDisc` type L18-122 — `= PoissonDisc` — Poisson Disc sampling (Bridson's algorithm) with noise-gated variants.
-  `is_valid` function L124-156 — `( grid: &[Vec<Option<usize>>], points: &[Point], x: f64, y: f64, gi: usize, gj: ...` — Poisson Disc sampling (Bridson's algorithm) with noise-gated variants.
-  `tests` module L159-245 — `-` — Poisson Disc sampling (Bridson's algorithm) with noise-gated variants.
-  `test_poisson_disc_basic` function L165-175 — `()` — Poisson Disc sampling (Bridson's algorithm) with noise-gated variants.
-  `test_poisson_disc_min_distance` function L178-197 — `()` — Poisson Disc sampling (Bridson's algorithm) with noise-gated variants.
-  `test_poisson_disc_deterministic` function L200-210 — `()` — Poisson Disc sampling (Bridson's algorithm) with noise-gated variants.
-  `test_poisson_disc_gated` function L213-237 — `()` — Poisson Disc sampling (Bridson's algorithm) with noise-gated variants.
-  `test_empty_area` function L240-244 — `()` — Poisson Disc sampling (Bridson's algorithm) with noise-gated variants.

#### crates/mimir-mapgen/src/elevation.rs

- pub `ContourLevel` struct L16-31 — `{ threshold: f64, texture: String, width: f64, layer: i32, min_points: usize, sm...` — Configuration for a single elevation contour level.
- pub `ShadowPathConfig` struct L35-44 — `{ texture: String, offset: f64, width: f64, layer: i32 }` — Configuration for shadow paths below cliffs.
- pub `ElevationConfig` struct L48-53 — `{ levels: Vec<ContourLevel>, pixels_per_cell: f64 }` — Configuration for elevation contour generation.
- pub `generate_elevation` function L97-144 — `( noise_map: &NoiseMap, config: &ElevationConfig, alloc: &NodeIdAllocator, ) -> ...` — Generate elevation contour paths from a noise map.
-  `ElevationConfig` type L55-91 — `impl Default for ElevationConfig` — Generates cliff and hill contour paths at configurable noise thresholds.
-  `default` function L56-90 — `() -> Self` — Generates cliff and hill contour paths at configurable noise thresholds.
-  `tests` module L147-265 — `-` — Generates cliff and hill contour paths at configurable noise thresholds.
-  `test_generate_elevation_basic` function L152-183 — `()` — Generates cliff and hill contour paths at configurable noise thresholds.
-  `test_generate_elevation_with_shadows` function L186-211 — `()` — Generates cliff and hill contour paths at configurable noise thresholds.
-  `test_generate_elevation_uniform` function L214-227 — `()` — Generates cliff and hill contour paths at configurable noise thresholds.
-  `test_contour_pixel_scaling` function L230-264 — `()` — Generates cliff and hill contour paths at configurable noise thresholds.

#### crates/mimir-mapgen/src/lib.rs

- pub `assets` module L10 — `-` — `mimir-mapgen` generates `.dungeondraft_map` files from YAML configuration,
- pub `biomes` module L11 — `-` — database layer.
- pub `contour` module L12 — `-` — database layer.
- pub `curves` module L13 — `-` — database layer.
- pub `distribution` module L14 — `-` — database layer.
- pub `elevation` module L15 — `-` — database layer.
- pub `format` module L16 — `-` — database layer.
- pub `noise_gen` module L17 — `-` — database layer.
- pub `objects` module L18 — `-` — database layer.
- pub `paths` module L19 — `-` — database layer.
- pub `pipeline` module L20 — `-` — database layer.
- pub `polygons` module L21 — `-` — database layer.
- pub `rooms` module L22 — `-` — database layer.
- pub `terrain` module L23 — `-` — database layer.
- pub `water` module L24 — `-` — database layer.

#### crates/mimir-mapgen/src/main.rs

-  `Cli` struct L12-15 — `{ command: Commands }` — Declarative Dungeondraft map generator
-  `Commands` enum L18-45 — `Generate | Validate | ListPresets`
-  `main` function L47-153 — `()`
-  `load_config` function L155-159 — `(path: &str) -> Result<MapConfig, Box<dyn std::error::Error>>`

#### crates/mimir-mapgen/src/noise_gen.rs

- pub `NoiseConfig` struct L11-22 — `{ seed: u32, octaves: usize, persistence: f64, lacunarity: f64, scale: f64 }` — Configuration for noise map generation.
- pub `NoiseMap` struct L38-42 — `{ width: usize, height: usize, data: Vec<Vec<f64>> }` — A 2D noise map with values normalized to 0.0–1.0.
- pub `generate` function L46-88 — `(width: usize, height: usize, config: &NoiseConfig) -> Self` — Generate a noise map with the given dimensions and config.
- pub `get` function L91-95 — `(&self, x: usize, y: usize) -> f64` — Get the value at (x, y), clamped to bounds.
- pub `sample` function L98-118 — `(&self, x: f64, y: f64) -> f64` — Get a bilinearly interpolated value at fractional coordinates.
- pub `apply_island_mode` function L122-124 — `(&mut self, falloff_strength: f64)` — Apply island mode: push values at edges toward 1.0 (high = water for terrain).
- pub `apply_lake_mode` function L128-130 — `(&mut self, falloff_strength: f64)` — Apply lake mode: push values at center toward 1.0 (high = water for terrain).
- pub `apply_canyon_mode` function L152-163 — `(&mut self, levels: &[f64])` — Apply canyon mode: force values below each threshold down to create sharp level boundaries.
- pub `to_byte_map` function L166-171 — `(&self) -> Vec<Vec<u8>>` — Convert to a 0-255 byte map for compatibility with reference impl conventions.
-  `NoiseConfig` type L24-34 — `impl Default for NoiseConfig` — Provides configurable multi-octave Perlin noise with island/canyon modifiers.
-  `default` function L25-33 — `() -> Self` — Provides configurable multi-octave Perlin noise with island/canyon modifiers.
-  `NoiseMap` type L44-172 — `= NoiseMap` — Provides configurable multi-octave Perlin noise with island/canyon modifiers.
-  `apply_radial_falloff` function L134-149 — `(&mut self, falloff_strength: f64, invert: bool)` — Shared radial falloff.
-  `tests` module L175-279 — `-` — Provides configurable multi-octave Perlin noise with island/canyon modifiers.
-  `test_noise_map_dimensions` function L179-186 — `()` — Provides configurable multi-octave Perlin noise with island/canyon modifiers.
-  `test_noise_map_normalized` function L189-205 — `()` — Provides configurable multi-octave Perlin noise with island/canyon modifiers.
-  `test_noise_map_deterministic` function L208-216 — `()` — Provides configurable multi-octave Perlin noise with island/canyon modifiers.
-  `test_noise_map_different_seeds` function L219-237 — `()` — Provides configurable multi-octave Perlin noise with island/canyon modifiers.
-  `test_bilinear_sample` function L240-246 — `()` — Provides configurable multi-octave Perlin noise with island/canyon modifiers.
-  `test_island_mode` function L249-264 — `()` — Provides configurable multi-octave Perlin noise with island/canyon modifiers.
-  `test_byte_map` function L267-278 — `()` — Provides configurable multi-octave Perlin noise with island/canyon modifiers.

#### crates/mimir-mapgen/src/objects.rs

- pub `ObjectConfig` struct L17-38 — `{ textures: Vec<String>, min_distance: f64, noise_lower: f64, noise_upper: f64, ...` — Configuration for a single object type to place.
- pub `TreeConfig` struct L60-67 — `{ tree: ObjectConfig, shadow: Option<ShadowConfig>, canopy: Option<CanopyConfig>...` — Configuration for tree placement with optional shadow and canopy layers.
- pub `ShadowConfig` struct L71-80 — `{ texture: String, offset: Vector2, layer: i32, scale_factor: f64 }` — Shadow configuration for trees.
- pub `CanopyConfig` struct L84-91 — `{ texture: String, layer: i32, scale_factor: f64 }` — Canopy configuration for trees (level 1 overhead foliage).
- pub `ClumpConfig` struct L95-104 — `{ primary: ObjectConfig, secondary: ObjectConfig, secondary_count: (u32, u32), s...` — Configuration for clump placement (primary + clustered secondaries).
- pub `place_objects` function L115-173 — `( noise_map: &NoiseMap, config: &ObjectConfig, pixel_width: f64, pixel_height: f...` — Place simple objects using noise-gated Poisson Disc sampling.
- pub `place_trees` function L176-222 — `( noise_map: &NoiseMap, config: &TreeConfig, pixel_width: f64, pixel_height: f64...` — Place trees with optional shadow and canopy objects.
- pub `place_clumps` function L225-287 — `( noise_map: &NoiseMap, config: &ClumpConfig, pixel_width: f64, pixel_height: f6...` — Place clumps: primary objects with clustered secondaries around each.
- pub `clear_corridor` function L295-304 — `(objects: &mut Vec<MapObject>, corridor: &[Point], half_width: f64)` — Remove objects that fall within a corridor (e.g., road clearing).
-  `ObjectConfig` type L40-56 — `impl Default for ObjectConfig` — Uses noise-gated Poisson Disc sampling for natural object distribution.
-  `default` function L41-55 — `() -> Self` — Uses noise-gated Poisson Disc sampling for natural object distribution.
-  `tests` module L307-502 — `-` — Uses noise-gated Poisson Disc sampling for natural object distribution.
-  `test_noise` function L313-315 — `() -> NoiseMap` — Uses noise-gated Poisson Disc sampling for natural object distribution.
-  `test_place_objects_basic` function L318-336 — `()` — Uses noise-gated Poisson Disc sampling for natural object distribution.
-  `test_place_objects_noise_gated` function L339-364 — `()` — Uses noise-gated Poisson Disc sampling for natural object distribution.
-  `test_place_objects_deterministic` function L367-388 — `()` — Uses noise-gated Poisson Disc sampling for natural object distribution.
-  `test_place_trees_with_shadow` function L391-419 — `()` — Uses noise-gated Poisson Disc sampling for natural object distribution.
-  `test_place_clumps` function L422-450 — `()` — Uses noise-gated Poisson Disc sampling for natural object distribution.
-  `test_clear_corridor` function L453-465 — `()` — Uses noise-gated Poisson Disc sampling for natural object distribution.
-  `test_custom_color` function L468-485 — `()` — Uses noise-gated Poisson Disc sampling for natural object distribution.
-  `test_unique_node_refs` function L488-501 — `()` — Uses noise-gated Poisson Disc sampling for natural object distribution.

#### crates/mimir-mapgen/src/paths.rs

- pub `Edge` enum L18-23 — `Left | Right | Top | Bottom` — Which map edge a road/river starts or ends at.
- pub `PathStyle` enum L28-33 — `Straight | Meandering` — Path style: how the centerline is generated.
- pub `RoadConfig` struct L44-69 — `{ from: Edge, to: Edge, texture: String, width: f64, layer: i32, style: PathStyl...` — Configuration for road generation.
- pub `EdgePathConfig` struct L73-82 — `{ texture: String, offset: f64, width: f64, layer: i32 }` — Configuration for edge/border paths along roads.
- pub `RiverConfig` struct L87-116 — `{ from: Edge, to: Edge, width: f64, style: PathStyle, step_distance: f64, fov: f...` — Configuration for river generation.
- pub `RoadResult` struct L159-168 — `{ road: MapPath, edge_paths: Vec<MapPath>, corridor_points: Vec<(f64, f64)>, cor...` — Result of road generation.
- pub `RiverResult` struct L171-180 — `{ bank_paths: Vec<MapPath>, corridor_points: Vec<(f64, f64)>, corridor_half_widt...` — Result of river generation.
- pub `generate_road` function L183-192 — `( noise_map: &NoiseMap, config: &RoadConfig, pixel_width: f64, pixel_height: f64...` — Generate a road across the map using greedy pathfinding.
- pub `generate_road_with_exclusions` function L195-275 — `( noise_map: &NoiseMap, config: &RoadConfig, pixel_width: f64, pixel_height: f64...` — Generate a road, avoiding exclusion zones (rooms).
- pub `generate_river` function L278-287 — `( noise_map: &NoiseMap, config: &RiverConfig, pixel_width: f64, pixel_height: f6...` — Generate a river across the map.
- pub `generate_river_with_exclusions` function L290-393 — `( noise_map: &NoiseMap, config: &RiverConfig, pixel_width: f64, pixel_height: f6...` — Generate a river, avoiding exclusion zones (rooms).
-  `PathStyle` type L35-39 — `impl Default for PathStyle` — Greedy pathfinding along noise ridges/valleys with Bezier smoothing.
-  `default` function L36-38 — `() -> Self` — Greedy pathfinding along noise ridges/valleys with Bezier smoothing.
-  `RoadConfig` type L118-135 — `impl Default for RoadConfig` — Greedy pathfinding along noise ridges/valleys with Bezier smoothing.
-  `default` function L119-134 — `() -> Self` — Greedy pathfinding along noise ridges/valleys with Bezier smoothing.
-  `RiverConfig` type L137-156 — `impl Default for RiverConfig` — Greedy pathfinding along noise ridges/valleys with Bezier smoothing.
-  `default` function L138-155 — `() -> Self` — Greedy pathfinding along noise ridges/valleys with Bezier smoothing.
-  `generate_meander` function L401-524 — `( start: (f64, f64), target: (f64, f64), pixel_width: f64, pixel_height: f64, no...` — Generate a meandering centerline using sinusoidal displacement + noise.
-  `clip_polyline_to_rect` function L530-574 — `( points: &[(f64, f64)], x_min: f64, y_min: f64, x_max: f64, y_max: f64, ) -> Ve...` — Clip a polyline to a rectangle, keeping only the interior portion.
-  `inside` function L535-537 — `(p: (f64, f64), x_min: f64, y_min: f64, x_max: f64, y_max: f64) -> bool` — Greedy pathfinding along noise ridges/valleys with Bezier smoothing.
-  `clip_polygon_to_rect` function L578-604 — `( polygon: &[(f64, f64)], x_min: f64, y_min: f64, x_max: f64, y_max: f64, ) -> V...` — Clip a closed polygon to a rectangle using Sutherland-Hodgman.
-  `sh_clip_edge` function L607-635 — `( polygon: &[(f64, f64)], inside: impl Fn((f64, f64)) -> bool, intersect: impl F...` — One pass of Sutherland-Hodgman: clip polygon against a single edge.
-  `clip_segment` function L638-669 — `( a: (f64, f64), b: (f64, f64), x_min: f64, y_min: f64, x_max: f64, y_max: f64, ...` — Liang-Barsky segment clip: returns clipped (entry, exit) points, or None.
-  `greedy_walk` function L676-761 — `( noise_map: &NoiseMap, start: (f64, f64), target: (f64, f64), pixel_width: f64,...` — Greedy pathfinding walk from start toward target.
-  `random_edge_point` function L764-777 — `( edge: Edge, width: f64, height: f64, margin: f64, rng: &mut impl Rng, ) -> (f6...` — Pick a random point along a map edge.
-  `tests` module L780-975 — `-` — Greedy pathfinding along noise ridges/valleys with Bezier smoothing.
-  `test_noise` function L786-795 — `() -> NoiseMap` — Greedy pathfinding along noise ridges/valleys with Bezier smoothing.
-  `test_greedy_walk_reaches_target` function L798-824 — `()` — Greedy pathfinding along noise ridges/valleys with Bezier smoothing.
-  `test_generate_road` function L827-846 — `()` — Greedy pathfinding along noise ridges/valleys with Bezier smoothing.
-  `test_generate_road_with_edges` function L849-870 — `()` — Greedy pathfinding along noise ridges/valleys with Bezier smoothing.
-  `test_generate_river` function L873-888 — `()` — Greedy pathfinding along noise ridges/valleys with Bezier smoothing.
-  `test_road_deterministic` function L891-908 — `()` — Greedy pathfinding along noise ridges/valleys with Bezier smoothing.
-  `test_road_meandering_style` function L911-927 — `()` — Greedy pathfinding along noise ridges/valleys with Bezier smoothing.
-  `test_river_straight_style` function L930-945 — `()` — Greedy pathfinding along noise ridges/valleys with Bezier smoothing.
-  `test_path_style_serde` function L948-956 — `()` — Greedy pathfinding along noise ridges/valleys with Bezier smoothing.
-  `test_random_edge_point` function L959-974 — `()` — Greedy pathfinding along noise ridges/valleys with Bezier smoothing.

#### crates/mimir-mapgen/src/pipeline.rs

- pub `MapConfig` struct L24-75 — `{ name: String, width: u32, height: u32, seed: Option<u64>, noise: NoiseConfig, ...` — Top-level map generation configuration.
- pub `LightingConfig` struct L79-88 — `{ ambient_light: String, ambient_energy: Option<f64>, shadow_color: Option<Strin...` — Lighting/environment configuration.
- pub `lighting_from_time_of_day` function L91-120 — `(time: &str) -> Option<LightingConfig>` — Get a LightingConfig from a time-of-day preset name.
- pub `ValidationError` struct L124-127 — `{ field: String, message: String }` — Validation errors for a MapConfig.
- pub `validate_config` function L130-181 — `(config: &MapConfig) -> Vec<ValidationError>` — Validate a MapConfig, returning a list of errors.
- pub `GenerateResult` struct L324-329 — `{ map: DungeondraftMap, stats: GenerateStats }` — Result of map generation.
- pub `GenerateStats` struct L333-340 — `{ objects_placed: usize, paths_generated: usize, water_polygons: usize, contour_...` — Statistics from map generation.
- pub `generate` function L357-629 — `(config: &MapConfig, seed_override: Option<u64>) -> GenerateResult` — Generate a complete `.dungeondraft_map` from a config.
-  `validate_rooms` function L183-280 — `(config: &MapConfig, errors: &mut Vec<ValidationError>)` — Config parsing, biome preset resolution, and staged map generation.
-  `validate_corridors` function L282-321 — `(config: &MapConfig, errors: &mut Vec<ValidationError>)` — Config parsing, biome preset resolution, and staged map generation.
-  `tests` module L632-1112 — `-` — Config parsing, biome preset resolution, and staged map generation.
-  `minimal_config` function L635-656 — `() -> MapConfig` — Config parsing, biome preset resolution, and staged map generation.
-  `test_generate_minimal` function L659-665 — `()` — Config parsing, biome preset resolution, and staged map generation.
-  `test_generate_with_terrain` function L668-682 — `()` — Config parsing, biome preset resolution, and staged map generation.
-  `test_generate_with_roads` function L685-693 — `()` — Config parsing, biome preset resolution, and staged map generation.
-  `test_generate_with_objects` function L696-706 — `()` — Config parsing, biome preset resolution, and staged map generation.
-  `test_generate_deterministic` function L709-727 — `()` — Config parsing, biome preset resolution, and staged map generation.
-  `test_seed_override` function L730-736 — `()` — Config parsing, biome preset resolution, and staged map generation.
-  `test_generate_with_lighting` function L739-752 — `()` — Config parsing, biome preset resolution, and staged map generation.
-  `test_lighting_presets` function L755-762 — `()` — Config parsing, biome preset resolution, and staged map generation.
-  `test_validate_config` function L765-775 — `()` — Config parsing, biome preset resolution, and staged map generation.
-  `test_full_pipeline` function L778-808 — `()` — Config parsing, biome preset resolution, and staged map generation.
-  `test_generate_with_rooms` function L811-900 — `()` — Config parsing, biome preset resolution, and staged map generation.
-  `test_generate_outdoor_only_no_regression` function L903-923 — `()` — Config parsing, biome preset resolution, and staged map generation.
-  `test_validate_room_out_of_bounds` function L926-938 — `()` — Config parsing, biome preset resolution, and staged map generation.
-  `test_validate_room_overlap` function L941-962 — `()` — Config parsing, biome preset resolution, and staged map generation.
-  `test_validate_duplicate_room_id` function L965-986 — `()` — Config parsing, biome preset resolution, and staged map generation.
-  `test_validate_portal_out_of_wall` function L989-1006 — `()` — Config parsing, biome preset resolution, and staged map generation.
-  `test_validate_corridor_invalid_room_ref` function L1009-1024 — `()` — Config parsing, biome preset resolution, and staged map generation.
-  `test_validate_terrain_slot_out_of_range` function L1027-1039 — `()` — Config parsing, biome preset resolution, and staged map generation.
-  `test_river_creates_water_without_water_config` function L1042-1075 — `()` — Config parsing, biome preset resolution, and staged map generation.
-  `test_river_water_not_overwritten_by_water_step` function L1078-1096 — `()` — Config parsing, biome preset resolution, and staged map generation.
-  `test_validate_room_zero_dimensions` function L1099-1111 — `()` — Config parsing, biome preset resolution, and staged map generation.

#### crates/mimir-mapgen/src/polygons.rs

- pub `PolygonConfig` struct L17-32 — `{ id: String, points: Vec<[f64; 2]>, terrain_slot: Option<usize>, wall_texture: ...` — A closed polygon area defined in grid coordinates.
- pub `PolygonPortalConfig` struct L40-49 — `{ edge: usize, position: f64, portal_type: PortalType }` — A portal on a polygon edge.
- pub `PolygonTerrainOverride` struct L57-62 — `{ points: Vec<[f64; 2]>, slot: usize }` — A terrain override that respects polygon boundaries (not just bounding box).
- pub `apply` function L67-94 — `(&self, splat_data: &mut [u8], map_cells_x: usize)` — Apply this override to a terrain splat map, only filling cells
- pub `PolygonLayoutResult` struct L99-105 — `{ walls: Vec<MapWall>, portals: Vec<MapPortal>, shape_wall_ids: Vec<serde_json::...` — Result of polygon layout generation.
- pub `generate_polygon_layout` function L524-594 — `( polygons: &[PolygonConfig], alloc: &NodeIdAllocator, ) -> PolygonLayoutResult` — Generate the polygon-based layout.
- pub `build_polygon_exclusion_zones` function L747-784 — `( polygons: &[PolygonConfig], ) -> Vec<crate::rooms::ExclusionZone>` — Build exclusion zones from polygon configs.
-  `default_wall_texture` function L34-36 — `() -> String` — Each polygon also registers a DD shape for interior fill.
-  `default_position` function L51-53 — `() -> f64` — Each polygon also registers a DD shape for interior fill.
-  `PolygonTerrainOverride` type L64-95 — `= PolygonTerrainOverride` — Each polygon also registers a DD shape for interior fill.
-  `Segment` struct L111-114 — `{ start: [f64; 2], end: [f64; 2] }` — A directed line segment with an integer key for its source polygon.
-  `EPS` variable L117 — `: f64` — Tolerance for treating two coordinates as equal.
-  `pts_eq` function L119-121 — `(a: &[f64; 2], b: &[f64; 2]) -> bool` — Each polygon also registers a DD shape for interior fill.
-  `collect_edges` function L126-140 — `(polygons: &[PolygonConfig]) -> Vec<Segment>` — Extract directed edges from all polygons (in pixel coordinates).
-  `subtract_overlap` function L147-207 — `(a: &Segment, b: &Segment) -> Option<Vec<Segment>>` — Two segments overlap if they are collinear and share any interval.
-  `remove_shared_edges` function L211-245 — `(edges: Vec<Segment>) -> Vec<Segment>` — Remove all shared (overlapping) portions between edges.
-  `segment_crossing` function L251-276 — `(a: &Segment, b: &Segment) -> Option<([f64; 2], f64, f64)>` — Find the crossing point of two non-collinear line segments.
-  `split_at_crossings` function L279-325 — `(edges: Vec<Segment>) -> Vec<Segment>` — Split all edges at crossing intersection points.
-  `turn_angle` function L331-335 — `(d_fwd: &[f64; 2], d_out: &[f64; 2]) -> f64` — Signed turn angle from direction d_fwd to d_out.
-  `walk_cw_loops` function L343-441 — `(segments: Vec<Segment>) -> Vec<Vec<[f64; 2]>>` — Walk the directed segment graph clockwise, using the leftmost-turn rule
-  `filter_interior_loops` function L444-474 — `(loops: Vec<Vec<[f64; 2]>>) -> Vec<Vec<[f64; 2]>>` — Remove loops whose centroid lies inside a larger loop.
-  `polygon_area` function L477-485 — `(pts: &[[f64; 2]]) -> f64` — Absolute area of a polygon (shoelace formula).
-  `polygon_centroid` function L488-493 — `(pts: &[[f64; 2]]) -> [f64; 2]` — Centroid of a polygon (average of vertices).
-  `point_in_polygon` function L496-513 — `(point: &[f64; 2], polygon: &[[f64; 2]]) -> bool` — Point-in-polygon test using ray casting.
-  `point_on_segment` function L600-626 — `(point: &[f64; 2], seg_start: &[f64; 2], seg_end: &[f64; 2]) -> Option<f64>` — Check if a point lies on a line segment.
-  `edge_orientation` function L634-644 — `(edge_start: &[f64; 2], edge_end: &[f64; 2]) -> (Vector2, f64)` — Compute portal rotation and direction for an edge.
-  `generate_portals` function L653-744 — `( polygons: &[PolygonConfig], loops: &[Vec<[f64; 2]>], wall_ids: &[String], allo...` — Generate portals for all polygons after wall loop computation.
-  `tests` module L789-1284 — `-` — Each polygon also registers a DD shape for interior fill.
-  `single_square_no_removal` function L795-805 — `()` — Each polygon also registers a DD shape for interior fill.
-  `two_adjacent_squares_shared_edge_removed` function L808-827 — `()` — Each polygon also registers a DD shape for interior fill.
-  `partial_overlap_splits_edge` function L830-840 — `()` — Each polygon also registers a DD shape for interior fill.
-  `three_squares_in_a_row` function L843-864 — `()` — Each polygon also registers a DD shape for interior fill.
-  `chain_simple_square` function L869-879 — `()` — Each polygon also registers a DD shape for interior fill.
-  `chain_two_adjacent_squares_forms_one_loop` function L882-901 — `()` — Each polygon also registers a DD shape for interior fill.
-  `chain_l_shape` function L904-924 — `()` — Each polygon also registers a DD shape for interior fill.
-  `single_polygon_one_closed_wall` function L929-946 — `()` — Each polygon also registers a DD shape for interior fill.
-  `two_adjacent_polygons_one_closed_wall` function L949-975 — `()` — Each polygon also registers a DD shape for interior fill.
-  `exclusion_zones` function L978-995 — `()` — Each polygon also registers a DD shape for interior fill.
-  `portal_on_perimeter_wall_anchored` function L1000-1028 — `()` — Each polygon also registers a DD shape for interior fill.
-  `portal_on_shared_edge_freestanding` function L1031-1065 — `()` — Each polygon also registers a DD shape for interior fill.
-  `portal_position_pixels` function L1068-1089 — `()` — Each polygon also registers a DD shape for interior fill.
-  `multiple_portals_on_different_edges` function L1092-1117 — `()` — Each polygon also registers a DD shape for interior fill.
-  `three_rooms_corridor_portal_freestanding` function L1120-1157 — `()` — Each polygon also registers a DD shape for interior fill.
-  `overlapping_ovals_merge_to_one_wall` function L1162-1200 — `()` — Each polygon also registers a DD shape for interior fill.
-  `overlapping_ovals_with_portals` function L1203-1243 — `()` — Each polygon also registers a DD shape for interior fill.
-  `overlapping_ovals_interior_portal_freestanding` function L1246-1283 — `()` — Each polygon also registers a DD shape for interior fill.

#### crates/mimir-mapgen/src/rooms.rs

- pub `WallSide` enum L17-22 — `North | South | East | West` — Which side of a room a wall, portal, or corridor attaches to.
- pub `PortalType` enum L27-32 — `Door | Window | Archway | SecretDoor` — Semantic portal type — maps to Dungeondraft portal textures.
- pub `default_texture` function L36-43 — `(&self) -> &'static str` — Return the default Dungeondraft texture path for this portal type.
- pub `default_radius` function L46-53 — `(&self) -> f64` — Return the default radius (half-width in pixels) for this portal type.
- pub `PortalConfig` struct L58-69 — `{ wall: WallSide, position: u32, portal_type: PortalType, width: u32 }` — A portal (door/window/archway) declaration on a room wall.
- pub `WallToggles` struct L77-86 — `{ north: bool, south: bool, east: bool, west: bool }` — Per-wall toggle — which sides of the room have walls.
- pub `is_enabled` function L105-112 — `(&self, side: WallSide) -> bool` — Check if a specific wall side is enabled.
- pub `RoomConfig` struct L117-137 — `{ id: String, x: u32, y: u32, width: u32, height: u32, terrain_slot: Option<usiz...` — A room declaration — a rectangular area on the grid.
- pub `CorridorConfig` struct L141-159 — `{ from: String, from_wall: WallSide, to: String, to_wall: WallSide, width: u32, ...` — A corridor connecting two rooms.
- pub `CorridorPortalConfig` struct L167-176 — `{ end: CorridorEnd, portal_type: PortalType, width: u32 }` — A portal declaration at a corridor end.
- pub `CorridorEnd` enum L181-184 — `From | To` — Which end of a corridor a portal sits at.
- pub `PIXELS_PER_GRID` variable L187 — `: f64` — Pixels per grid square in Dungeondraft maps.
- pub `ExclusionZone` struct L191-196 — `{ x: f64, y: f64, width: f64, height: f64 }` — An axis-aligned rectangular exclusion zone in pixel coordinates.
- pub `contains` function L200-202 — `(&self, px: f64, py: f64) -> bool` — Check if a point (in pixel coordinates) falls inside this zone.
- pub `build_exclusion_zones` function L206-271 — `(rooms: &[RoomConfig], corridors: &[CorridorConfig]) -> Vec<ExclusionZone>` — Build exclusion zones from room configs (rooms + corridors).
- pub `is_excluded` function L274-276 — `(zones: &[ExclusionZone], px: f64, py: f64) -> bool` — Check if a point falls inside any exclusion zone.
- pub `RoomLayoutResult` struct L280-291 — `{ walls: Vec<MapWall>, portals: Vec<MapPortal>, shape_wall_ids: Vec<serde_json::...` — Result of laying out rooms: walls, portals, shapes, and terrain overrides.
- pub `TerrainOverride` struct L295-306 — `{ cell_x: usize, cell_y: usize, cell_width: usize, cell_height: usize, slot: usi...` — A rectangular region to override in the terrain splat map.
- pub `apply` function L313-327 — `(&self, splat_data: &mut [u8], map_cells_x: usize)` — Apply this override to a terrain splat map.
- pub `generate_room_layout` function L335-357 — `( rooms: &[RoomConfig], corridors: &[CorridorConfig], alloc: &NodeIdAllocator, )...` — Generate the full room and corridor layout.
-  `PortalType` type L34-54 — `= PortalType` — All coordinates are in grid squares — converted to pixels internally (× 256).
-  `default_portal_width` function L71-73 — `() -> u32` — All coordinates are in grid squares — converted to pixels internally (× 256).
-  `default_true` function L88-90 — `() -> bool` — All coordinates are in grid squares — converted to pixels internally (× 256).
-  `WallToggles` type L92-101 — `impl Default for WallToggles` — All coordinates are in grid squares — converted to pixels internally (× 256).
-  `default` function L93-100 — `() -> Self` — All coordinates are in grid squares — converted to pixels internally (× 256).
-  `WallToggles` type L103-113 — `= WallToggles` — All coordinates are in grid squares — converted to pixels internally (× 256).
-  `default_corridor_width` function L161-163 — `() -> u32` — All coordinates are in grid squares — converted to pixels internally (× 256).
-  `ExclusionZone` type L198-203 — `= ExclusionZone` — All coordinates are in grid squares — converted to pixels internally (× 256).
-  `TerrainOverride` type L308-328 — `= TerrainOverride` — All coordinates are in grid squares — converted to pixels internally (× 256).
-  `generate_single_room` function L360-445 — `( room: &RoomConfig, alloc: &NodeIdAllocator, result: &mut RoomLayoutResult, )` — Generate walls, portals, shapes, and terrain override for a single room.
-  `make_portal` function L448-511 — `( room: &RoomConfig, portal: &PortalConfig, wall_id: &str, _corners: &[Vector2; ...` — Create a MapPortal from a room's PortalConfig.
-  `find_room` function L514-516 — `(rooms: &'a [RoomConfig], id: &str) -> Option<&'a RoomConfig>` — Find a room by ID in the rooms list.
-  `wall_connection_point` function L522-534 — `(room: &RoomConfig, side: WallSide, _corridor_width: u32) -> (f64, f64)` — Get the connection point on a room's wall for a corridor.
-  `generate_corridor` function L537-596 — `( corridor: &CorridorConfig, rooms: &[RoomConfig], alloc: &NodeIdAllocator, resu...` — Generate corridor walls and terrain between two rooms.
-  `generate_straight_corridor_walls` function L599-718 — `( x1: f64, y1: f64, // top-left x2: f64, y2: f64, // bottom-right horizontal: bo...` — Generate walls for a straight corridor (axis-aligned rectangle).
-  `generate_l_corridor` function L723-824 — `( from_x: f64, from_y: f64, to_x: f64, to_y: f64, half_width: f64, corridor: &Co...` — Generate an L-shaped corridor between two non-aligned points.
-  `tests` module L827-1332 — `-` — All coordinates are in grid squares — converted to pixels internally (× 256).
-  `test_wall_toggles_default` function L831-837 — `()` — All coordinates are in grid squares — converted to pixels internally (× 256).
-  `test_wall_side_serde` function L840-845 — `()` — All coordinates are in grid squares — converted to pixels internally (× 256).
-  `test_portal_type_textures` function L848-853 — `()` — All coordinates are in grid squares — converted to pixels internally (× 256).
-  `test_portal_type_serde` function L856-861 — `()` — All coordinates are in grid squares — converted to pixels internally (× 256).
-  `test_room_config_yaml` function L864-900 — `()` — All coordinates are in grid squares — converted to pixels internally (× 256).
-  `test_room_config_minimal_yaml` function L903-919 — `()` — All coordinates are in grid squares — converted to pixels internally (× 256).
-  `test_corridor_config_yaml` function L922-945 — `()` — All coordinates are in grid squares — converted to pixels internally (× 256).
-  `test_corridor_config_minimal_yaml` function L948-959 — `()` — All coordinates are in grid squares — converted to pixels internally (× 256).
-  `test_wall_toggles_is_enabled` function L962-973 — `()` — All coordinates are in grid squares — converted to pixels internally (× 256).
-  `test_generate_single_room_4_walls` function L980-1017 — `()` — All coordinates are in grid squares — converted to pixels internally (× 256).
-  `test_generate_room_with_portal` function L1020-1062 — `()` — All coordinates are in grid squares — converted to pixels internally (× 256).
-  `test_generate_room_with_terrain_override` function L1065-1087 — `()` — All coordinates are in grid squares — converted to pixels internally (× 256).
-  `test_terrain_override_apply` function L1090-1115 — `()` — All coordinates are in grid squares — converted to pixels internally (× 256).
-  `test_generate_multiple_rooms` function L1118-1148 — `()` — All coordinates are in grid squares — converted to pixels internally (× 256).
-  `test_east_wall_portal` function L1151-1184 — `()` — All coordinates are in grid squares — converted to pixels internally (× 256).
-  `two_rooms_horizontal` function L1188-1205 — `() -> Vec<RoomConfig>` — All coordinates are in grid squares — converted to pixels internally (× 256).
-  `test_straight_horizontal_corridor` function L1208-1231 — `()` — All coordinates are in grid squares — converted to pixels internally (× 256).
-  `test_straight_vertical_corridor` function L1234-1267 — `()` — All coordinates are in grid squares — converted to pixels internally (× 256).
-  `test_l_shaped_corridor` function L1270-1304 — `()` — All coordinates are in grid squares — converted to pixels internally (× 256).
-  `test_corridor_with_door` function L1307-1331 — `()` — All coordinates are in grid squares — converted to pixels internally (× 256).

#### crates/mimir-mapgen/src/terrain.rs

- pub `TerrainSlot` struct L14-21 — `{ texture: String, lower: f64, upper: f64 }` — Configuration for a single terrain texture slot.
- pub `TerrainConfig` struct L25-32 — `{ slots: [TerrainSlot; 4], blend_width: f64, smooth_blending: bool }` — Configuration for terrain generation.
- pub `generate_terrain` function L75-111 — `( noise_map: &NoiseMap, width: u32, height: u32, config: &TerrainConfig, ) -> Te...` — Generate terrain from a noise map.
- pub `apply_road_corridor` function L124-155 — `( terrain: &mut Terrain, width: u32, corridor: &[(f64, f64)], road_half_width: f...` — Modify terrain splat data along a road corridor.
-  `TerrainConfig` type L34-63 — `impl Default for TerrainConfig` — outputting DD-compatible terrain data.
-  `default` function L35-62 — `() -> Self` — outputting DD-compatible terrain data.
-  `compute_weights` function L158-197 — `(noise_val: f64, slots: &[TerrainSlot; 4], blend_width: f64) -> [u8; 4]` — Compute normalized RGBA weights for a noise value across 4 slots.
-  `slot_weight` function L200-215 — `(noise_val: f64, lower: f64, upper: f64, blend_width: f64) -> f64` — Compute the raw weight for a single slot based on noise value.
-  `tests` module L218-358 — `-` — outputting DD-compatible terrain data.
-  `test_generate_terrain_dimensions` function L223-231 — `()` — outputting DD-compatible terrain data.
-  `test_splat_weights_sum_to_255` function L234-250 — `()` — outputting DD-compatible terrain data.
-  `test_compute_weights_pure_slot` function L253-285 — `()` — outputting DD-compatible terrain data.
-  `test_compute_weights_blend_zone` function L288-318 — `()` — outputting DD-compatible terrain data.
-  `test_road_corridor` function L321-339 — `()` — outputting DD-compatible terrain data.
-  `test_terrain_textures` function L342-357 — `()` — outputting DD-compatible terrain data.

#### crates/mimir-mapgen/src/water.rs

- pub `WaterConfig` struct L16-34 — `{ threshold: f64, deep_color: String, shallow_color: String, blend_distance: f64...` — Configuration for water body generation.
- pub `generate_water` function L56-62 — `( noise_map: &NoiseMap, config: &WaterConfig, alloc: &NodeIdAllocator, ) -> Wate...` — Generate water bodies from a noise map.
- pub `generate_water_radial` function L67-73 — `( noise_map: &NoiseMap, config: &WaterConfig, alloc: &NodeIdAllocator, ) -> Wate...` — Generate water with radial sampling mode for lake presets.
- pub `generate_water_island` function L76-82 — `( noise_map: &NoiseMap, config: &WaterConfig, alloc: &NodeIdAllocator, ) -> Wate...` — Generate water for island-mode maps (water ring around edges).
- pub `water_from_polygon` function L374-395 — `( polygon: &[(f64, f64)], config: &WaterConfig, alloc: &NodeIdAllocator, ) -> Wa...` — Generate water from a river corridor polygon.
- pub `water_from_river` function L398-419 — `( polygon: &[(f64, f64)], config: &crate::paths::RiverConfig, alloc: &NodeIdAllo...` — Build a WaterTree from a river polygon, using the river config's own colors.
- pub `water_node_ref_pub` function L428-432 — `(alloc: &NodeIdAllocator) -> i64` — Public version of water_node_ref for use in pipeline.rs.
-  `WaterConfig` type L36-49 — `impl Default for WaterConfig` — outputting DD-compatible WaterTree structures.
-  `default` function L37-48 — `() -> Self` — outputting DD-compatible WaterTree structures.
-  `WaterMode` enum L85-89 — `Contour | Lake | Island` — outputting DD-compatible WaterTree structures.
-  `generate_water_inner` function L91-154 — `( noise_map: &NoiseMap, config: &WaterConfig, alloc: &NodeIdAllocator, mode: Wat...` — outputting DD-compatible WaterTree structures.
-  `generate_island_water_tree` function L160-223 — `( noise_map: &NoiseMap, config: &WaterConfig, alloc: &NodeIdAllocator, ) -> Wate...` — Build island water tree: ocean rectangle parent → island shoreline child (transparent hole).
-  `radial_water_polygon` function L230-281 — `(noise_map: &NoiseMap, config: &WaterConfig) -> Vec<Vec<(f64, f64)>>` — Generate a single closed water polygon by radial sampling from the map center.
-  `island_shoreline` function L287-369 — `(noise_map: &NoiseMap, config: &WaterConfig) -> Vec<(f64, f64)>` — Trace the island shoreline via radial sampling.
-  `water_node_ref` function L423-425 — `(alloc: &NodeIdAllocator) -> i64` — Generate a water tree node ref in the large negative range that DD expects.
-  `tests` module L435-517 — `-` — outputting DD-compatible WaterTree structures.
-  `test_generate_water_basic` function L440-464 — `()` — outputting DD-compatible WaterTree structures.
-  `test_generate_water_uniform` function L467-481 — `()` — outputting DD-compatible WaterTree structures.
-  `test_water_from_polygon` function L484-500 — `()` — outputting DD-compatible WaterTree structures.
-  `test_water_colors` function L503-516 — `()` — outputting DD-compatible WaterTree structures.

### crates/mimir-mapgen/src/format

> *Semantic summary to be generated by AI agent.*

#### crates/mimir-mapgen/src/format/entities.rs

- pub `MapObject` struct L55-68 — `{ texture: String, position: Vector2, scale: Vector2, rotation: f64, mirror: boo...` — A placed object on the map.
- pub `new` function L71-83 — `(texture: &str, position: Vector2, node_id: &str) -> Self` — Map entity types: objects, paths, lights, walls, patterns, portals, texts.
- pub `with_scale` function L85-88 — `(mut self, scale: f64) -> Self` — Map entity types: objects, paths, lights, walls, patterns, portals, texts.
- pub `with_rotation` function L90-93 — `(mut self, radians: f64) -> Self` — Map entity types: objects, paths, lights, walls, patterns, portals, texts.
- pub `with_layer` function L95-98 — `(mut self, layer: i32) -> Self` — Map entity types: objects, paths, lights, walls, patterns, portals, texts.
- pub `with_mirror` function L100-103 — `(mut self, mirror: bool) -> Self` — Map entity types: objects, paths, lights, walls, patterns, portals, texts.
- pub `with_custom_color` function L105-108 — `(mut self, color: &str) -> Self` — Map entity types: objects, paths, lights, walls, patterns, portals, texts.
- pub `MapPath` struct L113-122 — `{ texture: String, color: String, points: PoolVector2Array, width: f64, layer: i...` — A path on the map (roads, rivers, cliffs, etc.).
- pub `new` function L125-135 — `(texture: &str, points: Vec<Vector2>, width: f64, node_id: &str) -> Self` — Map entity types: objects, paths, lights, walls, patterns, portals, texts.
- pub `with_color` function L137-140 — `(mut self, color: &str) -> Self` — Map entity types: objects, paths, lights, walls, patterns, portals, texts.
- pub `with_layer` function L142-145 — `(mut self, layer: i32) -> Self` — Map entity types: objects, paths, lights, walls, patterns, portals, texts.
- pub `with_loop` function L147-150 — `(mut self, loop_path: bool) -> Self` — Map entity types: objects, paths, lights, walls, patterns, portals, texts.
- pub `MapLight` struct L155-163 — `{ position: Vector2, color: String, range: f64, intensity: f64, shadows: bool, l...` — A light source on the map.
- pub `new` function L166-176 — `(position: Vector2, color: &str, range: f64, node_id: &str) -> Self` — Map entity types: objects, paths, lights, walls, patterns, portals, texts.
- pub `MapWall` struct L185-209 — `{ points: PoolVector2Array, texture: String, color: String, is_loop: bool, wall_...` — A wall segment — a polyline of connected wall segments.
- pub `new_room` function L213-226 — `(points: Vec<Vector2>, texture: &str, node_id: &str) -> Self` — Create a new closed-polygon wall (room outline) with default settings.
- pub `new_open` function L229-242 — `(points: Vec<Vector2>, texture: &str, node_id: &str) -> Self` — Create a new open polyline wall with default settings.
- pub `with_color` function L244-247 — `(mut self, color: &str) -> Self` — Map entity types: objects, paths, lights, walls, patterns, portals, texts.
- pub `with_portals` function L249-252 — `(mut self, portals: Vec<MapPortal>) -> Self` — Map entity types: objects, paths, lights, walls, patterns, portals, texts.
- pub `MapPortal` struct L261-286 — `{ position: Vector2, rotation: f64, scale: Vector2, direction: Vector2, texture:...` — A portal (door/window/archway) — an opening in a wall.
- pub `new` function L290-314 — `( position: Vector2, rotation: f64, direction: Vector2, texture: &str, radius: f...` — Create a new portal attached to a wall.
- pub `new_freestanding` function L317-338 — `( position: Vector2, rotation: f64, direction: Vector2, texture: &str, radius: f...` — Create a freestanding portal (not attached to any wall).
- pub `with_closed` function L340-343 — `(mut self, closed: bool) -> Self` — Map entity types: objects, paths, lights, walls, patterns, portals, texts.
- pub `MapPattern` struct L348-351 — `{ data: serde_json::Value }` — A pattern (repeating texture region).
- pub `MapText` struct L355-364 — `{ text: String, position: Vector2, font_name: String, font_size: u32, font_color...` — A text label on the map.
-  `serialize_wall_id` function L11-18 — `(value: &String, serializer: S) -> Result<S::Ok, S::Error>` — Custom serializer for wall_id: numeric hex strings serialize as integers,
-  `deserialize_wall_id` function L21-51 — `(deserializer: D) -> Result<String, D::Error>` — Custom deserializer for wall_id: accepts both integer and string values.
-  `WallIdVisitor` struct L24 — `-` — Map entity types: objects, paths, lights, walls, patterns, portals, texts.
-  `WallIdVisitor` type L26-48 — `= WallIdVisitor` — Map entity types: objects, paths, lights, walls, patterns, portals, texts.
-  `Value` type L27 — `= String` — Map entity types: objects, paths, lights, walls, patterns, portals, texts.
-  `expecting` function L29-31 — `(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result` — Map entity types: objects, paths, lights, walls, patterns, portals, texts.
-  `visit_i64` function L33-35 — `(self, v: i64) -> Result<String, E>` — Map entity types: objects, paths, lights, walls, patterns, portals, texts.
-  `visit_u64` function L37-39 — `(self, v: u64) -> Result<String, E>` — Map entity types: objects, paths, lights, walls, patterns, portals, texts.
-  `visit_str` function L41-43 — `(self, v: &str) -> Result<String, E>` — Map entity types: objects, paths, lights, walls, patterns, portals, texts.
-  `visit_string` function L45-47 — `(self, v: String) -> Result<String, E>` — Map entity types: objects, paths, lights, walls, patterns, portals, texts.
-  `MapObject` type L70-109 — `= MapObject` — Map entity types: objects, paths, lights, walls, patterns, portals, texts.
-  `MapPath` type L124-151 — `= MapPath` — Map entity types: objects, paths, lights, walls, patterns, portals, texts.
-  `MapLight` type L165-177 — `= MapLight` — Map entity types: objects, paths, lights, walls, patterns, portals, texts.
-  `MapWall` type L211-253 — `= MapWall` — Map entity types: objects, paths, lights, walls, patterns, portals, texts.
-  `MapPortal` type L288-344 — `= MapPortal` — Map entity types: objects, paths, lights, walls, patterns, portals, texts.

#### crates/mimir-mapgen/src/format/godot_types.rs

- pub `Vector2` struct L11-14 — `{ x: f64, y: f64 }` — A 2D vector, serialized as `"Vector2( x, y )"`.
- pub `new` function L17-19 — `(x: f64, y: f64) -> Self` — These are encoded as strings in the JSON, e.g.
- pub `zero` function L21-23 — `() -> Self` — These are encoded as strings in the JSON, e.g.
- pub `PoolByteArray` struct L61 — `-` — A pool of bytes, serialized as `"PoolByteArray( 0, 1, 2, ...
- pub `new` function L64-66 — `() -> Self` — These are encoded as strings in the JSON, e.g.
- pub `from_vec` function L68-70 — `(v: Vec<u8>) -> Self` — These are encoded as strings in the JSON, e.g.
- pub `PoolVector2Array` struct L122 — `-` — A pool of 2D vectors, serialized as `"PoolVector2Array( x1, y1, x2, y2, ...
- pub `new` function L125-127 — `() -> Self` — These are encoded as strings in the JSON, e.g.
- pub `from_points` function L129-131 — `(points: Vec<Vector2>) -> Self` — These are encoded as strings in the JSON, e.g.
- pub `PoolIntArray` struct L193 — `-` — A pool of integers, serialized as `"PoolIntArray( 1, 2, 3, ...
- pub `new` function L196-198 — `() -> Self` — These are encoded as strings in the JSON, e.g.
- pub `filled` function L200-202 — `(value: i32, count: usize) -> Self` — These are encoded as strings in the JSON, e.g.
- pub `NullableVector2` enum L263-266 — `Null | Value` — A nullable Vector2 field — DD uses the string `"null"` instead of JSON null.
-  `Vector2` type L16-24 — `= Vector2` — These are encoded as strings in the JSON, e.g.
-  `Vector2` type L26-30 — `impl Serialize for Vector2` — These are encoded as strings in the JSON, e.g.
-  `serialize` function L27-29 — `(&self, serializer: S) -> Result<S::Ok, S::Error>` — These are encoded as strings in the JSON, e.g.
-  `Vector2` type L32-37 — `= Vector2` — These are encoded as strings in the JSON, e.g.
-  `deserialize` function L33-36 — `(deserializer: D) -> Result<Self, D::Error>` — These are encoded as strings in the JSON, e.g.
-  `parse_vector2` function L39-57 — `(s: &str) -> Result<Vector2, String>` — These are encoded as strings in the JSON, e.g.
-  `PoolByteArray` type L63-71 — `= PoolByteArray` — These are encoded as strings in the JSON, e.g.
-  `PoolByteArray` type L73-77 — `impl Default for PoolByteArray` — These are encoded as strings in the JSON, e.g.
-  `default` function L74-76 — `() -> Self` — These are encoded as strings in the JSON, e.g.
-  `PoolByteArray` type L79-89 — `impl Serialize for PoolByteArray` — These are encoded as strings in the JSON, e.g.
-  `serialize` function L80-88 — `(&self, serializer: S) -> Result<S::Ok, S::Error>` — These are encoded as strings in the JSON, e.g.
-  `PoolByteArray` type L91-96 — `= PoolByteArray` — These are encoded as strings in the JSON, e.g.
-  `deserialize` function L92-95 — `(deserializer: D) -> Result<Self, D::Error>` — These are encoded as strings in the JSON, e.g.
-  `parse_pool_byte_array` function L98-118 — `(s: &str) -> Result<PoolByteArray, String>` — These are encoded as strings in the JSON, e.g.
-  `PoolVector2Array` type L124-132 — `= PoolVector2Array` — These are encoded as strings in the JSON, e.g.
-  `PoolVector2Array` type L134-138 — `impl Default for PoolVector2Array` — These are encoded as strings in the JSON, e.g.
-  `default` function L135-137 — `() -> Self` — These are encoded as strings in the JSON, e.g.
-  `PoolVector2Array` type L140-150 — `impl Serialize for PoolVector2Array` — These are encoded as strings in the JSON, e.g.
-  `serialize` function L141-149 — `(&self, serializer: S) -> Result<S::Ok, S::Error>` — These are encoded as strings in the JSON, e.g.
-  `PoolVector2Array` type L152-157 — `= PoolVector2Array` — These are encoded as strings in the JSON, e.g.
-  `deserialize` function L153-156 — `(deserializer: D) -> Result<Self, D::Error>` — These are encoded as strings in the JSON, e.g.
-  `parse_pool_vector2_array` function L159-189 — `(s: &str) -> Result<PoolVector2Array, String>` — These are encoded as strings in the JSON, e.g.
-  `PoolIntArray` type L195-203 — `= PoolIntArray` — These are encoded as strings in the JSON, e.g.
-  `PoolIntArray` type L205-209 — `impl Default for PoolIntArray` — These are encoded as strings in the JSON, e.g.
-  `default` function L206-208 — `() -> Self` — These are encoded as strings in the JSON, e.g.
-  `PoolIntArray` type L211-221 — `impl Serialize for PoolIntArray` — These are encoded as strings in the JSON, e.g.
-  `serialize` function L212-220 — `(&self, serializer: S) -> Result<S::Ok, S::Error>` — These are encoded as strings in the JSON, e.g.
-  `PoolIntArray` type L223-228 — `= PoolIntArray` — These are encoded as strings in the JSON, e.g.
-  `deserialize` function L224-227 — `(deserializer: D) -> Result<Self, D::Error>` — These are encoded as strings in the JSON, e.g.
-  `parse_pool_int_array` function L230-250 — `(s: &str) -> Result<PoolIntArray, String>` — These are encoded as strings in the JSON, e.g.
-  `format_f64` function L253-259 — `(v: f64) -> String` — Format an f64 value, using integer format when there's no fractional part.
-  `NullableVector2` type L268-275 — `impl Serialize for NullableVector2` — These are encoded as strings in the JSON, e.g.
-  `serialize` function L269-274 — `(&self, serializer: S) -> Result<S::Ok, S::Error>` — These are encoded as strings in the JSON, e.g.
-  `NullableVector2` type L277-288 — `= NullableVector2` — These are encoded as strings in the JSON, e.g.
-  `deserialize` function L278-287 — `(deserializer: D) -> Result<Self, D::Error>` — These are encoded as strings in the JSON, e.g.
-  `Vector2` type L290-294 — `= Vector2` — These are encoded as strings in the JSON, e.g.
-  `fmt` function L291-293 — `(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result` — These are encoded as strings in the JSON, e.g.
-  `tests` module L297-372 — `-` — These are encoded as strings in the JSON, e.g.
-  `test_vector2_roundtrip` function L301-307 — `()` — These are encoded as strings in the JSON, e.g.
-  `test_vector2_float` function L310-316 — `()` — These are encoded as strings in the JSON, e.g.
-  `test_pool_byte_array_roundtrip` function L319-325 — `()` — These are encoded as strings in the JSON, e.g.
-  `test_pool_byte_array_empty` function L328-334 — `()` — These are encoded as strings in the JSON, e.g.
-  `test_pool_vector2_array_roundtrip` function L337-346 — `()` — These are encoded as strings in the JSON, e.g.
-  `test_pool_int_array_roundtrip` function L349-355 — `()` — These are encoded as strings in the JSON, e.g.
-  `test_nullable_vector2` function L358-364 — `()` — These are encoded as strings in the JSON, e.g.
-  `test_parse_with_extra_spaces` function L367-371 — `()` — These are encoded as strings in the JSON, e.g.

#### crates/mimir-mapgen/src/format/header.rs

- pub `Header` struct L9-16 — `{ creation_build: String, creation_date: CreationDate, uses_default_assets: bool...` — The top-level header of a `.dungeondraft_map` file.
- pub `CreationDate` struct L20-29 — `{ year: u32, month: u32, day: u32, weekday: u32, dst: bool, hour: u32, minute: u...` — Date/time the map was created.
- pub `AssetPackRef` struct L33-43 — `{ name: String, id: String, version: String, author: String, keywords: Option<se...` — Reference to a third-party asset pack used by the map.
- pub `CustomColorOverrides` struct L46-51 — `{ enabled: bool, min_redness: f64, min_saturation: f64, red_tolerance: f64 }` — Dungeondraft map header types.
- pub `EditorState` struct L55-75 — `{ current_level: i32, camera_position: Vector2, camera_zoom: i32, guide_position...` — Editor state preserved in the map file.
- pub `ColorPalettes` struct L78-95 — `{ object_custom_colors: Vec<String>, scatter_custom_colors: Vec<String>, light_c...` — Dungeondraft map header types.
- pub `new` function L99-117 — `(uses_default_assets: bool) -> Self` — Create a minimal header for a new map.
-  `Header` type L97-118 — `= Header` — Dungeondraft map header types.
-  `EditorState` type L120-137 — `impl Default for EditorState` — Dungeondraft map header types.
-  `default` function L121-136 — `() -> Self` — Dungeondraft map header types.

#### crates/mimir-mapgen/src/format/mod.rs

- pub `entities` module L6 — `-` — Provides type-safe Rust structs for the DD map JSON format, including
- pub `godot_types` module L7 — `-` — custom serde (de)serialization for Godot types (`Vector2`, `PoolByteArray`).
- pub `header` module L8 — `-` — custom serde (de)serialization for Godot types (`Vector2`, `PoolByteArray`).
- pub `world` module L9 — `-` — custom serde (de)serialization for Godot types (`Vector2`, `PoolByteArray`).
- pub `DungeondraftMap` struct L22-28 — `{ header: Header, world: World, mod_data: ModData }` — Top-level `.dungeondraft_map` file structure.
- pub `ModData` struct L32-35 — `{ node_table: BTreeMap<String, serde_json::Value> }` — Mod data section at the root of a `.dungeondraft_map` file.
- pub `new` function L39-45 — `(width: u32, height: u32) -> Self` — Create a new empty map with the given dimensions (in grid squares).
- pub `from_json` function L48-50 — `(json: &str) -> Result<Self, serde_json::Error>` — Parse a `.dungeondraft_map` file from JSON.
- pub `to_json` function L53-55 — `(&self) -> Result<String, serde_json::Error>` — Serialize to JSON string.
- pub `ground_level_mut` function L58-63 — `(&mut self) -> &mut Level` — Get a mutable reference to the ground level (level "0").
- pub `ground_level` function L66-68 — `(&self) -> Option<&Level>` — Get a reference to the ground level.
- pub `add_water_colors` function L72-81 — `(&mut self, deep: &str, shallow: &str)` — Add water colors to the header's color palettes.
- pub `NodeIdAllocator` struct L89-91 — `{ next_id: AtomicU64 }` — Allocator for unique node reference IDs.
- pub `new` function L94-98 — `(start: u64) -> Self` — custom serde (de)serialization for Godot types (`Vector2`, `PoolByteArray`).
- pub `next` function L101-104 — `(&self) -> String` — Allocate the next unique node ID as a hex string.
- pub `current` function L107-110 — `(&self) -> String` — Get the current next_node_id value as a hex string (for writing to the world).
-  `DungeondraftMap` type L37-82 — `= DungeondraftMap` — custom serde (de)serialization for Godot types (`Vector2`, `PoolByteArray`).
-  `NodeIdAllocator` type L93-111 — `= NodeIdAllocator` — custom serde (de)serialization for Godot types (`Vector2`, `PoolByteArray`).
-  `NodeIdAllocator` type L113-117 — `impl Default for NodeIdAllocator` — custom serde (de)serialization for Godot types (`Vector2`, `PoolByteArray`).
-  `default` function L114-116 — `() -> Self` — custom serde (de)serialization for Godot types (`Vector2`, `PoolByteArray`).
-  `tests` module L120-185 — `-` — custom serde (de)serialization for Godot types (`Vector2`, `PoolByteArray`).
-  `test_new_map_serializes` function L124-131 — `()` — custom serde (de)serialization for Godot types (`Vector2`, `PoolByteArray`).
-  `test_new_map_roundtrip` function L134-141 — `()` — custom serde (de)serialization for Godot types (`Vector2`, `PoolByteArray`).
-  `test_node_id_allocator` function L144-150 — `()` — custom serde (de)serialization for Godot types (`Vector2`, `PoolByteArray`).
-  `test_terrain_splat_size` function L153-166 — `()` — custom serde (de)serialization for Godot types (`Vector2`, `PoolByteArray`).
-  `test_map_object_builder` function L169-184 — `()` — custom serde (de)serialization for Godot types (`Vector2`, `PoolByteArray`).

#### crates/mimir-mapgen/src/format/world.rs

- pub `World` struct L11-32 — `{ format: u32, width: u32, height: u32, next_node_id: String, next_prefab_id: St...` — The world section of a `.dungeondraft_map` file.
- pub `MapSpaceInfo` struct L36-41 — `{ offset_map_size: u32, max_offset_distance: f64, cell_size: u32, seed: String }` — Map space info for material scatter.
- pub `Grid` struct L45-48 — `{ color: String, texture: String }` — Grid configuration.
- pub `Level` struct L52-88 — `{ label: String, environment: Environment, layers: BTreeMap<String, String>, sha...` — A single level (floor) in the map.
- pub `Environment` struct L92-101 — `{ baked_lighting: bool, ambient_light: String, ambient_energy: Option<f64>, shad...` — Lighting environment for a level.
- pub `Shapes` struct L120-125 — `{ polygons: Vec<serde_json::Value>, walls: Vec<serde_json::Value> }` — Wall and polygon shapes.
- pub `Tiles` struct L129-135 — `{ cells: PoolIntArray, colors: Vec<String>, lookup: BTreeMap<String, String> }` — Tile grid data.
- pub `Cave` struct L139-149 — `{ bitmap: PoolByteArray, ground_color: String, wall_color: String, entrance_bitm...` — Cave bitmap.
- pub `Terrain` struct L161-172 — `{ enabled: bool, expand_slots: bool, smooth_blending: bool, texture_1: String, t...` — Terrain configuration with 4-texture splat map.
- pub `new_uniform` function L179-196 — `(width: u32, height: u32, textures: [String; 4]) -> Self` — Create a new terrain with uniform texture_1 coverage.
- pub `Water` struct L201-206 — `{ disable_border: bool, tree: Option<WaterTree> }` — Water configuration.
- pub `WaterTree` struct L210-226 — `{ node_ref: i64, polygon: PoolVector2Array, join: i32, end: i32, is_open: bool, ...` — Recursive water polygon tree.
- pub `Roofs` struct L230-239 — `{ shade: bool, shade_contrast: f64, sun_direction: f64, roofs: Vec<serde_json::V...` — Roof configuration.
- pub `new_ground` function L264-314 — `(width: u32, height: u32) -> Self` — Create a default ground-level level.
- pub `new` function L319-346 — `(width: u32, height: u32) -> Self` — Create a new empty world with the given dimensions (in grid squares).
-  `default_ambient_light` function L103-105 — `() -> String` — Dungeondraft world and level types.
-  `Environment` type L107-116 — `impl Default for Environment` — Dungeondraft world and level types.
-  `default` function L108-115 — `() -> Self` — Dungeondraft world and level types.
-  `default_cave_color` function L151-153 — `() -> String` — Dungeondraft world and level types.
-  `default_cave_texture` function L155-157 — `() -> String` — Dungeondraft world and level types.
-  `Terrain` type L174-197 — `= Terrain` — Dungeondraft world and level types.
-  `default_true` function L241-243 — `() -> bool` — Dungeondraft world and level types.
-  `default_shade_contrast` function L244-246 — `() -> f64` — Dungeondraft world and level types.
-  `default_sun_direction` function L247-249 — `() -> f64` — Dungeondraft world and level types.
-  `Roofs` type L251-260 — `impl Default for Roofs` — Dungeondraft world and level types.
-  `default` function L252-259 — `() -> Self` — Dungeondraft world and level types.
-  `Level` type L262-315 — `= Level` — Dungeondraft world and level types.
-  `World` type L317-347 — `= World` — Dungeondraft world and level types.

### crates/mimir-mapgen/tests

> *Semantic summary to be generated by AI agent.*

#### crates/mimir-mapgen/tests/cli_integration.rs

-  `mapgen_bin` function L5-7 — `() -> Command` — CLI integration tests for mimir-mapgen binary.
-  `test_list_presets` function L10-18 — `()` — CLI integration tests for mimir-mapgen binary.
-  `test_generate_from_preset` function L21-50 — `()` — CLI integration tests for mimir-mapgen binary.
-  `test_generate_from_yaml_config` function L53-82 — `()` — CLI integration tests for mimir-mapgen binary.
-  `test_generate_deterministic_output` function L85-112 — `()` — CLI integration tests for mimir-mapgen binary.
-  `test_validate_valid_config` function L115-129 — `()` — CLI integration tests for mimir-mapgen binary.
-  `test_generate_no_args_fails` function L132-135 — `()` — CLI integration tests for mimir-mapgen binary.
-  `test_generate_both_config_and_preset_fails` function L138-150 — `()` — CLI integration tests for mimir-mapgen binary.
-  `test_generate_unknown_preset_fails` function L153-162 — `()` — CLI integration tests for mimir-mapgen binary.

#### crates/mimir-mapgen/tests/format_roundtrip.rs

-  `test_baseline_map_parse` function L6-24 — `()` — Round-trip test: parse baseline.dungeondraft_map → serialize → parse again.
-  `test_baseline_map_roundtrip` function L27-41 — `()` — Round-trip test: parse baseline.dungeondraft_map → serialize → parse again.
-  `test_wall_portal_roundtrip` function L44-96 — `()` — Round-trip test: parse baseline.dungeondraft_map → serialize → parse again.
-  `test_wall_builder` function L99-142 — `()` — Round-trip test: parse baseline.dungeondraft_map → serialize → parse again.

#### crates/mimir-mapgen/tests/polygon_snapshots.rs

-  `fixture` function L13-19 — `(name: &str) -> String` — cargo run -p mimir-mapgen -- generate <fixture>.yaml -o <fixture>.dungeondraft_map
-  `load_config` function L21-27 — `(yaml_name: &str) -> pipeline::MapConfig` — cargo run -p mimir-mapgen -- generate <fixture>.yaml -o <fixture>.dungeondraft_map
-  `assert_world_matches` function L29-47 — `(yaml_name: &str, baseline_name: &str)` — cargo run -p mimir-mapgen -- generate <fixture>.yaml -o <fixture>.dungeondraft_map
-  `snapshot_single_polygon` function L50-52 — `()` — cargo run -p mimir-mapgen -- generate <fixture>.yaml -o <fixture>.dungeondraft_map
-  `snapshot_two_adjacent_rooms` function L55-60 — `()` — cargo run -p mimir-mapgen -- generate <fixture>.yaml -o <fixture>.dungeondraft_map
-  `snapshot_three_rooms_two_corridors` function L63-68 — `()` — cargo run -p mimir-mapgen -- generate <fixture>.yaml -o <fixture>.dungeondraft_map
-  `snapshot_overlapping_ovals` function L71-76 — `()` — cargo run -p mimir-mapgen -- generate <fixture>.yaml -o <fixture>.dungeondraft_map

### crates/mimir-mcp/src

> *Semantic summary to be generated by AI agent.*

#### crates/mimir-mcp/src/context.rs

- pub `McpContext` struct L19-26 — `{ db_url: String, assets_dir: PathBuf, active_campaign_id: Mutex<Option<String>>...` — Shared context for the MCP server.
- pub `new` function L33-69 — `() -> Result<Self, McpError>` — Create a new context from `MIMIR_DATABASE_PATH` environment variable.
- pub `get_active_campaign_id` function L106-111 — `(&self) -> Option<String>` — Get the active campaign ID, if set.
- pub `set_active_campaign_id` function L114-118 — `(&self, campaign_id: Option<String>)` — Set the active campaign ID.
- pub `connect` function L124-128 — `(&self) -> Result<SqliteConnection, McpError>` — Create a new database connection.
- pub `for_testing` function L135-150 — `() -> Self` — Create a context for testing with a temporary file-based database.
-  `McpContext` type L28-151 — `= McpContext` — This allows concurrent read operations with SQLite WAL mode.
-  `expand_path` function L72-84 — `(path: &str) -> PathBuf` — Expand `~` and `$HOME` in a path string.
-  `default_db_path` function L87-103 — `() -> Option<String>` — Return the default database path for the current platform.
-  `COUNTER` variable L137 — `: AtomicU32` — This allows concurrent read operations with SQLite WAL mode.

#### crates/mimir-mcp/src/error.rs

- pub `McpError` enum L7-35 — `Initialization | Database | ToolNotFound | InvalidArguments | NoActiveCampaign |...` — Errors that can occur in the MCP server.
-  `McpError` type L37-41 — `= McpError` — MCP Error Types
-  `from` function L38-40 — `(e: diesel::result::Error) -> Self` — MCP Error Types
-  `McpError` type L43-47 — `= McpError` — MCP Error Types
-  `from` function L44-46 — `(e: serde_json::Error) -> Self` — MCP Error Types
-  `McpError` type L49-66 — `= McpError` — MCP Error Types
-  `from` function L50-65 — `(e: mimir_core::services::ServiceError) -> Self` — MCP Error Types

#### crates/mimir-mcp/src/handler.rs

- pub `MimirHandler` struct L23-25 — `{ context: Arc<McpContext> }` — Mimir MCP Server Handler.
- pub `new` function L29-34 — `() -> Result<Self, McpError>` — Create a new handler with initialized context.
- pub `with_context` function L37-39 — `(context: Arc<McpContext>) -> Self` — Create a handler with an existing context.
-  `MimirHandler` type L27-269 — `= MimirHandler` — Implements the ServerHandler trait to route tool calls to appropriate handlers.
-  `get_tools` function L42-126 — `() -> Vec<Tool>` — Get the list of available tools.
-  `execute_tool` function L129-268 — `(&self, name: &str, args: Value) -> Result<Value, McpError>` — Route a tool call to the appropriate handler.
-  `MimirHandler` type L272-322 — `impl ServerHandler for MimirHandler` — Implements the ServerHandler trait to route tool calls to appropriate handlers.
-  `handle_list_tools_request` function L273-284 — `( &self, _params: Option<PaginatedRequestParams>, _runtime: Arc<dyn McpServer>, ...` — Implements the ServerHandler trait to route tool calls to appropriate handlers.
-  `handle_call_tool_request` function L286-321 — `( &self, params: CallToolRequestParams, _runtime: Arc<dyn McpServer>, ) -> Resul...` — Implements the ServerHandler trait to route tool calls to appropriate handlers.
-  `tests` module L325-1305 — `-` — Implements the ServerHandler trait to route tool calls to appropriate handlers.
-  `EXPECTED_TOOLS` variable L331-413 — `: &[&str]` — Expected tool names — every MCP tool the server should publish.
-  `test_ctx` function L415-417 — `() -> Arc<McpContext>` — Implements the ServerHandler trait to route tool calls to appropriate handlers.
-  `all_expected_tools_are_published` function L420-432 — `()` — Implements the ServerHandler trait to route tool calls to appropriate handlers.
-  `no_duplicate_tool_names` function L435-446 — `()` — Implements the ServerHandler trait to route tool calls to appropriate handlers.
-  `published_tools_match_expected_count` function L449-460 — `()` — Implements the ServerHandler trait to route tool calls to appropriate handlers.
-  `every_published_tool_has_a_route` function L463-481 — `()` — Implements the ServerHandler trait to route tool calls to appropriate handlers.
-  `all_tools_have_descriptions` function L484-493 — `()` — Implements the ServerHandler trait to route tool calls to appropriate handlers.
-  `call_ok` function L500-505 — `(handler: &MimirHandler, name: &str, args: Value) -> Value` — Helper: call a tool by name and assert success, returning the result JSON.
-  `call_err` function L508-513 — `(handler: &MimirHandler, name: &str, args: Value) -> McpError` — Helper: call a tool by name and assert it returns an error.
-  `campaign_crud_lifecycle` function L518-570 — `()` — Implements the ServerHandler trait to route tool calls to appropriate handlers.
-  `setup_campaign` function L575-590 — `(handler: &MimirHandler) -> String` — Helper: create a campaign and set it active, return the campaign id.
-  `module_crud_lifecycle` function L593-645 — `()` — Implements the ServerHandler trait to route tool calls to appropriate handlers.
-  `document_crud_lifecycle` function L650-733 — `()` — Implements the ServerHandler trait to route tool calls to appropriate handlers.
-  `campaign_level_document` function L738-768 — `()` — Implements the ServerHandler trait to route tool calls to appropriate handlers.
-  `character_crud_lifecycle` function L773-828 — `()` — Implements the ServerHandler trait to route tool calls to appropriate handlers.
-  `character_filter_by_type` function L831-870 — `()` — Implements the ServerHandler trait to route tool calls to appropriate handlers.
-  `catalog_searches_return_empty_on_fresh_db` function L875-903 — `()` — Implements the ServerHandler trait to route tool calls to appropriate handlers.
-  `tool_not_found_for_unknown_name` function L908-912 — `()` — Implements the ServerHandler trait to route tool calls to appropriate handlers.
-  `campaign_required_tools_fail_without_active_campaign` function L915-934 — `()` — Implements the ServerHandler trait to route tool calls to appropriate handlers.
-  `create_character_requires_name` function L937-952 — `()` — Implements the ServerHandler trait to route tool calls to appropriate handlers.
-  `delete_campaign_requires_id` function L955-960 — `()` — Implements the ServerHandler trait to route tool calls to appropriate handlers.
-  `homebrew_monster_crud_lifecycle` function L965-1028 — `()` — Implements the ServerHandler trait to route tool calls to appropriate handlers.
-  `homebrew_spell_crud_lifecycle` function L1033-1088 — `()` — Implements the ServerHandler trait to route tool calls to appropriate handlers.
-  `homebrew_item_crud_lifecycle` function L1093-1148 — `()` — Implements the ServerHandler trait to route tool calls to appropriate handlers.
-  `homebrew_not_found_errors` function L1153-1182 — `()` — Implements the ServerHandler trait to route tool calls to appropriate handlers.
-  `homebrew_create_requires_name_and_data` function L1185-1224 — `()` — Implements the ServerHandler trait to route tool calls to appropriate handlers.
-  `homebrew_get_update_delete_require_id` function L1227-1252 — `()` — Implements the ServerHandler trait to route tool calls to appropriate handlers.
-  `homebrew_list_requires_active_campaign` function L1255-1274 — `()` — Implements the ServerHandler trait to route tool calls to appropriate handlers.
-  `homebrew_create_requires_active_campaign` function L1277-1304 — `()` — Implements the ServerHandler trait to route tool calls to appropriate handlers.

#### crates/mimir-mcp/src/lib.rs

- pub `context` module L6 — `-` — Model Context Protocol server for D&D 5e campaign management.
- pub `error` module L7 — `-` — Enables Claude Code and other MCP clients to interact with Mimir campaigns.
- pub `handler` module L8 — `-` — Enables Claude Code and other MCP clients to interact with Mimir campaigns.
- pub `response` module L9 — `-` — Enables Claude Code and other MCP clients to interact with Mimir campaigns.
- pub `tools` module L10 — `-` — Enables Claude Code and other MCP clients to interact with Mimir campaigns.

#### crates/mimir-mcp/src/main.rs

-  `main` function L17-79 — `() -> anyhow::Result<()>` — Runs the MCP server over stdio for Claude Code integration.

#### crates/mimir-mcp/src/response.rs

- pub `McpResponse` struct L23 — `-` — Helper for constructing standardized MCP tool responses.
- pub `list` function L40-46 — `(key: &str, items: Vec<Value>) -> Result<Value, McpError>` — Response for listing multiple entities.
- pub `get` function L62-64 — `(key: &str, data: Value) -> Result<Value, McpError>` — Response for getting a single entity.
- pub `created` function L80-85 — `(key: &str, data: Value) -> Result<Value, McpError>` — Response for creating a new entity.
- pub `updated` function L101-106 — `(key: &str, data: Value) -> Result<Value, McpError>` — Response for updating an existing entity.
- pub `deleted` function L121-126 — `(id: &str) -> Result<Value, McpError>` — Response for deleting an entity.
- pub `success` function L144-149 — `(data: Value) -> Result<Value, McpError>` — Response for generic success with additional data.
- pub `ok` function L161-163 — `(data: Value) -> Result<Value, McpError>` — Response for raw data (no wrapper).
- pub `added` function L179-184 — `(key: &str, data: Value) -> Result<Value, McpError>` — Response for operations that add/attach something to a parent entity.
- pub `removed` function L199-204 — `(id: &str) -> Result<Value, McpError>` — Response for operations that remove/detach something from a parent entity.
-  `McpResponse` type L25-205 — `= McpResponse` — for AI consumers.
-  `tests` module L208-276 — `-` — for AI consumers.
-  `test_list_response` function L212-219 — `()` — for AI consumers.
-  `test_get_response` function L222-227 — `()` — for AI consumers.
-  `test_created_response` function L230-235 — `()` — for AI consumers.
-  `test_updated_response` function L238-243 — `()` — for AI consumers.
-  `test_deleted_response` function L246-251 — `()` — for AI consumers.
-  `test_success_response` function L254-259 — `()` — for AI consumers.
-  `test_added_response` function L262-267 — `()` — for AI consumers.
-  `test_removed_response` function L270-275 — `()` — for AI consumers.

### crates/mimir-mcp/src/tools

> *Semantic summary to be generated by AI agent.*

#### crates/mimir-mcp/src/tools/campaign.rs

- pub `list_campaigns_tool` function L24-36 — `() -> Tool` — MCP tools for campaign management.
- pub `set_active_campaign_tool` function L38-60 — `() -> Tool` — MCP tools for campaign management.
- pub `get_campaign_details_tool` function L62-85 — `() -> Tool` — MCP tools for campaign management.
- pub `get_campaign_sources_tool` function L87-107 — `() -> Tool` — MCP tools for campaign management.
- pub `create_campaign_tool` function L109-128 — `() -> Tool` — MCP tools for campaign management.
- pub `update_campaign_tool` function L130-150 — `() -> Tool` — MCP tools for campaign management.
- pub `delete_campaign_tool` function L152-170 — `() -> Tool` — MCP tools for campaign management.
- pub `list_campaigns` function L176-195 — `(ctx: &Arc<McpContext>, _args: Value) -> Result<Value, McpError>` — MCP tools for campaign management.
- pub `set_active_campaign` function L197-222 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for campaign management.
- pub `get_campaign_details` function L224-293 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for campaign management.
- pub `get_campaign_sources` function L295-312 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for campaign management.
- pub `create_campaign` function L314-342 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for campaign management.
- pub `update_campaign` function L344-373 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for campaign management.
- pub `delete_campaign` function L375-394 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for campaign management.
- pub `export_campaign_tool` function L400-420 — `() -> Tool` — MCP tools for campaign management.
- pub `import_campaign_tool` function L422-443 — `() -> Tool` — MCP tools for campaign management.
- pub `preview_archive_tool` function L445-465 — `() -> Tool` — MCP tools for campaign management.
- pub `export_campaign` function L471-497 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for campaign management.
- pub `import_campaign` function L499-532 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for campaign management.
- pub `preview_archive` function L534-566 — `(_ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for campaign management.

#### crates/mimir-mcp/src/tools/catalog.rs

- pub `search_monsters_tool` function L24-47 — `() -> Tool` — MCP tools for searching the D&D 5e catalog.
- pub `search_items_tool` function L49-78 — `() -> Tool` — MCP tools for searching the D&D 5e catalog.
- pub `search_spells_tool` function L80-106 — `() -> Tool` — MCP tools for searching the D&D 5e catalog.
- pub `search_races_tool` function L108-127 — `() -> Tool` — MCP tools for searching the D&D 5e catalog.
- pub `search_classes_tool` function L129-148 — `() -> Tool` — MCP tools for searching the D&D 5e catalog.
- pub `search_backgrounds_tool` function L150-169 — `() -> Tool` — MCP tools for searching the D&D 5e catalog.
- pub `search_feats_tool` function L171-190 — `() -> Tool` — MCP tools for searching the D&D 5e catalog.
- pub `search_conditions_tool` function L192-211 — `() -> Tool` — MCP tools for searching the D&D 5e catalog.
- pub `search_monsters` function L217-311 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for searching the D&D 5e catalog.
- pub `search_items` function L313-361 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for searching the D&D 5e catalog.
- pub `search_spells` function L363-411 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for searching the D&D 5e catalog.
- pub `search_races` function L413-438 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for searching the D&D 5e catalog.
- pub `search_classes` function L440-465 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for searching the D&D 5e catalog.
- pub `search_backgrounds` function L467-492 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for searching the D&D 5e catalog.
- pub `search_feats` function L494-519 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for searching the D&D 5e catalog.
- pub `search_conditions` function L521-546 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for searching the D&D 5e catalog.

#### crates/mimir-mcp/src/tools/character.rs

- pub `list_characters_tool` function L23-44 — `() -> Tool` — MCP tools for character (NPC and PC) management.
- pub `get_character_tool` function L46-65 — `() -> Tool` — MCP tools for character (NPC and PC) management.
- pub `create_character_tool` function L67-95 — `() -> Tool` — MCP tools for character (NPC and PC) management.
- pub `edit_character_tool` function L97-140 — `() -> Tool` — MCP tools for character (NPC and PC) management.
- pub `delete_character_tool` function L142-158 — `() -> Tool` — MCP tools for character (NPC and PC) management.
- pub `add_item_to_character_tool` function L160-182 — `() -> Tool` — MCP tools for character (NPC and PC) management.
- pub `remove_item_from_character_tool` function L184-202 — `() -> Tool` — MCP tools for character (NPC and PC) management.
- pub `update_character_inventory_tool` function L204-225 — `() -> Tool` — MCP tools for character (NPC and PC) management.
- pub `get_character_inventory_tool` function L227-246 — `() -> Tool` — MCP tools for character (NPC and PC) management.
- pub `level_up_character_tool` function L248-282 — `() -> Tool` — MCP tools for character (NPC and PC) management.
- pub `add_character_spell_tool` function L284-314 — `() -> Tool` — MCP tools for character (NPC and PC) management.
- pub `remove_character_spell_tool` function L316-336 — `() -> Tool` — MCP tools for character (NPC and PC) management.
- pub `list_character_spells_tool` function L338-358 — `() -> Tool` — MCP tools for character (NPC and PC) management.
- pub `list_characters` function L364-414 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for character (NPC and PC) management.
- pub `get_character` function L416-504 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for character (NPC and PC) management.
- pub `create_character` function L506-585 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for character (NPC and PC) management.
- pub `edit_character` function L587-732 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for character (NPC and PC) management.
- pub `delete_character` function L734-748 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for character (NPC and PC) management.
- pub `add_item_to_character` function L750-817 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for character (NPC and PC) management.
- pub `remove_item_from_character` function L819-833 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for character (NPC and PC) management.
- pub `update_character_inventory` function L835-860 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for character (NPC and PC) management.
- pub `get_character_inventory` function L862-904 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for character (NPC and PC) management.
- pub `level_up_character` function L906-1008 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for character (NPC and PC) management.
- pub `add_character_spell` function L1010-1059 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for character (NPC and PC) management.
- pub `remove_character_spell` function L1061-1108 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for character (NPC and PC) management.
- pub `list_character_spells` function L1110-1154 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for character (NPC and PC) management.

#### crates/mimir-mcp/src/tools/document.rs

- pub `list_documents_tool` function L19-40 — `() -> Tool` — MCP tools for managing documents (campaign-level and module-level narrative content).
- pub `read_document_tool` function L42-58 — `() -> Tool` — MCP tools for managing documents (campaign-level and module-level narrative content).
- pub `create_document_tool` function L60-91 — `() -> Tool` — MCP tools for managing documents (campaign-level and module-level narrative content).
- pub `edit_document_tool` function L93-117 — `() -> Tool` — MCP tools for managing documents (campaign-level and module-level narrative content).
- pub `reorder_document_tool` function L119-144 — `() -> Tool` — MCP tools for managing documents (campaign-level and module-level narrative content).
- pub `delete_document_tool` function L146-164 — `() -> Tool` — MCP tools for managing documents (campaign-level and module-level narrative content).
- pub `list_documents` function L170-205 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for managing documents (campaign-level and module-level narrative content).
- pub `read_document` function L207-230 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for managing documents (campaign-level and module-level narrative content).
- pub `create_document` function L232-275 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for managing documents (campaign-level and module-level narrative content).
- pub `edit_document` function L277-325 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for managing documents (campaign-level and module-level narrative content).
- pub `reorder_document` function L327-360 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for managing documents (campaign-level and module-level narrative content).
- pub `delete_document` function L362-376 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for managing documents (campaign-level and module-level narrative content).

#### crates/mimir-mcp/src/tools/homebrew.rs

- pub `list_homebrew_items_tool` function L19-31 — `() -> Tool` — MCP tools for campaign homebrew item CRUD operations.
- pub `get_homebrew_item_tool` function L33-49 — `() -> Tool` — MCP tools for campaign homebrew item CRUD operations.
- pub `create_homebrew_item_tool` function L51-77 — `() -> Tool` — MCP tools for campaign homebrew item CRUD operations.
- pub `update_homebrew_item_tool` function L79-101 — `() -> Tool` — MCP tools for campaign homebrew item CRUD operations.
- pub `delete_homebrew_item_tool` function L103-119 — `() -> Tool` — MCP tools for campaign homebrew item CRUD operations.
- pub `list_homebrew_items` function L140-151 — `(ctx: &Arc<McpContext>, _args: Value) -> Result<Value, McpError>` — MCP tools for campaign homebrew item CRUD operations.
- pub `get_homebrew_item` function L153-163 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for campaign homebrew item CRUD operations.
- pub `create_homebrew_item` function L165-203 — `( ctx: &Arc<McpContext>, args: Value, ) -> Result<Value, McpError>` — MCP tools for campaign homebrew item CRUD operations.
- pub `update_homebrew_item` function L205-233 — `( ctx: &Arc<McpContext>, args: Value, ) -> Result<Value, McpError>` — MCP tools for campaign homebrew item CRUD operations.
- pub `delete_homebrew_item` function L235-245 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for campaign homebrew item CRUD operations.
-  `homebrew_to_json` function L125-138 — `(item: &mimir_core::models::campaign::CampaignHomebrewItem) -> Value` — MCP tools for campaign homebrew item CRUD operations.

#### crates/mimir-mcp/src/tools/homebrew_monster.rs

- pub `list_homebrew_monsters_tool` function L19-31 — `() -> Tool` — MCP tools for campaign homebrew monster CRUD operations.
- pub `get_homebrew_monster_tool` function L33-49 — `() -> Tool` — MCP tools for campaign homebrew monster CRUD operations.
- pub `create_homebrew_monster_tool` function L51-78 — `() -> Tool` — MCP tools for campaign homebrew monster CRUD operations.
- pub `update_homebrew_monster_tool` function L80-103 — `() -> Tool` — MCP tools for campaign homebrew monster CRUD operations.
- pub `delete_homebrew_monster_tool` function L105-121 — `() -> Tool` — MCP tools for campaign homebrew monster CRUD operations.
- pub `list_homebrew_monsters` function L143-154 — `(ctx: &Arc<McpContext>, _args: Value) -> Result<Value, McpError>` — MCP tools for campaign homebrew monster CRUD operations.
- pub `get_homebrew_monster` function L156-166 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for campaign homebrew monster CRUD operations.
- pub `create_homebrew_monster` function L168-207 — `( ctx: &Arc<McpContext>, args: Value, ) -> Result<Value, McpError>` — MCP tools for campaign homebrew monster CRUD operations.
- pub `update_homebrew_monster` function L209-242 — `( ctx: &Arc<McpContext>, args: Value, ) -> Result<Value, McpError>` — MCP tools for campaign homebrew monster CRUD operations.
- pub `delete_homebrew_monster` function L244-254 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for campaign homebrew monster CRUD operations.
-  `homebrew_monster_to_json` function L127-141 — `(monster: &mimir_core::models::campaign::CampaignHomebrewMonster) -> Value` — MCP tools for campaign homebrew monster CRUD operations.

#### crates/mimir-mcp/src/tools/homebrew_spell.rs

- pub `list_homebrew_spells_tool` function L19-31 — `() -> Tool` — MCP tools for campaign homebrew spell CRUD operations.
- pub `get_homebrew_spell_tool` function L33-49 — `() -> Tool` — MCP tools for campaign homebrew spell CRUD operations.
- pub `create_homebrew_spell_tool` function L51-77 — `() -> Tool` — MCP tools for campaign homebrew spell CRUD operations.
- pub `update_homebrew_spell_tool` function L79-101 — `() -> Tool` — MCP tools for campaign homebrew spell CRUD operations.
- pub `delete_homebrew_spell_tool` function L103-119 — `() -> Tool` — MCP tools for campaign homebrew spell CRUD operations.
- pub `list_homebrew_spells` function L140-151 — `(ctx: &Arc<McpContext>, _args: Value) -> Result<Value, McpError>` — MCP tools for campaign homebrew spell CRUD operations.
- pub `get_homebrew_spell` function L153-163 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for campaign homebrew spell CRUD operations.
- pub `create_homebrew_spell` function L165-203 — `( ctx: &Arc<McpContext>, args: Value, ) -> Result<Value, McpError>` — MCP tools for campaign homebrew spell CRUD operations.
- pub `update_homebrew_spell` function L205-233 — `( ctx: &Arc<McpContext>, args: Value, ) -> Result<Value, McpError>` — MCP tools for campaign homebrew spell CRUD operations.
- pub `delete_homebrew_spell` function L235-245 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for campaign homebrew spell CRUD operations.
-  `homebrew_spell_to_json` function L125-138 — `(spell: &mimir_core::models::campaign::CampaignHomebrewSpell) -> Value` — MCP tools for campaign homebrew spell CRUD operations.

#### crates/mimir-mcp/src/tools/map.rs

- pub `create_map_tool` function L20-45 — `() -> Tool` — MCP tools for map and token placement management.
- pub `list_maps_tool` function L47-68 — `() -> Tool` — MCP tools for map and token placement management.
- pub `get_map_tool` function L70-86 — `() -> Tool` — MCP tools for map and token placement management.
- pub `update_map_tool` function L88-110 — `() -> Tool` — MCP tools for map and token placement management.
- pub `delete_map_tool` function L112-128 — `() -> Tool` — MCP tools for map and token placement management.
- pub `add_token_to_map_tool` function L130-158 — `() -> Tool` — MCP tools for map and token placement management.
- pub `list_tokens_on_map_tool` function L160-179 — `() -> Tool` — MCP tools for map and token placement management.
- pub `remove_token_tool` function L181-197 — `() -> Tool` — MCP tools for map and token placement management.
- pub `create_map` function L223-288 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for map and token placement management.
- pub `list_maps` function L290-326 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for map and token placement management.
- pub `get_map` function L328-380 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for map and token placement management.
- pub `update_map` function L382-424 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for map and token placement management.
- pub `delete_map` function L426-441 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for map and token placement management.
- pub `add_token_to_map` function L443-499 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for map and token placement management.
- pub `list_tokens_on_map` function L501-542 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for map and token placement management.
- pub `remove_token` function L544-558 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for map and token placement management.
-  `app_data_dir` function L204-209 — `(ctx: &Arc<McpContext>) -> std::path::PathBuf` — Get the app_data_dir from context (parent of assets_dir).
-  `parse_lighting_mode` function L211-221 — `(s: &str) -> Result<LightingMode, McpError>` — MCP tools for map and token placement management.

#### crates/mimir-mcp/src/tools/mapgen.rs

- pub `generate_map_tool` function L17-41 — `() -> Tool` — MCP tools for procedural Dungeondraft map generation.
- pub `list_map_presets_tool` function L43-57 — `() -> Tool` — MCP tools for procedural Dungeondraft map generation.
- pub `validate_map_config_tool` function L59-80 — `() -> Tool` — MCP tools for procedural Dungeondraft map generation.
- pub `generate_map` function L86-161 — `(args: Value) -> Result<Value, McpError>` — MCP tools for procedural Dungeondraft map generation.
- pub `list_map_presets` function L163-176 — `(_args: Value) -> Result<Value, McpError>` — MCP tools for procedural Dungeondraft map generation.
- pub `validate_map_config` function L178-205 — `(args: Value) -> Result<Value, McpError>` — MCP tools for procedural Dungeondraft map generation.

#### crates/mimir-mcp/src/tools/mod.rs

- pub `campaign` module L8 — `-` — Each module provides tool definitions and handlers for a category of operations.
- pub `catalog` module L9 — `-` — Each module provides tool definitions and handlers for a category of operations.
- pub `character` module L10 — `-` — Each module provides tool definitions and handlers for a category of operations.
- pub `document` module L11 — `-` — Each module provides tool definitions and handlers for a category of operations.
- pub `homebrew` module L12 — `-` — Each module provides tool definitions and handlers for a category of operations.
- pub `homebrew_monster` module L13 — `-` — Each module provides tool definitions and handlers for a category of operations.
- pub `homebrew_spell` module L14 — `-` — Each module provides tool definitions and handlers for a category of operations.
- pub `map` module L15 — `-` — Each module provides tool definitions and handlers for a category of operations.
- pub `mapgen` module L16 — `-` — Each module provides tool definitions and handlers for a category of operations.
- pub `module` module L17 — `-` — Each module provides tool definitions and handlers for a category of operations.
- pub `create_properties` function L23-41 — `( props: Vec<(&str, &str, &str)>, ) -> Option<HashMap<String, serde_json::Map<St...` — Create a properties map for tool input schema.

#### crates/mimir-mcp/src/tools/module.rs

- pub `create_module_tool` function L22-46 — `() -> Tool` — MCP tools for module management.
- pub `list_modules_tool` function L48-60 — `() -> Tool` — MCP tools for module management.
- pub `get_module_details_tool` function L62-81 — `() -> Tool` — MCP tools for module management.
- pub `add_monster_to_module_tool` function L83-110 — `() -> Tool` — MCP tools for module management.
- pub `add_item_to_module_tool` function L112-133 — `() -> Tool` — MCP tools for module management.
- pub `update_module_tool` function L135-155 — `() -> Tool` — MCP tools for module management.
- pub `remove_monster_from_module_tool` function L157-175 — `() -> Tool` — MCP tools for module management.
- pub `delete_module_tool` function L177-195 — `() -> Tool` — MCP tools for module management.
- pub `create_module` function L201-232 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for module management.
- pub `list_modules` function L234-259 — `(ctx: &Arc<McpContext>, _args: Value) -> Result<Value, McpError>` — MCP tools for module management.
- pub `get_module_details` function L261-321 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for module management.
- pub `add_monster_to_module` function L323-409 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for module management.
- pub `remove_monster_from_module` function L411-435 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for module management.
- pub `add_item_to_module` function L437-452 — `(_ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for module management.
- pub `update_module` function L454-482 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for module management.
- pub `delete_module` function L484-498 — `(ctx: &Arc<McpContext>, args: Value) -> Result<Value, McpError>` — MCP tools for module management.

### crates/mimir-print/src

> *Semantic summary to be generated by AI agent.*

#### crates/mimir-print/src/builder.rs

- pub `VirtualFileRegistry` struct L29-31 — `{ files: Arc<RwLock<HashMap<String, Vec<u8>>>> }` — Registry for virtual files that will be available to Typst
- pub `new` function L35-39 — `() -> Self` — Create a new empty registry
- pub `register` function L42-48 — `(&self, name: &str, data: Vec<u8>) -> String` — Register a file and return the virtual path to use in Typst
- pub `into_files` function L51-55 — `(self) -> HashMap<String, Vec<u8>>` — Get all registered files
- pub `RenderContext` struct L62-69 — `{ temp_dir: PathBuf, base_path: Option<PathBuf>, virtual_files: VirtualFileRegis...` — Context passed to Renderable implementations during rendering
- pub `new` function L73-79 — `(temp_dir: PathBuf) -> Self` — Create a new render context with a temporary directory
- pub `with_base_path` function L82-85 — `(mut self, path: PathBuf) -> Self` — Set the base path for resolving relative references
- pub `Renderable` interface L102-134 — `{ fn to_typst(), fn toc_title(), fn page_break_before(), fn page_margin() }` — Trait for anything that can be rendered to Typst markup
- pub `DocumentConfig` struct L138-149 — `{ include_toc: bool, include_title_page: bool, page_numbers: bool, margin: f32, ...` — Configuration for document assembly
- pub `DocumentBuilder` struct L167-174 — `{ title: String, sections: Vec<Box<dyn Renderable>>, config: DocumentConfig, con...` — Builder for assembling PDF documents from sections
- pub `new` function L178-193 — `(title: impl Into<String>) -> Self` — Create a new document builder with the given title
- pub `with_templates_root` function L196-199 — `(mut self, path: PathBuf) -> Self` — Set the templates root directory (for shared components)
- pub `with_toc` function L202-205 — `(mut self, include: bool) -> Self` — Enable or disable table of contents generation
- pub `with_title_page` function L208-211 — `(mut self, include: bool) -> Self` — Enable or disable title page
- pub `with_page_numbers` function L214-217 — `(mut self, include: bool) -> Self` — Enable or disable page numbers
- pub `with_margin` function L220-223 — `(mut self, margin: f32) -> Self` — Set page margins (in inches)
- pub `with_font_size` function L226-229 — `(mut self, size: f32) -> Self` — Set the base font size (in points)
- pub `with_context` function L232-235 — `(mut self, context: RenderContext) -> Self` — Set the render context (temp directory, base path, etc.)
- pub `with_temp_dir` function L238-241 — `(mut self, temp_dir: PathBuf) -> Self` — Set the temporary directory for intermediate files
- pub `with_base_path` function L244-247 — `(mut self, base_path: PathBuf) -> Self` — Set the base path for resolving relative references
- pub `append` function L252-255 — `(mut self, section: R) -> Self` — Append a section to the document
- pub `push` function L258-261 — `(&mut self, section: R) -> &mut Self` — Append a section to the document (mutable reference version)
- pub `is_empty` function L264-266 — `(&self) -> bool` — Check if the document has any sections
- pub `len` function L269-271 — `(&self) -> usize` — Get the number of sections in the document
- pub `to_pdf` function L413-489 — `(self) -> Result<Vec<u8>>` — Render the document to PDF bytes
- pub `escape_typst_string` function L493-500 — `(s: &str) -> String` — Escape special characters for Typst strings
-  `VirtualFileRegistry` type L33-56 — `= VirtualFileRegistry` — ```
-  `RenderContext` type L71-86 — `= RenderContext` — ```
-  `RenderContext` type L88-96 — `impl Default for RenderContext` — ```
-  `default` function L89-95 — `() -> Self` — ```
-  `toc_title` function L116-118 — `(&self) -> Option<String>` — Title for table of contents entry
-  `page_break_before` function L123-125 — `(&self) -> bool` — Whether this section should start on a new page
-  `page_margin` function L131-133 — `(&self) -> Option<f32>` — Custom page margin for this section (in inches)
-  `DocumentConfig` type L151-161 — `impl Default for DocumentConfig` — ```
-  `default` function L152-160 — `() -> Self` — ```
-  `DocumentBuilder` type L176-490 — `= DocumentBuilder` — ```
-  `build_typst` function L274-332 — `(&self) -> Result<String>` — Build the complete Typst document
-  `build_preamble` function L335-381 — `(&self) -> String` — Build the document preamble (page setup, fonts, shared imports)
-  `build_title_page` function L384-395 — `(&self) -> String` — Build the title page
-  `build_toc` function L398-407 — `(&self) -> String` — Build the table of contents
-  `tests` module L503-695 — `-` — ```
-  `TestSection` struct L507-510 — `{ content: String, title: Option<String> }` — Simple test section that renders static content
-  `TestSection` type L512-524 — `= TestSection` — ```
-  `new` function L513-518 — `(content: &str) -> Self` — ```
-  `with_title` function L520-523 — `(mut self, title: &str) -> Self` — ```
-  `TestSection` type L526-534 — `impl Renderable for TestSection` — ```
-  `to_typst` function L527-529 — `(&self, _ctx: &RenderContext) -> Result<String>` — ```
-  `toc_title` function L531-533 — `(&self) -> Option<String>` — ```
-  `test_document_builder_empty` function L537-541 — `()` — ```
-  `test_document_builder_append` function L544-551 — `()` — ```
-  `test_document_builder_config` function L554-565 — `()` — ```
-  `test_build_typst_simple` function L568-577 — `()` — ```
-  `test_build_typst_with_toc` function L580-589 — `()` — ```
-  `test_escape_typst_string` function L592-597 — `()` — ```
-  `test_escape_typst_string_backslash_and_quotes` function L600-602 — `()` — ```
-  `test_build_typst_multiple_sections_ordered` function L605-624 — `()` — ```
-  `test_build_typst_toc_with_multiple_titles` function L627-642 — `()` — ```
-  `test_build_typst_sections_without_titles` function L645-653 — `()` — ```
-  `test_build_typst_empty_builder` function L656-662 — `()` — ```
-  `test_document_builder_title_page` function L665-670 — `()` — ```
-  `test_build_typst_contains_page_setup` function L673-683 — `()` — ```
-  `test_build_typst_page_break_between_sections` function L686-694 — `()` — ```

#### crates/mimir-print/src/embedded_templates.rs

- pub `STYLES_TYP` variable L7-230 — `: &str` — Shared styles template (colors, typography, page setup)
- pub `COMPONENTS_TYP` variable L233-370 — `: &str` — Components template (ability scores, stat blocks, cards, layout helpers)
- pub `ICONS_TYP` variable L373-568 — `: &str` — Icons template (class icons, spell school icons, damage type icons)
- pub `get_embedded_template` function L572-582 — `(path: &str) -> Option<&'static str>` — Get embedded template content by path
-  `tests` module L585-613 — `-` — No external template files are needed.
-  `test_get_embedded_template` function L589-595 — `()` — No external template files are needed.
-  `test_styles_contains_colors` function L598-602 — `()` — No external template files are needed.
-  `test_components_imports_styles` function L605-607 — `()` — No external template files are needed.
-  `test_icons_imports_styles` function L610-612 — `()` — No external template files are needed.

#### crates/mimir-print/src/error.rs

- pub `PrintError` enum L7-39 — `TemplateNotFound | TemplateReadError | CompilationError | PdfError | InvalidData...` — Errors that can occur during print/PDF generation
- pub `Result` type L41 — `= std::result::Result<T, PrintError>` — Print-specific error types

#### crates/mimir-print/src/lib.rs

- pub `error` module L42 — `-` — ```
- pub `world` module L43 — `-` — ```
- pub `service` module L44 — `-` — ```
- pub `builder` module L45 — `-` — ```
- pub `markdown` module L46 — `-` — ```
- pub `map_renderer` module L47 — `-` — ```
- pub `sections` module L48 — `-` — ```
- pub `embedded_templates` module L49 — `-` — ```
- pub `PrintState` struct L67-72 — `{ templates_dir: PathBuf, assets_dir: PathBuf }` — State for print functionality, managed by Tauri.
- pub `new` function L76-81 — `(templates_dir: PathBuf, assets_dir: PathBuf) -> Self` — Create a new PrintState
-  `PrintState` type L74-82 — `= PrintState` — ```

#### crates/mimir-print/src/map_renderer.rs

- pub `load_image_from_file` function L23-61 — `(path: &Path) -> Result<Vec<u8>>` — Load image bytes from a file, handling UVTT files by extracting their embedded image
- pub `RenderToken` struct L65-73 — `{ name: String, x: f32, y: f32, size: String, color: Option<String>, token_type:...` — Token data for rendering on maps
- pub `RenderMap` struct L113-122 — `{ name: String, image_path: String, width_px: i32, height_px: i32, grid_type: St...` — Map data for rendering
- pub `has_grid` function L126-128 — `(&self) -> bool` — Check if this map has a grid configured
- pub `RenderedMap` struct L133-140 — `{ name: String, with_grid: Vec<u8>, with_tokens: Option<Vec<u8>> }` — Rendered map output
- pub `MapPrintOptions` struct L144-155 — `{ show_grid: bool, show_los_walls: bool, show_positions: bool, los_walls: Vec<Ve...` — Options for map print rendering
- pub `RenderedMapForPrint` struct L159-166 — `{ image_bytes: Vec<u8>, width_px: u32, height_px: u32 }` — Rendered map for printing
- pub `render_map_with_grid` function L182-205 — `( map: &RenderMap, base_path: &Path, ) -> Result<RgbaImage>` — Render a map with grid overlay
- pub `render_map` function L288-330 — `( map: &RenderMap, tokens: &[RenderToken], base_path: &Path, ) -> Result<Rendere...` — Render a map with optional tokens
- pub `render_map_for_print` function L378-436 — `( map: &RenderMap, tokens: &[RenderToken], _base_path: &Path, image_base64: &str...` — Render a map for print output with configurable options
-  `UvttImageExtractor` struct L18-20 — `{ image: String }` — Simple UVTT file structure for extracting image data
-  `RenderToken` type L75-109 — `= RenderToken` — Supports LOS wall rendering and position markers for printed battle maps.
-  `grid_squares` function L77-87 — `(&self) -> f32` — Get grid squares for this token size
-  `default_color` function L90-99 — `(&self) -> Rgba<u8>` — Get default color based on token type
-  `get_color` function L102-108 — `(&self) -> Rgba<u8>` — Parse hex color or use default
-  `RenderMap` type L124-129 — `= RenderMap` — Supports LOS wall rendering and position markers for printed battle maps.
-  `parse_hex_color` function L169-179 — `(hex: &str) -> Option<Rgba<u8>>` — Parse a hex color string to Rgba
-  `draw_grid` function L208-245 — `(img: &mut RgbaImage, map: &RenderMap)` — Draw grid lines on an image
-  `draw_tokens` function L248-281 — `(img: &mut RgbaImage, tokens: &[RenderToken], grid_size_px: i32)` — Draw tokens on an image
-  `draw_los_walls` function L335-371 — `(img: &mut RgbaImage, walls: &[Vec<(f64, f64)>], pixels_per_grid: u32)` — Draw LOS walls on an image
-  `tests` module L439-585 — `-` — Supports LOS wall rendering and position markers for printed battle maps.
-  `create_test_image_bytes` function L444-463 — `(width: u32, height: u32) -> Vec<u8>` — Create a minimal valid PNG image for testing
-  `create_test_image_base64` function L465-467 — `(width: u32, height: u32) -> String` — Supports LOS wall rendering and position markers for printed battle maps.
-  `sample_map` function L469-480 — `() -> RenderMap` — Supports LOS wall rendering and position markers for printed battle maps.
-  `sample_token` function L482-492 — `(name: &str, x: f32, y: f32, size: &str, token_type: &str) -> RenderToken` — Supports LOS wall rendering and position markers for printed battle maps.
-  `test_parse_hex_color` function L495-500 — `()` — Supports LOS wall rendering and position markers for printed battle maps.
-  `test_token_grid_squares` function L503-512 — `()` — Supports LOS wall rendering and position markers for printed battle maps.
-  `test_token_default_colors` function L515-521 — `()` — Supports LOS wall rendering and position markers for printed battle maps.
-  `test_render_map_has_grid` function L524-534 — `()` — Supports LOS wall rendering and position markers for printed battle maps.
-  `test_render_map_for_print_basic` function L537-549 — `()` — Supports LOS wall rendering and position markers for printed battle maps.
-  `test_render_map_for_print_with_grid` function L552-562 — `()` — Supports LOS wall rendering and position markers for printed battle maps.
-  `test_render_map_for_print_with_los_walls` function L565-584 — `()` — Supports LOS wall rendering and position markers for printed battle maps.

#### crates/mimir-print/src/markdown.rs

- pub `ParsedDocument` struct L15-20 — `{ frontmatter: Value, typst_content: String }` — A parsed campaign document with frontmatter and converted content.
- pub `parse_campaign_document` function L29-54 — `(markdown: &str) -> Result<ParsedDocument>` — Parse a campaign document from markdown with YAML frontmatter.
- pub `markdown_to_typst` function L66-404 — `(markdown: &str) -> String` — Convert markdown text to Typst markup.
-  `escape_for_typst_string` function L408-410 — `(text: &str) -> String` — Escape text for use inside a Typst string literal.
-  `tests` module L413-614 — `-` — and converting them to Typst markup for PDF rendering.
-  `test_parse_document_with_frontmatter` function L417-432 — `()` — and converting them to Typst markup for PDF rendering.
-  `test_parse_document_without_frontmatter` function L435-440 — `()` — and converting them to Typst markup for PDF rendering.
-  `test_markdown_headers` function L443-450 — `()` — and converting them to Typst markup for PDF rendering.
-  `test_markdown_bold_italic` function L453-459 — `()` — and converting them to Typst markup for PDF rendering.
-  `test_markdown_links` function L462-467 — `()` — and converting them to Typst markup for PDF rendering.
-  `test_markdown_lists` function L470-477 — `()` — and converting them to Typst markup for PDF rendering.
-  `test_markdown_code_block` function L480-485 — `()` — and converting them to Typst markup for PDF rendering.
-  `test_markdown_inline_code` function L488-492 — `()` — and converting them to Typst markup for PDF rendering.
-  `test_special_chars_in_string_literals` function L495-505 — `()` — and converting them to Typst markup for PDF rendering.
-  `test_block_comment_chars_safe_in_strings` function L508-518 — `()` — and converting them to Typst markup for PDF rendering.
-  `test_quotes_escaped_in_strings` function L521-530 — `()` — and converting them to Typst markup for PDF rendering.
-  `test_markdown_blockquote` function L533-537 — `()` — and converting them to Typst markup for PDF rendering.
-  `test_markdown_horizontal_rule` function L540-544 — `()` — and converting them to Typst markup for PDF rendering.
-  `test_markdown_table` function L547-560 — `()` — and converting them to Typst markup for PDF rendering.
-  `test_markdown_table_with_formatting` function L563-568 — `()` — and converting them to Typst markup for PDF rendering.
-  `test_markdown_strikethrough` function L571-575 — `()` — and converting them to Typst markup for PDF rendering.
-  `test_markdown_nested_list` function L578-584 — `()` — and converting them to Typst markup for PDF rendering.
-  `test_markdown_html_comment` function L587-592 — `()` — and converting them to Typst markup for PDF rendering.
-  `test_markdown_image` function L595-599 — `()` — and converting them to Typst markup for PDF rendering.
-  `test_empty_markdown` function L602-605 — `()` — and converting them to Typst markup for PDF rendering.
-  `test_backslash_escaped_in_string_literal` function L608-613 — `()` — and converting them to Typst markup for PDF rendering.

#### crates/mimir-print/src/service.rs

- pub `TemplateInfo` struct L14-23 — `{ id: String, name: String, category: String, description: Option<String> }` — Information about an available template
- pub `PrintService` struct L26-29 — `{ templates_root: PathBuf }` — Service for generating PDFs from Typst templates
- pub `new` function L36-38 — `(templates_root: PathBuf) -> Self` — Create a new PrintService
- pub `templates_root` function L41-43 — `(&self) -> &PathBuf` — Get the templates root directory
- pub `render_to_pdf` function L54-99 — `( &self, template_path: &str, data: serde_json::Value, ) -> Result<Vec<u8>>` — Render a template to PDF bytes
- pub `save_pdf` function L103-107 — `(&self, path: &PathBuf, pdf_bytes: &[u8]) -> Result<()>` — Save PDF bytes to a file
- pub `list_templates` function L111-165 — `(&self) -> Result<Vec<TemplateInfo>>` — List all available templates
- pub `template_exists` function L168-170 — `(&self, template_path: &str) -> bool` — Check if a template exists
-  `PrintService` type L31-171 — `= PrintService` — PrintService - Core service for PDF generation
-  `format_diagnostics` function L174-186 — `(diagnostics: &[SourceDiagnostic]) -> String` — Format Typst diagnostics into a readable error message
-  `titlecase` function L189-206 — `(s: &str) -> String` — Convert a string to title case
-  `tests` module L209-311 — `-` — PrintService - Core service for PDF generation
-  `setup_test_template` function L214-231 — `(dir: &TempDir) -> PathBuf` — PrintService - Core service for PDF generation
-  `test_service_creation` function L234-238 — `()` — PrintService - Core service for PDF generation
-  `test_list_templates_empty` function L241-246 — `()` — PrintService - Core service for PDF generation
-  `test_list_templates` function L249-259 — `()` — PrintService - Core service for PDF generation
-  `test_template_exists` function L262-269 — `()` — PrintService - Core service for PDF generation
-  `test_titlecase` function L272-276 — `()` — PrintService - Core service for PDF generation
-  `test_render_to_pdf` function L279-310 — `()` — PrintService - Core service for PDF generation

#### crates/mimir-print/src/world.rs

- pub `MimirTypstWorld` struct L65-78 — `{ root_dir: PathBuf, main_file: FileId, main_content: Option<String>, sources: R...` — Mimir's custom Typst World implementation
- pub `from_content` function L85-95 — `(content: String, root_dir: PathBuf) -> Self` — Create a world from in-memory Typst content
- pub `from_content_with_files` function L100-118 — `( content: String, root_dir: PathBuf, files: HashMap<String, Vec<u8>>, ) -> Self` — Create a world from in-memory Typst content with pre-registered virtual files
- pub `register_file` function L124-130 — `(&self, name: &str, data: Vec<u8>) -> String` — Register an in-memory file that can be accessed via a virtual path
- pub `from_template` function L135-165 — `( templates_root: PathBuf, template_path: &str, data: serde_json::Value, ) -> Re...` — Create a world from a template file with JSON data injection
-  `FONT_DB` variable L25 — `: OnceLock<(LazyHash<FontBook>, Vec<Font>)>` — Static font database loaded once
-  `init_fonts` function L28-57 — `() -> (LazyHash<FontBook>, Vec<Font>)` — Initialize the font database from system fonts
-  `get_fonts` function L60-62 — `() -> &'static (LazyHash<FontBook>, Vec<Font>)` — Get or initialize the font database
-  `MimirTypstWorld` type L80-202 — `= MimirTypstWorld` — for the main document while resolving other resources from the filesystem.
-  `resolve_path` function L168-180 — `(&self, id: FileId) -> PathBuf` — Resolve a file path to actual filesystem path
-  `read_source_from_disk` function L183-201 — `(&self, id: FileId) -> FileResult<TypstSource>` — Read and cache a source file from disk
-  `MimirTypstWorld` type L204-286 — `= MimirTypstWorld` — for the main document while resolving other resources from the filesystem.
-  `library` function L205-207 — `(&self) -> &LazyHash<Library>` — for the main document while resolving other resources from the filesystem.
-  `book` function L209-212 — `(&self) -> &LazyHash<FontBook>` — for the main document while resolving other resources from the filesystem.
-  `main` function L214-216 — `(&self) -> FileId` — for the main document while resolving other resources from the filesystem.
-  `source` function L218-236 — `(&self, id: FileId) -> FileResult<TypstSource>` — for the main document while resolving other resources from the filesystem.
-  `file` function L238-262 — `(&self, id: FileId) -> FileResult<Bytes>` — for the main document while resolving other resources from the filesystem.
-  `font` function L264-267 — `(&self, index: usize) -> Option<Font>` — for the main document while resolving other resources from the filesystem.
-  `today` function L269-281 — `(&self, offset: Option<i64>) -> Option<Datetime>` — for the main document while resolving other resources from the filesystem.
-  `packages` function L283-285 — `(&self) -> &[(PackageSpec, Option<ecow::EcoString>)]` — for the main document while resolving other resources from the filesystem.
-  `tests` module L289-336 — `-` — for the main document while resolving other resources from the filesystem.
-  `test_from_content` function L295-304 — `()` — for the main document while resolving other resources from the filesystem.
-  `test_from_template` function L307-322 — `()` — for the main document while resolving other resources from the filesystem.
-  `test_template_not_found` function L325-335 — `()` — for the main document while resolving other resources from the filesystem.

### crates/mimir-print/src/sections

> *Semantic summary to be generated by AI agent.*

#### crates/mimir-print/src/sections/card_utils.rs

- pub `SMALL_CARD_DESC_BUDGET` variable L13 — `: usize` — Character budget for the description area on a 2.5" x 3.25" card.
- pub `CardTextSplit` struct L17-24 — `{ front: String, back: String, is_foldable: bool }` — Result of splitting card text for foldable layout
- pub `split_text_natural` function L34-82 — `(text: &str, budget: usize) -> CardTextSplit` — Split text at a natural boundary near `budget` characters.
- pub `flatten_entries` function L88-124 — `(entries: &[Value]) -> String` — Recursively flatten 5etools entry arrays into plain text.
- pub `escape_typst` function L127-140 — `(s: &str) -> String` — Escape special Typst characters in content text
-  `tests` module L143-272 — `-` — used by equipment cards, spell cards, and potentially other card types.
-  `test_short_text_no_split` function L150-155 — `()` — used by equipment cards, spell cards, and potentially other card types.
-  `test_split_on_paragraph` function L158-164 — `()` — used by equipment cards, spell cards, and potentially other card types.
-  `test_split_on_sentence` function L167-173 — `()` — used by equipment cards, spell cards, and potentially other card types.
-  `test_split_on_comma` function L176-184 — `()` — used by equipment cards, spell cards, and potentially other card types.
-  `test_split_on_word_boundary` function L187-193 — `()` — used by equipment cards, spell cards, and potentially other card types.
-  `test_split_on_list_item` function L196-204 — `()` — used by equipment cards, spell cards, and potentially other card types.
-  `test_flatten_plain_strings` function L209-212 — `()` — used by equipment cards, spell cards, and potentially other card types.
-  `test_flatten_list_items` function L215-221 — `()` — used by equipment cards, spell cards, and potentially other card types.
-  `test_flatten_named_sub_entries` function L224-231 — `()` — used by equipment cards, spell cards, and potentially other card types.
-  `test_flatten_table_caption` function L234-240 — `()` — used by equipment cards, spell cards, and potentially other card types.
-  `test_flatten_nested` function L243-256 — `()` — used by equipment cards, spell cards, and potentially other card types.
-  `test_escape_5etools_tags` function L261-266 — `()` — used by equipment cards, spell cards, and potentially other card types.
-  `test_escape_angle_brackets` function L269-271 — `()` — used by equipment cards, spell cards, and potentially other card types.

#### crates/mimir-print/src/sections/character.rs

- pub `CharacterData` struct L13-72 — `{ name: String, player_name: Option<String>, is_npc: bool, race_name: Option<Str...` — Character data for PDF rendering
- pub `ClassInfo` struct L76-82 — `{ class_name: String, class_source: String, level: i32, subclass_name: Option<St...` — Class level information
- pub `InventoryItem` struct L86-106 — `{ name: String, quantity: i32, equipped: bool, attuned: bool, item_type: Option<...` — Inventory item for display
- pub `Proficiencies` struct L110-117 — `{ skills: Vec<ProficiencyEntry>, saves: Vec<String>, languages: Vec<String>, arm...` — Proficiencies grouped by type
- pub `ProficiencyEntry` struct L121-124 — `{ name: String, expertise: bool }` — A proficiency entry that may have expertise
- pub `CharacterSection` struct L127-129 — `{ character: CharacterData }` — Character sheet section
- pub `new` function L133-135 — `(character: CharacterData) -> Self` — Create a new character section
-  `CharacterSection` type L131-196 — `= CharacterSection` — Shows basic info, ability scores, classes, and roleplay elements.
-  `total_level` function L138-140 — `(&self) -> i32` — Calculate total character level
-  `prof_bonus` function L143-150 — `(&self) -> i32` — Calculate proficiency bonus from total level
-  `class_string` function L153-169 — `(&self) -> String` — Format class string (e.g., "Fighter 5 / Rogue 3")
-  `modifier` function L172-174 — `(score: i32) -> i32` — Calculate ability modifier
-  `modifier_str` function L177-184 — `(score: i32) -> String` — Format modifier as string with sign
-  `total_gold` function L188-194 — `(&self) -> f64` — Calculate total gold value
-  `CharacterSection` type L198-790 — `impl Renderable for CharacterSection` — Shows basic info, ability scores, classes, and roleplay elements.
-  `to_typst` function L199-781 — `(&self, _ctx: &RenderContext) -> Result<String>` — Shows basic info, ability scores, classes, and roleplay elements.
-  `primary_box` function L212-227 — `(title: &str, content: &str, markup: bool) -> String` — Shows basic info, ability scores, classes, and roleplay elements.
-  `secondary_box` function L230-245 — `(title: &str, content: &str, markup: bool) -> String` — Shows basic info, ability scores, classes, and roleplay elements.
-  `tertiary_label` function L248-262 — `(title: &str, content: &str, markup: bool) -> String` — Shows basic info, ability scores, classes, and roleplay elements.
-  `toc_title` function L783-785 — `(&self) -> Option<String>` — Shows basic info, ability scores, classes, and roleplay elements.
-  `page_break_before` function L787-789 — `(&self) -> bool` — Shows basic info, ability scores, classes, and roleplay elements.
-  `tests` module L793-1272 — `-` — Shows basic info, ability scores, classes, and roleplay elements.
-  `test_character` function L796-882 — `() -> CharacterData` — Shows basic info, ability scores, classes, and roleplay elements.
-  `test_spellcaster` function L884-963 — `() -> CharacterData` — Shows basic info, ability scores, classes, and roleplay elements.
-  `test_total_level` function L966-970 — `()` — Shows basic info, ability scores, classes, and roleplay elements.
-  `test_prof_bonus` function L973-977 — `()` — Shows basic info, ability scores, classes, and roleplay elements.
-  `test_class_string` function L980-984 — `()` — Shows basic info, ability scores, classes, and roleplay elements.
-  `test_modifier` function L987-992 — `()` — Shows basic info, ability scores, classes, and roleplay elements.
-  `test_total_gold` function L995-999 — `()` — Shows basic info, ability scores, classes, and roleplay elements.
-  `test_toc_title` function L1002-1006 — `()` — Shows basic info, ability scores, classes, and roleplay elements.
-  `test_spellcaster_renders_spell_slots` function L1009-1019 — `()` — Shows basic info, ability scores, classes, and roleplay elements.
-  `test_non_caster_no_spell_section` function L1022-1029 — `()` — Shows basic info, ability scores, classes, and roleplay elements.
-  `test_all_18_skills_rendered` function L1032-1043 — `()` — Shows basic info, ability scores, classes, and roleplay elements.
-  `test_actions_reference` function L1046-1055 — `()` — Shows basic info, ability scores, classes, and roleplay elements.
-  `test_npc_renders_badge_and_info` function L1058-1076 — `()` — Shows basic info, ability scores, classes, and roleplay elements.
-  `test_weapon_attack_bonus_str_based` function L1079-1088 — `()` — Shows basic info, ability scores, classes, and roleplay elements.
-  `test_finesse_weapon_uses_better_mod` function L1091-1110 — `()` — Shows basic info, ability scores, classes, and roleplay elements.
-  `test_ranged_weapon_uses_dex` function L1113-1132 — `()` — Shows basic info, ability scores, classes, and roleplay elements.
-  `test_expertise_renders_diamond` function L1135-1146 — `()` — Shows basic info, ability scores, classes, and roleplay elements.
-  `test_multiclass_class_string_with_subclass` function L1149-1154 — `()` — Shows basic info, ability scores, classes, and roleplay elements.
-  `test_no_class_string` function L1157-1162 — `()` — Shows basic info, ability scores, classes, and roleplay elements.
-  `test_currency_renders_all_denominations` function L1165-1176 — `()` — Shows basic info, ability scores, classes, and roleplay elements.
-  `test_inventory_renders_equipped_and_attuned` function L1179-1188 — `()` — Shows basic info, ability scores, classes, and roleplay elements.
-  `test_personality_renders_all_four` function L1191-1203 — `()` — Shows basic info, ability scores, classes, and roleplay elements.
-  `test_proficiency_bonus_by_level` function L1206-1230 — `()` — Shows basic info, ability scores, classes, and roleplay elements.
-  `test_passive_perception_with_proficiency` function L1233-1242 — `()` — Shows basic info, ability scores, classes, and roleplay elements.
-  `test_spellcaster_attack_bonus_and_dc` function L1245-1253 — `()` — Shows basic info, ability scores, classes, and roleplay elements.
-  `test_modifier_edge_cases` function L1256-1264 — `()` — Shows basic info, ability scores, classes, and roleplay elements.
-  `test_modifier_str_formatting` function L1267-1271 — `()` — Shows basic info, ability scores, classes, and roleplay elements.

#### crates/mimir-print/src/sections/character_battle_card.rs

- pub `CharacterBattleCardSection` struct L12-17 — `{ characters: Vec<CharacterData>, show_cut_lines: bool }` — Character battle cards - half-page combat reference cards (2x2 layout)
- pub `new` function L21-26 — `(characters: Vec<CharacterData>) -> Self` — Create a new character battle cards section
- pub `from_single` function L29-31 — `(character: CharacterData) -> Self` — Create from a single character
- pub `with_cut_lines` function L34-37 — `(mut self, show: bool) -> Self` — Set whether to show cut lines
-  `CharacterBattleCardSection` type L19-350 — `= CharacterBattleCardSection` — Works for both PCs and NPCs - shows AC, HP, speed, attacks, saves, skills, etc.
-  `modifier` function L40-42 — `(score: i32) -> i32` — Calculate ability modifier
-  `modifier_str` function L45-52 — `(score: i32) -> String` — Format modifier as string with sign
-  `total_level` function L55-57 — `(character: &CharacterData) -> i32` — Calculate total character level
-  `prof_bonus` function L60-73 — `(character: &CharacterData) -> i32` — Calculate proficiency bonus from total level
-  `class_string` function L76-96 — `(character: &CharacterData) -> String` — Build class string
-  `estimate_hp` function L99-104 — `(character: &CharacterData) -> i32` — Calculate HP (estimate from level and CON if not stored)
-  `estimate_ac` function L107-131 — `(character: &CharacterData) -> i32` — Estimate AC (10 + DEX mod as base)
-  `render_card` function L134-288 — `(character: &CharacterData) -> String` — Render a single character battle card (half-page format)
-  `get_attacks` function L291-349 — `(character: &CharacterData) -> String` — Get attacks from equipped weapons
-  `CharacterBattleCardSection` type L352-423 — `impl Renderable for CharacterBattleCardSection` — Works for both PCs and NPCs - shows AC, HP, speed, attacks, saves, skills, etc.
-  `to_typst` function L353-408 — `(&self, _ctx: &RenderContext) -> Result<String>` — Works for both PCs and NPCs - shows AC, HP, speed, attacks, saves, skills, etc.
-  `toc_title` function L410-418 — `(&self) -> Option<String>` — Works for both PCs and NPCs - shows AC, HP, speed, attacks, saves, skills, etc.
-  `page_break_before` function L420-422 — `(&self) -> bool` — Works for both PCs and NPCs - shows AC, HP, speed, attacks, saves, skills, etc.
-  `render_hp_tracker` function L429-435 — `(hp: i32) -> String` — Render HP tracker based on HP value
-  `render_individual_boxes` function L439-454 — `(hp: i32) -> String` — Render individual HP boxes (1-20 HP)
-  `render_fives_and_ones` function L457-484 — `(hp: i32) -> String` — Render 5-HP boxes plus 1-HP boxes (for 21-100 HP)
-  `render_tens_and_ones` function L487-514 — `(hp: i32) -> String` — Render 10-HP boxes plus 1-HP boxes (for 101+ HP)
-  `tests` module L517-729 — `-` — Works for both PCs and NPCs - shows AC, HP, speed, attacks, saves, skills, etc.
-  `test_character` function L521-598 — `() -> CharacterData` — Works for both PCs and NPCs - shows AC, HP, speed, attacks, saves, skills, etc.
-  `test_modifier` function L601-605 — `()` — Works for both PCs and NPCs - shows AC, HP, speed, attacks, saves, skills, etc.
-  `test_battle_card_empty` function L608-611 — `()` — Works for both PCs and NPCs - shows AC, HP, speed, attacks, saves, skills, etc.
-  `test_battle_card_single` function L614-621 — `()` — Works for both PCs and NPCs - shows AC, HP, speed, attacks, saves, skills, etc.
-  `test_battle_card_renders` function L624-636 — `()` — Works for both PCs and NPCs - shows AC, HP, speed, attacks, saves, skills, etc.
-  `test_npc_card_colors` function L639-650 — `()` — Works for both PCs and NPCs - shows AC, HP, speed, attacks, saves, skills, etc.
-  `test_battle_card_shows_core_stats` function L653-665 — `()` — Works for both PCs and NPCs - shows AC, HP, speed, attacks, saves, skills, etc.
-  `test_battle_card_shows_equipped_weapons` function L668-675 — `()` — Works for both PCs and NPCs - shows AC, HP, speed, attacks, saves, skills, etc.
-  `test_battle_card_class_info` function L678-686 — `()` — Works for both PCs and NPCs - shows AC, HP, speed, attacks, saves, skills, etc.
-  `test_battle_card_multiple_characters` function L689-699 — `()` — Works for both PCs and NPCs - shows AC, HP, speed, attacks, saves, skills, etc.
-  `test_battle_card_ability_modifiers` function L702-713 — `()` — Works for both PCs and NPCs - shows AC, HP, speed, attacks, saves, skills, etc.
-  `test_battle_card_footer_pc_vs_npc` function L716-728 — `()` — Works for both PCs and NPCs - shows AC, HP, speed, attacks, saves, skills, etc.

#### crates/mimir-print/src/sections/equipment_cards.rs

- pub `EquipmentCardsSection` struct L14-19 — `{ items: Vec<Value>, show_cut_lines: bool }` — Equipment cards section - generates multi-up equipment cards for printing
- pub `new` function L23-28 — `(items: Vec<Value>) -> Self` — Create a new equipment cards section
- pub `from_json` function L31-37 — `(items: Value) -> Self` — Create from a JSON value (expects array)
- pub `with_cut_lines` function L40-43 — `(mut self, show: bool) -> Self` — Set whether to show cut lines
- pub `is_card_worthy` function L545-613 — `(item: &Value) -> bool` — Check if an item is "card-worthy" based on type, rarity, and content
-  `EquipmentCardsSection` type L21-386 — `= EquipmentCardsSection` — equipment icons to distinguish from spell cards.
-  `get_icon` function L46-66 — `(item_type: &str) -> &'static str` — Get equipment icon based on item type
-  `get_type_name` function L69-93 — `(item_type: &str) -> &'static str` — Get human-readable type name
-  `format_rarity` function L96-107 — `(rarity: &str) -> &str` — Format rarity for display
-  `extract_fields` function L110-236 — `(item: &Value) -> CardFields` — Extract common fields from an item for card rendering
-  `render_cards` function L239-379 — `(item: &Value) -> (String, Option<String>)` — Render the front card for an item.
-  `render_card` function L383-385 — `(item: &Value) -> String` — Render a single equipment card (front only, used by tests)
-  `EquipmentCardsSection` type L388-488 — `impl Renderable for EquipmentCardsSection` — equipment icons to distinguish from spell cards.
-  `to_typst` function L389-475 — `(&self, _ctx: &RenderContext) -> Result<String>` — equipment icons to distinguish from spell cards.
-  `toc_title` function L477-483 — `(&self) -> Option<String>` — equipment icons to distinguish from spell cards.
-  `page_break_before` function L485-487 — `(&self) -> bool` — equipment icons to distinguish from spell cards.
-  `CardFields` struct L491-503 — `{ name: String, source: String, icon_type: &'static str, type_name: &'static str...` — Extracted fields for equipment card rendering
-  `format_damage_type` function L506-523 — `(dt: &str) -> &str` — Format damage type abbreviation to full name
-  `format_property` function L526-542 — `(prop: &str) -> &'static str` — Format property abbreviation to full name
-  `tests` module L616-1106 — `-` — equipment icons to distinguish from spell cards.
-  `test_equipment_cards_empty` function L621-624 — `()` — equipment icons to distinguish from spell cards.
-  `test_equipment_cards_with_items` function L627-637 — `()` — equipment icons to distinguish from spell cards.
-  `test_is_card_worthy_weapon` function L640-654 — `()` — equipment icons to distinguish from spell cards.
-  `test_is_card_worthy_magic_item` function L657-664 — `()` — equipment icons to distinguish from spell cards.
-  `test_is_card_worthy_special_ammo` function L667-682 — `()` — equipment icons to distinguish from spell cards.
-  `test_is_card_worthy_with_notes` function L685-693 — `()` — equipment icons to distinguish from spell cards.
-  `test_is_card_worthy_mundane` function L696-703 — `()` — equipment icons to distinguish from spell cards.
-  `test_format_damage_type` function L706-711 — `()` — equipment icons to distinguish from spell cards.
-  `test_format_property` function L714-718 — `()` — equipment icons to distinguish from spell cards.
-  `test_get_icon` function L721-727 — `()` — equipment icons to distinguish from spell cards.
-  `test_get_icon_all_types` function L730-741 — `()` — equipment icons to distinguish from spell cards.
-  `test_get_type_name_all` function L744-760 — `()` — equipment icons to distinguish from spell cards.
-  `test_format_rarity` function L763-773 — `()` — equipment icons to distinguish from spell cards.
-  `test_render_card_weapon_with_damage` function L776-795 — `()` — equipment icons to distinguish from spell cards.
-  `test_render_card_armor_with_ac` function L798-813 — `()` — equipment icons to distinguish from spell cards.
-  `test_render_card_magic_item_with_rarity` function L816-833 — `()` — equipment icons to distinguish from spell cards.
-  `test_render_card_ranged_weapon_with_range` function L836-857 — `()` — equipment icons to distinguish from spell cards.
-  `test_render_card_attunement_with_requirement` function L860-870 — `()` — equipment icons to distinguish from spell cards.
-  `test_render_card_missing_type_defaults_to_gear` function L873-882 — `()` — equipment icons to distinguish from spell cards.
-  `test_render_card_short_description_fits` function L885-895 — `()` — equipment icons to distinguish from spell cards.
-  `test_render_card_long_description_foldable` function L898-915 — `()` — equipment icons to distinguish from spell cards.
-  `test_render_card_notes_as_description` function L918-927 — `()` — equipment icons to distinguish from spell cards.
-  `test_to_typst_grid_layout` function L930-941 — `()` — equipment icons to distinguish from spell cards.
-  `test_to_typst_cut_lines` function L944-951 — `()` — equipment icons to distinguish from spell cards.
-  `test_to_typst_no_cut_lines` function L954-961 — `()` — equipment icons to distinguish from spell cards.
-  `test_to_typst_empty_returns_comment` function L964-970 — `()` — equipment icons to distinguish from spell cards.
-  `test_to_typst_page_break_after_nine` function L973-982 — `()` — equipment icons to distinguish from spell cards.
-  `test_to_typst_icon_definitions` function L985-996 — `()` — equipment icons to distinguish from spell cards.
-  `test_format_damage_type_all` function L999-1010 — `()` — equipment icons to distinguish from spell cards.
-  `test_format_property_all` function L1013-1024 — `()` — equipment icons to distinguish from spell cards.
-  `test_is_card_worthy_attunement` function L1027-1035 — `()` — equipment icons to distinguish from spell cards.
-  `test_is_card_worthy_entries` function L1038-1046 — `()` — equipment icons to distinguish from spell cards.
-  `test_is_card_worthy_pipe_type` function L1049-1057 — `()` — equipment icons to distinguish from spell cards.
-  `test_page_break_before` function L1060-1063 — `()` — equipment icons to distinguish from spell cards.
-  `test_from_json` function L1066-1073 — `()` — equipment icons to distinguish from spell cards.
-  `test_from_json_non_array` function L1076-1080 — `()` — equipment icons to distinguish from spell cards.
-  `test_render_card_damage_single_no_type` function L1083-1092 — `()` — equipment icons to distinguish from spell cards.
-  `test_render_card_no_damage` function L1095-1105 — `()` — equipment icons to distinguish from spell cards.

#### crates/mimir-print/src/sections/map.rs

- pub `MapPreview` struct L18-21 — `{ name: String, source: MapPreviewSource }` — Map preview section - renders a map fit to a single page
- pub `new` function L37-51 — `(map: RenderMap, tokens: Vec<RenderToken>, base_path: PathBuf) -> Self` — Create a new map preview section from map data
- pub `from_rendered` function L54-59 — `(name: String, image_bytes: Vec<u8>) -> Self` — Create from pre-rendered image bytes
- pub `with_options` function L62-71 — `(mut self, options: MapPrintOptions) -> Self` — Set rendering options (only applies to FromMap source)
- pub `TileData` struct L128-141 — `{ image_bytes: Vec<u8>, label: String, row: u32, col: u32, width_px: u32, height...` — Pre-sliced tile data for tiled map printing
- pub `TiledMapSection` struct L144-149 — `{ name: String, source: TiledMapSource, margin: f32 }` — Tiled map section - renders a map at true scale across multiple pages
- pub `new` function L178-194 — `(map: RenderMap, tokens: Vec<RenderToken>, base_path: PathBuf) -> Self` — Create a new tiled map section from map data
- pub `from_tiles` function L197-207 — `(name: String, tiles: Vec<TileData>, tiles_x: u32, tiles_y: u32) -> Self` — Create from pre-sliced tile data
- pub `from_rendered` function L213-222 — `(name: String, image_bytes: Vec<u8>, grid_size_px: i32) -> Self` — Create from pre-rendered image bytes with grid information
- pub `with_margin` function L225-228 — `(mut self, margin: f32) -> Self` — Set custom margin (in inches)
- pub `with_options` function L231-240 — `(mut self, options: MapPrintOptions) -> Self` — Set rendering options (only applies to FromMap source)
-  `MapPreviewSource` enum L23-33 — `FromMap | PreRendered` — 2.
-  `MapPreview` type L35-72 — `= MapPreview` — 2.
-  `MapPreview` type L74-124 — `impl Renderable for MapPreview` — 2.
-  `to_typst` function L75-118 — `(&self, ctx: &RenderContext) -> Result<String>` — 2.
-  `toc_title` function L120-123 — `(&self) -> Option<String>` — 2.
-  `TiledMapSource` enum L151-171 — `FromMap | PreRendered | PreSliced` — 2.
-  `TILED_MAP_DEFAULT_MARGIN` variable L174 — `: f32` — Default margin for tiled maps (small for maximum drawing area)
-  `TiledMapSection` type L176-283 — `= TiledMapSection` — 2.
-  `render_assembly_guide` function L243-282 — `(&self, cols: u32, rows: u32) -> String` — Render assembly guide page
-  `TiledMapSection` type L285-522 — `impl Renderable for TiledMapSection` — 2.
-  `to_typst` function L286-509 — `(&self, ctx: &RenderContext) -> Result<String>` — 2.
-  `toc_title` function L511-513 — `(&self) -> Option<String>` — 2.
-  `page_break_before` function L515-517 — `(&self) -> bool` — 2.
-  `page_margin` function L519-521 — `(&self) -> Option<f32>` — 2.
-  `sanitize_filename` function L525-529 — `(name: &str) -> String` — Sanitize a filename for use in paths
-  `escape_typst_string` function L532-539 — `(s: &str) -> String` — Escape special characters for Typst strings
-  `tests` module L542-732 — `-` — 2.
-  `test_map` function L546-557 — `() -> RenderMap` — 2.
-  `test_sanitize_filename` function L560-564 — `()` — 2.
-  `test_map_preview_toc_title` function L567-573 — `()` — 2.
-  `create_test_tile_image` function L576-586 — `() -> Vec<u8>` — Create a minimal PNG image for testing
-  `test_escape_typst_string` function L589-594 — `()` — 2.
-  `test_tiled_map_toc_title` function L597-600 — `()` — 2.
-  `test_tiled_map_page_break_before` function L603-606 — `()` — 2.
-  `test_tiled_map_page_margin` function L609-612 — `()` — 2.
-  `test_tiled_map_custom_margin` function L615-619 — `()` — 2.
-  `test_assembly_guide_labels` function L622-637 — `()` — 2.
-  `test_map_preview_from_rendered` function L640-644 — `()` — 2.
-  `test_tiled_map_from_rendered` function L647-669 — `()` — 2.
-  `test_tiled_map_one_page_per_tile` function L672-731 — `()` — 2.

#### crates/mimir-print/src/sections/markdown.rs

- pub `MarkdownSection` struct L10-17 — `{ title: Option<String>, doc_type: Option<String>, typst_content: String }` — A markdown document section with optional YAML frontmatter
- pub `from_markdown` function L21-41 — `(markdown: &str) -> Result<Self>` — Create from raw markdown string (with optional YAML frontmatter)
- pub `from_file` function L44-52 — `(path: &Path) -> Result<Self>` — Create from a markdown file path
- pub `from_content` function L55-61 — `(content: &str, title: Option<&str>) -> Self` — Create from raw markdown content without frontmatter
- pub `with_title` function L64-67 — `(mut self, title: &str) -> Self` — Set an explicit title (overrides frontmatter)
- pub `doc_type` function L70-72 — `(&self) -> Option<&str>` — Get the document type (from frontmatter)
-  `MarkdownSection` type L19-73 — `= MarkdownSection` — Markdown document section
-  `MarkdownSection` type L75-83 — `impl Renderable for MarkdownSection` — Markdown document section
-  `to_typst` function L76-78 — `(&self, _ctx: &RenderContext) -> Result<String>` — Markdown document section
-  `toc_title` function L80-82 — `(&self) -> Option<String>` — Markdown document section
-  `tests` module L86-182 — `-` — Markdown document section
-  `test_from_markdown_with_frontmatter` function L90-104 — `()` — Markdown document section
-  `test_from_content` function L107-111 — `()` — Markdown document section
-  `test_toc_title` function L114-117 — `()` — Markdown document section
-  `test_toc_title_none_without_title` function L120-123 — `()` — Markdown document section
-  `test_with_title_overrides_frontmatter` function L126-130 — `()` — Markdown document section
-  `test_doc_type_extracted` function L133-137 — `()` — Markdown document section
-  `test_doc_type_none_when_missing` function L140-144 — `()` — Markdown document section
-  `test_to_typst_returns_content` function L147-152 — `()` — Markdown document section
-  `test_markdown_with_bold_italic` function L155-162 — `()` — Markdown document section
-  `test_markdown_with_list` function L165-174 — `()` — Markdown document section
-  `test_page_break_before_default` function L177-181 — `()` — Markdown document section

#### crates/mimir-print/src/sections/mod.rs

- pub `card_utils` module L6 — `-` — Each section type implements `Renderable` to produce Typst markup
- pub `character` module L7 — `-` — that can be assembled into a complete PDF document.
- pub `character_battle_card` module L8 — `-` — that can be assembled into a complete PDF document.
- pub `equipment_cards` module L9 — `-` — that can be assembled into a complete PDF document.
- pub `map` module L10 — `-` — that can be assembled into a complete PDF document.
- pub `markdown` module L11 — `-` — that can be assembled into a complete PDF document.
- pub `monster_cards` module L12 — `-` — that can be assembled into a complete PDF document.
- pub `spell_cards` module L13 — `-` — that can be assembled into a complete PDF document.
- pub `token_cutouts` module L14 — `-` — that can be assembled into a complete PDF document.
- pub `trap_cards` module L15 — `-` — that can be assembled into a complete PDF document.

#### crates/mimir-print/src/sections/monster_cards.rs

- pub `MonsterCardSection` struct L12-17 — `{ monsters: Vec<Value>, show_cut_lines: bool }` — Monster cards section - generates half-page monster reference cards (2x2 layout)
- pub `new` function L21-26 — `(monsters: Vec<Value>) -> Self` — Create a new monster cards section
- pub `from_json` function L29-35 — `(monsters: Value) -> Self` — Create from a JSON value (expects array)
- pub `with_cut_lines` function L38-41 — `(mut self, show: bool) -> Self` — Set whether to show cut lines
-  `MonsterCardSection` type L19-342 — `= MonsterCardSection` — Shows full stat blocks including attacks, resistances, and abilities.
-  `render_card` function L44-276 — `(monster: &Value) -> String` — Render a single monster card (half-page format)
-  `render_back_card` function L279-341 — `(monster: &Value) -> Option<String>` — Render the back card for foldable monsters
-  `MonsterCardSection` type L344-449 — `impl Renderable for MonsterCardSection` — Shows full stat blocks including attacks, resistances, and abilities.
-  `to_typst` function L345-440 — `(&self, _ctx: &RenderContext) -> Result<String>` — Shows full stat blocks including attacks, resistances, and abilities.
-  `toc_title` function L442-448 — `(&self) -> Option<String>` — Shows full stat blocks including attacks, resistances, and abilities.
-  `extract_creature_type` function L453-470 — `(monster: &Value) -> String` — Shows full stat blocks including attacks, resistances, and abilities.
-  `extract_alignment` function L472-499 — `(monster: &Value) -> String` — Shows full stat blocks including attacks, resistances, and abilities.
-  `extract_ac_full` function L501-526 — `(monster: &Value) -> String` — Shows full stat blocks including attacks, resistances, and abilities.
-  `extract_hp_full` function L528-546 — `(monster: &Value) -> String` — Shows full stat blocks including attacks, resistances, and abilities.
-  `extract_hp_average` function L549-562 — `(monster: &Value) -> i64` — Extract just the numeric HP value for tracker calculations
-  `render_hp_tracker` function L567-573 — `(hp: i64) -> String` — Render HP tracker based on HP value
-  `render_individual_boxes` function L577-589 — `(hp: i64) -> String` — Render individual 1-HP boxes (for 1-20 HP)
-  `render_fives_and_ones` function L592-619 — `(hp: i64) -> String` — Render 5-HP boxes plus 1-HP boxes (for 21-100 HP)
-  `render_tens_and_ones` function L622-649 — `(hp: i64) -> String` — Render 10-HP boxes plus 1-HP boxes (for 101+ HP)
-  `extract_speed_full` function L651-690 — `(monster: &Value) -> String` — Shows full stat blocks including attacks, resistances, and abilities.
-  `extract_cr` function L692-710 — `(monster: &Value) -> String` — Shows full stat blocks including attacks, resistances, and abilities.
-  `extract_saves` function L712-733 — `(monster: &Value) -> String` — Shows full stat blocks including attacks, resistances, and abilities.
-  `extract_senses` function L735-747 — `(monster: &Value) -> String` — Shows full stat blocks including attacks, resistances, and abilities.
-  `extract_languages` function L749-759 — `(monster: &Value) -> String` — Shows full stat blocks including attacks, resistances, and abilities.
-  `extract_damage_list` function L761-773 — `(monster: &Value, snake_key: &str, camel_key: &str) -> String` — Shows full stat blocks including attacks, resistances, and abilities.
-  `extract_condition_immunities` function L775-797 — `(monster: &Value) -> String` — Shows full stat blocks including attacks, resistances, and abilities.
-  `render_resistances_block` function L799-830 — `(vuln: &str, resist: &str, dmg_immune: &str, cond_immune: &str) -> String` — Shows full stat blocks including attacks, resistances, and abilities.
-  `SectionContent` struct L833-837 — `{ name: &'static str, content: String, char_count: usize }` — Section content with its rendered size
-  `CardLayout` struct L840-844 — `{ front_sections: Vec<SectionContent>, back_sections: Vec<SectionContent>, is_fo...` — Card layout - single or foldable double
-  `plan_card_layout` function L847-945 — `(monster: &Value) -> CardLayout` — Extract all abilities and determine card layout (single or foldable)
-  `SINGLE_CARD_BUDGET` variable L849 — `: usize` — Shows full stat blocks including attacks, resistances, and abilities.
-  `extract_ability_list` function L948-962 — `(monster: &Value, key: &str) -> Vec<(String, String)>` — Extract ability list from monster data
-  `format_abilities_full` function L965-979 — `(items: &[(String, String)]) -> String` — Format abilities without truncation
-  `render_sections` function L982-1017 — `(sections: &[SectionContent]) -> String` — Render sections into Typst blocks
-  `extract_entries_text` function L1020-1054 — `(entries: Option<&Value>) -> String` — Extract text from entries array, handling nested structures
-  `strip_5etools_tags` function L1057-1089 — `(text: &str) -> String` — Strip 5etools formatting tags and convert to plain text
-  `regex_replace` function L1091-1097 — `(text: &str, pattern: &str, replacement: &str) -> String` — Shows full stat blocks including attacks, resistances, and abilities.
-  `modifier` function L1099-1106 — `(score: i64) -> String` — Shows full stat blocks including attacks, resistances, and abilities.
-  `escape_typst` function L1108-1121 — `(s: &str) -> String` — Shows full stat blocks including attacks, resistances, and abilities.
-  `tests` module L1124-1683 — `-` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_monster_cards_empty` function L1129-1132 — `()` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_monster_cards_with_monsters` function L1135-1144 — `()` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_from_json` function L1147-1154 — `()` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_modifier` function L1157-1161 — `()` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_extract_alignment` function L1164-1167 — `()` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_strip_5etools_tags` function L1170-1175 — `()` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_extract_hp_average` function L1178-1190 — `()` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_render_individual_boxes` function L1193-1207 — `()` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_render_fives_and_ones` function L1210-1220 — `()` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_render_tens_and_ones` function L1223-1233 — `()` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_render_hp_tracker_tier_selection` function L1236-1251 — `()` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_goblin` function L1255-1283 — `() -> Value` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_dragon` function L1285-1316 — `() -> Value` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_render_card_basic_monster` function L1319-1348 — `()` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_render_card_senses_languages` function L1351-1360 — `()` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_render_card_actions_and_traits` function L1363-1376 — `()` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_render_card_damage_immunities` function L1379-1385 — `()` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_render_card_saves` function L1388-1393 — `()` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_render_card_multiple_speeds` function L1396-1403 — `()` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_foldable_card_with_lots_of_content` function L1406-1430 — `()` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_render_back_card_exists_for_complex_monster` function L1433-1459 — `()` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_render_back_card_none_for_simple_monster` function L1462-1466 — `()` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_non_foldable_has_no_fold_indicator` function L1469-1475 — `()` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_to_typst_grid_layout` function L1478-1491 — `()` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_to_typst_cut_lines` function L1494-1501 — `()` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_to_typst_no_cut_lines` function L1504-1511 — `()` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_to_typst_multiple_monsters` function L1514-1523 — `()` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_to_typst_page_break_after_four` function L1526-1537 — `()` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_size_name_mapping` function L1540-1550 — `()` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_extract_ac_full_simple` function L1553-1556 — `()` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_extract_ac_full_with_source` function L1559-1562 — `()` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_extract_hp_full_with_formula` function L1565-1568 — `()` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_extract_hp_full_simple` function L1571-1574 — `()` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_extract_speed_with_fly_hover` function L1577-1581 — `()` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_extract_cr_from_object` function L1584-1587 — `()` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_extract_creature_type_object` function L1590-1593 — `()` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_extract_creature_type_string` function L1596-1599 — `()` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_alignment_unaligned` function L1602-1605 — `()` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_alignment_any` function L1608-1611 — `()` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_escape_typst` function L1614-1618 — `()` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_extract_entries_text_nested` function L1621-1630 — `()` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_strip_5etools_tags_comprehensive` function L1633-1641 — `()` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_resistances_block_empty` function L1644-1647 — `()` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_resistances_block_with_content` function L1650-1660 — `()` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_condition_immunities_string` function L1663-1668 — `()` — Shows full stat blocks including attacks, resistances, and abilities.
-  `test_section_colors` function L1671-1682 — `()` — Shows full stat blocks including attacks, resistances, and abilities.

#### crates/mimir-print/src/sections/spell_cards.rs

- pub `SpellCardsSection` struct L14-19 — `{ spells: Vec<Value>, show_cut_lines: bool }` — Spell cards section - generates multi-up spell cards for printing
- pub `new` function L23-28 — `(spells: Vec<Value>) -> Self` — Create a new spell cards section
- pub `from_json` function L31-37 — `(spells: Value) -> Self` — Create from a JSON value (expects array)
- pub `with_cut_lines` function L40-43 — `(mut self, show: bool) -> Self` — Set whether to show cut lines
-  `SpellCardsSection` type L21-431 — `= SpellCardsSection` — comma, word) and produce foldable continuation cards.
-  `level_str` function L46-54 — `(level: i64) -> String` — Get level string
-  `school_icon` function L57-69 — `(school: &str) -> &'static str` — Get school icon function name
-  `school_name` function L72-84 — `(school: &str) -> &'static str` — Get school full name
-  `extract_fields` function L87-263 — `(spell: &Value) -> SpellFields` — Extract common spell fields for card rendering
-  `render_cards` function L266-429 — `(spell: &Value) -> (String, Option<String>)` — Render a spell as front card + optional continuation card
-  `SpellCardsSection` type L433-538 — `impl Renderable for SpellCardsSection` — comma, word) and produce foldable continuation cards.
-  `to_typst` function L434-525 — `(&self, _ctx: &RenderContext) -> Result<String>` — comma, word) and produce foldable continuation cards.
-  `toc_title` function L527-533 — `(&self) -> Option<String>` — comma, word) and produce foldable continuation cards.
-  `page_break_before` function L535-537 — `(&self) -> bool` — comma, word) and produce foldable continuation cards.
-  `SpellFields` struct L541-555 — `{ name: String, source: String, icon: &'static str, school_full: &'static str, l...` — Extracted spell fields for card rendering
-  `tests` module L558-663 — `-` — comma, word) and produce foldable continuation cards.
-  `test_spell_cards_empty` function L563-566 — `()` — comma, word) and produce foldable continuation cards.
-  `test_spell_cards_with_spells` function L569-577 — `()` — comma, word) and produce foldable continuation cards.
-  `test_level_str` function L580-586 — `()` — comma, word) and produce foldable continuation cards.
-  `test_escape_typst_special_chars` function L589-595 — `()` — comma, word) and produce foldable continuation cards.
-  `test_school_icon` function L598-605 — `()` — comma, word) and produce foldable continuation cards.
-  `test_school_name` function L608-612 — `()` — comma, word) and produce foldable continuation cards.
-  `test_short_spell_no_fold` function L615-626 — `()` — comma, word) and produce foldable continuation cards.
-  `test_long_spell_foldable` function L629-644 — `()` — comma, word) and produce foldable continuation cards.
-  `test_foldable_cards_in_grid` function L647-662 — `()` — comma, word) and produce foldable continuation cards.

#### crates/mimir-print/src/sections/token_cutouts.rs

- pub `CutoutToken` struct L16-27 — `{ name: String, size: String, token_type: String, image_bytes: Option<Vec<u8>>, ...` — Token data for cutout rendering
- pub `new` function L31-39 — `(name: String, size: String, token_type: String) -> Self` — Create a new cutout token
- pub `with_image` function L42-45 — `(mut self, bytes: Vec<u8>) -> Self` — Set the image bytes
- pub `with_quantity` function L48-51 — `(mut self, qty: u32) -> Self` — Set the quantity
- pub `TokenCutoutSection` struct L68-74 — `{ tokens: Vec<CutoutToken>, cell_size: f64, show_cut_lines: bool }` — Token cutout sheet - renders tokens as printable standees
- pub `new` function L78-84 — `(tokens: Vec<CutoutToken>) -> Self` — Create a new token cutout section
- pub `with_cell_size` function L87-90 — `(mut self, size: f64) -> Self` — Set the base cell size in inches
- pub `with_cut_lines` function L93-96 — `(mut self, show: bool) -> Self` — Set whether to show cut lines
-  `TOKEN_COUNTER` variable L12 — `: AtomicUsize` — Counter for unique token image filenames
-  `CutoutToken` type L29-65 — `= CutoutToken` — for use on physical battle maps.
-  `size_multiplier` function L54-64 — `(&self) -> f64` — Get the size multiplier for layout calculations
-  `TokenCutoutSection` type L76-231 — `= TokenCutoutSection` — for use on physical battle maps.
-  `token_type_color` function L99-107 — `(token_type: &str) -> &'static str` — Get color for token type (for fallback circles)
-  `render_token` function L110-199 — `( &self, token: &CutoutToken, ctx: &RenderContext, size_inches: f64, ) -> Result...` — Render a single token standee as a tent (back panel on top, image on bottom)
-  `render_token_grid` function L202-230 — `( &self, tokens: &[&CutoutToken], ctx: &RenderContext, size_mult: f64, ) -> Resu...` — Render a grid of same-sized tokens
-  `TokenCutoutSection` type L233-293 — `impl Renderable for TokenCutoutSection` — for use on physical battle maps.
-  `to_typst` function L234-288 — `(&self, ctx: &RenderContext) -> Result<String>` — for use on physical battle maps.
-  `toc_title` function L290-292 — `(&self) -> Option<String>` — for use on physical battle maps.
-  `sanitize_filename` function L296-306 — `(name: &str) -> String` — Sanitize a string for use as filename
-  `truncate_name` function L309-315 — `(name: &str, max_len: usize) -> String` — Truncate name to max length
-  `detect_image_format` function L319-339 — `(bytes: &[u8]) -> &'static str` — Detect image format from magic bytes
-  `tests` module L342-586 — `-` — for use on physical battle maps.
-  `test_size_multiplier` function L346-355 — `()` — for use on physical battle maps.
-  `test_truncate_name` function L358-361 — `()` — for use on physical battle maps.
-  `test_sanitize_filename` function L364-367 — `()` — for use on physical battle maps.
-  `test_detect_image_format` function L370-382 — `()` — for use on physical battle maps.
-  `test_size_multiplier_all_sizes` function L387-401 — `()` — for use on physical battle maps.
-  `test_token_type_colors` function L406-412 — `()` — for use on physical battle maps.
-  `test_cutout_token_with_quantity` function L417-422 — `()` — for use on physical battle maps.
-  `test_cutout_token_with_image` function L425-430 — `()` — for use on physical battle maps.
-  `test_cutout_token_default_quantity` function L433-437 — `()` — for use on physical battle maps.
-  `test_section_with_cell_size` function L440-443 — `()` — for use on physical battle maps.
-  `test_section_with_cut_lines` function L446-449 — `()` — for use on physical battle maps.
-  `test_empty_tokens_renders_comment` function L454-459 — `()` — for use on physical battle maps.
-  `test_toc_title` function L462-465 — `()` — for use on physical battle maps.
-  `test_render_token_fallback_circle` function L468-479 — `()` — for use on physical battle maps.
-  `test_render_token_name_truncation` function L482-490 — `()` — for use on physical battle maps.
-  `test_render_groups_by_size` function L493-511 — `()` — for use on physical battle maps.
-  `test_render_respects_quantity` function L514-523 — `()` — for use on physical battle maps.
-  `test_render_cut_lines_dashed` function L526-533 — `()` — for use on physical battle maps.
-  `test_render_no_cut_lines` function L536-548 — `()` — for use on physical battle maps.
-  `test_detect_image_format_unknown` function L551-554 — `()` — for use on physical battle maps.
-  `test_sanitize_filename_special_chars` function L557-561 — `()` — for use on physical battle maps.
-  `test_truncate_name_exact_length` function L564-567 — `()` — for use on physical battle maps.
-  `test_render_token_npc_color` function L570-576 — `()` — for use on physical battle maps.
-  `test_render_token_pc_color` function L579-585 — `()` — for use on physical battle maps.

#### crates/mimir-print/src/sections/trap_cards.rs

- pub `TrapCardSection` struct L12-17 — `{ traps: Vec<Value>, show_cut_lines: bool }` — Trap cards section - generates half-page trap reference cards (2x2 layout)
- pub `new` function L21-26 — `(traps: Vec<Value>) -> Self` — Create a new trap cards section
- pub `from_json` function L29-35 — `(traps: Value) -> Self` — Create from a JSON value (expects array)
- pub `with_cut_lines` function L38-41 — `(mut self, show: bool) -> Self` — Set whether to show cut lines
-  `TrapCardSection` type L19-249 — `= TrapCardSection` — Shows trap name, type, trigger, effect, countermeasures, and full details.
-  `get_trap_type_info` function L44-60 — `(trap: &Value) -> (&'static str, &'static str)` — Get trap type info (label and accent color)
-  `render_card` function L63-176 — `(trap: &Value) -> String` — Render a single trap card (half-page format)
-  `render_back_card` function L179-248 — `(trap: &Value) -> Option<String>` — Render back card for overflow content
-  `TrapCardSection` type L251-354 — `impl Renderable for TrapCardSection` — Shows trap name, type, trigger, effect, countermeasures, and full details.
-  `to_typst` function L252-345 — `(&self, _ctx: &RenderContext) -> Result<String>` — Shows trap name, type, trigger, effect, countermeasures, and full details.
-  `toc_title` function L347-353 — `(&self) -> Option<String>` — Shows trap name, type, trigger, effect, countermeasures, and full details.
-  `RenderedEntry` struct L357-360 — `{ typst: String, char_count: usize }` — A rendered entry block with its character count for layout planning
-  `TrapCardLayout` struct L363-367 — `{ front_entries: Vec<RenderedEntry>, back_entries: Vec<RenderedEntry>, is_foldab...` — Card layout - single or foldable double
-  `render_trap_entries` function L370-634 — `(trap: &Value, accent_color: &str) -> Vec<RenderedEntry>` — Render trap entries as individual blocks with char counts for layout planning
-  `plan_trap_layout` function L637-675 — `(trap: &Value, accent_color: &str) -> TrapCardLayout` — Plan trap card layout - determine if foldable needed
-  `SINGLE_CARD_BUDGET` variable L641 — `: usize` — Shows trap name, type, trigger, effect, countermeasures, and full details.
-  `render_dc_block` function L678-727 — `(trap: &Value) -> String` — Render the DC block with detection and disable DCs
-  `extract_dc_from_text` function L730-747 — `(text: &str) -> Option<Vec<String>>` — Extract DC values from text (e.g., "DC 15 Dexterity saving throw")
-  `abbreviate_ability` function L749-759 — `(ability: &str) -> &str` — Shows trap name, type, trigger, effect, countermeasures, and full details.
-  `extract_entry_text_full` function L762-777 — `(obj: &serde_json::Map<String, Value>) -> String` — Extract text from an entry object, including nested entries
-  `strip_5etools_tags` function L780-808 — `(text: &str) -> String` — Strip 5etools formatting tags and convert to plain text
-  `regex_replace` function L810-816 — `(text: &str, pattern: &str, replacement: &str) -> String` — Shows trap name, type, trigger, effect, countermeasures, and full details.
-  `escape_typst` function L819-832 — `(s: &str) -> String` — Escape special Typst characters
-  `tests` module L835-1221 — `-` — Shows trap name, type, trigger, effect, countermeasures, and full details.
-  `test_trap_cards_empty` function L840-843 — `()` — Shows trap name, type, trigger, effect, countermeasures, and full details.
-  `test_trap_cards_with_traps` function L846-854 — `()` — Shows trap name, type, trigger, effect, countermeasures, and full details.
-  `test_from_json` function L857-864 — `()` — Shows trap name, type, trigger, effect, countermeasures, and full details.
-  `test_strip_5etools_tags` function L867-871 — `()` — Shows trap name, type, trigger, effect, countermeasures, and full details.
-  `test_half_page_format` function L874-885 — `()` — Shows trap name, type, trigger, effect, countermeasures, and full details.
-  `test_trap_type_mechanical` function L890-894 — `()` — Shows trap name, type, trigger, effect, countermeasures, and full details.
-  `test_trap_type_magical` function L897-901 — `()` — Shows trap name, type, trigger, effect, countermeasures, and full details.
-  `test_trap_type_wilderness` function L904-908 — `()` — Shows trap name, type, trigger, effect, countermeasures, and full details.
-  `test_trap_type_weather` function L911-915 — `()` — Shows trap name, type, trigger, effect, countermeasures, and full details.
-  `test_trap_type_environmental` function L918-922 — `()` — Shows trap name, type, trigger, effect, countermeasures, and full details.
-  `test_trap_type_hazard` function L925-929 — `()` — Shows trap name, type, trigger, effect, countermeasures, and full details.
-  `test_trap_type_default` function L932-935 — `()` — Shows trap name, type, trigger, effect, countermeasures, and full details.
-  `test_trap_type_alternate_key` function L938-941 — `()` — Shows trap name, type, trigger, effect, countermeasures, and full details.
-  `test_pit_trap` function L945-967 — `() -> Value` — Shows trap name, type, trigger, effect, countermeasures, and full details.
-  `test_fire_glyph` function L969-989 — `() -> Value` — Shows trap name, type, trigger, effect, countermeasures, and full details.
-  `test_render_card_trap_name_and_type` function L992-998 — `()` — Shows trap name, type, trigger, effect, countermeasures, and full details.
-  `test_render_card_threat_badge_moderate` function L1001-1008 — `()` — Shows trap name, type, trigger, effect, countermeasures, and full details.
-  `test_render_card_threat_badge_deadly` function L1011-1017 — `()` — Shows trap name, type, trigger, effect, countermeasures, and full details.
-  `test_render_card_dc_block` function L1020-1026 — `()` — Shows trap name, type, trigger, effect, countermeasures, and full details.
-  `test_render_card_entries` function L1029-1036 — `()` — Shows trap name, type, trigger, effect, countermeasures, and full details.
-  `test_render_card_source_footer` function L1039-1044 — `()` — Shows trap name, type, trigger, effect, countermeasures, and full details.
-  `test_cut_lines_enabled` function L1047-1054 — `()` — Shows trap name, type, trigger, effect, countermeasures, and full details.
-  `test_cut_lines_disabled` function L1057-1064 — `()` — Shows trap name, type, trigger, effect, countermeasures, and full details.
-  `test_multiple_traps_grid` function L1067-1077 — `()` — Shows trap name, type, trigger, effect, countermeasures, and full details.
-  `test_dc_block_empty_when_no_dcs` function L1080-1083 — `()` — Shows trap name, type, trigger, effect, countermeasures, and full details.
-  `test_dc_extracted_from_entry_text` function L1086-1094 — `()` — Shows trap name, type, trigger, effect, countermeasures, and full details.
-  `test_abbreviate_ability` function L1097-1105 — `()` — Shows trap name, type, trigger, effect, countermeasures, and full details.
-  `test_trap_strip_5etools_skill_tag` function L1108-1112 — `()` — Shows trap name, type, trigger, effect, countermeasures, and full details.
-  `test_render_trap_entries_with_list` function L1115-1131 — `()` — Shows trap name, type, trigger, effect, countermeasures, and full details.
-  `test_render_trap_entries_with_table` function L1134-1155 — `()` — Shows trap name, type, trigger, effect, countermeasures, and full details.
-  `test_simple_back_card_none` function L1158-1163 — `()` — Shows trap name, type, trigger, effect, countermeasures, and full details.
-  `test_threat_badge_simple` function L1166-1175 — `()` — Shows trap name, type, trigger, effect, countermeasures, and full details.
-  `test_threat_badge_dangerous` function L1178-1187 — `()` — Shows trap name, type, trigger, effect, countermeasures, and full details.
-  `test_no_threat_badge` function L1190-1201 — `()` — Shows trap name, type, trigger, effect, countermeasures, and full details.
-  `test_page_break_with_five_traps` function L1204-1214 — `()` — Shows trap name, type, trigger, effect, countermeasures, and full details.
-  `test_escape_typst_trap` function L1217-1220 — `()` — Shows trap name, type, trigger, effect, countermeasures, and full details.

### docs/book

> *Semantic summary to be generated by AI agent.*

#### docs/book/book.js

-  `playground_text` function L9-20 — `function playground_text(playground, hidden = true)`
-  `fetch_with_timeout` function L23-28 — `function fetch_with_timeout(url, options, timeout = 6000)`
-  `handle_crate_list_update` function L47-70 — `function handle_crate_list_update(playground_block, playground_crates)`
-  `update_play_button` function L74-103 — `function update_play_button(pre_block, playground_crates)`
-  `run_rust_code` function L105-154 — `function run_rust_code(code_block)`
-  `showThemes` function L322-326 — `function showThemes()`
-  `updateThemeSelected` function L328-339 — `function updateThemeSelected()`
-  `hideThemes` function L341-345 — `function hideThemes()`
-  `get_saved_theme` function L347-355 — `function get_saved_theme()`
-  `delete_saved_theme` function L357-359 — `function delete_saved_theme()`
-  `get_theme` function L361-375 — `function get_theme()`
-  `set_theme` function L378-421 — `function set_theme(theme, store = true)`
-  `showSidebar` function L526-539 — `function showSidebar()`
-  `hideSidebar` function L541-554 — `function hideSidebar()`
-  `initResize` function L572-576 — `function initResize()`
-  `resize` function L577-588 — `function resize(e)`
-  `stopResize` function L590-594 — `function stopResize()`
-  `next` function L634-639 — `function next()`
-  `prev` function L640-645 — `function prev()`
-  `showHelp` function L646-677 — `function showHelp()`
-  `mouseHandler` function L652-663 — `-`
-  `escapeKeyHandler` function L666-673 — `-`
-  `hideHelp` function L678-680 — `function hideHelp()`
-  `hideTooltip` function L719-722 — `function hideTooltip(elem)`
-  `showTooltip` function L724-727 — `function showTooltip(elem, msg)`
-  `updateBorder` function L808-814 — `function updateBorder()`

#### docs/book/clipboard.min.js

-  `e` function L7 — `function e(t)`
-  `i` function L7 — `function i(t,e,n,o,r)`
-  `n` function L7 — `function n()`
-  `o` function L7 — `function o(t,e)`
-  `r` function L7 — `function r(t)`
-  `s` function L7 — `function s(t,e)`

#### docs/book/elasticlunr.min.js

-  `e` function L10 — `function e(e)`

#### docs/book/highlight.js

- pub `_collapse` method L6 — `_collapse(e)`
- pub `_walk` method L6 — `_walk(e,n)`
- pub `add` method L6 — `add(e)`
- pub `addKeyword` method L6 — `addKeyword(e,n)`
- pub `addRule` method L6 — `addRule(e,n)`
- pub `addSublanguage` method L6 — `addSublanguage(e,n)`
- pub `addText` method L6 — `addText(e)`
- pub `closeAllNodes` method L6 — `closeAllNodes()`
- pub `closeNode` method L6 — `closeNode(e)`
- pub `compile` method L6 — `compile()`
- pub `considerAll` method L6 — `considerAll()`
- pub `constructor` method L6 — `constructor(e)`
- pub `exec` method L6 — `exec(e)`
- pub `finalize` method L6 — `finalize()`
- pub `getMatcher` method L6 — `getMatcher(e)`
- pub `ignoreMatch` method L6 — `ignoreMatch()`
- pub `openNode` method L6 — `openNode(e)`
- pub `root` method L6 — `root()`
- pub `span` method L6 — `span(e)`
- pub `toHTML` method L6 — `toHTML()`
- pub `toJSON` method L6 — `toJSON()`
- pub `top` method L6 — `top()`
- pub `value` method L6 — `value()`
- pub `walk` method L6 — `walk(e)`
-  `A` function L6 — `function A(e,{languageName:n})`
-  `E` function L6 — `function E(e)`
-  `I` function L6 — `function I(e)`
-  `N` function L6 — `const N = ()`
-  `S` function L6 — `function S(e,n)`
-  `T` function L6 — `function T(e)`
-  `a` function L6 — `function a(e)`
-  `a` class L6 — `-`
-  `b` function L6 — `function b(e,n,t,r)`
-  `c` function L6 — `function c(e)`
-  `c` class L6 — `-`
-  `c` function L6 — `function c(e,n)`
-  `d` function L6 — `function d(e)`
-  `e` function L6 — `function e(n)`
-  `h` function L6 — `function h(e)`
-  `i` function L6 — `function i(e,n)`
-  `l` function L6 — `function l()`
-  `l` class L6 — `-`
-  `m` function L6 — `function m(e,t,a,s)`
-  `n` class L6 — `-`
-  `n` function L6 — `function n(n,t)`
-  `o` function L6 — `-`
-  `p` function L6 — `function p(e)`
-  `r` function L6 — `function r(e,...n)`
-  `t` function L6 — `function t(e)`
-  `t` class L6 — `-`
-  `u` function L6 — `function u(e)`
-  `u` class L6 — `-`
-  `u` function L6 — `function u()`
-  `v` function L6 — `function v(e,n)`
-  `w` function L6 — `function w(e,n)`
-  `x` function L6 — `function x(t,r)`
-  `t` function L9 — `function t(e)`
-  `e` function L18 — `function e(e)`
-  `n` function L18 — `function n(...n)`
-  `a` function L19 — `function a(...n)`
-  `e` function L19 — `function e(e)`
-  `n` function L19 — `function n(e)`
-  `s` function L19 — `function s(...n)`
-  `r` function L20 — `function r(...e)`
-  `s` function L20 — `function s(e)`
-  `e` function L47 — `function e(...e)`

#### docs/book/mark.min.js

-  `a` function L7 — `function a(e)`
-  `e` function L7 — `function e(e,t)`

#### docs/book/mermaid.min.js

- pub `constructor` method L716 — `constructor()`
- pub `visitEndAnchor` method L716 — `visitEndAnchor(a)`
- pub `constructor` method L718 — `constructor()`
- pub `visitStartAnchor` method L718 — `visitStartAnchor(a)`
- pub `constructor` method L806 — `constructor(k,L,R,O,M,B=!1)`
- pub `file` method L806 — `file(k)`
- pub `from` method L806 — `from(k)`
- pub `fsPath` method L806 — `fsPath()`
- pub `isUri` method L806 — `isUri(k)`
- pub `parse` method L806 — `parse(k,L=!1)`
- pub `revive` method L806 — `revive(k)`
- pub `toJSON` method L806 — `toJSON()`
- pub `toString` method L806 — `toString(k=!1)`
- pub `with` method L806 — `with(k)`
-  `$F` function L3 — `function $F(t,e,r)`
-  `B4` function L3-8 — `function B4(t)`
-  `DC` function L3 — `function DC(t,e)`
-  `I4` function L3 — `function I4(t,e,r,n)`
-  `IF` function L3 — `function IF(t,e)`
-  `LC` function L3 — `function LC(t,e,r)`
-  `MC` function L3 — `function MC(t,e)`
-  `NC` function L3 — `function NC(t,e=Q2e)`
-  `PF` function L3 — `function PF(t,e,r)`
-  `RC` function L3 — `function RC(t,e)`
-  `RF` function L3 — `function RF(t,e)`
-  `X2e` function L3 — `function X2e(t,e)`
-  `Y2e` function L3 — `function Y2e(t,e="defs")`
-  `_C` function L3 — `function _C(t,e)`
-  `a` function L3 — `function a(s)`
-  `axe` function L3 — `function axe()`
-  `cxe` function L3 — `function cxe()`
-  `e` function L3 — `function e(f)`
-  `h` function L3 — `function h()`
-  `i` function L3 — `function i(f)`
-  `ixe` function L3 — `function ixe(t)`
-  `k` function L3 — `function k(R)`
-  `l` function L3 — `function l(f)`
-  `lxe` function L3 — `function lxe()`
-  `n` function L3 — `function n(f,d)`
-  `nxe` function L3 — `function nxe(t)`
-  `oxe` function L3 — `function oxe(t)`
-  `r` function L3 — `function r(f)`
-  `rxe` function L3 — `function rxe(t)`
-  `s` function L3 — `function s(l)`
-  `sxe` function L3 — `function sxe(t)`
-  `txe` function L3 — `function txe(t)`
-  `u` function L3 — `function u(f)`
-  `y` function L3 — `function y(...v)`
-  `Rt` function L9 — `function Rt(nt)`
-  `st` function L9 — `function st()`
-  `$xe` function L14 — `function $xe(t)`
-  `Cr` function L14 — `function Cr(t,e)`
-  `He` function L14 — `function He()`
-  `Iy` function L14 — `function Iy(t,e)`
-  `Ka` function L14 — `function Ka(t)`
-  `Qf` function L14 — `function Qf(t)`
-  `Tt` function L14 — `function Tt(At,Ce,tt)`
-  `r` function L14 — `function r()`
-  `sz` function L14-15 — `function sz()`
-  `zxe` function L14 — `function zxe(t)`
-  `$z` function L15 — `function $z(t)`
-  `Abe` function L15 — `function Abe(t,e)`
-  `C` function L15 — `function C()`
-  `G` function L15 — `function G(t,e,r,n,i,a)`
-  `Nbe` function L15 — `function Nbe(t)`
-  `Nt` function L15 — `function Nt(t)`
-  `P7` function L15 — `function P7(t,e,r)`
-  `Ql` function L15 — `function Ql(t)`
-  `Qz` function L15 — `function Qz(t)`
-  `SG` function L15 — `function SG(t,e,r)`
-  `W7` function L15 — `function W7(t)`
-  `_7` function L15 — `function _7(t,e)`
-  `_be` function L15 — `function _be(t)`
-  `a3` function L15 — `function a3(t,e)`
-  `abe` function L15 — `function abe(t)`
-  `bz` function L15 — `function bz(t)`
-  `cbe` function L15 — `function cbe(t)`
-  `d4e` function L15 — `function d4e(t,e,r)`
-  `fe` function L15 — `function fe(t,e)`
-  `gbe` function L15 — `function gbe(t)`
-  `gz` function L15 — `function gz(t,e,r,n,i)`
-  `hz` function L15 — `function hz(t)`
-  `k3` function L15 — `function k3(t,e)`
-  `p4e` function L15 — `function p4e(t)`
-  `ph` function L15 — `function ph(t,e,r)`
-  `q7` function L15 — `function q7(t)`
-  `rG` function L15 — `function rG(t,e)`
-  `rd` function L15 — `function rd(t)`
-  `tG` function L15 — `function tG(t,e)`
-  `w` function L15 — `function w()`
-  `w3` function L15 — `function w3(t)`
-  `wz` function L15 — `function wz(t)`
-  `xr` function L15 — `function xr(t,e)`
-  `z7` function L15 — `function z7(t)`
-  `n` function L269-270 — `function n()`
-  `C` function L270 — `function C(ae)`
-  `j4e` function L275 — `function j4e()`
-  `$` function L352 — `function $(K)`
-  `$0` function L352 — `function $0(t)`
-  `$5` function L352 — `function $5(t,e)`
-  `$5e` function L352 — `function $5e()`
-  `$6e` function L352 — `function $6e(t)`
-  `$A` function L352 — `function $A(t,e,r)`
-  `$Ee` function L352 — `function $Ee(t,e)`
-  `$Se` function L352 — `function $Se(t,e,r,n)`
-  `$Te` function L352 — `function $Te(t)`
-  `$ke` function L352 — `function $ke(t)`
-  `$n` function L352 — `function $n(t)`
-  `$we` function L352 — `function $we(t)`
-  `A` function L352 — `function A(K,X)`
-  `A5e` function L352 — `function A5e(t,e)`
-  `A8` function L352 — `function A8(t)`
-  `AA` function L352 — `function AA(t)`
-  `AH` function L352 — `function AH()`
-  `AW` function L352 — `function AW(t)`
-  `Ake` function L352 — `function Ake(t,e)`
-  `Av` function L352 — `function Av(t)`
-  `Awe` function L352 — `function Awe(t,e,r)`
-  `B` function L352 — `function B(K)`
-  `B0` function L352 — `function B0(t,e,r)`
-  `B3` function L352 — `function B3(t)`
-  `B5` function L352 — `function B5(t)`
-  `B5e` function L352 — `function B5e(t)`
-  `B6e` function L352 — `function B6e(t)`
-  `BA` function L352 — `function BA(t,e)`
-  `BEe` function L352 — `function BEe(t)`
-  `BW` function L352 — `function BW(t,e,r,n,i,a,s,l,u,h)`
-  `Bi` function L352 — `function Bi(t,e)`
-  `Bke` function L352 — `function Bke(t)`
-  `Bn` function L352 — `function Bn(t)`
-  `Bwe` function L352 — `function Bwe(t,e)`
-  `C0` function L352 — `function C0(t,e)`
-  `C5e` function L352 — `function C5e(t)`
-  `CA` function L352 — `function CA(t)`
-  `CEe` function L352 — `function CEe(t)`
-  `CH` function L352 — `function CH(t)`
-  `CU` function L352 — `function CU(t,e)`
-  `CW` function L352 — `function CW(t)`
-  `C_` function L352 — `function C_(t)`
-  `Cke` function L352 — `function Cke(t)`
-  `Cv` function L352 — `function Cv(t,e,r)`
-  `Cwe` function L352 — `function Cwe(t,e,r)`
-  `D` function L352 — `function D(K,X,te)`
-  `D0` function L352 — `function D0(t,e,r)`
-  `D5e` function L352 — `function D5e(t)`
-  `DA` function L352 — `function DA(t)`
-  `DEe` function L352 — `function DEe(t)`
-  `DW` function L352 — `function DW(t)`
-  `Dh` function L352 — `function Dh(t,e,r)`
-  `Dke` function L352 — `function Dke(t)`
-  `Do` function L352 — `function Do(t)`
-  `Dwe` function L352 — `function Dwe(t,e)`
-  `E` function L352 — `function E(K,X)`
-  `E5e` function L352 — `function E5e(t,e,r)`
-  `E6e` function L352 — `function E6e(t,e,r)`
-  `E8` function L352 — `function E8(t,e,r,n,i)`
-  `E9` function L352 — `function E9(t,e)`
-  `ECe` function L352 — `function ECe(t)`
-  `EEe` function L352 — `function EEe(t)`
-  `EU` function L352 — `function EU(t)`
-  `EW` function L352 — `function EW(t,e)`
-  `Eh` function L352 — `function Eh(t)`
-  `Eke` function L352 — `function Eke(t,e)`
-  `Ewe` function L352 — `function Ewe(t)`
-  `F` function L352 — `function F(K)`
-  `F0` function L352 — `function F0(t)`
-  `F3e` function L352 — `function F3e()`
-  `F5` function L352 — `function F5(t,e)`
-  `F5e` function L352 — `function F5e()`
-  `F6e` function L352 — `function F6e(t)`
-  `FA` function L352 — `function FA()`
-  `FEe` function L352 — `function FEe(t,e)`
-  `FSe` function L352 — `function FSe(t,e,r)`
-  `FU` function L352 — `function FU(t)`
-  `Fi` function L352 — `function Fi(t,e)`
-  `Fke` function L352 — `function Fke(t)`
-  `Fv` function L352 — `function Fv(t,e,r)`
-  `Fwe` function L352 — `function Fwe(t,e)`
-  `G0` function L352 — `function G0(t)`
-  `G3` function L352 — `function G3(t)`
-  `G3e` function L352 — `function G3e({_intern:t,_key:e},r)`
-  `G5e` function L352 — `function G5e()`
-  `G6e` function L352 — `function G6e(t)`
-  `GA` function L352 — `function GA()`
-  `GEe` function L352 — `function GEe()`
-  `GTe` function L352 — `function GTe(t,e,r)`
-  `Ge` function L352 — `function Ge(t)`
-  `Gke` function L352 — `function Gke(t)`
-  `Gv` function L352 — `function Gv(t)`
-  `Gwe` function L352 — `function Gwe(t)`
-  `H` function L352 — `function H(K)`
-  `H5` function L352 — `function H5(t,e)`
-  `H5e` function L352 — `function H5e(t)`
-  `HA` function L352 — `function HA()`
-  `HTe` function L352 — `function HTe(t,e,r)`
-  `HW` function L352 — `function HW(t)`
-  `Hke` function L352 — `function Hke(t)`
-  `Hwe` function L352 — `function Hwe(t,e,r)`
-  `I` function L352 — `function I(D)`
-  `I3` function L352 — `function I3(t,e,r)`
-  `I5` function L352 — `function I5()`
-  `I5e` function L352 — `function I5e(t)`
-  `I6e` function L352 — `function I6e(t,e)`
-  `IA` function L352 — `function IA()`
-  `IU` function L352 — `function IU(t)`
-  `IV` function L352 — `function IV(t,e)`
-  `I_` function L352 — `function I_(t,e,r,n)`
-  `Ike` function L352 — `function Ike(t,e)`
-  `Iv` function L352 — `function Iv(t)`
-  `Iwe` function L352 — `function Iwe(t,e)`
-  `J3e` function L352 — `function J3e(t,e)`
-  `J8` function L352 — `function J8(t,e)`
-  `JA` function L352 — `function JA(t,e)`
-  `JEe` function L352 — `function JEe(t,e)`
-  `JSe` function L352 — `function JSe(t,e,r,n,i,a,s)`
-  `JTe` function L352 — `function JTe(t,e,r)`
-  `JV` function L352 — `function JV()`
-  `J_` function L352 — `function J_(t)`
-  `Jy` function L352 — `function Jy(t)`
-  `K3` function L352 — `function K3(t,e,r,n,i,a)`
-  `K3e` function L352 — `function K3e(t,e)`
-  `K5e` function L352 — `function K5e()`
-  `K6e` function L352 — `function K6e()`
-  `K8` function L352 — `function K8(t)`
-  `KG` function L352 — `function KG({_intern:t,_key:e},r)`
-  `KSe` function L352 — `function KSe(t)`
-  `KTe` function L352 — `function KTe(t,e,r)`
-  `K_` function L352 — `function K_(t)`
-  `Ki` function L352 — `function Ki(t,e)`
-  `Kwe` function L352 — `function Kwe(t)`
-  `L` function L352 — `function L(K,X,te)`
-  `L0` function L352 — `function L0()`
-  `L3` function L352 — `function L3(t,e,r)`
-  `L5e` function L352 — `function L5e(t)`
-  `L6e` function L352 — `function L6e(t)`
-  `LA` function L352 — `function LA(t)`
-  `LSe` function L352 — `function LSe(t)`
-  `LW` function L352 — `function LW(t,e)`
-  `L_` function L352 — `function L_(t)`
-  `Lke` function L352 — `function Lke(t,e)`
-  `Lv` function L352 — `function Lv(t)`
-  `Lwe` function L352 — `function Lwe(t,e)`
-  `M` function L352 — `function M(K,X,te)`
-  `M3` function L352 — `function M3(t,e)`
-  `M5` function L352 — `function M5(t,e,r,n,i,a,s)`
-  `M5e` function L352 — `function M5e(t)`
-  `M8` function L352 — `function M8(t,e)`
-  `MU` function L352 — `function MU(t)`
-  `MV` function L352 — `function MV(t,e)`
-  `Mke` function L352 — `function Mke(t,e)`
-  `Mwe` function L352 — `function Mwe(t,e)`
-  `N3` function L352 — `function N3(t,e)`
-  `N5` function L352 — `function N5(t)`
-  `N5e` function L352 — `function N5e()`
-  `N6e` function L352 — `function N6e(t,e)`
-  `N8` function L352 — `function N8(t,e)`
-  `NA` function L352 — `function NA(t)`
-  `NV` function L352 — `function NV(t)`
-  `N_` function L352 — `function N_(t,e)`
-  `Nke` function L352 — `function Nke(t,e)`
-  `Nv` function L352 — `function Nv(t)`
-  `Nwe` function L352 — `function Nwe(t,e)`
-  `O` function L352 — `function O(K,X,te)`
-  `O5e` function L352 — `function O5e()`
-  `OSe` function L352 — `function OSe(t,e)`
-  `OX` function L352 — `function OX(t,e)`
-  `O_` function L352 — `function O_(t,e)`
-  `Oke` function L352 — `function Oke()`
-  `Owe` function L352 — `function Owe(t,e)`
-  `P` function L352 — `function P(K)`
-  `P0` function L352 — `function P0(t,e,r)`
-  `P5` function L352 — `function P5(t)`
-  `P5e` function L352 — `function P5e(t)`
-  `PA` function L352 — `function PA(t)`
-  `PU` function L352 — `function PU(t,e,r,n)`
-  `Pke` function L352 — `function Pke(t)`
-  `Pwe` function L352 — `function Pwe(t,e)`
-  `Q` function L352 — `function Q(K)`
-  `Q3e` function L352 — `function Q3e()`
-  `Q8` function L352 — `function Q8(t)`
-  `QA` function L352 — `function QA(t,e,r)`
-  `QEe` function L352 — `function QEe(t,e)`
-  `QSe` function L352 — `function QSe(t)`
-  `QTe` function L352 — `function QTe(t,e,r)`
-  `Q_` function L352 — `function Q_(t)`
-  `Qwe` function L352 — `function Qwe(t)`
-  `R` function L352 — `function R(K,X,te)`
-  `R0` function L352 — `function R0(t)`
-  `R3` function L352 — `function R3(t,e,r)`
-  `R5e` function L352 — `function R5e(t,e)`
-  `R6e` function L352 — `function R6e(t,e)`
-  `R8` function L352 — `function R8(t,e)`
-  `RA` function L352 — `function RA(t)`
-  `RSe` function L352 — `function RSe(t)`
-  `RV` function L352 — `function RV(t)`
-  `RW` function L352 — `function RW(t)`
-  `R_` function L352 — `function R_(t)`
-  `Rke` function L352 — `function Rke(t,e)`
-  `Rq` function L352 — `function Rq(t)`
-  `Rv` function L352 — `function Rv(t)`
-  `Rwe` function L352 — `function Rwe(t,e)`
-  `S` function L352 — `function S(K,X,te,J)`
-  `S5e` function L352 — `function S5e(t,e,r)`
-  `S6e` function L352 — `function S6e(t)`
-  `S8` function L352 — `function S8(t)`
-  `SW` function L352 — `function SW()`
-  `S_` function L352 — `function S_(t)`
-  `Sh` function L352 — `function Sh(t,e)`
-  `Ske` function L352 — `function Ske(t,e)`
-  `Swe` function L352 — `function Swe(t,e,r)`
-  `T` function L352 — `function T(E)`
-  `T0` function L352 — `function T0(t,e,r,n)`
-  `T5e` function L352 — `function T5e(t,e)`
-  `T8` function L352 — `function T8(t)`
-  `TU` function L352 — `function TU(t)`
-  `TW` function L352 — `function TW(t,e,r)`
-  `T_` function L352 — `function T_(t,e)`
-  `Th` function L352 — `function Th()`
-  `Tke` function L352 — `function Tke(t,e)`
-  `Twe` function L352 — `function Twe(t,e,r)`
-  `U0` function L352 — `function U0(t)`
-  `U3` function L352 — `function U3(t)`
-  `U3e` function L352 — `function U3e(t)`
-  `U5` function L352 — `function U5(t)`
-  `U5e` function L352 — `function U5e()`
-  `U8` function L352 — `function U8(t,e)`
-  `UA` function L352 — `function UA()`
-  `USe` function L352 — `function USe(t,e)`
-  `UTe` function L352 — `function UTe(t,e,r)`
-  `UU` function L352 — `function UU()`
-  `Uke` function L352 — `function Uke(t,e,r,n,i,a,s,l)`
-  `Uv` function L352 — `function Uv(t)`
-  `Uwe` function L352 — `function Uwe(t,e)`
-  `V0` function L352 — `function V0(t)`
-  `V3` function L352 — `function V3(t,e,r,n)`
-  `V3e` function L352 — `function V3e({_intern:t,_key:e},r)`
-  `V5` function L352 — `function V5(t)`
-  `V5e` function L352 — `function V5e()`
-  `V8` function L352 — `function V8(t)`
-  `VA` function L352 — `function VA(t)`
-  `VEe` function L352 — `function VEe(t)`
-  `VTe` function L352 — `function VTe(t,e,r)`
-  `Vke` function L352 — `function Vke(t)`
-  `Vv` function L352 — `function Vv(t)`
-  `W0` function L352 — `function W0(t)`
-  `W3` function L352 — `function W3(t)`
-  `W5e` function L352 — `function W5e(t)`
-  `W8` function L352 — `function W8(t,e)`
-  `W9` function L352 — `function W9(t,e)`
-  `WA` function L352 — `function WA()`
-  `WTe` function L352 — `function WTe(t,e,r)`
-  `Wke` function L352 — `function Wke(t)`
-  `Wr` function L352 — `function Wr(t,e,r)`
-  `Wwe` function L352 — `function Wwe(t,e,r)`
-  `X3` function L352 — `function X3(t,e)`
-  `X3e` function L352 — `function X3e(t)`
-  `X5e` function L352 — `function X5e(t,e)`
-  `X6e` function L352 — `function X6e(t)`
-  `X8` function L352 — `function X8(t)`
-  `XA` function L352 — `function XA(t)`
-  `XTe` function L352 — `function XTe(t,e,r)`
-  `XW` function L352 — `function XW(t)`
-  `X_` function L352 — `function X_(t)`
-  `Xwe` function L352 — `function Xwe(t,e,r)`
-  `Y3e` function L352 — `function Y3e(t)`
-  `Y5e` function L352 — `function Y5e(t,e,r)`
-  `Y6e` function L352 — `function Y6e(t)`
-  `Y8` function L352 — `function Y8(t)`
-  `Y9` function L352 — `function Y9(t)`
-  `YA` function L352 — `function YA()`
-  `YSe` function L352 — `function YSe(t)`
-  `YTe` function L352 — `function YTe(t,e,r)`
-  `YW` function L352 — `function YW(t)`
-  `Yke` function L352 — `function Yke(t)`
-  `Ywe` function L352 — `function Ywe(t,e,r)`
-  `Z8` function L352 — `function Z8(t)`
-  `Z9` function L352 — `function Z9(t)`
-  `ZEe` function L352 — `function ZEe()`
-  `ZSe` function L352 — `function ZSe(t)`
-  `ZTe` function L352 — `function ZTe(t,e,r)`
-  `Zs` function L352 — `function Zs()`
-  `Zwe` function L352 — `function Zwe(t)`
-  `Zy` function L352 — `function Zy(t,e,r)`
-  `_` function L352 — `function _(K,X,te)`
-  `_5` function L352 — `function _5()`
-  `_5e` function L352 — `function _5e(t,e)`
-  `_6e` function L352 — `function _6e(t,e)`
-  `_W` function L352 — `function _W(t,e)`
-  `_ke` function L352 — `function _ke(t,e)`
-  `_v` function L352 — `function _v(t)`
-  `_we` function L352 — `function _we(t,e,r)`
-  `a` function L352 — `function a(l,u,h=0,f=l.length)`
-  `a5e` function L352 — `function a5e(t)`
-  `a6e` function L352 — `function a6e(t)`
-  `a8` function L352 — `function a8()`
-  `aSe` function L352 — `function aSe(t)`
-  `aU` function L352 — `function aU(t)`
-  `a_` function L352 — `function a_(t,e,r)`
-  `ake` function L352 — `function ake(t,e)`
-  `aq` function L352 — `function aq(t,e)`
-  `awe` function L352 — `function awe()`
-  `b0` function L352 — `function b0(t,e)`
-  `b5e` function L352 — `function b5e(t,e)`
-  `b8` function L352 — `function b8(t)`
-  `bA` function L352 — `function bA(t)`
-  `bCe` function L352 — `function bCe(t)`
-  `bU` function L352 — `function bU(t,e)`
-  `bW` function L352 — `function bW(t,e,r)`
-  `bX` function L352 — `function bX(t,e,r,n,i)`
-  `b_` function L352 — `function b_(t)`
-  `bh` function L352 — `function bh(t,e)`
-  `bke` function L352 — `function bke(t,e)`
-  `bl` function L352 — `function bl()`
-  `bq` function L352 — `function bq(t)`
-  `bwe` function L352 — `function bwe(t,e,r)`
-  `c5` function L352 — `function c5(t)`
-  `c6e` function L352 — `function c6e(t)`
-  `c8` function L352 — `function c8(t)`
-  `cH` function L352 — `function cH(t)`
-  `cTe` function L352 — `function cTe()`
-  `c_` function L352 — `function c_()`
-  `cd` function L352 — `function cd(t)`
-  `cke` function L352 — `function cke(t)`
-  `cwe` function L352 — `function cwe(t,e,r)`
-  `d` function L352 — `function d()`
-  `d5e` function L352 — `function d5e(t,e,r,n,i,a,s)`
-  `d6e` function L352 — `function d6e(t)`
-  `dke` function L352 — `function dke(t,e)`
-  `dl` function L352 — `function dl(t,e,r,n)`
-  `du` function L352 — `function du(t,e)`
-  `dv` function L352 — `function dv()`
-  `dw` function L352 — `function dw(t,e)`
-  `dwe` function L352 — `function dwe(t)`
-  `e` function L352 — `function e(d,p)`
-  `e5e` function L352 — `function e5e(t,e)`
-  `e6e` function L352 — `function e6e(t,e)`
-  `e8` function L352 — `function e8(t)`
-  `eCe` function L352 — `function eCe(t)`
-  `eU` function L352 — `function eU()`
-  `e_` function L352 — `function e_()`
-  `eke` function L352 — `function eke(t,e,r)`
-  `es` function L352 — `function es(t,e,r,n)`
-  `f` function L352 — `function f(d,p)`
-  `f5e` function L352 — `function f5e(t,e,r,n,i,a)`
-  `f6e` function L352 — `function f6e(t)`
-  `f8` function L352 — `function f8(t,e)`
-  `fA` function L352 — `function fA(t,e)`
-  `fCe` function L352 — `function fCe(t)`
-  `fEe` function L352 — `function fEe(t)`
-  `f_` function L352 — `function f_(t)`
-  `fke` function L352 — `function fke(t)`
-  `fq` function L352 — `function fq(t)`
-  `fu` function L352 — `function fu(t,e,r,n)`
-  `fv` function L352 — `function fv()`
-  `fwe` function L352 — `function fwe(t)`
-  `g5` function L352 — `function g5(t,e)`
-  `g5e` function L352 — `function g5e(t,e)`
-  `g6e` function L352 — `function g6e()`
-  `g8` function L352 — `function g8(t,e,r)`
-  `gA` function L352 — `function gA(t)`
-  `gH` function L352 — `function gH(t)`
-  `gU` function L352 — `function gU(t)`
-  `gW` function L352 — `function gW(t,e,r,n,i,a)`
-  `g_` function L352 — `function g_(t)`
-  `gke` function L352 — `function gke(t,e)`
-  `gl` function L352 — `function gl()`
-  `gq` function L352 — `function gq(t,e)`
-  `gu` function L352 — `function gu()`
-  `gv` function L352 — `function gv(t,e)`
-  `gwe` function L352 — `function gwe()`
-  `h` function L352 — `function h(d)`
-  `h5` function L352 — `function h5(t)`
-  `h5e` function L352 — `function h5e(t)`
-  `h6e` function L352 — `function h6e(t)`
-  `h8` function L352 — `function h8(t,e,r)`
-  `hCe` function L352 — `function hCe(t,e,r)`
-  `h_` function L352 — `function h_(t)`
-  `ha` function L352 — `function ha(t,e)`
-  `he` function L352 — `function he(K)`
-  `hke` function L352 — `function hke(t,e)`
-  `i` function L352 — `function i(l,u,h=0,f=l.length)`
-  `i5e` function L352 — `function i5e()`
-  `i6e` function L352 — `function i6e(t)`
-  `i8` function L352 — `function i8()`
-  `iTe` function L352 — `function iTe(t)`
-  `iU` function L352 — `function iU(t,e,r,n)`
-  `iV` function L352 — `function iV(t,e)`
-  `i_` function L352 — `function i_(t,e,r)`
-  `ic` function L352 — `function ic(t,e,r,n)`
-  `ie` function L352 — `function ie(K)`
-  `ike` function L352 — `function ike(t,e)`
-  `iwe` function L352 — `function iwe()`
-  `j` function L352 — `function j(K)`
-  `j3e` function L352 — `function j3e(t)`
-  `j5e` function L352 — `function j5e(t,e)`
-  `j6e` function L352 — `function j6e(t)`
-  `j8` function L352 — `function j8(t)`
-  `jA` function L352 — `function jA(t,e)`
-  `jEe` function L352 — `function jEe(t)`
-  `jTe` function L352 — `function jTe(t,e,r)`
-  `jV` function L352 — `function jV()`
-  `j_` function L352 — `function j_(t)`
-  `jwe` function L352 — `function jwe(t)`
-  `k` function L352 — `function k(K,X,te)`
-  `k5e` function L352 — `function k5e(t)`
-  `k6e` function L352 — `function k6e(t,e,r)`
-  `k8` function L352 — `function k8(t,e,r,n)`
-  `kCe` function L352 — `function kCe(t,e,r)`
-  `kH` function L352 — `function kH(t)`
-  `kU` function L352 — `function kU(t)`
-  `kW` function L352 — `function kW(t,e)`
-  `kh` function L352 — `function kh(t,e)`
-  `kke` function L352 — `function kke(t,e)`
-  `kv` function L352 — `function kv()`
-  `kwe` function L352 — `function kwe(t)`
-  `l` function L352 — `function l(h,f,d,p)`
-  `l5e` function L352 — `function l5e()`
-  `l6e` function L352 — `function l6e()`
-  `l8` function L352 — `function l8()`
-  `lCe` function L352 — `function lCe(t)`
-  `lTe` function L352 — `function lTe(t,e,r)`
-  `lV` function L352 — `function lV()`
-  `l_` function L352 — `function l_()`
-  `ld` function L352 — `function ld(t)`
-  `le` function L352 — `function le(K)`
-  `lke` function L352 — `function lke(t,e)`
-  `lq` function L352 — `function lq(t,e)`
-  `lwe` function L352 — `function lwe(t)`
-  `m5` function L352 — `function m5(t)`
-  `m5e` function L352 — `function m5e(t)`
-  `m9` function L352 — `function m9(t,e,r)`
-  `mEe` function L352 — `function mEe(t)`
-  `md` function L352 — `function md(t,e)`
-  `mke` function L352 — `function mke(t,e)`
-  `ml` function L352 — `function ml(t)`
-  `mq` function L352 — `function mq(t,e,r)`
-  `mwe` function L352 — `function mwe()`
-  `n` function L352 — `function n(i,a)`
-  `n5e` function L352 — `function n5e()`
-  `n6e` function L352 — `function n6e(t)`
-  `n8` function L352 — `function n8(t)`
-  `nCe` function L352 — `function nCe(t)`
-  `nU` function L352 — `function nU()`
-  `n_` function L352 — `function n_()`
-  `ne` function L352 — `function ne(K)`
-  `nke` function L352 — `function nke(t,e)`
-  `nq` function L352 — `function nq(t)`
-  `nv` function L352 — `function nv(t)`
-  `o5` function L352 — `function o5(t)`
-  `o5e` function L352 — `function o5e(t)`
-  `o8` function L352 — `function o8(t,e)`
-  `o9` function L352 — `function o9(t)`
-  `oTe` function L352 — `function oTe(t,e,r)`
-  `oU` function L352 — `function oU(t,e,r,n)`
-  `oV` function L352 — `function oV(t,e,r)`
-  `o_` function L352 — `function o_(t)`
-  `od` function L352 — `function od(t)`
-  `oi` function L352 — `function oi(t,e)`
-  `oke` function L352 — `function oke(t,e)`
-  `oq` function L352 — `function oq(t,e)`
-  `p` function L352 — `function p(m)`
-  `p5e` function L352 — `function p5e(t)`
-  `p6e` function L352 — `function p6e(t,e)`
-  `pA` function L352 — `function pA(t)`
-  `pke` function L352 — `function pke(t,e)`
-  `pl` function L352 — `function pl(t)`
-  `pq` function L352 — `function pq(t)`
-  `pu` function L352 — `function pu(t,e,r,n,i,a)`
-  `pv` function L352 — `function pv(t,e,r)`
-  `q5e` function L352 — `function q5e(t)`
-  `q8` function L352 — `function q8(t,e)`
-  `qA` function L352 — `function qA()`
-  `qEe` function L352 — `function qEe(t)`
-  `qSe` function L352 — `function qSe(t,e)`
-  `qTe` function L352 — `function qTe(t,e,r)`
-  `qV` function L352 — `function qV(t,e,r)`
-  `qW` function L352 — `function qW(t)`
-  `qke` function L352 — `function qke(t)`
-  `qwe` function L352 — `function qwe(t,e)`
-  `r` function L352 — `function r(n)`
-  `r5e` function L352 — `function r5e(t)`
-  `r8` function L352 — `function r8(t)`
-  `r9` function L352 — `function r9(t)`
-  `rCe` function L352 — `function rCe(t,e,r)`
-  `rTe` function L352 — `function rTe(t)`
-  `rU` function L352 — `function rU()`
-  `r_` function L352 — `function r_(t)`
-  `ra` function L352 — `function ra(t,e)`
-  `rc` function L352 — `function rc(t)`
-  `rke` function L352 — `function rke(t,e)`
-  `rv` function L352 — `function rv(t,e)`
-  `s` function L352 — `function s(l,u,h=0,f=l.length)`
-  `s5` function L352 — `function s5()`
-  `s6e` function L352 — `function s6e(t,e)`
-  `s8` function L352 — `function s8(t)`
-  `s9` function L352 — `function s9(t,e)`
-  `sTe` function L352 — `function sTe(t,e)`
-  `sU` function L352 — `function sU(t)`
-  `s_` function L352 — `function s_(t)`
-  `sd` function L352 — `function sd(t,e)`
-  `ske` function L352 — `function ske(t,e)`
-  `sv` function L352 — `function sv(t,e,r,n)`
-  `swe` function L352 — `function swe()`
-  `t` function L352 — `function t(i,a,s,l,u,h,f)`
-  `t5` function L352 — `function t5(t,e,r)`
-  `t5e` function L352 — `function t5e(t)`
-  `t8` function L352 — `function t8(t,e)`
-  `tCe` function L352 — `function tCe(t,e,r)`
-  `tTe` function L352 — `function tTe(t,e)`
-  `tU` function L352 — `function tU(t)`
-  `t_` function L352 — `function t_(t)`
-  `tke` function L352 — `function tke(t,e,r)`
-  `tq` function L352 — `function tq(t)`
-  `tv` function L352 — `function tv(t)`
-  `u` function L352 — `function u(h,f,d,p,m,g)`
-  `u5` function L352 — `function u5(t,e)`
-  `u5e` function L352 — `function u5e()`
-  `u6e` function L352 — `function u6e(t,e)`
-  `u8` function L352 — `function u8(t)`
-  `uCe` function L352 — `function uCe(t,e)`
-  `uTe` function L352 — `function uTe(t)`
-  `ua` function L352 — `function ua(t,e,r,n)`
-  `uke` function L352 — `function uke(t,e)`
-  `uq` function L352 — `function uq(t,e)`
-  `v0` function L352 — `function v0(t)`
-  `v5` function L352 — `function v5(t,e)`
-  `v5e` function L352 — `function v5e(t)`
-  `v6e` function L352 — `function v6e(t)`
-  `vCe` function L352 — `function vCe(t,e)`
-  `vEe` function L352 — `function vEe(t)`
-  `v_` function L352 — `function v_(t)`
-  `vd` function L352 — `function vd(t)`
-  `vke` function L352 — `function vke(t)`
-  `vq` function L352 — `function vq(t)`
-  `w5e` function L352 — `function w5e(t,e)`
-  `w6e` function L352 — `function w6e(t,e)`
-  `w8` function L352 — `function w8(t)`
-  `wCe` function L352 — `function wCe(t)`
-  `wU` function L352 — `function wU(t,e)`
-  `wW` function L352 — `function wW(t,e,r)`
-  `wh` function L352 — `function wh(t,e,r)`
-  `wke` function L352 — `function wke(t,e)`
-  `wl` function L352 — `function wl(t,e)`
-  `wq` function L352 — `function wq(t)`
-  `wu` function L352 — `function wu(t)`
-  `wwe` function L352 — `function wwe(t,e)`
-  `x0` function L352 — `function x0(t)`
-  `x5e` function L352 — `function x5e(t,e)`
-  `x6e` function L352 — `function x6e(t)`
-  `x8` function L352 — `function x8(t)`
-  `xA` function L352 — `function xA(t)`
-  `xEe` function L352 — `function xEe(t)`
-  `xW` function L352 — `function xW(t,e,r)`
-  `xd` function L352 — `function xd(t)`
-  `xh` function L352 — `function xh(t)`
-  `xke` function L352 — `function xke(t,e)`
-  `xn` function L352 — `function xn(t,e,r,n)`
-  `xv` function L352 — `function xv(t,e)`
-  `y0` function L352 — `function y0(t,e,r)`
-  `y5` function L352 — `function y5(t,e)`
-  `y5e` function L352 — `function y5e(t)`
-  `y6e` function L352 — `function y6e(t)`
-  `y8` function L352 — `function y8(t)`
-  `y9` function L352 — `function y9(t)`
-  `yU` function L352 — `function yU(t)`
-  `y_` function L352 — `function y_(t,e)`
-  `yke` function L352 — `function yke(t,e)`
-  `yq` function L352 — `function yq(t)`
-  `yv` function L352 — `function yv(t,e)`
-  `ywe` function L352 — `function ywe()`
-  `z` function L352 — `function z(K)`
-  `z5` function L352 — `function z5(t,e)`
-  `z5e` function L352 — `function z5e()`
-  `z8` function L352 — `function z8(t)`
-  `zA` function L352 — `function zA(t)`
-  `zSe` function L352 — `function zSe(t,e)`
-  `zTe` function L352 — `function zTe(t,e,r)`
-  `zU` function L352 — `function zU()`
-  `zke` function L352 — `function zke(t)`
-  `zv` function L352 — `function zv(t)`
-  `zwe` function L352 — `function zwe(t,e,r)`
-  `Cl` function L353 — `function Cl(t,e,r,n,i)`
-  `GX` function L353 — `function GX(t,e,r,n,i)`
-  `$Ce` function L357 — `function $Ce(t)`
-  `GCe` function L357 — `function GCe(t,e)`
-  `UCe` function L357 — `function UCe(t)`
-  `VCe` function L357 — `function VCe(t,e)`
-  `hj` function L357-359 — `function hj(t,e)`
-  `uj` function L357 — `function uj(t)`
-  `zCe` function L357 — `function zCe(t)`
-  `KCe` function L359-363 — `function KCe(t,e)`
-  `nD` function L359 — `function nD(t,e)`
-  `o2` function L359 — `function o2(t,e)`
-  `rD` function L359 — `function rD(t,e,r,n,i)`
-  `$7e` function L363 — `function $7e(t)`
-  `A7e` function L363 — `function A7e(t,e)`
-  `B7e` function L363 — `function B7e(t)`
-  `E7e` function L363 — `function E7e(t)`
-  `F7e` function L363 — `function F7e(t)`
-  `H7e` function L363 — `function H7e(t)`
-  `J7e` function L363 — `function J7e(t)`
-  `Ls` function L363 — `function Ls(t)`
-  `M7e` function L363 — `function M7e(t)`
-  `N7e` function L363 — `function N7e(t)`
-  `Nd` function L363 — `function Nd(t)`
-  `O7e` function L363 — `function O7e(t)`
-  `QX` function L363 — `function QX(t)`
-  `R7e` function L363 — `function R7e(t)`
-  `S7e` function L363 — `function S7e(t)`
-  `W7e` function L363 — `function W7e(t)`
-  `X7e` function L363 — `function X7e(t)`
-  `Z7e` function L363 — `function Z7e(t)`
-  `ZX` function L363-364 — `function ZX(t)`
-  `_7e` function L363 — `function _7e(t)`
-  `aAe` function L363 — `function aAe(t)`
-  `aD` function L363 — `function aD(t)`
-  `am` function L363 — `function am(t)`
-  `b7e` function L363 — `function b7e(t)`
-  `c7e` function L363 — `function c7e()`
-  `d7e` function L363 — `function d7e(t)`
-  `dc` function L363 — `function dc(t)`
-  `e7e` function L363 — `function e7e(t)`
-  `f7e` function L363 — `function f7e(t)`
-  `g7e` function L363 — `function g7e(t)`
-  `j7e` function L363 — `function j7e(t)`
-  `jX` function L363 — `function jX(t,e)`
-  `l7e` function L363 — `function l7e(t)`
-  `n` function L363 — `function n(i)`
-  `oAe` function L363 — `function oAe(t)`
-  `p7e` function L363 — `function p7e(t)`
-  `r7e` function L363 — `function r7e()`
-  `sAe` function L363 — `function sAe(t)`
-  `t7e` function L363 — `function t7e(t,e)`
-  `u7e` function L363 — `function u7e(t)`
-  `v7e` function L363 — `function v7e(t)`
-  `w7e` function L363 — `function w7e(t)`
-  `x7e` function L363 — `function x7e(t)`
-  `y7e` function L363 — `function y7e(t)`
-  `z7e` function L363 — `function z7e(t)`
-  `Ci` function L364 — `function Ci(t,e,r)`
-  `Qt` function L364 — `function Qt(t,e)`
-  `Tj` function L364 — `function Tj(t,e)`
-  `bw` function L364 — `function bw(t,e)`
-  `cAe` function L364 — `function cAe(t,e)`
-  `ej` function L364 — `function ej(t,e,r,n)`
-  `fD` function L364-365 — `function fD(t,e)`
-  `hD` function L364 — `function hD(t)`
-  `kw` function L364 — `function kw(t)`
-  `lAe` function L364 — `function lAe(t)`
-  `sm` function L364 — `function sm(t,e,r,n,i,a,s,l,u)`
-  `zh` function L364 — `function zh(t,e,r,n)`
-  `dAe` function L365 — `function dAe(t,e)`
-  `fAe` function L365 — `function fAe(t,e)`
-  `hAe` function L365 — `function hAe(t,e)`
-  `pAe` function L365-371 — `function pAe(t,e)`
-  `uAe` function L365 — `function uAe(t,e,r)`
-  `gAe` function L371 — `function gAe(t)`
-  `kj` function L371-372 — `function kj(t,e)`
-  `mAe` function L371 — `function mAe(t,e,r)`
-  `om` function L371 — `function om(t,e,r,n,i)`
-  `tj` function L371 — `function tj(t,e)`
-  `vAe` function L371 — `function vAe(t)`
-  `xAe` function L371 — `function xAe(t)`
-  `yAe` function L371 — `function yAe(t)`
-  `UAe` function L372 — `function UAe(t)`
-  `VAe` function L372 — `function VAe(t,e)`
-  `WAe` function L372 — `function WAe(t)`
-  `bAe` function L372 — `function bAe(t,e,r)`
-  `rj` function L372-374 — `function rj(t,e)`
-  `wAe` function L372 — `function wAe(t,e)`
-  `oD` function L374-375 — `function oD(t,e)`
-  `KAe` function L375 — `function KAe(t,e,r,n,i)`
-  `Nj` function L375 — `function Nj(t)`
-  `Tw` function L375 — `function Tw(t)`
-  `XAe` function L375 — `function XAe(t)`
-  `YAe` function L375 — `function YAe(t)`
-  `aj` function L375-379 — `function aj(t,e)`
-  `ij` function L375 — `function ij(t,e,r)`
-  `jAe` function L375 — `function jAe(t,e,r,n,i,a,s,l)`
-  `nj` function L375 — `function nj(t)`
-  `qAe` function L375 — `function qAe(t,e)`
-  `s2` function L375 — `function s2(t,e)`
-  `u` function L375 — `function u(h)`
-  `u2` function L375 — `function u2(t)`
-  `sj` function L379-380 — `function sj(t)`
-  `QAe` function L380-383 — `function QAe(t,e)`
-  `oj` function L383-386 — `function oj(t,e)`
-  `Au` function L386 — `function Au(t,e,r,n,i,a,s)`
-  `JAe` function L386 — `function JAe(t,e,r)`
-  `ZAe` function L386 — `function ZAe(t)`
-  `cD` function L386 — `function cD(t,e,r)`
-  `cj` function L386 — `function cj(t,e,r)`
-  `e8e` function L386 — `function e8e(t,e,r)`
-  `lj` function L386 — `function lj(t,e,r,n)`
-  `n8e` function L386-387 — `function n8e(t,e)`
-  `r8e` function L386 — `function r8e(t,e)`
-  `t8e` function L386 — `function t8e(t,e,r,n)`
-  `pD` function L387 — `function pD(t,e)`
-  `$8e` function L388 — `function $8e(t,e)`
-  `$j` function L388 — `function $j(t,e)`
-  `Fj` function L388 — `function Fj(t)`
-  `Gj` function L388 — `function Gj(t)`
-  `f2` function L388 — `function f2(t,e,r)`
-  `nn` function L388 — `function nn(t,e="")`
-  `pc` function L388 — `function pc(t,e)`
-  `vD` function L388 — `function vD()`
-  `z8e` function L388-390 — `function z8e(t,e,r)`
-  `zj` function L388 — `function zj(t,e,r,n,i)`
-  `Jr` function L390 — `function Jr(t,e)`
-  `G8e` function L443-445 — `function G8e(t,{markdownAutoWrap:e})`
-  `Jj` function L445-446 — `function Jj(t,e={})`
-  `s` function L445-446 — `function s(l,u="normal")`
-  `U8e` function L446 — `function U8e(t,e)`
-  `V8e` function L446 — `function V8e(t)`
-  `eK` function L446 — `function eK(t,{markdownAutoWrap:e}={})`
-  `n` function L446 — `function n(i)`
-  `nK` function L446-447 — `function nK(t,e)`
-  `rK` function L446 — `function rK(t,e,r,n)`
-  `CD` function L447 — `function CD(t,e,r=[],n=[])`
-  `H8e` function L447-448 — `function H8e(t,e,r,n,i=!1)`
-  `aK` function L447 — `function aK(t,e)`
-  `$w` function L448 — `function $w(t,e,r,n,i)`
-  `AD` function L448 — `function AD(t,e,r)`
-  `AK` function L448 — `function AK(t,e,r,n,i,a,s,l,u,h)`
-  `BD` function L448 — `function BD(t,e=.15,r)`
-  `Bw` function L448 — `function Bw(t,e,r,n=1)`
-  `CK` function L448 — `function CK(t)`
-  `DD` function L448 — `function DD(t)`
-  `DK` function L448 — `function DK(t,e,r)`
-  `EK` function L448 — `function EK(t,e=0)`
-  `FK` function L448 — `function FK(t,e)`
-  `Fo` function L448 — `function Fo(t,e,r,n,i,a)`
-  `Fw` function L448 — `function Fw(t,e,r)`
-  `HD` function L448 — `function HD(t,e,r,n)`
-  `IK` function L448 — `function IK(t,e)`
-  `K8e` function L448 — `function K8e(t,e,r,n)`
-  `LK` function L448 — `function LK(t)`
-  `La` function L448 — `function La(t,e,r,n)`
-  `Lw` function L448 — `function Lw(t,e,r,n,i,a)`
-  `MD` function L448 — `function MD(t,e,r)`
-  `MK` function L448 — `function MK(t,e,r,n,i,a,s)`
-  `Mw` function L448 — `function Mw(t,e,r)`
-  `OD` function L448 — `function OD(t,e)`
-  `Od` function L448 — `function Od(t,e,r)`
-  `Ow` function L448 — `function Ow(t,e,r)`
-  `PD` function L448 — `function PD(t,e)`
-  `PK` function L448 — `function PK(t,e)`
-  `Pw` function L448 — `function Pw(t,e)`
-  `Q8e` function L448 — `function Q8e(t,e,r)`
-  `RK` function L448 — `function RK(t,e)`
-  `SK` function L448 — `function SK(t)`
-  `TK` function L448 — `function TK(t,e,r,n,i,a,s,l)`
-  `Uh` function L448 — `function Uh(t,e,r,n,i,a=!1)`
-  `W8e` function L448 — `function W8e(t,e,r)`
-  `WD` function L448 — `function WD(t,e,r,n,i,a,s)`
-  `X8e` function L448 — `function X8e(t,e,r,n)`
-  `Xt` function L448 — `function Xt(t)`
-  `Y8e` function L448 — `function Y8e(t,e)`
-  `_D` function L448 — `function _D(t,e)`
-  `_K` function L448 — `function _K(t,e,r,n,i)`
-  `a_e` function L448 — `function a_e(t,e,r,n,i,a,s,l)`
-  `bK` function L448 — `function bK(t,e)`
-  `dm` function L448 — `function dm(t,e)`
-  `e_e` function L448 — `function e_e(t,e)`
-  `g2` function L448 — `function g2(t,e,r)`
-  `hK` function L448 — `function hK(t,e)`
-  `i_e` function L448 — `function i_e(t,e,r,n,i)`
-  `j8e` function L448 — `function j8e(t,e,r)`
-  `jD` function L448 — `function jD(t)`
-  `kK` function L448 — `function kK(t,e,r,n,i,a,s,l,u)`
-  `l_e` function L448-449 — `function l_e(t)`
-  `nr` function L448 — `function nr(t,e,r=1)`
-  `o_e` function L448 — `function o_e(t,e)`
-  `q8e` function L448 — `function q8e(t,e,r,n=!1)`
-  `qD` function L448 — `function qD(t,e,r,n)`
-  `sK` function L448 — `function sK(t,e,r)`
-  `s_e` function L448 — `function s_e(t,e,r)`
-  `t_e` function L448 — `function t_e(t,e,r,n=1)`
-  `vK` function L448 — `function vK(t,e)`
-  `wK` function L448 — `function wK(t)`
-  `x` function L448 — `function x()`
-  `x2` function L448 — `function x2(t,e)`
-  `xK` function L448 — `function xK(t,e,r,n,i,a,s,l,u)`
-  `y2` function L448 — `function y2(t)`
-  `zK` function L448 — `function zK(t,e)`
-  `zw` function L448 — `function zw(t)`
-  `CQ` function L449 — `function CQ(t,e)`
-  `Du` function L449 — `function Du(t,e,r)`
-  `EQ` function L449 — `function EQ(t,e,{config:{flowchart:r}})`
-  `FQ` function L449 — `function FQ(t,e)`
-  `HK` function L449 — `function HK(t,e)`
-  `HQ` function L449 — `function HQ(t,e,{config:{themeVariables:r}})`
-  `Hh` function L449 — `function Hh(t,e,r,n=100,i=0,a=180)`
-  `IQ` function L449 — `function IQ(t,e)`
-  `KK` function L449 — `function KK(t,e)`
-  `LQ` function L449 — `function LQ(t,e)`
-  `NQ` function L449 — `function NQ(t,e)`
-  `PQ` function L449 — `function PQ(t,e)`
-  `Ra` function L449 — `function Ra(t,e,r,n=100,i=0,a=180)`
-  `TQ` function L449 — `function TQ(t,e,{config:{themeVariables:r,flowchart:n}})`
-  `VK` function L449 — `function VK(t,e)`
-  `VQ` function L449 — `function VQ(t,e)`
-  `Wh` function L449 — `function Wh(t,e,r,n=100,i=0,a=180)`
-  `XK` function L449 — `function XK(t,e)`
-  `ZK` function L449 — `function ZK(t,e)`
-  `_Q` function L449 — `function _Q(t,e)`
-  `bQ` function L449 — `function bQ(t,e,{config:{themeVariables:r,flowchart:n}})`
-  `eQ` function L449 — `function eQ(t,e)`
-  `fQ` function L449 — `function fQ(t,e)`
-  `gQ` function L449 — `function gQ(t,e,{config:{themeVariables:r,flowchart:n}})`
-  `iQ` function L449 — `function iQ(t,e,{config:{themeVariables:r}})`
-  `lQ` function L449 — `function lQ(t,e,{dir:r,config:{state:n,themeVariables:i}})`
-  `pQ` function L449 — `function pQ(t,e)`
-  `qK` function L449 — `function qK(t,e)`
-  `qQ` function L449-453 — `function qQ(t,e)`
-  `rQ` function L449 — `function rQ(t,e)`
-  `sQ` function L449 — `function sQ(t,e)`
-  `uQ` function L449 — `function uQ(t,e)`
-  `vQ` function L449 — `function vQ(t,e,{config:{themeVariables:r,flowchart:n}})`
-  `zQ` function L449 — `function zQ(t,e)`
-  `XQ` function L453 — `function XQ(t,e)`
-  `v_e` function L453-454 — `function v_e(t)`
-  `y_e` function L453 — `function y_e(t,e)`
-  `KQ` function L454 — `function KQ(t,e)`
-  `TZ` function L454 — `function TZ(t,e)`
-  `ZQ` function L454 — `function ZQ(t,e)`
-  `bZ` function L454 — `function bZ(t,e)`
-  `eZ` function L454 — `function eZ(t,e)`
-  `fZ` function L454 — `function fZ(t,e,{config:{themeVariables:r}})`
-  `gZ` function L454 — `function gZ(t,e)`
-  `iZ` function L454 — `function iZ(t,e)`
-  `lZ` function L454 — `function lZ(t,e)`
-  `pZ` function L454 — `function pZ(t,e)`
-  `rZ` function L454 — `function rZ(t,e)`
-  `sZ` function L454 — `function sZ(t,e)`
-  `uZ` function L454 — `function uZ(t,e,{config:{themeVariables:r}})`
-  `vZ` function L454 — `function vZ(t,e)`
-  `CZ` function L460 — `function CZ(t,e)`
-  `EZ` function L460 — `function EZ(t,e)`
-  `IZ` function L460-462 — `function IZ(t,e)`
-  `LZ` function L460 — `function LZ(t,e)`
-  `NZ` function L460 — `function NZ(t,e)`
-  `_Z` function L460 — `function _Z(t,e)`
-  `$Z` function L462 — `function $Z(t,e)`
-  `BZ` function L462 — `function BZ(t,e,r,n,i=r.class.padding??12)`
-  `GZ` function L462 — `function GZ(t,e)`
-  `KD` function L462 — `function KD(t,e)`
-  `Lu` function L462 — `function Lu(t,e,r,n="")`
-  `UZ` function L462 — `function UZ(t,e,{config:r})`
-  `Vw` function L462 — `function Vw(t,e,r,n=[])`
-  `WZ` function L462 — `function WZ(t)`
-  `b2` function L462 — `function b2(t,e,r,n=0,i=0,a=[],s="")`
-  `x` function L462 — `function x()`
-  `P_e` function L470 — `function P_e(t)`
-  `Ww` function L470 — `function Ww(t,e)`
-  `Yw` function L470 — `function Yw(t,e)`
-  `$Me` function L476 — `function $Me(t)`
-  `$Ne` function L476 — `function $Ne(t)`
-  `AIe` function L476 — `function AIe(t,e,r)`
-  `AMe` function L476 — `function AMe(t)`
-  `ARe` function L476 — `function ARe(t,e,r)`
-  `Ane` function L476 — `function Ane(t)`
-  `B9e` function L476 — `function B9e(t)`
-  `BDe` function L476 — `function BDe(t)`
-  `BIe` function L476 — `function BIe(t,e)`
-  `BLe` function L476 — `function BLe(t)`
-  `BMe` function L476 — `function BMe(t,e)`
-  `BNe` function L476 — `function BNe(t)`
-  `BRe` function L476 — `function BRe(t,e)`
-  `CIe` function L476 — `function CIe(t,e)`
-  `CNe` function L476 — `function CNe(t)`
-  `CRe` function L476 — `function CRe(t,e)`
-  `D2` function L476 — `function D2(t)`
-  `D9e` function L476 — `function D9e(t)`
-  `DIe` function L476 — `function DIe(t,e,r,n,i)`
-  `DMe` function L476 — `function DMe(t,e,r)`
-  `E9e` function L476 — `function E9e(t,e)`
-  `EIe` function L476 — `function EIe(t,e)`
-  `EMe` function L476 — `function EMe(t,e)`
-  `ENe` function L476 — `function ENe(t)`
-  `ERe` function L476 — `function ERe(t,e)`
-  `Ec` function L476 — `function Ec(t,e,r,n)`
-  `Ene` function L476 — `function Ene(t)`
-  `FIe` function L476 — `function FIe(t,e)`
-  `FRe` function L476 — `function FRe(t,e)`
-  `G9e` function L476 — `function G9e(t)`
-  `GLe` function L476 — `function GLe(t)`
-  `GMe` function L476 — `function GMe(t,e)`
-  `GNe` function L476 — `function GNe(t,e)`
-  `GRe` function L476 — `function GRe(t,e)`
-  `Gne` function L476 — `function Gne(t,e,r)`
-  `H9e` function L476 — `function H9e(t)`
-  `HL` function L476 — `function HL(t,e)`
-  `HLe` function L476 — `function HLe(t)`
-  `HMe` function L476 — `function HMe(t,e)`
-  `HNe` function L476 — `function HNe(t)`
-  `Hne` function L476 — `function Hne(t,e,r)`
-  `IDe` function L476 — `function IDe(t,e)`
-  `IIe` function L476 — `function IIe(t,e,r)`
-  `IMe` function L476 — `function IMe(t,e,r)`
-  `IRe` function L476 — `function IRe(t)`
-  `JIe` function L476 — `function JIe(t)`
-  `JMe` function L476 — `function JMe(t,e)`
-  `JNe` function L476 — `function JNe(t,e,r)`
-  `JRe` function L476 — `function JRe(t,e)`
-  `JT` function L476 — `function JT(t)`
-  `K9e` function L476 — `function K9e(t)`
-  `KIe` function L476 — `function KIe(t)`
-  `KL` function L476 — `function KL(t)`
-  `KLe` function L476 — `function KLe(t)`
-  `KMe` function L476 — `function KMe(t)`
-  `KNe` function L476 — `function KNe(t,e)`
-  `KRe` function L476 — `function KRe(t,e,r)`
-  `Kne` function L476 — `function Kne(t)`
-  `L2` function L476 — `function L2()`
-  `LIe` function L476 — `function LIe(t,e,r,n)`
-  `LNe` function L476 — `function LNe(t,e,r)`
-  `MDe` function L476 — `function MDe(t)`
-  `MIe` function L476 — `function MIe(t,e)`
-  `MRe` function L476 — `function MRe(t)`
-  `NIe` function L476 — `function NIe(t,e)`
-  `NRe` function L476 — `function NRe(t)`
-  `O9e` function L476 — `function O9e(t,e)`
-  `OIe` function L476 — `function OIe(t,e)`
-  `ONe` function L476 — `function ONe(t)`
-  `ORe` function L476 — `function ORe(t)`
-  `One` function L476 — `function One(t,e,r,n,i,a)`
-  `PDe` function L476 — `function PDe(t)`
-  `PIe` function L476 — `function PIe(t)`
-  `PJ` function L476 — `function PJ(t,e,r,n,i)`
-  `PMe` function L476 — `function PMe(t)`
-  `PRe` function L476 — `function PRe(t,e,r,n)`
-  `Q9e` function L476 — `function Q9e(t,e,r)`
-  `QIe` function L476 — `function QIe(t)`
-  `QL` function L476 — `function QL(t)`
-  `QLe` function L476 — `function QLe(t)`
-  `QNe` function L476 — `function QNe(t,e)`
-  `QRe` function L476 — `function QRe(t,e,r)`
-  `R2` function L476 — `function R2(t,e)`
-  `RIe` function L476 — `function RIe(t,e)`
-  `RRe` function L476 — `function RRe(t,e)`
-  `SIe` function L476 — `function SIe(t,e)`
-  `SNe` function L476 — `function SNe(t,e)`
-  `SRe` function L476 — `function SRe(t)`
-  `Sne` function L476 — `function Sne(t)`
-  `T9e` function L476 — `function T9e(t,e,r)`
-  `TIe` function L476 — `function TIe(t,e,r,n)`
-  `TMe` function L476 — `function TMe(t,e,r,n,i)`
-  `TNe` function L476 — `function TNe(t,e)`
-  `TRe` function L476 — `function TRe(t)`
-  `TT` function L476 — `function TT(t)`
-  `Tne` function L476 — `function Tne(t)`
-  `ULe` function L476 — `function ULe(t)`
-  `UMe` function L476 — `function UMe(t,e,r,n)`
-  `UNe` function L476 — `function UNe(t)`
-  `Une` function L476 — `function Une(t)`
-  `Uo` function L476 — `function Uo(t)`
-  `V9e` function L476 — `function V9e(t,e)`
-  `VNe` function L476 — `function VNe(t,e,r)`
-  `Vne` function L476 — `function Vne(t,e,r,n,i)`
-  `W9e` function L476 — `function W9e(t,e)`
-  `WL` function L476 — `function WL(t,e,r,n,i)`
-  `WLe` function L476 — `function WLe(t,e)`
-  `WNe` function L476 — `function WNe(t,e)`
-  `WRe` function L476 — `function WRe(t,e,r)`
-  `Wd` function L476 — `function Wd(t,e)`
-  `Wne` function L476 — `function Wne(t,e,r,n)`
-  `X9e` function L476 — `function X9e(t)`
-  `XIe` function L476 — `function XIe(t)`
-  `XL` function L476 — `function XL(t,e)`
-  `XMe` function L476 — `function XMe(t)`
-  `XNe` function L476 — `function XNe(t,e,r,n)`
-  `Xee` function L476 — `function Xee(t,e,r,n,i)`
-  `Xne` function L476 — `function Xne(t)`
-  `Y9e` function L476 — `function Y9e(t,e)`
-  `YIe` function L476 — `function YIe(t)`
-  `YMe` function L476 — `function YMe(t,e)`
-  `YNe` function L476 — `function YNe(t)`
-  `YRe` function L476 — `function YRe(t,e,r,n)`
-  `Yne` function L476 — `function Yne(t)`
-  `ZIe` function L476 — `function ZIe(t)`
-  `ZL` function L476 — `function ZL(t)`
-  `ZMe` function L476 — `function ZMe(t,e)`
-  `ZNe` function L476 — `function ZNe(t,e)`
-  `ZRe` function L476 — `function ZRe(t)`
-  `Zne` function L476 — `function Zne(t,e,r)`
-  `_2` function L476 — `function _2(t,e,r,n)`
-  `_9e` function L476 — `function _9e(t)`
-  `_Ie` function L476 — `function _Ie(t,e,r,n)`
-  `_Me` function L476 — `function _Me(t,e)`
-  `_Ne` function L476 — `function _Ne(t,e,r,n)`
-  `_Re` function L476 — `function _Re(t,e)`
-  `_ne` function L476 — `function _ne(t)`
-  `a` function L476 — `function a(s)`
-  `aLe` function L476 — `function aLe(t)`
-  `aNe` function L476 — `function aNe(t)`
-  `aOe` function L476 — `function aOe(t)`
-  `ane` function L476 — `function ane(t,e)`
-  `b9e` function L476 — `function b9e(t,e,r,n)`
-  `bDe` function L476 — `function bDe(t)`
-  `bIe` function L476 — `function bIe(t,e)`
-  `bMe` function L476 — `function bMe(t)`
-  `bT` function L476 — `function bT(t,e,r,n,i,a)`
-  `bie` function L476 — `function bie(t,e,r)`
-  `bne` function L476 — `function bne(t)`
-  `cDe` function L476 — `function cDe(t,e)`
-  `cNe` function L476 — `function cNe(t)`
-  `cR` function L476 — `function cR(t,e)`
-  `d9e` function L476 — `function d9e(t)`
-  `dMe` function L476 — `function dMe(t)`
-  `dNe` function L476 — `function dNe(t,e)`
-  `dR` function L476 — `function dR(t,e,r,n)`
-  `dne` function L476 — `function dne(t)`
-  `e` function L476 — `function e(r)`
-  `eIe` function L476 — `function eIe(t,e)`
-  `eMe` function L476 — `function eMe(t,e,r)`
-  `eNe` function L476 — `function eNe(t,e)`
-  `eOe` function L476 — `function eOe(t)`
-  `ef` function L476 — `function ef(t)`
-  `eie` function L476 — `function eie(t,e,r)`
-  `f` function L476 — `function f(d)`
-  `fIe` function L476 — `function fIe(t)`
-  `fNe` function L476 — `function fNe(t,e)`
-  `g9e` function L476 — `function g9e(t)`
-  `gIe` function L476 — `function gIe(t)`
-  `gie` function L476 — `function gie(t)`
-  `gne` function L476 — `function gne(t,e)`
-  `h` function L476 — `function h(d)`
-  `hDe` function L476 — `function hDe()`
-  `hIe` function L476 — `function hIe(t)`
-  `hNe` function L476 — `function hNe(t,e)`
-  `hR` function L476 — `function hR(t)`
-  `hRe` function L476 — `function hRe(t,e,r,n,i,a,s)`
-  `hie` function L476 — `function hie(t,e)`
-  `hne` function L476 — `function hne(t)`
-  `i` function L476 — `function i(a)`
-  `iNe` function L476 — `function iNe(t,e)`
-  `iOe` function L476 — `function iOe(t)`
-  `iR` function L476 — `function iR(t)`
-  `iie` function L476 — `function iie(t)`
-  `ine` function L476 — `function ine(t,e)`
-  `j9e` function L476 — `function j9e(t)`
-  `jIe` function L476 — `function jIe(t)`
-  `jL` function L476 — `function jL(t,e,r,n)`
-  `jLe` function L476 — `function jLe(t,e,r,n,i,a)`
-  `jMe` function L476 — `function jMe(t)`
-  `jNe` function L476 — `function jNe(t,e,r)`
-  `jRe` function L476 — `function jRe(t)`
-  `jne` function L476 — `function jne(t,e,r,n,i,a,s)`
-  `k9e` function L476 — `function k9e(t,e,r)`
-  `kIe` function L476 — `function kIe(t)`
-  `kMe` function L476 — `function kMe(t,e,r)`
-  `kRe` function L476 — `function kRe(t)`
-  `kie` function L476 — `function kie(t)`
-  `kne` function L476 — `function kne(t)`
-  `l9e` function L476 — `function l9e(t)`
-  `lDe` function L476 — `function lDe(t,e)`
-  `lIe` function L476 — `function lIe(t,e,r)`
-  `lJ` function L476 — `function lJ(t)`
-  `lLe` function L476 — `function lLe(t)`
-  `lR` function L476 — `function lR(t,e)`
-  `lie` function L476 — `function lie(t,e)`
-  `lne` function L476 — `function lne(t,e)`
-  `m9e` function L476 — `function m9e(t)`
-  `mDe` function L476 — `function mDe(t,e)`
-  `mIe` function L476 — `function mIe(t,e,r)`
-  `mMe` function L476 — `function mMe(t,e)`
-  `mR` function L476 — `function mR(t)`
-  `mRe` function L476 — `function mRe(t,e,r,n,i,a)`
-  `mie` function L476 — `function mie(t,e)`
-  `mne` function L476 — `function mne(t)`
-  `n` function L476 — `function n(a)`
-  `n9e` function L476 — `function n9e(t,e)`
-  `nLe` function L476 — `function nLe(t,e,r)`
-  `nNe` function L476 — `function nNe(t,e)`
-  `nOe` function L476 — `function nOe(t)`
-  `oDe` function L476 — `function oDe(t,e,r,n)`
-  `oIe` function L476 — `function oIe(t,e)`
-  `oNe` function L476 — `function oNe(t,e,r)`
-  `oOe` function L476 — `function oOe(t)`
-  `oR` function L476 — `function oR(t,e)`
-  `pIe` function L476 — `function pIe(t)`
-  `pMe` function L476 — `function pMe(t)`
-  `pNe` function L476 — `function pNe(t,e)`
-  `pR` function L476 — `function pR(t,e)`
-  `pie` function L476 — `function pie(t,e,r)`
-  `pne` function L476 — `function pne(t)`
-  `q9e` function L476 — `function q9e(t,e,r)`
-  `qL` function L476 — `function qL(t,e,r)`
-  `qLe` function L476 — `function qLe(t,e)`
-  `qMe` function L476 — `function qMe(t,e,r)`
-  `r` function L476 — `function r(n)`
-  `r9e` function L476 — `function r9e(t)`
-  `rNe` function L476 — `function rNe(t,e,r)`
-  `rOe` function L476 — `function rOe(t)`
-  `rf` function L476 — `function rf(t)`
-  `rie` function L476 — `function rie(t,e)`
-  `rk` function L476 — `function rk(t,e,r)`
-  `s9e` function L476 — `function s9e(t)`
-  `sDe` function L476 — `function sDe(t)`
-  `sIe` function L476 — `function sIe(t,e,r)`
-  `sOe` function L476 — `function sOe(t)`
-  `sie` function L476 — `function sie(t,e)`
-  `sne` function L476 — `function sne(t)`
-  `tIe` function L476 — `function tIe(t,e,r)`
-  `tNe` function L476 — `function tNe(t,e)`
-  `tOe` function L476 — `function tOe(t)`
-  `tk` function L476 — `function tk(t)`
-  `u` function L476 — `function u(d,p)`
-  `uDe` function L476 — `function uDe(t,e)`
-  `uIe` function L476 — `function uIe(t)`
-  `uNe` function L476 — `function uNe(t,e)`
-  `uR` function L476 — `function uR(t,e)`
-  `uie` function L476 — `function uie(t,e,r)`
-  `une` function L476 — `function une(t)`
-  `v9e` function L476 — `function v9e()`
-  `vDe` function L476 — `function vDe(t,e)`
-  `vIe` function L476 — `function vIe(t)`
-  `vNe` function L476 — `function vNe(t,e)`
-  `vRe` function L476 — `function vRe(t,e,r,n,i,a)`
-  `vie` function L476 — `function vie(t)`
-  `vm` function L476 — `function vm(t,e,r)`
-  `vne` function L476 — `function vne(t,e)`
-  `w9e` function L476 — `function w9e(t)`
-  `wDe` function L476 — `function wDe(t)`
-  `wIe` function L476 — `function wIe(t,e)`
-  `wNe` function L476 — `function wNe(t,e)`
-  `wRe` function L476 — `function wRe(t,e,r,n)`
-  `wie` function L476 — `function wie(t)`
-  `x9e` function L476 — `function x9e(t,e)`
-  `xDe` function L476 — `function xDe(t,e,r)`
-  `xIe` function L476 — `function xIe(t,e)`
-  `xMe` function L476 — `function xMe(t,e,r,n)`
-  `xne` function L476 — `function xne(t,e,r,n,i,a)`
-  `yIe` function L476 — `function yIe(t,e)`
-  `yne` function L476 — `function yne(t,e)`
-  `zLe` function L476 — `function zLe(t)`
-  `zMe` function L476 — `function zMe(t,e,r)`
-  `zNe` function L476 — `function zNe(t,e)`
-  `zRe` function L476 — `function zRe(t,e,r,n)`
-  `zne` function L476 — `function zne(t,e,r)`
-  `P2e` function L479 — `function P2e()`
-  `wnt` function L479 — `function wnt(Ws)`
-  `Sn` function L484 — `function Sn()`
-  `q` function L631 — `function q()`
-  `xe` function L631 — `function xe(ct)`
-  `te` function L636 — `function te()`
-  `$R` function L690 — `function $R(t)`
-  `AR` function L690 — `function AR(t,e)`
-  `Ag` function L690 — `function Ag(t)`
-  `BOe` function L690 — `function BOe(t,e)`
-  `BR` function L690 — `function BR(t)`
-  `CR` function L690 — `function CR(t,e)`
-  `DR` function L690 — `function DR(t)`
-  `ER` function L690 — `function ER(t,e)`
-  `FOe` function L690 — `function FOe(t,e)`
-  `FR` function L690 — `function FR(t)`
-  `G2` function L690 — `function G2(t)`
-  `GOe` function L690 — `function GOe(t)`
-  `GR` function L690 — `function GR(t)`
-  `Gm` function L690 — `function Gm(t)`
-  `H2` function L690 — `function H2(t)`
-  `HOe` function L690 — `function HOe(t)`
-  `HR` function L690 — `function HR(t)`
-  `Ho` function L690 — `function Ho(t)`
-  `IOe` function L690 — `function IOe(t,e,r=lk)`
-  `IR` function L690 — `function IR(t)`
-  `Il` function L690 — `function Il(t)`
-  `JOe` function L690 — `function JOe(t)`
-  `KOe` function L690 — `function KOe(t)`
-  `Kd` function L690 — `function Kd(t)`
-  `LR` function L690 — `function LR(t)`
-  `Lc` function L690 — `function Lc(t)`
-  `Ll` function L690 — `function Ll(t)`
-  `M2` function L690 — `function M2(t)`
-  `MOe` function L690 — `function MOe(t)`
-  `MR` function L690 — `function MR(t)`
-  `Ml` function L690 — `function Ml(t)`
-  `Mu` function L690 — `function Mu(t)`
-  `NOe` function L690 — `function NOe(t)`
-  `NR` function L690 — `function NR(t)`
-  `Nc` function L690 — `function Nc(t,e)`
-  `OOe` function L690 — `function OOe(t,e=!0)`
-  `OR` function L690 — `function OR(t)`
-  `Oa` function L690 — `function Oa(t)`
-  `POe` function L690 — `function POe(t)`
-  `PR` function L690 — `function PR(t)`
-  `Pa` function L690 — `function Pa(t)`
-  `QOe` function L690 — `function QOe(t)`
-  `Qd` function L690 — `function Qd(t)`
-  `RR` function L690 — `function RR(t)`
-  `SR` function L690 — `function SR(t,e)`
-  `UOe` function L690 — `function UOe(t)`
-  `UR` function L690 — `function UR(t)`
-  `V2` function L690 — `function V2(t)`
-  `VOe` function L690 — `function VOe(t)`
-  `VR` function L690 — `function VR(t)`
-  `W2` function L690 — `function W2(t,e)`
-  `WOe` function L690 — `function WOe(t)`
-  `WR` function L690 — `function WR(t)`
-  `Wo` function L690 — `function Wo(t,e)`
-  `XOe` function L690 — `function XOe(t)`
-  `XR` function L690 — `function XR(t,e)`
-  `YOe` function L690 — `function YOe(t)`
-  `YR` function L690 — `function YR(t,e)`
-  `ZOe` function L690 — `function ZOe(t)`
-  `_R` function L690 — `function _R(t,e)`
-  `_g` function L690 — `function _g(t,e)`
-  `af` function L690 — `function af(t)`
-  `ar` function L690 — `function ar(t)`
-  `bk` function L690 — `function bk(t,e)`
-  `cae` function L690 — `function cae(t,e)`
-  `dae` function L690 — `function dae(t)`
-  `dk` function L690 — `function dk(t)`
-  `e` function L690 — `function e(a)`
-  `ePe` function L690 — `function ePe(t,e)`
-  `en` function L690 — `function en(...t)`
-  `ep` function L690 — `function ep(t)`
-  `fae` function L690 — `function fae(t,e=!0)`
-  `fk` function L690 — `function fk(t)`
-  `gk` function L690 — `function gk(t)`
-  `hae` function L690 — `function hae(t,e,r)`
-  `i` function L690 — `function i(a)`
-  `ii` function L690 — `function ii(t)`
-  `jOe` function L690 — `function jOe(t)`
-  `jR` function L690 — `function jR(t)`
-  `jd` function L690 — `function jd(t)`
-  `kR` function L690 — `function kR(t)`
-  `lae` function L690 — `function lae(t)`
-  `mk` function L690 — `function mk(t)`
-  `n` function L690 — `function n(a)`
-  `ok` function L690 — `function ok(t)`
-  `pae` function L690 — `function pae(t)`
-  `pk` function L690 — `function pk(t)`
-  `q2` function L690 — `function q2()`
-  `qOe` function L690 — `function qOe(t)`
-  `qR` function L690 — `function qR(t,e)`
-  `r` function L690 — `function r(a)`
-  `rp` function L690 — `function rp(t)`
-  `sf` function L690 — `function sf(t)`
-  `so` function L690 — `function so(t)`
-  `tPe` function L690 — `function tPe(t,e=Pa(t).parseResult.value)`
-  `tp` function L690 — `function tp(t,e)`
-  `uae` function L690 — `function uae(t,e)`
-  `va` function L690 — `function va(t)`
-  `vk` function L690 — `function vk(t)`
-  `yk` function L690 — `function yk(t)`
-  `zOe` function L690 — `function zOe(t)`
-  `zR` function L690 — `function zR(t)`
-  `Dg` function L698 — `function Dg(t)`
-  `ap` function L698 — `function ap(t)`
-  `bae` function L698 — `function bae(t)`
-  `eN` function L698 — `function eN(t)`
-  `i` function L698 — `function i()`
-  `iPe` function L698 — `function iPe(t)`
-  `l` function L698 — `function l(h)`
-  `rN` function L698 — `function rN(t,e)`
-  `tN` function L698 — `function tN(t)`
-  `u` function L698 — `function u(h)`
-  `$ae` function L701-709 — `function $ae(t,e=!1)`
-  `Aae` function L701 — `function Aae(t)`
-  `Bg` function L701 — `function Bg(t)`
-  `Bs` function L701 — `function Bs(t)`
-  `Cae` function L701 — `function Cae(t,e)`
-  `Eae` function L701 — `function Eae(t,e,r)`
-  `Iae` function L701 — `function Iae(t)`
-  `Ig` function L701 — `function Ig(t)`
-  `Iu` function L701 — `function Iu(t,e)`
-  `J2` function L701 — `function J2(t)`
-  `K2` function L701 — `function K2(t,e)`
-  `Mae` function L701 — `function Mae(t,e,r)`
-  `Mg` function L701 — `function Mg(t,e)`
-  `Ng` function L701 — `function Ng(t)`
-  `Pae` function L701 — `function Pae()`
-  `Pg` function L701 — `function Pg(t)`
-  `Q2` function L701 — `function Q2(t,e,r)`
-  `Rg` function L701 — `function Rg(t)`
-  `Sae` function L701 — `function Sae(t)`
-  `Sk` function L701 — `function Sk(t)`
-  `Tae` function L701 — `function Tae(t)`
-  `Z2` function L701 — `function Z2(t)`
-  `aN` function L701 — `function aN(t)`
-  `aPe` function L701 — `function aPe(t,e)`
-  `bPe` function L701 — `function bPe(t)`
-  `cN` function L701 — `function cN(t,e,r)`
-  `cPe` function L701 — `function cPe(t)`
-  `dPe` function L701 — `function dPe(t)`
-  `e` function L701 — `function e()`
-  `ex` function L701 — `function ex(t)`
-  `fN` function L701 — `function fN(t)`
-  `fPe` function L701 — `function fPe(t)`
-  `gPe` function L701 — `function gPe(t)`
-  `hN` function L701 — `function hN(t)`
-  `hPe` function L701 — `function hPe(t)`
-  `i` function L701 — `function i(a,s)`
-  `iN` function L701 — `function iN(t,e)`
-  `kae` function L701 — `function kae(t,e,r)`
-  `kk` function L701 — `function kk(t)`
-  `lN` function L701 — `function lN(t,e,r,n)`
-  `lPe` function L701 — `function lPe(t)`
-  `mPe` function L701 — `function mPe(t)`
-  `n` function L701 — `function n()`
-  `nN` function L701 — `function nN(t)`
-  `oN` function L701 — `function oN(t,e)`
-  `oPe` function L701 — `function oPe(t)`
-  `op` function L701 — `function op(t)`
-  `pN` function L701 — `function pN(t)`
-  `pPe` function L701 — `function pPe(t)`
-  `rx` function L701 — `function rx(t)`
-  `sN` function L701 — `function sN(t)`
-  `sPe` function L701 — `function sPe(t,e)`
-  `sp` function L701 — `function sp(t,e=[])`
-  `tx` function L701 — `function tx(t)`
-  `uN` function L701 — `function uN(t,e,r)`
-  `uPe` function L701 — `function uPe(t)`
-  `vN` function L701 — `function vN(t)`
-  `vPe` function L701 — `function vPe(t)`
-  `wPe` function L701 — `function wPe(t,e)`
-  `wae` function L701 — `function wae(t)`
-  `xPe` function L701 — `function xPe(t)`
-  `yN` function L701 — `function yN(t)`
-  `yPe` function L701 — `function yPe(t)`
-  `Bae` function L709 — `function Bae(t,e)`
-  `Nk` function L709 — `function Nk(t,e)`
-  `Rk` function L709 — `function Rk(t,e,r)`
-  `TN` function L709 — `function TN(t,e,r)`
-  `kN` function L709 — `function kN(t)`
-  `kPe` function L709 — `function kPe(t,e)`
-  `Uae` function L710-716 — `function Uae(t,e)`
-  `CPe` function L716 — `function CPe(t)`
-  `EPe` function L716 — `function EPe(t)`
-  `Hae` function L716 — `function Hae(t,e)`
-  `SPe` function L716 — `function SPe(t)`
-  `_Pe` function L716-718 — `function _Pe(t)`
-  `e` class L716 — `-`
-  `DPe` function L718 — `function DPe(t)`
-  `RPe` function L718-720 — `function RPe(t)`
-  `e` class L718 — `-`
-  `IPe` function L720 — `function IPe(t)`
-  `MPe` function L720 — `function MPe(t)`
-  `NPe` function L720 — `function NPe(t)`
-  `OPe` function L720 — `function OPe(t,e)`
-  `PPe` function L720-722 — `function PPe(t)`
-  `BPe` function L722 — `function BPe(t,e)`
-  `FPe` function L722 — `function FPe(t)`
-  `Gae` function L722 — `function Gae(t)`
-  `Vae` function L722 — `function Vae(t)`
-  `Wae` function L722-727 — `function Wae(t,e,r)`
-  `qae` function L727-731 — `function qae(t,e,r)`
-  `$Pe` function L731 — `function $Pe(t)`
-  `Kae` function L731 — `function Kae(t,e)`
-  `Xae` function L731 — `function Xae(t)`
-  `Yae` function L731 — `function Yae(t)`
-  `zPe` function L731-736 — `function zPe(t,e)`
-  `Bu` function L736 — `function Bu(t)`
-  `CN` function L736 — `function CN(t,e,r)`
-  `GPe` function L736 — `function GPe()`
-  `HPe` function L736 — `function HPe(t)`
-  `Ic` function L736 — `function Ic(t)`
-  `Jae` function L736 — `function Jae(t)`
-  `Pu` function L736 — `function Pu(t,e)`
-  `Qae` function L736 — `function Qae(t)`
-  `UPe` function L736 — `function UPe(t)`
-  `VPe` function L736 — `function VPe(t)`
-  `WPe` function L736 — `function WPe(t)`
-  `YPe` function L736 — `function YPe(t)`
-  `_N` function L736 — `function _N(t)`
-  `qPe` function L736 — `function qPe(t)`
-  `rse` function L736 — `function rse(t)`
-  `tse` function L736 — `function tse(t,e)`
-  `zg` function L736 — `function zg(t,e)`
-  `$` function L745 — `function $()`
-  `Fu` function L745 — `function Fu(t)`
-  `H` function L745 — `function H(le)`
-  `LN` function L745 — `function LN(t)`
-  `j` function L745 — `function j(le)`
-  `jPe` function L745-746 — `function jPe(t)`
-  `of` function L745 — `function of(t)`
-  `$u` function L746 — `function $u(t,e,r,n,i,a,s,l)`
-  `sx` function L746 — `function sx(t,e)`
-  `r` function L752 — `function r(f)`
-  `$k` function L776 — `function $k(t,e,r,n)`
-  `$se` function L776 — `function $se(t,e,r)`
-  `Ase` function L776 — `function Ase(t,e,r)`
-  `BN` function L776 — `function BN(t,e,r,n=[])`
-  `Cse` function L776 — `function Cse(t,e)`
-  `Dse` function L776 — `function Dse(t)`
-  `Ese` function L776 — `function Ese(t)`
-  `Fk` function L776 — `function Fk(t,e,r=[])`
-  `Fse` function L776 — `function Fse(t,e,r)`
-  `Gk` function L776 — `function Gk(t)`
-  `Hg` function L776 — `function Hg(t,e,r,n)`
-  `JPe` function L776 — `function JPe(t)`
-  `KPe` function L776 — `function KPe(t,e,r,n)`
-  `Lse` function L776 — `function Lse(t)`
-  `MN` function L776 — `function MN(t)`
-  `QPe` function L776 — `function QPe(t,e,r)`
-  `Sse` function L776 — `function Sse(t,e,r)`
-  `Tse` function L776 — `function Tse(t)`
-  `Uk` function L776 — `function Uk(t)`
-  `Vk` function L776 — `function Vk(t,e)`
-  `Vse` function L776-780 — `function Vse(t,e)`
-  `Wg` function L776 — `function Wg(t,e,r,n)`
-  `Wk` function L776 — `function Wk(t,e,r)`
-  `XN` function L776 — `function XN(t,e)`
-  `YN` function L776 — `function YN(t,e)`
-  `ZPe` function L776 — `function ZPe(t,e)`
-  `_se` function L776 — `function _se(t,e,r)`
-  `a` function L776 — `function a(l)`
-  `aBe` function L776 — `function aBe(t,e,r,n,i,a,s)`
-  `bse` function L776 — `function bse(t,e)`
-  `eBe` function L776 — `function eBe(t,e,r,n)`
-  `fse` function L776 — `function fse(t,e)`
-  `gse` function L776 — `function gse(t,e,r,n,i,a)`
-  `iBe` function L776 — `function iBe(t,e,r)`
-  `kse` function L776 — `function kse(t,e,r,n)`
-  `lBe` function L776 — `function lBe(t,e)`
-  `lf` function L776 — `function lf(t)`
-  `mse` function L776 — `function mse(t,e,r,n,i,a)`
-  `nBe` function L776 — `function nBe(t,e,r,n)`
-  `pse` function L776 — `function pse(t)`
-  `qN` function L776 — `function qN(t,e)`
-  `rBe` function L776 — `function rBe(t,e,r,n)`
-  `s` function L776 — `function s(l)`
-  `sBe` function L776 — `function sBe(t)`
-  `tBe` function L776 — `function tBe(t,e)`
-  `ux` function L776 — `function ux(t)`
-  `vse` function L776 — `function vse(t,e,r)`
-  `wse` function L776 — `function wse(t)`
-  `xse` function L776 — `function xse(t,e)`
-  `yse` function L776 — `function yse(t,e,r,n)`
-  `Use` function L780 — `function Use(t,e,r)`
-  `cBe` function L780 — `function cBe(t,e)`
-  `uBe` function L780 — `function uBe(t,e)`
-  `Jse` function L787 — `function Jse(t)`
-  `aE` function L787-788 — `function aE(t)`
-  `dBe` function L787 — `function dBe(t,e)`
-  `yx` function L787 — `function yx(t,e,r,n=!1)`
-  `ioe` function L794 — `function ioe(t,e)`
-  `lE` function L794 — `function lE(t=void 0)`
-  `ABe` function L800 — `function ABe(t,e,r,n)`
-  `Ai` function L800 — `function Ai(t,e)`
-  `BBe` function L800 — `function BBe(t,e,r,n)`
-  `CBe` function L800 — `function CBe(t,e,r)`
-  `DBe` function L800 — `function DBe(t,e)`
-  `EBe` function L800 — `function EBe(t,e,r)`
-  `FBe` function L800-803 — `function FBe(t)`
-  `IBe` function L800 — `function IBe(t)`
-  `JN` function L800 — `function JN(t,e=!0)`
-  `LBe` function L800 — `function LBe(t,e,r)`
-  `MBe` function L800 — `function MBe(t,e)`
-  `NBe` function L800 — `function NBe(t,e)`
-  `OBe` function L800 — `function OBe(t,e,r,n)`
-  `PBe` function L800 — `function PBe(t,e,r,n,i,a)`
-  `QN` function L800 — `function QN(t,e,r,n)`
-  `RBe` function L800 — `function RBe(t,e,r)`
-  `SBe` function L800 — `function SBe(t,e,r)`
-  `TBe` function L800 — `function TBe(t,e,r)`
-  `ZN` function L800 — `function ZN(t,e)`
-  `_Be` function L800 — `function _Be(t)`
-  `aa` function L800 — `function aa(t,e,r,n)`
-  `bBe` function L800 — `function bBe(t,e)`
-  `boe` function L800 — `function boe(t,e=!0)`
-  `doe` function L800 — `function doe(t)`
-  `dp` function L800 — `function dp(t,e,r)`
-  `e1` function L800 — `function e1(t,e,r,n,...i)`
-  `eM` function L800 — `function eM(t,e,r,n)`
-  `fp` function L800 — `function fp(t,e,r)`
-  `goe` function L800 — `function goe(t,e,r,n,i)`
-  `hf` function L800 — `function hf(t,e)`
-  `kBe` function L800 — `function kBe(t,e,r)`
-  `moe` function L800 — `function moe(t,e,r,n,i)`
-  `poe` function L800 — `function poe(t,e,r)`
-  `wBe` function L800 — `function wBe(t,e,r)`
-  `$Be` function L803 — `function $Be(t)`
-  `GBe` function L803 — `function GBe(t,e)`
-  `HBe` function L803 — `function HBe(t,e)`
-  `KBe` function L803 — `function KBe(t)`
-  `QBe` function L803 — `function QBe(t)`
-  `Toe` function L803 — `function Toe(t)`
-  `UBe` function L803 — `function UBe(t,e)`
-  `VBe` function L803 — `function VBe(t,e,r)`
-  `WBe` function L803 — `function WBe(t)`
-  `XBe` function L803 — `function XBe(t)`
-  `YBe` function L803 — `function YBe(t)`
-  `ZBe` function L803 — `function ZBe(t)`
-  `e` function L803 — `function e(r)`
-  `i` function L803 — `function i(a)`
-  `jBe` function L803 — `function jBe(t)`
-  `koe` function L803 — `function koe(t,e)`
-  `n` function L803 — `function n(a)`
-  `qBe` function L803 — `function qBe(t,e)`
-  `r` function L803 — `function r(n)`
-  `uE` function L803 — `function uE(t,e)`
-  `woe` function L803 — `function woe(t,e,r,n)`
-  `zBe` function L803 — `function zBe(t,e,r)`
-  `Dle` function L805 — `function Dle(t,e,r=e.terminal)`
-  `Lle` function L805 — `function Lle(t,e,r,n)`
-  `Nle` function L805 — `function Nle(t)`
-  `Rle` function L805 — `function Rle(t,e)`
-  `Rx` function L805 — `function Rx(t,e,r)`
-  `TM` function L805 — `function TM(t)`
-  `Vu` function L805 — `function Vu(t)`
-  `a` function L805 — `function a(m)`
-  `aFe` function L805 — `function aFe(t,e)`
-  `bM` function L805 — `function bM(t)`
-  `d` function L805 — `function d(m)`
-  `e` function L805 — `function e(h)`
-  `eFe` function L805 — `function eFe(t,e)`
-  `f` function L805 — `function f(m)`
-  `gp` function L805 — `function gp(t,e,r=!1)`
-  `h` function L805 — `function h(m)`
-  `i` function L805 — `function i(m)`
-  `iFe` function L805 — `function iFe(t,e)`
-  `l` function L805 — `function l(m,g,y)`
-  `lFe` function L805 — `function lFe(t,e)`
-  `n` function L805 — `function n(m)`
-  `nFe` function L805 — `function nFe(t,e)`
-  `oFe` function L805 — `function oFe(t,e)`
-  `p` function L805 — `function p(m,g)`
-  `r` function L805 — `function r(m)`
-  `rFe` function L805 — `function rFe(t,e)`
-  `s` function L805 — `function s(m)`
-  `sFe` function L805 — `function sFe(t,e)`
-  `tFe` function L805 — `function tFe(t,e)`
-  `u` function L805 — `function u(m)`
-  `xE` function L805 — `function xE(t)`
-  `xM` function L805 — `function xM(t,e)`
-  `yM` function L805 — `function yM(t)`
-  `$le` function L806 — `function $le(t)`
-  `AFe` function L806 — `function AFe(t,e)`
-  `Bc` function L806 — `function Bc(t)`
-  `C` function L806 — `function C(D)`
-  `CE` function L806 — `function CE()`
-  `CFe` function L806 — `function CFe(t)`
-  `CM` function L806 — `function CM()`
-  `DFe` function L806 — `function DFe(t)`
-  `E` function L806 — `function E(D)`
-  `Gle` function L806 — `function Gle(t)`
-  `IE` function L806 — `function IE(t)`
-  `IM` function L806 — `function IM(t,e)`
-  `Ile` function L806 — `function Ile(t)`
-  `JM` function L806 — `function JM(t)`
-  `Jle` function L806 — `function Jle(t,e)`
-  `Kle` function L806-810 — `function Kle(t)`
-  `LFe` function L806 — `function LFe(t,e,r)`
-  `MM` function L806 — `function MM()`
-  `Mle` function L806 — `function Mle(t)`
-  `Qle` function L806 — `function Qle(t)`
-  `RE` function L806 — `function RE(t)`
-  `RFe` function L806 — `function RFe(t,e)`
-  `SFe` function L806 — `function SFe(t)`
-  `TFe` function L806 — `function TFe(t,e,r,n)`
-  `UM` function L806 — `function UM(t)`
-  `Vle` function L806 — `function Vle(t)`
-  `Wle` function L806 — `function Wle(t)`
-  `Xle` function L806 — `function Xle(t)`
-  `Yle` function L806 — `function Yle(t)`
-  `Zle` function L806 — `function Zle(t)`
-  `_Fe` function L806 — `function _Fe(t,e)`
-  `a` function L806 — `function a(h)`
-  `aI` function L806 — `function aI(t)`
-  `b` function L806 — `function b(D,k)`
-  `bp` function L806 — `function bp(t)`
-  `cFe` function L806 — `function cFe(t)`
-  `dFe` function L806 — `function dFe(t)`
-  `e` function L806 — `function e(r)`
-  `eI` function L806 — `function eI(t)`
-  `ece` function L806 — `function ece(t)`
-  `fFe` function L806 — `function fFe(t)`
-  `g` class L806 — `-`
-  `hFe` function L806 — `function hFe(t)`
-  `i` function L806 — `function i(h)`
-  `iI` function L806 — `function iI(t,e)`
-  `l` function L806 — `function l(h)`
-  `n` function L806 — `function n(h)`
-  `nI` function L806 — `function nI(t,e,r)`
-  `p` class L806 — `-`
-  `qle` function L806 — `function qle(t)`
-  `r` function L806 — `function r(i,a,s)`
-  `rI` function L806 — `function rI(t,e)`
-  `s` function L806 — `function s(h)`
-  `tI` function L806 — `function tI(t,e)`
-  `u` function L806 — `function u(h)`
-  `uFe` function L806 — `function uFe(t)`
-  `v` function L806 — `function v(D,k,L)`
-  `vFe` function L806 — `function vFe(t)`
-  `w` function L806 — `function w(D,k)`
-  `wFe` function L806 — `function wFe(t)`
-  `x` function L806 — `function x(D)`
-  `xi` function L806 — `function xi(t)`
-  `zle` function L806 — `function zle(t,e,r=0)`
-  `FE` function L814 — `function FE(t,e)`
-  `ace` function L814 — `function ace(t,e)`
-  `ds` function L814 — `function ds(t)`
-  `fs` function L814 — `function fs(t)`
-  `ice` function L814 — `function ice(t)`
-  `rce` function L814 — `function rce(t,e,r,n)`
-  `ui` function L814 — `function ui(t,e,r,n,i,a,s,l,u)`
-  `Ace` function L815 — `function Ace(t)`
-  `Cce` function L815 — `function Cce(t)`
-  `Dce` function L815 — `function Dce(t)`
-  `Hu` function L815 — `function Hu(t)`
-  `IFe` function L815 — `function IFe()`
-  `Ice` function L815 — `function Ice(t)`
-  `Lce` function L815 — `function Lce(t)`
-  `Mce` function L815 — `function Mce(t)`
-  `Nce` function L815 — `function Nce(t)`
-  `Oce` function L815 — `function Oce(t)`
-  `Rce` function L815 — `function Rce(t)`
-  `Sce` function L815 — `function Sce(t)`
-  `_ce` function L815 — `function _ce(t)`
-  `KE` function L816 — `function KE(t=ps)`
-  `XE` function L816 — `function XE(t=ps)`
-  `ZE` function L816 — `function ZE(t=ps)`
-  `e6` function L816 — `function e6(t=ps)`
-  `i6` function L816 — `function i6(t=ps)`
-  `r6` function L816 — `function r6(t=ps)`
-  `uo` function L816 — `function uo(t,e)`
-  `$c` function L818 — `function $c(t,e)`
-  `Qce` function L818 — `function Qce(t)`
-  `jce` function L818 — `function jce(t,e,r)`
-  `u$e` function L818 — `function u$e(t,e)`
-  `zI` function L818 — `function zI()`
-  `se` function L882 — `function se(W)`
-  `ue` function L882 — `function ue()`
-  `A` function L887 — `function A(S)`
-  `C` function L887 — `function C(S,_,I,D)`
-  `E` function L887 — `function E(S,_,I,D)`
-  `Nue` function L887 — `function Nue(t,e,r)`
-  `T` function L887 — `function T(S,_)`
-  `b` function L887 — `function b(S,_,I,D,k,L,R)`
-  `k` function L887 — `function k()`
-  `m` function L887 — `function m(g)`
-  `v` function L887 — `function v(S,_)`
-  `w` function L887 — `function w(S,_,I,D,k,L,R,O)`
-  `x` function L887 — `function x(S,_,I)`
-  `ut` function L1168 — `function ut()`
-  `xt` function L1168 — `function xt(Ce)`
-  `AGe` function L1173 — `function AGe()`
-  `CGe` function L1173 — `function CGe(t)`
-  `EGe` function L1173 — `function EGe(t,e)`
-  `SGe` function L1173 — `function SGe(t)`
-  `TGe` function L1173 — `function TGe(t)`
-  `V` function L1173 — `function V()`
-  `Xu` function L1173 — `function Xu(t)`
-  `a` function L1173 — `function a(S)`
-  `bGe` function L1173 — `function bGe(t)`
-  `dO` function L1173 — `function dO(t)`
-  `dhe` function L1173 — `function dhe(t)`
-  `fO` function L1173 — `function fO(t)`
-  `gGe` function L1173 — `function gGe(t)`
-  `i` function L1173 — `function i(S)`
-  `kGe` function L1173 — `function kGe(t,e,r,n,i)`
-  `mGe` function L1173 — `function mGe(t)`
-  `phe` function L1173 — `function phe(t)`
-  `s` function L1173 — `function s(S)`
-  `vGe` function L1173 — `function vGe(t)`
-  `wGe` function L1173 — `function wGe(t)`
-  `xGe` function L1173 — `function xGe(t)`
-  `yGe` function L1173 — `function yGe(t)`
-  `Se` function L1174 — `function Se()`
-  `Z` function L1174 — `function Z(re)`
-  `$Ge` function L1179 — `function $Ge(t,e)`
-  `$he` function L1179 — `function $he()`
-  `BGe` function L1179 — `function BGe(t)`
-  `Dhe` function L1179 — `function Dhe(t,e,r,n)`
-  `FGe` function L1179 — `function FGe(t)`
-  `Fhe` function L1179 — `function Fhe()`
-  `GGe` function L1179 — `function GGe(t,e)`
-  `Ghe` function L1179 — `function Ghe(t,e)`
-  `HGe` function L1179 — `function HGe()`
-  `IGe` function L1179 — `function IGe(t)`
-  `Mhe` function L1179 — `function Mhe(t,e,r)`
-  `OGe` function L1179 — `function OGe(t)`
-  `PGe` function L1179 — `function PGe(t)`
-  `R` function L1179 — `function R()`
-  `S1` function L1179 — `function S1(t)`
-  `UGe` function L1179 — `function UGe()`
-  `Uhe` function L1179 — `function Uhe(t)`
-  `VGe` function L1179 — `function VGe(t,e)`
-  `Vhe` function L1179 — `function Vhe(t)`
-  `WGe` function L1179 — `function WGe()`
-  `h` function L1179 — `function h(v)`
-  `kO` function L1179 — `function kO(t)`
-  `l` function L1179 — `function l(v)`
-  `mO` function L1179 — `function mO(t)`
-  `u` function L1179 — `function u(v)`
-  `v6` function L1179 — `function v6(t)`
-  `vO` function L1179 — `function vO(t,e,r,n)`
-  `zGe` function L1179 — `function zGe(t)`
-  `zhe` function L1179 — `function zhe()`
-  `Yt` function L1180 — `function Yt(Dr)`
-  `bt` function L1180 — `function bt()`
-  `y` function L1180 — `function y(v)`
-  `de` function L1185 — `function de()`
-  `oe` function L1243 — `function oe()`
-  `re` function L1243 — `function re(Rt)`
-  `he` function L1248 — `function he()`
-  `Hc` function L1364 — `function Hc(t,e,r,n,i)`
-  `MVe` function L1364 — `function MVe(t,e,r)`
-  `OVe` function L1364 — `function OVe(t,e,r)`
-  `RVe` function L1364 — `function RVe(t,e,r,n,i,a,s)`
-  `T` function L1364 — `function T(F,P)`
-  `Ue` function L1364 — `function Ue(Tt)`
-  `_Ve` function L1364 — `function _Ve(t,e)`
-  `ct` function L1364 — `function ct()`
-  `e` function L1364 — `function e(a,s,l,u,h,f,d,p)`
-  `i` function L1364 — `function i(a,s)`
-  `l` function L1364 — `function l(h,f)`
-  `n` function L1364 — `function n(a,s,l,u,h,f,d,p)`
-  `r` function L1364 — `function r(i,a,s,l,u)`
-  `s` function L1364 — `function s(l)`
-  `t` function L1364 — `function t(a,s,l,u,h,f,d)`
-  `u` function L1364 — `function u(h,f)`
-  `Re` function L1369 — `function Re()`
-  `Oe` function L1527 — `function Oe()`
-  `ae` function L1527 — `function ae(xe)`
-  `B` function L1532 — `function B()`
-  `G6` function L1532 — `function G6(t,e,r)`
-  `ZO` function L1532 — `function ZO(t="",e=0,r="",n=$6)`
-  `lUe` function L1532 — `function lUe(t)`
-  `oUe` function L1532 — `function oUe(t)`
-  `ude` function L1532 — `function ude()`
-  `M` function L1746 — `function M()`
-  `O` function L1746 — `function O(K)`
-  `f` function L1751 — `function f()`
-  `B` function L1883 — `function B()`
-  `JUe` function L1883 — `function JUe(t)`
-  `M` function L1883 — `function M(X)`
-  `a` function L1883 — `function a(u)`
-  `e` function L1883 — `function e(i,a,s,l,u,h,f,d,p)`
-  `l` function L1883 — `function l(u)`
-  `n` function L1883 — `function n(i,a)`
-  `r` function L1883 — `function r(i,a,s,l,u)`
-  `s` function L1883 — `function s(u)`
-  `t` function L1883 — `function t(i,a,s,l,u,h,f,d)`
-  `a` function L1888 — `function a(u)`
-  `d` function L1888 — `function d()`
-  `dpe` function L1888 — `function dpe(t,e)`
-  `e` function L1888 — `function e(i,a,s,l,u,h,f,d,p)`
-  `l` function L1888 — `function l(u)`
-  `n` function L1888 — `function n(i,a)`
-  `r` function L1888 — `function r(i,a,s,l,u)`
-  `s` function L1888 — `function s(u)`
-  `t` function L1888 — `function t(i,a,s,l,u,h,f,d)`
-  `ie` function L1943 — `function ie()`
-  `j` function L1943 — `function j(ae)`
-  `Dpe` function L1948 — `function Dpe(t,e)`
-  `HHe` function L1948 — `function HHe(t)`
-  `If` function L1948 — `function If(t,e,r)`
-  `Mf` function L1948 — `function Mf(t,e)`
-  `OP` function L1948 — `function OP(t,e)`
-  `UHe` function L1948 — `function UHe(t)`
-  `WHe` function L1948 — `function WHe(t)`
-  `Wi` function L1948 — `function Wi(t)`
-  `X0e` function L1948 — `function X0e(t,e,r)`
-  `YHe` function L1948-1949 — `function YHe()`
-  `ZP` function L1948 — `function ZP(t,e)`
-  `_i` function L1948 — `function _i(t,e)`
-  `j0e` function L1948 — `function j0e(t)`
-  `qHe` function L1948 — `function qHe(t,e)`
-  `w` function L1948 — `function w()`
-  `XHe` function L1949-1950 — `function XHe()`
-  `mo` function L1950-1951 — `function mo(t,e)`
-  `$0e` function L1951 — `function $0e(t,e,r)`
-  `$1` function L1951 — `function $1(t,e,r,n,i)`
-  `$S` function L1951 — `function $S()`
-  `$Xe` function L1951 — `function $Xe(t,e)`
-  `$Ze` function L1951 — `function $Ze(t,e)`
-  `$ge` function L1951 — `function $ge(t,e)`
-  `$me` function L1951 — `function $me(t,e,r)`
-  `A` function L1951 — `function A()`
-  `AP` function L1951 — `function AP(t,e)`
-  `AXe` function L1951 — `function AXe(t)`
-  `Age` function L1951 — `function Age(t)`
-  `Aje` function L1951 — `function Aje(t,e)`
-  `BWe` function L1951 — `function BWe(t)`
-  `BZe` function L1951 — `function BZe(t,e,r,n,i)`
-  `C` function L1951 — `function C()`
-  `Cge` function L1951 — `function Cge(t,e,r)`
-  `Cje` function L1951 — `function Cje(t)`
-  `DS` function L1951 — `function DS(t,e,r)`
-  `DZe` function L1951 — `function DZe(t)`
-  `Dje` function L1951 — `function Dje(t,e,r)`
-  `Eje` function L1951 — `function Eje(t,e)`
-  `F1` function L1951 — `function F1(t,e)`
-  `FXe` function L1951 — `function FXe(t,e)`
-  `FZe` function L1951 — `function FZe(t,e,r)`
-  `Fge` function L1951 — `function Fge(t,e,r)`
-  `Fme` function L1951 — `function Fme(t,e,r)`
-  `GS` function L1951 — `function GS(t,e)`
-  `GWe` function L1951 — `function GWe(t)`
-  `Gb` function L1951 — `function Gb()`
-  `H0e` function L1951 — `function H0e(t,e)`
-  `HS` function L1951 — `function HS(t)`
-  `HXe` function L1951 — `function HXe(t)`
-  `HZe` function L1951 — `function HZe(t,e,r,n,i)`
-  `Hje` function L1951 — `function Hje(t,e,r)`
-  `IS` function L1951 — `function IS(t,e,r,n,i,a)`
-  `IXe` function L1951 — `function IXe(t,e)`
-  `IZe` function L1951 — `function IZe(t,e)`
-  `Ime` function L1951 — `function Ime(t)`
-  `JXe` function L1951 — `function JXe()`
-  `Jme` function L1951 — `function Jme(t,e,r)`
-  `KWe` function L1951 — `function KWe(t)`
-  `LZe` function L1951 — `function LZe(t)`
-  `Lb` function L1951 — `function Lb()`
-  `Lge` function L1951 — `function Lge(t,e)`
-  `MWe` function L1951 — `function MWe(t)`
-  `MZe` function L1951 — `function MZe(t)`
-  `Mge` function L1951 — `function Mge(t,e,r)`
-  `NP` function L1951 — `function NP(t,e,r,n,i)`
-  `NZe` function L1951 — `function NZe(t)`
-  `Nje` function L1951 — `function Nje(t,e,r)`
-  `OZe` function L1951 — `function OZe(t)`
-  `Oge` function L1951 — `function Oge(t)`
-  `PXe` function L1951 — `function PXe()`
-  `PZe` function L1951 — `function PZe(t)`
-  `Pje` function L1951 — `function Pje(t,e,r)`
-  `RP` function L1951 — `function RP(t,e,r,n,i)`
-  `RXe` function L1951 — `function RXe(t)`
-  `RZe` function L1951 — `function RZe(t)`
-  `Rb` function L1951 — `function Rb(t,e)`
-  `Rge` function L1951 — `function Rge(t,e,r)`
-  `SP` function L1951 — `function SP(t)`
-  `SWe` function L1951 — `function SWe(t)`
-  `SZe` function L1951 — `function SZe(t,e)`
-  `Sge` function L1951 — `function Sge(t,e)`
-  `T` function L1951 — `function T()`
-  `TB` function L1951 — `function TB(t,e,r)`
-  `TXe` function L1951 — `function TXe(t)`
-  `TZe` function L1951 — `function TZe(t,e,r)`
-  `UKe` function L1951 — `function UKe(t,e,r,n)`
-  `VWe` function L1951 — `function VWe(t)`
-  `VXe` function L1951 — `function VXe(t)`
-  `Vje` function L1951 — `function Vje(t,e,r,n)`
-  `WKe` function L1951 — `function WKe(t,e,r,n)`
-  `WWe` function L1951 — `function WWe(t)`
-  `WZe` function L1951 — `function WZe(t,e,r,n)`
-  `XXe` function L1951 — `function XXe(t,e)`
-  `Xje` function L1951 — `function Xje(t)`
-  `Y0e` function L1951 — `function Y0e(t)`
-  `ZYe` function L1951 — `function ZYe(t)`
-  `_Qe` function L1951 — `function _Qe(t,e,r)`
-  `_We` function L1951 — `function _We(t)`
-  `_ge` function L1951 — `function _ge(t,e,r)`
-  `a` function L1951 — `function a(f)`
-  `aXe` function L1951 — `function aXe(t)`
-  `aZe` function L1951 — `function aZe(t,e,r)`
-  `age` function L1951 — `function age(t)`
-  `ay` function L1951 — `function ay(t)`
-  `b` function L1951 — `function b(E)`
-  `b0e` function L1951 — `function b0e(t,e)`
-  `bXe` function L1951 — `function bXe()`
-  `bZe` function L1951 — `function bZe(t)`
-  `bje` function L1951 — `function bje(t,e)`
-  `cB` function L1951 — `function cB(t,e)`
-  `dB` function L1951 — `function dB(t)`
-  `dKe` function L1951 — `function dKe(t,e,r)`
-  `e` function L1951 — `function e(n,i,a)`
-  `eqe` function L1951 — `function eqe(t,e,r)`
-  `f` function L1951 — `function f(g,y,v)`
-  `f0e` function L1951 — `function f0e(t)`
-  `fB` function L1951 — `function fB(t,e,r,n)`
-  `fge` function L1951 — `function fge(t)`
-  `g` function L1951 — `function g(E)`
-  `g0e` function L1951 — `function g0e(t)`
-  `gB` function L1951 — `function gB(t)`
-  `gXe` function L1951 — `function gXe(t,e)`
-  `gZe` function L1951 — `function gZe(t)`
-  `hge` function L1951 — `function hge(t,e)`
-  `i` function L1951 — `function i(f,d)`
-  `iZe` function L1951 — `function iZe(t,e)`
-  `ije` function L1951 — `function ije(t)`
-  `kje` function L1951 — `function kje(t)`
-  `l` function L1951 — `function l(f)`
-  `lS` function L1951 — `function lS(t,e)`
-  `lge` function L1951 — `function lge(t)`
-  `lje` function L1951 — `function lje(t)`
-  `m` function L1951 — `function m(E)`
-  `mZe` function L1951 — `function mZe(t,e,r)`
-  `mje` function L1951 — `function mje(t)`
-  `n` function L1951 — `function n(i)`
-  `nge` function L1951 — `function nge(t)`
-  `nje` function L1951 — `function nje(t,e)`
-  `ny` function L1951 — `function ny(t)`
-  `oS` function L1951 — `function oS(t,e,r)`
-  `oZe` function L1951 — `function oZe(t,e,r,n)`
-  `oge` function L1951 — `function oge(t)`
-  `p` function L1951 — `function p(R,O)`
-  `pXe` function L1951 — `function pXe(t)`
-  `pZe` function L1951 — `function pZe(t,e,r)`
-  `po` function L1951 — `function po(t,e,r,n)`
-  `qKe` function L1951 — `function qKe(t,e,r,n)`
-  `qXe` function L1951 — `function qXe(t)`
-  `qYe` function L1951 — `function qYe(t,e)`
-  `qje` function L1951 — `function qje(t,e)`
-  `r` function L1951 — `function r(s)`
-  `rS` function L1951 — `function rS()`
-  `rge` function L1951 — `function rge(t)`
-  `ry` function L1951 — `function ry(t)`
-  `s` function L1951 — `function s(R)`
-  `sZe` function L1951 — `function sZe(t,e,r,n,i)`
-  `sge` function L1951 — `function sge(t)`
-  `sje` function L1951 — `function sje(t)`
-  `t` function L1951 — `function t()`
-  `tXe` function L1951 — `function tXe(t)`
-  `tge` function L1951 — `function tge(t)`
-  `tje` function L1951 — `function tje(t)`
-  `ty` function L1951 — `function ty(t)`
-  `u` function L1951 — `function u(R)`
-  `uje` function L1951 — `function uje(t,e)`
-  `v` function L1951 — `function v(E)`
-  `v0e` function L1951 — `function v0e(t)`
-  `vB` function L1951 — `function vB(t,e,r,n)`
-  `vWe` function L1951 — `function vWe(t,e)`
-  `vXe` function L1951 — `function vXe(t,e)`
-  `vZe` function L1951 — `function vZe(t,e,r,n,i,a)`
-  `w` function L1951 — `function w()`
-  `w0e` function L1951 — `function w0e(t,e)`
-  `wB` function L1951 — `function wB(t)`
-  `wZe` function L1951 — `function wZe(t,e,r)`
-  `x` function L1951 — `function x()`
-  `x0e` function L1951 — `function x0e(t,e,r,n,i)`
-  `xZe` function L1951 — `function xZe(t,e,r,n)`
-  `y` function L1951 — `function y(E)`
-  `y0e` function L1951 — `function y0e(t)`
-  `yWe` function L1951 — `function yWe(t)`
-  `yZe` function L1951 — `function yZe(t,e)`
-  `z0e` function L1951 — `function z0e(t,e,r)`
-  `zS` function L1951 — `function zS(t,e)`
-  `zje` function L1951 — `function zje(t,e)`
-  `D` function L1954 — `function D(K,X,te,J,se)`
-  `k` function L1954 — `function k(K,X)`
-  `r` function L1954 — `function r(n)`
-  `t` function L1954 — `function t(e)`
-  `JZe` function L2155 — `function JZe(t,e,r,n,i)`
-  `a` function L2155 — `function a(s)`
-  `h` function L2155 — `function h(d,p,m,g)`
-  `i` function L2155 — `function i()`
-  `l` function L2155 — `function l(u,h)`
-  `m` function L2155 — `function m(v)`
-  `n` function L2155 — `function n()`
-  `p` function L2155 — `function p(g,y,v)`
-  `r` function L2155 — `function r(n)`
-  `s` function L2155 — `function s(u,h,f)`
-  `t` function L2155 — `function t(e)`
-  `w` function L2155 — `function w()`
-  `Yge` function L2188 — `function Yge(t,e,r,n)`
-  `iJe` function L2188 — `function iJe(t,e)`
-  `nJe` function L2188 — `function nJe(t,e)`
-  `qge` function L2188 — `function qge(t,e,r,n,i)`
-  `rJe` function L2188 — `function rJe(t,e)`
-  `he` function L2244 — `function he()`
-  `le` function L2244 — `function le(ze)`
-  `E` function L2249 — `function E()`
-  `D` function L2331 — `function D(ie)`
-  `k` function L2331 — `function k()`
-  `$B` function L2336 — `function $B(t)`
-  `$Je` function L2336 — `function $Je(t)`
-  `A` function L2336 — `function A(O,M,B)`
-  `A1e` function L2336 — `function A1e()`
-  `BB` function L2336 — `function BB(t)`
-  `BJe` function L2336 — `function BJe(t)`
-  `C` function L2336 — `function C(O)`
-  `D` function L2336 — `function D({sourceLinks:O,targetLinks:M})`
-  `E` function L2336 — `function E(O,M,B)`
-  `E1e` function L2336 — `function E1e(t,e)`
-  `FB` function L2336 — `function FB(t,e)`
-  `FJe` function L2336 — `function FJe(t)`
-  `GB` function L2336 — `function GB(t)`
-  `GJe` function L2336 — `function GJe(t)`
-  `HB` function L2336 — `function HB()`
-  `HJe` function L2336 — `function HJe(t,e,r,n,i)`
-  `I` function L2336 — `function I(O,M,B,F)`
-  `JS` function L2336 — `function JS()`
-  `KS` function L2336 — `function KS(t,e)`
-  `L` function L2336 — `function L(O,M)`
-  `N1e` function L2336 — `function N1e(t)`
-  `PJe` function L2336 — `function PJe(t)`
-  `QS` function L2336 — `function QS()`
-  `R` function L2336 — `function R(O,M)`
-  `R1e` function L2336 — `function R1e(t)`
-  `S` function L2336 — `function S(O,M)`
-  `S1e` function L2336 — `function S1e({nodes:t})`
-  `T` function L2336 — `function T(O)`
-  `T1e` function L2336 — `function T1e(t,e)`
-  `UJe` function L2336 — `function UJe(t)`
-  `VJe` function L2336 — `function VJe(t)`
-  `WJe` function L2336 — `function WJe(t)`
-  `ZS` function L2336 — `function ZS(t)`
-  `_` function L2336 — `function _(O,M,B,F)`
-  `b` function L2336 — `function b({nodes:O})`
-  `cy` function L2336 — `function cy(t,e)`
-  `g` function L2336 — `function g()`
-  `hy` function L2336 — `function hy(t)`
-  `k` function L2336 — `function k(O)`
-  `k1e` function L2336 — `function k1e(t,e)`
-  `m4` function L2336 — `function m4(t,e)`
-  `p4` function L2336 — `function p4(t,e)`
-  `qB` function L2336 — `function qB()`
-  `qJe` function L2336 — `function qJe(t)`
-  `s` function L2336 — `function s()`
-  `uy` function L2336 — `function uy(t,e)`
-  `v` function L2336 — `function v({nodes:O})`
-  `w` function L2336 — `function w({nodes:O})`
-  `x` function L2336 — `function x({nodes:O})`
-  `y` function L2336 — `function y({nodes:O,links:M})`
-  `Iet` function L2363 — `function Iet(t,e,r,n)`
-  `Met` function L2363 — `function Met(t,e)`
-  `Net` function L2363 — `function Net(t,e,r,n)`
-  `Ret` function L2363 — `function Ret(t,e,r,n,i,a,s)`
-  `Q` function L2405 — `function Q(ce)`
-  `j` function L2405 — `function j()`
-  `Xet` function L2410 — `function Xet(t)`
-  `Yet` function L2410 — `function Yet(t)`
-  `b` function L2410 — `function b()`
-  `jet` function L2410 — `function jet(t)`
-  `Bye` function L2522 — `function Bye(t,e)`
-  `Iye` function L2522 — `function Iye(t,{minX:e,minY:r,maxX:n,maxY:i}={minX:0,minY:0,maxX:0,maxY:0})`
-  `Mye` function L2522 — `function Mye(t,e)`
-  `Oye` function L2522 — `function Oye(t)`
-  `eF` function L2522 — `function eF(t,e,r=0,n=0)`
-  `nC` function L2522 — `function nC(t,e)`
-  `wtt` function L2522 — `function wtt(t)`
-  `xtt` function L2522 — `function xtt(t,e)`
-  `Att` function L2525 — `function Att(t,e)`
-  `C` function L2525 — `function C()`
-  `Dtt` function L2525 — `function Dtt(t,e,r)`
-  `Hl` function L2525 — `function Hl(t,e,r,n)`
-  `Li` function L2525 — `function Li(t)`
-  `Ltt` function L2525 — `function Ltt(t,e,r,n)`
-  `Rtt` function L2525 — `function Rtt(t,e,r)`
-  `Zye` function L2525 — `function Zye(t,e)`
-  `_tt` function L2525 — `function _tt(t,e,r,n)`
-  `aF` function L2525 — `function aF(t,e,r,n)`
-  `dve` function L2525 — `function dve(t,e,r=!1)`
-  `gve` function L2525 — `function gve(t,e,r,n,i)`
-  `mve` function L2525 — `function mve(t,e,r)`
-  `oF` function L2525 — `function oF(t,e,r,n)`
-  `pve` function L2525 — `function pve(t,e,r)`
-  `rrt` function L2525 — `function rrt(t,e,r)`
-  `trt` function L2525 — `function trt(t,e,r)`
-  `I` function L2556 — `function I()`
-  `Pe` function L2556 — `function Pe(st,Ue)`
-  `a` function L2556 — `function a(s)`
-  `b` function L2556 — `function b(w,C)`
-  `d` function L2556 — `function d(p,m)`
-  `f` function L2556 — `function f(p,m,g,y)`
-  `h` function L2556 — `function h(d,p,m,g)`
-  `i` function L2556 — `function i()`
-  `l` function L2556 — `function l(u,h)`
-  `m` function L2556 — `function m()`
-  `n` function L2556 — `function n()`
-  `p` function L2556 — `function p(g,y,v)`
-  `r` function L2556 — `function r(n)`
-  `s` function L2556 — `function s(u,h,f)`
-  `u` function L2556 — `function u(g)`
-  `Art` function L2560 — `function Art(t,e)`
-  `Crt` function L2560 — `function Crt(t,e)`
-  `Drt` function L2560 — `function Drt(t,e)`
-  `Lrt` function L2560 — `function Lrt(t,e,r)`
-  `Nrt` function L2560 — `function Nrt(t,e,r,n,i,{spatialMaps:a,groupAlignments:s})`
-  `Rrt` function L2560 — `function Rrt(t)`
-  `Srt` function L2560 — `function Srt(t,e)`
-  `_rt` function L2560 — `function _rt(t,e)`
-  `m` function L2560 — `function m(g,y,v,x)`
-  `$f` function L2561 — `function $f(t,e)`
-  `A4` function L2561 — `function A4()`
-  `C4` function L2561 — `function C4(t,e,r)`
-  `Frt` function L2561 — `function Frt(t,e,r,n)`
-  `a2e` function L2561 — `function a2e()`
-  `al` function L2561 — `function al()`
-  `c2e` function L2561 — `function c2e(t,e)`
-  `d2e` function L2561 — `function d2e(t,e,r,n,i)`
-  `dC` function L2561 — `function dC(t,e,r,n,i,a,s,l)`
-  `f2e` function L2561 — `function f2e(t,e,r,n,i,a,s,l,u,h,f,d)`
-  `g2e` function L2561 — `function g2e(t,e)`
-  `gC` function L2561 — `function gC(t,e,r,n,i,a,s,l,u)`
-  `h2e` function L2561 — `function h2e(t)`
-  `hC` function L2561 — `function hC(t)`
-  `i2e` function L2561 — `function i2e()`
-  `l2e` function L2561 — `function l2e(t)`
-  `m2e` function L2561 — `function m2e(t,e,r,n)`
-  `mC` function L2561 — `function mC(t)`
-  `my` function L2561 — `function my(t,e)`
-  `o2e` function L2561 — `function o2e(t)`
-  `p2e` function L2561 — `function p2e(t)`
-  `pC` function L2561 — `function pC(t,e)`
-  `r2e` function L2561 — `function r2e(t)`
-  `rh` function L2561 — `function rh()`
-  `s2e` function L2561 — `function s2e(t)`
-  `t2e` function L2561 — `function t2e(t,e,r)`
-  `u2e` function L2561 — `function u2e(t,e)`
-  `vo` function L2561 — `function vo(t)`
-  `xF` function L2561 — `function xF(t)`
-  `y2e` function L2561 — `function y2e(t,e,r,n)`
-  `yC` function L2561 — `function yC(t,e)`
-  `yy` function L2561 — `function yy(t)`
-  `zf` function L2561 — `function zf(t,e,r)`
-  `w2e` function L2562 — `function w2e(t)`
-  `C2e` function L2563 — `function C2e(t)`
-  `T2e` function L2563 — `function T2e(t)`
-  `bF` function L2563 — `function bF(t)`
-  `int` function L2563 — `function int(t,e)`
-  `S2e` function L2569 — `function S2e(t,e)`
-  `fnt` function L2569 — `function fnt(t,e,r,n)`
-  `hnt` function L2569 — `function hnt(t={})`

#### docs/book/searcher.js

-  `hasFocus` function L56-58 — `function hasFocus()`
-  `removeChildren` function L60-64 — `function removeChildren(elem)`
-  `parseURL` function L67-91 — `function parseURL(url)`
-  `renderURL` function L94-111 — `function renderURL(urlobject)`
-  `formatSearchMetric` function L130-138 — `function formatSearchMetric(count, searchterm)`
-  `formatSearchResult` function L140-159 — `function formatSearchResult(result, searchterms)`
-  `makeTeaser` function L161-257 — `function makeTeaser(body, searchterms)`
-  `init` function L259-289 — `function init(config)`
-  `unfocusSearchbar` function L291-298 — `function unfocusSearchbar()`
-  `doSearchOrMarkFromUrl` function L301-334 — `function doSearchOrMarkFromUrl()`
-  `hide` function L321-328 — `const hide = ()`
-  `globalKeyHandler` function L337-402 — `function globalKeyHandler(e)`
-  `showSearch` function L404-416 — `function showSearch(yes)`
-  `showResults` function L418-424 — `function showResults(yes)`
-  `searchIconClickHandler` function L427-435 — `function searchIconClickHandler()`
-  `searchbarKeyUpHandler` function L438-453 — `function searchbarKeyUpHandler()`
-  `setSearchUrlParameters` function L459-482 — `function setSearchUrlParameters(searchterm, action)`
-  `doSearch` function L484-514 — `function doSearch(searchterm)`
-  `loadScript` function L516-525 — `function loadScript(url, id)`

#### docs/book/toc.js

- pub `constructor` method L7-9 — `constructor()`
- pub `connectedCallback` method L10-68 — `connectedCallback()`
-  `MDBookSidebarScrollbox` class L6-69 — `-`
-  `toggleSection` function L62-64 — `function toggleSection(ev)`

### docs

> *Semantic summary to be generated by AI agent.*

#### docs/mermaid.min.js

- pub `constructor` method L716 — `constructor()`
- pub `visitEndAnchor` method L716 — `visitEndAnchor(a)`
- pub `constructor` method L718 — `constructor()`
- pub `visitStartAnchor` method L718 — `visitStartAnchor(a)`
- pub `constructor` method L806 — `constructor(k,L,R,O,M,B=!1)`
- pub `file` method L806 — `file(k)`
- pub `from` method L806 — `from(k)`
- pub `fsPath` method L806 — `fsPath()`
- pub `isUri` method L806 — `isUri(k)`
- pub `parse` method L806 — `parse(k,L=!1)`
- pub `revive` method L806 — `revive(k)`
- pub `toJSON` method L806 — `toJSON()`
- pub `toString` method L806 — `toString(k=!1)`
- pub `with` method L806 — `with(k)`
-  `$F` function L3 — `function $F(t,e,r)`
-  `B4` function L3-8 — `function B4(t)`
-  `DC` function L3 — `function DC(t,e)`
-  `I4` function L3 — `function I4(t,e,r,n)`
-  `IF` function L3 — `function IF(t,e)`
-  `LC` function L3 — `function LC(t,e,r)`
-  `MC` function L3 — `function MC(t,e)`
-  `NC` function L3 — `function NC(t,e=Q2e)`
-  `PF` function L3 — `function PF(t,e,r)`
-  `RC` function L3 — `function RC(t,e)`
-  `RF` function L3 — `function RF(t,e)`
-  `X2e` function L3 — `function X2e(t,e)`
-  `Y2e` function L3 — `function Y2e(t,e="defs")`
-  `_C` function L3 — `function _C(t,e)`
-  `a` function L3 — `function a(s)`
-  `axe` function L3 — `function axe()`
-  `cxe` function L3 — `function cxe()`
-  `e` function L3 — `function e(f)`
-  `h` function L3 — `function h()`
-  `i` function L3 — `function i(f)`
-  `ixe` function L3 — `function ixe(t)`
-  `k` function L3 — `function k(R)`
-  `l` function L3 — `function l(f)`
-  `lxe` function L3 — `function lxe()`
-  `n` function L3 — `function n(f,d)`
-  `nxe` function L3 — `function nxe(t)`
-  `oxe` function L3 — `function oxe(t)`
-  `r` function L3 — `function r(f)`
-  `rxe` function L3 — `function rxe(t)`
-  `s` function L3 — `function s(l)`
-  `sxe` function L3 — `function sxe(t)`
-  `txe` function L3 — `function txe(t)`
-  `u` function L3 — `function u(f)`
-  `y` function L3 — `function y(...v)`
-  `Rt` function L9 — `function Rt(nt)`
-  `st` function L9 — `function st()`
-  `$xe` function L14 — `function $xe(t)`
-  `Cr` function L14 — `function Cr(t,e)`
-  `He` function L14 — `function He()`
-  `Iy` function L14 — `function Iy(t,e)`
-  `Ka` function L14 — `function Ka(t)`
-  `Qf` function L14 — `function Qf(t)`
-  `Tt` function L14 — `function Tt(At,Ce,tt)`
-  `r` function L14 — `function r()`
-  `sz` function L14-15 — `function sz()`
-  `zxe` function L14 — `function zxe(t)`
-  `$z` function L15 — `function $z(t)`
-  `Abe` function L15 — `function Abe(t,e)`
-  `C` function L15 — `function C()`
-  `G` function L15 — `function G(t,e,r,n,i,a)`
-  `Nbe` function L15 — `function Nbe(t)`
-  `Nt` function L15 — `function Nt(t)`
-  `P7` function L15 — `function P7(t,e,r)`
-  `Ql` function L15 — `function Ql(t)`
-  `Qz` function L15 — `function Qz(t)`
-  `SG` function L15 — `function SG(t,e,r)`
-  `W7` function L15 — `function W7(t)`
-  `_7` function L15 — `function _7(t,e)`
-  `_be` function L15 — `function _be(t)`
-  `a3` function L15 — `function a3(t,e)`
-  `abe` function L15 — `function abe(t)`
-  `bz` function L15 — `function bz(t)`
-  `cbe` function L15 — `function cbe(t)`
-  `d4e` function L15 — `function d4e(t,e,r)`
-  `fe` function L15 — `function fe(t,e)`
-  `gbe` function L15 — `function gbe(t)`
-  `gz` function L15 — `function gz(t,e,r,n,i)`
-  `hz` function L15 — `function hz(t)`
-  `k3` function L15 — `function k3(t,e)`
-  `p4e` function L15 — `function p4e(t)`
-  `ph` function L15 — `function ph(t,e,r)`
-  `q7` function L15 — `function q7(t)`
-  `rG` function L15 — `function rG(t,e)`
-  `rd` function L15 — `function rd(t)`
-  `tG` function L15 — `function tG(t,e)`
-  `w` function L15 — `function w()`
-  `w3` function L15 — `function w3(t)`
-  `wz` function L15 — `function wz(t)`
-  `xr` function L15 — `function xr(t,e)`
-  `z7` function L15 — `function z7(t)`
-  `n` function L269-270 — `function n()`
-  `C` function L270 — `function C(ae)`
-  `j4e` function L275 — `function j4e()`
-  `$` function L352 — `function $(K)`
-  `$0` function L352 — `function $0(t)`
-  `$5` function L352 — `function $5(t,e)`
-  `$5e` function L352 — `function $5e()`
-  `$6e` function L352 — `function $6e(t)`
-  `$A` function L352 — `function $A(t,e,r)`
-  `$Ee` function L352 — `function $Ee(t,e)`
-  `$Se` function L352 — `function $Se(t,e,r,n)`
-  `$Te` function L352 — `function $Te(t)`
-  `$ke` function L352 — `function $ke(t)`
-  `$n` function L352 — `function $n(t)`
-  `$we` function L352 — `function $we(t)`
-  `A` function L352 — `function A(K,X)`
-  `A5e` function L352 — `function A5e(t,e)`
-  `A8` function L352 — `function A8(t)`
-  `AA` function L352 — `function AA(t)`
-  `AH` function L352 — `function AH()`
-  `AW` function L352 — `function AW(t)`
-  `Ake` function L352 — `function Ake(t,e)`
-  `Av` function L352 — `function Av(t)`
-  `Awe` function L352 — `function Awe(t,e,r)`
-  `B` function L352 — `function B(K)`
-  `B0` function L352 — `function B0(t,e,r)`
-  `B3` function L352 — `function B3(t)`
-  `B5` function L352 — `function B5(t)`
-  `B5e` function L352 — `function B5e(t)`
-  `B6e` function L352 — `function B6e(t)`
-  `BA` function L352 — `function BA(t,e)`
-  `BEe` function L352 — `function BEe(t)`
-  `BW` function L352 — `function BW(t,e,r,n,i,a,s,l,u,h)`
-  `Bi` function L352 — `function Bi(t,e)`
-  `Bke` function L352 — `function Bke(t)`
-  `Bn` function L352 — `function Bn(t)`
-  `Bwe` function L352 — `function Bwe(t,e)`
-  `C0` function L352 — `function C0(t,e)`
-  `C5e` function L352 — `function C5e(t)`
-  `CA` function L352 — `function CA(t)`
-  `CEe` function L352 — `function CEe(t)`
-  `CH` function L352 — `function CH(t)`
-  `CU` function L352 — `function CU(t,e)`
-  `CW` function L352 — `function CW(t)`
-  `C_` function L352 — `function C_(t)`
-  `Cke` function L352 — `function Cke(t)`
-  `Cv` function L352 — `function Cv(t,e,r)`
-  `Cwe` function L352 — `function Cwe(t,e,r)`
-  `D` function L352 — `function D(K,X,te)`
-  `D0` function L352 — `function D0(t,e,r)`
-  `D5e` function L352 — `function D5e(t)`
-  `DA` function L352 — `function DA(t)`
-  `DEe` function L352 — `function DEe(t)`
-  `DW` function L352 — `function DW(t)`
-  `Dh` function L352 — `function Dh(t,e,r)`
-  `Dke` function L352 — `function Dke(t)`
-  `Do` function L352 — `function Do(t)`
-  `Dwe` function L352 — `function Dwe(t,e)`
-  `E` function L352 — `function E(K,X)`
-  `E5e` function L352 — `function E5e(t,e,r)`
-  `E6e` function L352 — `function E6e(t,e,r)`
-  `E8` function L352 — `function E8(t,e,r,n,i)`
-  `E9` function L352 — `function E9(t,e)`
-  `ECe` function L352 — `function ECe(t)`
-  `EEe` function L352 — `function EEe(t)`
-  `EU` function L352 — `function EU(t)`
-  `EW` function L352 — `function EW(t,e)`
-  `Eh` function L352 — `function Eh(t)`
-  `Eke` function L352 — `function Eke(t,e)`
-  `Ewe` function L352 — `function Ewe(t)`
-  `F` function L352 — `function F(K)`
-  `F0` function L352 — `function F0(t)`
-  `F3e` function L352 — `function F3e()`
-  `F5` function L352 — `function F5(t,e)`
-  `F5e` function L352 — `function F5e()`
-  `F6e` function L352 — `function F6e(t)`
-  `FA` function L352 — `function FA()`
-  `FEe` function L352 — `function FEe(t,e)`
-  `FSe` function L352 — `function FSe(t,e,r)`
-  `FU` function L352 — `function FU(t)`
-  `Fi` function L352 — `function Fi(t,e)`
-  `Fke` function L352 — `function Fke(t)`
-  `Fv` function L352 — `function Fv(t,e,r)`
-  `Fwe` function L352 — `function Fwe(t,e)`
-  `G0` function L352 — `function G0(t)`
-  `G3` function L352 — `function G3(t)`
-  `G3e` function L352 — `function G3e({_intern:t,_key:e},r)`
-  `G5e` function L352 — `function G5e()`
-  `G6e` function L352 — `function G6e(t)`
-  `GA` function L352 — `function GA()`
-  `GEe` function L352 — `function GEe()`
-  `GTe` function L352 — `function GTe(t,e,r)`
-  `Ge` function L352 — `function Ge(t)`
-  `Gke` function L352 — `function Gke(t)`
-  `Gv` function L352 — `function Gv(t)`
-  `Gwe` function L352 — `function Gwe(t)`
-  `H` function L352 — `function H(K)`
-  `H5` function L352 — `function H5(t,e)`
-  `H5e` function L352 — `function H5e(t)`
-  `HA` function L352 — `function HA()`
-  `HTe` function L352 — `function HTe(t,e,r)`
-  `HW` function L352 — `function HW(t)`
-  `Hke` function L352 — `function Hke(t)`
-  `Hwe` function L352 — `function Hwe(t,e,r)`
-  `I` function L352 — `function I(D)`
-  `I3` function L352 — `function I3(t,e,r)`
-  `I5` function L352 — `function I5()`
-  `I5e` function L352 — `function I5e(t)`
-  `I6e` function L352 — `function I6e(t,e)`
-  `IA` function L352 — `function IA()`
-  `IU` function L352 — `function IU(t)`
-  `IV` function L352 — `function IV(t,e)`
-  `I_` function L352 — `function I_(t,e,r,n)`
-  `Ike` function L352 — `function Ike(t,e)`
-  `Iv` function L352 — `function Iv(t)`
-  `Iwe` function L352 — `function Iwe(t,e)`
-  `J3e` function L352 — `function J3e(t,e)`
-  `J8` function L352 — `function J8(t,e)`
-  `JA` function L352 — `function JA(t,e)`
-  `JEe` function L352 — `function JEe(t,e)`
-  `JSe` function L352 — `function JSe(t,e,r,n,i,a,s)`
-  `JTe` function L352 — `function JTe(t,e,r)`
-  `JV` function L352 — `function JV()`
-  `J_` function L352 — `function J_(t)`
-  `Jy` function L352 — `function Jy(t)`
-  `K3` function L352 — `function K3(t,e,r,n,i,a)`
-  `K3e` function L352 — `function K3e(t,e)`
-  `K5e` function L352 — `function K5e()`
-  `K6e` function L352 — `function K6e()`
-  `K8` function L352 — `function K8(t)`
-  `KG` function L352 — `function KG({_intern:t,_key:e},r)`
-  `KSe` function L352 — `function KSe(t)`
-  `KTe` function L352 — `function KTe(t,e,r)`
-  `K_` function L352 — `function K_(t)`
-  `Ki` function L352 — `function Ki(t,e)`
-  `Kwe` function L352 — `function Kwe(t)`
-  `L` function L352 — `function L(K,X,te)`
-  `L0` function L352 — `function L0()`
-  `L3` function L352 — `function L3(t,e,r)`
-  `L5e` function L352 — `function L5e(t)`
-  `L6e` function L352 — `function L6e(t)`
-  `LA` function L352 — `function LA(t)`
-  `LSe` function L352 — `function LSe(t)`
-  `LW` function L352 — `function LW(t,e)`
-  `L_` function L352 — `function L_(t)`
-  `Lke` function L352 — `function Lke(t,e)`
-  `Lv` function L352 — `function Lv(t)`
-  `Lwe` function L352 — `function Lwe(t,e)`
-  `M` function L352 — `function M(K,X,te)`
-  `M3` function L352 — `function M3(t,e)`
-  `M5` function L352 — `function M5(t,e,r,n,i,a,s)`
-  `M5e` function L352 — `function M5e(t)`
-  `M8` function L352 — `function M8(t,e)`
-  `MU` function L352 — `function MU(t)`
-  `MV` function L352 — `function MV(t,e)`
-  `Mke` function L352 — `function Mke(t,e)`
-  `Mwe` function L352 — `function Mwe(t,e)`
-  `N3` function L352 — `function N3(t,e)`
-  `N5` function L352 — `function N5(t)`
-  `N5e` function L352 — `function N5e()`
-  `N6e` function L352 — `function N6e(t,e)`
-  `N8` function L352 — `function N8(t,e)`
-  `NA` function L352 — `function NA(t)`
-  `NV` function L352 — `function NV(t)`
-  `N_` function L352 — `function N_(t,e)`
-  `Nke` function L352 — `function Nke(t,e)`
-  `Nv` function L352 — `function Nv(t)`
-  `Nwe` function L352 — `function Nwe(t,e)`
-  `O` function L352 — `function O(K,X,te)`
-  `O5e` function L352 — `function O5e()`
-  `OSe` function L352 — `function OSe(t,e)`
-  `OX` function L352 — `function OX(t,e)`
-  `O_` function L352 — `function O_(t,e)`
-  `Oke` function L352 — `function Oke()`
-  `Owe` function L352 — `function Owe(t,e)`
-  `P` function L352 — `function P(K)`
-  `P0` function L352 — `function P0(t,e,r)`
-  `P5` function L352 — `function P5(t)`
-  `P5e` function L352 — `function P5e(t)`
-  `PA` function L352 — `function PA(t)`
-  `PU` function L352 — `function PU(t,e,r,n)`
-  `Pke` function L352 — `function Pke(t)`
-  `Pwe` function L352 — `function Pwe(t,e)`
-  `Q` function L352 — `function Q(K)`
-  `Q3e` function L352 — `function Q3e()`
-  `Q8` function L352 — `function Q8(t)`
-  `QA` function L352 — `function QA(t,e,r)`
-  `QEe` function L352 — `function QEe(t,e)`
-  `QSe` function L352 — `function QSe(t)`
-  `QTe` function L352 — `function QTe(t,e,r)`
-  `Q_` function L352 — `function Q_(t)`
-  `Qwe` function L352 — `function Qwe(t)`
-  `R` function L352 — `function R(K,X,te)`
-  `R0` function L352 — `function R0(t)`
-  `R3` function L352 — `function R3(t,e,r)`
-  `R5e` function L352 — `function R5e(t,e)`
-  `R6e` function L352 — `function R6e(t,e)`
-  `R8` function L352 — `function R8(t,e)`
-  `RA` function L352 — `function RA(t)`
-  `RSe` function L352 — `function RSe(t)`
-  `RV` function L352 — `function RV(t)`
-  `RW` function L352 — `function RW(t)`
-  `R_` function L352 — `function R_(t)`
-  `Rke` function L352 — `function Rke(t,e)`
-  `Rq` function L352 — `function Rq(t)`
-  `Rv` function L352 — `function Rv(t)`
-  `Rwe` function L352 — `function Rwe(t,e)`
-  `S` function L352 — `function S(K,X,te,J)`
-  `S5e` function L352 — `function S5e(t,e,r)`
-  `S6e` function L352 — `function S6e(t)`
-  `S8` function L352 — `function S8(t)`
-  `SW` function L352 — `function SW()`
-  `S_` function L352 — `function S_(t)`
-  `Sh` function L352 — `function Sh(t,e)`
-  `Ske` function L352 — `function Ske(t,e)`
-  `Swe` function L352 — `function Swe(t,e,r)`
-  `T` function L352 — `function T(E)`
-  `T0` function L352 — `function T0(t,e,r,n)`
-  `T5e` function L352 — `function T5e(t,e)`
-  `T8` function L352 — `function T8(t)`
-  `TU` function L352 — `function TU(t)`
-  `TW` function L352 — `function TW(t,e,r)`
-  `T_` function L352 — `function T_(t,e)`
-  `Th` function L352 — `function Th()`
-  `Tke` function L352 — `function Tke(t,e)`
-  `Twe` function L352 — `function Twe(t,e,r)`
-  `U0` function L352 — `function U0(t)`
-  `U3` function L352 — `function U3(t)`
-  `U3e` function L352 — `function U3e(t)`
-  `U5` function L352 — `function U5(t)`
-  `U5e` function L352 — `function U5e()`
-  `U8` function L352 — `function U8(t,e)`
-  `UA` function L352 — `function UA()`
-  `USe` function L352 — `function USe(t,e)`
-  `UTe` function L352 — `function UTe(t,e,r)`
-  `UU` function L352 — `function UU()`
-  `Uke` function L352 — `function Uke(t,e,r,n,i,a,s,l)`
-  `Uv` function L352 — `function Uv(t)`
-  `Uwe` function L352 — `function Uwe(t,e)`
-  `V0` function L352 — `function V0(t)`
-  `V3` function L352 — `function V3(t,e,r,n)`
-  `V3e` function L352 — `function V3e({_intern:t,_key:e},r)`
-  `V5` function L352 — `function V5(t)`
-  `V5e` function L352 — `function V5e()`
-  `V8` function L352 — `function V8(t)`
-  `VA` function L352 — `function VA(t)`
-  `VEe` function L352 — `function VEe(t)`
-  `VTe` function L352 — `function VTe(t,e,r)`
-  `Vke` function L352 — `function Vke(t)`
-  `Vv` function L352 — `function Vv(t)`
-  `W0` function L352 — `function W0(t)`
-  `W3` function L352 — `function W3(t)`
-  `W5e` function L352 — `function W5e(t)`
-  `W8` function L352 — `function W8(t,e)`
-  `W9` function L352 — `function W9(t,e)`
-  `WA` function L352 — `function WA()`
-  `WTe` function L352 — `function WTe(t,e,r)`
-  `Wke` function L352 — `function Wke(t)`
-  `Wr` function L352 — `function Wr(t,e,r)`
-  `Wwe` function L352 — `function Wwe(t,e,r)`
-  `X3` function L352 — `function X3(t,e)`
-  `X3e` function L352 — `function X3e(t)`
-  `X5e` function L352 — `function X5e(t,e)`
-  `X6e` function L352 — `function X6e(t)`
-  `X8` function L352 — `function X8(t)`
-  `XA` function L352 — `function XA(t)`
-  `XTe` function L352 — `function XTe(t,e,r)`
-  `XW` function L352 — `function XW(t)`
-  `X_` function L352 — `function X_(t)`
-  `Xwe` function L352 — `function Xwe(t,e,r)`
-  `Y3e` function L352 — `function Y3e(t)`
-  `Y5e` function L352 — `function Y5e(t,e,r)`
-  `Y6e` function L352 — `function Y6e(t)`
-  `Y8` function L352 — `function Y8(t)`
-  `Y9` function L352 — `function Y9(t)`
-  `YA` function L352 — `function YA()`
-  `YSe` function L352 — `function YSe(t)`
-  `YTe` function L352 — `function YTe(t,e,r)`
-  `YW` function L352 — `function YW(t)`
-  `Yke` function L352 — `function Yke(t)`
-  `Ywe` function L352 — `function Ywe(t,e,r)`
-  `Z8` function L352 — `function Z8(t)`
-  `Z9` function L352 — `function Z9(t)`
-  `ZEe` function L352 — `function ZEe()`
-  `ZSe` function L352 — `function ZSe(t)`
-  `ZTe` function L352 — `function ZTe(t,e,r)`
-  `Zs` function L352 — `function Zs()`
-  `Zwe` function L352 — `function Zwe(t)`
-  `Zy` function L352 — `function Zy(t,e,r)`
-  `_` function L352 — `function _(K,X,te)`
-  `_5` function L352 — `function _5()`
-  `_5e` function L352 — `function _5e(t,e)`
-  `_6e` function L352 — `function _6e(t,e)`
-  `_W` function L352 — `function _W(t,e)`
-  `_ke` function L352 — `function _ke(t,e)`
-  `_v` function L352 — `function _v(t)`
-  `_we` function L352 — `function _we(t,e,r)`
-  `a` function L352 — `function a(l,u,h=0,f=l.length)`
-  `a5e` function L352 — `function a5e(t)`
-  `a6e` function L352 — `function a6e(t)`
-  `a8` function L352 — `function a8()`
-  `aSe` function L352 — `function aSe(t)`
-  `aU` function L352 — `function aU(t)`
-  `a_` function L352 — `function a_(t,e,r)`
-  `ake` function L352 — `function ake(t,e)`
-  `aq` function L352 — `function aq(t,e)`
-  `awe` function L352 — `function awe()`
-  `b0` function L352 — `function b0(t,e)`
-  `b5e` function L352 — `function b5e(t,e)`
-  `b8` function L352 — `function b8(t)`
-  `bA` function L352 — `function bA(t)`
-  `bCe` function L352 — `function bCe(t)`
-  `bU` function L352 — `function bU(t,e)`
-  `bW` function L352 — `function bW(t,e,r)`
-  `bX` function L352 — `function bX(t,e,r,n,i)`
-  `b_` function L352 — `function b_(t)`
-  `bh` function L352 — `function bh(t,e)`
-  `bke` function L352 — `function bke(t,e)`
-  `bl` function L352 — `function bl()`
-  `bq` function L352 — `function bq(t)`
-  `bwe` function L352 — `function bwe(t,e,r)`
-  `c5` function L352 — `function c5(t)`
-  `c6e` function L352 — `function c6e(t)`
-  `c8` function L352 — `function c8(t)`
-  `cH` function L352 — `function cH(t)`
-  `cTe` function L352 — `function cTe()`
-  `c_` function L352 — `function c_()`
-  `cd` function L352 — `function cd(t)`
-  `cke` function L352 — `function cke(t)`
-  `cwe` function L352 — `function cwe(t,e,r)`
-  `d` function L352 — `function d()`
-  `d5e` function L352 — `function d5e(t,e,r,n,i,a,s)`
-  `d6e` function L352 — `function d6e(t)`
-  `dke` function L352 — `function dke(t,e)`
-  `dl` function L352 — `function dl(t,e,r,n)`
-  `du` function L352 — `function du(t,e)`
-  `dv` function L352 — `function dv()`
-  `dw` function L352 — `function dw(t,e)`
-  `dwe` function L352 — `function dwe(t)`
-  `e` function L352 — `function e(d,p)`
-  `e5e` function L352 — `function e5e(t,e)`
-  `e6e` function L352 — `function e6e(t,e)`
-  `e8` function L352 — `function e8(t)`
-  `eCe` function L352 — `function eCe(t)`
-  `eU` function L352 — `function eU()`
-  `e_` function L352 — `function e_()`
-  `eke` function L352 — `function eke(t,e,r)`
-  `es` function L352 — `function es(t,e,r,n)`
-  `f` function L352 — `function f(d,p)`
-  `f5e` function L352 — `function f5e(t,e,r,n,i,a)`
-  `f6e` function L352 — `function f6e(t)`
-  `f8` function L352 — `function f8(t,e)`
-  `fA` function L352 — `function fA(t,e)`
-  `fCe` function L352 — `function fCe(t)`
-  `fEe` function L352 — `function fEe(t)`
-  `f_` function L352 — `function f_(t)`
-  `fke` function L352 — `function fke(t)`
-  `fq` function L352 — `function fq(t)`
-  `fu` function L352 — `function fu(t,e,r,n)`
-  `fv` function L352 — `function fv()`
-  `fwe` function L352 — `function fwe(t)`
-  `g5` function L352 — `function g5(t,e)`
-  `g5e` function L352 — `function g5e(t,e)`
-  `g6e` function L352 — `function g6e()`
-  `g8` function L352 — `function g8(t,e,r)`
-  `gA` function L352 — `function gA(t)`
-  `gH` function L352 — `function gH(t)`
-  `gU` function L352 — `function gU(t)`
-  `gW` function L352 — `function gW(t,e,r,n,i,a)`
-  `g_` function L352 — `function g_(t)`
-  `gke` function L352 — `function gke(t,e)`
-  `gl` function L352 — `function gl()`
-  `gq` function L352 — `function gq(t,e)`
-  `gu` function L352 — `function gu()`
-  `gv` function L352 — `function gv(t,e)`
-  `gwe` function L352 — `function gwe()`
-  `h` function L352 — `function h(d)`
-  `h5` function L352 — `function h5(t)`
-  `h5e` function L352 — `function h5e(t)`
-  `h6e` function L352 — `function h6e(t)`
-  `h8` function L352 — `function h8(t,e,r)`
-  `hCe` function L352 — `function hCe(t,e,r)`
-  `h_` function L352 — `function h_(t)`
-  `ha` function L352 — `function ha(t,e)`
-  `he` function L352 — `function he(K)`
-  `hke` function L352 — `function hke(t,e)`
-  `i` function L352 — `function i(l,u,h=0,f=l.length)`
-  `i5e` function L352 — `function i5e()`
-  `i6e` function L352 — `function i6e(t)`
-  `i8` function L352 — `function i8()`
-  `iTe` function L352 — `function iTe(t)`
-  `iU` function L352 — `function iU(t,e,r,n)`
-  `iV` function L352 — `function iV(t,e)`
-  `i_` function L352 — `function i_(t,e,r)`
-  `ic` function L352 — `function ic(t,e,r,n)`
-  `ie` function L352 — `function ie(K)`
-  `ike` function L352 — `function ike(t,e)`
-  `iwe` function L352 — `function iwe()`
-  `j` function L352 — `function j(K)`
-  `j3e` function L352 — `function j3e(t)`
-  `j5e` function L352 — `function j5e(t,e)`
-  `j6e` function L352 — `function j6e(t)`
-  `j8` function L352 — `function j8(t)`
-  `jA` function L352 — `function jA(t,e)`
-  `jEe` function L352 — `function jEe(t)`
-  `jTe` function L352 — `function jTe(t,e,r)`
-  `jV` function L352 — `function jV()`
-  `j_` function L352 — `function j_(t)`
-  `jwe` function L352 — `function jwe(t)`
-  `k` function L352 — `function k(K,X,te)`
-  `k5e` function L352 — `function k5e(t)`
-  `k6e` function L352 — `function k6e(t,e,r)`
-  `k8` function L352 — `function k8(t,e,r,n)`
-  `kCe` function L352 — `function kCe(t,e,r)`
-  `kH` function L352 — `function kH(t)`
-  `kU` function L352 — `function kU(t)`
-  `kW` function L352 — `function kW(t,e)`
-  `kh` function L352 — `function kh(t,e)`
-  `kke` function L352 — `function kke(t,e)`
-  `kv` function L352 — `function kv()`
-  `kwe` function L352 — `function kwe(t)`
-  `l` function L352 — `function l(h,f,d,p)`
-  `l5e` function L352 — `function l5e()`
-  `l6e` function L352 — `function l6e()`
-  `l8` function L352 — `function l8()`
-  `lCe` function L352 — `function lCe(t)`
-  `lTe` function L352 — `function lTe(t,e,r)`
-  `lV` function L352 — `function lV()`
-  `l_` function L352 — `function l_()`
-  `ld` function L352 — `function ld(t)`
-  `le` function L352 — `function le(K)`
-  `lke` function L352 — `function lke(t,e)`
-  `lq` function L352 — `function lq(t,e)`
-  `lwe` function L352 — `function lwe(t)`
-  `m5` function L352 — `function m5(t)`
-  `m5e` function L352 — `function m5e(t)`
-  `m9` function L352 — `function m9(t,e,r)`
-  `mEe` function L352 — `function mEe(t)`
-  `md` function L352 — `function md(t,e)`
-  `mke` function L352 — `function mke(t,e)`
-  `ml` function L352 — `function ml(t)`
-  `mq` function L352 — `function mq(t,e,r)`
-  `mwe` function L352 — `function mwe()`
-  `n` function L352 — `function n(i,a)`
-  `n5e` function L352 — `function n5e()`
-  `n6e` function L352 — `function n6e(t)`
-  `n8` function L352 — `function n8(t)`
-  `nCe` function L352 — `function nCe(t)`
-  `nU` function L352 — `function nU()`
-  `n_` function L352 — `function n_()`
-  `ne` function L352 — `function ne(K)`
-  `nke` function L352 — `function nke(t,e)`
-  `nq` function L352 — `function nq(t)`
-  `nv` function L352 — `function nv(t)`
-  `o5` function L352 — `function o5(t)`
-  `o5e` function L352 — `function o5e(t)`
-  `o8` function L352 — `function o8(t,e)`
-  `o9` function L352 — `function o9(t)`
-  `oTe` function L352 — `function oTe(t,e,r)`
-  `oU` function L352 — `function oU(t,e,r,n)`
-  `oV` function L352 — `function oV(t,e,r)`
-  `o_` function L352 — `function o_(t)`
-  `od` function L352 — `function od(t)`
-  `oi` function L352 — `function oi(t,e)`
-  `oke` function L352 — `function oke(t,e)`
-  `oq` function L352 — `function oq(t,e)`
-  `p` function L352 — `function p(m)`
-  `p5e` function L352 — `function p5e(t)`
-  `p6e` function L352 — `function p6e(t,e)`
-  `pA` function L352 — `function pA(t)`
-  `pke` function L352 — `function pke(t,e)`
-  `pl` function L352 — `function pl(t)`
-  `pq` function L352 — `function pq(t)`
-  `pu` function L352 — `function pu(t,e,r,n,i,a)`
-  `pv` function L352 — `function pv(t,e,r)`
-  `q5e` function L352 — `function q5e(t)`
-  `q8` function L352 — `function q8(t,e)`
-  `qA` function L352 — `function qA()`
-  `qEe` function L352 — `function qEe(t)`
-  `qSe` function L352 — `function qSe(t,e)`
-  `qTe` function L352 — `function qTe(t,e,r)`
-  `qV` function L352 — `function qV(t,e,r)`
-  `qW` function L352 — `function qW(t)`
-  `qke` function L352 — `function qke(t)`
-  `qwe` function L352 — `function qwe(t,e)`
-  `r` function L352 — `function r(n)`
-  `r5e` function L352 — `function r5e(t)`
-  `r8` function L352 — `function r8(t)`
-  `r9` function L352 — `function r9(t)`
-  `rCe` function L352 — `function rCe(t,e,r)`
-  `rTe` function L352 — `function rTe(t)`
-  `rU` function L352 — `function rU()`
-  `r_` function L352 — `function r_(t)`
-  `ra` function L352 — `function ra(t,e)`
-  `rc` function L352 — `function rc(t)`
-  `rke` function L352 — `function rke(t,e)`
-  `rv` function L352 — `function rv(t,e)`
-  `s` function L352 — `function s(l,u,h=0,f=l.length)`
-  `s5` function L352 — `function s5()`
-  `s6e` function L352 — `function s6e(t,e)`
-  `s8` function L352 — `function s8(t)`
-  `s9` function L352 — `function s9(t,e)`
-  `sTe` function L352 — `function sTe(t,e)`
-  `sU` function L352 — `function sU(t)`
-  `s_` function L352 — `function s_(t)`
-  `sd` function L352 — `function sd(t,e)`
-  `ske` function L352 — `function ske(t,e)`
-  `sv` function L352 — `function sv(t,e,r,n)`
-  `swe` function L352 — `function swe()`
-  `t` function L352 — `function t(i,a,s,l,u,h,f)`
-  `t5` function L352 — `function t5(t,e,r)`
-  `t5e` function L352 — `function t5e(t)`
-  `t8` function L352 — `function t8(t,e)`
-  `tCe` function L352 — `function tCe(t,e,r)`
-  `tTe` function L352 — `function tTe(t,e)`
-  `tU` function L352 — `function tU(t)`
-  `t_` function L352 — `function t_(t)`
-  `tke` function L352 — `function tke(t,e,r)`
-  `tq` function L352 — `function tq(t)`
-  `tv` function L352 — `function tv(t)`
-  `u` function L352 — `function u(h,f,d,p,m,g)`
-  `u5` function L352 — `function u5(t,e)`
-  `u5e` function L352 — `function u5e()`
-  `u6e` function L352 — `function u6e(t,e)`
-  `u8` function L352 — `function u8(t)`
-  `uCe` function L352 — `function uCe(t,e)`
-  `uTe` function L352 — `function uTe(t)`
-  `ua` function L352 — `function ua(t,e,r,n)`
-  `uke` function L352 — `function uke(t,e)`
-  `uq` function L352 — `function uq(t,e)`
-  `v0` function L352 — `function v0(t)`
-  `v5` function L352 — `function v5(t,e)`
-  `v5e` function L352 — `function v5e(t)`
-  `v6e` function L352 — `function v6e(t)`
-  `vCe` function L352 — `function vCe(t,e)`
-  `vEe` function L352 — `function vEe(t)`
-  `v_` function L352 — `function v_(t)`
-  `vd` function L352 — `function vd(t)`
-  `vke` function L352 — `function vke(t)`
-  `vq` function L352 — `function vq(t)`
-  `w5e` function L352 — `function w5e(t,e)`
-  `w6e` function L352 — `function w6e(t,e)`
-  `w8` function L352 — `function w8(t)`
-  `wCe` function L352 — `function wCe(t)`
-  `wU` function L352 — `function wU(t,e)`
-  `wW` function L352 — `function wW(t,e,r)`
-  `wh` function L352 — `function wh(t,e,r)`
-  `wke` function L352 — `function wke(t,e)`
-  `wl` function L352 — `function wl(t,e)`
-  `wq` function L352 — `function wq(t)`
-  `wu` function L352 — `function wu(t)`
-  `wwe` function L352 — `function wwe(t,e)`
-  `x0` function L352 — `function x0(t)`
-  `x5e` function L352 — `function x5e(t,e)`
-  `x6e` function L352 — `function x6e(t)`
-  `x8` function L352 — `function x8(t)`
-  `xA` function L352 — `function xA(t)`
-  `xEe` function L352 — `function xEe(t)`
-  `xW` function L352 — `function xW(t,e,r)`
-  `xd` function L352 — `function xd(t)`
-  `xh` function L352 — `function xh(t)`
-  `xke` function L352 — `function xke(t,e)`
-  `xn` function L352 — `function xn(t,e,r,n)`
-  `xv` function L352 — `function xv(t,e)`
-  `y0` function L352 — `function y0(t,e,r)`
-  `y5` function L352 — `function y5(t,e)`
-  `y5e` function L352 — `function y5e(t)`
-  `y6e` function L352 — `function y6e(t)`
-  `y8` function L352 — `function y8(t)`
-  `y9` function L352 — `function y9(t)`
-  `yU` function L352 — `function yU(t)`
-  `y_` function L352 — `function y_(t,e)`
-  `yke` function L352 — `function yke(t,e)`
-  `yq` function L352 — `function yq(t)`
-  `yv` function L352 — `function yv(t,e)`
-  `ywe` function L352 — `function ywe()`
-  `z` function L352 — `function z(K)`
-  `z5` function L352 — `function z5(t,e)`
-  `z5e` function L352 — `function z5e()`
-  `z8` function L352 — `function z8(t)`
-  `zA` function L352 — `function zA(t)`
-  `zSe` function L352 — `function zSe(t,e)`
-  `zTe` function L352 — `function zTe(t,e,r)`
-  `zU` function L352 — `function zU()`
-  `zke` function L352 — `function zke(t)`
-  `zv` function L352 — `function zv(t)`
-  `zwe` function L352 — `function zwe(t,e,r)`
-  `Cl` function L353 — `function Cl(t,e,r,n,i)`
-  `GX` function L353 — `function GX(t,e,r,n,i)`
-  `$Ce` function L357 — `function $Ce(t)`
-  `GCe` function L357 — `function GCe(t,e)`
-  `UCe` function L357 — `function UCe(t)`
-  `VCe` function L357 — `function VCe(t,e)`
-  `hj` function L357-359 — `function hj(t,e)`
-  `uj` function L357 — `function uj(t)`
-  `zCe` function L357 — `function zCe(t)`
-  `KCe` function L359-363 — `function KCe(t,e)`
-  `nD` function L359 — `function nD(t,e)`
-  `o2` function L359 — `function o2(t,e)`
-  `rD` function L359 — `function rD(t,e,r,n,i)`
-  `$7e` function L363 — `function $7e(t)`
-  `A7e` function L363 — `function A7e(t,e)`
-  `B7e` function L363 — `function B7e(t)`
-  `E7e` function L363 — `function E7e(t)`
-  `F7e` function L363 — `function F7e(t)`
-  `H7e` function L363 — `function H7e(t)`
-  `J7e` function L363 — `function J7e(t)`
-  `Ls` function L363 — `function Ls(t)`
-  `M7e` function L363 — `function M7e(t)`
-  `N7e` function L363 — `function N7e(t)`
-  `Nd` function L363 — `function Nd(t)`
-  `O7e` function L363 — `function O7e(t)`
-  `QX` function L363 — `function QX(t)`
-  `R7e` function L363 — `function R7e(t)`
-  `S7e` function L363 — `function S7e(t)`
-  `W7e` function L363 — `function W7e(t)`
-  `X7e` function L363 — `function X7e(t)`
-  `Z7e` function L363 — `function Z7e(t)`
-  `ZX` function L363-364 — `function ZX(t)`
-  `_7e` function L363 — `function _7e(t)`
-  `aAe` function L363 — `function aAe(t)`
-  `aD` function L363 — `function aD(t)`
-  `am` function L363 — `function am(t)`
-  `b7e` function L363 — `function b7e(t)`
-  `c7e` function L363 — `function c7e()`
-  `d7e` function L363 — `function d7e(t)`
-  `dc` function L363 — `function dc(t)`
-  `e7e` function L363 — `function e7e(t)`
-  `f7e` function L363 — `function f7e(t)`
-  `g7e` function L363 — `function g7e(t)`
-  `j7e` function L363 — `function j7e(t)`
-  `jX` function L363 — `function jX(t,e)`
-  `l7e` function L363 — `function l7e(t)`
-  `n` function L363 — `function n(i)`
-  `oAe` function L363 — `function oAe(t)`
-  `p7e` function L363 — `function p7e(t)`
-  `r7e` function L363 — `function r7e()`
-  `sAe` function L363 — `function sAe(t)`
-  `t7e` function L363 — `function t7e(t,e)`
-  `u7e` function L363 — `function u7e(t)`
-  `v7e` function L363 — `function v7e(t)`
-  `w7e` function L363 — `function w7e(t)`
-  `x7e` function L363 — `function x7e(t)`
-  `y7e` function L363 — `function y7e(t)`
-  `z7e` function L363 — `function z7e(t)`
-  `Ci` function L364 — `function Ci(t,e,r)`
-  `Qt` function L364 — `function Qt(t,e)`
-  `Tj` function L364 — `function Tj(t,e)`
-  `bw` function L364 — `function bw(t,e)`
-  `cAe` function L364 — `function cAe(t,e)`
-  `ej` function L364 — `function ej(t,e,r,n)`
-  `fD` function L364-365 — `function fD(t,e)`
-  `hD` function L364 — `function hD(t)`
-  `kw` function L364 — `function kw(t)`
-  `lAe` function L364 — `function lAe(t)`
-  `sm` function L364 — `function sm(t,e,r,n,i,a,s,l,u)`
-  `zh` function L364 — `function zh(t,e,r,n)`
-  `dAe` function L365 — `function dAe(t,e)`
-  `fAe` function L365 — `function fAe(t,e)`
-  `hAe` function L365 — `function hAe(t,e)`
-  `pAe` function L365-371 — `function pAe(t,e)`
-  `uAe` function L365 — `function uAe(t,e,r)`
-  `gAe` function L371 — `function gAe(t)`
-  `kj` function L371-372 — `function kj(t,e)`
-  `mAe` function L371 — `function mAe(t,e,r)`
-  `om` function L371 — `function om(t,e,r,n,i)`
-  `tj` function L371 — `function tj(t,e)`
-  `vAe` function L371 — `function vAe(t)`
-  `xAe` function L371 — `function xAe(t)`
-  `yAe` function L371 — `function yAe(t)`
-  `UAe` function L372 — `function UAe(t)`
-  `VAe` function L372 — `function VAe(t,e)`
-  `WAe` function L372 — `function WAe(t)`
-  `bAe` function L372 — `function bAe(t,e,r)`
-  `rj` function L372-374 — `function rj(t,e)`
-  `wAe` function L372 — `function wAe(t,e)`
-  `oD` function L374-375 — `function oD(t,e)`
-  `KAe` function L375 — `function KAe(t,e,r,n,i)`
-  `Nj` function L375 — `function Nj(t)`
-  `Tw` function L375 — `function Tw(t)`
-  `XAe` function L375 — `function XAe(t)`
-  `YAe` function L375 — `function YAe(t)`
-  `aj` function L375-379 — `function aj(t,e)`
-  `ij` function L375 — `function ij(t,e,r)`
-  `jAe` function L375 — `function jAe(t,e,r,n,i,a,s,l)`
-  `nj` function L375 — `function nj(t)`
-  `qAe` function L375 — `function qAe(t,e)`
-  `s2` function L375 — `function s2(t,e)`
-  `u` function L375 — `function u(h)`
-  `u2` function L375 — `function u2(t)`
-  `sj` function L379-380 — `function sj(t)`
-  `QAe` function L380-383 — `function QAe(t,e)`
-  `oj` function L383-386 — `function oj(t,e)`
-  `Au` function L386 — `function Au(t,e,r,n,i,a,s)`
-  `JAe` function L386 — `function JAe(t,e,r)`
-  `ZAe` function L386 — `function ZAe(t)`
-  `cD` function L386 — `function cD(t,e,r)`
-  `cj` function L386 — `function cj(t,e,r)`
-  `e8e` function L386 — `function e8e(t,e,r)`
-  `lj` function L386 — `function lj(t,e,r,n)`
-  `n8e` function L386-387 — `function n8e(t,e)`
-  `r8e` function L386 — `function r8e(t,e)`
-  `t8e` function L386 — `function t8e(t,e,r,n)`
-  `pD` function L387 — `function pD(t,e)`
-  `$8e` function L388 — `function $8e(t,e)`
-  `$j` function L388 — `function $j(t,e)`
-  `Fj` function L388 — `function Fj(t)`
-  `Gj` function L388 — `function Gj(t)`
-  `f2` function L388 — `function f2(t,e,r)`
-  `nn` function L388 — `function nn(t,e="")`
-  `pc` function L388 — `function pc(t,e)`
-  `vD` function L388 — `function vD()`
-  `z8e` function L388-390 — `function z8e(t,e,r)`
-  `zj` function L388 — `function zj(t,e,r,n,i)`
-  `Jr` function L390 — `function Jr(t,e)`
-  `G8e` function L443-445 — `function G8e(t,{markdownAutoWrap:e})`
-  `Jj` function L445-446 — `function Jj(t,e={})`
-  `s` function L445-446 — `function s(l,u="normal")`
-  `U8e` function L446 — `function U8e(t,e)`
-  `V8e` function L446 — `function V8e(t)`
-  `eK` function L446 — `function eK(t,{markdownAutoWrap:e}={})`
-  `n` function L446 — `function n(i)`
-  `nK` function L446-447 — `function nK(t,e)`
-  `rK` function L446 — `function rK(t,e,r,n)`
-  `CD` function L447 — `function CD(t,e,r=[],n=[])`
-  `H8e` function L447-448 — `function H8e(t,e,r,n,i=!1)`
-  `aK` function L447 — `function aK(t,e)`
-  `$w` function L448 — `function $w(t,e,r,n,i)`
-  `AD` function L448 — `function AD(t,e,r)`
-  `AK` function L448 — `function AK(t,e,r,n,i,a,s,l,u,h)`
-  `BD` function L448 — `function BD(t,e=.15,r)`
-  `Bw` function L448 — `function Bw(t,e,r,n=1)`
-  `CK` function L448 — `function CK(t)`
-  `DD` function L448 — `function DD(t)`
-  `DK` function L448 — `function DK(t,e,r)`
-  `EK` function L448 — `function EK(t,e=0)`
-  `FK` function L448 — `function FK(t,e)`
-  `Fo` function L448 — `function Fo(t,e,r,n,i,a)`
-  `Fw` function L448 — `function Fw(t,e,r)`
-  `HD` function L448 — `function HD(t,e,r,n)`
-  `IK` function L448 — `function IK(t,e)`
-  `K8e` function L448 — `function K8e(t,e,r,n)`
-  `LK` function L448 — `function LK(t)`
-  `La` function L448 — `function La(t,e,r,n)`
-  `Lw` function L448 — `function Lw(t,e,r,n,i,a)`
-  `MD` function L448 — `function MD(t,e,r)`
-  `MK` function L448 — `function MK(t,e,r,n,i,a,s)`
-  `Mw` function L448 — `function Mw(t,e,r)`
-  `OD` function L448 — `function OD(t,e)`
-  `Od` function L448 — `function Od(t,e,r)`
-  `Ow` function L448 — `function Ow(t,e,r)`
-  `PD` function L448 — `function PD(t,e)`
-  `PK` function L448 — `function PK(t,e)`
-  `Pw` function L448 — `function Pw(t,e)`
-  `Q8e` function L448 — `function Q8e(t,e,r)`
-  `RK` function L448 — `function RK(t,e)`
-  `SK` function L448 — `function SK(t)`
-  `TK` function L448 — `function TK(t,e,r,n,i,a,s,l)`
-  `Uh` function L448 — `function Uh(t,e,r,n,i,a=!1)`
-  `W8e` function L448 — `function W8e(t,e,r)`
-  `WD` function L448 — `function WD(t,e,r,n,i,a,s)`
-  `X8e` function L448 — `function X8e(t,e,r,n)`
-  `Xt` function L448 — `function Xt(t)`
-  `Y8e` function L448 — `function Y8e(t,e)`
-  `_D` function L448 — `function _D(t,e)`
-  `_K` function L448 — `function _K(t,e,r,n,i)`
-  `a_e` function L448 — `function a_e(t,e,r,n,i,a,s,l)`
-  `bK` function L448 — `function bK(t,e)`
-  `dm` function L448 — `function dm(t,e)`
-  `e_e` function L448 — `function e_e(t,e)`
-  `g2` function L448 — `function g2(t,e,r)`
-  `hK` function L448 — `function hK(t,e)`
-  `i_e` function L448 — `function i_e(t,e,r,n,i)`
-  `j8e` function L448 — `function j8e(t,e,r)`
-  `jD` function L448 — `function jD(t)`
-  `kK` function L448 — `function kK(t,e,r,n,i,a,s,l,u)`
-  `l_e` function L448-449 — `function l_e(t)`
-  `nr` function L448 — `function nr(t,e,r=1)`
-  `o_e` function L448 — `function o_e(t,e)`
-  `q8e` function L448 — `function q8e(t,e,r,n=!1)`
-  `qD` function L448 — `function qD(t,e,r,n)`
-  `sK` function L448 — `function sK(t,e,r)`
-  `s_e` function L448 — `function s_e(t,e,r)`
-  `t_e` function L448 — `function t_e(t,e,r,n=1)`
-  `vK` function L448 — `function vK(t,e)`
-  `wK` function L448 — `function wK(t)`
-  `x` function L448 — `function x()`
-  `x2` function L448 — `function x2(t,e)`
-  `xK` function L448 — `function xK(t,e,r,n,i,a,s,l,u)`
-  `y2` function L448 — `function y2(t)`
-  `zK` function L448 — `function zK(t,e)`
-  `zw` function L448 — `function zw(t)`
-  `CQ` function L449 — `function CQ(t,e)`
-  `Du` function L449 — `function Du(t,e,r)`
-  `EQ` function L449 — `function EQ(t,e,{config:{flowchart:r}})`
-  `FQ` function L449 — `function FQ(t,e)`
-  `HK` function L449 — `function HK(t,e)`
-  `HQ` function L449 — `function HQ(t,e,{config:{themeVariables:r}})`
-  `Hh` function L449 — `function Hh(t,e,r,n=100,i=0,a=180)`
-  `IQ` function L449 — `function IQ(t,e)`
-  `KK` function L449 — `function KK(t,e)`
-  `LQ` function L449 — `function LQ(t,e)`
-  `NQ` function L449 — `function NQ(t,e)`
-  `PQ` function L449 — `function PQ(t,e)`
-  `Ra` function L449 — `function Ra(t,e,r,n=100,i=0,a=180)`
-  `TQ` function L449 — `function TQ(t,e,{config:{themeVariables:r,flowchart:n}})`
-  `VK` function L449 — `function VK(t,e)`
-  `VQ` function L449 — `function VQ(t,e)`
-  `Wh` function L449 — `function Wh(t,e,r,n=100,i=0,a=180)`
-  `XK` function L449 — `function XK(t,e)`
-  `ZK` function L449 — `function ZK(t,e)`
-  `_Q` function L449 — `function _Q(t,e)`
-  `bQ` function L449 — `function bQ(t,e,{config:{themeVariables:r,flowchart:n}})`
-  `eQ` function L449 — `function eQ(t,e)`
-  `fQ` function L449 — `function fQ(t,e)`
-  `gQ` function L449 — `function gQ(t,e,{config:{themeVariables:r,flowchart:n}})`
-  `iQ` function L449 — `function iQ(t,e,{config:{themeVariables:r}})`
-  `lQ` function L449 — `function lQ(t,e,{dir:r,config:{state:n,themeVariables:i}})`
-  `pQ` function L449 — `function pQ(t,e)`
-  `qK` function L449 — `function qK(t,e)`
-  `qQ` function L449-453 — `function qQ(t,e)`
-  `rQ` function L449 — `function rQ(t,e)`
-  `sQ` function L449 — `function sQ(t,e)`
-  `uQ` function L449 — `function uQ(t,e)`
-  `vQ` function L449 — `function vQ(t,e,{config:{themeVariables:r,flowchart:n}})`
-  `zQ` function L449 — `function zQ(t,e)`
-  `XQ` function L453 — `function XQ(t,e)`
-  `v_e` function L453-454 — `function v_e(t)`
-  `y_e` function L453 — `function y_e(t,e)`
-  `KQ` function L454 — `function KQ(t,e)`
-  `TZ` function L454 — `function TZ(t,e)`
-  `ZQ` function L454 — `function ZQ(t,e)`
-  `bZ` function L454 — `function bZ(t,e)`
-  `eZ` function L454 — `function eZ(t,e)`
-  `fZ` function L454 — `function fZ(t,e,{config:{themeVariables:r}})`
-  `gZ` function L454 — `function gZ(t,e)`
-  `iZ` function L454 — `function iZ(t,e)`
-  `lZ` function L454 — `function lZ(t,e)`
-  `pZ` function L454 — `function pZ(t,e)`
-  `rZ` function L454 — `function rZ(t,e)`
-  `sZ` function L454 — `function sZ(t,e)`
-  `uZ` function L454 — `function uZ(t,e,{config:{themeVariables:r}})`
-  `vZ` function L454 — `function vZ(t,e)`
-  `CZ` function L460 — `function CZ(t,e)`
-  `EZ` function L460 — `function EZ(t,e)`
-  `IZ` function L460-462 — `function IZ(t,e)`
-  `LZ` function L460 — `function LZ(t,e)`
-  `NZ` function L460 — `function NZ(t,e)`
-  `_Z` function L460 — `function _Z(t,e)`
-  `$Z` function L462 — `function $Z(t,e)`
-  `BZ` function L462 — `function BZ(t,e,r,n,i=r.class.padding??12)`
-  `GZ` function L462 — `function GZ(t,e)`
-  `KD` function L462 — `function KD(t,e)`
-  `Lu` function L462 — `function Lu(t,e,r,n="")`
-  `UZ` function L462 — `function UZ(t,e,{config:r})`
-  `Vw` function L462 — `function Vw(t,e,r,n=[])`
-  `WZ` function L462 — `function WZ(t)`
-  `b2` function L462 — `function b2(t,e,r,n=0,i=0,a=[],s="")`
-  `x` function L462 — `function x()`
-  `P_e` function L470 — `function P_e(t)`
-  `Ww` function L470 — `function Ww(t,e)`
-  `Yw` function L470 — `function Yw(t,e)`
-  `$Me` function L476 — `function $Me(t)`
-  `$Ne` function L476 — `function $Ne(t)`
-  `AIe` function L476 — `function AIe(t,e,r)`
-  `AMe` function L476 — `function AMe(t)`
-  `ARe` function L476 — `function ARe(t,e,r)`
-  `Ane` function L476 — `function Ane(t)`
-  `B9e` function L476 — `function B9e(t)`
-  `BDe` function L476 — `function BDe(t)`
-  `BIe` function L476 — `function BIe(t,e)`
-  `BLe` function L476 — `function BLe(t)`
-  `BMe` function L476 — `function BMe(t,e)`
-  `BNe` function L476 — `function BNe(t)`
-  `BRe` function L476 — `function BRe(t,e)`
-  `CIe` function L476 — `function CIe(t,e)`
-  `CNe` function L476 — `function CNe(t)`
-  `CRe` function L476 — `function CRe(t,e)`
-  `D2` function L476 — `function D2(t)`
-  `D9e` function L476 — `function D9e(t)`
-  `DIe` function L476 — `function DIe(t,e,r,n,i)`
-  `DMe` function L476 — `function DMe(t,e,r)`
-  `E9e` function L476 — `function E9e(t,e)`
-  `EIe` function L476 — `function EIe(t,e)`
-  `EMe` function L476 — `function EMe(t,e)`
-  `ENe` function L476 — `function ENe(t)`
-  `ERe` function L476 — `function ERe(t,e)`
-  `Ec` function L476 — `function Ec(t,e,r,n)`
-  `Ene` function L476 — `function Ene(t)`
-  `FIe` function L476 — `function FIe(t,e)`
-  `FRe` function L476 — `function FRe(t,e)`
-  `G9e` function L476 — `function G9e(t)`
-  `GLe` function L476 — `function GLe(t)`
-  `GMe` function L476 — `function GMe(t,e)`
-  `GNe` function L476 — `function GNe(t,e)`
-  `GRe` function L476 — `function GRe(t,e)`
-  `Gne` function L476 — `function Gne(t,e,r)`
-  `H9e` function L476 — `function H9e(t)`
-  `HL` function L476 — `function HL(t,e)`
-  `HLe` function L476 — `function HLe(t)`
-  `HMe` function L476 — `function HMe(t,e)`
-  `HNe` function L476 — `function HNe(t)`
-  `Hne` function L476 — `function Hne(t,e,r)`
-  `IDe` function L476 — `function IDe(t,e)`
-  `IIe` function L476 — `function IIe(t,e,r)`
-  `IMe` function L476 — `function IMe(t,e,r)`
-  `IRe` function L476 — `function IRe(t)`
-  `JIe` function L476 — `function JIe(t)`
-  `JMe` function L476 — `function JMe(t,e)`
-  `JNe` function L476 — `function JNe(t,e,r)`
-  `JRe` function L476 — `function JRe(t,e)`
-  `JT` function L476 — `function JT(t)`
-  `K9e` function L476 — `function K9e(t)`
-  `KIe` function L476 — `function KIe(t)`
-  `KL` function L476 — `function KL(t)`
-  `KLe` function L476 — `function KLe(t)`
-  `KMe` function L476 — `function KMe(t)`
-  `KNe` function L476 — `function KNe(t,e)`
-  `KRe` function L476 — `function KRe(t,e,r)`
-  `Kne` function L476 — `function Kne(t)`
-  `L2` function L476 — `function L2()`
-  `LIe` function L476 — `function LIe(t,e,r,n)`
-  `LNe` function L476 — `function LNe(t,e,r)`
-  `MDe` function L476 — `function MDe(t)`
-  `MIe` function L476 — `function MIe(t,e)`
-  `MRe` function L476 — `function MRe(t)`
-  `NIe` function L476 — `function NIe(t,e)`
-  `NRe` function L476 — `function NRe(t)`
-  `O9e` function L476 — `function O9e(t,e)`
-  `OIe` function L476 — `function OIe(t,e)`
-  `ONe` function L476 — `function ONe(t)`
-  `ORe` function L476 — `function ORe(t)`
-  `One` function L476 — `function One(t,e,r,n,i,a)`
-  `PDe` function L476 — `function PDe(t)`
-  `PIe` function L476 — `function PIe(t)`
-  `PJ` function L476 — `function PJ(t,e,r,n,i)`
-  `PMe` function L476 — `function PMe(t)`
-  `PRe` function L476 — `function PRe(t,e,r,n)`
-  `Q9e` function L476 — `function Q9e(t,e,r)`
-  `QIe` function L476 — `function QIe(t)`
-  `QL` function L476 — `function QL(t)`
-  `QLe` function L476 — `function QLe(t)`
-  `QNe` function L476 — `function QNe(t,e)`
-  `QRe` function L476 — `function QRe(t,e,r)`
-  `R2` function L476 — `function R2(t,e)`
-  `RIe` function L476 — `function RIe(t,e)`
-  `RRe` function L476 — `function RRe(t,e)`
-  `SIe` function L476 — `function SIe(t,e)`
-  `SNe` function L476 — `function SNe(t,e)`
-  `SRe` function L476 — `function SRe(t)`
-  `Sne` function L476 — `function Sne(t)`
-  `T9e` function L476 — `function T9e(t,e,r)`
-  `TIe` function L476 — `function TIe(t,e,r,n)`
-  `TMe` function L476 — `function TMe(t,e,r,n,i)`
-  `TNe` function L476 — `function TNe(t,e)`
-  `TRe` function L476 — `function TRe(t)`
-  `TT` function L476 — `function TT(t)`
-  `Tne` function L476 — `function Tne(t)`
-  `ULe` function L476 — `function ULe(t)`
-  `UMe` function L476 — `function UMe(t,e,r,n)`
-  `UNe` function L476 — `function UNe(t)`
-  `Une` function L476 — `function Une(t)`
-  `Uo` function L476 — `function Uo(t)`
-  `V9e` function L476 — `function V9e(t,e)`
-  `VNe` function L476 — `function VNe(t,e,r)`
-  `Vne` function L476 — `function Vne(t,e,r,n,i)`
-  `W9e` function L476 — `function W9e(t,e)`
-  `WL` function L476 — `function WL(t,e,r,n,i)`
-  `WLe` function L476 — `function WLe(t,e)`
-  `WNe` function L476 — `function WNe(t,e)`
-  `WRe` function L476 — `function WRe(t,e,r)`
-  `Wd` function L476 — `function Wd(t,e)`
-  `Wne` function L476 — `function Wne(t,e,r,n)`
-  `X9e` function L476 — `function X9e(t)`
-  `XIe` function L476 — `function XIe(t)`
-  `XL` function L476 — `function XL(t,e)`
-  `XMe` function L476 — `function XMe(t)`
-  `XNe` function L476 — `function XNe(t,e,r,n)`
-  `Xee` function L476 — `function Xee(t,e,r,n,i)`
-  `Xne` function L476 — `function Xne(t)`
-  `Y9e` function L476 — `function Y9e(t,e)`
-  `YIe` function L476 — `function YIe(t)`
-  `YMe` function L476 — `function YMe(t,e)`
-  `YNe` function L476 — `function YNe(t)`
-  `YRe` function L476 — `function YRe(t,e,r,n)`
-  `Yne` function L476 — `function Yne(t)`
-  `ZIe` function L476 — `function ZIe(t)`
-  `ZL` function L476 — `function ZL(t)`
-  `ZMe` function L476 — `function ZMe(t,e)`
-  `ZNe` function L476 — `function ZNe(t,e)`
-  `ZRe` function L476 — `function ZRe(t)`
-  `Zne` function L476 — `function Zne(t,e,r)`
-  `_2` function L476 — `function _2(t,e,r,n)`
-  `_9e` function L476 — `function _9e(t)`
-  `_Ie` function L476 — `function _Ie(t,e,r,n)`
-  `_Me` function L476 — `function _Me(t,e)`
-  `_Ne` function L476 — `function _Ne(t,e,r,n)`
-  `_Re` function L476 — `function _Re(t,e)`
-  `_ne` function L476 — `function _ne(t)`
-  `a` function L476 — `function a(s)`
-  `aLe` function L476 — `function aLe(t)`
-  `aNe` function L476 — `function aNe(t)`
-  `aOe` function L476 — `function aOe(t)`
-  `ane` function L476 — `function ane(t,e)`
-  `b9e` function L476 — `function b9e(t,e,r,n)`
-  `bDe` function L476 — `function bDe(t)`
-  `bIe` function L476 — `function bIe(t,e)`
-  `bMe` function L476 — `function bMe(t)`
-  `bT` function L476 — `function bT(t,e,r,n,i,a)`
-  `bie` function L476 — `function bie(t,e,r)`
-  `bne` function L476 — `function bne(t)`
-  `cDe` function L476 — `function cDe(t,e)`
-  `cNe` function L476 — `function cNe(t)`
-  `cR` function L476 — `function cR(t,e)`
-  `d9e` function L476 — `function d9e(t)`
-  `dMe` function L476 — `function dMe(t)`
-  `dNe` function L476 — `function dNe(t,e)`
-  `dR` function L476 — `function dR(t,e,r,n)`
-  `dne` function L476 — `function dne(t)`
-  `e` function L476 — `function e(r)`
-  `eIe` function L476 — `function eIe(t,e)`
-  `eMe` function L476 — `function eMe(t,e,r)`
-  `eNe` function L476 — `function eNe(t,e)`
-  `eOe` function L476 — `function eOe(t)`
-  `ef` function L476 — `function ef(t)`
-  `eie` function L476 — `function eie(t,e,r)`
-  `f` function L476 — `function f(d)`
-  `fIe` function L476 — `function fIe(t)`
-  `fNe` function L476 — `function fNe(t,e)`
-  `g9e` function L476 — `function g9e(t)`
-  `gIe` function L476 — `function gIe(t)`
-  `gie` function L476 — `function gie(t)`
-  `gne` function L476 — `function gne(t,e)`
-  `h` function L476 — `function h(d)`
-  `hDe` function L476 — `function hDe()`
-  `hIe` function L476 — `function hIe(t)`
-  `hNe` function L476 — `function hNe(t,e)`
-  `hR` function L476 — `function hR(t)`
-  `hRe` function L476 — `function hRe(t,e,r,n,i,a,s)`
-  `hie` function L476 — `function hie(t,e)`
-  `hne` function L476 — `function hne(t)`
-  `i` function L476 — `function i(a)`
-  `iNe` function L476 — `function iNe(t,e)`
-  `iOe` function L476 — `function iOe(t)`
-  `iR` function L476 — `function iR(t)`
-  `iie` function L476 — `function iie(t)`
-  `ine` function L476 — `function ine(t,e)`
-  `j9e` function L476 — `function j9e(t)`
-  `jIe` function L476 — `function jIe(t)`
-  `jL` function L476 — `function jL(t,e,r,n)`
-  `jLe` function L476 — `function jLe(t,e,r,n,i,a)`
-  `jMe` function L476 — `function jMe(t)`
-  `jNe` function L476 — `function jNe(t,e,r)`
-  `jRe` function L476 — `function jRe(t)`
-  `jne` function L476 — `function jne(t,e,r,n,i,a,s)`
-  `k9e` function L476 — `function k9e(t,e,r)`
-  `kIe` function L476 — `function kIe(t)`
-  `kMe` function L476 — `function kMe(t,e,r)`
-  `kRe` function L476 — `function kRe(t)`
-  `kie` function L476 — `function kie(t)`
-  `kne` function L476 — `function kne(t)`
-  `l9e` function L476 — `function l9e(t)`
-  `lDe` function L476 — `function lDe(t,e)`
-  `lIe` function L476 — `function lIe(t,e,r)`
-  `lJ` function L476 — `function lJ(t)`
-  `lLe` function L476 — `function lLe(t)`
-  `lR` function L476 — `function lR(t,e)`
-  `lie` function L476 — `function lie(t,e)`
-  `lne` function L476 — `function lne(t,e)`
-  `m9e` function L476 — `function m9e(t)`
-  `mDe` function L476 — `function mDe(t,e)`
-  `mIe` function L476 — `function mIe(t,e,r)`
-  `mMe` function L476 — `function mMe(t,e)`
-  `mR` function L476 — `function mR(t)`
-  `mRe` function L476 — `function mRe(t,e,r,n,i,a)`
-  `mie` function L476 — `function mie(t,e)`
-  `mne` function L476 — `function mne(t)`
-  `n` function L476 — `function n(a)`
-  `n9e` function L476 — `function n9e(t,e)`
-  `nLe` function L476 — `function nLe(t,e,r)`
-  `nNe` function L476 — `function nNe(t,e)`
-  `nOe` function L476 — `function nOe(t)`
-  `oDe` function L476 — `function oDe(t,e,r,n)`
-  `oIe` function L476 — `function oIe(t,e)`
-  `oNe` function L476 — `function oNe(t,e,r)`
-  `oOe` function L476 — `function oOe(t)`
-  `oR` function L476 — `function oR(t,e)`
-  `pIe` function L476 — `function pIe(t)`
-  `pMe` function L476 — `function pMe(t)`
-  `pNe` function L476 — `function pNe(t,e)`
-  `pR` function L476 — `function pR(t,e)`
-  `pie` function L476 — `function pie(t,e,r)`
-  `pne` function L476 — `function pne(t)`
-  `q9e` function L476 — `function q9e(t,e,r)`
-  `qL` function L476 — `function qL(t,e,r)`
-  `qLe` function L476 — `function qLe(t,e)`
-  `qMe` function L476 — `function qMe(t,e,r)`
-  `r` function L476 — `function r(n)`
-  `r9e` function L476 — `function r9e(t)`
-  `rNe` function L476 — `function rNe(t,e,r)`
-  `rOe` function L476 — `function rOe(t)`
-  `rf` function L476 — `function rf(t)`
-  `rie` function L476 — `function rie(t,e)`
-  `rk` function L476 — `function rk(t,e,r)`
-  `s9e` function L476 — `function s9e(t)`
-  `sDe` function L476 — `function sDe(t)`
-  `sIe` function L476 — `function sIe(t,e,r)`
-  `sOe` function L476 — `function sOe(t)`
-  `sie` function L476 — `function sie(t,e)`
-  `sne` function L476 — `function sne(t)`
-  `tIe` function L476 — `function tIe(t,e,r)`
-  `tNe` function L476 — `function tNe(t,e)`
-  `tOe` function L476 — `function tOe(t)`
-  `tk` function L476 — `function tk(t)`
-  `u` function L476 — `function u(d,p)`
-  `uDe` function L476 — `function uDe(t,e)`
-  `uIe` function L476 — `function uIe(t)`
-  `uNe` function L476 — `function uNe(t,e)`
-  `uR` function L476 — `function uR(t,e)`
-  `uie` function L476 — `function uie(t,e,r)`
-  `une` function L476 — `function une(t)`
-  `v9e` function L476 — `function v9e()`
-  `vDe` function L476 — `function vDe(t,e)`
-  `vIe` function L476 — `function vIe(t)`
-  `vNe` function L476 — `function vNe(t,e)`
-  `vRe` function L476 — `function vRe(t,e,r,n,i,a)`
-  `vie` function L476 — `function vie(t)`
-  `vm` function L476 — `function vm(t,e,r)`
-  `vne` function L476 — `function vne(t,e)`
-  `w9e` function L476 — `function w9e(t)`
-  `wDe` function L476 — `function wDe(t)`
-  `wIe` function L476 — `function wIe(t,e)`
-  `wNe` function L476 — `function wNe(t,e)`
-  `wRe` function L476 — `function wRe(t,e,r,n)`
-  `wie` function L476 — `function wie(t)`
-  `x9e` function L476 — `function x9e(t,e)`
-  `xDe` function L476 — `function xDe(t,e,r)`
-  `xIe` function L476 — `function xIe(t,e)`
-  `xMe` function L476 — `function xMe(t,e,r,n)`
-  `xne` function L476 — `function xne(t,e,r,n,i,a)`
-  `yIe` function L476 — `function yIe(t,e)`
-  `yne` function L476 — `function yne(t,e)`
-  `zLe` function L476 — `function zLe(t)`
-  `zMe` function L476 — `function zMe(t,e,r)`
-  `zNe` function L476 — `function zNe(t,e)`
-  `zRe` function L476 — `function zRe(t,e,r,n)`
-  `zne` function L476 — `function zne(t,e,r)`
-  `P2e` function L479 — `function P2e()`
-  `wnt` function L479 — `function wnt(Ws)`
-  `Sn` function L484 — `function Sn()`
-  `q` function L631 — `function q()`
-  `xe` function L631 — `function xe(ct)`
-  `te` function L636 — `function te()`
-  `$R` function L690 — `function $R(t)`
-  `AR` function L690 — `function AR(t,e)`
-  `Ag` function L690 — `function Ag(t)`
-  `BOe` function L690 — `function BOe(t,e)`
-  `BR` function L690 — `function BR(t)`
-  `CR` function L690 — `function CR(t,e)`
-  `DR` function L690 — `function DR(t)`
-  `ER` function L690 — `function ER(t,e)`
-  `FOe` function L690 — `function FOe(t,e)`
-  `FR` function L690 — `function FR(t)`
-  `G2` function L690 — `function G2(t)`
-  `GOe` function L690 — `function GOe(t)`
-  `GR` function L690 — `function GR(t)`
-  `Gm` function L690 — `function Gm(t)`
-  `H2` function L690 — `function H2(t)`
-  `HOe` function L690 — `function HOe(t)`
-  `HR` function L690 — `function HR(t)`
-  `Ho` function L690 — `function Ho(t)`
-  `IOe` function L690 — `function IOe(t,e,r=lk)`
-  `IR` function L690 — `function IR(t)`
-  `Il` function L690 — `function Il(t)`
-  `JOe` function L690 — `function JOe(t)`
-  `KOe` function L690 — `function KOe(t)`
-  `Kd` function L690 — `function Kd(t)`
-  `LR` function L690 — `function LR(t)`
-  `Lc` function L690 — `function Lc(t)`
-  `Ll` function L690 — `function Ll(t)`
-  `M2` function L690 — `function M2(t)`
-  `MOe` function L690 — `function MOe(t)`
-  `MR` function L690 — `function MR(t)`
-  `Ml` function L690 — `function Ml(t)`
-  `Mu` function L690 — `function Mu(t)`
-  `NOe` function L690 — `function NOe(t)`
-  `NR` function L690 — `function NR(t)`
-  `Nc` function L690 — `function Nc(t,e)`
-  `OOe` function L690 — `function OOe(t,e=!0)`
-  `OR` function L690 — `function OR(t)`
-  `Oa` function L690 — `function Oa(t)`
-  `POe` function L690 — `function POe(t)`
-  `PR` function L690 — `function PR(t)`
-  `Pa` function L690 — `function Pa(t)`
-  `QOe` function L690 — `function QOe(t)`
-  `Qd` function L690 — `function Qd(t)`
-  `RR` function L690 — `function RR(t)`
-  `SR` function L690 — `function SR(t,e)`
-  `UOe` function L690 — `function UOe(t)`
-  `UR` function L690 — `function UR(t)`
-  `V2` function L690 — `function V2(t)`
-  `VOe` function L690 — `function VOe(t)`
-  `VR` function L690 — `function VR(t)`
-  `W2` function L690 — `function W2(t,e)`
-  `WOe` function L690 — `function WOe(t)`
-  `WR` function L690 — `function WR(t)`
-  `Wo` function L690 — `function Wo(t,e)`
-  `XOe` function L690 — `function XOe(t)`
-  `XR` function L690 — `function XR(t,e)`
-  `YOe` function L690 — `function YOe(t)`
-  `YR` function L690 — `function YR(t,e)`
-  `ZOe` function L690 — `function ZOe(t)`
-  `_R` function L690 — `function _R(t,e)`
-  `_g` function L690 — `function _g(t,e)`
-  `af` function L690 — `function af(t)`
-  `ar` function L690 — `function ar(t)`
-  `bk` function L690 — `function bk(t,e)`
-  `cae` function L690 — `function cae(t,e)`
-  `dae` function L690 — `function dae(t)`
-  `dk` function L690 — `function dk(t)`
-  `e` function L690 — `function e(a)`
-  `ePe` function L690 — `function ePe(t,e)`
-  `en` function L690 — `function en(...t)`
-  `ep` function L690 — `function ep(t)`
-  `fae` function L690 — `function fae(t,e=!0)`
-  `fk` function L690 — `function fk(t)`
-  `gk` function L690 — `function gk(t)`
-  `hae` function L690 — `function hae(t,e,r)`
-  `i` function L690 — `function i(a)`
-  `ii` function L690 — `function ii(t)`
-  `jOe` function L690 — `function jOe(t)`
-  `jR` function L690 — `function jR(t)`
-  `jd` function L690 — `function jd(t)`
-  `kR` function L690 — `function kR(t)`
-  `lae` function L690 — `function lae(t)`
-  `mk` function L690 — `function mk(t)`
-  `n` function L690 — `function n(a)`
-  `ok` function L690 — `function ok(t)`
-  `pae` function L690 — `function pae(t)`
-  `pk` function L690 — `function pk(t)`
-  `q2` function L690 — `function q2()`
-  `qOe` function L690 — `function qOe(t)`
-  `qR` function L690 — `function qR(t,e)`
-  `r` function L690 — `function r(a)`
-  `rp` function L690 — `function rp(t)`
-  `sf` function L690 — `function sf(t)`
-  `so` function L690 — `function so(t)`
-  `tPe` function L690 — `function tPe(t,e=Pa(t).parseResult.value)`
-  `tp` function L690 — `function tp(t,e)`
-  `uae` function L690 — `function uae(t,e)`
-  `va` function L690 — `function va(t)`
-  `vk` function L690 — `function vk(t)`
-  `yk` function L690 — `function yk(t)`
-  `zOe` function L690 — `function zOe(t)`
-  `zR` function L690 — `function zR(t)`
-  `Dg` function L698 — `function Dg(t)`
-  `ap` function L698 — `function ap(t)`
-  `bae` function L698 — `function bae(t)`
-  `eN` function L698 — `function eN(t)`
-  `i` function L698 — `function i()`
-  `iPe` function L698 — `function iPe(t)`
-  `l` function L698 — `function l(h)`
-  `rN` function L698 — `function rN(t,e)`
-  `tN` function L698 — `function tN(t)`
-  `u` function L698 — `function u(h)`
-  `$ae` function L701-709 — `function $ae(t,e=!1)`
-  `Aae` function L701 — `function Aae(t)`
-  `Bg` function L701 — `function Bg(t)`
-  `Bs` function L701 — `function Bs(t)`
-  `Cae` function L701 — `function Cae(t,e)`
-  `Eae` function L701 — `function Eae(t,e,r)`
-  `Iae` function L701 — `function Iae(t)`
-  `Ig` function L701 — `function Ig(t)`
-  `Iu` function L701 — `function Iu(t,e)`
-  `J2` function L701 — `function J2(t)`
-  `K2` function L701 — `function K2(t,e)`
-  `Mae` function L701 — `function Mae(t,e,r)`
-  `Mg` function L701 — `function Mg(t,e)`
-  `Ng` function L701 — `function Ng(t)`
-  `Pae` function L701 — `function Pae()`
-  `Pg` function L701 — `function Pg(t)`
-  `Q2` function L701 — `function Q2(t,e,r)`
-  `Rg` function L701 — `function Rg(t)`
-  `Sae` function L701 — `function Sae(t)`
-  `Sk` function L701 — `function Sk(t)`
-  `Tae` function L701 — `function Tae(t)`
-  `Z2` function L701 — `function Z2(t)`
-  `aN` function L701 — `function aN(t)`
-  `aPe` function L701 — `function aPe(t,e)`
-  `bPe` function L701 — `function bPe(t)`
-  `cN` function L701 — `function cN(t,e,r)`
-  `cPe` function L701 — `function cPe(t)`
-  `dPe` function L701 — `function dPe(t)`
-  `e` function L701 — `function e()`
-  `ex` function L701 — `function ex(t)`
-  `fN` function L701 — `function fN(t)`
-  `fPe` function L701 — `function fPe(t)`
-  `gPe` function L701 — `function gPe(t)`
-  `hN` function L701 — `function hN(t)`
-  `hPe` function L701 — `function hPe(t)`
-  `i` function L701 — `function i(a,s)`
-  `iN` function L701 — `function iN(t,e)`
-  `kae` function L701 — `function kae(t,e,r)`
-  `kk` function L701 — `function kk(t)`
-  `lN` function L701 — `function lN(t,e,r,n)`
-  `lPe` function L701 — `function lPe(t)`
-  `mPe` function L701 — `function mPe(t)`
-  `n` function L701 — `function n()`
-  `nN` function L701 — `function nN(t)`
-  `oN` function L701 — `function oN(t,e)`
-  `oPe` function L701 — `function oPe(t)`
-  `op` function L701 — `function op(t)`
-  `pN` function L701 — `function pN(t)`
-  `pPe` function L701 — `function pPe(t)`
-  `rx` function L701 — `function rx(t)`
-  `sN` function L701 — `function sN(t)`
-  `sPe` function L701 — `function sPe(t,e)`
-  `sp` function L701 — `function sp(t,e=[])`
-  `tx` function L701 — `function tx(t)`
-  `uN` function L701 — `function uN(t,e,r)`
-  `uPe` function L701 — `function uPe(t)`
-  `vN` function L701 — `function vN(t)`
-  `vPe` function L701 — `function vPe(t)`
-  `wPe` function L701 — `function wPe(t,e)`
-  `wae` function L701 — `function wae(t)`
-  `xPe` function L701 — `function xPe(t)`
-  `yN` function L701 — `function yN(t)`
-  `yPe` function L701 — `function yPe(t)`
-  `Bae` function L709 — `function Bae(t,e)`
-  `Nk` function L709 — `function Nk(t,e)`
-  `Rk` function L709 — `function Rk(t,e,r)`
-  `TN` function L709 — `function TN(t,e,r)`
-  `kN` function L709 — `function kN(t)`
-  `kPe` function L709 — `function kPe(t,e)`
-  `Uae` function L710-716 — `function Uae(t,e)`
-  `CPe` function L716 — `function CPe(t)`
-  `EPe` function L716 — `function EPe(t)`
-  `Hae` function L716 — `function Hae(t,e)`
-  `SPe` function L716 — `function SPe(t)`
-  `_Pe` function L716-718 — `function _Pe(t)`
-  `e` class L716 — `-`
-  `DPe` function L718 — `function DPe(t)`
-  `RPe` function L718-720 — `function RPe(t)`
-  `e` class L718 — `-`
-  `IPe` function L720 — `function IPe(t)`
-  `MPe` function L720 — `function MPe(t)`
-  `NPe` function L720 — `function NPe(t)`
-  `OPe` function L720 — `function OPe(t,e)`
-  `PPe` function L720-722 — `function PPe(t)`
-  `BPe` function L722 — `function BPe(t,e)`
-  `FPe` function L722 — `function FPe(t)`
-  `Gae` function L722 — `function Gae(t)`
-  `Vae` function L722 — `function Vae(t)`
-  `Wae` function L722-727 — `function Wae(t,e,r)`
-  `qae` function L727-731 — `function qae(t,e,r)`
-  `$Pe` function L731 — `function $Pe(t)`
-  `Kae` function L731 — `function Kae(t,e)`
-  `Xae` function L731 — `function Xae(t)`
-  `Yae` function L731 — `function Yae(t)`
-  `zPe` function L731-736 — `function zPe(t,e)`
-  `Bu` function L736 — `function Bu(t)`
-  `CN` function L736 — `function CN(t,e,r)`
-  `GPe` function L736 — `function GPe()`
-  `HPe` function L736 — `function HPe(t)`
-  `Ic` function L736 — `function Ic(t)`
-  `Jae` function L736 — `function Jae(t)`
-  `Pu` function L736 — `function Pu(t,e)`
-  `Qae` function L736 — `function Qae(t)`
-  `UPe` function L736 — `function UPe(t)`
-  `VPe` function L736 — `function VPe(t)`
-  `WPe` function L736 — `function WPe(t)`
-  `YPe` function L736 — `function YPe(t)`
-  `_N` function L736 — `function _N(t)`
-  `qPe` function L736 — `function qPe(t)`
-  `rse` function L736 — `function rse(t)`
-  `tse` function L736 — `function tse(t,e)`
-  `zg` function L736 — `function zg(t,e)`
-  `$` function L745 — `function $()`
-  `Fu` function L745 — `function Fu(t)`
-  `H` function L745 — `function H(le)`
-  `LN` function L745 — `function LN(t)`
-  `j` function L745 — `function j(le)`
-  `jPe` function L745-746 — `function jPe(t)`
-  `of` function L745 — `function of(t)`
-  `$u` function L746 — `function $u(t,e,r,n,i,a,s,l)`
-  `sx` function L746 — `function sx(t,e)`
-  `r` function L752 — `function r(f)`
-  `$k` function L776 — `function $k(t,e,r,n)`
-  `$se` function L776 — `function $se(t,e,r)`
-  `Ase` function L776 — `function Ase(t,e,r)`
-  `BN` function L776 — `function BN(t,e,r,n=[])`
-  `Cse` function L776 — `function Cse(t,e)`
-  `Dse` function L776 — `function Dse(t)`
-  `Ese` function L776 — `function Ese(t)`
-  `Fk` function L776 — `function Fk(t,e,r=[])`
-  `Fse` function L776 — `function Fse(t,e,r)`
-  `Gk` function L776 — `function Gk(t)`
-  `Hg` function L776 — `function Hg(t,e,r,n)`
-  `JPe` function L776 — `function JPe(t)`
-  `KPe` function L776 — `function KPe(t,e,r,n)`
-  `Lse` function L776 — `function Lse(t)`
-  `MN` function L776 — `function MN(t)`
-  `QPe` function L776 — `function QPe(t,e,r)`
-  `Sse` function L776 — `function Sse(t,e,r)`
-  `Tse` function L776 — `function Tse(t)`
-  `Uk` function L776 — `function Uk(t)`
-  `Vk` function L776 — `function Vk(t,e)`
-  `Vse` function L776-780 — `function Vse(t,e)`
-  `Wg` function L776 — `function Wg(t,e,r,n)`
-  `Wk` function L776 — `function Wk(t,e,r)`
-  `XN` function L776 — `function XN(t,e)`
-  `YN` function L776 — `function YN(t,e)`
-  `ZPe` function L776 — `function ZPe(t,e)`
-  `_se` function L776 — `function _se(t,e,r)`
-  `a` function L776 — `function a(l)`
-  `aBe` function L776 — `function aBe(t,e,r,n,i,a,s)`
-  `bse` function L776 — `function bse(t,e)`
-  `eBe` function L776 — `function eBe(t,e,r,n)`
-  `fse` function L776 — `function fse(t,e)`
-  `gse` function L776 — `function gse(t,e,r,n,i,a)`
-  `iBe` function L776 — `function iBe(t,e,r)`
-  `kse` function L776 — `function kse(t,e,r,n)`
-  `lBe` function L776 — `function lBe(t,e)`
-  `lf` function L776 — `function lf(t)`
-  `mse` function L776 — `function mse(t,e,r,n,i,a)`
-  `nBe` function L776 — `function nBe(t,e,r,n)`
-  `pse` function L776 — `function pse(t)`
-  `qN` function L776 — `function qN(t,e)`
-  `rBe` function L776 — `function rBe(t,e,r,n)`
-  `s` function L776 — `function s(l)`
-  `sBe` function L776 — `function sBe(t)`
-  `tBe` function L776 — `function tBe(t,e)`
-  `ux` function L776 — `function ux(t)`
-  `vse` function L776 — `function vse(t,e,r)`
-  `wse` function L776 — `function wse(t)`
-  `xse` function L776 — `function xse(t,e)`
-  `yse` function L776 — `function yse(t,e,r,n)`
-  `Use` function L780 — `function Use(t,e,r)`
-  `cBe` function L780 — `function cBe(t,e)`
-  `uBe` function L780 — `function uBe(t,e)`
-  `Jse` function L787 — `function Jse(t)`
-  `aE` function L787-788 — `function aE(t)`
-  `dBe` function L787 — `function dBe(t,e)`
-  `yx` function L787 — `function yx(t,e,r,n=!1)`
-  `ioe` function L794 — `function ioe(t,e)`
-  `lE` function L794 — `function lE(t=void 0)`
-  `ABe` function L800 — `function ABe(t,e,r,n)`
-  `Ai` function L800 — `function Ai(t,e)`
-  `BBe` function L800 — `function BBe(t,e,r,n)`
-  `CBe` function L800 — `function CBe(t,e,r)`
-  `DBe` function L800 — `function DBe(t,e)`
-  `EBe` function L800 — `function EBe(t,e,r)`
-  `FBe` function L800-803 — `function FBe(t)`
-  `IBe` function L800 — `function IBe(t)`
-  `JN` function L800 — `function JN(t,e=!0)`
-  `LBe` function L800 — `function LBe(t,e,r)`
-  `MBe` function L800 — `function MBe(t,e)`
-  `NBe` function L800 — `function NBe(t,e)`
-  `OBe` function L800 — `function OBe(t,e,r,n)`
-  `PBe` function L800 — `function PBe(t,e,r,n,i,a)`
-  `QN` function L800 — `function QN(t,e,r,n)`
-  `RBe` function L800 — `function RBe(t,e,r)`
-  `SBe` function L800 — `function SBe(t,e,r)`
-  `TBe` function L800 — `function TBe(t,e,r)`
-  `ZN` function L800 — `function ZN(t,e)`
-  `_Be` function L800 — `function _Be(t)`
-  `aa` function L800 — `function aa(t,e,r,n)`
-  `bBe` function L800 — `function bBe(t,e)`
-  `boe` function L800 — `function boe(t,e=!0)`
-  `doe` function L800 — `function doe(t)`
-  `dp` function L800 — `function dp(t,e,r)`
-  `e1` function L800 — `function e1(t,e,r,n,...i)`
-  `eM` function L800 — `function eM(t,e,r,n)`
-  `fp` function L800 — `function fp(t,e,r)`
-  `goe` function L800 — `function goe(t,e,r,n,i)`
-  `hf` function L800 — `function hf(t,e)`
-  `kBe` function L800 — `function kBe(t,e,r)`
-  `moe` function L800 — `function moe(t,e,r,n,i)`
-  `poe` function L800 — `function poe(t,e,r)`
-  `wBe` function L800 — `function wBe(t,e,r)`
-  `$Be` function L803 — `function $Be(t)`
-  `GBe` function L803 — `function GBe(t,e)`
-  `HBe` function L803 — `function HBe(t,e)`
-  `KBe` function L803 — `function KBe(t)`
-  `QBe` function L803 — `function QBe(t)`
-  `Toe` function L803 — `function Toe(t)`
-  `UBe` function L803 — `function UBe(t,e)`
-  `VBe` function L803 — `function VBe(t,e,r)`
-  `WBe` function L803 — `function WBe(t)`
-  `XBe` function L803 — `function XBe(t)`
-  `YBe` function L803 — `function YBe(t)`
-  `ZBe` function L803 — `function ZBe(t)`
-  `e` function L803 — `function e(r)`
-  `i` function L803 — `function i(a)`
-  `jBe` function L803 — `function jBe(t)`
-  `koe` function L803 — `function koe(t,e)`
-  `n` function L803 — `function n(a)`
-  `qBe` function L803 — `function qBe(t,e)`
-  `r` function L803 — `function r(n)`
-  `uE` function L803 — `function uE(t,e)`
-  `woe` function L803 — `function woe(t,e,r,n)`
-  `zBe` function L803 — `function zBe(t,e,r)`
-  `Dle` function L805 — `function Dle(t,e,r=e.terminal)`
-  `Lle` function L805 — `function Lle(t,e,r,n)`
-  `Nle` function L805 — `function Nle(t)`
-  `Rle` function L805 — `function Rle(t,e)`
-  `Rx` function L805 — `function Rx(t,e,r)`
-  `TM` function L805 — `function TM(t)`
-  `Vu` function L805 — `function Vu(t)`
-  `a` function L805 — `function a(m)`
-  `aFe` function L805 — `function aFe(t,e)`
-  `bM` function L805 — `function bM(t)`
-  `d` function L805 — `function d(m)`
-  `e` function L805 — `function e(h)`
-  `eFe` function L805 — `function eFe(t,e)`
-  `f` function L805 — `function f(m)`
-  `gp` function L805 — `function gp(t,e,r=!1)`
-  `h` function L805 — `function h(m)`
-  `i` function L805 — `function i(m)`
-  `iFe` function L805 — `function iFe(t,e)`
-  `l` function L805 — `function l(m,g,y)`
-  `lFe` function L805 — `function lFe(t,e)`
-  `n` function L805 — `function n(m)`
-  `nFe` function L805 — `function nFe(t,e)`
-  `oFe` function L805 — `function oFe(t,e)`
-  `p` function L805 — `function p(m,g)`
-  `r` function L805 — `function r(m)`
-  `rFe` function L805 — `function rFe(t,e)`
-  `s` function L805 — `function s(m)`
-  `sFe` function L805 — `function sFe(t,e)`
-  `tFe` function L805 — `function tFe(t,e)`
-  `u` function L805 — `function u(m)`
-  `xE` function L805 — `function xE(t)`
-  `xM` function L805 — `function xM(t,e)`
-  `yM` function L805 — `function yM(t)`
-  `$le` function L806 — `function $le(t)`
-  `AFe` function L806 — `function AFe(t,e)`
-  `Bc` function L806 — `function Bc(t)`
-  `C` function L806 — `function C(D)`
-  `CE` function L806 — `function CE()`
-  `CFe` function L806 — `function CFe(t)`
-  `CM` function L806 — `function CM()`
-  `DFe` function L806 — `function DFe(t)`
-  `E` function L806 — `function E(D)`
-  `Gle` function L806 — `function Gle(t)`
-  `IE` function L806 — `function IE(t)`
-  `IM` function L806 — `function IM(t,e)`
-  `Ile` function L806 — `function Ile(t)`
-  `JM` function L806 — `function JM(t)`
-  `Jle` function L806 — `function Jle(t,e)`
-  `Kle` function L806-810 — `function Kle(t)`
-  `LFe` function L806 — `function LFe(t,e,r)`
-  `MM` function L806 — `function MM()`
-  `Mle` function L806 — `function Mle(t)`
-  `Qle` function L806 — `function Qle(t)`
-  `RE` function L806 — `function RE(t)`
-  `RFe` function L806 — `function RFe(t,e)`
-  `SFe` function L806 — `function SFe(t)`
-  `TFe` function L806 — `function TFe(t,e,r,n)`
-  `UM` function L806 — `function UM(t)`
-  `Vle` function L806 — `function Vle(t)`
-  `Wle` function L806 — `function Wle(t)`
-  `Xle` function L806 — `function Xle(t)`
-  `Yle` function L806 — `function Yle(t)`
-  `Zle` function L806 — `function Zle(t)`
-  `_Fe` function L806 — `function _Fe(t,e)`
-  `a` function L806 — `function a(h)`
-  `aI` function L806 — `function aI(t)`
-  `b` function L806 — `function b(D,k)`
-  `bp` function L806 — `function bp(t)`
-  `cFe` function L806 — `function cFe(t)`
-  `dFe` function L806 — `function dFe(t)`
-  `e` function L806 — `function e(r)`
-  `eI` function L806 — `function eI(t)`
-  `ece` function L806 — `function ece(t)`
-  `fFe` function L806 — `function fFe(t)`
-  `g` class L806 — `-`
-  `hFe` function L806 — `function hFe(t)`
-  `i` function L806 — `function i(h)`
-  `iI` function L806 — `function iI(t,e)`
-  `l` function L806 — `function l(h)`
-  `n` function L806 — `function n(h)`
-  `nI` function L806 — `function nI(t,e,r)`
-  `p` class L806 — `-`
-  `qle` function L806 — `function qle(t)`
-  `r` function L806 — `function r(i,a,s)`
-  `rI` function L806 — `function rI(t,e)`
-  `s` function L806 — `function s(h)`
-  `tI` function L806 — `function tI(t,e)`
-  `u` function L806 — `function u(h)`
-  `uFe` function L806 — `function uFe(t)`
-  `v` function L806 — `function v(D,k,L)`
-  `vFe` function L806 — `function vFe(t)`
-  `w` function L806 — `function w(D,k)`
-  `wFe` function L806 — `function wFe(t)`
-  `x` function L806 — `function x(D)`
-  `xi` function L806 — `function xi(t)`
-  `zle` function L806 — `function zle(t,e,r=0)`
-  `FE` function L814 — `function FE(t,e)`
-  `ace` function L814 — `function ace(t,e)`
-  `ds` function L814 — `function ds(t)`
-  `fs` function L814 — `function fs(t)`
-  `ice` function L814 — `function ice(t)`
-  `rce` function L814 — `function rce(t,e,r,n)`
-  `ui` function L814 — `function ui(t,e,r,n,i,a,s,l,u)`
-  `Ace` function L815 — `function Ace(t)`
-  `Cce` function L815 — `function Cce(t)`
-  `Dce` function L815 — `function Dce(t)`
-  `Hu` function L815 — `function Hu(t)`
-  `IFe` function L815 — `function IFe()`
-  `Ice` function L815 — `function Ice(t)`
-  `Lce` function L815 — `function Lce(t)`
-  `Mce` function L815 — `function Mce(t)`
-  `Nce` function L815 — `function Nce(t)`
-  `Oce` function L815 — `function Oce(t)`
-  `Rce` function L815 — `function Rce(t)`
-  `Sce` function L815 — `function Sce(t)`
-  `_ce` function L815 — `function _ce(t)`
-  `KE` function L816 — `function KE(t=ps)`
-  `XE` function L816 — `function XE(t=ps)`
-  `ZE` function L816 — `function ZE(t=ps)`
-  `e6` function L816 — `function e6(t=ps)`
-  `i6` function L816 — `function i6(t=ps)`
-  `r6` function L816 — `function r6(t=ps)`
-  `uo` function L816 — `function uo(t,e)`
-  `$c` function L818 — `function $c(t,e)`
-  `Qce` function L818 — `function Qce(t)`
-  `jce` function L818 — `function jce(t,e,r)`
-  `u$e` function L818 — `function u$e(t,e)`
-  `zI` function L818 — `function zI()`
-  `se` function L882 — `function se(W)`
-  `ue` function L882 — `function ue()`
-  `A` function L887 — `function A(S)`
-  `C` function L887 — `function C(S,_,I,D)`
-  `E` function L887 — `function E(S,_,I,D)`
-  `Nue` function L887 — `function Nue(t,e,r)`
-  `T` function L887 — `function T(S,_)`
-  `b` function L887 — `function b(S,_,I,D,k,L,R)`
-  `k` function L887 — `function k()`
-  `m` function L887 — `function m(g)`
-  `v` function L887 — `function v(S,_)`
-  `w` function L887 — `function w(S,_,I,D,k,L,R,O)`
-  `x` function L887 — `function x(S,_,I)`
-  `ut` function L1168 — `function ut()`
-  `xt` function L1168 — `function xt(Ce)`
-  `AGe` function L1173 — `function AGe()`
-  `CGe` function L1173 — `function CGe(t)`
-  `EGe` function L1173 — `function EGe(t,e)`
-  `SGe` function L1173 — `function SGe(t)`
-  `TGe` function L1173 — `function TGe(t)`
-  `V` function L1173 — `function V()`
-  `Xu` function L1173 — `function Xu(t)`
-  `a` function L1173 — `function a(S)`
-  `bGe` function L1173 — `function bGe(t)`
-  `dO` function L1173 — `function dO(t)`
-  `dhe` function L1173 — `function dhe(t)`
-  `fO` function L1173 — `function fO(t)`
-  `gGe` function L1173 — `function gGe(t)`
-  `i` function L1173 — `function i(S)`
-  `kGe` function L1173 — `function kGe(t,e,r,n,i)`
-  `mGe` function L1173 — `function mGe(t)`
-  `phe` function L1173 — `function phe(t)`
-  `s` function L1173 — `function s(S)`
-  `vGe` function L1173 — `function vGe(t)`
-  `wGe` function L1173 — `function wGe(t)`
-  `xGe` function L1173 — `function xGe(t)`
-  `yGe` function L1173 — `function yGe(t)`
-  `Se` function L1174 — `function Se()`
-  `Z` function L1174 — `function Z(re)`
-  `$Ge` function L1179 — `function $Ge(t,e)`
-  `$he` function L1179 — `function $he()`
-  `BGe` function L1179 — `function BGe(t)`
-  `Dhe` function L1179 — `function Dhe(t,e,r,n)`
-  `FGe` function L1179 — `function FGe(t)`
-  `Fhe` function L1179 — `function Fhe()`
-  `GGe` function L1179 — `function GGe(t,e)`
-  `Ghe` function L1179 — `function Ghe(t,e)`
-  `HGe` function L1179 — `function HGe()`
-  `IGe` function L1179 — `function IGe(t)`
-  `Mhe` function L1179 — `function Mhe(t,e,r)`
-  `OGe` function L1179 — `function OGe(t)`
-  `PGe` function L1179 — `function PGe(t)`
-  `R` function L1179 — `function R()`
-  `S1` function L1179 — `function S1(t)`
-  `UGe` function L1179 — `function UGe()`
-  `Uhe` function L1179 — `function Uhe(t)`
-  `VGe` function L1179 — `function VGe(t,e)`
-  `Vhe` function L1179 — `function Vhe(t)`
-  `WGe` function L1179 — `function WGe()`
-  `h` function L1179 — `function h(v)`
-  `kO` function L1179 — `function kO(t)`
-  `l` function L1179 — `function l(v)`
-  `mO` function L1179 — `function mO(t)`
-  `u` function L1179 — `function u(v)`
-  `v6` function L1179 — `function v6(t)`
-  `vO` function L1179 — `function vO(t,e,r,n)`
-  `zGe` function L1179 — `function zGe(t)`
-  `zhe` function L1179 — `function zhe()`
-  `Yt` function L1180 — `function Yt(Dr)`
-  `bt` function L1180 — `function bt()`
-  `y` function L1180 — `function y(v)`
-  `de` function L1185 — `function de()`
-  `oe` function L1243 — `function oe()`
-  `re` function L1243 — `function re(Rt)`
-  `he` function L1248 — `function he()`
-  `Hc` function L1364 — `function Hc(t,e,r,n,i)`
-  `MVe` function L1364 — `function MVe(t,e,r)`
-  `OVe` function L1364 — `function OVe(t,e,r)`
-  `RVe` function L1364 — `function RVe(t,e,r,n,i,a,s)`
-  `T` function L1364 — `function T(F,P)`
-  `Ue` function L1364 — `function Ue(Tt)`
-  `_Ve` function L1364 — `function _Ve(t,e)`
-  `ct` function L1364 — `function ct()`
-  `e` function L1364 — `function e(a,s,l,u,h,f,d,p)`
-  `i` function L1364 — `function i(a,s)`
-  `l` function L1364 — `function l(h,f)`
-  `n` function L1364 — `function n(a,s,l,u,h,f,d,p)`
-  `r` function L1364 — `function r(i,a,s,l,u)`
-  `s` function L1364 — `function s(l)`
-  `t` function L1364 — `function t(a,s,l,u,h,f,d)`
-  `u` function L1364 — `function u(h,f)`
-  `Re` function L1369 — `function Re()`
-  `Oe` function L1527 — `function Oe()`
-  `ae` function L1527 — `function ae(xe)`
-  `B` function L1532 — `function B()`
-  `G6` function L1532 — `function G6(t,e,r)`
-  `ZO` function L1532 — `function ZO(t="",e=0,r="",n=$6)`
-  `lUe` function L1532 — `function lUe(t)`
-  `oUe` function L1532 — `function oUe(t)`
-  `ude` function L1532 — `function ude()`
-  `M` function L1746 — `function M()`
-  `O` function L1746 — `function O(K)`
-  `f` function L1751 — `function f()`
-  `B` function L1883 — `function B()`
-  `JUe` function L1883 — `function JUe(t)`
-  `M` function L1883 — `function M(X)`
-  `a` function L1883 — `function a(u)`
-  `e` function L1883 — `function e(i,a,s,l,u,h,f,d,p)`
-  `l` function L1883 — `function l(u)`
-  `n` function L1883 — `function n(i,a)`
-  `r` function L1883 — `function r(i,a,s,l,u)`
-  `s` function L1883 — `function s(u)`
-  `t` function L1883 — `function t(i,a,s,l,u,h,f,d)`
-  `a` function L1888 — `function a(u)`
-  `d` function L1888 — `function d()`
-  `dpe` function L1888 — `function dpe(t,e)`
-  `e` function L1888 — `function e(i,a,s,l,u,h,f,d,p)`
-  `l` function L1888 — `function l(u)`
-  `n` function L1888 — `function n(i,a)`
-  `r` function L1888 — `function r(i,a,s,l,u)`
-  `s` function L1888 — `function s(u)`
-  `t` function L1888 — `function t(i,a,s,l,u,h,f,d)`
-  `ie` function L1943 — `function ie()`
-  `j` function L1943 — `function j(ae)`
-  `Dpe` function L1948 — `function Dpe(t,e)`
-  `HHe` function L1948 — `function HHe(t)`
-  `If` function L1948 — `function If(t,e,r)`
-  `Mf` function L1948 — `function Mf(t,e)`
-  `OP` function L1948 — `function OP(t,e)`
-  `UHe` function L1948 — `function UHe(t)`
-  `WHe` function L1948 — `function WHe(t)`
-  `Wi` function L1948 — `function Wi(t)`
-  `X0e` function L1948 — `function X0e(t,e,r)`
-  `YHe` function L1948-1949 — `function YHe()`
-  `ZP` function L1948 — `function ZP(t,e)`
-  `_i` function L1948 — `function _i(t,e)`
-  `j0e` function L1948 — `function j0e(t)`
-  `qHe` function L1948 — `function qHe(t,e)`
-  `w` function L1948 — `function w()`
-  `XHe` function L1949-1950 — `function XHe()`
-  `mo` function L1950-1951 — `function mo(t,e)`
-  `$0e` function L1951 — `function $0e(t,e,r)`
-  `$1` function L1951 — `function $1(t,e,r,n,i)`
-  `$S` function L1951 — `function $S()`
-  `$Xe` function L1951 — `function $Xe(t,e)`
-  `$Ze` function L1951 — `function $Ze(t,e)`
-  `$ge` function L1951 — `function $ge(t,e)`
-  `$me` function L1951 — `function $me(t,e,r)`
-  `A` function L1951 — `function A()`
-  `AP` function L1951 — `function AP(t,e)`
-  `AXe` function L1951 — `function AXe(t)`
-  `Age` function L1951 — `function Age(t)`
-  `Aje` function L1951 — `function Aje(t,e)`
-  `BWe` function L1951 — `function BWe(t)`
-  `BZe` function L1951 — `function BZe(t,e,r,n,i)`
-  `C` function L1951 — `function C()`
-  `Cge` function L1951 — `function Cge(t,e,r)`
-  `Cje` function L1951 — `function Cje(t)`
-  `DS` function L1951 — `function DS(t,e,r)`
-  `DZe` function L1951 — `function DZe(t)`
-  `Dje` function L1951 — `function Dje(t,e,r)`
-  `Eje` function L1951 — `function Eje(t,e)`
-  `F1` function L1951 — `function F1(t,e)`
-  `FXe` function L1951 — `function FXe(t,e)`
-  `FZe` function L1951 — `function FZe(t,e,r)`
-  `Fge` function L1951 — `function Fge(t,e,r)`
-  `Fme` function L1951 — `function Fme(t,e,r)`
-  `GS` function L1951 — `function GS(t,e)`
-  `GWe` function L1951 — `function GWe(t)`
-  `Gb` function L1951 — `function Gb()`
-  `H0e` function L1951 — `function H0e(t,e)`
-  `HS` function L1951 — `function HS(t)`
-  `HXe` function L1951 — `function HXe(t)`
-  `HZe` function L1951 — `function HZe(t,e,r,n,i)`
-  `Hje` function L1951 — `function Hje(t,e,r)`
-  `IS` function L1951 — `function IS(t,e,r,n,i,a)`
-  `IXe` function L1951 — `function IXe(t,e)`
-  `IZe` function L1951 — `function IZe(t,e)`
-  `Ime` function L1951 — `function Ime(t)`
-  `JXe` function L1951 — `function JXe()`
-  `Jme` function L1951 — `function Jme(t,e,r)`
-  `KWe` function L1951 — `function KWe(t)`
-  `LZe` function L1951 — `function LZe(t)`
-  `Lb` function L1951 — `function Lb()`
-  `Lge` function L1951 — `function Lge(t,e)`
-  `MWe` function L1951 — `function MWe(t)`
-  `MZe` function L1951 — `function MZe(t)`
-  `Mge` function L1951 — `function Mge(t,e,r)`
-  `NP` function L1951 — `function NP(t,e,r,n,i)`
-  `NZe` function L1951 — `function NZe(t)`
-  `Nje` function L1951 — `function Nje(t,e,r)`
-  `OZe` function L1951 — `function OZe(t)`
-  `Oge` function L1951 — `function Oge(t)`
-  `PXe` function L1951 — `function PXe()`
-  `PZe` function L1951 — `function PZe(t)`
-  `Pje` function L1951 — `function Pje(t,e,r)`
-  `RP` function L1951 — `function RP(t,e,r,n,i)`
-  `RXe` function L1951 — `function RXe(t)`
-  `RZe` function L1951 — `function RZe(t)`
-  `Rb` function L1951 — `function Rb(t,e)`
-  `Rge` function L1951 — `function Rge(t,e,r)`
-  `SP` function L1951 — `function SP(t)`
-  `SWe` function L1951 — `function SWe(t)`
-  `SZe` function L1951 — `function SZe(t,e)`
-  `Sge` function L1951 — `function Sge(t,e)`
-  `T` function L1951 — `function T()`
-  `TB` function L1951 — `function TB(t,e,r)`
-  `TXe` function L1951 — `function TXe(t)`
-  `TZe` function L1951 — `function TZe(t,e,r)`
-  `UKe` function L1951 — `function UKe(t,e,r,n)`
-  `VWe` function L1951 — `function VWe(t)`
-  `VXe` function L1951 — `function VXe(t)`
-  `Vje` function L1951 — `function Vje(t,e,r,n)`
-  `WKe` function L1951 — `function WKe(t,e,r,n)`
-  `WWe` function L1951 — `function WWe(t)`
-  `WZe` function L1951 — `function WZe(t,e,r,n)`
-  `XXe` function L1951 — `function XXe(t,e)`
-  `Xje` function L1951 — `function Xje(t)`
-  `Y0e` function L1951 — `function Y0e(t)`
-  `ZYe` function L1951 — `function ZYe(t)`
-  `_Qe` function L1951 — `function _Qe(t,e,r)`
-  `_We` function L1951 — `function _We(t)`
-  `_ge` function L1951 — `function _ge(t,e,r)`
-  `a` function L1951 — `function a(f)`
-  `aXe` function L1951 — `function aXe(t)`
-  `aZe` function L1951 — `function aZe(t,e,r)`
-  `age` function L1951 — `function age(t)`
-  `ay` function L1951 — `function ay(t)`
-  `b` function L1951 — `function b(E)`
-  `b0e` function L1951 — `function b0e(t,e)`
-  `bXe` function L1951 — `function bXe()`
-  `bZe` function L1951 — `function bZe(t)`
-  `bje` function L1951 — `function bje(t,e)`
-  `cB` function L1951 — `function cB(t,e)`
-  `dB` function L1951 — `function dB(t)`
-  `dKe` function L1951 — `function dKe(t,e,r)`
-  `e` function L1951 — `function e(n,i,a)`
-  `eqe` function L1951 — `function eqe(t,e,r)`
-  `f` function L1951 — `function f(g,y,v)`
-  `f0e` function L1951 — `function f0e(t)`
-  `fB` function L1951 — `function fB(t,e,r,n)`
-  `fge` function L1951 — `function fge(t)`
-  `g` function L1951 — `function g(E)`
-  `g0e` function L1951 — `function g0e(t)`
-  `gB` function L1951 — `function gB(t)`
-  `gXe` function L1951 — `function gXe(t,e)`
-  `gZe` function L1951 — `function gZe(t)`
-  `hge` function L1951 — `function hge(t,e)`
-  `i` function L1951 — `function i(f,d)`
-  `iZe` function L1951 — `function iZe(t,e)`
-  `ije` function L1951 — `function ije(t)`
-  `kje` function L1951 — `function kje(t)`
-  `l` function L1951 — `function l(f)`
-  `lS` function L1951 — `function lS(t,e)`
-  `lge` function L1951 — `function lge(t)`
-  `lje` function L1951 — `function lje(t)`
-  `m` function L1951 — `function m(E)`
-  `mZe` function L1951 — `function mZe(t,e,r)`
-  `mje` function L1951 — `function mje(t)`
-  `n` function L1951 — `function n(i)`
-  `nge` function L1951 — `function nge(t)`
-  `nje` function L1951 — `function nje(t,e)`
-  `ny` function L1951 — `function ny(t)`
-  `oS` function L1951 — `function oS(t,e,r)`
-  `oZe` function L1951 — `function oZe(t,e,r,n)`
-  `oge` function L1951 — `function oge(t)`
-  `p` function L1951 — `function p(R,O)`
-  `pXe` function L1951 — `function pXe(t)`
-  `pZe` function L1951 — `function pZe(t,e,r)`
-  `po` function L1951 — `function po(t,e,r,n)`
-  `qKe` function L1951 — `function qKe(t,e,r,n)`
-  `qXe` function L1951 — `function qXe(t)`
-  `qYe` function L1951 — `function qYe(t,e)`
-  `qje` function L1951 — `function qje(t,e)`
-  `r` function L1951 — `function r(s)`
-  `rS` function L1951 — `function rS()`
-  `rge` function L1951 — `function rge(t)`
-  `ry` function L1951 — `function ry(t)`
-  `s` function L1951 — `function s(R)`
-  `sZe` function L1951 — `function sZe(t,e,r,n,i)`
-  `sge` function L1951 — `function sge(t)`
-  `sje` function L1951 — `function sje(t)`
-  `t` function L1951 — `function t()`
-  `tXe` function L1951 — `function tXe(t)`
-  `tge` function L1951 — `function tge(t)`
-  `tje` function L1951 — `function tje(t)`
-  `ty` function L1951 — `function ty(t)`
-  `u` function L1951 — `function u(R)`
-  `uje` function L1951 — `function uje(t,e)`
-  `v` function L1951 — `function v(E)`
-  `v0e` function L1951 — `function v0e(t)`
-  `vB` function L1951 — `function vB(t,e,r,n)`
-  `vWe` function L1951 — `function vWe(t,e)`
-  `vXe` function L1951 — `function vXe(t,e)`
-  `vZe` function L1951 — `function vZe(t,e,r,n,i,a)`
-  `w` function L1951 — `function w()`
-  `w0e` function L1951 — `function w0e(t,e)`
-  `wB` function L1951 — `function wB(t)`
-  `wZe` function L1951 — `function wZe(t,e,r)`
-  `x` function L1951 — `function x()`
-  `x0e` function L1951 — `function x0e(t,e,r,n,i)`
-  `xZe` function L1951 — `function xZe(t,e,r,n)`
-  `y` function L1951 — `function y(E)`
-  `y0e` function L1951 — `function y0e(t)`
-  `yWe` function L1951 — `function yWe(t)`
-  `yZe` function L1951 — `function yZe(t,e)`
-  `z0e` function L1951 — `function z0e(t,e,r)`
-  `zS` function L1951 — `function zS(t,e)`
-  `zje` function L1951 — `function zje(t,e)`
-  `D` function L1954 — `function D(K,X,te,J,se)`
-  `k` function L1954 — `function k(K,X)`
-  `r` function L1954 — `function r(n)`
-  `t` function L1954 — `function t(e)`
-  `JZe` function L2155 — `function JZe(t,e,r,n,i)`
-  `a` function L2155 — `function a(s)`
-  `h` function L2155 — `function h(d,p,m,g)`
-  `i` function L2155 — `function i()`
-  `l` function L2155 — `function l(u,h)`
-  `m` function L2155 — `function m(v)`
-  `n` function L2155 — `function n()`
-  `p` function L2155 — `function p(g,y,v)`
-  `r` function L2155 — `function r(n)`
-  `s` function L2155 — `function s(u,h,f)`
-  `t` function L2155 — `function t(e)`
-  `w` function L2155 — `function w()`
-  `Yge` function L2188 — `function Yge(t,e,r,n)`
-  `iJe` function L2188 — `function iJe(t,e)`
-  `nJe` function L2188 — `function nJe(t,e)`
-  `qge` function L2188 — `function qge(t,e,r,n,i)`
-  `rJe` function L2188 — `function rJe(t,e)`
-  `he` function L2244 — `function he()`
-  `le` function L2244 — `function le(ze)`
-  `E` function L2249 — `function E()`
-  `D` function L2331 — `function D(ie)`
-  `k` function L2331 — `function k()`
-  `$B` function L2336 — `function $B(t)`
-  `$Je` function L2336 — `function $Je(t)`
-  `A` function L2336 — `function A(O,M,B)`
-  `A1e` function L2336 — `function A1e()`
-  `BB` function L2336 — `function BB(t)`
-  `BJe` function L2336 — `function BJe(t)`
-  `C` function L2336 — `function C(O)`
-  `D` function L2336 — `function D({sourceLinks:O,targetLinks:M})`
-  `E` function L2336 — `function E(O,M,B)`
-  `E1e` function L2336 — `function E1e(t,e)`
-  `FB` function L2336 — `function FB(t,e)`
-  `FJe` function L2336 — `function FJe(t)`
-  `GB` function L2336 — `function GB(t)`
-  `GJe` function L2336 — `function GJe(t)`
-  `HB` function L2336 — `function HB()`
-  `HJe` function L2336 — `function HJe(t,e,r,n,i)`
-  `I` function L2336 — `function I(O,M,B,F)`
-  `JS` function L2336 — `function JS()`
-  `KS` function L2336 — `function KS(t,e)`
-  `L` function L2336 — `function L(O,M)`
-  `N1e` function L2336 — `function N1e(t)`
-  `PJe` function L2336 — `function PJe(t)`
-  `QS` function L2336 — `function QS()`
-  `R` function L2336 — `function R(O,M)`
-  `R1e` function L2336 — `function R1e(t)`
-  `S` function L2336 — `function S(O,M)`
-  `S1e` function L2336 — `function S1e({nodes:t})`
-  `T` function L2336 — `function T(O)`
-  `T1e` function L2336 — `function T1e(t,e)`
-  `UJe` function L2336 — `function UJe(t)`
-  `VJe` function L2336 — `function VJe(t)`
-  `WJe` function L2336 — `function WJe(t)`
-  `ZS` function L2336 — `function ZS(t)`
-  `_` function L2336 — `function _(O,M,B,F)`
-  `b` function L2336 — `function b({nodes:O})`
-  `cy` function L2336 — `function cy(t,e)`
-  `g` function L2336 — `function g()`
-  `hy` function L2336 — `function hy(t)`
-  `k` function L2336 — `function k(O)`
-  `k1e` function L2336 — `function k1e(t,e)`
-  `m4` function L2336 — `function m4(t,e)`
-  `p4` function L2336 — `function p4(t,e)`
-  `qB` function L2336 — `function qB()`
-  `qJe` function L2336 — `function qJe(t)`
-  `s` function L2336 — `function s()`
-  `uy` function L2336 — `function uy(t,e)`
-  `v` function L2336 — `function v({nodes:O})`
-  `w` function L2336 — `function w({nodes:O})`
-  `x` function L2336 — `function x({nodes:O})`
-  `y` function L2336 — `function y({nodes:O,links:M})`
-  `Iet` function L2363 — `function Iet(t,e,r,n)`
-  `Met` function L2363 — `function Met(t,e)`
-  `Net` function L2363 — `function Net(t,e,r,n)`
-  `Ret` function L2363 — `function Ret(t,e,r,n,i,a,s)`
-  `Q` function L2405 — `function Q(ce)`
-  `j` function L2405 — `function j()`
-  `Xet` function L2410 — `function Xet(t)`
-  `Yet` function L2410 — `function Yet(t)`
-  `b` function L2410 — `function b()`
-  `jet` function L2410 — `function jet(t)`
-  `Bye` function L2522 — `function Bye(t,e)`
-  `Iye` function L2522 — `function Iye(t,{minX:e,minY:r,maxX:n,maxY:i}={minX:0,minY:0,maxX:0,maxY:0})`
-  `Mye` function L2522 — `function Mye(t,e)`
-  `Oye` function L2522 — `function Oye(t)`
-  `eF` function L2522 — `function eF(t,e,r=0,n=0)`
-  `nC` function L2522 — `function nC(t,e)`
-  `wtt` function L2522 — `function wtt(t)`
-  `xtt` function L2522 — `function xtt(t,e)`
-  `Att` function L2525 — `function Att(t,e)`
-  `C` function L2525 — `function C()`
-  `Dtt` function L2525 — `function Dtt(t,e,r)`
-  `Hl` function L2525 — `function Hl(t,e,r,n)`
-  `Li` function L2525 — `function Li(t)`
-  `Ltt` function L2525 — `function Ltt(t,e,r,n)`
-  `Rtt` function L2525 — `function Rtt(t,e,r)`
-  `Zye` function L2525 — `function Zye(t,e)`
-  `_tt` function L2525 — `function _tt(t,e,r,n)`
-  `aF` function L2525 — `function aF(t,e,r,n)`
-  `dve` function L2525 — `function dve(t,e,r=!1)`
-  `gve` function L2525 — `function gve(t,e,r,n,i)`
-  `mve` function L2525 — `function mve(t,e,r)`
-  `oF` function L2525 — `function oF(t,e,r,n)`
-  `pve` function L2525 — `function pve(t,e,r)`
-  `rrt` function L2525 — `function rrt(t,e,r)`
-  `trt` function L2525 — `function trt(t,e,r)`
-  `I` function L2556 — `function I()`
-  `Pe` function L2556 — `function Pe(st,Ue)`
-  `a` function L2556 — `function a(s)`
-  `b` function L2556 — `function b(w,C)`
-  `d` function L2556 — `function d(p,m)`
-  `f` function L2556 — `function f(p,m,g,y)`
-  `h` function L2556 — `function h(d,p,m,g)`
-  `i` function L2556 — `function i()`
-  `l` function L2556 — `function l(u,h)`
-  `m` function L2556 — `function m()`
-  `n` function L2556 — `function n()`
-  `p` function L2556 — `function p(g,y,v)`
-  `r` function L2556 — `function r(n)`
-  `s` function L2556 — `function s(u,h,f)`
-  `u` function L2556 — `function u(g)`
-  `Art` function L2560 — `function Art(t,e)`
-  `Crt` function L2560 — `function Crt(t,e)`
-  `Drt` function L2560 — `function Drt(t,e)`
-  `Lrt` function L2560 — `function Lrt(t,e,r)`
-  `Nrt` function L2560 — `function Nrt(t,e,r,n,i,{spatialMaps:a,groupAlignments:s})`
-  `Rrt` function L2560 — `function Rrt(t)`
-  `Srt` function L2560 — `function Srt(t,e)`
-  `_rt` function L2560 — `function _rt(t,e)`
-  `m` function L2560 — `function m(g,y,v,x)`
-  `$f` function L2561 — `function $f(t,e)`
-  `A4` function L2561 — `function A4()`
-  `C4` function L2561 — `function C4(t,e,r)`
-  `Frt` function L2561 — `function Frt(t,e,r,n)`
-  `a2e` function L2561 — `function a2e()`
-  `al` function L2561 — `function al()`
-  `c2e` function L2561 — `function c2e(t,e)`
-  `d2e` function L2561 — `function d2e(t,e,r,n,i)`
-  `dC` function L2561 — `function dC(t,e,r,n,i,a,s,l)`
-  `f2e` function L2561 — `function f2e(t,e,r,n,i,a,s,l,u,h,f,d)`
-  `g2e` function L2561 — `function g2e(t,e)`
-  `gC` function L2561 — `function gC(t,e,r,n,i,a,s,l,u)`
-  `h2e` function L2561 — `function h2e(t)`
-  `hC` function L2561 — `function hC(t)`
-  `i2e` function L2561 — `function i2e()`
-  `l2e` function L2561 — `function l2e(t)`
-  `m2e` function L2561 — `function m2e(t,e,r,n)`
-  `mC` function L2561 — `function mC(t)`
-  `my` function L2561 — `function my(t,e)`
-  `o2e` function L2561 — `function o2e(t)`
-  `p2e` function L2561 — `function p2e(t)`
-  `pC` function L2561 — `function pC(t,e)`
-  `r2e` function L2561 — `function r2e(t)`
-  `rh` function L2561 — `function rh()`
-  `s2e` function L2561 — `function s2e(t)`
-  `t2e` function L2561 — `function t2e(t,e,r)`
-  `u2e` function L2561 — `function u2e(t,e)`
-  `vo` function L2561 — `function vo(t)`
-  `xF` function L2561 — `function xF(t)`
-  `y2e` function L2561 — `function y2e(t,e,r,n)`
-  `yC` function L2561 — `function yC(t,e)`
-  `yy` function L2561 — `function yy(t)`
-  `zf` function L2561 — `function zf(t,e,r)`
-  `w2e` function L2562 — `function w2e(t)`
-  `C2e` function L2563 — `function C2e(t)`
-  `T2e` function L2563 — `function T2e(t)`
-  `bF` function L2563 — `function bF(t)`
-  `int` function L2563 — `function int(t,e)`
-  `S2e` function L2569 — `function S2e(t,e)`
-  `fnt` function L2569 — `function fnt(t,e,r,n)`
-  `hnt` function L2569 — `function hnt(t={})`

### scripts

> *Semantic summary to be generated by AI agent.*

#### scripts/build-sidecar.mjs

-  `detectTarget` function L21-64 — `function detectTarget()`
-  `main` function L66-103 — `function main()`

#### scripts/extract-srd-fixtures.py

- pub `connect` function L30-34 — `def connect()`
- pub `entity_to_json` function L37-48 — `def entity_to_json(row: dict) -> dict` — Mimic the Rust entity_to_json: merge id/name/source into parsed data blob.
- pub `dict_factory` function L51-52 — `def dict_factory(cursor, row)`
- pub `extract_classes` function L57-61 — `def extract_classes(conn) -> list[dict]`
- pub `extract_subclasses` function L64-74 — `def extract_subclasses(conn) -> list[dict]`
- pub `extract_class_features` function L77-92 — `def extract_class_features(conn) -> list[dict]`
- pub `extract_subclass_features` function L95-113 — `def extract_subclass_features(conn) -> list[dict]`
- pub `extract_backgrounds` function L116-120 — `def extract_backgrounds(conn) -> list[dict]`
- pub `extract_races` function L123-127 — `def extract_races(conn) -> list[dict]`
- pub `extract_items` function L130-185 — `def extract_items(conn) -> list[dict]` — Extract a representative sample of SRD items: weapons, armor, and a few magic items.
- pub `extract_spells` function L188-232 — `def extract_spells(conn) -> list[dict]` — Extract a representative sample of SRD spells across levels and schools.
- pub `extract_monsters` function L235-261 — `def extract_monsters(conn) -> list[dict]` — Extract a representative sample of SRD monsters across CRs.
- pub `build_test_characters` function L266-434 — `def build_test_characters() -> list[dict]` — Build synthetic test character data matching CharacterResponse format.
- pub `build_homebrew_fixtures` function L437-485 — `def build_homebrew_fixtures() -> list[dict]` — Build synthetic homebrew item fixtures in 5etools format.
- pub `write_json` function L490-494 — `def write_json(path: Path, data: Any)`
- pub `write_ts_fixture` function L497-549 — `def write_ts_fixture(path: Path, var_name: str, data: list[dict])` — Write a TypeScript fixture file exporting the data array and named exports.
- pub `write_ts_index` function L552-560 — `def write_ts_index(path: Path, modules: list[str])` — Write an index.ts that re-exports all fixture modules.
- pub `build_rust_fixture` function L565-584 — `def build_rust_fixture(rows: list[dict], table_columns: list[str]) -> list[dict]` — Build Rust fixtures that include both table columns and the raw data blob.
- pub `main` function L589-680 — `def main()`

