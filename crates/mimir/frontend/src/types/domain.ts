// Supplementary domain types that extend the base API types
// Main types (Campaign, Module, Document) are defined in api.ts

// Re-export the API types for backwards compatibility
export type { Campaign, Module, Document } from './api'
