name: Release

on:
  push:
    tags:
      - 'v*.*.*'

env:
  CARGO_TERM_COLOR: always

jobs:
  build-desktop:
    name: Build Desktop Applications
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: 'macos-latest'
            target: 'aarch64-apple-darwin'
            args: '--target aarch64-apple-darwin'
          - platform: 'macos-latest'
            target: 'x86_64-apple-darwin'
            args: '--target x86_64-apple-darwin'
          - platform: 'ubuntu-22.04'
            target: 'x86_64-unknown-linux-gnu'
            args: ''
          - platform: 'windows-latest'
            target: 'x86_64-pc-windows-msvc'
            args: ''

    runs-on: ${{ matrix.platform }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: lts/*

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Setup Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: '. -> target'

      - name: Install dependencies (ubuntu only)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

      - name: Install frontend dependencies
        working-directory: ./crates/mimir/frontend
        run: npm ci

      - name: Build mimir-mcp sidecar (unix)
        if: matrix.platform != 'windows-latest'
        env:
          TAURI_ENV_TARGET_TRIPLE: ${{ matrix.target }}
        run: bash scripts/build-sidecar.sh

      - name: Build mimir-mcp sidecar (windows)
        if: matrix.platform == 'windows-latest'
        shell: pwsh
        env:
          TAURI_ENV_TARGET_TRIPLE: ${{ matrix.target }}
        run: .\scripts\build-sidecar.ps1

      - name: Upload mimir-mcp binary (unix)
        if: matrix.platform != 'windows-latest'
        uses: actions/upload-artifact@v4
        with:
          name: mimir-mcp-${{ matrix.target }}
          path: crates/mimir/binaries/mimir-mcp-${{ matrix.target }}

      - name: Upload mimir-mcp binary (windows)
        if: matrix.platform == 'windows-latest'
        uses: actions/upload-artifact@v4
        with:
          name: mimir-mcp-${{ matrix.target }}
          path: crates/mimir/binaries/mimir-mcp-${{ matrix.target }}.exe

      - name: Build Tauri app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.RELEASE_TOKEN }}
          TAURI_ENV_TARGET_TRIPLE: ${{ matrix.target }}
        with:
          projectPath: ./crates/mimir
          tagName: app-v__VERSION__
          releaseName: 'Mimir v__VERSION__'
          releaseBody: 'See the assets to download this version and install.'
          releaseDraft: true
          prerelease: false
          args: ${{ matrix.args }}

  upload-mcp-binaries:
    name: Attach MCP binaries to release
    needs: build-desktop
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download all mimir-mcp artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: mimir-mcp-*
          path: mcp-binaries
          merge-multiple: false

      - name: Get version from tag
        id: version
        run: echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Upload MCP binaries to release
        env:
          GH_TOKEN: ${{ secrets.RELEASE_TOKEN }}
        run: |
          TAG="app-v${{ steps.version.outputs.version }}"
          echo "Uploading MCP binaries to release ${TAG}..."

          for dir in mcp-binaries/mimir-mcp-*/; do
            for file in "$dir"*; do
              filename=$(basename "$file")
              echo "Uploading $filename..."
              gh release upload "$TAG" "$file" --clobber
            done
          done
